---
phase: 260908-1dm-shrink-wallet-web-docker-image-from-2-66
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - Dockerfile
  - docker/entrypoint.sh
files_deleted: []
autonomous: true
requirements:
  - QUICK-1dm-DOCKER-SHRINK
estimate:
  tokens: 22500
  raw_tokens: 45000
  tasks: 3
  confidence: high
must_haves:
  truths:
    - "D-01: Runner ships Next standalone + src/generated + Prisma migrate closure only (not builder full node_modules); fallback npm ci --omit=dev if primary fails"
    - "D-02: Primary build uses node:24-alpine same-libc for deps+builder+runner; fallback NODE_IMAGE=node:24-bookworm-slim if alpine/native fails"
    - "D-03: entrypoint still runs prisma migrate deploy then node server.js; Prisma CLI size tax measured and reported (migrate vs no-migrate / du / dive)"
    - "Final wallet-web image size is substantially below baseline ~2.66GB (target: drop the ~1GB full-modules layer and ~1GB ownership-duplication layer)"
    - "Ownership of /app content uses COPY --chown; no post-COPY recursive ownership RUN that rewrites GB-scale layers"
  artifacts:
    - path: Dockerfile
      provides: "ARG NODE_IMAGE alpine-first; prisma-cli stage; standalone+overlay runner; COPY --chown hygiene; documented slim + omit=dev fallbacks"
    - path: docker/entrypoint.sh
      provides: "Unchanged migrate-then-server path (or measurement-only ARG skip for twin tag)"
  key_links:
    - from: prisma-cli stage npm install prisma@lock
      to: runner ./node_modules/.bin/prisma
      via: "COPY --chown merge overlay into standalone node_modules"
    - from: docker/entrypoint.sh migrate deploy
      to: /data/wallet.db volume
      via: "DATABASE_URL=file:/data/wallet.db then exec node server.js"
    - from: ARG NODE_IMAGE
      to: deps + builder + prisma-cli + runner FROM lines
      via: "Same libc for native better-sqlite3 + Prisma engines"
---

<objective>
D-01: Shrink wallet-web image by dropping builder full `node_modules` from runner; ship Next standalone + `src/generated` + dedicated Prisma CLI closure overlay (fallback: `npm ci --omit=dev`).
D-02: Try `node:24-alpine` with same-libc multi-stage; fallback `node:24-bookworm-slim`.
D-03: Keep migrate in entrypoint; measure and report Prisma CLI / migrate tooling size tax.

Purpose: cut ~2.66GB baseline (full modules COPY + ownership rewrite duplication) while keeping in-container migrate + better-sqlite3.
Output: rewritten Dockerfile, green docker smoke (migrate + HTTP), SUMMARY with final size + Prisma CLI tax numbers.
</objective>

<execution_context>
@/home/artem/Documents/wallet/.cursor/gsd-core/workflows/execute-plan.md
@/home/artem/Documents/wallet/.cursor/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/quick/260908-1dm-shrink-wallet-web-docker-image-from-2-66/260908-1dm-CONTEXT.md
@.planning/quick/260908-1dm-shrink-wallet-web-docker-image-from-2-66/260908-1dm-RESEARCH.md
@Dockerfile
@docker/entrypoint.sh
@next.config.ts
@package.json
@docker-compose.yml

# Baseline (RESEARCH): wallet-web:latest ≈ 2.66GB — runner layers ~1.05GB full node_modules + ~1.07GB recursive ownership RUN.
# Prefer dedicated prisma-cli install stage over cherry-pick (Prisma 7 needs effect/fast-check/pure-rand + @prisma/config peers).
# Discretion: ARG NODE_IMAGE; same libc build+runner on alpine; measure tax via twin tags + du + dive.
</context>

<tasks>

<task type="tracer">
  <name>Tracer: alpine standalone + prisma-cli overlay Dockerfile</name>
  <files>Dockerfile</files>
  <action>
    D-02: Rewrite `Dockerfile` so every stage (`dependencies`, `builder`, optional `prisma-cli`, `runner`) uses `ARG NODE_IMAGE=node:24-alpine` (default) and `FROM ${NODE_IMAGE}` — same libc for native better-sqlite3 and Prisma engines. Document in a short header comment that fallback is `--build-arg NODE_IMAGE=node:24-bookworm-slim`.

    D-02 alpine deps toolchain: replace apt install with `apk add --no-cache python3 make g++` (and openssl/ca-certificates where needed on runner). For slim fallback path, keep apt `python3 make g++` / openssl pattern behind the same ARG (use shell conditionals or duplicate documented blocks — prefer one ARG-driven file that works for both bases: detect apk vs apt, or document that slim rebuild uses the existing apt lines when NODE_IMAGE is bookworm-slim). Executor discretion: cleanest approach that keeps one Dockerfile.

    D-01 primary runner shape (per RESEARCH):
    - Keep deps: `npm ci` + `npm rebuild better-sqlite3`.
    - Keep builder: `prisma generate` + `npm run build` (standalone already on via `next.config.ts`).
    - Add `prisma-cli` stage: empty workdir, install production closure for exact `prisma` version from `package.json` / lock (`7.10.0`) with `--omit=dev` (dedicated install — do not hand-pick folders).
    - Runner: install TLS/certs for the base; `mkdir` + chown only `/data` (small dir OK); COPY with `--chown=node:node` for standalone, static, public, prisma/, prisma.config.ts, src/generated, entrypoint, and merge prisma-cli `node_modules` into `./node_modules` (overlay must merge, not wipe standalone NFT modules). Copy package.json only if needed for prisma/config resolution.
    - Remove the runner copy of the full builder `node_modules` tree.
    - Set ownership via `COPY --chown=node:node` on app artifacts; do not add a post-COPY recursive ownership RUN over `/app` (that duplicated ~1GB in baseline). `chmod +x` on entrypoint alone is fine.
    - Keep `USER node`, `EXPOSE 3000`, `ENTRYPOINT ["./docker/entrypoint.sh"]`.

    Do not widen `outputFileTracingIncludes` further for migrate CLI — overlay carries CLI weight (RESEARCH). Leave `next.config.ts` / `entrypoint.sh` unchanged in this task unless a one-line path fix is required for `.bin/prisma`.

    Build: `docker build -t wallet-web:migrate .` (or tag `wallet-web:latest` after success). If alpine native/build fails, switch NODE_IMAGE to bookworm-slim and retry before declaring primary failed (still D-02).
  </action>
  <verify>
    <automated>docker build -t wallet-web:migrate . 2>&1 | tee /tmp/wallet-web-build.log | tail -n 40; docker image inspect wallet-web:migrate --format '{{.Size}}' | awk '{ if ($1+0 >= 2500000000) { print "FAIL size still >=2.5GB raw:", $1; exit 1 } else { print "OK raw bytes:", $1; exit 0 } }'</automated>
  </verify>
  <done>
    D-01/D-02: Image builds with alpine-first ARG (or slim fallback after alpine fail); runner uses standalone + prisma-cli overlay; ownership via COPY --chown; raw image size well under 2.5GB uncompressed inspect bytes (vs ~2.66GB baseline).
  </done>
</task>

<task type="auto">
  <name>Smoke migrate+HTTP; apply D-01/D-02 fallbacks if needed</name>
  <files>Dockerfile, docker/entrypoint.sh</files>
  <action>
    D-03: Keep `docker/entrypoint.sh` calling `./node_modules/.bin/prisma migrate deploy` then `exec node server.js` for the shipped default image.

    Smoke against a fresh volume dir (do not corrupt host `./data` without backup): e.g. `mkdir -p /tmp/wallet-web-smoke-data && docker run --rm -d --name wallet-web-smoke -p 127.0.0.1:3001:3000 -e DATABASE_URL=file:/data/wallet.db -v /tmp/wallet-web-smoke-data:/data wallet-web:migrate`, wait for logs showing migrate success + server start, then `curl -sf http://127.0.0.1:3001/api/health` (compose healthcheck path). Stop/remove container after.

    D-01 fallback: if migrate missing-module / app won't start / native binding broken with overlay — switch runner to `npm ci --omit=dev` (+ rebuild better-sqlite3) after copying package.json + lock + prisma sources, still using COPY --chown and still without builder-full modules. Re-smoke until green.

    D-02 fallback: if alpine still fails after overlay or omit=dev attempt — set default or build-arg to `node:24-bookworm-slim` and rebuild/smoke. Document which path shipped in SUMMARY.

    Optional measurement hook only: ARG or target that omits prisma-cli COPY for twin tag `wallet-web:no-migrate` (measurement image; not compose default). If added, measurement entrypoint may skip migrate — do not ship that as default ENTRYPOINT behavior.
  </action>
  <verify>
    <automated>docker rm -f wallet-web-smoke 2>/dev/null; mkdir -p /tmp/wallet-web-smoke-data && docker run --rm -d --name wallet-web-smoke -p 127.0.0.1:3001:3000 -e DATABASE_URL=file:/data/wallet.db -v /tmp/wallet-web-smoke-data:/data wallet-web:migrate && for i in $(seq 1 40); do curl -sf http://127.0.0.1:3001/api/health && break; sleep 2; done && curl -sf http://127.0.0.1:3001/api/health && docker logs wallet-web-smoke 2>&1 | grep -q 'prisma migrate deploy' && docker rm -f wallet-web-smoke</automated>
  </verify>
  <done>
    D-03: Container runs migrate then serves `/api/health`. D-01/D-02: primary or documented fallback path is the tagged `wallet-web:migrate` image that passes smoke.
  </done>
</task>

<task type="auto">
  <name>Measure Prisma CLI size tax + record vs 2.66GB baseline</name>
  <files>Dockerfile</files>
  <action>
    D-03: Produce measurable Prisma CLI / migrate tooling tax and record numbers in SUMMARY (required):

    1. Twin tags: build `wallet-web:migrate` (ship path) and `wallet-web:no-migrate` (same Dockerfile minus prisma-cli overlay / migrate — measurement only). Compare `docker images wallet-web --format '{{.Repository}}:{{.Tag}} {{.Size}}'`.
    2. In-container `du -sh` on migrate image for prisma CLI closure paths under `/app/node_modules` (at least `prisma`, `@prisma`, and major peers like `effect` if present).
    3. Run `dive wallet-web:migrate` (or `CI=true dive --json` if non-interactive) and note the layer(s) / largest paths attributable to the CLI overlay.

    Optional: print `du -sh` from prisma-cli stage during build for pure-closure size before merge.

    Also record final ship image size vs baseline ~2.66GB and confirm `docker history` no longer shows ~1GB recursive ownership rewrite + ~1GB full-modules copy as the two dominant runner layers.

    Do not change app runtime behavior in this task beyond measurement ARG/target already introduced. Tag the intended ship image as `wallet-web:latest` when measurements complete if compose expects that name.
  </action>
  <verify>
    <automated>docker images wallet-web --format '{{.Repository}}:{{.Tag}} {{.Size}}' | tee /tmp/wallet-web-sizes.txt; test -s /tmp/wallet-web-sizes.txt; docker run --rm --entrypoint du wallet-web:migrate -sh /app/node_modules/prisma /app/node_modules/@prisma 2>&1 | tee /tmp/wallet-web-prisma-du.txt; test -s /tmp/wallet-web-prisma-du.txt; command -v dive >/dev/null</automated>
  </verify>
  <done>
    D-03: SUMMARY can quote (a) migrate vs no-migrate image sizes, (b) in-image `du` for prisma/@prisma, (c) dive layer note, (d) final size vs ~2.66GB baseline. Ship default still includes migrate.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Build host → image layers | Untrusted only via base images + existing lockfile; no new npm packages |
| Container → `/data` volume | Entrypoint migrate writes SQLite on mounted volume |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-1dm-01 | Tampering | Runner filesystem | medium | mitigate | Drop builder/dev trees from runner; smaller attack surface than full node_modules |
| T-1dm-02 | Elevation of Privilege | USER in runner | medium | mitigate | Keep `USER node`; set ownership via COPY --chown; only `/data` mkdir chown |
| T-1dm-03 | Information Disclosure | Build logs / twin tags | low | accept | no-migrate tag is local measurement only; not compose default |
| T-1dm-SC | Tampering | npm/base images | low | accept | No new registry packages; pin prisma@7.10.0 from existing lock; base tag node:24-* |
</threat_model>

<verification>
- `docker build` ship image under substantially less than 2.66GB
- Smoke: migrate logs + `/api/health` on ephemeral volume
- Prisma CLI tax: twin-tag sizes + `du` + dive noted in SUMMARY
- Entrypoint default still migrate-then-server
</verification>

<success_criteria>
- Image shrinks by removing full builder modules copy and ownership-duplication layer
- Alpine tried first (same libc); slim / omit=dev fallbacks documented if used
- Migrate remains in shipped entrypoint; CLI size tax quantified
- `/api/health` green after container start
</success_criteria>

<output>
Create `.planning/quick/260908-1dm-shrink-wallet-web-docker-image-from-2-66/260908-1dm-SUMMARY.md` when done (orchestrator may own docs commit).
</output>

## Source Audit

| SOURCE | ID | Item | Plan | Status |
|--------|-----|------|------|--------|
| GOAL | — | Shrink wallet-web Docker image from ~2.66GB; maximize reduction | 01 | COVERED |
| REQ | QUICK-1dm-DOCKER-SHRINK | Same as goal (quick task) | 01 | COVERED |
| RESEARCH | — | Drop full node_modules COPY; eliminate post-COPY recursive ownership RUN | 01 T1 | COVERED |
| RESEARCH | — | prisma-cli dedicated install stage overlay | 01 T1 | COVERED |
| RESEARCH | — | Same-libc alpine build+runner; ARG slim fallback | 01 T1–T2 | COVERED |
| RESEARCH | — | Measure Prisma CLI tax (twin/du/dive) | 01 T3 | COVERED |
| RESEARCH | out-of-scope | Simplify prisma.config.ts; shadcn NFT exclude | — | excluded |
| CONTEXT | D-01 | Minimal runtime primary; npm ci --omit=dev fallback | 01 T1–T2 | COVERED |
| CONTEXT | D-02 | alpine primary; bookworm-slim fallback | 01 T1–T2 | COVERED |
| CONTEXT | D-03 | Migrate stays in-image; measure CLI size tax | 01 T2–T3 | COVERED |
