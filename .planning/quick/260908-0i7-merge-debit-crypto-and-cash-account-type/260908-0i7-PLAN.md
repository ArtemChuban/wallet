---
phase: 260908-0i7-merge-debit-crypto-and-cash-account-type
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - prisma/schema.prisma
  - prisma/seed.ts
  - src/lib/account-type.ts
  - src/lib/validations/account.ts
  - src/lib/validations/account.test.ts
  - src/app/accounts/actions.test.ts
  - src/components/accounts/AccountFormDialog.tsx
  - src/components/accounts/AccountList.tsx
  - src/components/accounts/SetBalanceDialog.tsx
  - src/lib/net-worth.ts
  - src/lib/net-worth.test.ts
  - src/lib/historical-series.ts
  - src/lib/historical-series.test.ts
  - src/lib/iniso.test.ts
autonomous: true
requirements:
  - QUICK-0i7-ASSET
estimate:
  tokens: 20000
  raw_tokens: 40000
  tasks: 3
  confidence: high
must_haves:
  truths:
    - "Create account accepts type ASSET (any allowed currency) and FIAT_CREDIT; create rejects legacy debit/crypto/cash type strings"
    - "Create/edit form offers only Asset + Credit; default create type is ASSET; currency picker unchanged (no type↔currency lock)"
    - "Legacy FIAT_DEBIT / CRYPTO / CASH rows still load; UI labels them as Актив; NW/charts still treat non-credit as assets"
    - "Prisma AccountType keeps legacy members plus ASSET; no forced row rewrite this task"
  artifacts:
    - path: prisma/schema.prisma
      provides: "AccountType enum includes ASSET while retaining legacy members"
    - path: src/lib/account-type.ts
      provides: "isCreditType / isAssetType / accountTypeLabel shared helpers"
    - path: src/lib/validations/account.ts
      provides: "createAccountSchema write enum ASSET | FIAT_CREDIT only"
    - path: src/components/accounts/AccountFormDialog.tsx
      provides: "TYPE_OPTIONS = Asset + Credit only; default ASSET"
  key_links:
    - from: AccountFormDialog TYPE_OPTIONS
      to: createAccountSchema / createAccount
      via: "FormData type field; Zod write gate blocks legacy types"
    - from: Account.type (DB, possibly legacy)
      to: AccountList / SetBalanceDialog / net-worth
      via: "isAssetType / isCreditType / accountTypeLabel normalize display + branching"
    - from: prisma seed non-credit rows
      to: fresh DB
      via: "seed writes ASSET so new envs exercise write path"
---

<objective>
Merge `FIAT_DEBIT` / `CRYPTO` / `CASH` into canonical `ASSET` (D-01); keep `FIAT_CREDIT` separate. Soft-compat: writes only `ASSET`|`FIAT_CREDIT`, reads still accept legacy enum members, no hard-drop and no forced row UPDATE (D-02). Single Asset UI + currency picker (D-03); any allowed currency on ASSET (D-04).

Purpose: one asset account type; difference is currency only.
Output: schema+helpers+Zod write gate, Asset/Credit UI, soft-read NW/labels, seed+tests green.
</objective>

<execution_context>
@/home/artem/Documents/wallet/.cursor/gsd-core/workflows/execute-plan.md
@/home/artem/Documents/wallet/.cursor/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/quick/260908-0i7-merge-debit-crypto-and-cash-account-type/260908-0i7-CONTEXT.md
@.planning/quick/260908-0i7-merge-debit-crypto-and-cash-account-type/260908-0i7-RESEARCH.md
@prisma/schema.prisma
@src/lib/validations/account.ts
@src/app/accounts/actions.ts
@src/components/accounts/AccountFormDialog.tsx
@src/lib/net-worth.ts

# Live scope (planning 2026-09-08): authorized touchpoints from codegraph/Grep — not STATE.md history.
# Discretion (RESEARCH): helpers in src/lib/account-type.ts; RU labels Актив / Кредитный; leave DB rows as-is; seed → ASSET.
</context>

<tasks>

<task type="tracer" tdd="true">
  <name>Tracer: ASSET write path (schema + Zod + create + form options)</name>
  <files>prisma/schema.prisma, src/lib/account-type.ts, src/lib/validations/account.ts, src/lib/validations/account.test.ts, src/app/accounts/actions.test.ts, src/components/accounts/AccountFormDialog.tsx</files>
  <behavior>
    - createAccountSchema accepts { type: "ASSET", currencyCode, name } without creditLimitMajor
    - createAccountSchema accepts FIAT_CREDIT only with positive creditLimitMajor (existing rules unchanged)
    - createAccountSchema rejects type strings FIAT_DEBIT, CRYPTO, CASH on create (D-02 writes)
    - createAccountSchema rejects creditLimitMajor when type is ASSET
    - createAccount persists type ASSET with creditLimitMinor null (actions.test)
    - AccountFormDialog create default type is ASSET; TYPE_OPTIONS values are exactly ASSET and FIAT_CREDIT (D-03)
  </behavior>
  <action>
    Per D-01/D-02: add `ASSET` to Prisma `AccountType` enum; keep existing members `FIAT_DEBIT`, `FIAT_CREDIT`, `CRYPTO`, `CASH`. Do not remove enum members. Do not run SQL that rewrites existing Account.type values. Run `npx prisma generate` (SQLite TEXT — migration SQL may be empty/no-op; follow repo migrate habit if a migration file is required for history).

    Add `src/lib/account-type.ts` exporting: `isCreditType(t)` → true only for FIAT_CREDIT; `isAssetType(t)` → true for ASSET and the three legacy non-credit members; `accountTypeLabel(t)` → "Кредитный" if credit else "Актив".

    In `account.ts`, replace create write enum with `z.enum(["ASSET", "FIAT_CREDIT"])` (D-02). Keep creditLimitMajor superRefine keyed on FIAT_CREDIT vs non-credit. Do not accept legacy types on create.

    Update `AccountFormDialog.tsx`: TYPE_OPTIONS only `{ value: "ASSET", label: "Актив" }` and `{ value: "FIAT_CREDIT", label: "Кредитный" }` (D-03). Create-mode default state = ASSET. Edit read-only type display uses `accountTypeLabel` so legacy rows show Актив. Currency select stays unfiltered (D-04). Widen AccountRow type union to include ASSET plus legacy members for edit props.

    Rewrite create-path tests in `account.test.ts` / `actions.test.ts` from four-type create loops to ASSET + FIAT_CREDIT create; add explicit rejection cases for legacy create types. Prefer TDD: failing tests first, then implementation.
  </action>
  <verify>
    <automated>npx vitest run src/lib/validations/account.test.ts src/app/accounts/actions.test.ts</automated>
  </verify>
  <done>
    Prisma enum has ASSET + legacy members; Zod create accepts only ASSET|FIAT_CREDIT; form offers Asset+Credit with default ASSET; create tests green (D-01, D-02, D-03, D-04).
  </done>
</task>

<task type="auto" tdd="true">
  <name>Soft-read UI labels + seed ASSET</name>
  <files>src/components/accounts/AccountList.tsx, src/components/accounts/SetBalanceDialog.tsx, prisma/seed.ts</files>
  <behavior>
    - AccountList shows Актив for ASSET and for legacy non-credit types; Кредитный for FIAT_CREDIT
    - SetBalanceDialog treats non-credit (including ASSET and legacy) as balance path; FIAT_CREDIT as limit path via isCreditType
    - prisma/seed.ts non-credit accounts use type ASSET
  </behavior>
  <action>
    Per D-02 soft-read: widen AccountList / SetBalanceDialog account `type` unions to include `ASSET` and keep legacy members so existing rows type-check. Replace local TYPE_LABELS maps with `accountTypeLabel` / `isCreditType` from `src/lib/account-type.ts` (no per-component three-way asset string lists).

    Per RESEARCH seed guidance (D-02 write path for fresh envs): change non-credit seed accounts from FIAT_DEBIT/CASH/CRYPTO to ASSET. Leave FIAT_CREDIT seed unchanged. Do not add a data migration UPDATE for existing DBs.
  </action>
  <verify>
    <automated>npx vitest run src/lib/validations/account.test.ts src/app/accounts/actions.test.ts && node -e "const fs=require('fs'); const s=fs.readFileSync('prisma/seed.ts','utf8'); if(!/type:\s*[\"']ASSET[\"']/.test(s)) process.exit(1); if(/type:\s*[\"'](FIAT_DEBIT|CRYPTO|CASH)[\"']/.test(s)) process.exit(1);"</automated>
  </verify>
  <done>
    List/set-balance soft-read legacy as Asset; seed non-credit rows are ASSET (D-02, D-03).
  </done>
</task>

<task type="auto" tdd="true">
  <name>NW soft-read types + suite gate</name>
  <files>src/lib/net-worth.ts, src/lib/net-worth.test.ts, src/lib/historical-series.ts, src/lib/historical-series.test.ts, src/lib/iniso.test.ts</files>
  <behavior>
    - NetWorthAccountType includes ASSET and still includes legacy FIAT_DEBIT / CRYPTO / CASH / FIAT_CREDIT
    - compute paths treat ASSET like former non-credit (branch remains credit vs not-credit)
    - At least one fixture uses type ASSET; some fixtures remain on legacy types to prove soft-compat reads
  </behavior>
  <action>
    Per D-01/D-02: extend `NetWorthAccountType` with `"ASSET"` while retaining legacy union members. Prefer `isCreditType` for credit branching where touching those call sites; do not narrow NW inputs to ASSET-only (would break unmigrated rows).

    Update tests: add ≥1 ASSET case in net-worth (and historical-series if types flow through); keep some legacy-typed fixtures. Adjust iniso fixtures only if typings require ASSET awareness — prefer leaving legacy fixtures that still type-check.

    page.tsx / dashboard components that only compare `=== "FIAT_CREDIT"` need no logic change; only touch them if TypeScript fails after the union update (live: Dashboard* import NetWorthAccountType — usually inherits).
  </action>
  <verify>
    <automated>npm test</automated>
  </verify>
  <done>
    Full Vitest suite green; NW accepts ASSET + legacy soft-read; no hard-drop of enum members (D-01, D-02).
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Browser form → createAccount server action | Untrusted FormData `type` / `creditLimitMajor` |
| DB Account.type → UI/NW | Soft-read of legacy enum strings must not crash or mis-label credit |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-0i7-01 | Tampering | createAccountSchema | medium | mitigate | Write enum only ASSET\|FIAT_CREDIT; reject legacy type on create even if Prisma still stores them |
| T-0i7-02 | Tampering | creditLimit on ASSET | medium | mitigate | Zod superRefine + existing CHECK (non-FIAT_CREDIT ⇒ creditLimitMinor NULL) |
| T-0i7-03 | Elevation | name-update path | low | accept | Existing D-15 name-only update; type not mutable via update (unchanged) |
| T-0i7-SC | Tampering | npm installs | low | accept | No new packages this task |
</threat_model>

<verification>
- Vitest: account + actions create gate
- Seed: non-credit ASSET only
- Full `npm test`
- Manual spot-check optional: create Asset with USDT and RUB; open legacy CRYPTO row → label Актив
</verification>

<success_criteria>
- New accounts persist as ASSET or FIAT_CREDIT only
- UI shows two type choices; currency unconstrained for ASSET
- Legacy rows readable and labeled Актив
- Prisma still has legacy enum members; no forced UPDATE migration
</success_criteria>

<output>
Create `.planning/quick/260908-0i7-merge-debit-crypto-and-cash-account-type/260908-0i7-SUMMARY.md` when done (orchestrator may own docs commit).
</output>

## Source Audit

| SOURCE | ID | Item | Plan | Status |
|--------|-----|------|------|--------|
| GOAL | — | Merge debit/crypto/cash → one asset type; credit separate; soft migrate | 01 | COVERED |
| REQ | QUICK-0i7-ASSET | Same as goal (quick task) | 01 | COVERED |
| RESEARCH | — | Prisma keep members + Zod write gate + helpers + UI + seed + NW soft-read | 01 | COVERED |
| RESEARCH | out-of-scope | Hard-drop / forced UPDATE / credit grace | — | excluded |
| CONTEXT | D-01 | Canonical name ASSET; FIAT_CREDIT unchanged | 01 T1 | COVERED |
| CONTEXT | D-02 | Soft-compat writes/reads; no hard-drop; rows may stay | 01 T1–T3 | COVERED |
| CONTEXT | D-03 | Single Asset UI + currency | 01 T1–T2 | COVERED |
| CONTEXT | D-04 | Any allowed currency; no type↔currency lock | 01 T1 | COVERED |
