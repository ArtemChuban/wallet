---
phase: 29-kapital-overlay-saviso
plan: 01
type: execute
wave: 0
depends_on: []
files_modified:
  - src/lib/saviso.test.ts
  - src/lib/nw-forecast.test.ts
  - src/components/dashboard/nw-forecast-ui.test.ts
autonomous: true
requirements:
  - INT-02
  - INT-03
  - SAVISO-01
  - SAVISO-02
estimate:
  tokens: 25000
  raw_tokens: 50000
  tasks: 3
  confidence: high
must_haves:
  truths:
    - "SAVISO-01/SAVISO-02 lock: forecast sources never call balance snapshot mutators, and a SAVINGS historical series equals snapshot minors with an unused interest fixture (D-12)"
    - "D-11 red expects: grace samples subtract the primary payment; event plannedAmountMinor stays the positive payment; today fact equality is not required when grace events exist"
    - "INT-03 boundary lock: a missing FX rate drops the interest slot and lists the currency code; a rate as of today includes the slot; a rate dated after today does not (LOCF as of today)"
    - "INT-03 precision lock: FX conversion stays on convertOtherMinorToPrimaryMinor; this phase does not add a second rounding mode; interest truncate-toward-0 stays the Phase 28 helper"
    - "UI file-scan locks «Накопительный», «Ожидаемое начисление», «Ожидаемый платёж», plus and minus prefixes, shell listInterestSlotsInRange, and absence of the retired grace subtitle (D-01, D-02, D-04, D-05, D-08, D-09, D-10)"
  artifacts:
    - path: src/lib/saviso.test.ts
      provides: SAVISO import wall, never-call scan, SAVINGS golden series
    - path: src/lib/nw-forecast.test.ts
      provides: signed grace totals and interest FX boundary expects
    - path: src/components/dashboard/nw-forecast-ui.test.ts
      provides: tooltip copy, prefix, and shell membership file-scan
  key_links:
    - from: src/lib/saviso.test.ts
      to: buildNetWorthSeries
      via: SAVINGS account golden total equals snapshot minor
      pattern: totalPrimaryMinor
    - from: src/lib/nw-forecast.test.ts
      to: forecastDeltaMinor grace arm
      via: signed primary totals that fail until Plan 29-02
      pattern: 950_000n
    - from: src/components/dashboard/nw-forecast-ui.test.ts
      to: DashboardChartsShell
      via: source expect for listInterestSlotsInRange
      pattern: listInterestSlotsInRange
---

<objective>
Wave 0 Vitest contracts for INT-02, INT-03, SAVISO-01, SAVISO-02, and D-01 through D-13 display/line locks. No production edits.

Purpose: Plan 29-02 turns the red forecast and tooltip scans green without rewriting expects. Isolation tests lock behavior that is already true.
Output: src/lib/saviso.test.ts (new), rewritten grace expects in src/lib/nw-forecast.test.ts, extended src/components/dashboard/nw-forecast-ui.test.ts.
</objective>

<execution_context>
@/home/artem/Documents/wallet/.cursor/gsd-core/workflows/execute-plan.md
@/home/artem/Documents/wallet/.cursor/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/29-kapital-overlay-saviso/29-CONTEXT.md
@.planning/phases/29-kapital-overlay-saviso/29-RESEARCH.md
@.planning/phases/29-kapital-overlay-saviso/29-PATTERNS.md
@.planning/phases/29-kapital-overlay-saviso/29-UI-SPEC.md
@.planning/phases/29-kapital-overlay-saviso/29-VALIDATION.md
@src/lib/griso.test.ts
@src/lib/iniso.test.ts
@src/lib/nw-forecast.test.ts
@src/components/dashboard/nw-forecast-ui.test.ts
</context>

## Artifacts this phase produces

- `src/lib/saviso.test.ts` — new file. No new production symbols.

## Flagged assumptions (unclassified edges — not dropped)

INT-02, SAVISO-01, and SAVISO-02 have no boundary/precision class in the spec-less probe. They stay unresolved assumptions, not silent drops:

- INT-02: the user-visible surface is the existing dashed series plus tooltip rows. No second series (C-01).
- SAVISO-01: isolation means no snapshot writes and no change to historical LOCF inputs.
- SAVISO-02: the regression is this new file's import wall, never-call scan, and golden series.

INT-03 boundary and precision are resolved below as explicit expects, not left unresolved.

<tasks>

<task type="auto">
  <name>Wave 0: SAVISO isolation suite</name>
  <files>src/lib/saviso.test.ts</files>
  <read_first>
    .planning/phases/29-kapital-overlay-saviso/29-PATTERNS.md
    .planning/phases/29-kapital-overlay-saviso/29-CONTEXT.md
    src/lib/griso.test.ts
    src/lib/iniso.test.ts
    src/lib/mcp/tools/forecast.test.ts
    src/lib/historical-series.ts
  </read_first>
  <behavior>
    - net-worth.ts and historical-series.ts do not import savings-interest or nw-forecast (SAVISO-01, D-12)
    - savings-interest.ts, nw-forecast.ts, DashboardChartsShell.tsx, and page.tsx do not call balanceSnapshot create, update, upsert, or delete (SAVISO-01)
    - buildNetWorthSeries on a type SAVINGS account with snapshot minor 100_000n returns totalPrimaryMinor 100_000n twice; an unused interestMinor and annualRateBps fixture is voided (SAVISO-02)
    - BuildNetWorthSeriesInput keys do not include interest (type-level Extract wall)
  </behavior>
  <action>
    Create src/lib/saviso.test.ts only. Copy the GRISO import-wall describe and the INISO golden series shape (D-12, SAVISO-01, SAVISO-02). Account type is SAVINGS. Expect snapshot minors, not snapshot plus interest. Void an unused interest fixture the way GRISO voids an unused grace concept. Scan the four forecast-path files for snapshot mutators. Add the type-level wall that forbids an interest key on BuildNetWorthSeriesInput. Do not edit production files, MCP tools, or the savings todo. Do not import savings-interest into the historical builder from this test's production targets.
  </action>
  <verify>
    <automated>./node_modules/.bin/vitest run src/lib/saviso.test.ts</automated>
    <fails_when>non-zero exit, or "0 passed" in the summary line</fails_when>
  </verify>
  <acceptance_criteria>
    - src/lib/saviso.test.ts exists and the vitest command exits 0
    - The golden expect is totalPrimaryMinor 100_000n twice for type SAVINGS
    - No file under src/lib/mcp/ is modified
  </acceptance_criteria>
  <done>saviso.test.ts passes against current production. Historical series identity and the snapshot-mutator ban are locked.</done>
</task>

<task type="auto">
  <name>Wave 0: signed grace totals and FX boundary</name>
  <files>src/lib/nw-forecast.test.ts</files>
  <read_first>
    .planning/phases/29-kapital-overlay-saviso/29-RESEARCH.md
    .planning/phases/29-kapital-overlay-saviso/29-PATTERNS.md
    src/lib/nw-forecast.test.ts
    src/lib/nw-forecast.ts
  </read_first>
  <behavior>
    - Future OPEN grace 50_000n, anchor 1_000_000n, today 2026-03-01: today stays 1_000_000n; 2026-03-15 and 2026-03-31 become 950_000n; plannedAmountMinor stays 50_000n (D-11)
    - Overdue OPEN fold: today sample becomes 450_000n (was 500_000n with grace 50_000n); later samples stay at that dipped running total (D-11)
    - Grace-only horizon: today stays 800_000n; 2026-03-20 and 2026-03-31 become 790_000n; series stays non-empty (D-11)
    - Same-day income 100_000n plus grace 50_000n: day becomes 1_050_000n; both kinds stay on forecastEvents (D-11)
    - Same-day interest 50_000n plus grace 50_000n: day becomes 1_000_000n; both kinds stay on forecastEvents (D-11)
    - Non-primary interest with no rate: excludedMissingFxCurrencies equals ["USD"] and includedSlotCount is 0 (INT-03 boundary)
    - Same slot with a USD rate whose asOfDate is today: includedSlotCount is 1 (INT-03 boundary, rate present)
    - Same slot with a USD rate whose asOfDate is the day after today: slot dropped (LOCF as of today, one step past the threshold)
    - Mixed income plus interest missing codes are unique and sorted
    - nw-forecast.ts source contains convertOtherMinorToPrimaryMinor and does not gain a second rounding helper (INT-03 precision)
    - nw-forecast.ts still does not match savings-interest
    - Do not assert forecast equals nw on today when grace events exist
  </behavior>
  <action>
    Edit src/lib/nw-forecast.test.ts only. Rewrite the five flat-grace expects to the signed totals in the behavior block (D-11). Rename titles that say the line is neutral or that NW moves by income only or interest only. Keep sampling those dates. plannedAmountMinor stays the positive payment. Tighten the non-primary interest FX case to the currency list, and add the rate-present and day-after-today cases using the existing USD rate fixture shape (asOfDate plus rateToPrimaryScaled). Add one mixed income-plus-interest miss whose codes are unique and sorted. Add a source expect that the builder still calls convertOtherMinorToPrimaryMinor and does not introduce another rounding mode (INT-03 precision). Leave the savings-interest import wall. Do not edit src/lib/nw-forecast.ts, src/lib/savings-interest.ts, or any MCP file. Do not change Phase 28 interest truncate expects.
  </action>
  <verify>
    <automated>./node_modules/.bin/vitest run src/lib/nw-forecast.test.ts; test $? -ne 0 && grep -q '950_000n' src/lib/nw-forecast.test.ts && grep -q '1_050_000n' src/lib/nw-forecast.test.ts</automated>
    <fails_when>overall command exits non-zero because the suite stayed green (grace still flat) or 950_000n is absent from the test file</fails_when>
  </verify>
  <acceptance_criteria>
    - The five grace cases expect the signed minors in the behavior block
    - excludedMissingFxCurrencies expect for the USD interest miss is the list ["USD"], not only a count
    - A today-dated rate includes the slot and a next-day rate drops it
    - vitest on this file exits non-zero until Plan 29-02 flips the grace arm
    - src/lib/nw-forecast.ts is unmodified by this task
  </acceptance_criteria>
  <done>Signed grace expects and the INT-03 FX boundary expects exist. The suite is red on the grace arm and still forbids a savings-interest import in the builder.</done>
</task>

<task type="auto">
  <name>Wave 0: tooltip and shell file-scan</name>
  <files>src/components/dashboard/nw-forecast-ui.test.ts</files>
  <read_first>
    .planning/phases/29-kapital-overlay-saviso/29-UI-SPEC.md
    .planning/phases/29-kapital-overlay-saviso/29-PATTERNS.md
    src/components/dashboard/nw-forecast-ui.test.ts
    src/components/dashboard/NetWorthHistoryChart.tsx
    src/components/dashboard/DashboardChartsShell.tsx
  </read_first>
  <behavior>
    - Chart source contains Накопительный, Ожидаемое начисление, and Ожидаемый платёж (D-04, D-05, D-09)
    - Chart source does not contain the retired subtitle NW без изменения (оплата карты) (D-09)
    - Header Платёж для беспроцентного stays
    - Interest amount source uses a plus prefix on formatChartNumber; grace amount source uses a minus prefix on the positive major; the Прогноз level calls formatChartNumber with no extra plus (D-08, D-09, D-10)
    - Interest rows sort with localeCompare, locale ru, sensitivity base (D-02)
    - Missing interest name fallback is счёт plus accountId (D-01)
    - Shell source calls listInterestSlotsInRange and sets kind interest (INT-02)
    - Banner scan still rejects доходы and грейс and also rejects накопительный on the shell (C-03)
    - Single strokeDasharray 5 5 Line remains (C-01)
    - Tooltip card roots use text-sm; the card is not text-xs (UI-SPEC Label)
  </behavior>
  <action>
    Edit src/components/dashboard/nw-forecast-ui.test.ts only. Replace the file-scan that requires the current grace subtitle with expects for the three locked strings in the behavior block (D-04, D-05, D-09). Assert the retired subtitle sentence is absent. Keep the grace header. Add source expects for the plus prefix, the minus prefix, an unsigned Прогноз level (D-08, D-10), the ru localeCompare sort (D-02), the missing-name fallback (D-01), text-sm on the tooltip roots, shell listInterestSlotsInRange, and kind interest (INT-02). Extend the banner scan so the shell does not name the savings kind (C-03). Keep the single dashed Line expect (C-01). Do not edit NetWorthHistoryChart.tsx, DashboardChartsShell.tsx, or MCP files. Do not add a React renderer.
  </action>
  <verify>
    <automated>./node_modules/.bin/vitest run src/components/dashboard/nw-forecast-ui.test.ts; test $? -ne 0 && grep -q 'Ожидаемое начисление' src/components/dashboard/nw-forecast-ui.test.ts && grep -q 'listInterestSlotsInRange' src/components/dashboard/nw-forecast-ui.test.ts</automated>
    <fails_when>overall command exits non-zero because the UI suite stayed green or the new copy expect is absent from the test file</fails_when>
  </verify>
  <acceptance_criteria>
    - The test file expects Ожидаемый платёж, Накопительный, and Ожидаемое начисление
    - The test file expects the shell to reference listInterestSlotsInRange
    - vitest on this file exits non-zero until Plan 29-02 updates the chart and shell
    - Production chart and shell files are unmodified by this task
  </acceptance_criteria>
  <done>UI file-scan is red against the current chart subtitle and the shell that does not yet load interest slots.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Wave 0 expects → Plan 29-02 production | A wrong red contract would lock a flat grace line or a second rounding mode |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-29-01 | Tampering | saviso.test.ts | high | mitigate | Never-call scan on snapshot mutators plus golden series that equals the snapshot minor (D-12, SAVISO-01, SAVISO-02) |
| T-29-02 | Tampering | nw-forecast.test.ts grace expects | high | mitigate | Red expects lock the negative primary addend and keep event magnitude positive (D-11) |
| T-29-03 | Tampering | interest FX expects | high | mitigate | Missing rate lists the currency code; a rate as of today includes the slot; no second rounding mode (INT-03) |
| T-29-04 | Tampering | overlay slots fed into interest math | medium | mitigate | This plan does not wire the shell. The UI scan only locks listInterestSlotsInRange on savings inputs, which Plan 29-02 must pass without income or grace slots |
| T-29-SC | Tampering | npm installs | high | mitigate | No package install. Vitest 4.1.11 is already local |
</threat_model>

<verification>
saviso.test.ts exits 0. nw-forecast.test.ts and nw-forecast-ui.test.ts exit non-zero and contain the new contract strings. No production file changes.
</verification>

<success_criteria>
Plan 29-02 can turn the same three files green without rewriting the signed minors, the FX boundary, or the tooltip copy expects.
</success_criteria>

<output>
Create `.planning/phases/29-kapital-overlay-saviso/29-01-SUMMARY.md` when done
</output>
