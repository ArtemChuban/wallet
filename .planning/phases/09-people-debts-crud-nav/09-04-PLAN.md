---
phase: 09-people-debts-crud-nav
plan: 04
type: execute
wave: 3
depends_on:
  - "09-02"
files_modified:
  - src/components/accounts/AccountList.tsx
  - src/components/accounts/AccountList.test.tsx
autonomous: true
requirements:
  - PERSON-02
  - DNAV-01
estimate:
  tokens: 16000
  raw_tokens: 32000
  tasks: 2
  confidence: high
must_haves:
  truths:
    - Balance-snapshot delete on /accounts uses in-dialog second-step confirm with copy «Удалить снимок за {date}? Это нельзя отменить.» (D-17 / D-16)
    - AccountList snapshot delete no longer invokes a browser native confirm API
    - Confirm pending disables the confirm button while deleteBalanceSnapshot runs
    - Existing deleteBalanceSnapshot Server Action behavior unchanged (same FormData id contract)
    - Nav «Долги» regression still green after accounts confirm migration (DNAV-01)
  prohibitions:
    - statement: MUST NOT use a browser native confirm dialog for balance-snapshot delete
      requirement_id: PERSON-02
      category: values
    - statement: MUST NOT expand this plan to migrate RateList confirm UX (out of D-17 scope)
      requirement_id: DNAV-01
      category: values
  artifacts:
    - path: src/components/accounts/AccountList.tsx
      provides: in-dialog snapshot delete confirm (D-17)
    - path: src/components/accounts/AccountList.test.tsx
      provides: regression asserting no browser native confirm in AccountList
  key_links:
    - from: src/components/accounts/AccountList.tsx
      to: src/components/debts/DestructiveConfirmStep.tsx
      via: shared confirm second step (or equivalent local Dialog state)
      pattern: "Удалить снимок"
    - from: src/components/accounts/AccountList.tsx
      to: src/app/accounts/actions.ts
      via: deleteBalanceSnapshot FormData id
      pattern: "deleteBalanceSnapshot"
---

## Phase Goal

User can manage people and debts in a dedicated «Долги» section (without repayments UI yet).

<objective>
Migrate account balance-snapshot delete off browser native confirm onto the same in-dialog second-step pattern (D-17 / D-16 constitution), without changing RateList or snapshot Server Action contracts.

Purpose: Finish Phase 9 destructive-UX constitution for the scoped accounts call site so debts and snapshots share one confirm model.
Output: Updated AccountList confirm UX + regression test; DNAV nav tests still green.
</objective>

<execution_context>
@/home/artem/Documents/wallet/.cursor/gsd-core/workflows/execute-plan.md
@/home/artem/Documents/wallet/.cursor/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/09-people-debts-crud-nav/09-CONTEXT.md
@.planning/phases/09-people-debts-crud-nav/09-UI-SPEC.md
@.planning/phases/09-people-debts-crud-nav/09-PATTERNS.md
@src/components/accounts/AccountList.tsx
@src/components/debts/DestructiveConfirmStep.tsx
@src/app/accounts/actions.ts
@src/components/nav.test.ts
</context>

<assumptions>
## Planner assumptions (specless / discretion)

- Reuse DestructiveConfirmStep from Plan 02 when import path is clean for accounts; otherwise duplicate the Dialog state machine locally — D-16 behavior is mandatory either way.
- RateList confirm migration explicitly out of scope (RESEARCH Pitfall 5 / D-17).
</assumptions>

## Artifacts this phase produces

- AccountList in-dialog snapshot delete confirm (D-17)
- `src/components/accounts/AccountList.test.tsx` regression against browser native confirm

<tasks>

<task type="auto" tdd="true">
  <name>Regression test: AccountList must not use browser native confirm</name>
  <files>src/components/accounts/AccountList.test.tsx</files>
  <read_first>
    src/components/accounts/AccountList.tsx
    .planning/phases/09-people-debts-crud-nav/09-VALIDATION.md
    src/components/nav.test.ts
  </read_first>
  <behavior>
    - Test reads AccountList source (or renders delete flow) and asserts the browser native confirm API is not referenced
    - Optionally asserts UI-SPEC snapshot confirm copy substring «Удалить снимок за» exists in source
  </behavior>
  <action>
    Create AccountList.test.tsx that fails while AccountList still references the browser native confirm API (readFileSync/source scan acceptable, matching DISOL-style gates). Include assertion for Russian snapshot confirm copy template from UI-SPEC. Do not change AccountList yet — leave red for the next task.
  </action>
  <verify>
    <automated>npx vitest run src/components/accounts/AccountList.test.tsx; test $? -ne 0</automated>
    <fails_when>vitest exits 0 before migration (test not actually red) or test file missing</fails_when>
  </verify>
  <acceptance_criteria>
    - test -f src/components/accounts/AccountList.test.tsx
    - Test fails while AccountList still uses browser native confirm
  </acceptance_criteria>
  <done>Red regression test for D-17 migration exists</done>
</task>

<task type="auto">
  <name>Migrate snapshot delete to in-dialog second-step confirm</name>
  <reversibility rating="costly">D-16/D-17 constitution call site — wrong confirm UX reintroduces banned browser dialogs on accounts.</reversibility>
  <files>src/components/accounts/AccountList.tsx</files>
  <read_first>
    .planning/phases/09-people-debts-crud-nav/09-UI-SPEC.md
    src/components/accounts/AccountList.tsx
    src/components/debts/DestructiveConfirmStep.tsx
    src/app/accounts/actions.ts
  </read_first>
  <action>
    Replace the AccountList snapshot delete browser native confirm flow with an in-dialog second step (reuse DestructiveConfirmStep if practical). Copy: «Удалить снимок за {date}? Это нельзя отменить.» Entry label «Удалить снимок»; confirm label «Удалить снимок»; back «Назад»; disable confirm while pending (UI-SPEC / D-17). Keep existing useTransition + FormData id + deleteBalanceSnapshot + role=alert error display. Do not modify deleteBalanceSnapshot action signature. Do not touch RateList. Do not change nav.
  </action>
  <verify>
    <automated>npx vitest run src/components/accounts/AccountList.test.tsx src/components/nav.test.ts src/app/accounts/actions.test.ts && ! grep -n 'window\.confirm' src/components/accounts/AccountList.tsx && grep -q 'Удалить снимок за' src/components/accounts/AccountList.tsx</automated>
    <fails_when>non-zero vitest exit, window.confirm still in AccountList.tsx, or UI-SPEC snapshot confirm copy missing</fails_when>
  </verify>
  <acceptance_criteria>
    - AccountList.tsx has no window.confirm
    - Snapshot confirm uses UI-SPEC Russian loss copy
    - AccountList.test.tsx, nav.test.ts, and accounts actions tests exit 0
  </acceptance_criteria>
  <done>D-17 complete: snapshot delete uses in-dialog confirm; RateList untouched</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Browser → deleteBalanceSnapshot | Existing FormData id boundary unchanged |
| Confirm UI → action | Second-step intent gate before destructive delete |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-09-10 | Destruction | AccountList snapshot delete | medium | mitigate | In-dialog confirm with explicit loss copy before deleteBalanceSnapshot |
| T-09-11 | Tampering | Scope creep RateList | low | accept | Explicitly out of D-17 — leave RateList unchanged this phase |
| T-09-SC | Tampering | npm installs | high | accept | No new packages |
</threat_model>

<verification>
AccountList confirm migrated; regression + nav + accounts action tests green; RateList untouched.
</verification>

<success_criteria>
Balance-snapshot delete uses D-16 second-step dialog; AccountList has no browser native confirm; DNAV tests still pass.
</success_criteria>

<output>
Create `.planning/phases/09-people-debts-crud-nav/09-04-SUMMARY.md` when done
</output>
