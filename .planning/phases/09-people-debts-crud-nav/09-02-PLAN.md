---
phase: 09-people-debts-crud-nav
plan: 02
type: execute
wave: 2
depends_on:
  - "09-01"
files_modified:
  - src/app/debts/actions.ts
  - src/app/debts/actions.test.ts
  - src/app/debts/page.tsx
  - src/components/debts/PersonFormDialog.tsx
  - src/components/debts/DebtsList.tsx
  - src/components/debts/DestructiveConfirmStep.tsx
autonomous: true
requirements:
  - PERSON-01
  - PERSON-02
estimate:
  tokens: 26000
  raw_tokens: 52000
  tasks: 3
  confidence: high
must_haves:
  truths:
    - User can rename a person via dialog «Изменить имя» from the person group header (PERSON-01 / D-08)
    - User can delete a person only when debt count is 0; otherwise Russian blocked copy «Нельзя удалить человека, пока есть долги» (PERSON-02 / D-07)
    - Person delete uses in-dialog second-step confirm with copy «Удалить человека «{name}»? Это нельзя отменить.» — never a browser native confirm (D-16)
    - All people appear on /debts including zero-debt groups with «Нет долгов» + «Новый долг» CTA shell (D-03 / D-21)
    - Person groups sort A–Z by name (D-04)
    - When ≥1 person exists, page header shows both «Новый человек» and «Новый долг» CTAs (D-22) — debt dialog may be stub trigger until Plan 03
    - Repeated deletePerson when debts remain returns the same blocked message (PERSON-02 idempotency)
    - statement: Long person names wrap or ellipsis in group headers; full value editable in rename dialog
      verification: backstop
  prohibitions:
    - statement: MUST NOT delete a person who still has any debts (OPEN or CLOSED)
      requirement_id: PERSON-02
      category: safety
    - statement: MUST NOT use a browser native confirm dialog for person delete
      requirement_id: PERSON-02
      category: values
    - statement: MUST NOT soft-delete people or cascade-delete their debts from the person delete action
      requirement_id: PERSON-02
      category: values
  artifacts:
    - path: src/app/debts/actions.ts
      provides: renamePerson, deletePerson
    - path: src/components/debts/PersonFormDialog.tsx
      provides: rename mode
    - path: src/components/debts/DestructiveConfirmStep.tsx
      provides: shared in-dialog confirm second step
    - path: src/components/debts/DebtsList.tsx
      provides: person groups, empty-group chrome, rename/delete controls
  key_links:
    - from: src/app/debts/actions.ts
      to: prisma.debt.count
      via: PERSON-02 pre-check before person.delete
      pattern: "debt.count"
    - from: src/components/debts/DebtsList.tsx
      to: src/components/debts/DestructiveConfirmStep.tsx
      via: person delete second step
      pattern: "DestructiveConfirmStep"
    - from: src/app/debts/actions.ts
      to: src/lib/validations/debts.ts
      via: renamePersonSchema
      pattern: "renamePersonSchema"
---

## Phase Goal

User can manage people and debts in a dedicated «Долги» section (without repayments UI yet).

<objective>
Complete people management on `/debts`: rename, PERSON-02 guarded delete with destructive confirm constitution, grouped empty states, and dual header CTAs (per D-03, D-07, D-08, D-16, D-21, D-22).

Purpose: Finish PERSON-01/PERSON-02 UX before debt create/edit expansion.
Output: renamePerson/deletePerson actions, DestructiveConfirmStep, DebtsList person groups with empty-group chrome.
</objective>

<execution_context>
@/home/artem/Documents/wallet/.cursor/gsd-core/workflows/execute-plan.md
@/home/artem/Documents/wallet/.cursor/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/09-people-debts-crud-nav/09-01-PLAN.md
@.planning/phases/09-people-debts-crud-nav/09-CONTEXT.md
@.planning/phases/09-people-debts-crud-nav/09-UI-SPEC.md
@.planning/phases/09-people-debts-crud-nav/09-PATTERNS.md
@src/app/debts/actions.ts
@src/components/debts/DebtsList.tsx
@src/components/debts/PersonFormDialog.tsx
@src/components/accounts/AccountFormDialog.tsx
@src/lib/validations/debts.ts
</context>

<assumption_delta_decision>
  <noun>Debt optional metadata (dueDate, note)</noun>
  <decision>no-change</decision>
  <rationale>Detector fired on ROADMAP “optional due/note” wording. Phase 8 already stores nullable dueDate/note; Phase 9 only surfaces them in debt UI (Plan 03). They do not replace person, currency, or initial as debt identity anchors.</rationale>
</assumption_delta_decision>

<assumptions>
## Planner assumptions (specless / discretion)

- A-edge PERSON-02 concurrency: count-then-delete race possible if a debt is created between checks; Prisma Restrict / P2003 is the belt — return generic delete failure copy if race hits.
- Discretion: Extract DestructiveConfirmStep shared client helper now (≥2 call sites across Plans 02–04).
- «Новый долг» header/group CTAs may open a disabled stub or empty DebtFormDialog shell until Plan 03 wires create — button labels and placement must match UI-SPEC D-22/D-21.
</assumptions>

## Artifacts this phase produces

- `renamePerson` / `deletePerson` in `src/app/debts/actions.ts`
- `PersonFormDialog` rename mode («Изменить имя» / «Сохранить имя»)
- `DestructiveConfirmStep` shared confirm UI
- `DebtsList` person group headers with rename/delete + «Нет долгов» empty groups

<tasks>

<task type="auto" tdd="true">
  <name>Add renamePerson + deletePerson action tests then implement</name>
  <files>src/app/debts/actions.ts, src/app/debts/actions.test.ts</files>
  <read_first>
    src/app/debts/actions.ts
    src/app/debts/actions.test.ts
    src/app/accounts/actions.ts
    src/lib/validations/debts.ts
    .planning/phases/09-people-debts-crud-nav/09-RESEARCH.md
  </read_first>
  <behavior>
    - renamePerson uses renamePersonSchema; updates name only; revalidatePath("/debts"); maps P2002 to Russian name error
    - deletePerson when debt.count > 0 returns message «Нельзя удалить человека, пока есть долги» and does not call person.delete (PERSON-02 / D-07)
    - deletePerson when count is 0 deletes person and revalidates /debts
    - P2003 fallback returns Russian delete failure copy
  </behavior>
  <action>
    Extend actions.test.ts with renamePerson success/P2002 cases and deletePerson blocked-when-debts / success-when-zero cases (PERSON-01, PERSON-02). Implement renamePerson and deletePerson in actions.ts: always ensureSqlitePragmas; pre-count debts before delete; never cascade-delete debts from person delete; catch P2003 as belt. Use UI-SPEC blocked copy verbatim. Do not implement debt mutations here.
  </action>
  <verify>
    <automated>npx vitest run src/app/debts/actions.test.ts && grep -q 'renamePerson' src/app/debts/actions.ts && grep -q 'deletePerson' src/app/debts/actions.ts && grep -q 'Нельзя удалить человека, пока есть долги' src/app/debts/actions.ts</automated>
    <fails_when>non-zero vitest exit, missing rename/delete exports, or blocked PERSON-02 copy absent from actions.ts</fails_when>
  </verify>
  <acceptance_criteria>
    - renamePerson and deletePerson exported and covered by tests
    - deletePerson refuses when debts exist without calling person.delete
    - npx vitest run src/app/debts/actions.test.ts exits 0
  </acceptance_criteria>
  <done>Person rename/delete Server Actions green with PERSON-02 guard</done>
</task>

<task type="auto">
  <name>DestructiveConfirmStep + person header rename/delete UX</name>
  <reversibility rating="costly">D-16 app-wide destructive confirm constitution — wrong pattern spreads to all future deletes; undo means reworking every call site.</reversibility>
  <files>src/components/debts/DestructiveConfirmStep.tsx, src/components/debts/PersonFormDialog.tsx, src/components/debts/DebtsList.tsx</files>
  <read_first>
    .planning/phases/09-people-debts-crud-nav/09-UI-SPEC.md
    .planning/phases/09-people-debts-crud-nav/09-CONTEXT.md
    src/components/debts/DebtsList.tsx
    src/components/debts/PersonFormDialog.tsx
    src/components/ui/dialog.tsx
    src/components/ui/button.tsx
  </read_first>
  <action>
    Add DestructiveConfirmStep client helper: first step entry label, second step RU loss copy, confirm/back buttons, pending disable (D-16). Wire person group header controls «Изменить имя» / «Удалить человека» per UI-SPEC. Rename uses PersonFormDialog edit mode (D-08). Delete: if server returns PERSON-02 blocked message, show it without confirm; if allowed, open confirm with «Удалить человека «{name}»? Это нельзя отменить.» then call deletePerson. Do not call any browser native confirm API. Prefer Dialog state machine — do not add AlertDialog package.
  </action>
  <verify>
    <automated>test -f src/components/debts/DestructiveConfirmStep.tsx && grep -q 'DestructiveConfirmStep' src/components/debts/DebtsList.tsx && grep -q 'Изменить имя' src/components/debts/DebtsList.tsx && ! grep -R --include='*.tsx' -n 'window\.confirm' src/components/debts/</automated>
    <fails_when>confirm helper missing, DebtsList missing rename wiring, or debts components still call window.confirm</fails_when>
  </verify>
  <acceptance_criteria>
    - DestructiveConfirmStep exists and is used for person delete
    - PersonFormDialog supports rename mode with UI-SPEC titles/actions
    - No window.confirm under src/components/debts/
  </acceptance_criteria>
  <done>Person rename/delete UI follows D-16 confirm constitution</done>
</task>

<task type="auto">
  <name>Person groups empty chrome + dual header CTAs</name>
  <files>src/components/debts/DebtsList.tsx, src/app/debts/page.tsx</files>
  <read_first>
    .planning/phases/09-people-debts-crud-nav/09-UI-SPEC.md
    src/app/debts/page.tsx
    src/components/debts/DebtsList.tsx
    src/components/accounts/AccountList.tsx
  </read_first>
  <action>
    Ensure DebtsList layout is person-grouped per D-01 (person header with nested debts under each group — no flat-only debt list; no `/debts/[personId]` or other person-detail route). Include every person (D-03) sorted A–Z (D-04). Empty person groups show «Нет долгов» plus «Новый долг» CTA (D-21). When people.length ≥ 1, page header shows both «Новый человек» and «Новый долг» (D-22); when zero people, keep empty-state CTAs only (no crowded header). «Новый долг» may mount DebtFormDialog stub trigger for Plan 03 to fill — labels and placement must match UI-SPEC now. Preserve Plan 01 empty «Нет людей» path (D-20).
  </action>
  <verify>
    <automated>grep -q 'Нет долгов' src/components/debts/DebtsList.tsx && grep -q 'Новый долг' src/components/debts/DebtsList.tsx && grep -q 'Новый человек' src/app/debts/page.tsx && grep -q 'Новый долг' src/app/debts/page.tsx && npx vitest run src/app/debts/actions.test.ts</automated>
    <fails_when>missing empty-group or header CTA copy, or actions tests fail</fails_when>
  </verify>
  <acceptance_criteria>
    - Empty groups and dual header CTAs match D-21/D-22 copy
    - All people listed including zero-debt groups
    - actions tests still green
  </acceptance_criteria>
  <done>Grouped people list empty states and CTAs complete PERSON-01 list UX</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Browser → deletePerson / renamePerson | Untrusted ids and names |
| UI confirm step → deletePerson | User intent gate before destructive write |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-09-04 | Tampering | deletePerson | high | mitigate | Pre-count debts + Restrict FK; blocked RU message; no cascade from person delete |
| T-09-05 | Tampering | renamePerson | medium | mitigate | renamePersonSchema.strict(); P2002 unique handling |
| T-09-06 | Repudiation | DestructiveConfirmStep | low | mitigate | Explicit second-step confirm with loss copy before delete |
| T-09-SC | Tampering | npm installs | high | accept | No new packages |
</threat_model>

<verification>
Person rename/delete actions + UI confirm; empty groups; dual CTAs; PERSON-02 tests green.
</verification>

<success_criteria>
Users rename people; delete blocked when debts exist; confirm step used; empty-group and header CTAs match UI-SPEC.
</success_criteria>

<output>
Create `.planning/phases/09-people-debts-crud-nav/09-02-SUMMARY.md` when done
</output>
