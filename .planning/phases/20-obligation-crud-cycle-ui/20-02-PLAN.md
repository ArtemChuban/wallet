---
phase: 20-obligation-crud-cycle-ui
plan: 02
type: execute
wave: 2
depends_on:
  - 20-01
files_modified:
  - src/app/accounts/actions.ts
  - src/app/accounts/actions.test.ts
  - src/components/accounts/CreditGraceDialog.tsx
  - src/components/accounts/CreditGraceAmountDialog.tsx
  - src/components/accounts/credit-grace-ui.test.ts
autonomous: true
requirements:
  - OBL-01
  - OBL-02
estimate:
  tokens: 35000
  raw_tokens: 70000
  tasks: 2
  confidence: high
must_haves:
  truths:
    - "OPEN amount editable via amount dialog; cycleStartAsOf and dueAsOf stay frozen on update (D-10, OBL-01)"
    - "Оплачено opens DestructiveConfirmStep with editable closedAsOf defaulting to today; backdate allowed (D-11, OBL-02)"
    - "CLOSED → OPEN reopen allowed with DestructiveConfirmStep confirm (D-12, OBL-02)"
    - "Confirm flows never use native browser confirm dialogs"
    - "CLOSED history collapsed behind Показать оплаченные / Скрыть оплаченные (D-06)"
    - "close/reopen/update actions never write BalanceSnapshot"
  prohibitions:
    - statement: MUST NOT recompute stored dueAsOf on amount update
      requirement_id: OBL-01
      category: isolation
    - statement: MUST NOT use native browser confirm for close or reopen
      requirement_id: OBL-02
      category: values
    - statement: MUST NOT write BalanceSnapshot from close/reopen/update
      requirement_id: OBL-02
      category: isolation
  artifacts:
    - path: src/app/accounts/actions.ts
      provides: updateCreditGraceObligation, closeCreditGraceObligation, reopenCreditGraceObligation
    - path: src/components/accounts/CreditGraceDialog.tsx
      provides: Оплачено / reopen confirm steps + collapsed CLOSED (D-06, D-11, D-12)
    - path: src/components/accounts/CreditGraceAmountDialog.tsx
      provides: edit OPEN amount path (D-10)
  key_links:
    - from: Оплачено button
      to: DestructiveConfirmStep
      via: children date field closedAsOf + pendingLabel Сохранение… (D-11)
      pattern: "DestructiveConfirmStep"
    - from: closeCreditGraceObligation
      to: status CLOSED + closedAsOf
      via: update semantics / Zod (OBL-02)
      pattern: "closeCreditGraceObligation"
    - from: CLOSED list
      to: Показать оплаченные toggle
      via: DebtsList collapsed pattern (D-06)
      pattern: "Показать оплаченные"
---

<objective>
Complete obligation lifecycle: update OPEN amount (D-10), early close with editable closedAsOf (D-11), reopen with confirm (D-12), collapsed CLOSED history (D-06) — OBL-01 edit + OBL-02.

Purpose: Finish write-path CRUD and confirm UX after Plan 01 create tracer.
Output: update/close/reopen actions + dialog steps green under actions.test + UI source-scan progress.
</objective>

<execution_context>
@/home/artem/Documents/wallet/.cursor/gsd-core/workflows/execute-plan.md
@/home/artem/Documents/wallet/.cursor/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/20-obligation-crud-cycle-ui/20-CONTEXT.md
@.planning/phases/20-obligation-crud-cycle-ui/20-UI-SPEC.md
@.planning/phases/20-obligation-crud-cycle-ui/20-PATTERNS.md
@.planning/phases/20-obligation-crud-cycle-ui/20-01-PLAN.md
@src/app/accounts/actions.ts
@src/app/accounts/actions.test.ts
@src/components/accounts/CreditGraceDialog.tsx
@src/components/accounts/CreditGraceAmountDialog.tsx
@src/components/ui/destructive-confirm-step.tsx
@src/components/debts/DebtDetailDialog.tsx
@src/components/debts/DebtsList.tsx
@src/lib/validations/credit-grace.ts
</context>

<tasks>

<task type="auto" tdd="true">
  <name>update / close / reopen server actions + isolation tests</name>
  <files>src/app/accounts/actions.ts, src/app/accounts/actions.test.ts</files>
  <read_first>
    src/app/accounts/actions.ts
    src/app/accounts/actions.test.ts
    src/lib/validations/credit-grace.ts
    .planning/phases/20-obligation-crud-cycle-ui/20-RESEARCH.md
  </read_first>
  <behavior>
    - updateCreditGraceObligation updates amountMinor/note for OPEN only; leaves cycleStartAsOf and dueAsOf unchanged (D-10, Phase 19 D-05)
    - closeCreditGraceObligation sets status CLOSED + closedAsOf from FormData; rejects missing closedAsOf
    - reopenCreditGraceObligation sets status OPEN and clears closedAsOf (D-12)
    - All three never call prisma.balanceSnapshot create/update/delete
    - Non-OPEN update / wrong account type return field or form errors
  </behavior>
  <action>
    Add updateCreditGraceObligation, closeCreditGraceObligation, and reopenCreditGraceObligation to src/app/accounts/actions.ts using updateCreditGraceObligationSchema / thin wrappers (RESEARCH discretion). Load obligation + account; gate FIAT_CREDIT; for update keep cycle keys frozen (D-10); for close set CLOSED + closedAsOf; for reopen set OPEN and null closedAsOf (D-11, D-12). Map Zod field errors into AccountActionState; revalidatePath /accounts and /. Never touch balanceSnapshot models (T-20-03). Green the Wave 0 action test stubs for update/close/reopen + isolation + closedAsOf pairing. Prefer dedicated close/reopen exports for clearer RU messages per RESEARCH Q3. Do not implement forecast membership changes.
  </action>
  <verify>
    <automated>npx vitest run src/app/accounts/actions.test.ts</automated>
    <fails_when>vitest non-zero, "0 passed" in summary, or close/update/reopen cases still failing</fails_when>
  </verify>
  <done>update/close/reopen actions green with snapshot isolation asserts</done>
</task>

<task type="auto">
  <name>Amount edit + close/reopen steps + collapsed CLOSED</name>
  <files>src/components/accounts/CreditGraceDialog.tsx, src/components/accounts/CreditGraceAmountDialog.tsx, src/components/accounts/credit-grace-ui.test.ts</files>
  <read_first>
    src/components/accounts/CreditGraceDialog.tsx
    src/components/accounts/CreditGraceAmountDialog.tsx
    src/components/ui/destructive-confirm-step.tsx
    src/components/debts/DebtDetailDialog.tsx
    src/components/debts/DebtsList.tsx
    .planning/phases/20-obligation-crud-cycle-ui/20-UI-SPEC.md
  </read_first>
  <behavior>
    - OPEN row «Изменить» opens CreditGraceAmountDialog edit mode with frozen Цикл / Оплатить до (D-10)
    - «Оплачено» → in-dialog DestructiveConfirmStep; children = Дата оплаты input default calendarDateToday(); pendingLabel Сохранение…; confirm «Отметить оплаченным» (D-11, UI-SPEC)
    - CLOSED «Вернуть к оплате» → DestructiveConfirmStep pendingLabel Возврат… (D-12)
    - CLOSED rows behind «Показать оплаченные» / «Скрыть оплаченные» chevron toggle starting collapsed (D-06)
    - Source-scan asserts DestructiveConfirmStep import and absence of native browser confirm API usage in grace components
  </behavior>
  <action>
    Wire CreditGraceAmountDialog edit path for OPEN rows («Изменить») calling updateCreditGraceObligation; keep cycleStartAsOf/dueAsOf read-only (D-10). In CreditGraceDialog add confirm-step state machine (DebtDetailDialog analog): Оплачено → DestructiveConfirmStep with children date field «Дата оплаты» defaulting to injected today / calendarDateToday(); pendingLabel «Сохранение…»; submit closeCreditGraceObligation (D-11). Reopen CLOSED via «Вернуть к оплате» confirm with pendingLabel «Возврат…» (D-12). Copy messages from 20-UI-SPEC verbatim. Collapse CLOSED behind «Показать оплаченные» using DebtsList chevron pattern (D-06). Update credit-grace-ui.test.ts to require DestructiveConfirmStep import in CreditGraceDialog and to fail if grace component sources call the native browser confirm API. Do not add overdue warn chrome here (Plan 03). Do not put snapshot debt amounts inside grace dialog (D-16).
  </action>
  <verify>
    <automated>npx vitest run src/app/accounts/actions.test.ts src/components/accounts/credit-grace-ui.test.ts &amp;&amp; grep -q 'closeCreditGraceObligation\|Оплачено' src/components/accounts/CreditGraceDialog.tsx &amp;&amp; grep -q 'pendingLabel' src/components/accounts/CreditGraceDialog.tsx &amp;&amp; grep -q 'Показать оплаченные' src/components/accounts/CreditGraceDialog.tsx</automated>
    <fails_when>vitest non-zero, close/Оплачено wiring missing, pendingLabel unused in grace dialog, or collapsed paid toggle copy absent</fails_when>
  </verify>
  <acceptance_criteria>
    - OPEN amount editable; due/cycle frozen (D-10, OBL-01)
    - Early close + reopen use DestructiveConfirmStep with editable closedAsOf on close (D-11, D-12, OBL-02)
    - CLOSED history starts collapsed (D-06)
  </acceptance_criteria>
  <done>Lifecycle UI complete for update/close/reopen + collapsed paid history</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| FormData → close/reopen/update actions | Untrusted status, closedAsOf, amountMajor, id |
| Confirm UI → Server Action | User intent for irreversible-looking close (reversible via reopen) |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-20-01 | Tampering | Crafted closedAsOf / amount on update | high | mitigate | Zod update schema + OPEN-only update gate |
| T-20-03 | Tampering | Snapshot write from close/reopen | high | mitigate | Actions touch CreditGraceObligation only; tests assert |
| T-20-06 | Tampering | Close without closedAsOf | medium | mitigate | Zod CLOSED↔closedAsOf refine; UI required date field |
| T-20-07 | Repudiation | Accidental close | low | accept | Reopen with confirm (D-12); single-user local app |
| T-20-SC | Tampering | npm installs | high | mitigate | Zero new packages |
</threat_model>

<verification>
npx vitest run src/app/accounts/actions.test.ts src/components/accounts/credit-grace-ui.test.ts green; close/reopen/collapsed copy present in CreditGraceDialog.
</verification>

<success_criteria>
OBL-01 edit + OBL-02 early close/reopen delivered with DestructiveConfirmStep; D-06/D-10/D-11/D-12 locked; GRISO preview holds.
</success_criteria>

<output>
Create `.planning/phases/20-obligation-crud-cycle-ui/20-02-SUMMARY.md` when done
</output>
