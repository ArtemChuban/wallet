---
phase: 01-docker-sqlite-foundation
plan: 02
type: execute
wave: 2
depends_on:
  - 01-01
files_modified:
  - components.json
  - src/lib/utils.ts
  - src/components/ui/.gitkeep
  - src/app/globals.css
  - src/app/layout.tsx
  - package.json
  - package-lock.json
autonomous: true
requirements:
  - PLAT-01
estimate:
  tokens: 25000
  raw_tokens: 25000
  tasks: 2
  confidence: low
must_haves:
  truths:
    - shadcn/ui is initialized on the Next App Router shell
    - cn helper exists for later UI phases
    - globals.css is part of the app layout stylesheet chain
  artifacts:
    - path: components.json
      provides: shadcn project config
    - path: src/lib/utils.ts
      provides: cn() helper
    - path: src/app/globals.css
      provides: Tailwind / shadcn CSS entry
    - path: src/components/ui
      provides: primitives directory for later phases
  key_links:
    - from: src/app/layout.tsx
      to: src/app/globals.css
      via: stylesheet import
      pattern: "globals.css"
    - from: src/lib/utils.ts
      to: components.json
      via: shadcn aliases
      pattern: "export function cn|export const cn"
---

<objective>
**As a** local Wallet user, **I want to** have shadcn/ui on the App Router shell, **so that** later UI phases share locked component conventions (D-04, PLAT-01 foundation).

Purpose: Complete scaffold after Plan 01 Next pins; keep Phase 1 component usage light.
Output: components.json, utils cn(), globals.css wired into layout, ui/ directory.
</objective>

<execution_context>
@/home/artem/Documents/wallet/.cursor/gsd-core/workflows/execute-plan.md
@/home/artem/Documents/wallet/.cursor/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/01-docker-sqlite-foundation/01-CONTEXT.md
@.planning/phases/01-docker-sqlite-foundation/01-RESEARCH.md
@.planning/phases/01-docker-sqlite-foundation/01-PATTERNS.md
@.planning/phases/01-docker-sqlite-foundation/01-01-SUMMARY.md
</context>

<assumptions>
## Planner assumptions (specless probe edges for PLAT-01)

- **empty:** No domain UI rows required; shadcn shell alone is enough for Phase 1.
- **adjacency:** N/A for UI kit init.
- **ordering:** N/A.
</assumptions>

<tasks>

<task type="auto">
  <name>Task 1: Initialize shadcn/ui + cn helper</name>
  <reversibility rating="costly">D-04 locks UI primitive conventions for later phases.</reversibility>
  <files>components.json, src/lib/utils.ts, src/components/ui/.gitkeep, src/app/globals.css, package.json, package-lock.json</files>
  <read_first>
    .planning/phases/01-docker-sqlite-foundation/01-CONTEXT.md
    .planning/phases/01-docker-sqlite-foundation/01-RESEARCH.md
    .planning/phases/01-docker-sqlite-foundation/01-PATTERNS.md
    src/app/layout.tsx
    package.json
  </read_first>
  <action>
    Per D-04, run shadcn init for the Next App Router project (Tailwind path consistent with Plan 01 scaffold). Ensure src/lib/utils.ts exports cn via clsx and tailwind-merge. Keep Phase 1 component usage light: create src/components/ui/ directory (gitkeep or one primitive such as button if init adds it). Ensure src/app/globals.css exists from init/Tailwind. Do not build domain CRUD UI. Leave src/app/page.tsx as a temporary shell page; Plan 04 replaces copy with ready-page proof «Кошелёк готов».
  </action>
  <verify>
    <automated>test -f components.json && test -f src/lib/utils.ts && grep -q 'export function cn\|export const cn' src/lib/utils.ts && test -d src/components/ui && test -f src/app/globals.css</automated>
    <fails_when>exit non-zero if components.json, utils cn export, components/ui directory, or globals.css missing</fails_when>
  </verify>
  <acceptance_criteria>
    - components.json exists from shadcn init
    - src/lib/utils.ts exports cn
    - src/components/ui directory exists
    - src/app/globals.css exists
  </acceptance_criteria>
  <done>shadcn files and cn helper committed</done>
</task>

<task type="auto">
  <name>Task 2: Wire globals.css into App Router layout</name>
  <files>src/app/layout.tsx, src/app/globals.css</files>
  <read_first>
    src/app/layout.tsx
    src/app/globals.css
    components.json
  </read_first>
  <action>
    Ensure root layout imports ./globals.css (or the path shadcn/Tailwind expects) so the stylesheet chain is live for Plan 04 SSR ready page. Do not add interactive CRUD or extra marketing sections. Keep layout minimal per D-04 Phase 1 light usage.
  </action>
  <verify>
    <automated>test -f src/app/layout.tsx && grep -q 'globals.css' src/app/layout.tsx && test -f src/app/globals.css</automated>
    <fails_when>exit non-zero if layout lacks globals.css import or globals.css file missing</fails_when>
  </verify>
  <acceptance_criteria>
    - src/app/layout.tsx imports globals.css
    - Scaffold still has no domain CRUD UI
  </acceptance_criteria>
  <done>shadcn shell ready for Plan 04 ready page</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| npm registry → UI deps | shadcn-related packages (clsx, tailwind-merge, cva) enter trust zone |
| Repo tree → future container image | CSS/components become image build inputs |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-01-SC | Tampering | shadcn/tailwind helper installs | medium | mitigate | Install only via official shadcn init against already-pinned Next tree from Plan 01; no unpinned prisma upgrades |
| T-01-01 | Information Disclosure | future Compose publish | high | accept | No ports in this plan; Plan 04 enforces 127.0.0.1 bind |
</threat_model>

## Artifacts this phase produces

| Symbol / path | Kind | Introduced in |
|---------------|------|---------------|
| `components.json` | config | 01-02 |
| `cn` in `src/lib/utils.ts` | function | 01-02 |
| `src/app/globals.css` | stylesheet | 01-02 |
| `src/components/ui/` | directory | 01-02 |

<verification>
components.json, cn export, globals.css, and layout import present.
</verification>

<success_criteria>
D-04 shadcn shell initialized and stylesheet wired; ready for money schema and Docker plans.
</success_criteria>

<output>
Create `.planning/phases/01-docker-sqlite-foundation/01-02-SUMMARY.md` when done
</output>
