---
phase: 01-docker-sqlite-foundation
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - package.json
  - package-lock.json
  - next.config.ts
  - tsconfig.json
  - vitest.config.ts
  - src/app/layout.tsx
  - src/app/page.tsx
  - .gitignore
  - .dockerignore
  - .env.example
  - data/.gitkeep
  - scripts/smoke-persist.sh
  - src/lib/money.test.ts
autonomous: false
requirements:
  - PLAT-01
estimate:
  tokens: 35000
  raw_tokens: 35000
  tasks: 2
  confidence: low
must_haves:
  truths:
    - Approved package pins for Next 16.3.4 and Prisma 7.10.0 stack exist in package.json (no prisma@8 RC)
    - Next App Router project with src/ and standalone-ready next.config exists
    - Wave 0 Vitest harness runs and money convention tests exist (may fail until Plan 03)
  artifacts:
    - path: package.json
      provides: pinned Next/Prisma/vitest dependency set and test script
    - path: vitest.config.ts
      provides: Vitest runner config
    - path: src/lib/money.test.ts
      provides: failing-or-pending RATE_SCALE_E8 and schema convention tests
    - path: scripts/smoke-persist.sh
      provides: smoke script scaffold for later persist proof
  key_links:
    - from: package.json
      to: next@16.3.4 + prisma@7.10.0
      via: exact version pins after legitimacy gate
      pattern: "16.3.4|7.10.0"
---

<objective>
**As a** local Wallet user, **I want to** have an approved Next.js + Prisma scaffold with Wave 0 tests, **so that** later plans can add shadcn, lock SQLite money conventions, and prove Docker persistence (PLAT-01).

Purpose: Clear supply-chain gate (RESEARCH Package Legitimacy) and land the App Router shell (D-01) before shadcn, schema, or Docker work.
Output: Tracked Next app skeleton, pinned deps, Vitest harness, smoke script stub.
</objective>

<execution_context>
@/home/artem/Documents/wallet/.cursor/gsd-core/workflows/execute-plan.md
@/home/artem/Documents/wallet/.cursor/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-docker-sqlite-foundation/01-CONTEXT.md
@.planning/phases/01-docker-sqlite-foundation/01-RESEARCH.md
@.planning/phases/01-docker-sqlite-foundation/01-PATTERNS.md
@.planning/phases/01-docker-sqlite-foundation/01-VALIDATION.md
</context>

<assumptions>
## Planner assumptions (specless probe edges for PLAT-01)

- **empty:** After migrate, an empty domain-row database (schema-only) is a valid healthy state; readiness does not require seed rows.
- **adjacency:** Host `./data` and container `/data` meet only via Compose bind-mount; one SQLite file identity (`wallet.db`), no multi-DB merge semantics.
- **ordering:** Element-ordering probes do not apply to a single SQLite file; file identity is path-stable under the mount.
</assumptions>

<tasks>

<task type="checkpoint:human-verify" gate="blocking-human">
  <name>Task 1: Confirm SUS package legitimacy before install</name>
  <read_first>
    .planning/phases/01-docker-sqlite-foundation/01-RESEARCH.md
    .planning/phases/01-docker-sqlite-foundation/01-CONTEXT.md
  </read_first>
  <what-built>Supply-chain gate before installing Next 16.3.4 and Prisma 7.10.0 stack (D-01, D-03; threat T-01-SC)</what-built>
  <how-to-verify>
    1. Open npmjs.com/package/next and confirm pin 16.3.4 is official Vercel package
    2. Open npmjs.com/package/prisma, @prisma/client, @prisma/adapter-better-sqlite3 — confirm 7.10.0 each; do not approve 8.x RC as Phase 1 baseline
    3. Open npmjs.com/package/better-sqlite3 — confirm 13.0.3 is WiseLibs package
    4. Reply approved with those exact pins (skip lucide-react unless needed later)
  </how-to-verify>
  <action>
    Halt until a human confirms the RESEARCH Package Legitimacy Audit for [SUS] packages before any npm install of the app stack. Human must open npmjs.com pages for: next (pin 16.3.4), prisma and @prisma/client and @prisma/adapter-better-sqlite3 (pin 7.10.0 each), better-sqlite3 (pin 13.0.3). Confirm each is the official package (vercel/next.js, prisma/prisma, WiseLibs/better-sqlite3), not a typosquat, and that prisma dist-tag latest is NOT installed if it resolves to an 8.x RC. Optional lucide-react: skip install in Phase 1 unless ready page needs an icon. Record confirmation in the resume signal. Threat T-01-SC.
  </action>
  <resume-signal>Reply: approved pins next@16.3.4 prisma@7.10.0 @prisma/client@7.10.0 @prisma/adapter-better-sqlite3@7.10.0 better-sqlite3@13.0.3</resume-signal>
  <verify>
    <automated>test -f .planning/phases/01-docker-sqlite-foundation/01-CONTEXT.md && grep -c 'D-01' .planning/phases/01-docker-sqlite-foundation/01-CONTEXT.md | grep -q '[1-9]'</automated>
    <fails_when>exit non-zero if CONTEXT missing or D-01 citation absent (checkpoint cannot proceed without locked stack context)</fails_when>
  </verify>
  <acceptance_criteria>
    - Human explicitly approved pins: next@16.3.4, prisma@7.10.0, @prisma/client@7.10.0, @prisma/adapter-better-sqlite3@7.10.0, better-sqlite3@13.0.3
    - Human rejected installing prisma@8 RC / unpinned latest for Prisma CLI
  </acceptance_criteria>
  <done>Package legitimacy gate passed; executor may run npm installs with pinned versions only</done>
</task>

<task type="auto">
  <name>Task 2: Scaffold Next App Router + pinned deps + Wave 0 Vitest</name>
  <reversibility rating="costly">D-01 Next.js and D-03 Prisma pins shape Dockerfile, routing, and all later UI/API; undo is a coordinated stack rewrite.</reversibility>
  <files>package.json, package-lock.json, next.config.ts, tsconfig.json, vitest.config.ts, src/app/layout.tsx, src/app/page.tsx, .gitignore, .dockerignore, .env.example, data/.gitkeep, scripts/smoke-persist.sh, src/lib/money.test.ts</files>
  <read_first>
    .planning/phases/01-docker-sqlite-foundation/01-CONTEXT.md
    .planning/phases/01-docker-sqlite-foundation/01-RESEARCH.md
    .planning/phases/01-docker-sqlite-foundation/01-PATTERNS.md
    .planning/phases/01-docker-sqlite-foundation/01-VALIDATION.md
  </read_first>
  <action>
    After Task 1 approval, scaffold a TypeScript App Router app in the repo root with src/ directory and @/* alias (create-next-app into current tree or equivalent file set). Pin runtime deps per D-01 and RESEARCH: next@16.3.4, react@19.2.8, react-dom@19.2.8, and add (but do not fully wire Prisma client until Plan 03) prisma@7.10.0, @prisma/client@7.10.0, @prisma/adapter-better-sqlite3@7.10.0, better-sqlite3@13.0.3, zod@4.5.4, dotenv@17.4.2 as dependencies; vitest@4.1.11 and @types/better-sqlite3 as devDependencies. Set next.config.ts output to standalone per D-01 and include outputFileTracingIncludes for prisma, @prisma, and better-sqlite3 paths from RESEARCH. Add npm script "test": "vitest run". Create vitest.config.ts targeting src. Create src/lib/money.test.ts that asserts RATE_SCALE_E8 equals 100000000n once exported from src/lib/money.ts, and that documents expected schema conventions (BigInt money/rate fields; Currency.scale required) — tests may fail until Plan 03 creates money.ts and schema (RED Wave 0). Create scripts/smoke-persist.sh as executable scaffold that exits 1 with message that Docker persist smoke is not wired yet (Plan 04 completes it). Create data/.gitkeep; gitignore data/*.db, data/*-wal, data/*-shm, and src/generated/. Add .env.example with DATABASE_URL=file:./data/wallet.db for host-side tooling. Add .dockerignore excluding node_modules, .next, data/*.db. Do not create Dockerfile, prisma schema, or shadcn init in this plan (Plans 02–04).
  </action>
  <verify>
    <automated>test -f package.json && node -e "const p=require('./package.json'); const n=p.dependencies.next; if(n!=='16.3.4'&&n!=='^16.3.4') process.exit(1); const pr=p.devDependencies?.prisma||p.dependencies?.prisma; if(!String(pr).includes('7.10.0')) process.exit(1);" && test -f vitest.config.ts && test -f src/lib/money.test.ts && test -f scripts/smoke-persist.sh && test -f data/.gitkeep && grep -q "output: ['\"]standalone['\"]\|output: \"standalone\"" next.config.ts</automated>
    <fails_when>exit 1 when next pin missing 16.3.4, prisma pin missing 7.10.0, vitest.config.ts or money.test.ts or smoke script or data/.gitkeep absent, or next.config lacks standalone output</fails_when>
  </verify>
  <acceptance_criteria>
    - package.json lists next 16.3.4 and prisma 7.10.0 (dependency or devDependency)
    - next.config.ts enables standalone output
    - vitest.config.ts and src/lib/money.test.ts exist
    - scripts/smoke-persist.sh exists and is executable intent (mode +x preferred)
    - data/.gitkeep exists; .gitignore ignores SQLite runtime files under data/
    - No Dockerfile yet (owned by Plan 04)
    - No components.json yet (owned by Plan 02)
  </acceptance_criteria>
  <done>Next scaffold and Wave 0 harness committed with locked package pins</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Developer workstation → npm registry | Package tarballs cross into the trust zone at install |
| Repo tree → future container image | Scaffold files become image build inputs |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-01-SC | Tampering | npm installs (next, prisma, adapters, better-sqlite3) | high | mitigate | blocking-human legitimacy checkpoint; pin 7.10.0 / 16.3.4; forbid Prisma 8 RC |
| T-01-01 | Information Disclosure | future Compose publish | high | mitigate | Document 127.0.0.1 bind in .env.example notes; enforce in Plan 04 Compose |
| T-01-02 | Information Disclosure | data/ host directory | medium | mitigate | gitignore DB files; chmod guidance deferred to Plan 04 data volume task |
| T-01-03 | Tampering | DATABASE_URL | medium | mitigate | .env.example fixed file:./data/wallet.db only; no user-supplied path API |
| T-01-04 | Elevation of Privilege | container user | high | mitigate | No Dockerfile in this plan; Plan 04 must use USER node |
| T-01-05 | Tampering | baked demo DB | medium | mitigate | data/.gitkeep only; no committed wallet.db |
</threat_model>

## Artifacts this phase produces

| Symbol / path | Kind | Introduced in |
|---------------|------|---------------|
| `package.json` scripts `test`, `build`, `start` | config | 01-01 |
| `next.config.ts` `output: "standalone"` | config | 01-01 |
| `vitest.config.ts` | config | 01-01 |
| `src/lib/money.test.ts` | test | 01-01 |
| `scripts/smoke-persist.sh` | script scaffold | 01-01 |
| `data/.gitkeep` | mount target | 01-01 |

<verification>
Wave 0 files exist; package pins match RESEARCH; `npm test` may fail until Plan 03 money module lands.
</verification>

<success_criteria>
Human-approved pins installed; Next scaffold present; Wave 0 test and smoke scaffolds tracked.
</success_criteria>

<output>
Create `.planning/phases/01-docker-sqlite-foundation/01-01-SUMMARY.md` when done
</output>
