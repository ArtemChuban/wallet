---
phase: 01-docker-sqlite-foundation
plan: 03
type: execute
wave: 3
depends_on:
  - 01-02
files_modified:
  - Dockerfile
  - docker-compose.yml
  - docker/entrypoint.sh
  - src/lib/db.ts
  - src/app/api/health/route.ts
  - src/app/page.tsx
  - src/app/layout.tsx
  - scripts/smoke-persist.sh
  - README.md
autonomous: true
requirements:
  - PLAT-01
estimate:
  tokens: 55000
  raw_tokens: 55000
  tasks: 3
  confidence: low
must_haves:
  truths:
    - User can docker compose up and open the ready UI at http://127.0.0.1:3000/
    - SQLite file remains on host ./data after compose down and up; health returns 200
    - Readiness is 200 only when DB reachable and migrations applied; otherwise 503
  artifacts:
    - path: Dockerfile
      provides: multi-stage bookworm build + bookworm-slim runner with USER node
    - path: docker-compose.yml
      provides: web service, 127.0.0.1:3000 publish, ./data:/data, healthcheck
    - path: docker/entrypoint.sh
      provides: prisma migrate deploy then node server.js
    - path: src/app/api/health/route.ts
      provides: readiness Route Handler
    - path: src/app/page.tsx
      provides: Russian ready page proving UI + DB
    - path: scripts/smoke-persist.sh
      provides: automated persist smoke
  key_links:
    - from: docker/entrypoint.sh
      to: prisma migrate deploy
      via: pre-start blocking migrate
      pattern: "migrate deploy"
    - from: docker-compose.yml
      to: src/app/api/health/route.ts
      via: healthcheck fetch
      pattern: "/api/health"
    - from: volumes ./data:/data
      to: DATABASE_URL file:/data/wallet.db
      via: Compose environment
      pattern: "file:/data/wallet.db"
---

<objective>
**As a** local Wallet user, **I want to** start Wallet with Docker Compose and keep SQLite on my host, **so that** my data survives container stop/start and readiness reflects a migrated database (PLAT-01).

Purpose: Walking-skeleton tracer through Docker → migrate-on-start → health → ready UI → persist smoke (D-02, D-05, D-06).
Output: Dockerfile, Compose, entrypoint, health route, ready page, working smoke-persist.sh.
</objective>

<execution_context>
@/home/artem/Documents/wallet/.cursor/gsd-core/workflows/execute-plan.md
@/home/artem/Documents/wallet/.cursor/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/01-docker-sqlite-foundation/01-CONTEXT.md
@.planning/phases/01-docker-sqlite-foundation/01-RESEARCH.md
@.planning/phases/01-docker-sqlite-foundation/01-PATTERNS.md
@.planning/phases/01-docker-sqlite-foundation/01-SKELETON.md
@.planning/phases/01-docker-sqlite-foundation/01-02-SUMMARY.md
</context>

<assumptions>
## Planner assumptions (specless probe edges for PLAT-01)

- **empty:** Fresh volume after first migrate is healthy; ready page may show DB ok with zero domain rows.
- **adjacency:** Compose bind `./data:/data` makes host `./data/wallet.db` and container `/data/wallet.db` the same inode via mount; no second DB file.
- **ordering:** N/A beyond Compose healthcheck retry order (start_period then interval retries).
</assumptions>

<tasks>

<task type="tracer">
  <name>End-to-end Docker ready path — compose up, migrate, health, UI</name>
  <reversibility rating="costly">D-02 migrate-on-start entrypoint and D-05 image topology are Compose contracts reused by every later phase.</reversibility>
  <files>Dockerfile, docker-compose.yml, docker/entrypoint.sh, src/lib/db.ts, src/app/api/health/route.ts, src/app/page.tsx, src/app/layout.tsx, next.config.ts</files>
  <read_first>
    .planning/phases/01-docker-sqlite-foundation/01-CONTEXT.md
    .planning/phases/01-docker-sqlite-foundation/01-RESEARCH.md
    .planning/phases/01-docker-sqlite-foundation/01-PATTERNS.md
    .planning/phases/01-docker-sqlite-foundation/01-SKELETON.md
    prisma/schema.prisma
    src/lib/db.ts
    next.config.ts
  </read_first>
  <action>
    Wire ONE production-quality path through every layer this phase owns (D-01, D-02, D-05, D-06). Create multi-stage Dockerfile: node:24-bookworm deps/builder with python3/make/g++ for better-sqlite3; runner node:24-bookworm-slim with openssl/ca-certificates; copy standalone output, static, public, prisma/, prisma.config.ts, and whatever Prisma CLI bits migrate needs (tracing includes and/or explicit COPY); USER node; ENTRYPOINT docker/entrypoint.sh. Entrypoint: set -e; run prisma migrate deploy against DATABASE_URL; then exec node server.js (D-02). Compose service web: build ., ports 127.0.0.1:3000:3000 only, environment DATABASE_URL=file:/data/wallet.db, volume ./data:/data, healthcheck curling or node-fetching http://127.0.0.1:3000/api/health with start_period at least 40s. Implement GET src/app/api/health/route.ts returning 200 JSON status ok only when prisma can query SQLite and migrations are present (e.g. SELECT 1 plus _prisma_migrations count > 0); else 503 status not_ready. Ready page src/app/page.tsx shows Russian copy Кошелёк готов and a DB readiness signal from server-side prisma (not health-only). Ensure db.ts uses file:/data/wallet.db inside container via env. Do not add domain CRUD. Real error handling on this single path: entrypoint fails closed if migrate fails; health fails closed if DB missing.
  </action>
  <verify>
    <automated>docker compose build && docker compose up -d && for i in $(seq 1 40); do curl -sf http://127.0.0.1:3000/api/health && break; sleep 3; done && curl -sf http://127.0.0.1:3000/api/health | grep -q ok && curl -sf http://127.0.0.1:3000/ | grep -q 'Кошелёк готов' && test -f data/wallet.db</automated>
    <fails_when>compose build/up fails, health never returns 200 with ok within ~120s, ready page missing Кошелёк готов, or data/wallet.db absent on host after up</fails_when>
    <human-check>
      <what-built>Dockerized Wallet ready page plus /api/health</what-built>
      <how-to-verify>Open http://127.0.0.1:3000/ in a browser after compose up; confirm Russian ready copy and that the page indicates DB readiness</how-to-verify>
    </human-check>
  </verify>
  <acceptance_criteria>
    - Dockerfile uses bookworm build and bookworm-slim runner and contains USER node
    - docker-compose.yml publishes 127.0.0.1:3000:3000 and mounts ./data:/data
    - docker/entrypoint.sh runs migrate deploy before node server.js
    - curl http://127.0.0.1:3000/api/health returns 200 with ok after healthy start
    - curl http://127.0.0.1:3000/ body contains Кошелёк готов
    - Host path data/wallet.db exists while stack is up
  </acceptance_criteria>
  <done>Single happy path works end-to-end in Docker and is committed</done>
</task>

<task type="auto">
  <name>Task 2: Persist smoke + volume permission hardening</name>
  <files>scripts/smoke-persist.sh, README.md, data/.gitkeep, docker-compose.yml</files>
  <read_first>
    .planning/phases/01-docker-sqlite-foundation/01-RESEARCH.md
    .planning/phases/01-docker-sqlite-foundation/01-VALIDATION.md
    scripts/smoke-persist.sh
    docker-compose.yml
  </read_first>
  <action>
    Replace the Plan 01 smoke stub with a real scripts/smoke-persist.sh that: ensures data dir mode 700 when possible; docker compose up -d --build; waits for /api/health 200; records a marker (write a known BalanceAmountStub row via a one-shot node/prisma script or SQL through the running app pattern — prefer prisma execute / small script using src/lib/db.ts against file:./data/wallet.db on host); docker compose down; assert data/wallet.db still exists; docker compose up -d; wait for health 200; assert marker row still present; exit 0 on success. Document in README.md: docker compose up --build, open 127.0.0.1:3000, chmod 700 data, bind-mount contract, WAL default on native Linux with DELETE fallback note for virtiofs/NFS. Threat mitigations T-01-01 and T-01-02.
  </action>
  <verify>
    <automated>./scripts/smoke-persist.sh</automated>
    <fails_when>script exits non-zero; DB missing after down/up; health not 200; marker row missing after restart</fails_when>
  </verify>
  <acceptance_criteria>
    - ./scripts/smoke-persist.sh exits 0
    - After script, data/wallet.db exists on host
    - README.md documents compose up and 127.0.0.1:3000
    - data directory permission guidance mentions mode 700
  </acceptance_criteria>
  <done>Persist-across-restart proven by automated smoke</done>
</task>

<task type="auto">
  <name>Task 3: Readiness negative path + security bind assertions</name>
  <files>src/app/api/health/route.ts, docker-compose.yml, Dockerfile</files>
  <read_first>
    .planning/phases/01-docker-sqlite-foundation/01-RESEARCH.md
    src/app/api/health/route.ts
    docker-compose.yml
    Dockerfile
  </read_first>
  <action>
    Harden and prove failure direction for readiness (success criterion 3). Ensure health returns 503 when database file is unreachable (add a vitest with mocked prisma throwing, and/or a documented compose override test that points DATABASE_URL at a non-writable path and expects 503 — prefer unit/integration test under src/app/api/health/ if faster than full compose). Assert Compose ports line is exactly 127.0.0.1:3000:3000 (not 3000:3000). Assert Dockerfile contains USER node and does not COPY any *.db into the image. Confirm no demo DB is ADDed. Keep health read-only with no user path query params.
  </action>
  <verify>
    <automated>grep -q '127.0.0.1:3000:3000' docker-compose.yml && ! grep -E '^[[:space:]]*-[[:space:]]*"?3000:3000' docker-compose.yml && grep -q 'USER node' Dockerfile && ! grep -E 'COPY.*\.db|ADD.*\.db' Dockerfile && npm test</automated>
    <fails_when>localhost bind missing, wildcard 3000:3000 present, USER node missing, DB file copied in Dockerfile, or npm test fails</fails_when>
  </verify>
  <acceptance_criteria>
    - docker-compose.yml contains 127.0.0.1:3000:3000
    - Dockerfile contains USER node
    - Dockerfile has no COPY/ADD of .db files
    - Health negative-path coverage exists (test or scripted assertion of 503 on DB failure)
    - npm test exits 0
  </acceptance_criteria>
  <done>Security binds and readiness failure path verified</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Browser/LAN → Next on :3000 | Untrusted network clients may reach published port |
| Container process → /data SQLite | App UID writes durable finance data |
| Image build context → runner | Build artifacts must not include host DB |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-01-01 | Information Disclosure | docker-compose.yml ports | high | mitigate | Bind 127.0.0.1:3000:3000 only |
| T-01-02 | Information Disclosure | ./data host DB | medium | mitigate | chmod 700 guidance + smoke script; gitignore *.db |
| T-01-03 | Tampering | DATABASE_URL | high | mitigate | Compose fixes file:/data/wallet.db; health accepts no path input |
| T-01-04 | Elevation of Privilege | Dockerfile USER | high | mitigate | USER node in runner |
| T-01-05 | Tampering / Spoofing | image layers | medium | mitigate | Empty volume + migrate on start; never COPY wallet.db |
| T-01-SC | Tampering | image npm deps | high | mitigate | Pins from Plan 01; bookworm rebuild of better-sqlite3 only from lockfile |
</threat_model>

## Artifacts this phase produces

| Symbol / path | Kind | Introduced in |
|---------------|------|---------------|
| `Dockerfile` | image | 01-03 |
| `docker-compose.yml` `web` service | compose | 01-03 |
| `docker/entrypoint.sh` | entrypoint | 01-03 |
| `GET` handler in `src/app/api/health/route.ts` | route | 01-03 |
| Ready page copy `Кошелёк готов` | UI | 01-03 |
| `scripts/smoke-persist.sh` (complete) | smoke | 01-03 |

<verification>
docker compose path healthy; persist smoke green; security greps pass; npm test green.
</verification>

<success_criteria>
PLAT-01 success criteria 1–3 observable: UI via Compose, persist across restart, health gated on migrated DB.
</success_criteria>

<output>
Create `.planning/phases/01-docker-sqlite-foundation/01-03-SUMMARY.md` when done
</output>
