---
phase: 11-charts-primary-totals
plan: 03
type: execute
wave: 2
depends_on:
  - "11-01"
files_modified:
  - src/app/debts/page.tsx
  - src/components/debts/DebtsPrimaryTotalsHero.tsx
autonomous: true
requirements:
  - DTOTAL-01
estimate:
  tokens: 22000
  raw_tokens: 44000
  tasks: 2
  confidence: high
must_haves:
  truths:
    - /debts header always shows two side-by-side columns «Я должен» | «Мне должны» in primary currency, including when both amounts are 0 (D-10 / D-11 / D-12 / DTOTAL-01)
    - Totals use computeDebtPrimaryTotals on OPEN debts with LOCF rateToPrimaryScaled as-of today Europe/Moscow (Phase 8 D-16–D-19)
    - When isPartial, banner «Итог неполный» plus excluded-debt list with currency and reason (at least нет курса) appears (D-13)
    - statement: Hero remains visible on empty people/debts page with 0 / 0 primary amounts (D-12)
      verification: backstop
  prohibitions:
    - statement: MUST NOT hide the hero when totals are zero
      requirement_id: DTOTAL-01
      category: values
    - statement: MUST NOT call revalidatePath on dashboard root from debts totals wiring
      requirement_id: DTOTAL-01
      category: isolation
    - statement: MUST NOT include CLOSED debts in primary totals aggregates
      requirement_id: DTOTAL-01
      category: values
  artifacts:
    - path: src/app/debts/page.tsx
      provides: LOCF FX + computeDebtPrimaryTotals wiring + hero/banner render
    - path: src/components/debts/DebtsPrimaryTotalsHero.tsx
      provides: Я должен | Мне должны columns + optional partial list
  key_links:
    - from: src/app/debts/page.tsx
      to: src/lib/debts.ts
      via: computeDebtPrimaryTotals after LOCF rates
      pattern: "computeDebtPrimaryTotals"
    - from: src/app/debts/page.tsx
      to: src/components/debts/DebtsPrimaryTotalsHero.tsx
      via: formatted primary majors + excluded rows
      pattern: "DebtsPrimaryTotalsHero"
---

<objective>
Wire `/debts` primary totals hero and FX partial honesty list (DTOTAL-01 / D-10–D-13).

Purpose: Surface Phase 8 computeDebtPrimaryTotals in the section header with Капитал-style partial honesty.
Output: Always-visible Я должен | Мне должны hero + «Итог неполный» excluded-debt list when isPartial.
</objective>

<execution_context>
@/home/artem/Documents/wallet/.cursor/gsd-core/workflows/execute-plan.md
@/home/artem/Documents/wallet/.cursor/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/11-charts-primary-totals/11-CONTEXT.md
@.planning/phases/11-charts-primary-totals/11-RESEARCH.md
@.planning/phases/11-charts-primary-totals/11-PATTERNS.md
@src/app/debts/page.tsx
@src/app/page.tsx
@src/lib/debts.ts
@src/lib/locf.ts
@src/lib/dates.ts
@src/lib/money.ts
@src/components/debts/DebtsList.tsx
</context>

<assumptions>
## Planner assumptions (specless / discretion)

- A-edge DTOTAL-01 unclassified: OPEN-only aggregates via existing helper; missing FX → exclude + isPartial; join person name + currencyCode for excluded row labels (RESEARCH open question 3).
- Hero shows even with zero people/debts (D-12 / RESEARCH open question 4) — place above empty-state CTA.
- New presentational component DebtsPrimaryTotalsHero.tsx under components/debts (discretion); logic stays on RSC page.
- Existing computeDebtPrimaryTotals tests remain the automated math gate; page wiring verified by grep + helper suite.
</assumptions>

## Artifacts this phase produces

- `DebtsPrimaryTotalsHero` — two-column primary totals + partial banner/list
- `/debts` page LOCF FX as-of today → `computeDebtPrimaryTotals` → hero props

<tasks>

<task type="tracer">
  <name>End-to-end /debts LOCF → computeDebtPrimaryTotals → hero 0/0</name>
  <reversibility rating="reversible">Hero is additive RSC presentation over existing helper; removable without schema change.</reversibility>
  <files>src/app/debts/page.tsx, src/components/debts/DebtsPrimaryTotalsHero.tsx</files>
  <read_first>
    .planning/phases/11-charts-primary-totals/11-CONTEXT.md
    .planning/phases/11-charts-primary-totals/11-PATTERNS.md
    src/app/debts/page.tsx
    src/app/page.tsx
    src/lib/debts.ts
    src/lib/locf.ts
    src/lib/dates.ts
    src/lib/money.ts
    src/lib/debts.test.ts
  </read_first>
  <behavior>
    - Page loads OPEN debts with remainingMinor + currency scale/code, LOCF rates lte today via firstHitLocfMap, maps rateToPrimaryScaled into DebtPrimaryTotalsInput, calls computeDebtPrimaryTotals (DTOTAL-01 / D-11)
    - Hero always rendered above list with «Я должен» and «Мне должны» formatted primary amounts including 0/0 (D-10 / D-12)
    - Existing computeDebtPrimaryTotals unit tests still pass
  </behavior>
  <action>
    On src/app/debts/page.tsx (per D-10 / D-11 / D-12 / DTOTAL-01): mirror Капитал LOCF pattern — calendarDateToday Europe/Moscow, load FxRate rows lte today, firstHitLocfMap by currencyCode, build DebtPrimaryTotalsInput for each OPEN debt (remainingMinor from ledger helpers already used on page, rateToPrimaryScaled from LOCF or identity for primary). Call computeDebtPrimaryTotals. Create DebtsPrimaryTotalsHero.tsx with two side-by-side columns «Я должен» | «Мне должны» using formatMinorToMajor + primary code. Always render hero above DebtsList / empty CTA — never gate on non-zero totals or hasDebts. Preserve openedAsOf serialization from Plan 01. Do not import debts into net-worth modules. Do not enrich Капитал banner here (Plan 04).
  </action>
  <verify>
    <automated>npx vitest run src/lib/debts.test.ts -t computeDebtPrimaryTotals && grep -q 'computeDebtPrimaryTotals' src/app/debts/page.tsx && grep -q 'Я должен' src/components/debts/DebtsPrimaryTotalsHero.tsx && grep -q 'Мне должны' src/components/debts/DebtsPrimaryTotalsHero.tsx && grep -q 'DebtsPrimaryTotalsHero' src/app/debts/page.tsx</automated>
    <fails_when>vitest non-zero, page missing computeDebtPrimaryTotals, hero missing RU column labels, or page not mounting DebtsPrimaryTotalsHero</fails_when>
  </verify>
  <acceptance_criteria>
    - src/app/debts/page.tsx calls computeDebtPrimaryTotals and renders DebtsPrimaryTotalsHero unconditionally in the header region
    - Hero component contains both «Я должен» and «Мне должны»
    - computeDebtPrimaryTotals vitest describe stays green
  </acceptance_criteria>
  <done>Always-visible /debts primary hero wired through LOCF + computeDebtPrimaryTotals</done>
</task>

<task type="auto">
  <name>Partial banner + excluded debt list on /debts</name>
  <files>src/components/debts/DebtsPrimaryTotalsHero.tsx, src/app/debts/page.tsx</files>
  <read_first>
    .planning/phases/11-charts-primary-totals/11-CONTEXT.md
    .planning/phases/11-charts-primary-totals/11-PATTERNS.md
    src/app/page.tsx
    src/components/dashboard/DashboardAccountList.tsx
    src/components/debts/DebtsPrimaryTotalsHero.tsx
    src/lib/debts.ts
  </read_first>
  <behavior>
    - When isPartial true: show «Итог неполный» status banner plus list of excluded debts with identity, currencyCode, and reason (нет курса for no_fx) (D-13 / DTOTAL-01)
    - When isPartial false: omit banner/list
    - Excluded rows come from computeDebtPrimaryTotals rows where includedInTotal is false, joined to person name + currency on the page
  </behavior>
  <action>
    Extend DebtsPrimaryTotalsHero (per D-13): when isPartial, render Капитал-style banner container (rounded-lg border bg-muted/60 role=status) with title «Итог неполный», short RU helper, and ul of excluded debts showing person name, currencyCode, and reason text for no_fx (discretion wording OK if currency visible). Page passes excluded row view-models joined from totals.rows. Do not change helper math. Do not touch src/app/page.tsx Капитал banner (Plan 04 / D-14).
  </action>
  <verify>
    <automated>npx vitest run src/lib/debts.test.ts -t computeDebtPrimaryTotals && grep -q 'Итог неполный' src/components/debts/DebtsPrimaryTotalsHero.tsx && grep -q 'isPartial' src/components/debts/DebtsPrimaryTotalsHero.tsx && grep -q 'no_fx\|нет курса' src/components/debts/DebtsPrimaryTotalsHero.tsx</automated>
    <fails_when>vitest non-zero, banner title missing, isPartial not referenced, or excluded reason/currency cue absent</fails_when>
  </verify>
  <acceptance_criteria>
    - DebtsPrimaryTotalsHero shows «Итог неполный» only in the partial path and lists excluded debts with currency/reason
    - computeDebtPrimaryTotals suite still green
  </acceptance_criteria>
  <done>/debts partial honesty lists excluded debts with currency reason (D-13)</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| SQLite FX + debts → RSC totals | Server-trusted DB reads; no client-supplied rates for aggregates |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-11-04 | Information disclosure | Excluded debt list | low | accept | Single-user local app; list shows person/currency already visible on /debts |
| T-11-01 | Tampering | Totals inputs | low | accept | Inputs built server-side from Prisma + LOCF; no FormData path |
| T-11-SC | Tampering | npm installs | low | accept | No new packages |
</threat_model>

<verification>
Helper vitest + greps for computeDebtPrimaryTotals, hero labels, and partial banner.
</verification>

<success_criteria>
/debts always shows Я должен | Мне должны in primary; missing FX yields «Итог неполный» with named excluded debts.
</success_criteria>

<output>
Create `.planning/phases/11-charts-primary-totals/11-03-SUMMARY.md` when done
</output>
