---
phase: 11-charts-primary-totals
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - prisma/schema.prisma
  - prisma/migrations/
  - src/lib/debts.ts
  - src/lib/debts.test.ts
  - src/lib/validations/debts.ts
  - src/lib/validations/debts.test.ts
  - src/app/debts/actions.ts
  - src/app/debts/page.tsx
  - src/components/debts/DebtFormDialog.tsx
  - src/components/debts/DebtDetailDialog.tsx
  - src/components/debts/DebtPrincipalStackChart.tsx
autonomous: false
requirements:
  - DCHART-01
  - DCHART-02
  - DISOL-01
estimate:
  tokens: 45000
  raw_tokens: 90000
  tasks: 3
  confidence: high
must_haves:
  truths:
    - Debt create requires calendar openedAsOf (YYYY-MM-DD); series and chart start from that date, not wall-clock createdAt alone (D-08 / DCHART-01)
    - «История» tab shows one stacked chart (repaidCumulative + remaining = principal) above the event timeline (D-01 / D-02 / D-03 / DCHART-01 / DCHART-02)
    - With no events, chart is flat from openedAsOf to today Europe/Moscow at initial remaining (D-04)
    - Chart series uses native majors only — primary FX is not applied to stack points (DCHART-01 / RESEARCH pitfall)
    - statement: Debt chart lives only in existing История tab — no second chart surface and no new Графики tab (D-01 / D-03)
      verification: backstop
    - statement: NW math and historical-series builders stay free of debt-domain imports after chart lands (DISOL-01)
      verification: backstop
  prohibitions:
    - statement: MUST NOT add a second debt chart or a new chart tab
      requirement_id: DCHART-01
      category: values
    - statement: MUST NOT convert debt chart series through primary FX rates
      requirement_id: DCHART-01
      category: values
    - statement: MUST NOT import debt series helpers into historical-series.ts or net-worth.ts
      requirement_id: DISOL-01
      category: isolation
    - statement: MUST NOT change computeNetWorthRows or NW chart math when wiring debt charts
      requirement_id: DISOL-01
      category: isolation
  artifacts:
    - path: prisma/schema.prisma
      provides: Debt.openedAsOf String YYYY-MM-DD required
    - path: prisma/migrations/
      provides: debt_opened_as_of migration with backfill
    - path: src/lib/debts.ts
      provides: buildDebtPrincipalStackSeries (+ point type)
    - path: src/components/debts/DebtPrincipalStackChart.tsx
      provides: client stacked stepAfter Area chart
    - path: src/components/debts/DebtDetailDialog.tsx
      provides: chart mounted first inside История tabpanel
  key_links:
    - from: src/components/debts/DebtDetailDialog.tsx
      to: src/components/debts/DebtPrincipalStackChart.tsx
      via: history tabpanel chart-first render
      pattern: "DebtPrincipalStackChart"
    - from: src/components/debts/DebtPrincipalStackChart.tsx
      to: src/lib/debts.ts
      via: buildDebtPrincipalStackSeries points as chart data
      pattern: "buildDebtPrincipalStackSeries"
    - from: src/app/debts/actions.ts
      to: src/lib/validations/debts.ts
      via: createDebtSchema openedAsOf
      pattern: "openedAsOf"
---

## Phase Goal

Debt detail charts (native) and section-level primary totals with FX honesty.

<objective>
Ship the Phase 11 tracer: immutable Debt.openedAsOf (D-08) through create form/action, pure buildDebtPrincipalStackSeries, and one stacked native chart above the timeline in «История» (D-01–D-04 / DCHART-01 / DCHART-02) without NW coupling (DISOL-01).

Purpose: Prove open-date → series → chart end-to-end before hero totals and Капитал banner parity.
Output: migrated openedAsOf, green Wave 0→tracer series/Zod tests, DebtPrincipalStackChart in История.
</objective>

<execution_context>
@/home/artem/Documents/wallet/.cursor/gsd-core/workflows/execute-plan.md
@/home/artem/Documents/wallet/.cursor/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/11-charts-primary-totals/11-CONTEXT.md
@.planning/phases/11-charts-primary-totals/11-RESEARCH.md
@.planning/phases/11-charts-primary-totals/11-PATTERNS.md
@.planning/phases/11-charts-primary-totals/11-VALIDATION.md
@src/lib/debts.ts
@src/lib/debts.test.ts
@src/lib/validations/debts.ts
@src/lib/validations/debts.test.ts
@src/app/debts/actions.ts
@src/app/debts/page.tsx
@src/components/debts/DebtDetailDialog.tsx
@src/components/debts/DebtFormDialog.tsx
@src/components/dashboard/NetWorthHistoryChart.tsx
@src/components/ui/chart.tsx
@prisma/schema.prisma
</context>

<assumptions>
## Planner assumptions (specless / discretion)

- A-edge DCHART-01/02 unclassified: one stacked Area (repaidMajor + remainingMajor) satisfies both requirements per CONTEXT interpretation (D-03).
- A3 RESEARCH: emit one chart point per distinct asOfDate after applying that day's events in D-07 order (end-of-day collapse).
- A4 RESEARCH: tertiary sort key kind with repayment before sizeChange when (asOfDate, id) collide across tables.
- A1/A2: field name openedAsOf; backfill from createdAt as Europe/Moscow calendar date in migration SQL/script.
- A5/A6: series keys repaidMajor/remainingMajor; chart component under src/components/debts/.
- Assumption-delta scan 11: detected=false (adding openedAsOf field, not pluralizing identity).
- Flagged: allow any valid YYYY-MM-DD for openedAsOf like dueDate (no future ban unless product later adds it).
</assumptions>

<assumption_delta_decision>
assumption-delta scan 11 returned detected=false — no singular→plural identity rename. Proceed with additive Debt.openedAsOf only.
</assumption_delta_decision>

## Artifacts this phase produces

- `Debt.openedAsOf` — required YYYY-MM-DD on Prisma Debt + migration backfill
- `buildDebtPrincipalStackSeries` / `DebtPrincipalStackPoint` in `src/lib/debts.ts`
- `DebtPrincipalStackChart` client component
- Create-form «Дата» (`openedAsOf`) + createDebt persistence
- Chart-first «История» panel in `DebtDetailDialog`
- Later plans: `/debts` hero (11-03), Капитал excluded list (11-04), update immutability polish (11-02)

<tasks>

<task type="checkpoint:decision" gate="blocking-human">
  <name>Confirm one-way Debt.openedAsOf schema + Moscow backfill (D-08)</name>
  <decision>Add required Debt.openedAsOf String (YYYY-MM-DD) with migration backfill for existing rows; series start uses this field (CONTEXT D-08).</decision>
  <context>
    One-way door: SQLite reshape + data backfill. Undo needs another migration and rewrite of create form, Zod, series, and chart start. CONTEXT already locked the product behavior; this gate confirms field name and backfill TZ before SQL lands. Auto-mode must not auto-pick — gate is blocking-human.
  </context>
  <options>
    <option id="openedAsOf-moscow-backfill">
      <name>openedAsOf String + Europe/Moscow backfill from createdAt (RECOMMENDED)</name>
      <pros>Matches CONTEXT D-08 naming discretion; product TZ consistent with calendarDateToday; clear series start</pros>
      <cons>Requires SQLite redefine-table migration + Prisma generate</cons>
    </option>
    <option id="openedAsOf-utc-date">
      <name>openedAsOf String + UTC date(createdAt) backfill</name>
      <pros>Simpler SQL date()</pros>
      <cons>Off-by-one vs Moscow calendar near midnight (RESEARCH A2 risk)</cons>
    </option>
    <option id="createdAt-only">
      <name>No schema field — derive open day from createdAt at read time</name>
      <pros>No migration</pros>
      <cons>Contradicts locked D-08 past-dated create; cannot set open date independent of wall clock</cons>
    </option>
  </options>
  <resume-signal>Select: openedAsOf-moscow-backfill (recommended), openedAsOf-utc-date, or createdAt-only</resume-signal>
</task>

<task type="auto" tdd="true">
  <name>Wave 0: red buildDebtPrincipalStackSeries + openedAsOf Zod tests</name>
  <files>src/lib/debts.test.ts, src/lib/validations/debts.test.ts</files>
  <read_first>
    .planning/phases/11-charts-primary-totals/11-VALIDATION.md
    .planning/phases/11-charts-primary-totals/11-RESEARCH.md
    .planning/phases/11-charts-primary-totals/11-PATTERNS.md
    src/lib/debts.test.ts
    src/lib/debts.ts
    src/lib/validations/debts.test.ts
    src/lib/validations/debts.ts
  </read_first>
  <behavior>
    - buildDebtPrincipalStackSeries: seed at openedAsOf with repaid=0 remaining=initial; flat to today when no events; after repayment remaining drops and repaid rises; size-change changes stack height without a third series; one point per distinct asOfDate; order asOfDate then id then kind; every point repaid+remaining equals current principal (D-03 / D-04 / D-06 / D-07 / DCHART-01 / DCHART-02)
    - createDebtSchema / createDebtWithNewPersonSchema require openedAsOf matching YYYY-MM-DD (D-08)
    - updateDebtMetaSchema shape has no openedAsOf property; .strict() rejects smuggled openedAsOf (D-09)
  </behavior>
  <action>
    After checkpoint selects openedAsOf-moscow-backfill (per D-08): extend src/lib/debts.test.ts with describe buildDebtPrincipalStackSeries covering open→today flat, single repayment step, size-change height change, multi-day sequence, same-day multi-event collapse to one point, and asOfDate/id/kind ordering. Extend src/lib/validations/debts.test.ts so create schemas require openedAsOf and update schema omits/rejects it (mirror initialAmountMajor immutability tests). Do not implement production series or Zod field yet — suites must fail until tracer. Do not add /debts hero or Капитал banner tests here.
  </action>
  <verify>
    <automated>npx vitest run src/lib/debts.test.ts src/lib/validations/debts.test.ts; test $? -ne 0</automated>
    <fails_when>vitest exits 0 (Wave 0 already green) or buildDebtPrincipalStackSeries / openedAsOf describe blocks missing</fails_when>
  </verify>
  <acceptance_criteria>
    - grep -q 'buildDebtPrincipalStackSeries' src/lib/debts.test.ts
    - grep -q 'openedAsOf' src/lib/validations/debts.test.ts
    - npx vitest run src/lib/debts.test.ts src/lib/validations/debts.test.ts exits non-zero before tracer lands
  </acceptance_criteria>
  <done>Red Wave 0 tests exist for stack series and openedAsOf create/update Zod contracts</done>
</task>

<task type="tracer">
  <name>End-to-end openedAsOf → stack series → История chart</name>
  <reversibility rating="one-way">Debt.openedAsOf is on-disk SQLite shape with backfilled rows; undoing requires another migration plus create/series/chart rewrites (D-08).</reversibility>
  <files>prisma/schema.prisma, prisma/migrations/, src/lib/debts.ts, src/lib/debts.test.ts, src/lib/validations/debts.ts, src/lib/validations/debts.test.ts, src/app/debts/actions.ts, src/app/debts/page.tsx, src/components/debts/DebtFormDialog.tsx, src/components/debts/DebtDetailDialog.tsx, src/components/debts/DebtPrincipalStackChart.tsx</files>
  <read_first>
    .planning/phases/11-charts-primary-totals/11-CONTEXT.md
    .planning/phases/11-charts-primary-totals/11-RESEARCH.md
    .planning/phases/11-charts-primary-totals/11-PATTERNS.md
    prisma/schema.prisma
    prisma/migrations/20260903005200_account_credit_limit_check/migration.sql
    src/lib/debts.ts
    src/lib/validations/debts.ts
    src/app/debts/actions.ts
    src/app/debts/page.tsx
    src/components/debts/DebtFormDialog.tsx
    src/components/debts/DebtDetailDialog.tsx
    src/components/dashboard/NetWorthHistoryChart.tsx
    src/lib/dates.ts
    src/lib/money.ts
    node_modules/next/dist/docs/01-app/01-getting-started/07-mutating-data.md
  </read_first>
  <behavior>
    - Debt.openedAsOf required String; migration backfills existing rows; createDebtSchema requires openedAsOf; createDebt persists it; create form shows required «Дата» defaulting to calendarDateToday Europe/Moscow (D-08)
    - buildDebtPrincipalStackSeries implements D-04/D-06/D-07 algorithm in native minors converted to majors at boundary; Wave 0 series tests pass (DCHART-01 / DCHART-02)
    - DebtPrincipalStackChart: two Area layers same stackId, type stepAfter, default stackOffset (not sign), no RangePreset chrome (D-03 / D-05)
    - DebtDetailDialog История tabpanel renders chart above timeline/empty copy (D-01 / D-02); empty timeline still shows flat chart
    - /debts page serializes openedAsOf onto debt rows for detail; revalidatePath stays /debts only
    - Chart module does not import from historical-series or net-worth (DISOL-01)
  </behavior>
  <action>
    Implement per D-08 checkpoint choice: add Debt.openedAsOf String to prisma/schema.prisma; create SQLite redefine-table migration named debt_opened_as_of that backfills openedAsOf for existing debts from createdAt in Europe/Moscow (or UTC date if that option won). Run DATABASE_URL=file:./data/wallet.db npx prisma migrate deploy and regenerate client. Add openedAsOf: asOfDateSchema to createDebtSchema and createDebtWithNewPersonSchema; leave updateDebtMetaSchema without the field (.strict()). Persist openedAsOf in createDebt / compound create branches. Add create-only «Дата» input name=openedAsOf on DebtFormDialog (type=date, default calendarDateToday). Implement buildDebtPrincipalStackSeries in src/lib/debts.ts per RESEARCH Pattern 1 (seed open, step events, flat to today, one point per date, kind tertiary). Create DebtPrincipalStackChart.tsx using ChartContainer + AreaChart shell from NetWorthHistoryChart but stepAfter Areas and no sign stackOffset / no presets (D-03 / D-05). Mount chart first inside История tabpanel in DebtDetailDialog (D-01 / D-02); pass openedAsOf, events, scale, today. Serialize openedAsOf on src/app/debts/page.tsx debt props. Do not wire /debts hero or Капитал excluded list (Plans 03–04). Do not put debt imports into net-worth.ts or historical-series.ts (DISOL-01).
  </action>
  <verify>
    <automated>mkdir -p data && DATABASE_URL="file:./data/wallet.db" npx prisma migrate deploy && npx vitest run src/lib/debts.test.ts src/lib/validations/debts.test.ts && grep -q 'openedAsOf' prisma/schema.prisma && grep -q 'buildDebtPrincipalStackSeries' src/lib/debts.ts && grep -q 'DebtPrincipalStackChart' src/components/debts/DebtDetailDialog.tsx && grep -q 'stepAfter' src/components/debts/DebtPrincipalStackChart.tsx</automated>
    <fails_when>migrate deploy fails, vitest non-zero, openedAsOf missing from schema, series export missing, chart not mounted in DebtDetailDialog, or stepAfter absent from chart component</fails_when>
  </verify>
  <acceptance_criteria>
    - prisma/schema.prisma contains openedAsOf on Debt; migration applied (migrate status up to date)
    - create schemas require openedAsOf; Wave 0 series + Zod tests green
    - DebtFormDialog create path posts openedAsOf; DebtDetailDialog История shows DebtPrincipalStackChart above timeline
    - DebtPrincipalStackChart uses stepAfter stack Areas; no RangePreset controls in that file
    - grep -c "from '@/lib/debts'" src/lib/net-worth.ts src/lib/historical-series.ts stays 0 (DISOL-01)
  </acceptance_criteria>
  <done>OpenedAsOf migrated and create-wired; stack series green; one native stacked chart renders first in История</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Browser form → Server Action createDebt | Untrusted FormData including openedAsOf calendar string |
| Client chart props → Recharts render | Serialized majors/dates from RSC; notes must not become HTML |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-11-01 | Tampering | createDebt / openedAsOf Zod | medium | mitigate | Require YYYY-MM-DD via asOfDateSchema; reject malformed strings before Prisma write |
| T-11-02 | Tampering | updateDebtMeta mass-assignment | high | mitigate | Keep openedAsOf off updateDebtMetaSchema; .strict() rejects smuggled keys (Plan 02 hardens edit UI) |
| T-11-03 | XSS | DebtPrincipalStackChart tooltip | medium | mitigate | Text-only ChartTooltip labels (Погашено / Остаток); never inject debt.note as HTML |
| T-11-SC | Tampering | npm installs | high | accept | No new packages this phase — recharts@3.10.1 already pinned; Package Legitimacy Audit OK |
</threat_model>

<verification>
Wave 0 red then tracer green: series + Zod + migrate deploy + chart mount grep. DISOL import scan on net-worth.ts and historical-series.ts clean.
</verification>

<success_criteria>
User can create a debt with past/today open date and see a native stacked composition chart at the top of «История» for that debt; NW modules remain uncoupled.
</success_criteria>

<output>
Create `.planning/phases/11-charts-primary-totals/11-01-SUMMARY.md` when done
</output>
