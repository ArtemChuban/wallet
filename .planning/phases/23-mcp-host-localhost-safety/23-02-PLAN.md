---
phase: 23-mcp-host-localhost-safety
plan: 02
type: execute
wave: 2
depends_on:
  - "23-01"
files_modified:
  - src/lib/mcp/localhost-guard.ts
  - src/lib/mcp/localhost-guard.test.ts
  - src/lib/mcp/create-handler.ts
  - src/lib/mcp/tools/wallet-ping.ts
  - src/app/api/mcp/route.ts
  - src/app/api/mcp/route.test.ts
  - next.config.ts
autonomous: true
requirements:
  - HOST-01
  - HOST-02
estimate:
  tokens: 45000
  raw_tokens: 90000
  tasks: 3
  confidence: high
must_haves:
  truths:
    - Wallet serves MCP at /api/mcp inside the same Next.js process (HOST-01)
    - External client can complete MCP initialize (+ tools/list) on loopback via curl (D-13, HOST-01)
    - Non-loopback Host or Origin rejected with HTTP 403 and reason bad_host or bad_origin (D-09, D-10, HOST-02)
    - Host port must equal process.env.PORT default 3000; wrong port rejected as bad_host (D-03)
    - Missing Origin allowed; loopback Host values 127.0.0.1 localhost and [::1] accepted (D-01, D-02)
    - Only wallet_ping registered; payload ok true service wallet-mcp ISO timestamp (D-05, D-06)
    - Compose host publish remains 127.0.0.1:3000:3000 (D-14, HOST-02)
    - Vitest guard matrix + mocked route smoke green (D-15)
    - Health readiness route unchanged this phase (D-16)
  prohibitions:
    - statement: MUST NOT spawn sidecar or agent subprocess
      category: isolation
    - statement: MUST NOT widen Compose host publish beyond loopback bind
      category: scope
    - statement: MUST NOT register write-capable MCP tools
      category: scope
    - statement: MUST NOT set Edge runtime on MCP route
      category: isolation
    - statement: MUST NOT modify health readiness route this phase
      category: scope
    - statement: MUST NOT add DISOL INISO or GRISO isolation copy this phase
      category: scope
    - statement: MUST NOT register capital or side-ledger MCP tools
      category: scope
  artifacts:
    - path: src/lib/mcp/localhost-guard.ts
      provides: withLocalhostGuard Host/Origin/port policy + 403 reason body
    - path: src/lib/mcp/create-handler.ts
      provides: createWalletMcpHandler SDK factory responseMode json legacy stateless
    - path: src/lib/mcp/tools/wallet-ping.ts
      provides: registerWalletPing only
    - path: src/app/api/mcp/route.ts
      provides: nodejs force-dynamic GET POST DELETE guarded fetch mount
    - path: src/lib/mcp/localhost-guard.test.ts
      provides: D-15 good/bad Host Origin port matrix green
    - path: src/app/api/mcp/route.test.ts
      provides: mocked handler smoke green
  key_links:
    - from: src/app/api/mcp/route.ts
      to: withLocalhostGuard
      via: reject before mcp.fetch
      pattern: "withLocalhostGuard"
    - from: createWalletMcpHandler
      to: "@modelcontextprotocol/server createMcpHandler"
      via: handler.fetch mount
      pattern: "responseMode"
    - from: registerWalletPing
      to: McpServer.registerTool
      via: wallet_ping only
      pattern: "wallet_ping"
---

## Specless probe fallback

SPEC absent. Truths from ROADMAP success criteria + CONTEXT D-01…D-16. Prohibitions descriptor-less anti-goals.

## Phase Goal

Running wallet exposes in-process Streamable HTTP MCP on localhost only — same Next lifecycle, no sidecar (ROADMAP Phase 23 / HOST-01, HOST-02).

<objective>
Ship production-quality tracer: custom localhost-guard (D-01…D-03, D-09…D-12) + SDK createMcpHandler with responseMode json (D-08 via RESEARCH) + wallet_ping only (D-05, D-06) + thin /api/mcp route, then expand Vitest matrix and prove curl initialize + Compose loopback publish (D-13, D-14, D-15, HOST-01, HOST-02).

Purpose: Prove in-process Streamable HTTP MCP with DNS-rebinding defenses end-to-end before capital tools (Phase 24).
Output: Guarded /api/mcp serving wallet_ping; green Vitest; curl initialize smoke; Compose publish unchanged.
</objective>

<execution_context>
@/home/artem/Documents/wallet/.cursor/gsd-core/workflows/execute-plan.md
@/home/artem/Documents/wallet/.cursor/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/OPERATOR.md
@.planning/phases/23-mcp-host-localhost-safety/23-CONTEXT.md
@.planning/phases/23-mcp-host-localhost-safety/23-RESEARCH.md
@.planning/phases/23-mcp-host-localhost-safety/23-PATTERNS.md
@.planning/phases/23-mcp-host-localhost-safety/23-VALIDATION.md
@.planning/phases/23-mcp-host-localhost-safety/23-01-SUMMARY.md
@src/app/api/health/route.ts
@docker-compose.yml
@next.config.ts
@package.json
</context>

<interface>
## Plan 01 → Plan 02 contracts

- `mcp-handler@^2` and `@modelcontextprotocol/server@^2` present in package.json / node_modules
- Wave 0 stubs at `src/lib/mcp/localhost-guard.test.ts` and `src/app/api/mcp/route.test.ts` (todo → real expects this plan)
- Implement create with `@modelcontextprotocol/server` `createMcpHandler` returning `{ fetch }`; do not use mcp-handler create path (responseMode gap)
</interface>

<assumptions>
## Planner assumptions

- A1: Reject Host headers that omit `:PORT` when listen port is 3000 (strict PORT match per RESEARCH open question).
- A2: 403 JSON `{ error: "Forbidden", reason: "bad_host" | "bad_origin" }`.
- A3: Extend next.config.ts outputFileTracingIncludes for MCP packages only if `.next/standalone` miss after build.
- A4: McpServer version string "1.4.0"; instructions exact D-07 wording.
</assumptions>

## Artifacts this phase produces

- `src/lib/mcp/localhost-guard.ts` + tests
- `src/lib/mcp/create-handler.ts`
- `src/lib/mcp/tools/wallet-ping.ts`
- `src/app/api/mcp/route.ts` + tests
- MCP deps already from Plan 01
- Optional NFT includes
- Unchanged `docker-compose.yml` ports; unchanged health route

## Artifacts this plan produces

- Full guarded MCP mount + wallet_ping
- Green D-15 Vitest suites
- Curl initialize (+ tools/list) smoke evidence in SUMMARY / human-check
- Compose publish checklist confirmation

<tasks>

<task type="tracer">
  <name>End-to-end guarded /api/mcp + wallet_ping</name>
  <reversibility rating="costly">Public /api/mcp mount + SDK handler shape become Phase 24–26 contract; path or transport change needs client/docs rewrites.</reversibility>
  <files>src/lib/mcp/localhost-guard.ts, src/lib/mcp/create-handler.ts, src/lib/mcp/tools/wallet-ping.ts, src/app/api/mcp/route.ts, src/lib/mcp/localhost-guard.test.ts, src/app/api/mcp/route.test.ts</files>
  <read_first>
    .planning/phases/23-mcp-host-localhost-safety/23-CONTEXT.md
    .planning/phases/23-mcp-host-localhost-safety/23-RESEARCH.md
    .planning/phases/23-mcp-host-localhost-safety/23-PATTERNS.md
    src/app/api/health/route.ts
    src/lib/db.ts
    src/app/api/mcp/route.test.ts
    src/lib/mcp/localhost-guard.test.ts
    node_modules/@modelcontextprotocol/server/dist/esm/index.d.mts
    node_modules/next/dist/docs/
    package.json
  </read_first>
  <behavior>
    - withLocalhostGuard returns undefined for Host 127.0.0.1:PORT / localhost:PORT / [::1]:PORT with Origin missing or loopback; returns 403 reason bad_host for evil host or wrong port; 403 reason bad_origin for foreign Origin (D-01, D-02, D-03, D-09, D-10)
    - createWalletMcpHandler uses @modelcontextprotocol/server createMcpHandler factory with responseMode json and legacy stateless; registers only wallet_ping (D-05, D-06, D-07, D-08)
    - route exports runtime nodejs, dynamic force-dynamic, GET/POST/DELETE wrapping guard then handler.fetch (D-08, HOST-01)
    - At least one real guard unit + one route mock smoke assert green (convert Wave 0 todos for happy Host + bad Host short-circuit)
  </behavior>
  <action>
    Precondition: Plan 01 deps installed. Implement src/lib/mcp/localhost-guard.ts exporting withLocalhostGuard(req): Response | undefined per D-01…D-03, D-09…D-11: reuse SDK validateHostHeader / validateOriginHeader / localhostAllowedHostnames / localhostAllowedOrigins for parse; add listenPort from process.env.PORT default 3000; require Host port string equality (reject missing port when PORT is 3000 per A1); on fail Response.json with error Forbidden and reason bad_host or bad_origin status 403; console.warn with host and origin values. Do not use SDK hostHeaderValidationResponse / originValidationResponse for the final Response (wrong body). Implement src/lib/mcp/tools/wallet-ping.ts registerWalletPing(server) registering wallet_ping with empty zod object input and content text JSON of ok true, service wallet-mcp, timestamp ISO (D-05, D-06). Implement src/lib/mcp/create-handler.ts createWalletMcpHandler using createMcpHandler and McpServer from @modelcontextprotocol/server — not mcp-handler create — with name wallet-mcp version 1.4.0 and instructions read-only localhost wallet MCP; tools expand later (D-07, D-08); options responseMode json and legacy stateless; registerWalletPing only. Thin src/app/api/mcp/route.ts: export runtime nodejs and dynamic force-dynamic; create handler once; handle(req) runs withLocalhostGuard then mcp.fetch; export handle as GET POST DELETE (D-08, HOST-01). Convert enough Wave 0 it.todo cases to real expects so verify is green for: one good Host pass and one bad Host 403 before fetch. Leave remaining matrix cases for the expansion task if needed. Leave docker-compose.yml untouched. Leave src/app/api/health/route.ts unchanged (D-16). No CORS headers. No capital tools. Optional one-line comment in create-handler citing mcp-handler responseMode gap. If first next build / standalone miss of server package, extend next.config.ts outputFileTracingIncludes analog to prisma paths.
  </action>
  <verify>
    <automated>test -f src/lib/mcp/localhost-guard.ts &amp;&amp; test -f src/lib/mcp/create-handler.ts &amp;&amp; test -f src/lib/mcp/tools/wallet-ping.ts &amp;&amp; test -f src/app/api/mcp/route.ts &amp;&amp; grep -q 'runtime.*=.*["'\'']nodejs["'\'']' src/app/api/mcp/route.ts &amp;&amp; grep -q 'force-dynamic' src/app/api/mcp/route.ts &amp;&amp; grep -q 'wallet_ping' src/lib/mcp/tools/wallet-ping.ts &amp;&amp; grep -q 'responseMode' src/lib/mcp/create-handler.ts &amp;&amp; grep -q 'withLocalhostGuard' src/app/api/mcp/route.ts &amp;&amp; npx vitest run src/lib/mcp/localhost-guard.test.ts src/app/api/mcp/route.test.ts</automated>
    <fails_when>missing mcp files, runtime/force-dynamic/wallet_ping/responseMode/withLocalhostGuard markers absent, or vitest non-zero</fails_when>
  </verify>
  <acceptance_criteria>
    - /api/mcp route mounts guarded SDK fetch with nodejs + force-dynamic (HOST-01, D-08)
    - wallet_ping only tool registered with D-06 payload shape (D-05, D-06)
    - Guard rejects bad Host with 403 reason bad_host; good Host reaches fetch in route smoke (D-09, D-10, D-15)
    - create-handler imports createMcpHandler from @modelcontextprotocol/server with responseMode json (D-08)
  </acceptance_criteria>
  <done>Tracer path green: guarded /api/mcp + wallet_ping + minimal Vitest</done>
</task>

<task type="auto" tdd="true">
  <name>Expand localhost-guard matrix + route mock smoke</name>
  <files>src/lib/mcp/localhost-guard.ts, src/lib/mcp/localhost-guard.test.ts, src/app/api/mcp/route.test.ts</files>
  <read_first>
    .planning/phases/23-mcp-host-localhost-safety/23-RESEARCH.md
    src/lib/mcp/localhost-guard.ts
    src/lib/mcp/localhost-guard.test.ts
    src/app/api/mcp/route.test.ts
    src/app/api/health/route.test.ts
  </read_first>
  <behavior>
    - Host 127.0.0.1:3000 no Origin → pass
    - Host localhost:3000 no Origin → pass
    - Host [::1]:3000 no Origin → pass
    - Host evil.com:3000 → 403 bad_host
    - Host 127.0.0.1:9999 → 403 bad_host
    - Host missing port when PORT=3000 → 403 bad_host
    - Good Host + Origin https://evil.com → 403 bad_origin
    - Good Host + Origin http://127.0.0.1:3000 → pass
    - Route: bad Host → fetch not called; good Host → fetch called (D-15)
  </behavior>
  <action>
    Per D-15 and HOST-02: convert all Wave 0 guard/route it.todo cases to real expects covering RESEARCH Vitest matrix plus missing-port reject (A1). Mock createWalletMcpHandler.fetch in route.test.ts; assert guard short-circuits before fetch on bad Host and calls fetch on good Host. Keep PORT default 3000 in tests (set process.env.PORT when needed). Do not add CORS headers. Do not register extra tools. Do not edit health route or Compose ports.
  </action>
  <verify>
    <automated>npx vitest run src/lib/mcp/localhost-guard.test.ts src/app/api/mcp/route.test.ts</automated>
    <fails_when>vitest non-zero, remaining it.todo on D-15 matrix owners, or "0 passed" in summary</fails_when>
  </verify>
  <acceptance_criteria>
    - Full D-15 Host/Origin/port matrix green (D-01, D-02, D-03, D-09, D-10)
    - Route smoke proves fetch gated by guard (HOST-01, HOST-02)
  </acceptance_criteria>
  <done>Vitest MCP suites fully green for guard + route smoke</done>
</task>

<task type="auto">
  <name>Curl initialize smoke + Compose publish checklist</name>
  <files>src/app/api/mcp/route.ts</files>
  <read_first>
    .planning/phases/23-mcp-host-localhost-safety/23-CONTEXT.md
    .planning/phases/23-mcp-host-localhost-safety/23-RESEARCH.md
    .planning/OPERATOR.md
    docker-compose.yml
  </read_first>
  <precondition>Tracer task shipped /api/mcp route; node_modules present; PORT available (default 3000)</precondition>
  <action>
    Per D-13, D-14, HOST-01, HOST-02 and OPERATOR.md: start app with npm run dev (or reuse running :3000). Curl MCP initialize then tools/list against http://127.0.0.1:$PORT/api/mcp with Content-Type application/json, Accept application/json text/event-stream, Host 127.0.0.1:$PORT — expect initialize success and tools/list including wallet_ping. Prefer 127.0.0.1 over localhost for smoke (D-04). Confirm docker-compose.yml ports line still binds 127.0.0.1:3000:3000 (checklist only — no file-assert test). Leave src/app/api/health/route.ts unchanged (D-16). Record curl evidence in SUMMARY. human_verify_mode=end-of-phase — embed checks below for UAT harvest; agent drives smoke without mid-flight halt.
  </action>
  <verify>
    <automated>grep -q '127.0.0.1:3000:3000' docker-compose.yml &amp;&amp; test -f src/app/api/mcp/route.ts &amp;&amp; ! grep -q 'Access-Control-Allow' src/app/api/mcp/route.ts src/lib/mcp/*.ts &amp;&amp; npx vitest run src/lib/mcp/localhost-guard.test.ts src/app/api/mcp/route.test.ts &amp;&amp; npm test</automated>
    <fails_when>Compose ports line missing loopback publish, Access-Control-Allow present under mcp sources, vitest or npm test non-zero</fails_when>
    <human-check>
      <url>http://127.0.0.1:3000/api/mcp</url>
      <steps>
        1. With wallet running, curl initialize JSON-RPC to /api/mcp (Host includes listen port) — expect 200 JSON initialize result (D-13)
        2. curl tools/list — expect wallet_ping listed (D-05)
        3. Confirm docker-compose.yml still publishes 127.0.0.1:3000:3000 (D-14)
        4. Optional: MCP Inspector — not a blocker
      </steps>
      <pass-criteria>Initialize + tools/list succeed on loopback; Compose publish remains loopback-only</pass-criteria>
    </human-check>
  </verify>
  <acceptance_criteria>
    - Curl initialize (+ tools/list) succeeds on loopback (D-13, HOST-01)
    - Compose publish remains 127.0.0.1:3000:3000 (D-14, HOST-02)
    - No CORS allow headers on MCP sources (D-12)
    - Full npm test green
  </acceptance_criteria>
  <done>Phase done bar met: curl initialize + Compose checklist + suite green</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| External CLI/browser → Next `/api/mcp` | Untrusted Host/Origin/body cross into Node process |
| Next process → MCP tool handlers | Only wallet_ping; no DB this phase |
| Host network → container publish | Compose bind controls LAN reachability |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-23-01 | Spoofing | localhost-guard Host/Origin | high | mitigate | Allowlist 127.0.0.1 localhost [::1]; reject else 403 bad_host/bad_origin (D-01, D-02, D-09, D-10) |
| T-23-02 | Information Disclosure | docker-compose.yml ports | high | mitigate | Keep 127.0.0.1:3000:3000; UAT checklist asserts no 0.0.0.0 host publish (D-14, HOST-02) |
| T-23-03 | Spoofing | Host port vs PORT | high | mitigate | Explicit PORT equality in guard; wrong/missing port → bad_host (D-03, A1) |
| T-23-04 | Spoofing | CSRF foreign Origin | high | mitigate | Reject non-loopback Origin even when Host good; missing Origin allowed for CLI (D-02) |
| T-23-05 | Elevation of Privilege | CORS on /api/mcp | medium | mitigate | No Access-Control-* headers (D-12); verify grep in smoke task |
| T-23-SC | Tampering | npm installs | high | mitigate | Already gated in Plan 01; no new packages this plan |
</threat_model>

<verification>
Vitest MCP files green; npm test green; Compose ports line intact; curl initialize human-check for UAT.
</verification>

<success_criteria>
In-process /api/mcp serves wallet_ping behind localhost Host/Origin/port guards; curl initialize works; Compose stays loopback-published.
</success_criteria>

<output>
Create `.planning/phases/23-mcp-host-localhost-safety/23-02-SUMMARY.md` when done
</output>
