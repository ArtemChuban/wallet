---
phase: 06-historical-charts
plan: 02
type: execute
wave: 2
depends_on:
  - 06-01
files_modified:
  - src/lib/historical-series.ts
  - src/lib/historical-series.test.ts
  - src/components/dashboard/AccountHistoryChart.tsx
  - src/components/dashboard/DashboardAccountList.tsx
  - src/components/dashboard/DashboardChartsShell.tsx
  - src/app/page.tsx
autonomous: true
requirements:
  - CHART-02
  - CHART-03
estimate:
  tokens: 24000
  raw_tokens: 48000
  tasks: 3
  confidence: high
must_haves:
  truths:
    - Expanding an account row on / shows a chart-only body with no snapshot history list and no set/delete controls (CHART-02, D-02, D-04)
    - Expand aria labels are Показать график счёта collapsed and Скрыть график счёта expanded (UI-SPEC)
    - Non-credit account chart defaults to native series; native↔primary toggle labels В валюте счёта and В {PRIMARY} (D-09)
    - Toggle is hidden when account currency is primary (D-10)
    - Account primary-mode points skip dates with null FX leaving a gap connectNulls false (D-16, CHART-03)
    - Account chart uses the same shared RangePreset as the NW chart (D-08)
    - Account sample dates are that account snapshots union FX dates for its currency when primary mode union today (D-07)
    - Expand panel uses bg-muted/40 and px-4 py-4 matching Phase 03 history expand chrome
  prohibitions:
    - statement: MUST NOT show snapshot history list or balance mutation controls inside home expand
      requirement_id: CHART-02
      category: values
    - statement: MUST NOT place per-account charts on /accounts in this phase
      requirement_id: CHART-02
      category: values
    - statement: MUST NOT invent primary-mode points when FX is null
      requirement_id: CHART-03
      category: values
    - statement: MUST NOT use connectNulls true on primary account series
      requirement_id: CHART-03
      category: values
  artifacts:
    - path: src/lib/historical-series.ts
      provides: buildAccountSeries native and primary modes
    - path: src/components/dashboard/AccountHistoryChart.tsx
      provides: client LineChart plus toggle for non-credit accounts
    - path: src/components/dashboard/DashboardAccountList.tsx
      provides: client expand chevron hosting AccountHistoryChart
    - path: src/components/dashboard/DashboardChartsShell.tsx
      provides: shared range passed into account list charts
  key_links:
    - from: DashboardAccountList
      to: AccountHistoryChart
      via: expand body only
      pattern: "AccountHistoryChart"
    - from: AccountHistoryChart
      to: buildAccountSeries
      via: native or primary mode with shared RangePreset
      pattern: "buildAccountSeries"
    - from: DashboardChartsShell
      to: DashboardAccountList
      via: range prop D-08
      pattern: "range|RangePreset"
---

## Phase Goal

**As a** local Wallet user, **I want to** trust historical net-worth and per-account charts built from as-of balances and as-of FX, **so that** capital history stays honest when rates change (CHART-01, CHART-02, CHART-03).

## Artifacts this phase produces

| Artifact | Plan | Purpose |
|----------|------|---------|
| `buildAccountSeries` in historical-series | 02 | Per-account native/primary sparse series |
| `AccountHistoryChart.tsx` | 02–03 | Per-account chart island |
| `DashboardAccountList.tsx` expand | 02 | Chart-only expand on `/` |

<objective>
Vertical slice for CHART-02: expand account rows on / to show native-default history charts with shared range and primary toggle (hide when primary currency); skip null-FX primary points per D-16.

Purpose: Deliver per-account history without mutations, reusing Plan 01 series and shell.
Output: buildAccountSeries + tests, AccountHistoryChart, expandable DashboardAccountList wired to shared range.
</objective>

<execution_context>
@/home/artem/Documents/wallet/.cursor/gsd-core/workflows/execute-plan.md
@/home/artem/Documents/wallet/.cursor/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/06-historical-charts/06-CONTEXT.md
@.planning/phases/06-historical-charts/06-RESEARCH.md
@.planning/phases/06-historical-charts/06-PATTERNS.md
@.planning/phases/06-historical-charts/06-UI-SPEC.md
@.planning/phases/06-historical-charts/06-01-PLAN.md
@src/lib/historical-series.ts
@src/lib/balances.ts
@src/lib/fx.ts
@src/lib/money.ts
@src/components/dashboard/DashboardChartsShell.tsx
@src/components/dashboard/DashboardAccountList.tsx
@src/components/accounts/AccountList.tsx
@src/app/page.tsx
</context>

<tasks>

<task type="auto" tdd="true">
  <name>buildAccountSeries native and primary with FX skip</name>
  <files>src/lib/historical-series.ts, src/lib/historical-series.test.ts</files>
  <read_first>
    .planning/phases/06-historical-charts/06-CONTEXT.md
    .planning/phases/06-historical-charts/06-RESEARCH.md
    .planning/phases/06-historical-charts/06-PATTERNS.md
    src/lib/historical-series.ts
    src/lib/historical-series.test.ts
    src/lib/fx.ts
    src/lib/balances.ts
    src/lib/money.ts
  </read_first>
  <behavior>
    - Native mode events = account snapshot dates union today; Y is native major number from LOCF amount
    - Primary mode events = snapshots union FX dates for account currency union today; uses convertOtherMinorToPrimaryMinor
    - Primary mode skips sample dates where LOCF FX is null (D-16); native mode still includes those balance events
    - Window filter identical to buildNetWorthSeries via windowStartForPreset
    - Primary-currency account primary mode equals native (identity) without requiring FxRate
  </behavior>
  <action>
    Per D-07, D-09, D-16, CHART-02, CHART-03: export buildAccountSeries from historical-series.ts accepting account id currency scale isPrimaryCurrency creditLimitMinor type, snapshot/rate arrays, preset, today, and mode native or primary. For non-credit accounts return points with asOfDate and value major number (and optional valueMinor bigint for tests). Skip primary points when rate null. Reuse locf-at-D helpers from Plan 01. Add Vitest cases for native events, primary FX conversion, D-16 skip, and primary-currency identity. Do not implement credit stack fields yet (Plan 03).
  </action>
  <verify>
    <automated>npm test -- src/lib/historical-series.test.ts</automated>
    <fails_when>non-zero exit or failed assertions in historical-series.test.ts</fails_when>
  </verify>
  <acceptance_criteria>
    - buildAccountSeries exported and covered for native primary D-16 and identity
    - Full historical-series.test.ts exits 0
  </acceptance_criteria>
  <done>Per-account series math tested for non-credit native/primary paths</done>
</task>

<task type="auto">
  <name>Account expand chart-only with native default LineChart</name>
  <files>src/components/dashboard/AccountHistoryChart.tsx, src/components/dashboard/DashboardAccountList.tsx, src/components/dashboard/DashboardChartsShell.tsx, src/app/page.tsx</files>
  <read_first>
    .planning/phases/06-historical-charts/06-UI-SPEC.md
    .planning/phases/06-historical-charts/06-PATTERNS.md
    src/components/accounts/AccountList.tsx
    src/components/dashboard/DashboardAccountList.tsx
    src/components/dashboard/DashboardChartsShell.tsx
    src/components/dashboard/NetWorthHistoryChart.tsx
    src/app/page.tsx
  </read_first>
  <action>
    Per D-02, D-04, D-08, D-09, CHART-02: convert DashboardAccountList into a client component (or wrap rows in a client expander) mirroring AccountList expand state with ChevronRight/ChevronDown, aria-expanded, aria-label Показать график счёта / Скрыть график счёта. Expanded body is bg-muted/40 px-4 py-4 containing only AccountHistoryChart — no history list, no delete, no set-balance. Create AccountHistoryChart for non-credit accounts: LineChart h-[200px] stroke var(--chart-3) connectNulls false dot when length less than or equal to 1; default mode native; pass shared range from DashboardChartsShell into list then chart so preset changes recompute account series. Serialize any needed account metadata (id name currencyCode isPrimaryCurrency scale type creditLimitMinor string) from page through shell. Keep empty-dashboard CTA path unchanged. For FIAT_CREDIT rows in this plan: expand chrome works and AccountHistoryChart receives isCredit true but renders the non-credit Line path using available LOCF only — Plan 03 replaces that branch with stacked Area debt plus available per D-11/D-12.
  </action>
  <verify>
    <automated>npm test -- src/lib/historical-series.test.ts && grep -q 'AccountHistoryChart' src/components/dashboard/DashboardAccountList.tsx && grep -q 'Показать график счёта' src/components/dashboard/DashboardAccountList.tsx && grep -q 'Скрыть график счёта' src/components/dashboard/DashboardAccountList.tsx && grep -q 'use client' src/components/dashboard/AccountHistoryChart.tsx</automated>
    <fails_when>tests fail, expand aria labels missing, AccountHistoryChart not referenced from list, or AccountHistoryChart lacks use client</fails_when>
  </verify>
  <acceptance_criteria>
    - Expand chrome uses UI-SPEC aria labels
    - AccountHistoryChart is client and hosted in expand body
    - historical-series tests remain green
  </acceptance_criteria>
  <done>User can expand an account on / and see a native history chart for shared range</done>
</task>

<task type="auto">
  <name>Native↔primary toggle hide when primary currency</name>
  <files>src/components/dashboard/AccountHistoryChart.tsx, src/lib/historical-series.test.ts</files>
  <read_first>
    .planning/phases/06-historical-charts/06-UI-SPEC.md
    .planning/phases/06-historical-charts/06-CONTEXT.md
    src/components/dashboard/AccountHistoryChart.tsx
    src/lib/historical-series.ts
  </read_first>
  <action>
    Per D-09, D-10, D-16, UI-SPEC: add toggle Buttons В валюте счёта and В {PRIMARY} with default native; hide entire toggle when isPrimaryCurrency true. Switching to primary recomputes buildAccountSeries mode primary; null-FX dates omitted so Line gaps with connectNulls false. Tooltip date via formatAsOfDisplay; amounts Geist Mono Label size; static chartConfig. No partial badges. Confirm D-16 covered in tests from Task 1 still green.
  </action>
  <verify>
    <automated>npm test -- src/lib/historical-series.test.ts && grep -q 'В валюте счёта' src/components/dashboard/AccountHistoryChart.tsx && grep -q 'isPrimaryCurrency' src/components/dashboard/AccountHistoryChart.tsx && grep -q 'connectNulls' src/components/dashboard/AccountHistoryChart.tsx</automated>
    <fails_when>tests fail, native toggle copy missing, primary-currency hide gate missing, or connectNulls not set on Line</fails_when>
  </verify>
  <acceptance_criteria>
    - Toggle copy В валюте счёта present
    - Component branches on isPrimaryCurrency to hide toggle
    - Line uses connectNulls false
  </acceptance_criteria>
  <done>Per-account native/primary toggle works; primary currency accounts hide toggle</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Expand panel → AccountHistoryChart | Account name and series labels from DB into tooltip/SVG |
| Shared range client state → account series | Preset enum only; recomputes from trusted serialized rows |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-06-05 | Tampering | AccountHistoryChart tooltip | medium | mitigate | React text escaping for accountName; no dangerouslySetInnerHTML |
| T-06-06 | Tampering | Expand body | low | mitigate | Chart-only body; no mutation forms or Server Actions (D-04) |
| T-06-07 | Information | Primary-mode series | low | accept | Skipping null-FX points may reveal FX gaps; intentional honesty per D-16 |
| T-06-SC | Tampering | npm recharts | high | mitigate | Already installed/pinned in Plan 01; no new packages this plan |
</threat_model>

<verification>
npm test -- src/lib/historical-series.test.ts; expand row shows chart; shared range applies; toggle hides for primary currency.
</verification>

<success_criteria>
CHART-02 non-credit path works on / expand; D-08/D-09/D-10/D-16 honored; CHART-03 skip-null-FX covered.
</success_criteria>

<output>
Create `.planning/phases/06-historical-charts/06-02-SUMMARY.md` when done
</output>
