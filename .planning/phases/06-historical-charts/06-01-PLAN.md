---
phase: 06-historical-charts
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/lib/dates.ts
  - src/lib/dates.test.ts
  - src/lib/historical-series.ts
  - src/lib/historical-series.test.ts
  - src/components/ui/chart.tsx
  - src/components/dashboard/DashboardChartsShell.tsx
  - src/components/dashboard/DashboardRangeControl.tsx
  - src/components/dashboard/NetWorthHistoryChart.tsx
  - src/app/page.tsx
  - package.json
  - package-lock.json
autonomous: true
requirements:
  - CHART-01
  - CHART-03
estimate:
  tokens: 28000
  raw_tokens: 56000
  tasks: 3
  confidence: high
must_haves:
  truths:
    - User sees historical net-worth LineChart in primary currency on / under Капитал when accounts.length > 0 (CHART-01, D-01, D-03)
    - Layout order is Hero Капитал then optional partial banner then shared range plus NW chart then account list (D-03)
    - Shared range presets are exactly 30д 90д 1г всё with default selected 30д and group name Период (D-05, D-06, D-08)
    - Chart sample dates are event dates union today only never one point per calendar day (D-07, CHART-03)
    - Each NW point total uses LOCF balance as of that date times LOCF FX as of that date via computeNetWorthRows (CHART-03)
    - Later FX row dated after sample date D does not change totalPrimaryMinor at D (CHART-03 regression)
    - Partial NW days still emit a point from included accounts only with no partial badge on the point (D-14)
    - Window bounds: date >= windowStart and date <= today inclusive; event one day before windowStart excluded; all preset has null start (CHART-03 boundary)
    - Authoritative series totals stay BigInt in lib tests; chart Y majors are Number(formatMinorToMajor) at the client boundary (CHART-03 precision, RESEARCH A1)
    - Zero accounts omits the entire chart section and keeps Phase 05 empty CTA (UI Considerations empty)
    - statement: ChartContainer paints at h-[200px] with visible NW series after shadcn chart install
      verification: backstop
  prohibitions:
    - statement: MUST NOT densify series to one point per calendar day
      requirement_id: CHART-03
      category: values
    - statement: MUST NOT apply today's FX to past chart points
      requirement_id: CHART-03
      category: values
    - statement: MUST NOT mark NW chart points as partial with badge or tooltip
      requirement_id: CHART-01
      category: values
    - statement: MUST NOT add dedicated /history or /charts route
      requirement_id: CHART-01
      category: safety
    - statement: MUST NOT add chart-related Server Actions or mutations on /
      requirement_id: CHART-01
      category: safety
    - statement: MUST NOT use dangerouslySetInnerHTML in chart tooltips or labels
      requirement_id: CHART-01
      category: safety
    - statement: MUST NOT change Prisma schema in this phase
      requirement_id: CHART-01
      category: safety
  artifacts:
    - path: src/lib/dates.ts
      provides: addCalendarDays and windowStartForPreset helpers
    - path: src/lib/historical-series.ts
      provides: buildNetWorthSeries pure event∪today LOCF builder
    - path: src/lib/historical-series.test.ts
      provides: CHART-01 and CHART-03 unit coverage including later-FX regression
    - path: src/components/ui/chart.tsx
      provides: shadcn ChartContainer ChartTooltip ChartConfig
    - path: src/components/dashboard/NetWorthHistoryChart.tsx
      provides: client NW LineChart
    - path: src/components/dashboard/DashboardChartsShell.tsx
      provides: shared range useState default 30d
    - path: src/components/dashboard/DashboardRangeControl.tsx
      provides: Russian preset Buttons 30д 90д 1г всё
    - path: src/app/page.tsx
      provides: hero then charts shell then list wiring with serialized LOCF payloads
  key_links:
    - from: src/lib/historical-series.ts
      to: computeNetWorthRows
      via: per-sample-date NetWorthAccountInput LOCF maps
      pattern: "computeNetWorthRows"
    - from: src/app/page.tsx
      to: DashboardChartsShell
      via: serialized snapshots rates accounts strings no BigInt across RSC boundary
      pattern: "DashboardChartsShell"
    - from: DashboardChartsShell
      to: buildNetWorthSeries
      via: client recompute on RangePreset change
      pattern: "buildNetWorthSeries|RangePreset"
---

## Phase Goal

**As a** local Wallet user, **I want to** trust historical net-worth and per-account charts built from as-of balances and as-of FX, **so that** capital history stays honest when rates change (CHART-01, CHART-02, CHART-03).

## Artifacts this phase produces

| Artifact | Plan | Purpose |
|----------|------|---------|
| `src/lib/dates.ts` (+ tests) | 01 | Calendar window helpers for presets |
| `src/lib/historical-series.ts` (+ tests) | 01–03 | Pure NW and account series builders |
| `src/components/ui/chart.tsx` | 01 | shadcn Chart + recharts@3.10.1 |
| `src/components/dashboard/DashboardChartsShell.tsx` | 01 | Shared range client state |
| `src/components/dashboard/DashboardRangeControl.tsx` | 01 | 30д/90д/1г/всё Buttons |
| `src/components/dashboard/NetWorthHistoryChart.tsx` | 01 | NW LineChart |
| `src/components/dashboard/AccountHistoryChart.tsx` | 02–03 | Per-account Line/stacked Area |
| `src/components/dashboard/DashboardAccountList.tsx` | 02–03 | Expand → chart-only body |
| `src/app/page.tsx` | 01–03 | Hero → chart shell → list |

<objective>
Tracer slice for CHART-01/CHART-03: date window helpers, pure NW series builder with Vitest, shadcn Chart install, and NW history chart on / under Капитал with shared range presets defaulting to 30d.

Purpose: Prove as-of LOCF series math end-to-end on the dashboard before per-account expand work.
Output: historical-series lib + green tests, chart.tsx, DashboardChartsShell/RangeControl/NetWorthHistoryChart, page wiring.
</objective>

<execution_context>
@/home/artem/Documents/wallet/.cursor/gsd-core/workflows/execute-plan.md
@/home/artem/Documents/wallet/.cursor/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/06-historical-charts/06-CONTEXT.md
@.planning/phases/06-historical-charts/06-RESEARCH.md
@.planning/phases/06-historical-charts/06-PATTERNS.md
@.planning/phases/06-historical-charts/06-VALIDATION.md
@.planning/phases/06-historical-charts/06-UI-SPEC.md
@src/lib/net-worth.ts
@src/lib/net-worth.test.ts
@src/lib/balances.ts
@src/lib/fx.ts
@src/lib/money.ts
@src/lib/dates.ts
@src/app/page.tsx
@src/components/dashboard/DashboardAccountList.tsx
@package.json
@components.json
</context>

<context_lock_waiver>
D-07 (event∪today sampling) locked in 06-CONTEXT.md rated costly — flag on tracer; no checkpoint:decision re-ask. D-01/D-03/D-05/D-06/D-08 placement and range locks honored as written.
</context_lock_waiver>

<assumptions>
- CHART-01 unclassified probe: satisfied by NW LineChart under Капитал on / when accounts.length > 0 (D-01/D-03).
- CHART-03 boundary: windowStart inclusive, today inclusive; dates before windowStart dropped; `all` uses null start (UI-SPEC Window row; RESEARCH A4: 1y = 365 calendar days).
- CHART-03 precision: lib asserts BigInt totalPrimaryMinor; chart props use number majors via Number(formatMinorToMajor) (RESEARCH A1 acceptable for v1).
- Shared range state is client useState not URL/searchParams (RESEARCH Pattern 3 / Claude's Discretion).
- Serialize snapshot/rate minors as strings for RSC→client; recompute series in client with BigInt (RESEARCH Open Question 2 recommendation).
</assumptions>

<tasks>

<task type="tracer">
  <name>End-to-end NW history on / — default 30d as-of series</name>
  <reversibility rating="costly">D-07 locks sparse event∪today sampling; switching to daily densification later changes series shape and performance assumptions.</reversibility>
  <files>src/lib/dates.ts, src/lib/dates.test.ts, src/lib/historical-series.ts, src/lib/historical-series.test.ts, src/components/ui/chart.tsx, src/components/dashboard/DashboardChartsShell.tsx, src/components/dashboard/NetWorthHistoryChart.tsx, src/app/page.tsx, package.json, package-lock.json</files>
  <read_first>
    .planning/phases/06-historical-charts/06-CONTEXT.md
    .planning/phases/06-historical-charts/06-RESEARCH.md
    .planning/phases/06-historical-charts/06-PATTERNS.md
    .planning/phases/06-historical-charts/06-UI-SPEC.md
    .planning/phases/06-historical-charts/06-VALIDATION.md
    src/lib/net-worth.ts
    src/lib/dates.ts
    src/lib/money.ts
    src/app/page.tsx
    package.json
    components.json
  </read_first>
  <behavior>
    - windowStartForPreset("30d", today) returns YYYY-MM-DD exactly 30 calendar days before today; "all" returns null
    - buildNetWorthSeries emits points only for union of balance snapshot dates FX dates for non-primary currencies in play and today filtered to window
    - Past sample date uses LOCF rate dated on or before that date not a later rate (CHART-03)
    - page.tsx renders DashboardChartsShell between hero/partial banner and DashboardAccountList when accounts.length > 0
    - NetWorthHistoryChart mounts ChartContainer with h-[200px] Line dataKey nw using var(--chart-3) ChartConfig key nw label Капитал
  </behavior>
  <action>
    Per D-01, D-03, D-06, D-07, D-14, CHART-01, CHART-03: extend src/lib/dates.ts with pure addCalendarDays and windowStartForPreset for presets 30d 90d 1y all (1y = 365 calendar days per RESEARCH A4); add matching cases in dates.test.ts. Create src/lib/historical-series.ts exporting RangePreset type and buildNetWorthSeries that takes in-memory accounts plus snapshot/rate arrays (amountMinor and rateToPrimaryScaled as bigint), builds per-date LOCF maps with asOfDate lte D first-wins, calls computeNetWorthRows per sample date, ignores isPartial for point marking (D-14), always unions today, returns points with asOfDate totalPrimaryMinor and nw major number. Add src/lib/historical-series.test.ts with at least one CHART-03 case: FX on D1 and D2 later; sample between them uses D1 rate. Run npx shadcn@latest add chart from official registry only; pin recharts to 3.10.1 after install (Package Legitimacy OK — no human gate). Create NetWorthHistoryChart client component using ChartContainer ChartTooltip ChartTooltipContent LineChart CartesianGrid XAxis YAxis Line type linear connectNulls false stroke var(--chart-3) dot when data.length less than or equal to 1. Create DashboardChartsShell client component accepting serializable account/snapshot/rate payloads (minors as strings), default range 30d, computing NW series via buildNetWorthSeries after BigInt revive. Extend page.tsx: when accounts.length greater than 0 pass serialized payloads into shell after hero and partial banner; omit shell when zero accounts. Do not implement account expand charts or full four-button RangeControl yet — Task 3. No Prisma schema changes. No new Server Actions.
  </action>
  <verify>
    <automated>npm test -- src/lib/historical-series.test.ts src/lib/dates.test.ts && test -f src/components/ui/chart.tsx && test -f src/components/dashboard/NetWorthHistoryChart.tsx && grep -q 'DashboardChartsShell' src/app/page.tsx && grep -q 'buildNetWorthSeries' src/lib/historical-series.ts && grep -q 'windowStartForPreset' src/lib/dates.ts</automated>
    <fails_when>non-zero vitest exit, chart.tsx or NetWorthHistoryChart missing, page lacks DashboardChartsShell, or builders/helpers absent from lib files</fails_when>
  </verify>
  <acceptance_criteria>
    - historical-series.test.ts and dates.test.ts exit 0
    - src/components/ui/chart.tsx exists after shadcn add chart
    - page.tsx imports or renders DashboardChartsShell when accounts present
    - buildNetWorthSeries and windowStartForPreset exported
  </acceptance_criteria>
  <done>User path on / shows NW history chart fed by as-of LOCF series for default 30d window</done>
</task>

<task type="auto" tdd="true">
  <name>Wave 0 historical-series.test.ts full CHART matrix</name>
  <files>src/lib/historical-series.ts, src/lib/historical-series.test.ts, src/lib/dates.ts, src/lib/dates.test.ts</files>
  <read_first>
    .planning/phases/06-historical-charts/06-VALIDATION.md
    .planning/phases/06-historical-charts/06-RESEARCH.md
    .planning/phases/06-historical-charts/06-PATTERNS.md
    src/lib/historical-series.ts
    src/lib/historical-series.test.ts
    src/lib/net-worth.test.ts
    src/lib/fx.test.ts
  </read_first>
  <behavior>
    - CHART-03: point at mid-date between two FX rows uses earlier rate totalPrimaryMinor exactly
    - D-07: series length equals distinct event∪today dates in window not calendar day count
    - D-14: day with one account missing FX still emits NW point from included accounts without a partial flag field on the point
    - Window: event one day before windowStart excluded; today always present when accounts exist; all preset includes earliest event
    - Primary-currency accounts use identity conversion without FxRate row (Phase 5 pitfall)
  </behavior>
  <action>
    Per CHART-01, CHART-03, D-07, D-14: expand historical-series.test.ts to cover later-FX rewrite guard, sparse event∪today sampling, partial day still plotted, window inclusive bounds, and primary identity. Keep builders pure with in-memory fixtures — no Prisma mocks. Extend dates.test.ts for 30d 90d 1y all windowStartForPreset edges including leap-safe calendar subtract. Implement any missing branches in buildNetWorthSeries only; do not add account-series builders yet (Plan 02).
  </action>
  <verify>
    <automated>npm test -- src/lib/historical-series.test.ts src/lib/dates.test.ts</automated>
    <fails_when>non-zero exit or vitest summary shows failed tests for historical-series or dates</fails_when>
  </verify>
  <acceptance_criteria>
    - Tests cover CHART-03 later-FX, D-07 sampling, D-14 partial plot, window bounds
    - npm test -- src/lib/historical-series.test.ts src/lib/dates.test.ts exits 0
  </acceptance_criteria>
  <done>CHART-01/CHART-03 series math fully unit-tested without UI</done>
</task>

<task type="auto">
  <name>Shared range presets 30д 90д 1г всё on NW chart</name>
  <files>src/components/dashboard/DashboardRangeControl.tsx, src/components/dashboard/DashboardChartsShell.tsx, src/components/dashboard/NetWorthHistoryChart.tsx, src/app/page.tsx</files>
  <read_first>
    .planning/phases/06-historical-charts/06-UI-SPEC.md
    .planning/phases/06-historical-charts/06-CONTEXT.md
    src/components/dashboard/DashboardChartsShell.tsx
    src/components/dashboard/NetWorthHistoryChart.tsx
    src/components/ui/button.tsx
    src/app/page.tsx
  </read_first>
  <action>
    Per D-05, D-06, D-08, UI-SPEC Copywriting: create DashboardRangeControl client component with four Button presets labeled 30д 90д 1г всё; aria-pressed on active; accessible group name Период; selected uses default/primary variant weight 600; min-h-11 tap targets; gap-2 between buttons; mb-4 before chart. Wire DashboardChartsShell useState RangePreset default 30d; on change recompute buildNetWorthSeries and pass points to NetWorthHistoryChart. Keep static chartConfig object — no JSON.parse of user chart config. Empty series still mounts axes with Y domain 0..1 and X ticks windowStart plus today via formatAsOfDisplay (D-13 prep for Plan 03 polish). Omit account expand wiring (Plan 02).
  </action>
  <verify>
    <automated>npm test -- src/lib/historical-series.test.ts && grep -q '30д' src/components/dashboard/DashboardRangeControl.tsx && grep -q '90д' src/components/dashboard/DashboardRangeControl.tsx && grep -q '1г' src/components/dashboard/DashboardRangeControl.tsx && grep -q 'всё' src/components/dashboard/DashboardRangeControl.tsx && grep -q 'Период' src/components/dashboard/DashboardRangeControl.tsx && grep -q 'DashboardRangeControl' src/components/dashboard/DashboardChartsShell.tsx</automated>
    <fails_when>historical-series tests fail, any preset label or Период missing, or shell does not render DashboardRangeControl</fails_when>
  </verify>
  <acceptance_criteria>
    - All four Russian preset labels present
    - Group name Период present
    - Shell imports DashboardRangeControl and holds shared range state
  </acceptance_criteria>
  <done>Shared range control drives NW chart recomputation for all four presets</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| RSC page.tsx → client chart shell | Serialized snapshot/rate/account payloads cross into browser; must be strings/numbers only |
| Chart tooltip / SVG labels | Account and series labels rendered as React text — untrusted DB strings |
| npm install recharts / shadcn chart | Supply-chain boundary for new dependency |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-06-01 | Tampering | ChartTooltip labels | medium | mitigate | Render names/amounts as React text only; never dangerouslySetInnerHTML |
| T-06-02 | Tampering | chartConfig | low | mitigate | Static ChartConfig object in source; no runtime JSON.parse of user config |
| T-06-03 | Information | Client series rebuild | low | accept | Client only filters preset enum and recomputes from server-fetched rows; no new mutation surface |
| T-06-04 | Elevation | / chart UI | low | mitigate | No new Server Actions; read-only dashboard remains (D-04 / Phase 5 D-16) |
| T-06-SC | Tampering | npm recharts via shadcn chart | high | mitigate | Package Legitimacy Audit verdict OK for recharts; pin 3.10.1; no postinstall scripts |
</threat_model>

<verification>
npm test -- src/lib/historical-series.test.ts && npm test after wave; page shows NW chart under Капитал with working presets.
</verification>

<success_criteria>
CHART-01 visible on /; CHART-03 unit-proven; shared range defaults to 30д; no schema changes; no chart Server Actions.
</success_criteria>

<output>
Create `.planning/phases/06-historical-charts/06-01-SUMMARY.md` when done
</output>
