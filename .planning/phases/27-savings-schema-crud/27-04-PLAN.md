---
phase: 27-savings-schema-crud
plan: 04
type: execute
wave: 3
depends_on:
  - 27-02
  - 27-03
files_modified:
  - src/lib/savings-accrual-display.ts
  - src/lib/savings-accrual-display.test.ts
  - src/components/accounts/AccountList.tsx
  - src/app/accounts/page.tsx
  - src/app/accounts/actions.test.ts
autonomous: true
requirements:
  - ACCT-02
  - ACCT-03
estimate:
  tokens: 35000
  raw_tokens: 70000
  tasks: 3
  confidence: high
must_haves:
  truths:
    - SAVINGS list secondary shows Накопительный · CCY · {16.50}% · сегодня|через N дн. — not raw DOM (ACCT-03, D-11, D-12)
    - Countdown uses clampDayOfMonth + Moscow today injected from page (D-05, D-12)
    - Rate list uses dot major percent from formatBpsToPercentMajor (D-01, UI-SPEC A2)
    - Manual BalanceSnapshot upsert still accepts SAVINGS like other assets; metadata paths do not auto-write interest snapshots (ACCT-02, D-16)
    - "UI populated SAVINGS row: name; secondary meta; LOCF line like ASSET"
    - "UI zero-one-many: 0 empty state; 1+ bordered list; SAVINGS mixes with ASSET/credit"
    - "UI overflow list: page scrolls; name truncate + title full name"
    - "UI long-text countdown: fixed short chrome сегодня / через N дн."
    - "UI backstop long-text name: maxLength 120; list truncates; full name in edit Dialog"
  artifacts:
    - path: src/lib/savings-accrual-display.ts
      provides: nextAccrualAsOf; formatAccrualCountdown (display-only)
    - path: src/components/accounts/AccountList.tsx
      provides: SAVINGS secondary rate% + countdown
    - path: src/app/accounts/page.tsx
      provides: serializes annualRateBps + accrualDayOfMonth + Moscow today for list/form
  key_links:
    - from: accounts/page.tsx
      to: AccountList / AccountFormDialog
      via: props annualRateBps, accrualDayOfMonth, todayAsOf
      pattern: "annualRateBps"
    - from: formatAccrualCountdown
      to: clampDayOfMonth
      via: nextAccrualAsOf
      pattern: "clampDayOfMonth"
    - from: upsertBalanceSnapshot
      to: SAVINGS Account
      via: existing non-credit ≥0 path unchanged (D-16)
      pattern: "upsertBalanceSnapshot"
---

<objective>
Ship list/detail SAVINGS visibility (rate% + days-until) and prove manual snapshot path unchanged (ACCT-02, ACCT-03, D-11, D-12, D-16).

Purpose: Users see accrual schedule on `/accounts` without raw DOM; NW principal already covered by soft types — confirm snapshot honesty.
Output: savings-accrual-display module, AccountList secondary line, page serialization, green display + D-16 tests.
</objective>

<execution_context>
@/home/artem/Documents/wallet/.cursor/gsd-core/workflows/execute-plan.md
@/home/artem/Documents/wallet/.cursor/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/27-savings-schema-crud/27-CONTEXT.md
@.planning/phases/27-savings-schema-crud/27-RESEARCH.md
@.planning/phases/27-savings-schema-crud/27-UI-SPEC.md
@.planning/phases/27-savings-schema-crud/27-02-SUMMARY.md
@.planning/phases/27-savings-schema-crud/27-03-SUMMARY.md
@src/lib/dates.ts
@src/lib/savings-rate.ts
@src/components/accounts/AccountList.tsx
@src/app/accounts/page.tsx
@src/app/accounts/actions.ts
</context>

## Artifacts this phase produces

- `src/lib/savings-accrual-display.ts` (+ tests green)
- AccountList SAVINGS secondary segments
- page.tsx props for rate/DOM/today
- D-16 regression coverage on upsertBalanceSnapshot for SAVINGS fixture

<tasks>

<task type="auto" tdd="true">
  <name>Display-only next accrual + countdown helpers</name>
  <files>src/lib/savings-accrual-display.ts, src/lib/savings-accrual-display.test.ts</files>
  <read_first>
    .planning/phases/27-savings-schema-crud/27-RESEARCH.md
    .planning/phases/27-savings-schema-crud/27-UI-SPEC.md
    src/lib/dates.ts
    src/lib/savings-accrual-display.test.ts
  </read_first>
  <behavior>
    - nextAccrualAsOf(today, dom) uses clampDayOfMonth; Feb-31 → Feb-28; after accrual day rolls next month (D-05)
    - formatAccrualCountdown returns «сегодня» when next === today; else «через N дн.» for N≥1 (D-12)
  </behavior>
  <action>
    Implement src/lib/savings-accrual-display.ts pure helpers only — nextAccrualAsOf + formatAccrualCountdown (names may match Wave 0 imports). Reuse clampDayOfMonth / addCalendarDays from dates.ts; inject today string (Moscow calendarDateToday from page). Display-only — no monthly interest amount math, no ForecastSlotKind, no BalanceSnapshot writes (Phases 28–30 / D-16). Turn Wave 0 savings-accrual-display.test.ts green.
  </action>
  <verify>
    <automated>npx vitest run src/lib/savings-accrual-display.test.ts</automated>
    <fails_when>non-zero exit, or "0 passed" in the summary line</fails_when>
  </verify>
  <acceptance_criteria>
    - Clamp + «сегодня» / «через N дн.» cases green (D-05, D-12, ACCT-03)
    - Module has no Prisma imports and no interest amount formula
  </acceptance_criteria>
  <done>Accrual countdown helpers green</done>
</task>

<task type="auto">
  <name>AccountList secondary + page serialize rate/DOM/today</name>
  <files>src/components/accounts/AccountList.tsx, src/app/accounts/page.tsx</files>
  <read_first>
    .planning/phases/27-savings-schema-crud/27-UI-SPEC.md
    .planning/phases/27-savings-schema-crud/27-CONTEXT.md
    src/components/accounts/AccountList.tsx
    src/app/accounts/page.tsx
    src/lib/savings-rate.ts
    src/lib/savings-accrual-display.ts
    src/lib/dates.ts
  </read_first>
  <action>
    Extend AccountListItem + page findMany serialization with annualRateBps and accrualDayOfMonth; pass calendarDateToday() as todayAsOf into list (and form edit props from 27-03). For SAVINGS rows, secondary meta segments joined by ` · `: accountTypeLabel (Накопительный) · currencyCode · formatBpsToPercentMajor(bps)+`%` · formatAccrualCountdown(...) per D-11, D-12, ACCT-03. Do not render raw accrualDayOfMonth on the list secondary line. Keep LOCF balance line as existing asset pattern. Wire edit dialog props so SAVINGS prefills work. Empty state / truncate / mixed-type list behavior unchanged except SAVINGS meta. No Капитал UI chrome.
  </action>
  <verify>
    <automated>npx vitest run src/lib/savings-accrual-display.test.ts src/lib/account-type.test.ts &amp;&amp; grep -q 'annualRateBps' src/app/accounts/page.tsx &amp;&amp; grep -q 'accrualDayOfMonth' src/app/accounts/page.tsx &amp;&amp; grep -q 'formatAccrualCountdown\|formatBpsToPercentMajor' src/components/accounts/AccountList.tsx</automated>
    <fails_when>vitest non-zero, page omits rate/DOM serialization, or list lacks formatAccrualCountdown/formatBpsToPercentMajor usage</fails_when>
  </verify>
  <acceptance_criteria>
    - List secondary shows rate% + countdown, not raw DOM (D-12, ACCT-03)
    - Type label Накопительный via accountTypeLabel (D-11)
    - page.tsx serializes annualRateBps + accrualDayOfMonth for list/form
  </acceptance_criteria>
  <done>List/detail SAVINGS visibility live on /accounts</done>
</task>

<task type="auto" tdd="true">
  <name>D-16 manual snapshot path for SAVINGS unchanged</name>
  <files>src/app/accounts/actions.test.ts</files>
  <read_first>
    .planning/phases/27-savings-schema-crud/27-CONTEXT.md
    src/app/accounts/actions.ts
    src/app/accounts/actions.test.ts
  </read_first>
  <behavior>
    - upsertBalanceSnapshot accepts SAVINGS account with non-credit ≥0 amount like ASSET (D-16, ACCT-02)
    - createAccount / updateAccount metadata paths still do not write BalanceSnapshot (D-16)
  </behavior>
  <action>
    Extend actions.test.ts with SAVINGS fixture for upsertBalanceSnapshot happy path (reuse existing non-credit ≥0 assertions). Reconfirm metadata create/update tests still assert no snapshot side effects. Do not add auto interest accrual writer. Do not modify SetBalanceDialog UI.
  </action>
  <verify>
    <automated>npx vitest run src/app/accounts/actions.test.ts &amp;&amp; npm test</automated>
    <fails_when>non-zero exit on actions subset or full suite, or "0 passed" in a summary line</fails_when>
  </verify>
  <acceptance_criteria>
    - SAVINGS snapshot upsert covered green (D-16, ACCT-02)
    - Full npm test green as phase gate sampling
  </acceptance_criteria>
  <done>D-16 snapshot honesty proven; full suite green</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| RSC props → AccountList | Rate/DOM ints trusted from DB after Zod/CHECK |
| upsertBalanceSnapshot FormData → Account | Amount validation must treat SAVINGS as asset (≥0) |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-27-01 | Tampering | list display | low | accept | Read-only RSC props from DB; no client type edit on list |
| T-27-02 | Tampering | displayed rate | low | accept | formatBpsToPercentMajor from stored Int; writes gated elsewhere |
| T-27-05 | Tampering | auto interest snapshot | high | mitigate | No interest writer; upsertBalanceSnapshot remains manual only (D-16); tests assert metadata paths skip snapshot |
| T-27-SC | Tampering | npm installs | high | mitigate | Zero new packages |
</threat_model>

<verification>
savings-accrual-display + actions vitest green; list/page wired; npm test green.
</verification>

<success_criteria>
SAVINGS list shows rate% + countdown; manual snapshots work; no auto interest writes; full suite green.
</success_criteria>

<output>
Create `.planning/phases/27-savings-schema-crud/27-04-SUMMARY.md` when done
</output>
