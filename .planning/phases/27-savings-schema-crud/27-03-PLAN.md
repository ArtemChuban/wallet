---
phase: 27-savings-schema-crud
plan: 03
type: execute
wave: 2
depends_on:
  - 27-02
files_modified:
  - src/lib/validations/account.ts
  - src/app/accounts/actions.ts
  - src/components/accounts/AccountFormDialog.tsx
autonomous: true
requirements:
  - ACCT-01
  - ACCT-03
estimate:
  tokens: 35000
  raw_tokens: 70000
  tasks: 2
  confidence: high
must_haves:
  truths:
    - User can edit SAVINGS name + annual rate + accrual DOM in one dialog submit (ACCT-01, ACCT-03, D-06, D-08)
    - Edit title is «Изменить счёт»; description does not inventory editable fields (D-09)
    - Type and currency stay immutable after create; server loads type from DB (D-08, T-27-01)
    - Clearing rate or DOM to null on edit is rejected (D-06)
    - Rate/DOM fields visible on edit only when account.type === SAVINGS (D-10)
    - "UI empty edit SAVINGS: prefills name + rate major + DOM from row; never blank rate/DOM for existing SAVINGS"
    - "UI partial edit non-SAVINGS: rate/DOM absent; name editable; credit limit read-only when credit"
    - "UI loading form: pending disables submit; label «Сохранение…»; Dialog stays open until success"
    - "UI error form: field errors under inputs + form-level Copywriting / state.message"
  artifacts:
    - path: src/lib/validations/account.ts
      provides: updateAccountSchema requiring name; savings rate+DOM when action gates SAVINGS
    - path: src/app/accounts/actions.ts
      provides: coherent updateAccount for SAVINGS name+rate+DOM
    - path: src/components/accounts/AccountFormDialog.tsx
      provides: edit SAVINGS fields + D-09 copy + create gates complete
  key_links:
    - from: AccountFormDialog edit submit
      to: updateAccount action
      via: DB type === SAVINGS requires percent+DOM → annualRateBps
      pattern: "updateAccount"
    - from: updateAccount
      to: Account.type in DB
      via: ignore client type/currency (D-08)
      pattern: "findUnique"
---

<objective>
Expand tracer with coherent SAVINGS edit (name+rate+DOM) and locked RU form chrome (ACCT-01, ACCT-03, D-06, D-08, D-09, D-10, D-13).

Purpose: After create works, users must change rate/DOM without name-only dead-end.
Output: updateAccount path + AccountFormDialog edit/create polish; green actions update tests.
</objective>

<execution_context>
@/home/artem/Documents/wallet/.cursor/gsd-core/workflows/execute-plan.md
@/home/artem/Documents/wallet/.cursor/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/27-savings-schema-crud/27-CONTEXT.md
@.planning/phases/27-savings-schema-crud/27-RESEARCH.md
@.planning/phases/27-savings-schema-crud/27-UI-SPEC.md
@.planning/phases/27-savings-schema-crud/27-PATTERNS.md
@.planning/phases/27-savings-schema-crud/27-02-SUMMARY.md
@src/lib/validations/account.ts
@src/app/accounts/actions.ts
@src/components/accounts/AccountFormDialog.tsx
@src/lib/savings-rate.ts
</context>

## Artifacts this phase produces

- Coherent `updateAccount` (or evolved `updateAccountName`) for SAVINGS name+rate+DOM
- `updateAccountSchema` / refine twin for edit
- Edit dialog copy «Изменить счёт» / «Тип и валюта не меняются.»
- Prefill formatBpsToPercentMajor for edit rate field

<tasks>

<task type="auto" tdd="true">
  <name>Coherent updateAccount for SAVINGS name+rate+DOM</name>
  <files>src/lib/validations/account.ts, src/app/accounts/actions.ts</files>
  <read_first>
    .planning/phases/27-savings-schema-crud/27-CONTEXT.md
    .planning/phases/27-savings-schema-crud/27-RESEARCH.md
    src/lib/validations/account.ts
    src/app/accounts/actions.ts
    src/app/accounts/actions.test.ts
    src/lib/savings-rate.ts
  </read_first>
  <behavior>
    - updateAccount for SAVINGS persists new name + annualRateBps + accrualDayOfMonth together (D-06, D-08, ACCT-01)
    - Reject empty/null rate or DOM on SAVINGS update (D-06)
    - Non-SAVINGS update changes name only; ignores/rejects savings fields (D-08, D-15)
    - Server uses DB account.type — client cannot change type/currency (D-08, T-27-01)
    - Metadata update does not call balanceSnapshot create/update (D-16)
  </behavior>
  <reversibility rating="costly">D-08 breaks prior name-only edit contract for SAVINGS; callers and UI must stay coherent.</reversibility>
  <action>
    Prefer one updateAccount action replacing/extending updateAccountName (discretion): always validate name; load account by id; if type === SAVINGS require annualRatePercentMajor + accrualDayOfMonth 1–31, parsePercentToBps, persist both ints (never null) per D-06, D-08, ACCT-01. If not SAVINGS, update name only and do not write savings columns. Ignore client-supplied type/currency. Keep existing grace/credit actions untouched. Ensure Wave 0 actions update tests turn green. UI-SPEC error strings for missing/invalid rate/DOM should surface via action state messages when validation fails. Do not invent interest snapshot writers.
  </action>
  <verify>
    <automated>npx vitest run src/app/accounts/actions.test.ts src/lib/validations/account.test.ts</automated>
    <fails_when>non-zero exit, or "0 passed" in the summary line</fails_when>
  </verify>
  <acceptance_criteria>
    - SAVINGS update persists name+bps+DOM in one path (D-06, D-08)
    - Non-SAVINGS remains name-only regarding savings fields
    - Actions tests asserting no BalanceSnapshot on metadata update are green (D-16)
  </acceptance_criteria>
  <done>Coherent SAVINGS update action green under Vitest</done>
</task>

<task type="auto">
  <name>AccountFormDialog edit/create chrome for SAVINGS</name>
  <files>src/components/accounts/AccountFormDialog.tsx</files>
  <read_first>
    .planning/phases/27-savings-schema-crud/27-UI-SPEC.md
    .planning/phases/27-savings-schema-crud/27-CONTEXT.md
    src/components/accounts/AccountFormDialog.tsx
    src/app/accounts/actions.ts
  </read_first>
  <action>
    Wire edit mode to coherent updateAccount: for account.type === SAVINGS show editable «Годовой %» and «День начисления» prefilled via formatBpsToPercentMajor / DOM int (D-08, D-10, D-13, ACCT-03). Title all types «Изменить счёт»; description «Тип и валюта не меняются.» — do not list editable fields (D-09). Drop legacy name-only title/description. Create path already gated in 27-02 — ensure type switch clears savings fields from submit payload when leaving SAVINGS; pending label «Сохранение…»; field/form errors per UI-SPEC Copywriting. Extend AccountRow props with annualRateBps + accrualDayOfMonth (page wiring may land in 27-04). maxLength 120 on name unchanged (UI backstop). Do not add list secondary line here.
  </action>
  <verify>
    <automated>npx vitest run src/app/accounts/actions.test.ts &amp;&amp; grep -q 'Изменить счёт' src/components/accounts/AccountFormDialog.tsx &amp;&amp; grep -q 'Годовой %' src/components/accounts/AccountFormDialog.tsx &amp;&amp; grep -q 'День начисления' src/components/accounts/AccountFormDialog.tsx &amp;&amp; ! grep -q 'только название' src/components/accounts/AccountFormDialog.tsx</automated>
    <fails_when>vitest non-zero, D-09 title missing, gated labels missing, or legacy «только название» copy still present</fails_when>
  </verify>
  <acceptance_criteria>
    - Edit dialog title «Изменить счёт» (D-09)
    - SAVINGS edit shows rate+DOM fields; non-SAVINGS does not (D-10)
    - Legacy name-only edit marketing copy removed
  </acceptance_criteria>
  <done>Form create/edit SAVINGS chrome matches UI-SPEC + D-08…D-13</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Browser edit FormData → updateAccount | Client may forge type/currency/rate |
| updateAccount → SQLite CHECK | Invalid null rate/DOM must fail Zod before/at DB |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-27-01 | Tampering | edit type/currency | high | mitigate | Load type/currency from DB; ignore client values (D-08) |
| T-27-02 | Tampering | edit rate | high | mitigate | Zod + parsePercentToBps &gt;= 0; CHECK retains bps &gt;= 0 |
| T-27-03 | Tampering | savings fields on non-SAVINGS edit | high | mitigate | Action ignores/rejects savings fields unless DB type SAVINGS |
| T-27-04 | Tampering | edit DOM | medium | mitigate | Zod 1–31; null clear rejected (D-06) |
| T-27-05 | Tampering | snapshot on metadata edit | high | mitigate | updateAccount writes Account only (D-16) |
| T-27-SC | Tampering | npm installs | high | mitigate | Zero new packages |
</threat_model>

<verification>
actions + account validation vitest green; form has D-09 title and D-13 labels; legacy name-only copy gone.
</verification>

<success_criteria>
User can edit SAVINGS rate/DOM with locked chrome; type/currency immutable; no snapshot side effects on metadata update.
</success_criteria>

<output>
Create `.planning/phases/27-savings-schema-crud/27-03-SUMMARY.md` when done
</output>
