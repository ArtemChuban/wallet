---
phase: 14-dohody-crud-nav
plan: 03
type: execute
wave: 3
depends_on:
  - "14-02"
files_modified:
  - src/app/income/page.tsx
  - src/components/income/IncomeList.tsx
  - src/components/income/IncomeFormDialog.tsx
  - src/components/income/income-ui.test.ts
autonomous: true
requirements:
  - SRC-01
  - SRC-02
  - UI-01
estimate:
  tokens: 30000
  raw_tokens: 60000
  tasks: 3
  confidence: high
must_haves:
  truths:
    - "Income list groups by Person including empty groups with «Нет доходов» + CTA (D-01, D-03, UI-SPEC E2)"
    - "Rows within a group sort by nearest next planned date ascending, mixed recurring+one-time (D-02)"
    - "Each compact row shows type label Ежемесячный/Разовый + amount + currency + planned date; no note on row (D-04)"
    - "Single «Новый доход» opens one dialog with kind toggle; create defaults kind=recurring and currency=primary (D-05, D-08)"
    - "In-group create pre-fills defaultPersonId; dialog supports existing/new Person (D-06, D-07)"
    - "Edit only via «Изменить» button — not row-click-to-edit (D-12)"
    - "Header CTAs «Новый человек» + «Новый доход» when people exist (D-15); honesty copy always visible (D-11)"
    - "Income source delete and person delete use DestructiveConfirmStep with UI-SPEC Russian copy (UI-01, D-16)"
    - "Definitions are list entities; next planned may be past without overdue styling (D-09, D-10)"
    - "Zero people: «Нет людей» empty + CTAs (UI-SPEC E1); pending submit disables primary and keeps dialog open (UI Considerations loading)"
    - "Form errors show field + form-level Copywriting error; person delete blocked copy when refs exist (UI Considerations error)"
  prohibitions:
    - statement: MUST NOT use browser native confirm dialogs in income components
      requirement_id: UI-01
      category: absence
    - statement: MUST NOT show record-actual, overdue заполни chrome, variance, totals hero, or DebtDetailDialog analog
      requirement_id: UI-01
      category: absence
    - statement: MUST NOT default create currency to first-by-code sort — must use primary (D-08)
      requirement_id: SRC-01
      category: values
  artifacts:
    - path: src/components/income/IncomeList.tsx
      provides: Person-grouped list + person delete confirm + empty CTAs
    - path: src/components/income/IncomeFormDialog.tsx
      provides: kind-toggle create/edit + source delete confirm
    - path: src/app/income/page.tsx
      provides: full load + nextOpen mapping + header CTAs
    - path: src/components/income/income-ui.test.ts
      provides: file-scan DestructiveConfirmStep present; native confirm absent
  key_links:
    - from: IncomeFormDialog
      to: income actions
      via: useActionState create/update + delete FormData
      pattern: "useActionState"
    - from: IncomeList
      to: deletePerson
      via: DestructiveConfirmStep
      pattern: "DestructiveConfirmStep"
    - from: page.tsx
      to: nextOpenPlannedAsOf
      via: map recurring defs before list props
      pattern: "nextOpenPlannedAsOf"
---

<objective>
Wire Debts-clone income UI: Person-grouped list, kind-toggle form dialog, DestructiveConfirm deletes (D-01..D-16, SRC-01, SRC-02, UI-01).

Purpose: User-visible CRUD completing Phase 14 success criteria.
Output: IncomeList + IncomeFormDialog + fully wired `/income` page + UI file-scan tests.
</objective>

<execution_context>
@/home/artem/Documents/wallet/.cursor/gsd-core/workflows/execute-plan.md
@/home/artem/Documents/wallet/.cursor/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/14-dohody-crud-nav/14-CONTEXT.md
@.planning/phases/14-dohody-crud-nav/14-UI-SPEC.md
@.planning/phases/14-dohody-crud-nav/14-PATTERNS.md
@.planning/phases/14-dohody-crud-nav/14-RESEARCH.md
@src/app/debts/page.tsx
@src/components/debts/DebtsList.tsx
@src/components/debts/DebtFormDialog.tsx
@src/components/debts/PersonFormDialog.tsx
@src/components/ui/destructive-confirm-step.tsx
@src/app/income/page.tsx
@src/app/income/actions.ts
@src/lib/income.ts
</context>

## Artifacts this phase produces

- Full `/income` CRUD UI (list + dialogs)
- File-scan proof DestructiveConfirmStep used; no native confirm in income UI

<tasks>

<task type="tracer">
  <name>End-to-end create recurring via dialog on Person-grouped list</name>
  <reversibility rating="costly">D-01 Person-grouped list/page data shape mirrors DebtsList — changing grouping later rewrites list components.</reversibility>
  <files>src/app/income/page.tsx, src/components/income/IncomeList.tsx, src/components/income/IncomeFormDialog.tsx</files>
  <read_first>
    src/app/debts/page.tsx
    src/components/debts/DebtsList.tsx
    src/components/debts/DebtFormDialog.tsx
    src/app/income/page.tsx
    src/app/income/actions.ts
    src/lib/income.ts
    .planning/phases/14-dohody-crud-nav/14-UI-SPEC.md
  </read_first>
  <behavior>
    - Page loads all people with recurringIncomes + oneTimeIncomes (+ actuals needed for next-open), currencies, primaryCurrencyCode (D-01, D-03, D-08)
    - IncomeList renders at least one person group; empty group shows «Нет доходов» + «Новый доход» with defaultPersonId (D-03, D-06)
    - IncomeFormDialog create mode: kind toggle defaults recurring; currency state initializes to primaryCurrencyCode not currencies[0] (D-05, D-08)
    - Submitting create recurring calls createRecurringIncome and closes on success via router.refresh
  </behavior>
  <action>
    Expand page.tsx to include recurring/one-time (+ actuals for next-open), compute nextOpenPlannedAsOf per recurring def, pass serializable props (BigInt→string) into IncomeList (D-02, D-09, D-10). Header: when people.length &gt; 0 show PersonFormDialog «Новый человек» + IncomeFormDialog «Новый доход» (D-15); keep D-11 honesty. Implement IncomeList clone of DebtsList person groups without CLOSED section or detail dialog (D-01, D-12). Implement IncomeFormDialog create path only for tracer: kind Select «Ежемесячный»/«Разовый», person existing/new toggle (D-07), primary currency default (D-08), visible «Начало с» default calendarDateToday Europe/Moscow, amount/dayOfMonth fields; useActionState → createRecurringIncome / createOneTimeIncome by kind. Do not implement edit/delete confirm yet (next task). Do not copy debts currencies[0] default.
  </action>
  <verify>
    <automated>test -f src/components/income/IncomeList.tsx &amp;&amp; test -f src/components/income/IncomeFormDialog.tsx &amp;&amp; grep -q 'defaultPersonId' src/components/income/IncomeList.tsx &amp;&amp; grep -q 'primaryCurrencyCode' src/components/income/IncomeFormDialog.tsx &amp;&amp; grep -q 'IncomeList' src/app/income/page.tsx &amp;&amp; grep -q 'Новый доход' src/app/income/page.tsx &amp;&amp; npx vitest run src/lib/validations/income.test.ts src/app/income/actions.test.ts src/components/nav.test.ts</automated>
    <fails_when>component files missing, primaryCurrencyCode/defaultPersonId wiring absent, page missing IncomeList/Новый доход, or vitest non-zero</fails_when>
  </verify>
  <acceptance_criteria>
    - Page wires IncomeList with people + currencies + primaryCurrencyCode
    - Create dialog defaults kind recurring and currency primary (D-05, D-08)
    - Empty group CTA passes defaultPersonId (D-06)
  </acceptance_criteria>
  <done>Thin create path visible on Person-grouped /income.</done>
</task>

<task type="auto">
  <name>Edit/delete income + person delete via DestructiveConfirmStep (D-12, D-16, UI-01)</name>
  <files>src/components/income/IncomeList.tsx, src/components/income/IncomeFormDialog.tsx, src/components/income/income-ui.test.ts</files>
  <read_first>
    src/components/debts/DebtFormDialog.tsx
    src/components/debts/DebtsList.tsx
    src/components/ui/destructive-confirm-step.tsx
    .planning/phases/14-dohody-crud-nav/14-UI-SPEC.md
  </read_first>
  <behavior>
    - Compact rows show type + amount + currency + next planned date; «Изменить» opens edit dialog; no row role=button click-to-edit (D-04, D-12)
    - Edit locks person/currency/kind read-only; editable amount + schedule + note (A1)
    - Edit dialog delete enters confirm-delete step using DestructiveConfirmStep with income source copy from UI-SPEC (UI-01)
    - Person header delete uses DestructiveConfirmStep; blocks with UI-SPEC message when debts or income refs (D-16)
    - income-ui.test.ts file-scan: income components import DestructiveConfirmStep; source text must not call native confirm APIs
  </behavior>
  <action>
    Complete IncomeFormDialog edit mode + confirm-delete step mirroring DebtFormDialog (D-12, UI-01) with UI-SPEC titles/labels/delete copy. Kind locked on edit. Wire deleteRecurringIncome / deleteOneTimeIncome. Complete IncomeList row rendering sorted by nextPlannedAsOf, type labels, formatMinorToMajor, «Изменить» outline button only. Person rename via PersonFormDialog; person delete dialog + DestructiveConfirmStep + deletePerson from debts actions; pre-check debts-or-income. Add `src/components/income/income-ui.test.ts` reading IncomeList/IncomeFormDialog source: expect DestructiveConfirmStep import present; expect no `window.confirm` / `confirm(` browser confirm usage. Past dates render as normal date text — no overdue class/chrome (D-10).
    <!-- planner-discipline-allow: window.confirm -->
  </action>
  <verify>
    <automated>npx vitest run src/components/income/income-ui.test.ts &amp;&amp; grep -q 'DestructiveConfirmStep' src/components/income/IncomeFormDialog.tsx &amp;&amp; grep -q 'DestructiveConfirmStep' src/components/income/IncomeList.tsx &amp;&amp; grep -q 'Изменить' src/components/income/IncomeList.tsx</automated>
    <fails_when>vitest non-zero, DestructiveConfirmStep missing from list/dialog, or Изменить control absent</fails_when>
  </verify>
  <acceptance_criteria>
    - Edit and delete flows use DestructiveConfirmStep
    - income-ui.test.ts fails if native confirm appears in income components
    - Row has no click-to-edit handler; only «Изменить»
  </acceptance_criteria>
  <done>Full edit/delete + person delete confirms ship.</done>
</task>

<task type="auto">
  <name>UI-SPEC empty/error polish + full suite gate</name>
  <files>src/app/income/page.tsx, src/components/income/IncomeList.tsx, src/components/income/IncomeFormDialog.tsx</files>
  <read_first>
    .planning/phases/14-dohody-crud-nav/14-UI-SPEC.md
    src/components/income/IncomeList.tsx
    src/components/income/IncomeFormDialog.tsx
  </read_first>
  <behavior>
    - Zero people empty uses UI-SPEC heading/body + both CTAs (E1)
    - Form-level error uses UI-SPEC «Не удалось сохранить…» on action failure
    - Pending states disable submit / show Удаление… on confirm (loading contract)
    - Long person names wrap/break-all like DebtsList (overflow)
    - npm test full suite green including flipped UI-00 and nav order
  </behavior>
  <action>
    Sweep copy against UI-SPEC Copywriting Contract (empty, errors, dialog titles/actions, field labels). Ensure create one-time shows optional «Заметка»; recurring shows «День месяца» + «Начало с». Confirm no overdue/actual UI leaked. Run full `npm test` and fix regressions from Phase 13 UI-00 flip / nav expectations. Soften PersonFormDialog description only if already touched; otherwise leave (A5).
  </action>
  <verify>
    <automated>npm test</automated>
    <fails_when>non-zero exit, or any failing vitest suite in summary</fails_when>
  </verify>
  <acceptance_criteria>
    - UI-SPEC locked strings present for honesty, empty states, blocked delete, kind labels
    - npm test green (includes nav, income, debts actions, validations)
    - No Phase 15 overdue chrome in income components
  </acceptance_criteria>
  <done>Phase 14 UI complete; full suite green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Browser UI → Server Actions | Client can omit confirm step; server still enforces validation/Restrict |
| Rendered notes/names | XSS surface via React text nodes |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-14-02 | Spoofing | note/person name display | low | mitigate | React text escaping; Zod max length |
| T-14-05 | Elevation | person delete UI | medium | mitigate | Client pre-check + server recount (Plan 02); confirm step UX only |
| T-14-07 | Tampering | bypass confirm UI | low | accept | Confirm is UX constitution; mutations still require explicit action POST |
| T-14-03 | Tampering | accidental NW coupling in UI | medium | mitigate | Income components call income/person actions only — no snapshot writers |
| T-14-SC | Tampering | npm installs | high | mitigate | Zero new packages; shadcn blocks already installed |
</threat_model>

<verification>
npm test
</verification>

<success_criteria>
User manages recurring/one-time income from «Доходы»; deletes use DestructiveConfirmStep; listing/creating never changes balances; suite green.
</success_criteria>

<output>
Create `.planning/phases/14-dohody-crud-nav/14-03-SUMMARY.md` when done
</output>
