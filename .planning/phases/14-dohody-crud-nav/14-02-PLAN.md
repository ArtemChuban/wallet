---
phase: 14-dohody-crud-nav
plan: 02
type: execute
wave: 2
depends_on:
  - "14-01"
files_modified:
  - src/app/income/actions.ts
  - src/app/income/actions.test.ts
  - src/app/debts/actions.ts
  - src/app/debts/actions.test.ts
  - src/components/debts/DebtsList.tsx
autonomous: true
requirements:
  - SRC-01
  - SRC-02
  - UI-01
estimate:
  tokens: 27500
  raw_tokens: 55000
  tasks: 3
  confidence: high
must_haves:
  truths:
    - "User can persist recurring income via Server Action with dayOfMonth, plannedAmountMinor, currency, person, startAsOf (SRC-01)"
    - "User can persist one-time income via Server Action with plannedAsOf, amount, currency, person, optional note (SRC-02)"
    - "Create supports existing personId and nested new Person (D-07)"
    - "Update/delete definition actions revalidatePath /income only for income mutations (UI-01)"
    - "deletePerson blocks when debts OR recurring/one-time income refs exist; message matches UI-SPEC (D-16)"
    - "Person create/rename/delete revalidate both /debts and /income (D-16)"
    - "Income actions never write account-balance snapshots or import net-worth/historical-series modules (UI-01 / ISO)"
    - "Two identical create submits insert two rows — not upsert (SRC-01/02 idempotency)"
    - "One-time update calls assertOneTimePlanImmutable when an actual exists (Phase 13 lock)"
  prohibitions:
    - statement: MUST NOT mutate Account balances or net-worth historical series from income actions
      requirement_id: UI-01
      category: isolation
    - statement: MUST NOT revalidatePath dashboard root '/' from income or person mutations
      requirement_id: UI-01
      category: isolation
    - statement: MUST NOT cascade-delete Person when income/debts remain — Restrict only
      requirement_id: UI-01
      category: integrity
  artifacts:
    - path: src/app/income/actions.ts
      provides: create/update/delete recurring + one-time Server Actions
    - path: src/app/income/actions.test.ts
      provides: Zod error paths + isolation file-scan
    - path: src/app/debts/actions.ts
      provides: deletePerson income counts + dual revalidatePath
    - path: src/app/debts/actions.test.ts
      provides: income Restrict + dual revalidate asserts
    - path: src/components/debts/DebtsList.tsx
      provides: client pre-check + blocked message for debts-or-income
  key_links:
    - from: createRecurringIncome
      to: prisma.recurringIncome.create
      via: Zod safeParse + parseMajorToMinor
      pattern: "revalidatePath(\"/income\")"
    - from: deletePerson
      to: prisma.recurringIncome.count / prisma.oneTimeIncome.count
      via: Restrict gate before person.delete
      pattern: "долги или доходы"
    - from: actions.test.ts
      to: actions.ts source text
      via: isolation file-scan
      pattern: "actions.test.ts"
---

<objective>
Ship income Server Actions (SRC-01, SRC-02) with isolation scans (UI-01) and extend Person Restrict + dual revalidate across debts/income (D-16).

Purpose: Persist definitions before full UI wiring; close Person delete hole that ignores income FKs.
Output: `src/app/income/actions.ts` + tests; updated debts Person actions + DebtsList pre-check.
</objective>

<execution_context>
@/home/artem/Documents/wallet/.cursor/gsd-core/workflows/execute-plan.md
@/home/artem/Documents/wallet/.cursor/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/14-dohody-crud-nav/14-CONTEXT.md
@.planning/phases/14-dohody-crud-nav/14-RESEARCH.md
@.planning/phases/14-dohody-crud-nav/14-PATTERNS.md
@.planning/phases/14-dohody-crud-nav/14-UI-SPEC.md
@src/app/debts/actions.ts
@src/app/debts/actions.test.ts
@src/components/debts/DebtsList.tsx
@src/lib/validations/income.ts
@src/lib/income.ts
@src/lib/money.ts
</context>

<assumption_delta_decision>
- **Noun primary:** optional note on one-time create — unchanged from Plan 01 (no-change).
- **Decision:** no-change
- **Rationale:** Action maps empty note to null/omit; does not require note.
</assumption_delta_decision>

## Artifacts this phase produces

- Income Server Actions + isolation tests
- Person Restrict spanning debts + income with dual `revalidatePath`
- (UI list/dialog in Plan 03)

<tasks>

<task type="tracer">
  <name>End-to-end createRecurringIncome action + isolation scan scaffold</name>
  <files>src/app/income/actions.ts, src/app/income/actions.test.ts</files>
  <read_first>
    src/app/debts/actions.ts
    src/app/debts/actions.test.ts
    src/lib/validations/income.ts
    src/lib/money.ts
    .planning/phases/14-dohody-crud-nav/14-PATTERNS.md
    node_modules/next/dist/docs/01-app/02-guides/server-actions.md
  </read_first>
  <behavior>
    - createRecurringIncome with valid personId FormData persists RecurringIncome and returns success (SRC-01)
    - Invalid dayOfMonth / amount returns fieldErrors without Prisma write
    - actions.ts source must not reference account-balance snapshot model names or net-worth / historical-series import paths (UI-01)
    - On success calls revalidatePath("/income") only (not dashboard root)
  </behavior>
  <action>
    Create `"use server"` `src/app/income/actions.ts` mirroring debts action header helpers (ensureSqlitePragmas, fracDigitCount/scale checks as debts). Implement tracer `createRecurringIncome`: branch personId present → createRecurringIncomeSchema; else WithNewPerson nested person+recurring create (D-07). Parse plannedAmountMajor via parseMajorToMinor + currency.scale; default path requires startAsOf from FormData (caller supplies Moscow today). prisma.recurringIncome.create; revalidatePath("/income") only. Add `src/app/income/actions.test.ts` with mocked prisma: validation reject path + success path + file-scan that actions.ts has no account-balance snapshot identifiers and no `@/lib/net-worth` or `@/lib/historical-series` imports (UI-01). Do not implement one-time/update/delete yet (next task). Do not import UI components.
    <!-- planner-discipline-allow: BalanceSnapshot -->
  </action>
  <verify>
    <automated>npx vitest run src/app/income/actions.test.ts &amp;&amp; test -f src/app/income/actions.ts &amp;&amp; grep -q 'createRecurringIncome' src/app/income/actions.ts &amp;&amp; grep -q 'revalidatePath("/income")' src/app/income/actions.ts</automated>
    <fails_when>vitest non-zero, actions.ts missing, createRecurringIncome or revalidatePath("/income") absent</fails_when>
  </verify>
  <acceptance_criteria>
    - createRecurringIncome exported and covered by at least one success + one validation-failure test
    - Isolation file-scan in actions.test.ts fails if net-worth or historical-series imports appear
    - No revalidatePath("/") in income actions
  </acceptance_criteria>
  <done>Recurring create path + isolation scaffold green.</done>
</task>

<task type="auto" tdd="true">
  <name>One-time + update/delete income actions (SRC-02, UI-01)</name>
  <files>src/app/income/actions.ts, src/app/income/actions.test.ts</files>
  <read_first>
    src/app/income/actions.ts
    src/lib/validations/income.ts
    src/lib/income.ts
    src/app/debts/actions.ts
  </read_first>
  <behavior>
    - createOneTimeIncome persists plannedAsOf + optional note (SRC-02)
    - updateRecurring / updateOneTime edit amount/schedule/note only; smuggled person/currency ignored via .strict()
    - updateOneTime invokes assertOneTimePlanImmutable when actual exists
    - deleteRecurring / deleteOneTime delete definition (Cascade actuals via schema); DestructiveConfirm is UI-only
    - Second identical create inserts another row (idempotency = insert)
  </behavior>
  <action>
    Expand actions.ts with createOneTimeIncome (+ WithNewPerson), updateRecurringIncome, updateOneTimeIncome, deleteRecurringIncome, deleteOneTimeIncome per PATTERNS. On one-time update: load existing + actual count/join; call assertOneTimePlanImmutable before write when actual present. Always revalidatePath("/income") on success. Extend actions.test.ts for one-time create, update reject when immutable, delete success, and isolation scan still green. Keep money as BigInt minors only (precision). Do not add list UI.
  </action>
  <verify>
    <automated>npx vitest run src/app/income/actions.test.ts &amp;&amp; grep -q 'createOneTimeIncome' src/app/income/actions.ts &amp;&amp; grep -q 'assertOneTimePlanImmutable' src/app/income/actions.ts &amp;&amp; grep -q 'deleteRecurringIncome\|deleteOneTimeIncome' src/app/income/actions.ts</automated>
    <fails_when>vitest non-zero, or createOneTime/assertOneTime/delete exports missing</fails_when>
  </verify>
  <acceptance_criteria>
    - All six CRUD entry points exist (create×2 kinds, update×2, delete×2) or kind-dispatched equivalents covering both models
    - assertOneTimePlanImmutable wired on one-time update
    - Isolation scan still passes
  </acceptance_criteria>
  <done>Full income definition CRUD actions green.</done>
</task>

<task type="auto" tdd="true">
  <name>Person Restrict spans income + dual revalidatePath (D-16)</name>
  <files>src/app/debts/actions.ts, src/app/debts/actions.test.ts, src/components/debts/DebtsList.tsx</files>
  <read_first>
    src/app/debts/actions.ts
    src/app/debts/actions.test.ts
    src/components/debts/DebtsList.tsx
    .planning/phases/14-dohody-crud-nav/14-UI-SPEC.md
  </read_first>
  <behavior>
    - deletePerson blocks when debtCount=0 but recurringIncome or oneTimeIncome count &gt; 0 (D-16)
    - Blocked message is exactly `Нельзя удалить человека, пока есть долги или доходы`
    - Successful deletePerson / createPerson / renamePerson call revalidatePath for both /debts and /income
    - DebtsList client pre-check uses combined debt+income count / flag from page props
  </behavior>
  <action>
    Extend deletePerson: after debt.count, also count prisma.recurringIncome and prisma.oneTimeIncome for personId; if any &gt; 0 return blocked UI-SPEC message (D-16). On success and on createPerson/renamePerson success, revalidatePath("/debts") and revalidatePath("/income"). Update DebtsList BLOCKED_DELETE_MESSAGE and pre-check to treat income refs like debts — page must pass incomeCount or combined hasBlockingRefs (update debts/page.tsx props only if required for compile; prefer computing counts in DebtsList from included relation lengths if page already loads people without income — then extend debts page include or pass explicit counts). Extend actions.test.ts: mock income counts; assert block when income&gt;0 debt=0; assert dual revalidate on success; update old message expectations. Server remains source of truth (client pre-check is UX only).
  </action>
  <verify>
    <automated>npx vitest run src/app/debts/actions.test.ts &amp;&amp; grep -q 'долги или доходы' src/app/debts/actions.ts &amp;&amp; grep -q 'revalidatePath("/income")' src/app/debts/actions.ts &amp;&amp; grep -q 'долги или доходы' src/components/debts/DebtsList.tsx</automated>
    <fails_when>vitest non-zero, blocked message or /income revalidate missing from debts actions, or DebtsList message stale</fails_when>
  </verify>
  <acceptance_criteria>
    - deletePerson tests cover income-only block and dual revalidate
    - DebtsList shows updated blocked copy
    - createPerson/renamePerson also dual-revalidate
  </acceptance_criteria>
  <done>Person Restrict + dual revalidate across domains.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Browser → income Server Actions | Untrusted FormData crosses into Prisma writes |
| Browser → deletePerson | Client pre-check bypassable; server must recount |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-14-01 | Tampering | income actions FormData | medium | mitigate | Zod `.strict()` + coerce int IDs + positive major before Prisma |
| T-14-03 | Tampering | side-ledger isolation | high | mitigate | File-scan bans snapshot/NW/historical imports in actions.ts |
| T-14-05 | Elevation | deletePerson | medium | mitigate | Server recount debts+income before delete; FK Restrict backstop |
| T-14-06 | Tampering | one-time plan after actual | medium | mitigate | assertOneTimePlanImmutable on update |
| T-14-04 | Elevation | unauthenticated actions | low | accept | Local single-user app |
| T-14-SC | Tampering | npm installs | high | mitigate | Zero new packages |
</threat_model>

<verification>
npx vitest run src/app/income/actions.test.ts src/app/debts/actions.test.ts
</verification>

<success_criteria>
Income CRUD actions persist definitions; isolation scan green; Person delete blocks on income refs; dual revalidate works.
</success_criteria>

<output>
Create `.planning/phases/14-dohody-crud-nav/14-02-SUMMARY.md` when done
</output>
