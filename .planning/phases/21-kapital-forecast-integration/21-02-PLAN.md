---
phase: 21-kapital-forecast-integration
plan: 02
type: execute
wave: 2
depends_on:
  - 21-01
files_modified:
  - src/app/page.tsx
  - src/components/dashboard/DashboardChartsShell.tsx
  - src/components/dashboard/nw-forecast-ui.test.ts
autonomous: true
requirements:
  - GRFCST-01
  - GRFCST-02
estimate:
  tokens: 40000
  raw_tokens: 80000
  tasks: 2
  confidence: high
must_haves:
  truths:
    - "Капитал `/` RSC loads OPEN credit grace obligations with account currency/name and passes forecastGrace props (C-07, GRFCST-01)"
    - "DashboardChartsShell merges income slots + grace slots into one buildNetWorthForecastSeries call (C-03)"
    - "showForecast true when FX-included grace exists even if cumulative NW flat (D-07)"
    - "One quiet banner under NW chart: «Прогноз неполный · нет курса {CODES}» with unique alphabetical codes across income+grace (D-14…D-17, GRFCST-02)"
    - "When all slots FX-excluded: Line hidden (includedSlotCount===0) but banner still shows full code list (D-18)"
    - "mergeFactAndForecast attaches forecastEvents onto chart points for today + future sample dates (D-06, D-09 prep)"
    - "No income/grace kind tags in banner copy — currency codes only (D-16)"
    - "Empty forecast (no income slots and no grace slots, no FX exclusions): hide «Прогноз» Line + legend; fact chart continues unchanged (UI-SPEC empty E1)"
    - "Loading: RSC page load + client useMemo merge — no grace-specific skeleton on chart/shell (UI-SPEC loading E1)"
    - "Page load error: existing `/` shell failure path unchanged — no new forecast-specific error chrome (UI-SPEC error E3)"
    - statement: "Long missing-code list wraps inside existing p-4 muted banner card without ellipsis (UI-SPEC backstop)"
      verification: backstop
  prohibitions:
    - statement: MUST NOT add a second chart series or legend entry for grace
      requirement_id: GRFCST-01
      category: scope
    - statement: MUST NOT feed CLOSED obligations into forecastGrace
      requirement_id: GRFCST-01
      category: isolation
    - statement: MUST NOT call BalanceSnapshot writes or import grace into computeNetWorthRows path
      requirement_id: GRFCST-01
      category: isolation
    - statement: MUST NOT invent FX when rate missing
      requirement_id: GRFCST-02
      category: values
  artifacts:
    - path: src/app/page.tsx
      provides: OPEN grace load + forecastGrace serialization
    - path: src/components/dashboard/DashboardChartsShell.tsx
      provides: grace slot merge, banner codes, forecastEvents on points
    - path: src/components/dashboard/nw-forecast-ui.test.ts
      provides: green Plan 02 banner-code file-scan asserts
  key_links:
    - from: page.tsx OPEN query
      to: DashboardChartsShell forecastGrace
      via: string minors across RSC boundary
      pattern: "forecastGrace"
    - from: openGraceForecastMembership
      to: buildNetWorthForecastSeries slots
      via: kind grace concat with income openSlots
      pattern: "kind: \"grace\""
    - from: excludedMissingFxCurrencies
      to: partial banner
      via: join \", \" after «нет курса»
      pattern: "нет курса"
---

## Phase Goal

Open grace obligations appear on Капитал «Прогноз» with the same FX honesty as income forecast (ROADMAP Phase 21 / GRFCST-01, GRFCST-02).

<objective>
Tracer: page OPEN grace load → DashboardChartsShell membership merge → one «Прогноз» series + partial banner with unique FX codes (C-03, C-06, C-07, D-14…D-18, GRFCST-01, GRFCST-02).

Purpose: Wire overlay inputs and honesty chrome after pure math from 21-01.
Output: `/` passes forecastGrace; shell merges; banner lists codes; point event metadata ready for Plan 03 tooltips.
</objective>

<execution_context>
@/home/artem/Documents/wallet/.cursor/gsd-core/workflows/execute-plan.md
@/home/artem/Documents/wallet/.cursor/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/21-kapital-forecast-integration/21-CONTEXT.md
@.planning/phases/21-kapital-forecast-integration/21-RESEARCH.md
@.planning/phases/21-kapital-forecast-integration/21-PATTERNS.md
@.planning/phases/21-kapital-forecast-integration/21-UI-SPEC.md
@.planning/phases/21-kapital-forecast-integration/21-01-SUMMARY.md
@src/app/page.tsx
@src/app/accounts/page.tsx
@src/components/dashboard/DashboardChartsShell.tsx
@src/lib/nw-forecast.ts
@src/lib/credit-grace.ts
</context>

<assumptions>
## Planner assumptions

- **RESOLVED RESEARCH Q1:** Lean Prisma OPEN-only load on `/` (not nested all CLOSED via accounts include) — C-07.
- Income membership unchanged: from = today+1 / plannedAsOf &gt; today (C-06 horizon via forecastHorizonEnd).
- Banner microcopy: `Прогноз неполный · нет курса ${codes.join(", ")}` (D-14…D-17; UI-SPEC template).
- Schema-gate: still no prisma schema edits / migrate.
- Drift: do not pass grace into computeNetWorthRows / historical builders.
</assumptions>

## Artifacts this plan produces

- `forecastGrace` prop path on `/`
- Shell merge of grace + income + banner codes + `forecastEvents` on merged points
- Green Plan 02 UI file-scan for banner codes

<tasks>

<task type="tracer">
  <name>End-to-end `/` OPEN grace → shell merge → banner codes</name>
  <reversibility rating="costly">D-14…D-18 banner contract + forecastGrace RSC prop become Капитал chrome SoT; undo needs coordinated page/shell/UI tests.</reversibility>
  <files>src/app/page.tsx, src/components/dashboard/DashboardChartsShell.tsx, src/components/dashboard/nw-forecast-ui.test.ts</files>
  <read_first>
    .planning/phases/21-kapital-forecast-integration/21-CONTEXT.md
    .planning/phases/21-kapital-forecast-integration/21-UI-SPEC.md
    .planning/phases/21-kapital-forecast-integration/21-PATTERNS.md
    src/app/page.tsx
    src/app/accounts/page.tsx
    src/components/dashboard/DashboardChartsShell.tsx
    src/lib/credit-grace.ts
    src/lib/nw-forecast.ts
    src/components/dashboard/nw-forecast-ui.test.ts
  </read_first>
  <behavior>
    - page Promise.all loads OPEN obligations only; amountMinor serialized string; account name + currency fields present (C-07)
    - Shell ForecastGracePayload parallel to forecastIncome; revive BigInt; call openGraceForecastMembership(today, horizonEnd); map to ForecastSlot kind grace (D-01…D-05)
    - Concat grace slots with income openSlots into one buildNetWorthForecastSeries (C-03)
    - showForecast = includedSlotCount &gt; 0 including grace-only flat (D-07)
    - showPartialBanner when isPartialForecast or excludedMissingFxCount &gt; 0; banner text includes unique codes; still show when points empty (D-14…D-18)
    - mergeFactAndForecast copies builder event metadata onto NetWorthChartPoint.forecastEvents for sampled dates including today fold (D-06, D-09 prep)
    - File-scan: shell joins codes; retains «Прогноз неполный» / «нет курса» / role=status; does not add slot-kind labels beside codes (D-16)
  </behavior>
  <action>
    Extend src/app/page.tsx to load OPEN CreditGraceObligation rows (filter status OPEN) with account currency/name needed for slots; serialize amountMinor to string; pass new forecastGrace prop into DashboardChartsShell. Do not route grace through computeNetWorthRows. Extend DashboardChartsShell props with ForecastGracePayload; keep income assembly unchanged (C-06). Build grace ForecastSlots via Plan 01 membership helper then concat with income slots; call buildNetWorthForecastSeries once. Render one dashed series only (no second Line). Update partial banner to append unique sorted codes from excludedMissingFxCurrencies after «нет курса» with `", "` join (D-15, D-17); keep muted Label styling from UI-SPEC; when includedSlotCount===0 but codes present, hide Line and keep banner (D-18). Extend mergeFactAndForecast (or adjacent write path) to attach forecastEvents from builder metadata onto points for tooltips in Plan 03. Convert Plan 02 Wave 0 UI todos to real asserts for banner code join / template. Leave Plan 03 tooltip RU todos skipped. Zero new packages; no prisma migrate.
  </action>
  <verify>
    <automated>npx vitest run src/lib/nw-forecast.test.ts src/components/dashboard/nw-forecast-ui.test.ts &amp;&amp; grep -q 'forecastGrace' src/app/page.tsx &amp;&amp; grep -q 'forecastGrace' src/components/dashboard/DashboardChartsShell.tsx &amp;&amp; grep -q 'excludedMissingFxCurrencies\|join(", ")' src/components/dashboard/DashboardChartsShell.tsx</automated>
    <fails_when>vitest non-zero, forecastGrace missing from page or shell, or banner code-join/excludedMissingFxCurrencies wiring absent from shell</fails_when>
  </verify>
  <acceptance_criteria>
    - OPEN-only grace reaches shell (C-07, GRFCST-01)
    - One merged series + D-07 grace-only showForecast path
    - Banner lists unique FX codes; D-18 banner-without-line (GRFCST-02, D-14…D-18)
    - forecastEvents present on merged points for Plan 03
  </acceptance_criteria>
  <done>Tracer green: page→shell→banner codes + event metadata</done>
</task>

<task type="auto">
  <name>Isolation smoke — page/shell stay off historical LOCF path</name>
  <files>src/app/page.tsx, src/components/dashboard/DashboardChartsShell.tsx, src/lib/credit-grace.test.ts</files>
  <read_first>
    src/app/page.tsx
    src/lib/credit-grace.test.ts
    src/lib/net-worth.ts
    src/lib/historical-series.ts
  </read_first>
  <behavior>
    - page still computes NW via computeNetWorthRows without grace inputs
    - credit-grace.test.ts GRISO scan: net-worth.ts and historical-series.ts still have no credit-grace import
    - shell may import credit-grace membership + nw-forecast; must not import net-worth/historical-series for grace
  </behavior>
  <action>
    Confirm page grace load is overlay-only props — computeNetWorthRows / snapshot LOCF Maps unchanged. Keep or lightly extend GRISO file-scan in credit-grace.test.ts so Phase 21 does not introduce credit-grace imports into net-worth.ts or historical-series.ts. Optionally assert page.tsx does not pass grace into computeNetWorthRows call site. No schema changes.
  </action>
  <verify>
    <automated>npx vitest run src/lib/credit-grace.test.ts &amp;&amp; ! grep -E 'credit-grace' src/lib/net-worth.ts src/lib/historical-series.ts</automated>
    <fails_when>vitest non-zero, or credit-grace string appears in net-worth.ts or historical-series.ts</fails_when>
  </verify>
  <acceptance_criteria>
    - Historical LOCF modules grace-free (Phase 22 prep)
    - Overlay wiring only on page/shell
  </acceptance_criteria>
  <done>Isolation smoke green after shell wiring</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Prisma OPEN query → RSC props | Serialized obligation amounts/names cross into client shell |
| Shell slot assembly → builder | Client-revived BigInt/currency must still hit FX gate |
| Banner UI ← missing FX codes | Honesty surface for excluded currencies |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-21-05 | Tampering | Shell/builder FX path | high | mitigate | Reuse builder exclude+codes; never invent rates in shell |
| T-21-06 | Tampering | page.tsx NW path | high | mitigate | Grace props overlay-only; computeNetWorthRows unchanged; GRISO scan |
| T-21-07 | Information Disclosure | CLOSED over-fetch on `/` | medium | mitigate | status OPEN query filter on `/` |
| T-21-08 | Tampering | XSS via accountName in forecastEvents payload | low | accept | React text nodes only; no dangerouslySetInnerHTML (Plan 03 renders) |
| T-21-SC | Tampering | npm installs | high | mitigate | Zero new packages |
</threat_model>

<verification>
npx vitest run src/lib/nw-forecast.test.ts src/components/dashboard/nw-forecast-ui.test.ts src/lib/credit-grace.test.ts; forecastGrace wired; banner codes present; GRISO files clean.
</verification>

<success_criteria>
GRFCST-01 overlay inputs on `/`; GRFCST-02 banner lists unique codes; D-14…D-18 honored; historical modules untouched.
</success_criteria>

<output>
Create `.planning/phases/21-kapital-forecast-integration/21-02-SUMMARY.md` when done
</output>
