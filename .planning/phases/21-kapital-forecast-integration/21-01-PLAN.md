---
phase: 21-kapital-forecast-integration
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/lib/nw-forecast.test.ts
  - src/components/dashboard/nw-forecast-ui.test.ts
  - src/lib/credit-grace.ts
  - src/lib/credit-grace.test.ts
  - src/lib/nw-forecast.ts
autonomous: true
requirements:
  - GRFCST-01
  - GRFCST-02
estimate:
  tokens: 50000
  raw_tokens: 100000
  tasks: 3
  confidence: high
must_haves:
  truths:
    - "Wave 0: nw-forecast + nw-forecast-ui suites exit 0 with Plan 02/03 chrome as it.todo/describe.skip only (no hard-fail poison)"
    - "OPEN grace membership folds overdue of any age onto today; future dues only in (today, horizonEnd]; CLOSED excluded (D-01…D-04, C-07)"
    - "Kind-aware buildNetWorthForecastSeries: income keeps plannedAsOf > today; grace allows sampleAsOf >= today after fold (D-01, D-04)"
    - "A′: FX-included grace samples due/today date and adds 0n to cumulative NW; income addend unchanged on same day (C-01, D-06, GRFCST-01)"
    - "Grace-only horizon still yields non-empty flat forecast points (D-07)"
    - "Missing FX excludes grace slot, increments excludedMissingFxCount, returns unique alphabetical excludedMissingFxCurrencies (D-05, D-15, D-17, GRFCST-02)"
    - "All-FX-excluded → empty points, isPartialForecast true, full code list retained (D-18)"
    - statement: "Long missing-code list wraps in quiet p-4 banner without ellipsis (UI-SPEC overflow backstop)"
      verification: backstop
  prohibitions:
    - statement: MUST NOT apply signed negative grace NW dip
      requirement_id: GRFCST-01
      category: values
    - statement: MUST NOT invent FX rates or use rate 1 on miss
      requirement_id: GRFCST-02
      category: isolation
    - statement: MUST NOT import credit-grace into nw-forecast, net-worth, or historical-series
      requirement_id: GRFCST-01
      category: isolation
    - statement: MUST NOT write BalanceSnapshot or change historical LOCF builders this phase
      requirement_id: GRFCST-01
      category: isolation
  artifacts:
    - path: src/lib/nw-forecast.test.ts
      provides: Wave 0 + green A′/fold/FX-code/grace-only/same-day unit coverage
    - path: src/components/dashboard/nw-forecast-ui.test.ts
      provides: Wave 0 owner stubs for banner codes (Plan 02) and tooltip RU (Plan 03)
    - path: src/lib/credit-grace.ts
      provides: openGraceForecastMembership (or equivalent) OPEN→sampleAsOf helper
    - path: src/lib/nw-forecast.ts
      provides: kind-aware ForecastSlot, 0-addend grace, excludedMissingFxCurrencies, event metadata
  key_links:
    - from: openGraceForecastMembership
      to: ForecastSlot kind grace
      via: shell/builder boundary (no nw-forecast import into credit-grace)
      pattern: "sampleAsOf"
    - from: buildNetWorthForecastSeries
      to: excludedMissingFxCurrencies
      via: FX miss collect unique sorted codes (D-15/D-17)
      pattern: "excludedMissingFxCurrencies"
    - from: grace converted row
      to: dateSet sample
      via: primaryMinor 0n after FX gate still dateSet.add (D-06)
      pattern: "0n"
---

## Phase Goal

Open grace obligations appear on Капитал «Прогноз» with the same FX honesty as income forecast (ROADMAP Phase 21 / GRFCST-01, GRFCST-02).

<objective>
Wave 0 Nyquist stubs, then tracer: OPEN grace membership (fold overdue → today) → kind-aware `buildNetWorthForecastSeries` with A′ 0-addend + unique missing FX codes (C-01, C-05, C-07, D-01…D-07, D-15, D-17, D-18, GRFCST-01, GRFCST-02).

Purpose: Prove pure overlay math before page/shell/tooltip chrome.
Output: Green membership + forecast unit suites; UI file-scan stubs owned by Plans 02–03.
</objective>

<execution_context>
@/home/artem/Documents/wallet/.cursor/gsd-core/workflows/execute-plan.md
@/home/artem/Documents/wallet/.cursor/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/21-kapital-forecast-integration/21-CONTEXT.md
@.planning/phases/21-kapital-forecast-integration/21-RESEARCH.md
@.planning/phases/21-kapital-forecast-integration/21-PATTERNS.md
@.planning/phases/21-kapital-forecast-integration/21-VALIDATION.md
@.planning/phases/21-kapital-forecast-integration/21-UI-SPEC.md
@.planning/phases/18-bank-contract-study-discuss-locks/18-CONTEXT.md
@.planning/milestones/v1.2-phases/17-nw-forecast-overlay-isolation/17-CONTEXT.md
@src/lib/nw-forecast.ts
@src/lib/nw-forecast.test.ts
@src/lib/credit-grace.ts
@src/lib/credit-grace.test.ts
@src/components/dashboard/nw-forecast-ui.test.ts
</context>

<assumptions>
## Planner assumptions

- A1 (RESEARCH): Missing FX codes sorted alphabetically before join.
- A2 (RESEARCH): Primary-converted amount after successful FX is enough for D-11 display (native optional later).
- A3 (RESEARCH): Extending chart point payload with forecastEvents is acceptable for Recharts tooltip.
- Discretion: A′ = FX gate then += 0n (not paired ± offset leg).
- Assumption-delta detector fired on CONTEXT noise («second» series negation / «Multiple OPEN» / «chosen» layout) — identity model stays `ForecastSlot.kind` discriminator; no remodel.
- Schema-gate: **no** `prisma/schema.prisma` edits and **no** migrate/db push this phase (overlay-only; schema already shipped Phase 19).
- Drift: Phase 22 owns GRACEISO / historical identity — this plan must not touch `net-worth.ts` / `historical-series.ts` write paths or BalanceSnapshot.
- Zero new npm packages.
</assumptions>

## Artifacts this plan produces

- Extended Wave 0 stubs in `nw-forecast.test.ts` + `nw-forecast-ui.test.ts`
- `openGraceForecastMembership` (name flexible) in `credit-grace.ts` + unit tests
- Kind-aware `ForecastSlot` / builder result with `excludedMissingFxCurrencies` + includable event metadata

<tasks>

<task type="auto">
  <name>Wave 0 Nyquist stubs — forecast math + UI owner skips</name>
  <files>src/lib/nw-forecast.test.ts, src/components/dashboard/nw-forecast-ui.test.ts</files>
  <read_first>
    .planning/phases/21-kapital-forecast-integration/21-VALIDATION.md
    .planning/phases/21-kapital-forecast-integration/21-RESEARCH.md
    src/lib/nw-forecast.test.ts
    src/components/dashboard/nw-forecast-ui.test.ts
  </read_first>
  <behavior>
    - Existing Phase 17 income/stair-step/FX tests remain fully green (no rewrite that breaks them)
    - New describe("grace A′ / membership / FX codes") uses it.todo until Plan 01 tracer greens them — names cover: future OPEN sampled ΔNW=0; overdue fold→today; grace-only flat points; same-day income+grace income-only NW delta; FX miss codes unique; CLOSED never fed as slot
    - nw-forecast-ui.test.ts: plan-02 banner-codes it.todo/describe.skip (join pattern / codes in banner); plan-03 tooltip RU it.todo/describe.skip for «Платёж для беспроцентного» and «NW без изменения (оплата карты)» — full-file vitest exits 0
  </behavior>
  <action>
    Extend src/lib/nw-forecast.test.ts with a Plan-01-owned grace describe using it.todo stubs for GRFCST-01/02 behaviors listed in 21-VALIDATION Wave 0 (A′ 0-delta, today-fold sample, grace-only flat, FX currency list, same-day income+grace, CLOSED out). Do not plant hard-failing expects that poison Wave 0. Keep all existing income regression tests green. Extend src/components/dashboard/nw-forecast-ui.test.ts with owner describes: Plan 02 (banner lists unique FX codes via join or template; quiet role=status retained) as it.todo/describe.skip; Plan 03 (tooltip locked RU strings from C-04) as it.todo/describe.skip. Existing Phase 17 chrome asserts stay green. No production lib changes in this task.
  </action>
  <verify>
    <automated>npx vitest run src/lib/nw-forecast.test.ts src/components/dashboard/nw-forecast-ui.test.ts</automated>
    <fails_when>vitest non-zero exit, or summary shows failed tests (todos/skips must not fail)</fails_when>
  </verify>
  <acceptance_criteria>
    - Wave 0 sampling command exits 0
    - Grace math cases exist as todos; UI Plan 02/03 owners skipped/todo
    - Income regression tests still pass
  </acceptance_criteria>
  <done>Wave 0 green with non-poisoning stubs for Plans 01–03</done>
</task>

<task type="auto" tdd="true">
  <name>OPEN grace forecast membership helper (fold + window)</name>
  <files>src/lib/credit-grace.ts, src/lib/credit-grace.test.ts</files>
  <read_first>
    .planning/phases/21-kapital-forecast-integration/21-CONTEXT.md
    .planning/phases/21-kapital-forecast-integration/21-PATTERNS.md
    src/lib/credit-grace.ts
    src/lib/credit-grace.test.ts
  </read_first>
  <behavior>
    - CLOSED / non-OPEN rows excluded (C-07)
    - dueAsOf &lt; today → sampleAsOf = today for any age overdue (D-01, D-02, D-03)
    - dueAsOf === today → today bucket (D-04)
    - today &lt; dueAsOf ≤ horizonEnd → future sampleAsOf = dueAsOf (D-04)
    - dueAsOf &gt; horizonEnd → excluded (D-04)
    - Output carries obligationId, original dueAsOf, amountMinor, account currency fields, accountId/name for tooltip (D-11/D-12 prep)
  </behavior>
  <action>
    Add a pure helper in src/lib/credit-grace.ts (e.g. openGraceForecastMembership) that maps OPEN obligation rows + today + horizonEnd into membership DTOs with sampleAsOf after fold. Reuse isGraceOverdue / YYYY-MM-DD compare — no Date clock inside lib. Do not import nw-forecast (avoid cycle); return lean fields the shell will map to ForecastSlot kind grace. Keep existing GRISO isolation smoke in credit-grace.test.ts green. Add unit cases for overdue fold, today due, future window, beyond horizon, CLOSED out (D-01…D-04, C-07). No Prisma.
  </action>
  <verify>
    <automated>npx vitest run src/lib/credit-grace.test.ts</automated>
    <fails_when>vitest non-zero, or membership describe missing/failing for fold/CLOSED/horizon cases</fails_when>
  </verify>
  <acceptance_criteria>
    - Overdue OPEN folds to today (D-02, D-03)
    - Future/horizon/CLOSED rules match D-04 / C-07
    - GRISO scan still bans credit-grace in net-worth and historical-series
  </acceptance_criteria>
  <done>Membership helper tested; CLOSED never members</done>
</task>

<task type="tracer">
  <name>End-to-end A′ grace slots through buildNetWorthForecastSeries</name>
  <reversibility rating="costly">C-01 A′ 0-addend and D-01/D-06 kind-aware sampling reshape ForecastSlot/builder contracts; undo needs coordinated test+shell updates.</reversibility>
  <files>src/lib/nw-forecast.ts, src/lib/nw-forecast.test.ts</files>
  <read_first>
    .planning/phases/21-kapital-forecast-integration/21-CONTEXT.md
    .planning/phases/21-kapital-forecast-integration/21-RESEARCH.md
    .planning/phases/21-kapital-forecast-integration/21-PATTERNS.md
    src/lib/nw-forecast.ts
    src/lib/nw-forecast.test.ts
  </read_first>
  <behavior>
    - ForecastSlot requires kind "income" | "grace"; income path filter stays plannedAsOf &gt; today; grace allows plannedAsOf &gt;= today and &lt;= horizonEnd (D-01, D-04)
    - After FX success, grace contributes 0n to running sum but dateSet still includes sample date (C-01, D-06); plannedAmountMinor retained for event/display metadata
    - Same-day income+grace: cumulative NW moves by income only; both events available in builder metadata (C-03, D-06)
    - Grace-only slots → includedSlotCount &gt; 0 and non-empty flat points today→horizon (D-07)
    - FX miss on grace: exclude slot, do not invent rate; collect currency into excludedMissingFxCurrencies unique sorted; excludedMissingFxCount++; isPartialForecast true (C-05, D-05, D-15, D-17, GRFCST-02)
    - All slots FX-excluded → points []; still return full unique code list (D-18)
    - Import wall unchanged: dates / locf / money only
  </behavior>
  <action>
    Extend ForecastSlot with kind and optional grace tooltip fields (accountId, accountName, dueAsOf original) per RESEARCH Pattern 1. Update buildNetWorthForecastSeries: kind-aware membership filter (replace income-only plannedAsOf &gt; today gate); income adds converted primaryMinor; grace passes same FX LOCF@today gate then pushes 0n addend while recording includable event metadata for that sampleAsOf (C-01, D-06). Return excludedMissingFxCurrencies (unique, alphabetical) alongside existing counters (D-15, D-17). Ensure includedSlotCount counts FX-included grace even when addend is 0n so D-07 showForecast stays true downstream. Convert Plan 01 Wave 0 it.todos in nw-forecast.test.ts to real expects covering A′ vs income baseline, overdue sample on today (caller folds before builder — feed sampleAsOf=today), grace-only flat, same-day income+grace, FX code list uniqueness across income+grace misses, income regression. Update primarySlot test factories to set kind: "income". Do not touch page/shell/chart in this plan. Do not modify prisma schema.
  </action>
  <verify>
    <automated>npx vitest run src/lib/nw-forecast.test.ts src/lib/credit-grace.test.ts</automated>
    <fails_when>vitest non-zero, "0 passed" on grace describes, or excludedMissingFxCurrencies assertions fail</fails_when>
  </verify>
  <acceptance_criteria>
    - A′ ΔNW=0 with sampled grace dates (C-01, D-06, GRFCST-01)
    - Kind-aware filters honor D-01/D-04; grace-only flat D-07
    - FX honesty + unique codes (GRFCST-02, D-15, D-17, D-18)
    - nw-forecast import wall intact
  </acceptance_criteria>
  <done>Tracer green: membership + A′ builder + FX codes unit-proven</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Slot inputs → buildNetWorthForecastSeries | Untrusted amounts/currencies/dates from shell; pure math must not invent FX |
| credit-grace helper → nw-forecast | Membership DTO only; no DB / no historical NW imports |
| Overlay math → historical LOCF modules | Must remain disconnected (Phase 22 GRISO) |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-21-01 | Tampering | buildNetWorthForecastSeries FX gate | high | mitigate | locfRateAsOf null → exclude + record currency; never invent rate (C-05 / GRFCST-02) |
| T-21-02 | Tampering | Grace leaking into net-worth / historical-series | high | mitigate | Import wall on nw-forecast; GRISO scan in credit-grace.test.ts stays green; no BalanceSnapshot APIs |
| T-21-03 | Information Disclosure | Over-broad CLOSED obligation data in membership | medium | mitigate | Helper accepts/emits OPEN only (C-07); page query filter deferred to Plan 02 |
| T-21-04 | Spoofing | Spoofed sampleAsOf past due | low | accept | Single-user local app; membership recomputed from dueAsOf+today in helper |
| T-21-SC | Tampering | npm installs | high | mitigate | Zero new packages — no install checkpoint |
</threat_model>

<verification>
npx vitest run src/lib/nw-forecast.test.ts src/lib/credit-grace.test.ts src/components/dashboard/nw-forecast-ui.test.ts — math green; UI stubs non-poisoning.
</verification>

<success_criteria>
GRFCST-01 A′ math + membership fold proven; GRFCST-02 FX code list returned from builder; income regression green; schema untouched.
</success_criteria>

<output>
Create `.planning/phases/21-kapital-forecast-integration/21-01-SUMMARY.md` when done
</output>
