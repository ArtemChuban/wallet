---
phase: 28-interest-math-forecast-kind
plan: 01
type: execute
wave: 0
depends_on: []
files_modified:
  - src/lib/savings-interest.test.ts
  - src/lib/nw-forecast.test.ts
autonomous: true
requirements:
  - INT-01
estimate:
  tokens: 20000
  raw_tokens: 40000
  tasks: 2
  confidence: high
must_haves:
  truths:
    - "Red Vitest contracts lock INT-01 ÷12 truncate, D-02 compound growth (month 2 larger than month 1), and D-10 zero-skip before savings-interest.ts exists"
    - "Red contracts lock accrual DOM via nextAccrualAsOf, including short February and leap day, and future-only membership (no slot on today) per D-01 and D-07"
    - "Red contracts lock kind interest future ΔNW = +amount, same-day grace does not add, and a today-dated interest slot is excluded (D-12, SC #3, SC #4)"
    - "Existing grace-only ΔNW=0 case in nw-forecast.test.ts still runs and stays green"
  artifacts:
    - path: src/lib/savings-interest.test.ts
      provides: "Failing INT-01 formula, compound chain, calendar, multi-account, source-isolation contracts"
    - path: src/lib/nw-forecast.test.ts
      provides: "Interest window and ΔNW contracts; grace regression kept"
  key_links:
    - from: src/lib/savings-interest.test.ts
      to: Plan 28-02 savings-interest.ts
      via: "imports monthlyInterestMinor and listInterestSlotsInRange"
      pattern: listInterestSlotsInRange
    - from: src/lib/nw-forecast.test.ts
      to: Plan 28-02 slotInWindow
      via: "today-dated interest slot must yield includedSlotCount 0"
      pattern: interest
---

<objective>
Author Wave 0 failing Vitest contracts for INT-01 monthly interest and ForecastSlotKind interest (D-01…D-13) so Plan 28-02 turns them green. No production implementation.

Purpose: Lock ÷12 truncate, D-02 compound chain (overrides research flat LOCF), future-only window, and +ΔNW before the module exists.
Output: src/lib/savings-interest.test.ts (new) and extended src/lib/nw-forecast.test.ts. Both files red. Zero production edits.
</objective>

<execution_context>
@/home/artem/Documents/wallet/.cursor/gsd-core/workflows/execute-plan.md
@/home/artem/Documents/wallet/.cursor/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/28-interest-math-forecast-kind/28-CONTEXT.md
@.planning/phases/28-interest-math-forecast-kind/28-RESEARCH.md
@.planning/phases/28-interest-math-forecast-kind/28-PATTERNS.md
@.planning/phases/28-interest-math-forecast-kind/28-VALIDATION.md
@src/lib/nw-forecast.test.ts
@src/lib/nw-forecast.ts
@src/lib/credit-grace.test.ts
@src/lib/savings-accrual-display.test.ts
</context>

<tasks>

<task type="auto">
  <name>Wave 0: red savings-interest contracts</name>
  <files>src/lib/savings-interest.test.ts</files>
  <read_first>
    .planning/phases/28-interest-math-forecast-kind/28-CONTEXT.md
    .planning/phases/28-interest-math-forecast-kind/28-RESEARCH.md
    .planning/phases/28-interest-math-forecast-kind/28-PATTERNS.md
    src/lib/credit-grace.test.ts
    src/lib/savings-accrual-display.test.ts
  </read_first>
  <behavior>
    - monthlyInterestMinor(1_000_000n, 1650) === 13750n (D-08, INT-01)
    - monthlyInterestMinor(1_000_000n, 1200) === 10000n; monthlyInterestMinor(1_010_000n, 1200) === 10100n (D-02, D-09)
    - monthlyInterestMinor(100n, 10000) === 8n; monthlyInterestMinor(108n, 10000) === 9n (D-09, no fractional carry)
    - monthlyInterestMinor(1n, 1) === 0n; monthlyInterestMinor(0n, 1200) === 0n; monthlyInterestMinor(1_000_000n, 0) === 0n; monthlyInterestMinor(-100n, 10000) === 0n (D-10; negative LOCF does not return a debit)
    - listInterestSlotsInRange one account balance 1_000_000n, annualRateBps 1200, accrualDayOfMonth 15, today 2026-03-01, horizonEnd 2026-04-30 emits 2026-03-15 interestMinor 10000n then 2026-04-15 interestMinor 10100n (D-01, D-02, D-05)
    - Same account with balance 100n and annualRateBps 10000 emits 8n then 9n on those two dates (D-09)
    - annualRateBps 0, balance 0n, or balance 1n at 1 bps emits no slots across that horizon (D-10)
    - today 2026-02-10, DOM 31, horizonEnd 2026-03-31 emits plannedAsOf 2026-02-28 and 2026-03-31 (D-07 clamp via nextAccrualAsOf)
    - today 2026-02-28, DOM 31, horizonEnd 2026-03-31 emits only 2026-03-31 (D-07 future-only; sticky accrual day must not repeat 2026-02-28)
    - today 2024-02-01, DOM 31, horizonEnd 2024-02-29 emits 2024-02-29 (leap clamp)
    - plannedAsOf equal to horizonEnd is included; the next accrual after horizonEnd is absent
    - Two accounts, same DOM, different balanceMinor, compound independently; output sorted by plannedAsOf then accountId (D-04, D-13 parentId equals accountId)
    - Currency fields on the input round-trip onto the slot; the enumerator does not convert FX (D-06)
    - Source smoke reads the interest module and rejects an exponentiation-call identifier, plus forecast-builder / historical-NW / database-client imports (pattern text only inside this test file, copied from 28-PATTERNS.md source smoke)
  </behavior>
  <action>
    Create src/lib/savings-interest.test.ts only. Import monthlyInterestMinor and listInterestSlotsInRange from ./savings-interest (module does not exist yet — this file must fail to load). Use vitest describe/it/expect and a fixture helper for account rows, mirroring credit-grace.test.ts style. Encode every behavior bullet as a hard expect, not it.todo. D-02 overrides the milestone research flat-LOCF sentence: assert month 2 is 10100n, never a second 10000n. D-03 is structural: fixtures pass only today LOCF balanceMinor, never income or grace amounts. D-05: one annualRateBps and one accrualDayOfMonth per account for the whole run. D-11: these two export names are the contract. Do not add src/lib/savings-interest.ts. Do not edit nw-forecast, Dashboard, MCP loaders, Prisma, or package.json. Do not add npm packages.
  </action>
  <verify>
    <automated>./node_modules/.bin/vitest run src/lib/savings-interest.test.ts; test $? -ne 0 && test -f src/lib/savings-interest.test.ts && grep -q '10100n' src/lib/savings-interest.test.ts && grep -q '13750n' src/lib/savings-interest.test.ts && grep -q '2026-02-28' src/lib/savings-interest.test.ts && grep -q 'listInterestSlotsInRange' src/lib/savings-interest.test.ts</automated>
  </verify>
  <done>savings-interest.test.ts exists, cites the golden minors and February clamps, and the vitest run exits non-zero because the module is absent</done>
</task>

<task type="auto">
  <name>Wave 0: red interest forecast-kind contracts</name>
  <files>src/lib/nw-forecast.test.ts</files>
  <read_first>
    .planning/phases/28-interest-math-forecast-kind/28-RESEARCH.md
    .planning/phases/28-interest-math-forecast-kind/28-PATTERNS.md
    src/lib/nw-forecast.ts
    src/lib/nw-forecast.test.ts
  </read_first>
  <behavior>
    - Anchor 1_000_000n, kind interest, plannedAmountMinor 50_000n, plannedAsOf 2026-03-15, primary currency, today 2026-03-01, horizonEnd 2026-03-31 → that point forecastPrimaryMinor is 1_050_000n and includedSlotCount is 1 (D-12, SC #3)
    - Same fixture with plannedAsOf equal to today → includedSlotCount is 0 and points stay empty (D-07, D-12, SC #4). This case must fail against current slotInWindow, which treats non-income kinds like grace
    - Same-day interest 50_000n plus grace 50_000n on 2026-03-15 → forecastPrimaryMinor is 1_050_000n; forecast event kinds are grace and interest (D-03, D-12)
    - Existing future grace-only case still expects forecastPrimaryMinor 1_000_000n (regression)
    - ForecastSlot kind list in the CLOSED test includes interest alongside income and grace
    - Interest fixture sets parentId and accountId to the same account id and does not set dueAsOf (D-13)
  </behavior>
  <action>
    Extend src/lib/nw-forecast.test.ts only. Add an interest slot helper beside graceSlot: kind interest, parentId equal to accountId, optional accountName, no dueAsOf (D-13). Add the three behavior cases above using the file's existing today constant 2026-03-01 and buildNetWorthForecastSeries. Update the kind-literal test so the array includes interest and still rejects CLOSED. Do not change production nw-forecast.ts. Do not change the grace-only ΔNW=0 test body. Do not wire DashboardChartsShell, page.tsx, or MCP membership. The future +ΔNW case may already pass because the current addend treats every non-grace kind as a credit; keep it as the locked contract. The today-dated case must be a hard expect that fails today.
  </action>
  <verify>
    <automated>./node_modules/.bin/vitest run src/lib/nw-forecast.test.ts; test $? -ne 0 && grep -q 'interest' src/lib/nw-forecast.test.ts && grep -q '1_050_000n' src/lib/nw-forecast.test.ts</automated>
  </verify>
  <done>nw-forecast.test.ts contains interest ΔNW and today-exclusion expects, the suite exits non-zero, and the pre-existing grace-flat case is still present</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Wave 0 expects → Plan 28-02 production | Wrong red contracts lock a flat or grace-window formula |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-28-01 | Tampering | monthly interest minor | high | mitigate | Red expects lock bigint golden minors (13750n, 10000n then 10100n, 8n then 9n). Plan 28-02 implements the division. No float |
| T-28-02 | Tampering | slot dated today | high | mitigate | Red expect: interest plannedAsOf equal to today yields includedSlotCount 0. Must fail until slotInWindow changes |
| T-28-03 | Tampering | historical NW / snapshots | high | mitigate | This plan edits tests only. No BalanceSnapshot, no historical-series, no shell wire |
| T-28-04 | Tampering | negative LOCF | medium | mitigate | Red expect: negative balance helper returns 0n and emits no slot |
| T-28-SC | Tampering | npm installs | high | mitigate | No package install in this plan |
</threat_model>

<verification>
Wave 0 vitest on savings-interest.test.ts and nw-forecast.test.ts exits non-zero. No production files change. Grace-flat regression remains in the forecast test file.
</verification>

<success_criteria>
INT-01 contracts exist and fail for the missing module and for today-dated interest membership. Plan 28-02 can turn the same files green without rewriting expects.
</success_criteria>

<output>
Create `.planning/phases/28-interest-math-forecast-kind/28-01-SUMMARY.md` when done
</output>
