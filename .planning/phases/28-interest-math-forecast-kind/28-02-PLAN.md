---
phase: 28-interest-math-forecast-kind
plan: 02
type: execute
wave: 1
depends_on:
  - 28-01
files_modified:
  - src/lib/savings-interest.ts
  - src/lib/nw-forecast.ts
  - src/lib/mcp/reads/load-forecast-overlay.ts
  - src/lib/nw-forecast.test.ts
autonomous: true
requirements:
  - INT-01
estimate:
  tokens: 28000
  raw_tokens: 56000
  tasks: 2
  confidence: high
must_haves:
  truths:
    - "First future monthly credit equals today LOCF minor times annualRateBps over 12, truncated toward 0; the next month uses principal grown by that truncated credit only (D-02, D-08, D-09, INT-01, SC #1)"
    - "Accrual dates come from nextAccrualAsOf, so DOM 31 clamps to Feb 28 or Feb 29, and a run that starts on the accrual day does not emit that day (D-01, D-07, SC #2, SC #4)"
    - "buildNetWorthForecastSeries adds display primary for kind interest and still adds 0 for grace; a slot dated today is outside the window (D-12, SC #3, SC #4)"
    - "Two SAVINGS accounts grow separate principals; the builder sums their precomputed deltas and does not recompute interest (D-03, D-04)"
    - "SerializedForecastEvent.kind accepts interest so kind assignment still typechecks, while the overlay loader still builds only income and grace slots (D-12)"
  artifacts:
    - path: src/lib/savings-interest.ts
      provides: monthlyInterestMinor and listInterestSlotsInRange
    - path: src/lib/nw-forecast.ts
      provides: ForecastSlotKind interest, income-style window, exhaustive ΔNW
    - path: src/lib/mcp/reads/load-forecast-overlay.ts
      provides: SerializedForecastEvent.kind union includes interest
    - path: src/lib/nw-forecast.test.ts
      provides: Hand-mapped enumerator slots stair-step; missing FX excludes interest
  key_links:
    - from: listInterestSlotsInRange
      to: nextAccrualAsOf
      via: cursor starts the day after today and advances the day after each accrual
      pattern: nextAccrualAsOf
    - from: ForecastSlot kind interest
      to: buildNetWorthForecastSeries primaryMinor
      via: exhaustive switch returns displayPrimaryMinor for interest and 0n for grace
      pattern: displayPrimaryMinor
    - from: ForecastSlotKind
      to: SerializedForecastEvent.kind
      via: same three-literal union
      pattern: interest
---

<objective>
Ship the INT-01 vertical slice: compound monthly slots from today LOCF, and kind interest that stair-steps net worth, unit-tested end to end. D-02 overrides the research flat-LOCF default. Do not wire Dashboard or MCP membership (D-12).

Purpose: Phase 29 can map these slots onto the overlay without inventing the formula or the kind.
Output: src/lib/savings-interest.ts, widened nw-forecast.ts, widened SerializedForecastEvent.kind, green unit tests.
</objective>

<execution_context>
@/home/artem/Documents/wallet/.cursor/gsd-core/workflows/execute-plan.md
@/home/artem/Documents/wallet/.cursor/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/28-interest-math-forecast-kind/28-CONTEXT.md
@.planning/phases/28-interest-math-forecast-kind/28-RESEARCH.md
@.planning/phases/28-interest-math-forecast-kind/28-PATTERNS.md
@.planning/phases/28-interest-math-forecast-kind/28-01-SUMMARY.md
@src/lib/savings-accrual-display.ts
@src/lib/dates.ts
@src/lib/nw-forecast.ts
@src/lib/mcp/reads/load-forecast-overlay.ts
@src/lib/savings-interest.test.ts
@src/lib/nw-forecast.test.ts
</context>

<tasks>

<task type="tracer">
  <name>End-to-end interest slot: compound enumerator plus ΔNW kind</name>
  <reversibility rating="costly">D-02, D-08, D-11, and D-12 lock the public helper, the truncate chain, and ForecastSlotKind. Later phases call this API; undoing it means coordinated caller changes, not a data migration.</reversibility>
  <files>src/lib/savings-interest.ts, src/lib/nw-forecast.ts, src/lib/mcp/reads/load-forecast-overlay.ts</files>
  <read_first>
    .planning/phases/28-interest-math-forecast-kind/28-CONTEXT.md
    .planning/phases/28-interest-math-forecast-kind/28-RESEARCH.md
    .planning/phases/28-interest-math-forecast-kind/28-PATTERNS.md
    src/lib/savings-interest.test.ts
    src/lib/nw-forecast.ts
    src/lib/nw-forecast.test.ts
    src/lib/savings-accrual-display.ts
    src/lib/mcp/reads/load-forecast-overlay.ts
  </read_first>
  <action>
    Create src/lib/savings-interest.ts (D-11). Export monthlyInterestMinor(balanceMinor, annualRateBps) and listInterestSlotsInRange(accounts, today, horizonEnd). Header states the module is pure: no database client, no forecast series builder, no historical NW, no FX module, no income, no grace. Imports only addCalendarDays from dates and nextAccrualAsOf from savings-accrual-display (D-07). Do not copy clampDayOfMonth.

    monthlyInterestMinor (D-08): if annualRateBps is less than or equal to 0 or balanceMinor is less than or equal to 0n, return 0n. Otherwise one bigint division: balance times BigInt of annualRateBps, divided by the product of 12 and 10000. Truncate toward 0 is the operator. Do not use an exponentiation API, a monthly APY factor, Math.floor, or a float multiply (SC #1, T-28-01).

    InterestAccountInput: accountId, optional accountName, balanceMinor (this account today LOCF only, D-02, D-03), annualRateBps, accrualDayOfMonth (D-05, current values for every slot in the run), currencyCode, currencyScale, isPrimaryCurrency (pass-through, D-06; do not convert). InterestForecastSlot: plannedAsOf, interestMinor, parentId, accountId, optional accountName, and the same currency fields. parentId equals accountId (D-13).

    Enumerator (D-01, D-02, D-04, D-07, D-09, D-10): each account has its own principal starting at balanceMinor. cursor starts at addCalendarDays(today, 1). While cursor is within horizonEnd, accrual is nextAccrualAsOf(cursor, accrualDayOfMonth). If accrual is after horizonEnd, stop that account. interestMinor is monthlyInterestMinor(principal, annualRateBps). Emit only when interestMinor is greater than 0n, then add that truncated interestMinor to principal. Always set cursor to addCalendarDays(accrual, 1), including months that emit nothing, so the sticky accrual day cannot repeat. Inclusive horizonEnd. Do not fold a missed past accrual onto today. Do not read income or grace amounts. After all accounts, sort by plannedAsOf ascending then accountId ascending.

    In nw-forecast.ts widen ForecastSlotKind to income, grace, or interest (D-12). slotInWindow: after the horizonEnd cap, interest uses the same strict after-today predicate as income. Grace keeps the predicate that allows today. Replace the non-grace addend ternary with an exhaustive switch: income and interest return displayPrimaryMinor; grace returns 0n; default assigns never. Do not import savings-interest into nw-forecast. Do not recompute principal inside the builder (D-03). Leave the FX-as-of-today gate as it is.

    In load-forecast-overlay.ts widen SerializedForecastEvent.kind with interest so kind assignment from ForecastSlotKind typechecks (Pitfall 6). Do not call listInterestSlotsInRange. Do not add SAVINGS queries. Leave the loader slot list as income plus grace only (D-12). Do not edit DashboardChartsShell, page.tsx, mcp/tools/forecast.ts, historical-series.ts, net-worth.ts, account actions, or Prisma. No new npm packages (T-28-SC).
  </action>
  <verify>
    <automated>./node_modules/.bin/vitest run src/lib/savings-interest.test.ts src/lib/nw-forecast.test.ts</automated>
  </verify>
  <done>Both Wave 0 files exit 0. A 1_000_000n at 1200 bps run emits 10000n then 10100n. A today-dated interest slot is excluded. A future interest slot increases forecast primary by its amount. Grace-only ΔNW stays flat. SerializedForecastEvent.kind includes interest and the loader still does not emit interest membership.</done>
</task>

<task type="auto">
  <name>Prove independent accounts stair-step and missing FX excludes interest</name>
  <files>src/lib/nw-forecast.test.ts</files>
  <read_first>
    src/lib/savings-interest.ts
    src/lib/nw-forecast.ts
    src/lib/nw-forecast.test.ts
    .planning/phases/28-interest-math-forecast-kind/28-RESEARCH.md
  </read_first>
  <behavior>
    - Two primary accounts, balances 1_000_000n and 2_000_000n, both annualRateBps 1200, DOM 15, today 2026-03-01, horizonEnd 2026-03-31: listInterestSlotsInRange returns 10000n and 20000n on 2026-03-15 with distinct parentIds (D-04)
    - Hand-map those slots to ForecastSlot kind interest (parentId = accountId, plannedAmountMinor = interestMinor, primary currency) and buildNetWorthForecastSeries with anchor 0n: the 2026-03-15 point forecastPrimaryMinor is 30000n (D-03, D-12; builder adds precomputed amounts)
    - nw-forecast.ts source does not import the interest module (compound stays in the enumerator)
    - One non-primary interest slot, currency USD, rates empty, plannedAsOf 2026-03-15: excludedMissingFxCount is at least 1, isPartialForecast is true, includedSlotCount is 0 (D-06 FX gate already in the builder)
  </behavior>
  <action>
    Extend src/lib/nw-forecast.test.ts. Import listInterestSlotsInRange from ./savings-interest inside the test only. Add the two-account stair-step case and the USD missing-rate case from the behavior block. Add a readFileSync assertion that src/lib/nw-forecast.ts does not reference the interest module path. Do not change savings-interest.ts or the builder to special-case this test. If the missing-FX case fails, the existing non-primary branch is wrong for interest — fix the kind window or addend only, not a second FX path. Do not wire Dashboard or MCP membership.
  </action>
  <verify>
    <automated>./node_modules/.bin/vitest run src/lib/savings-interest.test.ts src/lib/nw-forecast.test.ts</automated>
  </verify>
  <done>The mapped two-account point is 30000n, the USD interest slot is excluded as missing FX, and both unit files stay green</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Caller-supplied today LOCF bigint + bps → interest slots | Untrusted fixture or future page input crosses into forecast ΔNW |
| ForecastSlot kind interest → buildNetWorthForecastSeries | Kind mistake can drop a credit or double-count a snapshot day |
| SerializedForecastEvent.kind | Wider union must not start loading interest rows in this phase |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-28-01 | Tampering | monthlyInterestMinor | high | mitigate | Single bigint division by 12 times 10000. Guard non-positive bps and non-positive balance to 0n. No float and no exponentiation API |
| T-28-02 | Tampering | listInterestSlotsInRange and slotInWindow | high | mitigate | Cursor is the day after today. Interest window is plannedAsOf strictly after today. No synthetic today credit added into starting principal |
| T-28-03 | Tampering | historical NW / BalanceSnapshot | high | mitigate | Interest module does not import historical NW or a database client. Builder does not import the interest module. No snapshot writes. Loader membership stays income plus grace |
| T-28-04 | Tampering | negative LOCF emit | medium | mitigate | Helper returns 0n for balance less than or equal to 0n. Enumerator emits only interestMinor greater than 0n |
| T-28-05 | Tampering | shared or overlay principal | medium | mitigate | Per-account principal starts at injected today LOCF and grows only by that account truncated credit. Builder adds plannedAmountMinor as given |
| T-28-SC | Tampering | npm installs | high | mitigate | No package install. Package legitimacy table in RESEARCH is empty because nothing is installed |
</threat_model>

<verification>
Quick: ./node_modules/.bin/vitest run src/lib/savings-interest.test.ts src/lib/nw-forecast.test.ts
Phase gate: npm test
Confirm load-forecast-overlay.ts still passes only income and grace slots into buildNetWorthForecastSeries.
</verification>

<success_criteria>
INT-01 holds in unit tests: ÷12 truncate, compound chain, clamped future accrual dates, kind interest ΔNW positive, today excluded, grace still zero, serialized kind widened, no Dashboard or MCP membership wire.
</success_criteria>

<output>
Create `.planning/phases/28-interest-math-forecast-kind/28-02-SUMMARY.md` when done
</output>
