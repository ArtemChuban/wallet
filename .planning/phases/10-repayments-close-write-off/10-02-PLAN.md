---
phase: 10-repayments-close-write-off
plan: 02
type: execute
wave: 2
depends_on:
  - "10-01"
files_modified:
  - src/lib/validations/debts.ts
  - src/lib/validations/debts.test.ts
  - src/app/debts/actions.ts
  - src/app/debts/actions.test.ts
  - src/app/debts/page.tsx
  - src/components/debts/DebtDetailDialog.tsx
  - src/components/debts/DebtsList.tsx
  - src/components/debts/DebtFormDialog.tsx
autonomous: true
requirements:
  - REPAY-02
  - REPAY-03
estimate:
  tokens: 26000
  raw_tokens: 52000
  tasks: 3
  confidence: high
must_haves:
  truths:
    - User sees mixed timeline of repayments and size-changes newest-first in DebtDetailDialog (REPAY-02 / D-05)
    - Event type labels use «Погашение» for repayments; manual size-changes «Изменение суммы» until Plan 03 adds «Списание» (D-07 partial)
    - User deletes a repayment via in-dialog second-step confirm; remaining recalculates; status reopens OPEN when remaining > 0 (REPAY-03 / D-06 / DEBT-04 adjacency)
    - deleteRepayment twice on same id: second call fails gracefully without corrupting ledger (REPAY-03 idempotency)
    - CLOSED debts stay in same person group under collapsed «Закрытые (N)» default closed; still openable for full detail (D-11 / D-12 / D-13)
    - statement: Concurrent delete+create on same debt serializes via SQLite; status always matches remaining after each successful transaction (REPAY-03 concurrency)
      verification: backstop
  prohibitions:
    - statement: MUST NOT use native browser confirm dialogs for delete — use DestructiveConfirmStep only
      requirement_id: REPAY-03
      category: values
    - statement: MUST NOT put CLOSED debts in a page-global closed section outside person groups
      requirement_id: REPAY-02
      category: values
    - statement: MUST NOT call revalidatePath on dashboard root
      requirement_id: REPAY-03
      category: isolation
  artifacts:
    - path: src/lib/validations/debts.ts
      provides: deleteRepaymentSchema (and deleteSizeChangeSchema stub for Plan 03)
    - path: src/app/debts/actions.ts
      provides: deleteRepayment with status recompute
    - path: src/components/debts/DebtDetailDialog.tsx
      provides: mixed newest-first timeline + repayment delete confirm
    - path: src/components/debts/DebtsList.tsx
      provides: per-person «Закрытые (N)» collapsed subsection
    - path: src/app/debts/page.tsx
      provides: full event rows (id, asOfDate, amount/delta, note) as strings
  key_links:
    - from: src/components/debts/DebtDetailDialog.tsx
      to: src/app/debts/actions.ts
      via: deleteRepayment FormData id
      pattern: "deleteRepayment"
    - from: src/components/debts/DebtsList.tsx
      to: Debt.status / remaining
      via: OPEN vs CLOSED split
      pattern: "Закрытые"
    - from: src/app/debts/actions.ts
      to: src/lib/debts.ts
      via: remainingMinor + statusForRemaining after delete
      pattern: "statusForRemaining"
---

## Phase Goal

Partial dated repayments with history/delete, auto-close at zero, early write-off close.

<objective>
Expand tracer with mixed event history, repayment delete+reopen, and per-person collapsed CLOSED subsection (REPAY-02, REPAY-03 / D-05, D-06, D-11–D-13).

Purpose: Complete repayment lifecycle UX before forgive/size-change.
Output: deleteRepayment action, timeline UI, «Закрытые (N)» grouping, green tests.
</objective>

<execution_context>
@/home/artem/Documents/wallet/.cursor/gsd-core/workflows/execute-plan.md
@/home/artem/Documents/wallet/.cursor/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/10-repayments-close-write-off/10-CONTEXT.md
@.planning/phases/10-repayments-close-write-off/10-RESEARCH.md
@.planning/phases/10-repayments-close-write-off/10-PATTERNS.md
@.planning/phases/10-repayments-close-write-off/10-01-PLAN.md
@src/app/debts/actions.ts
@src/app/debts/actions.test.ts
@src/components/debts/DebtDetailDialog.tsx
@src/components/debts/DebtsList.tsx
@src/components/debts/DestructiveConfirmStep.tsx
@src/components/accounts/AccountList.tsx
@src/lib/validations/balance.ts
@src/lib/validations/debts.ts
</context>

<assumptions>
## Planner assumptions (specless / discretion)

- A-edge REPAY-02 unclassified: mixed timeline = repayments + size-changes; UI sort newest-first (asOfDate DESC, id DESC); domain remaining stays order-independent sum (Phase 8 D-10).
- A-edge REPAY-03 idempotency: second delete of same id returns Russian error / message and does not call revalidate on failure; ledger unchanged.
- A-edge REPAY-03 concurrency: interactive $transaction reload full ledger after delete then statusForRemaining; no multi-tab merge beyond SQLite lock.
- Discretion: Empty history copy Russian microcopy OK as long as D-07 repayment label «Погашение» holds; «Списание» wiring waits Plan 03 isForgive.
- Size-change delete UI may show for existing rows but deleteSizeChange action implementation can land fully in Plan 03 if schema/actions not ready — prefer implement deleteRepayment fully here; add deleteSizeChangeSchema now and wire deleteSizeChange action if trivial reuse, else Plan 03 owns size-change delete.
</assumptions>

## Artifacts this phase produces

- `src/lib/validations/debts.ts` — `deleteRepaymentSchema`, `deleteSizeChangeSchema`
- `src/lib/validations/debts.test.ts` — delete schema coerce/strict cases
- `src/app/debts/actions.ts` — `deleteRepayment` (+ optional `deleteSizeChange` if ready)
- `src/components/debts/DebtDetailDialog.tsx` — mixed timeline newest-first; DestructiveConfirmStep delete
- `src/components/debts/DebtsList.tsx` — «Закрытые (N)» collapsed by default (D-11/D-12)
- `src/app/debts/page.tsx` — event row serialization for timeline

<tasks>

<task type="auto" tdd="true">
  <name>deleteRepaymentSchema + red delete/reopen action tests</name>
  <files>src/lib/validations/debts.ts, src/lib/validations/debts.test.ts, src/app/debts/actions.test.ts</files>
  <read_first>
    .planning/phases/10-repayments-close-write-off/10-VALIDATION.md
    src/lib/validations/balance.ts
    src/lib/validations/debts.ts
    src/lib/validations/debts.test.ts
    src/app/debts/actions.test.ts
  </read_first>
  <behavior>
    - deleteRepaymentSchema / deleteSizeChangeSchema: positive int id coerce; .strict() rejects smuggled keys (REPAY-03 / T-10-03)
    - deleteRepayment removes row, recomputes remaining, sets OPEN when remaining > 0 after prior CLOSED (REPAY-03)
    - missing id / failed delete returns message without revalidatePath
    - revalidatePath("/debts") only on success
  </behavior>
  <action>
    Add deleteRepaymentSchema and deleteSizeChangeSchema to src/lib/validations/debts.ts mirroring deleteBalanceSchema shape (id coerce positive int, .strict()). Extend debts.test.ts with coerce/reject-smuggle cases. Extend actions.test.ts with failing deleteRepayment tests: reopen OPEN after delete when remaining > 0, DISOL revalidate only /debts, graceful miss. Do not implement deleteRepayment production body yet if tests must stay red for TDD — implement in next task of this plan after red confirmed, or same commit only after green path in task 2. Prefer: this task lands schemas green + red action tests; next task greens deleteRepayment.
  </action>
  <verify>
    <automated>npx vitest run src/lib/validations/debts.test.ts && (npx vitest run src/app/debts/actions.test.ts; test $? -ne 0)</automated>
    <fails_when>validations suite non-zero, or actions suite already fully green without deleteRepayment implementation pending</fails_when>
  </verify>
  <acceptance_criteria>
    - grep -q 'deleteRepaymentSchema' src/lib/validations/debts.ts
    - grep -q 'deleteRepayment' src/app/debts/actions.test.ts
    - validations tests pass; at least one new deleteRepayment action test fails until task 2
  </acceptance_criteria>
  <done>Delete schemas green; red deleteRepayment reopen tests authored</done>
</task>

<task type="auto" tdd="true">
  <name>deleteRepayment action + mixed timeline + delete confirm</name>
  <files>src/app/debts/actions.ts, src/app/debts/page.tsx, src/components/debts/DebtDetailDialog.tsx, src/components/debts/DebtFormDialog.tsx</files>
  <read_first>
    .planning/phases/10-repayments-close-write-off/10-CONTEXT.md
    .planning/phases/10-repayments-close-write-off/10-PATTERNS.md
    src/app/debts/actions.ts
    src/components/debts/DebtDetailDialog.tsx
    src/components/accounts/AccountList.tsx
    src/components/debts/DestructiveConfirmStep.tsx
  </read_first>
  <behavior>
    - deleteRepayment: Zod deleteRepaymentSchema → $transaction delete by id → reload parent debt ledger → statusForRemaining → revalidatePath("/debts") (REPAY-03 / D-06)
    - Timeline merges repayments + sizeChanges; sort asOfDate DESC then id DESC; label «Погашение» for repayment kind (REPAY-02 / D-05 / D-07)
    - Delete uses DestructiveConfirmStep second step inside detail Dialog — no native browser confirm (D-06)
    - Notes render as React text (T-10-05)
  </behavior>
  <action>
    Implement deleteRepayment in actions.ts: parse deleteRepaymentSchema, $transaction find repayment by id, delete, reload debt with all events, remainingMinor + statusForRemaining update, revalidatePath("/debts") only (T-10-03 / T-10-04). Extend page.tsx event selects to include id, asOfDate, note, amountMinor/deltaMinor (strings to client). In DebtDetailDialog build mixed timeline newest-first; show «Погашение» for repayments and «Изменение суммы» for size-changes without isForgive yet (D-05/D-07). Wire repayment delete through DestructiveConfirmStep with Russian confirm copy (D-06). Keep forms above history (A4). Do not add forgive button here. <!-- planner-discipline-allow: window.confirm -->
  </action>
  <verify>
    <automated>npx vitest run src/app/debts/actions.test.ts src/lib/validations/debts.test.ts && grep -q 'deleteRepayment' src/app/debts/actions.ts && grep -q 'Погашение' src/components/debts/DebtDetailDialog.tsx && grep -q 'DestructiveConfirmStep' src/components/debts/DebtDetailDialog.tsx && ! grep -R --include='*.tsx' -n 'window\.confirm' src/components/debts/</automated>
    <fails_when>non-zero vitest, missing deleteRepayment, missing «Погашение» label, or native confirm grepped under src/components/debts/</fails_when>
  </verify>
  <acceptance_criteria>
    - deleteRepayment reopen + DISOL tests pass
    - Timeline shows «Погашение» and newest-first merge (REPAY-02 / D-05)
    - Delete path uses DestructiveConfirmStep (D-06)
    - No native browser confirm under src/components/debts/
  </acceptance_criteria>
  <done>History + delete repayment + reopen green</done>
</task>

<task type="auto">
  <name>Per-person collapsed «Закрытые (N)» subsection</name>
  <files>src/components/debts/DebtsList.tsx</files>
  <read_first>
    .planning/phases/10-repayments-close-write-off/10-CONTEXT.md
    src/components/debts/DebtsList.tsx
    src/app/debts/page.tsx
  </read_first>
  <behavior>
    - Within each person group, OPEN debts listed normally; CLOSED debts under «Закрытые (N)» collapsed by default (D-11 / D-12)
    - Expanding CLOSED still opens DebtDetailDialog with full access (D-13)
    - Not a page-global closed section
  </behavior>
  <action>
    Update DebtsList PersonGroup to split debts by status CLOSED vs OPEN (or remaining === 0 after sync — prefer debt.status from page). Render OPEN rows first; render collapsed disclosure «Закрытые (N)» default closed containing CLOSED DebtCompactRow instances that still open DebtDetailDialog (D-11/D-12/D-13). Do not move closed debts outside person groups. Keep «Изменить» stopPropagation.
  </action>
  <verify>
    <automated>grep -q 'Закрытые' src/components/debts/DebtsList.tsx && grep -q 'DebtDetailDialog' src/components/debts/DebtsList.tsx && npx vitest run src/app/debts/actions.test.ts</automated>
    <fails_when>«Закрытые» missing in DebtsList, detail dialog unwired, or actions tests fail</fails_when>
  </verify>
  <acceptance_criteria>
    - DebtsList contains «Закрытые» subsection copy (D-11)
    - CLOSED rows remain reachable for detail (D-13)
    - actions suite still green
  </acceptance_criteria>
  <done>CLOSED subsection UX landed per D-11–D-13</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Browser → deleteRepayment | Untrusted FormData id |
| Client timeline props | Event notes / amounts from RSC |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-10-03 | Tampering | deleteRepayment | high | mitigate | deleteRepaymentSchema positive int; Prisma delete by id; graceful miss; single-user residual IDOR accepted low |
| T-10-04 | Tampering | revalidatePath | high | mitigate | revalidatePath("/debts") only on success; tests assert |
| T-10-05 | Tampering | note XSS | medium | mitigate | React text nodes; existing note max length |
| T-10-01 | Tampering | remaining after delete | medium | mitigate | Reload full ledger in transaction before statusForRemaining |
| T-10-SC | Tampering | npm installs | high | accept | No new packages |
</threat_model>

<verification>
delete schemas + deleteRepayment reopen tests green; timeline labels present; «Закрытые» in DebtsList; no native confirm in debts components.
</verification>

<success_criteria>
User sees history, deletes repayments with reopen, and finds CLOSED debts under per-person collapsed «Закрытые».
</success_criteria>

<output>
Create `.planning/phases/10-repayments-close-write-off/10-02-SUMMARY.md` when done
</output>
