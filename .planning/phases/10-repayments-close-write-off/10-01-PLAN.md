---
phase: 10-repayments-close-write-off
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/app/debts/actions.ts
  - src/app/debts/actions.test.ts
  - src/app/debts/page.tsx
  - src/components/debts/DebtDetailDialog.tsx
  - src/components/debts/DebtsList.tsx
  - src/components/debts/DebtFormDialog.tsx
autonomous: true
requirements:
  - REPAY-01
  - DEBT-04
estimate:
  tokens: 28000
  raw_tokens: 56000
  tasks: 3
  confidence: high
must_haves:
  truths:
    - User records same-currency partial repayment with asOfDate (backdating allowed) from dedicated debt detail Dialog (REPAY-01 / D-01 / D-03)
    - Entire debt row click opens DebtDetailDialog; meta «Изменить» stays separate with stopPropagation (D-02 / D-04)
    - After repayment that brings remaining to 0, Debt.status becomes CLOSED via statusForRemaining (DEBT-04)
    - Over-repayment rejected by assertRepaymentAmount before write (T-10-01)
    - createRepayment is idempotent per call: each successful submit inserts one DebtRepayment row; duplicate submits create distinct rows (REPAY-01 edge)
    - statement: Future asOfDate rejected with «Дата не может быть в будущем»; backdating still allowed (RESEARCH A3 / D-10 adjacency)
      verification: backstop
  prohibitions:
    - statement: MUST NOT put repayment UI inside DebtFormDialog
      requirement_id: REPAY-01
      category: values
    - statement: MUST NOT call revalidatePath on dashboard root; only /debts
      requirement_id: REPAY-01
      category: isolation
    - statement: MUST NOT flip Debt.status to CLOSED without a ledger event that zeros remaining
      requirement_id: DEBT-04
      category: values
    - statement: MUST NOT introduce writeOffMinor column or WRITE_OFF repayment type
      requirement_id: DEBT-04
      category: values
  artifacts:
    - path: src/app/debts/actions.ts
      provides: createRepayment Server Action with transactional status sync
    - path: src/app/debts/actions.test.ts
      provides: createRepayment + over-repay + CLOSED-at-zero + DISOL revalidate coverage
    - path: src/components/debts/DebtDetailDialog.tsx
      provides: dedicated Dialog with repayment form (D-01)
    - path: src/components/debts/DebtsList.tsx
      provides: row click opens detail; Изменить stopPropagation
    - path: src/app/debts/page.tsx
      provides: status + remainingMinor serialized for list/detail
  key_links:
    - from: src/components/debts/DebtsList.tsx
      to: src/components/debts/DebtDetailDialog.tsx
      via: row click controlled open
      pattern: "DebtDetailDialog"
    - from: src/app/debts/actions.ts
      to: src/lib/debts.ts
      via: remainingMinor + assertRepaymentAmount + statusForRemaining
      pattern: "createRepayment"
    - from: src/app/debts/actions.ts
      to: src/lib/validations/debts.ts
      via: createRepaymentSchema.safeParse
      pattern: "createRepaymentSchema"
---

## Phase Goal

Partial dated repayments with history/delete, auto-close at zero, early write-off close.

<objective>
Ship tracer vertical slice: create repayment → remaining and Debt.status update visible on /debts via dedicated DebtDetailDialog (REPAY-01, DEBT-04 / D-01–D-04).

Purpose: Prove event write + status sync end-to-end before delete/history and forgive expansion.
Output: createRepayment action, DebtDetailDialog repayment form, row-click open, green Wave 0 + tracer tests.
</objective>

<execution_context>
@/home/artem/Documents/wallet/.cursor/gsd-core/workflows/execute-plan.md
@/home/artem/Documents/wallet/.cursor/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/10-repayments-close-write-off/10-CONTEXT.md
@.planning/phases/10-repayments-close-write-off/10-RESEARCH.md
@.planning/phases/10-repayments-close-write-off/10-PATTERNS.md
@.planning/phases/10-repayments-close-write-off/10-VALIDATION.md
@src/app/debts/actions.ts
@src/app/debts/actions.test.ts
@src/app/debts/page.tsx
@src/components/debts/DebtsList.tsx
@src/components/debts/DebtFormDialog.tsx
@src/components/debts/DestructiveConfirmStep.tsx
@src/lib/debts.ts
@src/lib/validations/debts.ts
@src/app/accounts/actions.ts
</context>

<assumptions>
## Planner assumptions (specless / discretion)

- A-edge REPAY-01 unclassified: interpret as partial same-currency repayment with asOfDate; Zod createRepaymentSchema + assertRepaymentAmount + status sync (CONTEXT D-01 surface).
- A-edge DEBT-04 unclassified: auto-close means persist Debt.status = statusForRemaining(remaining) after every repayment write — no separate close toggle.
- A-edge REPAY concurrency (shared with later plans): SQLite single-writer; interactive $transaction binds event create + status update; interrupted mid-transaction rolls back both.
- Discretion A1: Hide «Простить остаток» deferred to Plan 03; this plan has no forgive UI.
- Discretion A3: Reject future asOfDate like accounts («Дата не может быть в будущем»); backdating allowed.
- Discretion A4: Forms above history in DebtDetailDialog; history list itself lands Plan 02 — tracer may show empty history chrome or omit timeline rows until Plan 02.
- Schema isForgive deferred to Plan 03 (one-way gate); tracer repayment path does not need it.
</assumptions>

## Artifacts this phase produces

- `src/app/debts/actions.ts` — `createRepayment` (Plan 01); later plans add delete/size-change/forgive
- `src/app/debts/actions.test.ts` — createRepayment + over-repay + CLOSED + revalidatePath("/debts") only
- `src/components/debts/DebtDetailDialog.tsx` — dedicated detail Dialog (D-01); repayment form first
- `src/components/debts/DebtsList.tsx` — row click → detail; «Изменить» stopPropagation (D-02/D-04)
- `src/app/debts/page.tsx` — pass `status` + remainingMinor strings (and event stubs as needed)
- `src/components/debts/DebtFormDialog.tsx` — extend DebtRow type with `status` if shared; no repay fields inside form body

<tasks>

<task type="auto" tdd="true">
  <name>Wave 0: red createRepayment + over-repay + CLOSED-at-zero tests</name>
  <files>src/app/debts/actions.test.ts</files>
  <read_first>
    .planning/phases/10-repayments-close-write-off/10-VALIDATION.md
    .planning/phases/10-repayments-close-write-off/10-PATTERNS.md
    src/app/debts/actions.test.ts
    src/app/accounts/actions.test.ts
    src/lib/validations/debts.ts
    src/lib/debts.ts
  </read_first>
  <behavior>
    - createRepayment parses createRepaymentSchema; loads debt ledger; assertRepaymentAmount; creates DebtRepayment; updates Debt.status via statusForRemaining; revalidatePath("/debts") only (REPAY-01 / DEBT-04)
    - amount exceeding remaining returns field/message error and does not write (T-10-01)
    - repayment equal to remaining sets status CLOSED
    - future asOfDate returns «Дата не может быть в будущем»
    - revalidatePath never called with dashboard root path
  </behavior>
  <action>
    Extend src/app/debts/actions.test.ts mocks: add prisma.debtRepayment, prisma.debt.findUniqueOrThrow (or findUnique), prisma.$transaction that invokes callback with tx sharing mocked methods, and mock calendarDateToday from @/lib/balances like accounts/actions.test.ts. Author failing tests for createRepayment success (partial pay stays OPEN), full pay → CLOSED, over-repay rejection, future-date rejection, and DISOL revalidatePath("/debts") only. Do not implement createRepayment production code yet — tests must fail until tracer. Do not add delete/forgive tests here (Plans 02–03).
  </action>
  <verify>
    <automated>npx vitest run src/app/debts/actions.test.ts; test $? -ne 0</automated>
    <fails_when>vitest exits 0 (tests already green before tracer) or createRepayment describe missing</fails_when>
  </verify>
  <acceptance_criteria>
    - grep -q 'createRepayment' src/app/debts/actions.test.ts
    - grep -q 'CLOSED' src/app/debts/actions.test.ts
    - npx vitest run src/app/debts/actions.test.ts exits non-zero before tracer lands
  </acceptance_criteria>
  <done>Red Wave 0 tests exist for createRepayment, over-repay, CLOSED-at-zero, future date, DISOL</done>
</task>

<task type="tracer">
  <name>End-to-end create repayment → remaining/status visible</name>
  <reversibility rating="reversible">Repayment action and detail Dialog are additive UI/API; undo is revert with cascade-safe DebtRepayment rows.</reversibility>
  <files>src/app/debts/actions.ts, src/components/debts/DebtDetailDialog.tsx, src/components/debts/DebtsList.tsx, src/app/debts/page.tsx, src/components/debts/DebtFormDialog.tsx</files>
  <read_first>
    .planning/phases/10-repayments-close-write-off/10-CONTEXT.md
    .planning/phases/10-repayments-close-write-off/10-RESEARCH.md
    .planning/phases/10-repayments-close-write-off/10-PATTERNS.md
    src/app/debts/actions.ts
    src/components/debts/DebtFormDialog.tsx
    src/components/debts/DebtsList.tsx
    src/app/debts/page.tsx
    src/lib/debts.ts
    src/lib/validations/debts.ts
    src/app/accounts/actions.ts
    node_modules/next/dist/docs/01-app/01-getting-started/07-mutating-data.md
  </read_first>
  <behavior>
    - createRepayment uses createRepaymentSchema.strict(), calendarDateToday future gate, parseMajorToMinor against debt.currency.scale, remainingMinor + assertRepaymentAmount, prisma.$transaction create repayment + Debt.status = statusForRemaining, revalidatePath("/debts") only (REPAY-01 / DEBT-04 / D-01)
    - DebtDetailDialog is separate Dialog (not Sheet, not inside DebtFormDialog body) with amountMajor, asOfDate default today, optional note (D-01 / D-03)
    - DebtsList: clicking debt row opens detail; «Изменить» uses stopPropagation and opens DebtFormDialog edit (D-02 / D-04)
    - page serializes status + remainingMinor strings so list refreshes show closed/open after repay
  </behavior>
  <action>
    Implement createRepayment in src/app/debts/actions.ts per RESEARCH Pattern 1 and PATTERNS: Zod createRepaymentSchema, reject asOfDate greater than calendarDateToday with Russian future-date message, load debt with repayments/sizeChanges/currency inside prisma.$transaction, compute remainingBefore via remainingMinor, assertRepaymentAmount, create DebtRepayment, update Debt.status via statusForRemaining(remainingAfter), revalidatePath("/debts") only (DISOL-01 / T-10-04). Map domain assert failures to actionable Russian field errors. Add DebtDetailDialog.tsx as dedicated Dialog with formKey remount, useActionState(createRepayment), forms above any history placeholder (discretion A4); wire «Изменить» to open existing DebtFormDialog edit mode (D-04). Update DebtsList so entire compact row opens detail (D-02); wrap meta edit control with stopPropagation. Extend page.tsx select/serialize to include debt.status and remainingMinor string (prefer stored status after sync writes). Extend DebtRow type in DebtFormDialog if shared. Do not implement delete/forgive/size-change or CLOSED subsection here. Do not add Sheet. Do not import debts into NW modules.
  </action>
  <verify>
    <automated>npx vitest run src/app/debts/actions.test.ts && grep -q 'createRepayment' src/app/debts/actions.ts && grep -q 'statusForRemaining' src/app/debts/actions.ts && test -f src/components/debts/DebtDetailDialog.tsx && grep -q 'DebtDetailDialog' src/components/debts/DebtsList.tsx && grep -q 'stopPropagation' src/components/debts/DebtsList.tsx</automated>
    <fails_when>non-zero vitest exit, missing createRepayment export, missing DebtDetailDialog file, or DebtsList missing detail/stopPropagation wiring</fails_when>
  </verify>
  <acceptance_criteria>
    - Wave 0 createRepayment tests pass including CLOSED-at-zero and over-repay
    - DebtDetailDialog exists and opens from row click (D-01/D-02)
    - createRepayment syncs Debt.status; revalidatePath only /debts
    - Meta «Изменить» does not open detail due to stopPropagation (D-04)
  </acceptance_criteria>
  <done>Tracer green: repayment create end-to-end with remaining/status visible and tests</done>
</task>

<task type="auto">
  <name>Keep DISOL + debts action suite green</name>
  <files>src/lib/debts.test.ts</files>
  <read_first>
    src/lib/debts.test.ts
    src/app/page.tsx
    src/lib/net-worth.ts
  </read_first>
  <action>
    Run Phase 8 DISOL isolation suite unchanged. Confirm this plan introduced no debt imports into net-worth.ts, historical-series.ts, or app/page.tsx. Revert any accidental NW coupling. Do not change domain math formulas.
  </action>
  <verify>
    <automated>npx vitest run src/lib/debts.test.ts && npx vitest run src/app/debts/actions.test.ts</automated>
    <fails_when>non-zero vitest exit or DISOL-01 describe failures</fails_when>
  </verify>
  <acceptance_criteria>
    - debts.test.ts DISOL-01 cases still pass
    - createRepayment tests remain green
  </acceptance_criteria>
  <done>Isolation preserved; plan 01 tests green</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Browser → createRepayment | Untrusted FormData (debtId, amountMajor, asOfDate, note) |
| RSC → client props | Serialized debt rows / remaining / status strings |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-10-01 | Tampering | createRepayment | high | mitigate | assertRepaymentAmount before write; Zod .strict(); scale-aware parseMajorToMinor |
| T-10-02 | Tampering | createRepayment smuggled fields | medium | mitigate | createRepaymentSchema.strict(); ignore unknown FormData keys |
| T-10-04 | Tampering | revalidatePath | high | mitigate | Only revalidatePath("/debts"); tests assert not dashboard root |
| T-10-05 | Tampering | note XSS | medium | mitigate | optionalNoteSchema trim/max 500; React text nodes only |
| T-10-SC | Tampering | npm installs | high | accept | No new packages this plan — legitimacy N/A |
</threat_model>

<verification>
Wave 0 red then tracer green; createRepayment + DISOL suites pass; detail Dialog wired from row click.
</verification>

<success_criteria>
User can open debt detail and record a dated repayment; remaining and status update; over-repay blocked; DISOL intact.
</success_criteria>

<output>
Create `.planning/phases/10-repayments-close-write-off/10-01-SUMMARY.md` when done
</output>
