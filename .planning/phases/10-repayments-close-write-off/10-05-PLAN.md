---
phase: 10-repayments-close-write-off
plan: 05
type: execute
wave: 5
depends_on:
  - "10-04"
files_modified:
  - src/components/debts/forgiveClientError.ts
  - src/components/debts/forgiveClientError.test.ts
  - src/components/debts/DebtDetailDialog.tsx
  - src/components/debts/DebtDetailDialog.test.ts
  - src/app/debts/actions.ts
autonomous: true
gap_closure: true
cancelled: true
cancelled_reason: "User waived G-10-8 opaque stale-tab UX (2026-09-05); deferred follow-up"
gap_ids:
  - G-10-8
requirements:
  - REPAY-01
  - REPAY-03
  - DEBT-05
estimate:
  tokens: 16000
  raw_tokens: 32000
  tasks: 2
  confidence: high
must_haves:
  truths:
    - Forgive failure with only errors.deltaMajor shows that Russian field error (never client opaque fallback) (G-10-8 / WR-01)
    - Forgive failure with message refresh RU keeps DestructiveConfirmStep open and shows that message via actionError (G-10-8 / CR-01)
    - Forgive tabpanel renders actionError when set (defense in depth) (G-10-8 / CR-01)
    - Opaque client fallback never wins when server returns refresh RU or deltaMajor (G-10-8)
  prohibitions:
    - statement: Do not reopen G-10-5 P2025 / isRecordNotFound catch rewrites on createRepayment createSizeChange delete paths
      category: values
    - statement: Do not add a forgive-flag column or migration for DebtSizeChange
      category: values
    - statement: Do not change timeline labels to introduce a distinct forgive/write-off label
      category: values
    - statement: Do not call revalidatePath on dashboard root
      requirement_id: DISOL-01
      category: isolation
    - statement: Do not expand scope to WR-02 pendingLabel WR-03 createdAt sort or WR-04 deleteSizeChange floor mapping
      category: values
  artifacts:
    - path: src/components/debts/forgiveClientError.ts
      provides: pure resolveForgiveFailureMessage chaining asOfDate then deltaMajor then message then opaque fallback
    - path: src/components/debts/forgiveClientError.test.ts
      provides: vitest proving refresh RU and deltaMajor beat opaque fallback
    - path: src/components/debts/DebtDetailDialog.tsx
      provides: forgive handleConfirm uses resolver; keeps confirm on failure; forgive tab shows actionError
    - path: src/components/debts/DebtDetailDialog.test.ts
      provides: source-contract greps for deltaMajor chain keep-confirm forgive-tab alert
  key_links:
    - from: DebtDetailDialog handleConfirm forgive branch
      to: resolveForgiveFailureMessage
      via: setActionError(resolveForgiveFailureMessage(result)); return without clearing confirm
      pattern: "resolveForgiveFailureMessage"
    - from: forgive tabpanel
      to: actionError
      via: role=alert paragraph when actionError set
      pattern: "role=\"alert\""
---

## Gap

**gap_id:** G-10-8  
**truth:** After peer-tab delete, stale repay/size/forgive shows «Долг или запись не найдены. Обновите страницу.» (never opaque catch-all) — forgive path must surface server refresh RU / deltaMajor, not synthesize opaque or hide it  
**source:** `.planning/phases/10-repayments-close-write-off/10-UAT.md` test 5 + `.planning/debug/concurrent-stale-opaque-after-g105.md` + `10-REVIEW.md` CR-01 / WR-01

<objective>
Close G-10-8: fix DebtDetailDialog forgive error surface (include deltaMajor, keep confirm on failure, render actionError on forgive tab), optionally dual-map forgiveRemaining OVER_FLOOR/DELTA_ZERO onto message, and lock with vitest so opaque fallback never wins when server sends refresh RU or deltaMajor.

Purpose: Re-UAT concurrency still shows opaque because forgive client ignores field errors and clears confirm (CR-01/WR-01); server P2025 map from 10-04 stays untouched.
Output: forgiveClientError helper + tests, DebtDetailDialog forgive UX fix, optional actions.ts message dual-map.
</objective>

<execution_context>
@/home/artem/Documents/wallet/.cursor/gsd-core/workflows/execute-plan.md
@/home/artem/Documents/wallet/.cursor/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/10-repayments-close-write-off/10-UAT.md
@.planning/debug/concurrent-stale-opaque-after-g105.md
@.planning/phases/10-repayments-close-write-off/10-REVIEW.md
@.planning/phases/10-repayments-close-write-off/10-04-PLAN.md
@.planning/phases/10-repayments-close-write-off/10-04-SUMMARY.md
@src/components/debts/DebtDetailDialog.tsx
@src/app/debts/actions.ts
@src/components/accounts/AccountList.test.ts
</context>

<assumptions>
- Root cause is client forgive chain (WR-01 + CR-01); 10-04 server P2025 mapping already OK for missing debt — do not reopen that work.
- Exact refresh RU from 10-04: «Долг или запись не найдены. Обновите страницу.»
- Project vitest is node + `src/**/*.test.ts` only (no RTL/jsdom) — use pure helper unit tests + readFileSync source contracts (AccountList.test.ts pattern).
- Optional server dual-map: forgiveRemaining OVER_FLOOR / DELTA_ZERO may return message (same RU as errors.deltaMajor) in addition to or instead of only errors.deltaMajor so all clients share one field.
- Delete-repayment / delete-sizeChange confirm clearing unchanged; only forgive failure retention required for G-10-8.
</assumptions>

<tasks>

<task type="tracer" tdd="true">
  <name>End-to-end: forgive error resolver — deltaMajor/refresh beat opaque</name>
  <files>src/components/debts/forgiveClientError.ts, src/components/debts/forgiveClientError.test.ts</files>
  <read_first>
    .planning/debug/concurrent-stale-opaque-after-g105.md
    .planning/phases/10-repayments-close-write-off/10-REVIEW.md
    src/app/debts/actions.ts
    src/components/debts/DebtDetailDialog.tsx
  </read_first>
  <behavior>
    - resolveForgiveFailureMessage with only errors.deltaMajor returns that string (G-10-8 / WR-01)
    - resolveForgiveFailureMessage with message refresh RU returns that string (G-10-8)
    - When neither field errors nor message present, returns the same client opaque fallback string currently used in DebtDetailDialog forgive handleConfirm
    - Prefer asOfDate[0] over deltaMajor[0] over message over opaque (chain order matches CR-01 review snippet)
    - Dedicated vitest titles contain "(G-10-8)"
  </behavior>
  <action>
    Per diagnosis G-10-8 and 10-REVIEW CR-01/WR-01: create pure helper module src/components/debts/forgiveClientError.ts exporting resolveForgiveFailureMessage(result) that reads DebtActionState-shaped { errors?, message? } and returns asOfDate[0] then deltaMajor[0] then message then the existing forgive-client opaque fallback string (copy verbatim from DebtDetailDialog forgive handleConfirm today). Author vitest first in forgiveClientError.test.ts with titles containing "(G-10-8)": (1) deltaMajor-only OVER_FLOOR RU wins — not opaque; (2) message-only refresh RU «Долг или запись не найдены. Обновите страницу.» wins — not opaque; (3) empty result uses opaque fallback. Do not modify DebtDetailDialog or actions.ts in this task. Do not touch isRecordNotFound / P2025 catches.
  </action>
  <verify>
    <automated>grep -q '(G-10-8)' src/components/debts/forgiveClientError.test.ts && npx vitest run src/components/debts/forgiveClientError.test.ts</automated>
    <fails_when>grep miss for G-10-8 test titles, non-zero vitest exit, zero tests matched, or opaque wins when deltaMajor/refresh present</fails_when>
  </verify>
  <done>Pure resolver green: deltaMajor and refresh RU beat opaque; empty falls back; tracer committed</done>
</task>

<task type="auto" tdd="true">
  <name>Wire forgive confirm retention + tab alert + optional message dual-map</name>
  <files>src/components/debts/DebtDetailDialog.tsx, src/components/debts/DebtDetailDialog.test.ts, src/app/debts/actions.ts</files>
  <read_first>
    src/components/debts/forgiveClientError.ts
    src/components/debts/DebtDetailDialog.tsx
    src/app/debts/actions.ts
    src/components/accounts/AccountList.test.ts
    .planning/phases/10-repayments-close-write-off/10-REVIEW.md
  </read_first>
  <behavior>
    - Forgive handleConfirm uses resolveForgiveFailureMessage (includes deltaMajor) (G-10-8 / WR-01)
    - On forgive failure, do not call setConfirm(null) — DestructiveConfirmStep stays so actionError remains visible (G-10-8 / CR-01)
    - Forgive tabpanel also renders actionError with role=alert (G-10-8 / CR-01)
    - Optional: forgiveRemaining OVER_FLOOR / DELTA_ZERO returns message with same RU as errors.deltaMajor (or message alone) so message-based clients see it (G-10-8)
  </behavior>
  <action>
    Per D diagnosis / CR-01 / WR-01: in DebtDetailDialog forgive branch of handleConfirm, setActionError(resolveForgiveFailureMessage(result)) and return without setConfirm(null) on failure (keep confirm open). Import resolveForgiveFailureMessage from ./forgiveClientError. On forgive tabpanel (tab === "forgive"), render actionError the same way confirm steps and history already do (role=alert destructive text). Claude discretion: in forgiveRemaining catch for OVER_FLOOR and DELTA_ZERO, also set message to the same Russian string currently used in errors.deltaMajor (keep errors.deltaMajor or message-only — either OK if client chain covers both). Do not rewrite createRepayment/createSizeChange/delete* P2025 branches. Do not change WR-02 pendingLabel, WR-03 sort, or WR-04 deleteSizeChange. Author DebtDetailDialog.test.ts source contracts (AccountList.test.ts readFileSync pattern) asserting: resolveForgiveFailureMessage import/use; forgive failure path has no setConfirm(null) between forgiveRemaining await and return (delete paths may still clear confirm); forgive tabpanel contains actionError alert render. Titles include "(G-10-8)".
  </action>
  <verify>
    <automated>npx vitest run src/components/debts/forgiveClientError.test.ts src/components/debts/DebtDetailDialog.test.ts && grep -q 'resolveForgiveFailureMessage' src/components/debts/DebtDetailDialog.tsx && grep -q 'errors?.deltaMajor\|deltaMajor' src/components/debts/forgiveClientError.ts</automated>
    <fails_when>non-zero vitest exit, missing resolver wiring in dialog, or forgive failure still clears confirm before return</fails_when>
  </verify>
  <acceptance_criteria>
    - grep -q 'resolveForgiveFailureMessage' src/components/debts/DebtDetailDialog.tsx
    - npx vitest run src/components/debts/forgiveClientError.test.ts src/components/debts/DebtDetailDialog.test.ts exits 0
    - DebtDetailDialog.test.ts covers G-10-8 keep-confirm and forgive-tab actionError
  </acceptance_criteria>
  <done>Forgive failures show deltaMajor or refresh RU on open confirm and forgive tab; opaque never wins when those present; G-10-8 closable for client path</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Server Action result → DebtDetailDialog | Client must display server-authored RU; must not invent opaque when structured errors exist |
| Concurrent tabs → forgiveRemaining | Stale debtId / assert failures already mapped server-side; UI must surface them |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-10-G8-01 | Information Disclosure | forgiveClientError / handleConfirm | low | mitigate | Only show fixed RU from server fields; no stack traces |
| T-10-G8-02 | Tampering | stale forgive submit | medium | accept | Server already rejects; this plan only surfaces errors (10-04 maps P2025) |
| T-10-G8-03 | Denial of Service | repeated forgive fails | low | accept | Single-user local app; confirm stays open for correction |
| T-10-SC | Tampering | npm/pip/cargo installs | high | accept | No new packages in this plan |
</threat_model>

<verification>
npx vitest run src/components/debts/forgiveClientError.test.ts src/components/debts/DebtDetailDialog.test.ts green; dialog wires resolver; forgive failure retains confirm; forgive tab shows actionError; no P2025 catch rewrites outside optional forgiveRemaining message dual-map.
</verification>

<success_criteria>
G-10-8 closable for diagnosed client path: forgive shows deltaMajor / refresh RU (not opaque when those present); confirm stays on failure; forgive tab alerts; vitest proves opaque never wins for those inputs.
</success_criteria>

## Source Audit (gap_closure scope)

| SOURCE | ID | Item | Plan | Status |
|--------|-----|------|------|--------|
| UAT | G-10-8 | Forgive client surface refresh/deltaMajor; never opaque when server sends them | 10-05 | COVERED |
| DEBUG | H3/H4 | WR-01 deltaMajor ignore + CR-01 confirm clear / no tab alert | 10-05 | COVERED |
| DEBUG | H1/H2 | P2025 instanceof / 10-04 missing | — | EXCLUDED (rejected; do not reopen) |
| REVIEW | CR-01 / WR-01 | Keep confirm + deltaMajor + tab actionError | 10-05 | COVERED |
| REVIEW | WR-02..04 / IN-* | pendingLabel / sort / deleteSizeChange floor | — | EXCLUDED (out of G-10-8 scope) |
| GOAL | Phase 10 | Early forgive close UX (already shipped 01–03) | 01–03 | COVERED prior |
| REQ | REPAY-01/03, DEBT-05 | Concurrent failure UX for forgive path | 10-05 | COVERED |

<output>
Create `.planning/phases/10-repayments-close-write-off/10-05-SUMMARY.md` when done
</output>
