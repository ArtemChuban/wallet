---
phase: 04-dated-fx
plan: 03
type: execute
wave: 3
depends_on:
  - 04-02
files_modified:
  - src/app/currencies/actions.ts
  - src/app/currencies/actions.test.ts
  - src/app/currencies/rates/page.tsx
  - src/components/currencies/RateList.tsx
  - src/components/currencies/SetRateDialog.tsx
autonomous: false
requirements:
  - FX-01
  - FX-02
estimate:
  tokens: 20000
  raw_tokens: 40000
  tasks: 3
  confidence: high
must_haves:
  truths:
    - Rate history expands inline under the currency rate row (not Sheet/Dialog-only history) (D-10, D-12)
    - History ordered newest first by asOfDate descending (FX-01 ordering, Phase 3 D-14 mirror)
    - History rows show DD.MM.YYYY · rate PRIMARY за 1 CODE in storage direction (UI-SPEC)
    - Delete lives only on history rows with confirm «Удалить курс за {date}? Это нельзя отменить.» (D-13, UI-SPEC)
    - After last rate deleted, expand collapses and row reverts to «Нет курса» + «Задать первый курс» with no invented 0/1 (D-14, D-15, UI empty E4)
    - Expand/collapse aria-labels «Показать историю курсов» / «Скрыть историю курсов»; 44×44 tap target (UI-SPEC)
    - History panel uses muted background; delete uses destructive styling; delete label «Удалить курс» (UI-SPEC color)
    - User can overwrite a past rate by set-rate with that date and delete via history (FX-01 mutation, D-12)
    - Past rates remain readable via LOCF for earlier D after a newer rate is added (FX-02 forward-effective, success criterion 4)
  prohibitions:
    - statement: MUST NOT place delete controls inside SetRateDialog
      requirement_id: FX-01
      category: values
    - statement: MUST NOT invent zero or one after all rates for a currency are deleted
      requirement_id: FX-02
      category: values
    - statement: MUST NOT mark a history row as current/latest with a special badge
      requirement_id: FX-02
      category: values
    - statement: MUST NOT add amount convert/preview UI while polishing history
      requirement_id: FX-02
      category: values
  artifacts:
    - path: src/app/currencies/actions.ts
      provides: deleteFxRate Server Action
    - path: src/components/currencies/RateList.tsx
      provides: expand history UI + delete controls
    - path: src/app/currencies/rates/page.tsx
      provides: history rows serialized for each non-primary currency
  key_links:
    - from: history Удалить курс button
      to: deleteFxRate
      via: Server Action + confirm then revalidatePath currencies + rates
      pattern: "deleteFxRate"
    - from: rates/page.tsx history query
      to: RateList expand panel
      via: newest-first rate props as strings
      pattern: "asOfDate|rateToPrimaryScaled"
---

## Phase Goal

**As a** local Wallet user, **I want to** maintain dated exchange rates between the primary currency and other currencies, **so that** multi-currency amounts convert honestly as of any date (FX-01, FX-02).

<objective>
Complete history mutation UX: inline expandable newest-first rate list, deleteFxRate (history-only), then Russian UI human smoke on /currencies/rates.

Purpose: Finish FX-01 overwrite/delete and honest empty after last delete (FX-02 / D-15).
Output: Expandable history + delete + human-verified Russian chrome.
</objective>

<execution_context>
@/home/artem/Documents/wallet/.cursor/gsd-core/workflows/execute-plan.md
@/home/artem/Documents/wallet/.cursor/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/04-dated-fx/04-CONTEXT.md
@.planning/phases/04-dated-fx/04-UI-SPEC.md
@.planning/phases/04-dated-fx/04-PATTERNS.md
@.planning/phases/04-dated-fx/04-VALIDATION.md
@.planning/phases/04-dated-fx/04-02-PLAN.md
@src/app/currencies/actions.ts
@src/app/currencies/rates/page.tsx
@src/components/currencies/RateList.tsx
@src/components/currencies/SetRateDialog.tsx
@src/components/accounts/AccountList.tsx
</context>

<tasks>

<task type="tracer">
  <name>End-to-end expand history + delete one rate</name>
  <files>src/app/currencies/actions.ts, src/app/currencies/actions.test.ts, src/app/currencies/rates/page.tsx, src/components/currencies/RateList.tsx</files>
  <read_first>
    .planning/phases/04-dated-fx/04-CONTEXT.md
    .planning/phases/04-dated-fx/04-UI-SPEC.md
    .planning/phases/04-dated-fx/04-PATTERNS.md
    src/app/currencies/actions.ts
    src/app/currencies/rates/page.tsx
    src/components/currencies/RateList.tsx
    src/components/accounts/AccountList.tsx
    src/lib/validations/fx.ts
  </read_first>
  <behavior>
    - deleteFxRate validates id via deleteFxRateSchema; deletes row; revalidatePath("/currencies") and revalidatePath("/currencies/rates"); errors return «Не удалось удалить курс. Попробуйте снова.»
    - rates page loads per-currency histories ordered asOfDate desc; serialize rateToPrimaryScaled as string
    - RateList useState expand; ChevronRight/ChevronDown; history rows; delete only in history with window.confirm copy from UI-SPEC
  </behavior>
  <action>
    Per D-10, D-12, D-13 and 04-UI-SPEC history section: add the history-row delete Server Action on currencies actions (wire it only from expanded history controls — keep SetRateDialog free of delete wiring). Extend actions.test.ts for delete export + success/error paths and dual revalidatePath. Load newest-first histories in rates/page.tsx for non-primary currencies that have rates. Make RateList expand with per-row useState; show history only when expanded and history.length greater than 0; on zero rates after delete, no expand affordance and first-rate CTA returns. Confirm before delete using UI-SPEC string with DD.MM.YYYY date (confirm button wording «Удалить курс»). Disable delete control while pending. History row format: date · formatted rate PRIMARY за 1 CODE.
  </action>
  <verify>
    <automated>npm test -- --run src/app/currencies/actions.test.ts && grep -q 'deleteFxRate' src/app/currencies/actions.ts && grep -q 'Показать историю курсов\|ChevronRight\|ChevronDown' src/components/currencies/RateList.tsx && grep -q 'Удалить курс' src/components/currencies/RateList.tsx && ! grep -q 'deleteFxRate' src/components/currencies/SetRateDialog.tsx</automated>
    <fails_when>tests fail, deleteFxRate missing from actions, expand/delete UI absent from RateList, or SetRateDialog contains deleteFxRate symbol</fails_when>
  </verify>
  <acceptance_criteria>
    - deleteFxRate exported and tested
    - RateList has expand aria-label «Показать историю курсов» and «Удалить курс» in history region
    - SetRateDialog.tsx has no import/call of the delete Server Action (verify uses negative grep on that symbol name)
  </acceptance_criteria>
  <done>History expand + delete path works end-to-end on /currencies/rates</done>
</task>

<task type="auto">
  <name>History empty/overflow polish + full automated suite</name>
  <files>src/components/currencies/RateList.tsx, src/app/currencies/rates/page.tsx</files>
  <read_first>
    .planning/phases/04-dated-fx/04-UI-SPEC.md
    src/components/currencies/RateList.tsx
  </read_first>
  <action>
    Per UI Considerations: ensure zero-rate currencies have no expand control; after last delete UX collapses to «Нет курса» + «Задать первый курс»; history panel bg-muted/40; destructive styling on delete; page/main scroll for long history (no nested max-height trap required). Optional «История» section label. Geist Mono for rates/dates/codes. Run full npm test suite green.
  </action>
  <verify>
    <automated>npm test && grep -q 'bg-muted\|destructive\|Задать первый курс' src/components/currencies/RateList.tsx && grep -q 'Нет курса' src/components/currencies/RateList.tsx</automated>
    <fails_when>npm test non-zero, or RateList lacks muted/destructive/first-rate/empty-rate styling cues</fails_when>
  </verify>
  <acceptance_criteria>
    - npm test exits 0
    - RateList includes muted panel and first-rate / empty-rate CTA strings
  </acceptance_criteria>
  <done>History polish complete; full suite green</done>
</task>

<task type="checkpoint:human-verify" gate="blocking-human">
  <name>Russian UI smoke on /currencies/rates</name>
  <files>src/components/currencies/RateList.tsx, src/components/currencies/SetRateDialog.tsx, src/app/currencies/layout.tsx</files>
  <read_first>
    .planning/phases/04-dated-fx/04-UI-SPEC.md
    .planning/phases/04-dated-fx/04-VALIDATION.md
  </read_first>
  <what-built>Dated FX rates under Currencies — tabs «Валюты»|«Курсы», SetRateDialog with direction toggle, LOCF rows, expandable history with delete (D-01–D-17)</what-built>
  <how-to-verify>
    1. Ensure Plan 01 migrate deploy applied; start app (npm run dev or docker compose up)
    2. Nav «Валюты» opens /currencies/rates; tabs switch «Валюты» | «Курсы» (D-01–D-04)
    3. Empty non-primary: «Нет курса» → «Задать первый курс» → set past/today rate → row shows rate · на DD.MM.YYYY (D-14)
    4. Direction toggle default 1 OTHER = N PRIMARY; try alternate then confirm stored LOCF is primary-per-other (D-05, D-06)
    5. Same-date set overwrites; older date remains for earlier as-of (FX-02 forward-effective)
    6. Expand history newest first; delete one rate with confirm; after last delete CTA returns (no invented 0/1) (D-12, D-13)
    7. Future date and rate less than or equal to 0 rejected with Russian errors; no amount calculator; no top-level /fx
    8. Reply approved if chrome matches 04-UI-SPEC
  </how-to-verify>
  <action>
    Halt for human visual/locale confirmation of FX-01/FX-02 and 04-UI-SPEC after automated tests are green. Record approval in resume signal.
  </action>
  <resume-signal>Reply: approved RU UI rates on /currencies/rates (or list blocking UI defects)</resume-signal>
  <verify>
    <automated>npm test</automated>
    <fails_when>full suite exits non-zero before human review</fails_when>
    <human-check>
      <what-built>Russian set-rate Dialog, LOCF rows, tabs, history delete on /currencies/rates</what-built>
      <how-to-verify>Walk checklist in how-to-verify; confirm UI-SPEC Russian strings and no invented rate</how-to-verify>
    </human-check>
  </verify>
  <acceptance_criteria>
    - Human confirms Russian set-rate + history + tabs + LOCF behavior on /currencies/rates
    - npm test exits 0 at checkpoint time
  </acceptance_criteria>
  <done>Human-verified Phase 04 UI smoke passed</done>
</task>

</tasks>

## Artifacts this phase produces

| Symbol | Kind | File |
|--------|------|------|
| `deleteFxRate` | Server Action | `src/app/currencies/actions.ts` |
| RateList expand/history/delete UI | component behavior | `src/components/currencies/RateList.tsx` |
| per-currency rate history props | RSC serialization | `src/app/currencies/rates/page.tsx` |

(Plan 03 completes FX-01 mutation surface; LOCF helper from Plan 01 remains the FX-02 contract for Phases 5–6.)

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Browser FormData id → deleteFxRate | Untrusted snapshot id for delete |
| History UI → LOCF honesty | After delete, list must not invent rates |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-04-09 | Tampering | deleteFxRate id | medium | mitigate | deleteFxRateSchema positive int; Prisma delete by id only |
| T-04-10 | Tampering | post-delete LOCF display | high | mitigate | Null LOCF → «Нет курса»; never invent 0/1 (D-14, D-15) |
| T-04-11 | Repudiation | delete without confirm | low | mitigate | window.confirm with UI-SPEC Russian copy before delete |
| T-04-SC | Tampering | npm/pip/cargo installs | high | accept | No new packages this plan |
</threat_model>

<verification>
actions tests green; deleteFxRate present; RateList expand/delete chrome; SetRateDialog free of delete symbol; full npm test; human smoke.
</verification>

<success_criteria>
User can expand rate history, delete a dated rate, and recover honest empty/LOCF state; Russian UI smoke approved.
</success_criteria>

<output>
Create `.planning/phases/04-dated-fx/04-03-SUMMARY.md` when done
</output>
