---
phase: 04-dated-fx
plan: 02
type: execute
wave: 2
depends_on:
  - 04-01
files_modified:
  - src/app/currencies/actions.ts
  - src/app/currencies/actions.test.ts
  - src/app/currencies/layout.tsx
  - src/app/currencies/page.tsx
  - src/app/currencies/rates/page.tsx
  - src/components/currencies/RateList.tsx
  - src/components/currencies/SetRateDialog.tsx
  - src/components/currencies/SetRateDialog.test.ts
  - src/components/nav.tsx
autonomous: true
requirements:
  - FX-01
  - FX-02
estimate:
  tokens: 24000
  raw_tokens: 48000
  tasks: 3
  confidence: high
must_haves:
  truths:
    - User can set a dated primary↔other rate via Dialog on /currencies/rates and see LOCF rate plus producing as-of date on the row (FX-01, FX-02, D-05–D-10)
    - Currencies area has Link tabs «Валюты» | «Курсы» at /currencies and /currencies/rates; nav «Валюты» lands on /currencies/rates (D-01–D-04)
    - Future asOfDate is rejected server-side with «Дата не может быть в будущем» even if client max is bypassed (D-08, T-04-01)
    - Rate less than or equal to 0 rejected with «Курс должен быть больше 0»; primary currencyCode rejected with «Выберите валюту, отличную от основной» (D-09, D-16)
    - Same-date set overwrites via prisma.fxRate.upsert on currencyCode_asOfDate; always persists rateToPrimaryScaled (D-05, D-11)
    - Direction default on open is toPrimary («1 OTHER = N PRIMARY»); fromPrimary invert before write (D-05, D-06)
    - When LOCF is null, row shows «Нет курса» plus accent CTA «Задать первый курс» and never shows invented 0.00/1.00 (D-14, D-15, UI empty)
    - When LOCF exists, secondary CTA is «Задать курс»; dialog title «Задать курс»; submit «Сохранить курс» (UI-SPEC copy)
    - Other-currency Select lists all non-primary currencies even with zero accounts (D-07)
    - Zero non-primary currencies shows «Нет валют для курса» with next-step copy to Валюты tab (UI-SPEC)
    - BigInt rateToPrimaryScaled crosses RSC→client as string (Phase 02/03 pattern)
    - No amount convert/preview calculator on the rates tab (D-17)
    - statement: Direction toggle labels with codes stay on one line or wrap within dialog width without clipping tap targets
      verification: backstop
  prohibitions:
    - statement: MUST NOT add a top-level /fx nav item
      requirement_id: FX-01
      category: values
    - statement: MUST NOT put rate delete controls inside SetRateDialog
      requirement_id: FX-01
      category: values
    - statement: MUST NOT invent 0.00 or 1.00 when LOCF is null
      requirement_id: FX-02
      category: values
    - statement: MUST NOT show an amount convert/preview calculator on the rates tab
      requirement_id: FX-02
      category: values
    - statement: MUST NOT allow other↔other FX pairs or primary self-rate rows
      requirement_id: FX-01
      category: values
  artifacts:
    - path: src/app/currencies/actions.ts
      provides: upsertFxRate Server Action
    - path: src/components/currencies/SetRateDialog.tsx
      provides: Russian set-rate Dialog with direction toggle + rate + asOfDate
    - path: src/app/currencies/rates/page.tsx
      provides: RSC batch LOCF-as-of-today props for RateList
    - path: src/components/currencies/RateList.tsx
      provides: LOCF row display + first-rate CTA + SetRateDialog triggers
    - path: src/app/currencies/layout.tsx
      provides: «Валюты» | «Курсы» Link tabs
  key_links:
    - from: SetRateDialog
      to: upsertFxRate
      via: useActionState + FormData currencyCode/rateMajor/asOfDate/direction
      pattern: "upsertFxRate"
    - from: src/app/currencies/rates/page.tsx
      to: LOCF batch / calendarDateToday
      via: findMany asOfDate lte today + first-per-currencyCode
      pattern: "calendarDateToday"
    - from: upsertFxRate
      to: prisma.fxRate.upsert
      via: currencyCode_asOfDate compound unique
      pattern: "currencyCode_asOfDate"
    - from: src/components/nav.tsx
      to: /currencies/rates
      via: href change + /currencies prefix active
      pattern: "/currencies/rates"
---

## Phase Goal

**As a** local Wallet user, **I want to** maintain dated exchange rates between the primary currency and other currencies, **so that** multi-currency amounts convert honestly as of any date (FX-01, FX-02).

<objective>
Ship the set-rate vertical slice inside Currencies: tabs + nav rates-first landing, upsertFxRate Server Action, SetRateDialog with direction toggle, and current LOCF display per non-primary currency.

Purpose: User can write dated primary↔other rates (FX-01) and see correct as-of-today LOCF on the rates list (FX-02) without history delete yet.
Output: Working Dialog → upsert → LOCF row on /currencies/rates with tabs chrome.
</objective>

<execution_context>
@/home/artem/Documents/wallet/.cursor/gsd-core/workflows/execute-plan.md
@/home/artem/Documents/wallet/.cursor/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/04-dated-fx/04-CONTEXT.md
@.planning/phases/04-dated-fx/04-RESEARCH.md
@.planning/phases/04-dated-fx/04-PATTERNS.md
@.planning/phases/04-dated-fx/04-UI-SPEC.md
@.planning/phases/04-dated-fx/04-01-PLAN.md
@src/app/currencies/actions.ts
@src/app/currencies/page.tsx
@src/components/nav.tsx
@src/components/accounts/AccountList.tsx
@src/components/accounts/SetBalanceDialog.tsx
@src/lib/fx.ts
@src/lib/money.ts
@src/lib/validations/fx.ts
@src/lib/balances.ts
</context>

<assumptions>
## Planner assumptions

- A6: Link underline tabs in layout.tsx — no shadcn Tabs install.
- History expand/delete are Plan 03 (D-12, D-13).
- Direction toggle = two Button states (UI-SPEC inventory); no ToggleGroup package.
</assumptions>

<context_lock_waiver>
D-04 (/currencies vs /currencies/rates paths) and D-05 (always store rateToPrimaryScaled) already locked in 04-CONTEXT.md. Keep costly/one-way ratings where relevant; skip checkpoint:decision — do not re-ask.
</context_lock_waiver>

<tasks>

<task type="tracer">
  <name>End-to-end set rate via Dialog + LOCF on rates list + tabs/nav</name>
  <reversibility rating="costly">D-04 URL paths /currencies and /currencies/rates are the shareable nav/bookmark contract; changing them later needs redirects.</reversibility>
  <files>src/app/currencies/layout.tsx, src/app/currencies/rates/page.tsx, src/app/currencies/actions.ts, src/app/currencies/actions.test.ts, src/components/currencies/RateList.tsx, src/components/currencies/SetRateDialog.tsx, src/components/currencies/SetRateDialog.test.ts, src/components/nav.tsx</files>
  <read_first>
    .planning/phases/04-dated-fx/04-CONTEXT.md
    .planning/phases/04-dated-fx/04-UI-SPEC.md
    .planning/phases/04-dated-fx/04-PATTERNS.md
    src/app/accounts/actions.ts
    src/app/accounts/page.tsx
    src/components/accounts/SetBalanceDialog.tsx
    src/components/accounts/AccountList.tsx
    src/app/currencies/actions.ts
    src/components/nav.tsx
    src/lib/validations/fx.ts
    src/lib/fx.ts
    src/lib/money.ts
  </read_first>
  <behavior>
    - upsertFxRate Zod-parses FormData; rejects future asOfDate, rate less than or equal to 0n, isPrimary currency; direction fromPrimary runs invertRateScaled before persist; upsert on currencyCode_asOfDate; revalidatePath both /currencies and /currencies/rates
    - rates page: non-primary currencies; batch LOCF as of calendarDateToday Europe/Moscow; serialize rateToPrimaryScaled as string; empty «Нет курса» + «Задать первый курс»
    - SetRateDialog: useActionState, formKey remount, Select non-primary, direction default toPrimary, DD.MM.YYYY + hidden ISO, submit «Сохранить курс»
    - layout Link tabs «Валюты»|/currencies and «Курсы»|/currencies/rates; nav href /currencies/rates with active on /currencies prefix
  </behavior>
  <action>
    Per D-01–D-11, D-14–D-17 and 04-UI-SPEC: add currencies/layout.tsx with Link underline tabs (exact-path active so both tabs do not highlight together). Create rates/page.tsx force-dynamic RSC mirroring accounts page batch LOCF (findMany asOfDate lte today, first-per-currencyCode) plus empty zero-non-primary copy. Add upsertFxRate to currencies/actions.ts (ensureSqlitePragmas, setFxRateSchema, calendarDateToday future gate, load currency reject isPrimary, parseRateToScaled, optional invertRateScaled, rate greater than 0n check, compound upsert, dual revalidatePath). Extend actions.test.ts for future/primary/zero/invert/upsert/revalidate paths. Build SetRateDialog (mirror SetBalanceDialog remount + dates helpers) and RateList (one row per non-primary; LOCF display CODE · rate PRIMARY · на DD.MM.YYYY; first-rate accent CTA). Change nav.tsx currencies href to /currencies/rates; keep «Валюты» active for any pathname starting with /currencies. No history delete in this plan. No amount preview UI.
  </action>
  <verify>
    <automated>npm test -- --run src/app/currencies/actions.test.ts src/lib/fx.test.ts && test -f src/components/currencies/SetRateDialog.tsx && test -f src/components/currencies/RateList.tsx && test -f src/app/currencies/rates/page.tsx && test -f src/app/currencies/layout.tsx && grep -q 'upsertFxRate' src/app/currencies/actions.ts && grep -q 'Задать первый курс' src/components/currencies/RateList.tsx && grep -q 'Нет курса' src/components/currencies/RateList.tsx && grep -q '/currencies/rates' src/components/nav.tsx && grep -q 'Курсы' src/app/currencies/layout.tsx</automated>
    <fails_when>tests fail, SetRateDialog/RateList/rates page/layout missing, upsertFxRate absent, first-rate/empty copy missing, nav not pointing at rates path, or Курсы tab label absent from layout</fails_when>
  </verify>
  <acceptance_criteria>
    - upsertFxRate exported and covered by actions.test.ts
    - /currencies/rates RSC and layout tabs exist
    - RateList shows «Нет курса» / «Задать первый курс» empty path
    - nav currencies link targets /currencies/rates
  </acceptance_criteria>
  <done>Set-rate happy path works end-to-end on /currencies/rates with tabs and LOCF list</done>
</task>

<task type="auto" tdd="true">
  <name>Direction invert + pair/rate/date Server Action gates</name>
  <files>src/app/currencies/actions.ts, src/app/currencies/actions.test.ts, src/components/currencies/SetRateDialog.tsx, src/components/currencies/SetRateDialog.test.ts</files>
  <read_first>
    .planning/phases/04-dated-fx/04-UI-SPEC.md
    src/app/currencies/actions.ts
    src/components/accounts/SetBalanceDialog.tsx
    src/lib/money.ts
  </read_first>
  <behavior>
    - fromPrimary direction stores inverted rateToPrimaryScaled only (never a direction column)
    - primary currencyCode returns field error «Выберите валюту, отличную от основной»
    - rateMajor parsing to less than or equal to 0n or invert result less than or equal to 0n returns «Курс должен быть больше 0»
    - asOfDate after calendarDateToday returns «Дата не может быть в будущем»
    - Dialog direction labels use selected OTHER + primary codes; default toPrimary selected on open
  </behavior>
  <action>
    Per D-05, D-06, D-08, D-09, D-16: harden upsertFxRate tests and implementation for invert path, primary rejection, positivity, and future date (Russian copy from UI-SPEC). In SetRateDialog, wire direction toggle Buttons with labels `1 {OTHER} = N {PRIMARY}` / `1 {PRIMARY} = N {OTHER}` (placeholders until Select chosen); hidden direction FormData field; rate Input inputMode decimal; isPending disables «Сохранить курс»; form-level «Не удалось сохранить. Проверьте поля и попробуйте снова.» Catch-all. Prefer Select required before rate entry. Do not add delete UI to the dialog.
  </action>
  <verify>
    <automated>npm test -- --run src/app/currencies/actions.test.ts src/components/currencies/SetRateDialog.test.ts src/lib/validations/fx.test.ts && grep -q 'Курс должен быть больше 0\|Дата не может быть в будущем\|Выберите валюту, отличную от основной' src/app/currencies/actions.ts && grep -q 'toPrimary\|fromPrimary\|Направление' src/components/currencies/SetRateDialog.tsx</automated>
    <fails_when>tests fail, Russian gate strings absent from actions.ts, or direction toggle wiring absent from SetRateDialog</fails_when>
  </verify>
  <acceptance_criteria>
    - actions.test.ts covers invert, primary reject, zero rate, future date
    - SetRateDialog has direction control and Направление label
    - Server Action emits UI-SPEC Russian field errors for those gates
  </acceptance_criteria>
  <done>Direction invert and validation gates covered; dialog chrome matches UI-SPEC</done>
</task>

<task type="auto">
  <name>Set-rate pending UX + rates empty/populated polish</name>
  <files>src/components/currencies/SetRateDialog.tsx, src/components/currencies/RateList.tsx, src/app/currencies/rates/page.tsx</files>
  <read_first>
    .planning/phases/04-dated-fx/04-UI-SPEC.md
    src/components/currencies/SetRateDialog.tsx
    src/components/currencies/RateList.tsx
    src/components/accounts/AccountList.tsx
  </read_first>
  <action>
    Per UI Considerations loading/populated/overflow: keep Dialog open until success then close; disable primary submit while isPending; populated LOCF focal pattern CODE · formatted rate · на DD.MM.YYYY with Geist Mono; secondary «Задать курс» when rates exist; page/main scroll (no nested max-height trap); zero-non-primary empty state copy from UI-SPEC. Serialize BigInt as strings only. Run targeted currency/fx tests green.
  </action>
  <verify>
    <automated>grep -q 'isPending\|Сохранить курс' src/components/currencies/SetRateDialog.tsx && grep -q 'Задать курс\|Задать первый курс\|Нет курса' src/components/currencies/RateList.tsx && npm test -- --run src/app/currencies/actions.test.ts src/components/currencies/SetRateDialog.test.ts src/lib/fx.test.ts</automated>
    <fails_when>isPending/submit copy missing, RateList CTA/empty strings missing, or listed npm test run non-zero</fails_when>
  </verify>
  <acceptance_criteria>
    - SetRateDialog uses isPending on submit button labeled «Сохранить курс»
    - RateList includes empty and secondary set-rate CTA strings
    - Targeted vitest run exits 0
  </acceptance_criteria>
  <done>Pending UX and rates list polish complete for Plan 02 slice</done>
</task>

</tasks>

## Artifacts this phase produces

| Symbol | Kind | File |
|--------|------|------|
| `upsertFxRate` | Server Action | `src/app/currencies/actions.ts` |
| `FxRateActionState` | type | `src/app/currencies/actions.ts` |
| `SetRateDialog` | component | `src/components/currencies/SetRateDialog.tsx` |
| `RateList` | component | `src/components/currencies/RateList.tsx` |
| `RateListItem` | type | `src/components/currencies/RateList.tsx` |
| currencies `layout` | route layout | `src/app/currencies/layout.tsx` |
| rates `page` | RSC page | `src/app/currencies/rates/page.tsx` |

(Plan 02 subset; Plan 03 adds delete + history symbols.)

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Browser FormData → upsertFxRate | Untrusted currencyCode, rateMajor, asOfDate, direction |
| RSC props → RateList client | Serialized rate strings must not invent missing LOCF |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-04-01 | Tampering | upsertFxRate gates | high | mitigate | Server Zod + future date + rate greater than 0n + isPrimary reject (D-08, D-09, D-16) |
| T-04-06 | Tampering | direction field | high | mitigate | Direction UX-only; always write rateToPrimaryScaled after optional invertRateScaled (D-05) |
| T-04-07 | Tampering | mass assignment | medium | mitigate | setFxRateSchema.strict(); ignore unknown FormData keys |
| T-04-08 | Spoofing | currency codes in UI | low | accept | React text escaping; codes constrained at create (Phase 02) |
| T-04-SC | Tampering | npm/pip/cargo installs | high | accept | No new packages — Link tabs / existing Button only |
</threat_model>

<verification>
actions + SetRateDialog tests green; rates page/layout/nav present; empty LOCF shows «Нет курса»; upsertFxRate wired.
</verification>

<success_criteria>
User can open /currencies/rates, set a dated primary↔other rate, and see today's LOCF on the list without invented rates.
</success_criteria>

<output>
Create `.planning/phases/04-dated-fx/04-02-SUMMARY.md` when done
</output>
