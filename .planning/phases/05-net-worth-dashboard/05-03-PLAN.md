---
phase: 05-net-worth-dashboard
plan: 03
type: execute
wave: 3
depends_on:
  - 05-02
files_modified:
  - src/app/page.tsx
  - src/components/dashboard/DashboardAccountList.tsx
autonomous: false
requirements:
  - NW-01
  - NW-02
  - NW-03
  - ACCT-03
estimate:
  tokens: 15000
  raw_tokens: 30000
  tasks: 3
  confidence: high
must_haves:
  truths:
    - Zero accounts omits hero block and shows empty state «Нет счетов» plus CTA «Перейти к счетам» linking /accounts (D-12, E1)
    - When accounts exist but none includable, hero shows formatted zero or partial sum per computeNetWorthRows with partial warning if rows present (E2)
    - DB or prisma fetch failure renders «Не удалось загрузить данные. Проверьте базу данных и обновите страницу.» inside standard main shell without separate readiness route (D-02, E6)
    - Nav on / shows active «Главная» with order Главная · Валюты · Счета (D-03, D-04)
    - RSC await means no client skeleton; page streams when data ready (E4)
    - Dashboard supports zero, one, and many accounts in flat list (E8 zero-one-many)
  prohibitions:
    - statement: MUST NOT retain standalone readiness page as primary / UX
      requirement_id: NW-01
      category: values
    - statement: MUST NOT redirect DB errors to a different route
      requirement_id: NW-01
      category: values
  artifacts:
    - path: src/components/dashboard/DashboardAccountList.tsx
      provides: empty state with Button asChild Link
    - path: src/app/page.tsx
      provides: try/catch DB error surface and empty-account branch
  key_links:
    - from: DashboardAccountList
      to: /accounts
      via: empty-state Button asChild Link
      pattern: "Перейти к счетам"
    - from: src/app/page.tsx
      to: prisma batch fetch
      via: try/catch with Russian error render
      pattern: "Не удалось загрузить данные"
---

## Phase Goal

**As a** local Wallet user, **I want to** see true current net worth and per-account balances in native and primary currency, **so that** I know my capital at a glance (NW-01, NW-02, NW-03, ACCT-03).

<objective>
Ship empty-state CTA, inline DB error handling, and human-verify Russian dashboard smoke to close Phase 5 UI contract.

Purpose: Cover D-02 and D-12 edge states and validate end-to-end dashboard UX.
Output: Production-ready / dashboard with empty and error paths plus human sign-off.
</objective>

<execution_context>
@/home/artem/Documents/wallet/.cursor/gsd-core/workflows/execute-plan.md
@/home/artem/Documents/wallet/.cursor/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/05-net-worth-dashboard/05-UI-SPEC.md
@.planning/phases/05-net-worth-dashboard/05-CONTEXT.md
@.planning/phases/05-net-worth-dashboard/05-VALIDATION.md
@.planning/phases/05-net-worth-dashboard/05-02-PLAN.md
@src/app/page.tsx
@src/components/dashboard/DashboardAccountList.tsx
@src/components/currencies/RateList.tsx
</context>

<tasks>

<task type="auto">
  <name>Empty accounts state — no hero, CTA to /accounts</name>
  <files>src/components/dashboard/DashboardAccountList.tsx, src/app/page.tsx</files>
  <read_first>
    .planning/phases/05-net-worth-dashboard/05-UI-SPEC.md
    .planning/phases/05-net-worth-dashboard/05-CONTEXT.md
    src/components/dashboard/DashboardAccountList.tsx
    src/components/currencies/RateList.tsx
    src/app/page.tsx
  </read_first>
  <action>
    Per D-12: when accounts array length is zero, page.tsx must not render hero «Капитал» block. DashboardAccountList returns empty state with heading «Нет счетов», body «Создайте первый счёт, чтобы увидеть капитал.», and Button default variant asChild wrapping Link href /accounts labeled «Перейти к счетам» — mirror RateList empty CTA pattern. When accounts length greater than zero, keep hero and list from Plans 01–02.
  </action>
  <verify>
    <automated>grep -q 'Нет счетов' src/components/dashboard/DashboardAccountList.tsx && grep -q 'Перейти к счетам' src/components/dashboard/DashboardAccountList.tsx && grep -q 'accounts.length' src/app/page.tsx</automated>
    <fails_when>empty-state copy or CTA missing, or page always renders hero regardless of account count</fails_when>
  </verify>
  <acceptance_criteria>
    - Empty state heading and body match UI-SPEC
    - CTA links to /accounts
    - Hero omitted when no accounts
  </acceptance_criteria>
  <done>Zero-account dashboard guides user to create first account (D-12)</done>
</task>

<task type="auto">
  <name>Inline DB fetch error — Russian message in page shell</name>
  <files>src/app/page.tsx</files>
  <read_first>
    .planning/phases/05-net-worth-dashboard/05-UI-SPEC.md
    .planning/phases/05-net-worth-dashboard/05-CONTEXT.md
    src/app/page.tsx
  </read_first>
  <action>
    Per D-02: wrap dashboard prisma batch fetch in try/catch. On failure render main with mx-auto max-w-3xl gap-8 px-4 py-8 shell and body copy «Не удалось загрузить данные. Проверьте базу данных и обновите страницу.» — do not redirect and do not restore standalone readiness page. Remove or stop using getDbReadiness as primary UX if still referenced. Keep export const dynamic force-dynamic. Do not leak stack traces to UI.
  </action>
  <verify>
    <automated>grep -q 'Не удалось загрузить данные' src/app/page.tsx && grep -q 'try' src/app/page.tsx && grep -q 'force-dynamic' src/app/page.tsx && ! grep -q 'Кошелёк готов' src/app/page.tsx</automated>
    <fails_when>DB error copy missing, try/catch absent, readiness stub heading remains, or dynamic export removed</fails_when>
  </verify>
  <acceptance_criteria>
    - page.tsx contains try/catch around data load
    - Russian DB error string matches UI-SPEC
    - Readiness stub heading «Кошелёк готов» removed
  </acceptance_criteria>
  <done>Database unavailable shows inline Russian error on / (D-02)</done>
</task>

<task type="checkpoint:human-verify" gate="blocking-human">
  <name>Russian dashboard UI smoke on /</name>
  <files>src/app/page.tsx, src/components/nav.tsx, src/components/dashboard/DashboardAccountList.tsx</files>
  <read_first>
    .planning/phases/05-net-worth-dashboard/05-UI-SPEC.md
    .planning/phases/05-net-worth-dashboard/05-VALIDATION.md
    .planning/phases/05-net-worth-dashboard/05-CONTEXT.md
  </read_first>
  <what-built>Net-worth dashboard at / — hero «Капитал», read-only account list, partial-warning callout, empty state, nav Главная-first (D-01–D-16, NW-01–03, ACCT-03)</what-built>
  <how-to-verify>
    1. Ensure Plans 01–02 complete; npm test green; start app (npm run dev or docker compose up)
    2. Nav order «Главная» · «Валюты» · «Счета»; «Главная» active on / (D-03, D-04)
    3. Hero shows «Капитал» + amount + PRIMARY code only; no as-of subtitle (D-05–D-08)
    4. Asset row: native amount CODE + converted PRIMARY column (NW-02, NW-03)
    5. Credit row: native доступно + долг; primary debt only — available never as asset (D-15, ACCT-03)
    6. Account missing balance: «нет баланса» / «—»; missing FX: «— · нет курса» (D-09, D-10)
    7. When any account excluded: «Итог неполный» banner between hero and list (D-11)
    8. Zero accounts: no hero; «Нет счетов» + «Перейти к счетам» → /accounts (D-12)
    9. Optional: break DATABASE_URL → inline «Не удалось загрузить данные…» (D-02)
    10. Reply approved if chrome matches 05-UI-SPEC
  </how-to-verify>
  <action>
    Halt for human visual confirmation of NW-01–03, ACCT-03, and 05-UI-SPEC after automated tests are green. Record approval in resume signal.
  </action>
  <resume-signal>Reply: approved RU UI dashboard on / (or list blocking UI defects)</resume-signal>
  <verify>
    <automated>npm test</automated>
    <fails_when>full suite exits non-zero before human review</fails_when>
    <human-check>
      <what-built>Russian net-worth dashboard at / with hero, list, partial warning, empty state</what-built>
      <how-to-verify>Walk checklist in how-to-verify; confirm UI-SPEC Russian strings and credit debt-only primary column</how-to-verify>
    </human-check>
  </verify>
  <acceptance_criteria>
    - Human confirms Russian dashboard chrome on /
    - npm test exits 0 at checkpoint time
  </acceptance_criteria>
  <done>Human-verify PASS recorded for Phase 5 dashboard chrome</done>
</task>

</tasks>

## Artifacts this phase produces

| Symbol | Kind | File |
|--------|------|------|
| empty state block | UI | `src/components/dashboard/DashboardAccountList.tsx` |
| DB error surface | RSC branch | `src/app/page.tsx` |

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| DB unavailable → user | Error message must not expose connection strings or stack |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-05-07 | Information disclosure | catch block UI | medium | mitigate | Fixed Russian user copy; log details server-side only |
| T-05-08 | Denial of service | prisma fetch | low | accept | Local single-user app; inline error allows refresh retry |
</threat_model>

<verification>
npm test full suite green; empty state and DB error copy present; human-verify checkpoint PASS for Russian / dashboard.
</verification>

<success_criteria>
Phase 5 dashboard complete: / replaces readiness, all NW requirements satisfied, human-approved Russian UI.
</success_criteria>

<output>
Create `.planning/phases/05-net-worth-dashboard/05-03-SUMMARY.md` when done
</output>
