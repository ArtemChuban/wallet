---
phase: 05-net-worth-dashboard
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/lib/net-worth.ts
  - src/lib/net-worth.test.ts
  - src/app/page.tsx
  - src/components/nav.tsx
  - src/components/dashboard/DashboardAccountList.tsx
autonomous: true
requirements:
  - NW-01
  - NW-02
  - NW-03
estimate:
  tokens: 24000
  raw_tokens: 48000
  tasks: 3
  confidence: high
must_haves:
  truths:
    - computeNetWorthRows returns signed totalPrimaryMinor and per-row contributionPrimaryMinor for primary-currency FIAT_DEBIT LOCF (NW-01)
    - Hero on / shows Label «Капитал» plus Display amount and PRIMARY code only with no as-of subtitle (D-05, D-06, D-07, D-08, E7)
    - Nav order is «Главная» · «Валюты» · «Счета» with «Готовность» removed (D-01, D-03, D-04, E5)
    - DashboardAccountList is flat read-only bordered ul with name plus native plus primary columns; no expand or set-balance actions (D-13, D-14, D-16, E8)
    - Asset row native column shows formatted LOCF amount and CODE when snapshot exists (NW-02, E8)
    - Non-primary asset primary column uses convertOtherMinorToPrimaryMinor when LOCF FX exists (NW-03)
    - Primary-currency accounts convert via identity without FxRate row (RESEARCH pitfall 3, NW-03)
    - Page batch-loads balance snapshots and FX rates with asOfDate lte calendarDateToday Europe/Moscow; no N+1 getBalanceAsOf loops (E4)
    - statement: Long account names truncate with ellipsis and title attribute on dashboard rows
      verification: backstop
  prohibitions:
    - statement: MUST NOT sum creditLimitMinor or available credit into hero total
      requirement_id: ACCT-03
      category: values
    - statement: MUST NOT treat null LOCF or null FX as zero in contributionPrimaryMinor
      requirement_id: NW-01
      category: values
    - statement: MUST NOT add schema migrations or new npm packages in Phase 5
      requirement_id: NW-01
      category: safety
  artifacts:
    - path: src/lib/net-worth.ts
      provides: computeNetWorthRows pure aggregation for Phase 6 reuse
    - path: src/lib/net-worth.test.ts
      provides: unit tests for NW math matrix
    - path: src/app/page.tsx
      provides: NW dashboard RSC replacing readiness stub
    - path: src/components/dashboard/DashboardAccountList.tsx
      provides: read-only three-column account list
    - path: src/components/nav.tsx
      provides: Главная-first nav per D-04
  key_links:
    - from: src/app/page.tsx
      to: computeNetWorthRows
      via: NetWorthAccountInput built from batch LOCF maps
      pattern: "computeNetWorthRows"
    - from: src/lib/net-worth.ts
      to: convertOtherMinorToPrimaryMinor
      via: non-primary asset primary column and contribution
      pattern: "convertOtherMinorToPrimaryMinor"
    - from: src/app/page.tsx
      to: prisma balanceSnapshot and fxRate
      via: findMany asOfDate lte today first-wins maps
      pattern: "locfByAccount|locfByCurrency"
---

## Phase Goal

**As a** local Wallet user, **I want to** see true current net worth and per-account balances in native and primary currency, **so that** I know my capital at a glance (NW-01, NW-02, NW-03).

<objective>
Tracer slice: pure net-worth aggregation, replace / readiness stub with dashboard RSC, update nav, and prove primary plus non-primary asset rows end-to-end with hero «Капитал».

Purpose: Lock NW math and landing route before credit, partial, and empty-state chrome (Plans 02–03).
Output: src/lib/net-worth.ts, green net-worth.test.ts matrix, / dashboard with hero and asset rows.
</objective>

<execution_context>
@/home/artem/Documents/wallet/.cursor/gsd-core/workflows/execute-plan.md
@/home/artem/Documents/wallet/.cursor/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/05-net-worth-dashboard/05-CONTEXT.md
@.planning/phases/05-net-worth-dashboard/05-RESEARCH.md
@.planning/phases/05-net-worth-dashboard/05-PATTERNS.md
@.planning/phases/05-net-worth-dashboard/05-VALIDATION.md
@.planning/phases/05-net-worth-dashboard/05-UI-SPEC.md
@src/lib/balances.ts
@src/lib/fx.ts
@src/lib/money.ts
@src/app/accounts/page.tsx
@src/app/currencies/rates/page.tsx
@src/app/page.tsx
@src/components/nav.tsx
</context>

<context_lock_waiver>
D-01 (replace / with dashboard) and D-03/D-04 (nav labels/order) locked in 05-CONTEXT.md. D-01 rated costly — flag on tracer task; no checkpoint:decision re-ask.
</context_lock_waiver>

<tasks>

<task type="tracer">
  <name>End-to-end «Капитал» on / — primary asset row path</name>
  <reversibility rating="costly">D-01 makes / the default NW landing; reverting splits home vs dashboard again.</reversibility>
  <files>src/lib/net-worth.ts, src/lib/net-worth.test.ts, src/app/page.tsx, src/components/nav.tsx, src/components/dashboard/DashboardAccountList.tsx</files>
  <read_first>
    .planning/phases/05-net-worth-dashboard/05-CONTEXT.md
    .planning/phases/05-net-worth-dashboard/05-RESEARCH.md
    .planning/phases/05-net-worth-dashboard/05-PATTERNS.md
    .planning/phases/05-net-worth-dashboard/05-UI-SPEC.md
    src/lib/balances.ts
    src/lib/fx.ts
    src/lib/money.ts
    src/app/accounts/page.tsx
    src/app/currencies/rates/page.tsx
    src/app/page.tsx
    src/components/nav.tsx
  </read_first>
  <behavior>
    - computeNetWorthRows sums primary FIAT_DEBIT locfAmountMinor with positive contributionPrimaryMinor and isPartial false for single included row
    - page.tsx batch-fetches accounts, primary currency, snapshots lte today, rates lte today; builds locfByAccount and locfByCurrency maps per accounts page pattern
    - Hero renders «Капитал» label and formatMinorToMajor total plus primary code; force-dynamic preserved
    - nav links array is Главная /, Валюты /currencies/rates, Счета /accounts; Готовность entry gone
    - DashboardAccountList renders at least one primary asset row with name and native and primary columns; read-only ul chrome
  </behavior>
  <action>
    Per D-01, D-03, D-04, D-05, D-06, D-07, D-08, D-13, D-14, D-16: create src/lib/net-worth.ts exporting NetWorthAccountInput, NetWorthRow, and computeNetWorthRows with inclusion rules from RESEARCH (null locf → excludeReason no_balance; primary isPrimaryCurrency → identity; asset types → positive contribution). Add src/lib/net-worth.test.ts with at least primary FIAT_DEBIT sum case so tracer verify is green. Replace src/app/page.tsx readiness UI with dashboard Server Component: ensureSqlitePragmas, calendarDateToday Europe/Moscow, Promise.all batch queries mirroring accounts and rates pages, map to computeNetWorthRows inputs, format hero on server. Create src/components/dashboard/DashboardAccountList.tsx as Server Component accepting pre-formatted row props (name, nativeDisplay, primaryDisplay strings) for asset rows only in this task — no client actions. Update src/components/nav.tsx links per UI-SPEC. Omit partial warning, empty state, credit rows, and DB try/catch until Plans 02–03. No prisma schema changes.
  </action>
  <verify>
    <automated>npx vitest run src/lib/net-worth.test.ts -t "sums asset" && test -f src/lib/net-worth.ts && test -f src/components/dashboard/DashboardAccountList.tsx && grep -q 'computeNetWorthRows' src/app/page.tsx && grep -q 'Капитал' src/app/page.tsx && grep -q 'Главная' src/components/nav.tsx && ! grep -q 'Готовность' src/components/nav.tsx</automated>
    <fails_when>net-worth tests fail, lib or DashboardAccountList missing, page lacks computeNetWorthRows or Капитал, nav lacks Главная, or Готовность still present in nav links</fails_when>
  </verify>
  <acceptance_criteria>
    - computeNetWorthRows exported from src/lib/net-worth.ts with primary asset test green
    - page.tsx imports computeNetWorthRows and renders hero «Капитал»
    - nav.tsx first link is Главная href /
    - DashboardAccountList.tsx exists with bordered read-only list markup
  </acceptance_criteria>
  <done>Single primary asset path works from batch LOCF through hero and one dashboard row on /</done>
</task>

<task type="auto" tdd="true">
  <name>Wave 0 net-worth.test.ts full matrix</name>
  <files>src/lib/net-worth.ts, src/lib/net-worth.test.ts</files>
  <read_first>
    .planning/phases/05-net-worth-dashboard/05-VALIDATION.md
    .planning/phases/05-net-worth-dashboard/05-RESEARCH.md
    src/lib/net-worth.ts
    src/lib/fx.test.ts
    src/lib/balances.test.ts
  </read_first>
  <behavior>
    - subtracts credit debt only; available not in contributionPrimaryMinor (NW-01, ACCT-03)
    - excludes non-primary asset without FX with excludeReason no_fx and isPartial true (D-10)
    - excludes account without LOCF with excludeReason no_balance (D-09)
    - primary currency identity without rateToPrimaryScaled (NW-03)
    - mixed portfolio total equals sum of included contributions only
    - nativeDisplayMinor and primaryDisplayMinor fields populated per account type semantics for downstream UI
  </behavior>
  <action>
    Extend src/lib/net-worth.test.ts per 05-VALIDATION Wave 0: cases named or filtered by subtracts credit debt, excludes, primary currency, nativeDisplay, no_fx, and mixed portfolio. Cover FIAT_CREDIT debt negation, CRYPTO non-primary with FX, null LOCF, null FX, and isPartial when any row has includedInTotal false. Implement any missing branches in computeNetWorthRows using creditDebtMinor and convertOtherMinorToPrimaryMinor — no Prisma imports. Keep pure BigInt math only.
  </action>
  <verify>
    <automated>npx vitest run src/lib/net-worth.test.ts && npm test -- --run src/lib/fx.test.ts src/lib/balances.test.ts</automated>
    <fails_when>net-worth.test.ts exits non-zero, or fx/balances regression tests fail</fails_when>
  </verify>
  <acceptance_criteria>
    - net-worth.test.ts covers credit debt, no_balance, no_fx, primary identity, and isPartial
    - Full net-worth test file exits 0
    - FX and balances regression suites remain green
  </acceptance_criteria>
  <done>NW-01–03 and ACCT-03 aggregation rules unit-tested without UI</done>
</task>

<task type="auto">
  <name>Non-primary asset rows and flat multi-account list</name>
  <files>src/app/page.tsx, src/components/dashboard/DashboardAccountList.tsx</files>
  <read_first>
    .planning/phases/05-net-worth-dashboard/05-UI-SPEC.md
    .planning/phases/05-net-worth-dashboard/05-PATTERNS.md
    src/app/page.tsx
    src/components/dashboard/DashboardAccountList.tsx
    src/lib/net-worth.ts
    src/lib/money.ts
  </read_first>
  <action>
    Per D-13, D-14, NW-02, NW-03: extend page.tsx to pass all accounts ordered by name into computeNetWorthRows; format each included asset row native column as amount CODE via formatMinorToMajor; format primary column as converted amount PRIMARY when non-primary has LOCF FX; primary-currency rows show same numeric amount in both columns. Extend DashboardAccountList to map all accounts in flat ul with three-column layout matching AccountList row density (px-4 py-3, font-mono amounts, truncate name with title). Add visually hidden or Label header row for Счёт, В валюте счёта, В {PRIMARY} per UI-SPEC a11y. Do not implement credit-specific copy, partial banner, empty state, or missing-state hints in this task — Plan 02.
  </action>
  <verify>
    <automated>npm test -- --run src/lib/net-worth.test.ts && grep -q 'DashboardAccountList' src/app/page.tsx && grep -q 'В валюте счёта\|sr-only\|Счёт' src/components/dashboard/DashboardAccountList.tsx && grep -q 'locfByCurrency\|fxRate.findMany' src/app/page.tsx</automated>
    <fails_when>tests fail, page does not render DashboardAccountList, column a11y labels absent, or page lacks batch FX fetch</fails_when>
  </verify>
  <acceptance_criteria>
    - page.tsx batch-loads FX rates and wires locfByCurrency map
    - DashboardAccountList renders multiple asset rows with native and primary columns
    - List column headers present for a11y
  </acceptance_criteria>
  <done>Multi-account flat asset list with non-primary conversion visible on /</done>
</task>

</tasks>

## Artifacts this phase produces

| Symbol | Kind | File |
|--------|------|------|
| `NetWorthAccountInput` | type | `src/lib/net-worth.ts` |
| `NetWorthRow` | type | `src/lib/net-worth.ts` |
| `computeNetWorthRows` | pure function | `src/lib/net-worth.ts` |
| `DashboardAccountList` | Server Component | `src/components/dashboard/DashboardAccountList.tsx` |
| home `page` | RSC page | `src/app/page.tsx` |
| `Nav` links config | client component | `src/components/nav.tsx` |

(Plan 01 subset; Plans 02–03 add credit, partial, empty, and error chrome.)

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| SQLite read → page.tsx RSC | Untrusted DB content rendered as escaped text |
| net-worth.ts pure input | Caller must supply honest LOCF maps; lib must not invent zero |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-05-01 | Tampering | computeNetWorthRows | high | mitigate | Pure BigInt; null LOCF/FX → exclude not zero; unit tests lock sign rules |
| T-05-02 | Tampering | credit available in total | high | mitigate | contribution uses creditDebtMinor only; tests assert available never sums (Plan 01 tests + Plan 02 UI) |
| T-05-03 | Information disclosure | page error surface | low | accept | Generic Russian copy deferred to Plan 03; no stack in UI |
| T-05-SC | Tampering | npm installs | high | accept | No new packages — composition only per RESEARCH Package Legitimacy Audit |
</threat_model>

<verification>
npx vitest run src/lib/net-worth.test.ts green; / renders «Капитал»; nav shows Главная first without Готовность; asset rows show native and primary columns.
</verification>

<success_criteria>
User opening / sees hero net worth for includable primary and non-primary asset accounts with read-only flat list; NW math covered by unit tests.
</success_criteria>

<output>
Create `.planning/phases/05-net-worth-dashboard/05-01-SUMMARY.md` when done
</output>
