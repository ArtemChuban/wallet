---
phase: 08-debts-schema-domain-math
plan: 03
type: execute
wave: 2
depends_on:
  - "08-01"
files_modified:
  - src/lib/validations/debts.ts
  - src/lib/validations/debts.test.ts
autonomous: true
requirements:
  - DEBT-03
  - DISOL-01
estimate:
  tokens: 12000
  raw_tokens: 24000
  tasks: 2
  confidence: high
must_haves:
  truths:
    - createDebtSchema / related Zod shapes accept positive initial major and direction enum; there is no schema that updates initialAmountMinor (DEBT-03 / D-03 readiness for Phase 9)
    - asOfDate fields use YYYY-MM-DD regex with Russian invalid-date message matching balance validations
    - repayment and size-change Zod shapes require asOfDate and amount/delta majors; domain remaining rules stay in debts.ts asserts not Zod
    - Full npm test suite remains green including DISOL-01 scan from Plan 01
  prohibitions:
    - statement: MUST NOT add an update-initial Zod schema that mutates create-time principal
      requirement_id: DEBT-03
      category: safety
    - statement: MUST NOT put remaining/over-repay business rules inside Zod (shape only)
      requirement_id: DEBT-02
      category: values
  artifacts:
    - path: src/lib/validations/debts.ts
      provides: person/debt/repayment/size-change Zod contracts for Phase 9–10
    - path: src/lib/validations/debts.test.ts
      provides: accept/reject cases with Russian messages
  key_links:
    - from: src/lib/validations/debts.ts
      to: src/lib/validations/balance.ts
      via: asOfDateSchema regex + strict object pattern
      pattern: "asOfDate"
    - from: validations shape
      to: src/lib/debts.ts asserts
      via: Zod parses majors/dates; domain asserts enforce remaining (Phase 10 actions)
      pattern: "shape only"
---

<objective>
Ship thin Phase-9-ready Zod contracts under `src/lib/validations/debts.ts` (person name, debt create/edit shape without initial mutation, repayment/size-change asOfDate + majors) and prove the full Vitest suite including DISOL-01 stays green.

Purpose: Ready Server Action input shapes without implementing UI; keep domain math in debts.ts.
Output: validations/debts.ts + tests; full `npm test` green.
</objective>

<execution_context>
@/home/artem/Documents/wallet/.cursor/gsd-core/workflows/execute-plan.md
@/home/artem/Documents/wallet/.cursor/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/08-debts-schema-domain-math/08-CONTEXT.md
@.planning/phases/08-debts-schema-domain-math/08-PATTERNS.md
@.planning/phases/08-debts-schema-domain-math/08-RESEARCH.md
@src/lib/validations/balance.ts
@src/lib/validations/account.ts
@src/lib/validations/balance.test.ts
@src/lib/debts.ts
</context>

## Artifacts this phase produces

- `src/lib/validations/debts.ts`: `createPersonSchema`, `renamePersonSchema`, `createDebtSchema`, `updateDebtMetaSchema` (direction/dueDate/note/currency as appropriate — not initial amount), `createRepaymentSchema`, `createSizeChangeSchema` (names discretionary if exports cover create person/debt + event shapes)
- `src/lib/validations/debts.test.ts`
- No UI routes or nav changes

<tasks>

<task type="auto" tdd="true">
  <name>Wave 0 red Zod tests for debts validations</name>
  <files>src/lib/validations/debts.test.ts</files>
  <read_first>
    .planning/phases/08-debts-schema-domain-math/08-PATTERNS.md
    src/lib/validations/balance.test.ts
    src/lib/validations/account.ts
    src/lib/validations/balance.ts
  </read_first>
  <behavior>
    - createPersonSchema accepts trimmed non-empty name; rejects empty
    - createDebtSchema accepts direction I_OWE|THEY_OWE, currencyCode, positive initial major, optional dueDate YYYY-MM-DD and note
    - createDebtSchema rejects non-positive initial major
    - updateDebtMetaSchema accepts note/dueDate/direction edits but has no initialAmount/initialMajor field
    - createRepaymentSchema / createSizeChangeSchema require asOfDate YYYY-MM-DD; malformed date yields Russian «Укажите дату»
  </behavior>
  <action>
    Author src/lib/validations/debts.test.ts following balance.test.ts patterns: safeParse accept/reject cases for person name, debt create with positive initial major and direction enum, update meta without any initial-amount field (DEBT-03 / D-03), repayment and size-change asOfDate regex with message Укажите дату. Leave production module unimplemented so tests fail until Task 2 — or skip failing run if using file-existence verify only for this task. Prefer writing failing expects against named schema exports that Task 2 will create.
  </action>
  <verify>
    <automated>test -f src/lib/validations/debts.test.ts && grep -q 'createDebtSchema\|asOfDate\|I_OWE' src/lib/validations/debts.test.ts && grep -q 'Укажите дату' src/lib/validations/debts.test.ts</automated>
    <fails_when>debts.test.ts missing, or createDebt/asOfDate/direction/Russian date message coverage absent</fails_when>
  </verify>
  <acceptance_criteria>
    - src/lib/validations/debts.test.ts exists with person/debt/event Zod cases
    - Cases assert no initial-amount field on update-meta schema export name
    - Invalid asOfDate expects «Укажите дату»
  </acceptance_criteria>
  <done>Zod Wave 0 test file authored</done>
</task>

<task type="auto" tdd="true">
  <name>Implement validations/debts.ts and green full suite</name>
  <files>src/lib/validations/debts.ts, src/lib/validations/debts.test.ts</files>
  <read_first>
    src/lib/validations/balance.ts
    src/lib/validations/account.ts
    src/lib/validations/debts.test.ts
    src/lib/debts.ts
    .planning/phases/08-debts-schema-domain-math/08-CONTEXT.md
  </read_first>
  <behavior>
    - All Task 1 Zod cases pass
    - Schemas use .strict() objects
    - Shape only — no remaining/over-repay checks in Zod
  </behavior>
  <action>
    Implement src/lib/validations/debts.ts mirroring balance.ts asOfDateSchema and account.ts enum/positive-major helpers. Export createPersonSchema, renamePersonSchema, createDebtSchema (positive initial major required), updateDebtMetaSchema without initial amount fields (DEBT-03), createRepaymentSchema (positive amount major + asOfDate + optional note), createSizeChangeSchema (non-zero delta major as string sign-aware or separate direction — prefer signed major string parsed later by actions; Zod rejects empty; domain assertSizeDelta still owns zero-delta reject). Russian messages for date/amount empties like existing validations. Do not add UI. Do not import from net-worth or page. After green debts validation tests, run full npm test to confirm DISOL-01 and prior suite still pass.
  </action>
  <verify>
    <automated>npx vitest run src/lib/validations/debts.test.ts src/lib/debts.test.ts && npm test && test -f src/lib/validations/debts.ts && grep -q 'createDebtSchema' src/lib/validations/debts.ts && grep -q 'updateDebtMetaSchema' src/lib/validations/debts.ts</automated>
    <fails_when>non-zero vitest or npm test exit, validations module missing, or create/update debt schema exports absent</fails_when>
  </verify>
  <acceptance_criteria>
    - src/lib/validations/debts.ts exports person/debt/event schemas listed in artifacts
    - updateDebtMetaSchema has no initial major/amount key
    - npx vitest run src/lib/validations/debts.test.ts exits 0
    - npm test exits 0 (includes DISOL-01)
  </acceptance_criteria>
  <done>Zod contracts ready for Phase 9; full suite green with isolation intact</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| HTTP/FormData → Zod | Untrusted strings coerced before BigInt parse in later actions |
| Zod → domain asserts | Shape vs remaining rules must stay split |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-08-01 | Tampering | createRepaymentSchema / createSizeChangeSchema | medium | mitigate | Positive/non-empty majors at shape layer; debts.ts asserts remain authoritative for remaining |
| T-08-07 | Tampering | updateDebtMetaSchema | medium | mitigate | Omit initial amount fields (DEBT-03) |
| T-08-04 | Tampering | validations importing NW | high | mitigate | validations stay isolated; npm test keeps DISOL-01 green |
| T-08-05 | Tampering | Injection | low | accept | Zod string limits only; Prisma parameterized writes in later phases |
| T-08-SC | Tampering | npm installs | high | mitigate | No new packages |
</threat_model>

<verification>
Validation unit tests green; full npm test green including DISOL-01.
</verification>

<success_criteria>
Phase-9-ready Zod shapes exist without initial-mutation path; suite green; no UI added.
</success_criteria>

<output>
Create `.planning/phases/08-debts-schema-domain-math/08-03-SUMMARY.md` when done
</output>
