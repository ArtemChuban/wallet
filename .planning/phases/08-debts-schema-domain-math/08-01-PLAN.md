---
phase: 08-debts-schema-domain-math
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - prisma/schema.prisma
  - prisma/migrations/
  - src/lib/debts.ts
  - src/lib/debts.test.ts
  - src/lib/foundation.test.ts
autonomous: false
requirements:
  - DEBT-02
  - DISOL-01
estimate:
  tokens: 21000
  raw_tokens: 42000
  tasks: 3
  confidence: high
must_haves:
  truths:
    - remainingMinor equals initialAmountMinor plus sum of size deltas minus sum of repayment amounts (CONTEXT D-04; DEBT-02 override)
    - remainingMinor of 0n maps to CLOSED and remainingMinor greater than 0n maps to OPEN via statusForRemaining (D-12, D-13)
    - remainingMinor never returns a negative value from the happy-path sum helpers when inputs already satisfy D-15 (callers use asserts before apply)
    - Person, Debt, DebtRepayment, and DebtSizeChange tables exist after migrate deploy
    - net-worth.ts, historical-series.ts, and app/page.tsx contain no debt-module imports (DISOL-01 / D-24)
    - Pure remaining helpers are idempotent: same bigint inputs yield the same bigint output on repeated calls
    - Money fields use BigInt minors only — no Float money columns on debt models
  prohibitions:
    - statement: MUST NOT import the debts domain module from net-worth.ts, historical-series.ts, or app/page.tsx
      requirement_id: DISOL-01
      category: isolation
    - statement: MUST NOT add a forgive-amount column on Debt or a special WRITE_OFF repayment kind
      requirement_id: DEBT-02
      category: values
    - statement: MUST NOT store closedAt on Debt
      requirement_id: DEBT-02
      category: values
    - statement: MUST NOT put @@unique on (debtId, asOfDate) for repayment or size-change models
      requirement_id: DEBT-02
      category: safety
  artifacts:
    - path: prisma/schema.prisma
      provides: Person, Debt, DebtRepayment, DebtSizeChange, DebtDirection, DebtStatus; Currency.debts
    - path: prisma/migrations
      provides: debts_schema migration SQL with Restrict/Cascade FKs
    - path: src/lib/debts.ts
      provides: currentPrincipalMinor, remainingMinor, statusForRemaining
    - path: src/lib/debts.test.ts
      provides: DEBT-02 remaining cases, status sync smoke, DISOL-01 source scan
  key_links:
    - from: src/lib/debts.ts
      to: CONTEXT D-04
      via: currentPrincipalMinor and remainingMinor pure sums
      pattern: "remainingMinor"
    - from: prisma/schema.prisma
      to: Person / Debt / event FKs
      via: onDelete Restrict Person→Debt and Currency→Debt; Cascade Debt→events
      pattern: "onDelete"
    - from: src/lib/debts.test.ts
      to: src/lib/net-worth.ts
      via: DISOL-01 readFileSync isolation gate
      pattern: "DISOL-01"
---

## Phase Goal

**As a** local Wallet user, **I want** people/debts ledger primitives and remaining math locked in pure helpers with tests, **so that** later CRUD/repayment UI can trust principal and remaining without touching net worth.

<objective>
Confirm one-way ledger schema decisions (D-01, D-02), then ship Person/Debt/event models plus migration and the thin remaining/status tracer in `src/lib/debts.ts` with Vitest — including DISOL-01 isolation scan.

Purpose: Prove size-change ledger remaining (DEBT-02 CONTEXT override) and isolation before assert/totals expansion.
Output: Migrated schema, `src/lib/debts.ts` core exports, green tracer tests, host migrate deploy.
</objective>

<execution_context>
@/home/artem/Documents/wallet/.cursor/gsd-core/workflows/execute-plan.md
@/home/artem/Documents/wallet/.cursor/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/08-debts-schema-domain-math/08-CONTEXT.md
@.planning/phases/08-debts-schema-domain-math/08-RESEARCH.md
@.planning/phases/08-debts-schema-domain-math/08-PATTERNS.md
@.planning/phases/08-debts-schema-domain-math/08-VALIDATION.md
@prisma/schema.prisma
@src/lib/net-worth.ts
@src/lib/money.ts
@src/lib/foundation.test.ts
@docker/entrypoint.sh
</context>

<assumptions>
## Planner assumptions (specless / discretion)

- A1: Model name `DebtSizeChange` (signed `deltaMinor`) — RESEARCH discretion.
- A2: Repayment `amountMinor` must be strictly greater than 0n (enforced in Plan 02 asserts; schema stores BigInt only).
- A3: `Person.name` is `@unique` (Account.name analog).
- A4: Phase 8 remaining/status use order-independent sums only; both event models get `createdAt DateTime @default(now())` for Phase 11 ordering (D-10 / RESEARCH pitfall 3). Cross-table comparable insert `id` is not required this phase.
- A5: `debts.ts` stays pure — no Prisma imports (RESEARCH A5).
- Concurrency (unresolved probe): SQLite single-writer; pure helpers give deterministic results for identical inputs; no multi-writer transaction guarantees beyond existing DB lock behavior.
- Edge DEBT-03 boundary/precision/idempotency probes: remaining >= 0 enforced via Plan 02 asserts; BigInt minors avoid float precision loss; helpers are pure/idempotent on same inputs (must_haves truths).
- A7 (Docker migrate-on-start deferral): ROADMAP/CONTEXT success criterion “migrate on Docker start” has **no dedicated Docker compose smoke** in this plan. Equivalent gate = existing `docker/entrypoint.sh` already runs `prisma migrate deploy` on container start **plus** Task 3 host `DATABASE_URL=file:./data/wallet.db npx prisma migrate deploy`. Full Docker smoke optional/manual only (see 08-VALIDATION.md Manual-Only).
</assumptions>

## Artifacts this phase produces

- `Person` / `Debt` / `DebtRepayment` / `DebtSizeChange` Prisma models
- `DebtDirection` (`I_OWE` | `THEY_OWE`) and `DebtStatus` (`OPEN` | `CLOSED`) enums
- Migration under `prisma/migrations/` named with `debts_schema`
- `src/lib/debts.ts`: `currentPrincipalMinor`, `remainingMinor`, `statusForRemaining` (this plan); later plans add asserts + `computeDebtPrimaryTotals`
- `src/lib/debts.test.ts`: remaining/status/DISOL cases (expanded in later plans)
- `Currency.debts` relation back-reference

<tasks>

<task type="checkpoint:decision" gate="blocking">
  <name>Confirm one-way size-change ledger schema (D-01, D-02)</name>
  <decision>Lock Debt remaining model: no forgive-amount column on Debt; two event tables (repayments + signed size changes) per CONTEXT D-01 and D-02</decision>
  <context>
    CONTEXT already recorded D-01/D-02 as one-way. Undoing after migrate means a new migration plus rewrite of helpers/tests and every later debts phase. ROADMAP/REQUIREMENTS still mention the old forgive-field formula — CONTEXT overrides them. Confirm before schema lands.
  </context>
  <options>
    <option id="size-change-two-tables">
      <name>Size-change ledger + two event tables (CONTEXT D-01/D-02)</name>
      <pros>Matches locked discuss-phase decisions; auditable size history; initial principal stays visible; early close = size-change down</pros>
      <cons>Two tables; Phase 11 must order mixed events carefully (createdAt / future seq)</cons>
    </option>
    <option id="writeoff-field">
      <name>Debt forgive-amount field (stale REQUIREMENTS wording)</name>
      <pros>Matches unupdated REQUIREMENTS/ROADMAP text literally</pros>
      <cons>Contradicts locked CONTEXT; weaker audit trail for size ups/downs</cons>
    </option>
    <option id="unified-event-table">
      <name>Single polymorphic DebtEvent table</name>
      <pros>One id sequence for cross-kind order</pros>
      <cons>Contradicts locked D-02; larger schema redesign if CONTEXT stays authoritative</cons>
    </option>
  </options>
  <resume-signal>Select: size-change-two-tables (recommended), writeoff-field, or unified-event-table</resume-signal>
</task>

<task type="tracer">
  <name>End-to-end debts schema + remainingMinor tracer + DISOL scan</name>
  <reversibility rating="one-way">D-01/D-02 schema + remaining formula become on-disk SQLite shape; undoing needs migration and helper/test rewrites across Phases 9–11.</reversibility>
  <files>prisma/schema.prisma, prisma/migrations/, src/lib/debts.ts, src/lib/debts.test.ts</files>
  <read_first>
    .planning/phases/08-debts-schema-domain-math/08-CONTEXT.md
    .planning/phases/08-debts-schema-domain-math/08-RESEARCH.md
    .planning/phases/08-debts-schema-domain-math/08-PATTERNS.md
    prisma/schema.prisma
    src/lib/net-worth.ts
    src/lib/money.ts
    src/lib/foundation.test.ts
    src/lib/net-worth.test.ts
  </read_first>
  <behavior>
    - currentPrincipalMinor(initial, deltas) = initial + Σ deltas
    - remainingMinor(initial, deltas, repayments) = currentPrincipal − Σ repayments (D-04 / DEBT-02)
    - statusForRemaining(0n) = CLOSED; statusForRemaining(positive) = OPEN; negative throws
    - schema: Person, Debt (initialAmountMinor BigInt, status enum, direction enum, optional dueDate/note), DebtRepayment, DebtSizeChange with deltaMinor; FK Restrict/Cascade per D-20–D-22; no unique on event (debtId, asOfDate)
    - DISOL-01: isolation targets do not match debt-module import patterns
  </behavior>
  <action>
    After checkpoint selects size-change-two-tables (per D-01, D-02): extend prisma/schema.prisma with enums DebtDirection (I_OWE, THEY_OWE) and DebtStatus (OPEN, CLOSED). Add Person (id, name @unique, timestamps, debts). Add Debt with personId Restrict (D-20), currencyCode Restrict (D-22), direction, initialAmountMinor BigInt (create-time principal per D-03), optional dueDate/note strings, status default OPEN, timestamps, repayments and sizeChanges relations — no forgive-amount column and no closedAt (D-01, D-11). Add DebtRepayment and DebtSizeChange with debtId onDelete Cascade (D-21), asOfDate YYYY-MM-DD string with backdating allowed (D-07), amountMinor or signed deltaMinor, optional nullable note on both event models (D-06), createdAt default now (A4). Phase 8 remaining uses order-independent sums only; do not merge-sort by id across event kinds for remaining (D-10 deferred to Phase 11 ordering via createdAt). Do not add @@unique on (debtId, asOfDate) (D-08, D-09). Add debts Debt[] on Currency. Generate migration non-interactively named debts_schema (migrate dev --create-only or equivalent); SQL must use ON DELETE RESTRICT for Person/Currency→Debt and ON DELETE CASCADE for Debt→events (D-20, D-21, D-22). Implement src/lib/debts.ts pure exports currentPrincipalMinor, remainingMinor, statusForRemaining per RESEARCH Pattern 1–2 / D-04 / D-11–D-13 — BigInt only, no Prisma. Author src/lib/debts.test.ts covering remaining formula cases (initial only; with deltas; with repayments; early-close style down-delta to zero), status sync smoke, schema file greps for initialAmountMinor/deltaMinor/DebtSizeChange and for Restrict/Cascade wording, plus DISOL-01 readFileSync scan of net-worth.ts, historical-series.ts, and app/page.tsx rejecting debt-module import patterns from RESEARCH (D-24). Do not implement assert* or computeDebtPrimaryTotals here (Plan 02). Do not build chart series (D-23). Do not import debts into NW modules.
  </action>
  <verify>
    <automated>npx vitest run src/lib/debts.test.ts && grep -q 'model Person' prisma/schema.prisma && grep -q 'model Debt' prisma/schema.prisma && grep -q 'model DebtRepayment' prisma/schema.prisma && grep -q 'model DebtSizeChange' prisma/schema.prisma && grep -q 'currentPrincipalMinor' src/lib/debts.ts && grep -q 'remainingMinor' src/lib/debts.ts && grep -q 'statusForRemaining' src/lib/debts.ts && grep -q 'DISOL-01' src/lib/debts.test.ts</automated>
    <fails_when>non-zero vitest exit, missing Person/Debt/event models, missing remaining/status exports, or DISOL-01 describe absent</fails_when>
  </verify>
  <acceptance_criteria>
    - prisma/schema.prisma defines Person, Debt, DebtRepayment, DebtSizeChange with D-20 D-21 D-22 onDelete semantics and no event (debtId, asOfDate) unique
    - Event models include optional note (D-06) and asOfDate (D-07); remaining helpers do not cross-sort by id (D-10 sums-only)
    - Debt uses status DebtStatus OPEN|CLOSED and size adjustments via DebtSizeChange.deltaMinor only
    - src/lib/debts.ts exports currentPrincipalMinor, remainingMinor, statusForRemaining
    - src/lib/debts.test.ts covers D-04 remaining cases and DISOL-01 isolation scan
    - npx vitest run src/lib/debts.test.ts exits 0
  </acceptance_criteria>
  <done>Tracer path green: schema + remaining/status helpers + DISOL scan; migrate deploy is next task</done>
</task>

<task type="auto">
  <name>Host prisma migrate deploy + foundation table allow-list</name>
  <files>data/wallet.db, prisma/migrations/, src/lib/foundation.test.ts</files>
  <read_first>
    .planning/phases/08-debts-schema-domain-math/08-RESEARCH.md
    prisma/schema.prisma
    src/lib/foundation.test.ts
    docker/entrypoint.sh
  </read_first>
  <precondition>prisma/migrations contains the new debts_schema migration.sql from the tracer task</precondition>
  <action>
    If host SQLite is locked by wallet-web, stop the container briefly (Phase 02 lesson). Run DATABASE_URL=file:./data/wallet.db npx prisma migrate deploy (not db push). Confirm migrate status reports up to date. Update src/lib/foundation.test.ts sqlite_master IN-list and expected sorted table array to include Person, Debt, DebtRepayment, and DebtSizeChange alongside existing Currency/Account/FxRate/BalanceSnapshot/_prisma_migrations. Assert applied migrations include a name containing debts_schema. Do not COPY wallet.db into Docker images.
  </action>
  <verify>
    <automated>mkdir -p data && DATABASE_URL="file:./data/wallet.db" npx prisma migrate deploy && test -f data/wallet.db && DATABASE_URL="file:./data/wallet.db" npx prisma migrate status 2>&1 | grep -qi 'up to date\|Database schema is up to date\|No pending migrations' && npx vitest run src/lib/foundation.test.ts</automated>
    <fails_when>migrate deploy non-zero, data/wallet.db missing, pending migrations remain, or foundation.test.ts fails Person/Debt table expectations</fails_when>
  </verify>
  <acceptance_criteria>
    - DATABASE_URL=file:./data/wallet.db npx prisma migrate deploy exits 0
    - foundation.test.ts expects Person, Debt, DebtRepayment, DebtSizeChange tables and debts_schema migration name
    - prisma migrate status reports no pending migrations
  </acceptance_criteria>
  <done>Blocking host migrate deploy succeeded; foundation proves debt tables live</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Schema/migration → SQLite file | On-disk ledger shape; FK Restrict/Cascade must match CONTEXT |
| Pure debts helpers → callers | Untrusted event amounts cross into remaining math (asserts Plan 02) |
| Debts module → NW modules | Isolation boundary; coupling would corrupt capital semantics |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-08-01 | Tampering | remainingMinor / event amounts | high | mitigate | BigInt-only fields; Plan 02 domain asserts reject remaining less than 0 and zero deltas |
| T-08-02 | Tampering | Person→Debt delete | high | mitigate | onDelete Restrict (D-20) in schema + migration SQL |
| T-08-03 | Tampering | Debt→events orphan/wipe | medium | mitigate | onDelete Cascade only on intentional debt delete (D-21); Person cannot cascade-wipe via Restrict |
| T-08-04 | Tampering | net-worth.ts / historical-series.ts / page.tsx | high | mitigate | DISOL-01 Vitest source-scan; no debt imports in NW paths |
| T-08-05 | Tampering | Raw SQL injection | low | accept | No new raw SQL this plan; Prisma Client only; existing PRAGMA helpers unchanged |
| T-08-SC | Tampering | npm installs | high | mitigate | No new packages this phase — skip install checkpoint |
</threat_model>

<verification>
npx vitest run src/lib/debts.test.ts and src/lib/foundation.test.ts green; migrate deploy up to date; schema models present.
</verification>

<success_criteria>
Person/Debt/event tables migrate on host; remainingMinor implements D-04; DISOL-01 scan passes; no NW coupling.
Docker migrate-on-start deferred: covered equivalently by docker/entrypoint.sh + host migrate deploy (A7) — no required compose smoke.
</success_criteria>

<output>
Create `.planning/phases/08-debts-schema-domain-math/08-01-SUMMARY.md` when done
</output>
