---
phase: 15-plan-vs-actual-overdue
plan: 03
type: execute
wave: 3
depends_on:
  - "15-02"
files_modified:
  - src/app/globals.css
  - src/components/income/IncomeFactDialog.tsx
  - src/components/income/IncomeList.tsx
  - src/components/income/income-ui.test.ts
  - src/app/income/page.tsx
autonomous: true
requirements:
  - ACT-02
  - ACT-03
estimate:
  tokens: 30000
  raw_tokens: 60000
  tasks: 3
  confidence: high
must_haves:
  truths:
    - "isIncomeOverdue true → list shows «заполни» badge + primary «Заполни» CTA with warning amber chrome not destructive (ACT-02, D-08, D-09)"
    - "plannedAsOf >= today without actual → outline «Внести факт» and no «заполни» badge (ACT-02, D-10)"
    - "Dedicated IncomeFactDialog opens from row CTAs — not via definition «Изменить» and not DebtDetail-style sheet (D-01, D-20)"
    - "Fact form: actual amount + actualAsOf required, note optional; RO plan date + plan amount; first-fill defaults amount=plan and actualAsOf=Moscow today (D-02, D-03, D-16)"
    - "Edit mode «Изменить факт»; delete actual via DestructiveConfirmStep with UI-SPEC Russian copy (D-04, D-17)"
    - "Filled one-time row shows «получено» + plan + actual + Δ + больше/меньше/как плана; next-open without actual shows no Δ numbers (ACT-03, D-12, D-15, D-18)"
    - "Fact dialog live Отклонение preview updates while typing using incomeVarianceMinor/phrase (ACT-03, D-14, D-16)"
    - "Recurring after fill advances to new next open — no filled-month residual list row (D-07)"
    - "globals.css defines --warning / --warning-foreground + @theme aliases used by заполни chip (D-09)"
  prohibitions:
    - statement: MUST NOT mount chart/Recharts components on /income this phase
      requirement_id: ACT-03
      category: absence
    - statement: MUST NOT style overdue chrome with destructive tokens
      requirement_id: ACT-02
      category: safety
    - statement: MUST NOT use browser native confirm for delete actual
      requirement_id: ACT-01
      category: safety
    - statement: MUST NOT add multi-slot history expand/chevron or past-recurring-edit feed
      requirement_id: ACT-03
      category: absence
    - statement: MUST NOT install new npm packages including Badge registry
      requirement_id: ACT-02
      category: safety
    - statement: MUST NOT add nav overdue badge
      requirement_id: ACT-02
      category: absence
  artifacts:
    - path: src/app/globals.css
      provides: --warning / --warning-foreground theme tokens
    - path: src/components/income/IncomeFactDialog.tsx
      provides: dedicated fact create/edit/delete dialog
    - path: src/components/income/IncomeList.tsx
      provides: overdue badge + fact CTAs + one-time variance chrome
    - path: src/components/income/income-ui.test.ts
      provides: file-scan for заполни / CTA labels / DestructiveConfirmStep / no native confirm
  key_links:
    - from: IncomeList overdue gate
      to: IncomeFactDialog trigger labels
      via: Заполни | Внести факт | Изменить факт (D-17)
      pattern: "Заполни"
    - from: IncomeFactDialog
      to: upsert/delete actual actions
      via: useActionState + DestructiveConfirmStep
      pattern: "IncomeFactDialog"
    - from: one-time hasActual row
      to: incomeVarianceMinor + phrase
      via: inline plan/actual/Δ chrome (D-18)
      pattern: "получено"
---

## Specless probe fallback

| Req | Category | Acceptance |
|-----|----------|------------|
| ACT-02 | state | Overdue → «заполни» + warning chrome; calm → «Внести факт» without badge |
| ACT-03 | state | Filled one-time shows plan+actual+Δ copy; unfilled next-open no Δ; no chart dependency added |

<objective>
Ship IncomeFactDialog + list overdue/variance chrome + warning tokens so users fill facts and see «заполни» + inline Δ on «Доходы» (D-01, D-08..D-10, D-12, D-15..D-18, ACT-02, ACT-03).

Purpose: Complete phase UX on top of Plan 01–02 actions and props.
Output: Fact dialog, list chrome, CSS warning tokens, income-ui tests green, full suite gate.
</objective>

<execution_context>
@/home/artem/Documents/wallet/.cursor/gsd-core/workflows/execute-plan.md
@/home/artem/Documents/wallet/.cursor/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/15-plan-vs-actual-overdue/15-CONTEXT.md
@.planning/phases/15-plan-vs-actual-overdue/15-RESEARCH.md
@.planning/phases/15-plan-vs-actual-overdue/15-PATTERNS.md
@.planning/phases/15-plan-vs-actual-overdue/15-UI-SPEC.md
@.planning/phases/15-plan-vs-actual-overdue/15-VALIDATION.md
@src/components/income/IncomeFormDialog.tsx
@src/components/income/IncomeList.tsx
@src/components/income/income-ui.test.ts
@src/components/debts/DebtDetailDialog.tsx
@src/components/ui/destructive-confirm-step.tsx
@src/app/globals.css
@src/app/income/page.tsx
@src/lib/income.ts
@src/lib/dates.ts
@src/lib/money.ts
</context>

<tasks>

<task type="auto" reversibility="costly">
  <name>Warning tokens + IncomeFactDialog (D-01, D-02, D-03, D-16)</name>
  <files>src/app/globals.css, src/components/income/IncomeFactDialog.tsx</files>
  <read_first>
    .planning/phases/15-plan-vs-actual-overdue/15-UI-SPEC.md
    .planning/phases/15-plan-vs-actual-overdue/15-PATTERNS.md
    src/app/globals.css
    src/components/income/IncomeFormDialog.tsx
    src/components/debts/DebtDetailDialog.tsx
    src/components/ui/destructive-confirm-step.tsx
    src/app/income/actions.ts
    src/lib/income.ts
    src/lib/dates.ts
    src/lib/money.ts
  </read_first>
  <behavior>
    - globals.css :root + .dark + @theme define --warning / --warning-foreground and --color-warning* aliases (D-09, UI-SPEC)
    - IncomeFactDialog is separate client dialog from IncomeFormDialog (D-01)
    - Fields: RO plan date/amount; actual amount + actualAsOf required; note optional (D-02, D-16)
    - First-fill defaults: amount = plan major; actualAsOf = calendarDateToday Europe/Moscow — never plan date (D-03)
    - Live Отклонение uses incomeVarianceMinor + incomeVariancePhrase while typing (ACT-03)
    - Edit prefill from actual; delete via DestructiveConfirmStep with UI-SPEC delete-fact copy (D-04, D-17)
    - Submit labels: overdue create Заполни; calm create Внести факт; edit Сохранить факт (D-17, UI-SPEC)
  </behavior>
  <action>
    Add warning CSS variables per UI-SPEC Color section beside existing destructive tokens (D-09). Create IncomeFactDialog.tsx following IncomeFormDialog shell (Dialog remount on open, useActionState, success → router.refresh) and DebtDetail repay field stack for amount/date/note — but dedicated fact surface (D-01, costly reversibility). Wire upsertRecurringIncomeActual / upsertOneTimeIncomeActual / delete* by income.kind. Hidden plannedAsOf = slot nextPlannedAsOf. RO labels «План · дата» / «План · сумма»; editable «Факт · сумма» / «Факт · дата» / «Заметка»; live «Отклонение» (D-02, D-16, ACT-03). Defaults per D-03. Delete confirm message exact UI-SPEC Copywriting Contract. Do not fold into IncomeFormDialog. Do not mount chart components. Zero new packages (span chip later uses bg-warning/15). Omit past-recurring-edit affordance.
  </action>
  <verify>
    <automated>test -f src/components/income/IncomeFactDialog.tsx &amp;&amp; grep -q 'DestructiveConfirmStep' src/components/income/IncomeFactDialog.tsx &amp;&amp; grep -q 'calendarDateToday' src/components/income/IncomeFactDialog.tsx &amp;&amp; grep -q 'incomeVarianceMinor\|incomeVariancePhrase' src/components/income/IncomeFactDialog.tsx &amp;&amp; grep -q '--warning' src/app/globals.css &amp;&amp; grep -q 'План · дата' src/components/income/IncomeFactDialog.tsx</automated>
    <fails_when>FactDialog missing, warning tokens absent, or RO plan / variance / DestructiveConfirmStep missing</fails_when>
  </verify>
  <acceptance_criteria>
    - IncomeFactDialog exists as separate component (D-01)
    - First-fill uses Moscow today for actualAsOf (D-03)
    - Delete uses DestructiveConfirmStep only (D-04)
    - --warning tokens present in globals.css (D-09)
  </acceptance_criteria>
  <done>Fact dialog + warning tokens ready to wire into list.</done>
</task>

<task type="auto">
  <name>IncomeList overdue + variance chrome + fact CTAs (ACT-02, ACT-03)</name>
  <files>src/components/income/IncomeList.tsx, src/app/income/page.tsx</files>
  <read_first>
    src/components/income/IncomeList.tsx
    src/components/income/IncomeFactDialog.tsx
    src/app/income/page.tsx
    .planning/phases/15-plan-vs-actual-overdue/15-UI-SPEC.md
    .planning/phases/15-plan-vs-actual-overdue/15-CONTEXT.md
  </read_first>
  <behavior>
    - Overdue: badge text заполни with warning chip classes + primary Button Заполни (D-08, D-09, ACT-02)
    - Calm unfilled: outline Внести факт; no badge (D-10)
    - hasActual one-time: статус получено + plan/actual/Δ + phrase + Изменить факт (D-12, D-15, D-18, ACT-03)
    - Unfilled next-open: no Δ numbers (D-15)
    - Definition Изменить remains beside fact CTAs (D-20)
    - Pass today if needed; flex-wrap row action cluster per UI-SPEC
  </behavior>
  <action>
    Extend IncomeCompactRow: compute display from row.overdue / row.hasActual props from page (ACT-02, ACT-03). Add warning chip span with classes from UI-SPEC (bg-warning/15 text-warning-foreground) for overdue only — never destructive classes on badge (D-08, D-09). Wire IncomeFactDialog triggers with labels Заполни / Внести факт / Изменить факт (D-17). One-time filled: show получено + inline plan/actual/Δ via incomeVarianceMinor/phrase in source currency (D-12, D-13, D-14, D-18). Keep IncomeFormDialog «Изменить» (D-20). Do not add multi-slot expand. Do not change sort (D-11 already on page). Ensure page props already include fields from Plan 02; only touch page if prop threading gaps remain.
  </action>
  <verify>
    <automated>npx vitest run src/components/income/income-ui.test.ts src/lib/income.test.ts &amp;&amp; grep -q 'заполни' src/components/income/IncomeList.tsx &amp;&amp; grep -q 'Заполни' src/components/income/IncomeList.tsx &amp;&amp; grep -q 'Внести факт' src/components/income/IncomeList.tsx &amp;&amp; grep -q 'Изменить факт' src/components/income/IncomeList.tsx &amp;&amp; grep -q 'IncomeFactDialog' src/components/income/IncomeList.tsx &amp;&amp; grep -q 'получено' src/components/income/IncomeList.tsx</automated>
    <fails_when>list missing overdue/fact chrome strings or IncomeFactDialog import, or income-ui tests fail</fails_when>
  </verify>
  <acceptance_criteria>
    - Overdue and calm CTA labels match D-08/D-10/D-17 (ACT-02)
    - One-time filled variance chrome present (D-18, ACT-03)
    - Definition Изменить still present (D-20)
  </acceptance_criteria>
  <done>List shows overdue + fact entry + one-time variance.</done>
</task>

<task type="auto">
  <name>income-ui file-scan polish + full suite gate</name>
  <files>src/components/income/income-ui.test.ts</files>
  <read_first>
    src/components/income/income-ui.test.ts
    src/components/income/IncomeList.tsx
    src/components/income/IncomeFactDialog.tsx
    .planning/phases/15-plan-vs-actual-overdue/15-VALIDATION.md
    .planning/phases/15-plan-vs-actual-overdue/15-UI-SPEC.md
  </read_first>
  <behavior>
    - income-ui.test.ts asserts list contains заполни, Внести факт, Изменить факт, получено (ACT-02, ACT-03)
    - FactDialog source contains DestructiveConfirmStep and does not use browser native confirm API
    - List overdue badge path uses warning class tokens not destructive for заполни chip
    - FactDialog / list do not import chart UI module
    - Full npm test suite green
  </behavior>
  <action>
    Extend income-ui.test.ts file-scans per VALIDATION Wave 0 / RESEARCH: заполни + CTA labels on IncomeList; DestructiveConfirmStep on IncomeFactDialog; assert absence of browser native confirm API usage in fact dialog; assert warning token usage for overdue chip and that заполни chrome is not paired with destructive text classes; assert no chart component import on income list/fact files (D-09, D-12, ACT-02, ACT-03). Fix any copy mismatches against UI-SPEC. Run full suite as phase gate.
    <!-- planner-discipline-allow: window.confirm -->
  </action>
  <verify>
    <automated>npx vitest run src/components/income/income-ui.test.ts src/lib/income.test.ts src/app/income/actions.test.ts src/lib/validations/income.test.ts &amp;&amp; npm test</automated>
    <fails_when>any vitest/npm test failure or required chrome strings missing from scans</fails_when>
  </verify>
  <acceptance_criteria>
    - income-ui tests cover ACT-02/ACT-03 chrome + DestructiveConfirmStep (D-04, D-08, D-17)
    - Full npm test green (VALIDATION phase gate)
    - No chart import on income fact/list surfaces (D-12)
  </acceptance_criteria>
  <done>Phase 15 UI verified; suite green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Browser UI → Server Actions | Client can skip confirm step visually; server still validates |
| Rendered note / amounts | XSS via text nodes |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-15-05 | Tampering | note display / input | low | mitigate | React text escaping; Zod max 500; no dangerouslySetInnerHTML |
| T-15-07 | Tampering | bypass DestructiveConfirmStep | low | accept | Confirm is UX constitution; delete still requires explicit action POST |
| T-15-04 | Tampering | accidental NW coupling in UI | medium | mitigate | Fact dialog calls income actual actions only |
| T-15-06 | Spoofing | overdue chrome confusion | low | mitigate | Warning amber not destructive (D-09); copy from UI-SPEC |
| T-15-SC | Tampering | npm / Badge registry | high | mitigate | Zero new packages; CSS chip only |
</threat_model>

<verification>
npm test
</verification>

<success_criteria>
User records facts via IncomeFactDialog; overdue shows «заполни»+«Заполни»; one-time filled shows variance; DestructiveConfirmStep for delete actual; suite green; no chart on /income.
</success_criteria>

<output>
Create `.planning/phases/15-plan-vs-actual-overdue/15-03-SUMMARY.md` when done
</output>
