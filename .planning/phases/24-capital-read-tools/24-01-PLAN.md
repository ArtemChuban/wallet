---
phase: 24-capital-read-tools
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/lib/mcp/serialize.ts
  - src/lib/mcp/serialize.test.ts
  - src/lib/mcp/as-of.ts
  - src/lib/mcp/as-of.test.ts
  - src/lib/mcp/tools/accounts.test.ts
  - src/lib/mcp/tools/net-worth.test.ts
  - src/lib/mcp/tools/balances.test.ts
  - src/lib/mcp/tools/fx.test.ts
  - .planning/phases/24-capital-read-tools/24-VALIDATION.md
autonomous: true
requirements:
  - CAP-01
  - CAP-02
  - CAP-03
  - CAP-04
estimate:
  tokens: 20000
  raw_tokens: 40000
  tasks: 3
  confidence: high
must_haves:
  truths:
    - minorToJson / rate helpers stringify bigint minors with scale (D-09)
    - resolveAsOf defaults via calendarDateToday Europe/Moscow; optionalAsOfSchema rejects garbage YYYY-MM-DD (D-05, D-06, D-08)
    - Wave 0 CAP tool test files exist under src/lib/mcp/tools/ and exit 0 with todo/skip only until later plans fill them
  prohibitions:
    - statement: MUST NOT install new npm packages this phase
      category: scope
    - statement: MUST NOT register side-ledger MCP tools
      category: scope
    - statement: MUST NOT add CI mutate-import ban under mcp path
      category: scope
    - statement: MUST NOT widen localhost Host Origin policy
      category: isolation
  artifacts:
    - path: src/lib/mcp/serialize.ts
      provides: BigInt/null JSON helpers for CAP money fields
    - path: src/lib/mcp/as-of.ts
      provides: optionalAsOfSchema + resolveAsOf shared by CAP tools
    - path: src/lib/mcp/tools/accounts.test.ts
      provides: Wave 0 CAP-01 stub
    - path: src/lib/mcp/tools/net-worth.test.ts
      provides: Wave 0 CAP-02 stub
    - path: src/lib/mcp/tools/balances.test.ts
      provides: Wave 0 CAP-03 stub
    - path: src/lib/mcp/tools/fx.test.ts
      provides: Wave 0 CAP-04 stub
  key_links:
    - from: resolveAsOf
      to: calendarDateToday
      via: Europe/Moscow default when asOf omitted
      pattern: "calendarDateToday"
    - from: minorToJson
      to: CAP tool JSON payloads
      via: string minors + scale pairing
      pattern: "minorToJson"
---

## Specless probe fallback

SPEC absent. Truths from ROADMAP Phase 24 + CONTEXT D-01…D-16 + RESEARCH Wave 0 map.

## Phase Goal

External agent can read accounts, net worth, balances, and FX via MCP with the same honesty as Капитал UI (CAP-01…CAP-04).

<objective>
Land Wave 0 Nyquist stubs for all four CAP tool suites plus production serialize.ts (D-09) and as-of.ts (D-05, D-06, D-08) so Plan 02 tracer can register get_net_worth without inventing money JSON or today defaults.

Purpose: Shared adapters before any registerTool; keep suite green with todo/skip stubs.
Output: serialize + as-of modules green; four tools/*.test.ts stubs present; VALIDATION Wave 0 rows updated.
</objective>

<execution_context>
@/home/artem/Documents/wallet/.cursor/gsd-core/workflows/execute-plan.md
@/home/artem/Documents/wallet/.cursor/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/24-capital-read-tools/24-CONTEXT.md
@.planning/phases/24-capital-read-tools/24-RESEARCH.md
@.planning/phases/24-capital-read-tools/24-PATTERNS.md
@.planning/phases/24-capital-read-tools/24-VALIDATION.md
@src/lib/dates.ts
@src/lib/money.ts
@src/lib/dates.test.ts
@src/lib/mcp/tools/wallet-ping.ts
</context>

<assumptions>
## Planner assumptions

- A1 (RESEARCH): get_account_balance unknown accountId → success payload with error account_not_found (Plans 03).
- A2: openWorldHint false alongside readOnlyHint on CAP tools (Plans 02–04).
- A3: list_accounts omits grace DOM fields (D-04).
- A4: English zod messages for MCP asOf (D-14).
- No new npm packages; vitest/zod/MCP SDK already installed.
</assumptions>

## Artifacts this plan produces

- `src/lib/mcp/serialize.ts` + `serialize.test.ts`
- `src/lib/mcp/as-of.ts` + `as-of.test.ts`
- Wave 0 stubs: `tools/{accounts,net-worth,balances,fx}.test.ts`
- VALIDATION.md Wave 0 file-exists updates

<tasks>

<task type="auto" tdd="true">
  <name>Wave 0 Nyquist stubs for CAP tool suites</name>
  <files>src/lib/mcp/tools/accounts.test.ts, src/lib/mcp/tools/net-worth.test.ts, src/lib/mcp/tools/balances.test.ts, src/lib/mcp/tools/fx.test.ts, .planning/phases/24-capital-read-tools/24-VALIDATION.md</files>
  <behavior>
    - Each CAP tool test file exists and runs under vitest with it.todo or describe.skip only (exit 0)
    - No hard-fail expects until later plans fill behaviors
  </behavior>
  <action>
    Create four colocated Wave 0 stub test files under src/lib/mcp/tools/ mirroring Phase 23 localhost-guard stub style: describe block naming CAP-01…CAP-04 owners, each with it.todo cases for metadata list / NW honesty / conversionOk false / FX LOCF filter. Do not implement tools yet. Update 24-VALIDATION.md Wave 0 checklist and per-task File Exists column to ✅ for stub paths when present. No package installs.
  </action>
  <verify>
    <automated>test -f src/lib/mcp/tools/accounts.test.ts &amp;&amp; test -f src/lib/mcp/tools/net-worth.test.ts &amp;&amp; test -f src/lib/mcp/tools/balances.test.ts &amp;&amp; test -f src/lib/mcp/tools/fx.test.ts &amp;&amp; npx vitest run src/lib/mcp/tools/accounts.test.ts src/lib/mcp/tools/net-worth.test.ts src/lib/mcp/tools/balances.test.ts src/lib/mcp/tools/fx.test.ts</automated>
    <fails_when>any stub file missing, vitest non-zero, or hard-fail assertion poison (unexpected failed expects)</fails_when>
  </verify>
  <done>Four CAP tool stub tests exist and vitest exits 0 with todo/skip only.</done>
</task>

<task type="auto" tdd="true">
  <name>serialize.ts minor/rate JSON helpers (D-09)</name>
  <files>src/lib/mcp/serialize.ts, src/lib/mcp/serialize.test.ts</files>
  <behavior>
    - minorToJson(null) → null; minorToJson(123n) → "123"
    - Helpers never coerce bigint money to JS number
    - Rate helper returns string + documents rateScale 8 pairing for callers
  </behavior>
  <action>
    Per D-09 and PATTERNS serialize analog: add src/lib/mcp/serialize.ts exporting minorToJson(value: bigint | null): string | null and a rateToJson helper that returns rateToPrimaryScaled as string (callers pair rateScale: 8 from RATE_SCALE_E8). Cover with serialize.test.ts like dates.test.ts / money.test.ts. Never use Number() on minors. This module is shared by all CAP tools (CAP-01…04 money fields).
  </action>
  <verify>
    <automated>npx vitest run src/lib/mcp/serialize.test.ts</automated>
    <fails_when>vitest non-zero, "0 passed" in summary, or missing serialize.test.ts</fails_when>
  </verify>
  <done>serialize helpers green; bigint minors stringify; null stays null.</done>
</task>

<task type="auto" tdd="true">
  <name>as-of.ts resolveAsOf + optionalAsOfSchema (D-05/D-06/D-08)</name>
  <files>src/lib/mcp/as-of.ts, src/lib/mcp/as-of.test.ts</files>
  <behavior>
    - resolveAsOf(undefined) === calendarDateToday("Europe/Moscow")
    - resolveAsOf("2020-01-15") returns that string
    - optionalAsOfSchema rejects non YYYY-MM-DD with English message
    - Future calendar dates parse as valid input shape (no future reject)
  </behavior>
  <action>
    Per D-05, D-06, D-08 and RESEARCH sketch: export optionalAsOfSchema as zod string regex ^\d{4}-\d{2}-\d{2}$ with English message "asOf must be YYYY-MM-DD", and resolveAsOf(asOf?: string) returning asOf ?? calendarDateToday("Europe/Moscow"). Do not copy RU form validation messages. Do not reject future dates. Tests in as-of.test.ts. Shared by get_net_worth / get_account_balance / list_fx_rates.
  </action>
  <verify>
    <automated>npx vitest run src/lib/mcp/as-of.test.ts</automated>
    <fails_when>vitest non-zero, "0 passed" in summary, or resolveAsOf bypasses calendarDateToday</fails_when>
  </verify>
  <done>Single today path via calendarDateToday; strict wire asOf; future allowed.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| MCP tool input → as-of/serialize helpers | Untrusted asOf strings cross into shared validators |
| Adapter JSON → agent client | Money honesty depends on string minors not float |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-24-01 | Information Disclosure | serialize.ts | medium | mitigate | Stringify minors only; never log payloads in helpers |
| T-24-04 | Tampering / Input | as-of.ts | medium | mitigate | Zod YYYY-MM-DD; English validation error; no future ban |
| T-24-SC | Tampering | npm installs | high | accept | No new packages this plan — tree already audited in RESEARCH |
</threat_model>

<verification>
npx vitest run src/lib/mcp/serialize.test.ts src/lib/mcp/as-of.test.ts src/lib/mcp/tools/
</verification>

<success_criteria>
Wave 0 CAP stubs present; serialize + as-of green; suite under src/lib/mcp/tools/ exits 0.
</success_criteria>

<output>
Create `.planning/phases/24-capital-read-tools/24-01-SUMMARY.md` when done
</output>
