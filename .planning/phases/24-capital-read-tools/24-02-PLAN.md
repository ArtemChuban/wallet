---
phase: 24-capital-read-tools
plan: 02
type: execute
wave: 2
depends_on:
  - "24-01"
files_modified:
  - src/lib/mcp/reads/load-net-worth-asof.ts
  - src/lib/mcp/tools/net-worth.ts
  - src/lib/mcp/tools/net-worth.test.ts
  - src/lib/mcp/create-handler.ts
autonomous: true
requirements:
  - CAP-02
estimate:
  tokens: 40000
  raw_tokens: 80000
  tasks: 2
  confidence: high
must_haves:
  truths:
    - Agent can call get_net_worth via MCP and receive totalPrimaryMinor string + isPartial + per-account rows with excludeReason (CAP-02, D-10)
    - Omitted asOf resolves through resolveAsOf / calendarDateToday (D-05, D-06)
    - Rows enriched with accountName, currencyCode, type (D-12); money fields are strings + scale (D-09)
    - Empty wallet returns success with zero/empty structured honesty, not tool error (D-07)
    - Tool registered as get_net_worth with readOnlyHint true; wallet_ping remains (D-01, D-02, D-13)
    - Server instructions replaced with capital-era English + RU aliases (D-15 start)
  prohibitions:
    - statement: MUST NOT reimplement computeNetWorthRows inclusion rules in the tool
      category: isolation
    - statement: MUST NOT fold debts or income into NW payload
      category: scope
    - statement: MUST NOT register side-ledger tools
      category: scope
    - statement: MUST NOT invent FX rates when missing
      category: isolation
  artifacts:
    - path: src/lib/mcp/reads/load-net-worth-asof.ts
      provides: page-parity LOCF + computeNetWorthRows assembler
    - path: src/lib/mcp/tools/net-worth.ts
      provides: registerGetNetWorth MCP tool
    - path: src/lib/mcp/create-handler.ts
      provides: capital-era instructions + get_net_worth registration
  key_links:
    - from: loadNetWorthAsOf
      to: computeNetWorthRows
      via: NetWorthAccountInput batch from firstHitLocfMap
      pattern: "computeNetWorthRows"
    - from: registerGetNetWorth
      to: createWalletMcpHandler
      via: register after wallet_ping
      pattern: "get_net_worth"
    - from: resolveAsOf
      to: get_net_worth handler
      via: optional asOf input
      pattern: "resolveAsOf"
---

## Specless probe fallback

SPEC absent. Truths from ROADMAP CAP-02 success + CONTEXT D-01…D-15.

## Phase Goal

External agent can read accounts, net worth, balances, and FX via MCP with the same honesty as Капитал UI.

<objective>
Ship production-quality tracer for CAP-02: page-parity load-net-worth-asof → serialize → registerGetNetWorth → createWalletMcpHandler capital-era instructions (D-01, D-02, D-05…D-15 for NW path).

Purpose: Prove end-to-end capital MCP honesty on one tool before expanding catalog.
Output: get_net_worth live beside wallet_ping; green net-worth adapter tests.
</objective>

<execution_context>
@/home/artem/Documents/wallet/.cursor/gsd-core/workflows/execute-plan.md
@/home/artem/Documents/wallet/.cursor/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/24-capital-read-tools/24-CONTEXT.md
@.planning/phases/24-capital-read-tools/24-RESEARCH.md
@.planning/phases/24-capital-read-tools/24-PATTERNS.md
@.planning/phases/24-capital-read-tools/24-01-SUMMARY.md
@src/app/page.tsx
@src/lib/net-worth.ts
@src/lib/locf.ts
@src/lib/mcp/create-handler.ts
@src/lib/mcp/tools/wallet-ping.ts
@src/lib/mcp/serialize.ts
@src/lib/mcp/as-of.ts
</context>

<interface>
## Plan 01 → Plan 02 contracts

- `minorToJson` / rate helpers from `src/lib/mcp/serialize.ts`
- `optionalAsOfSchema` + `resolveAsOf` from `src/lib/mcp/as-of.ts`
- Wave 0 `src/lib/mcp/tools/net-worth.test.ts` stub ready to fill
</interface>

<assumptions>
## Planner assumptions

- A1: Missing account on balance tool deferred to Plan 03.
- A2: openWorldHint false with readOnlyHint on get_net_worth.
- Tracer registers only get_net_worth + wallet_ping; accounts/balances/fx land Plans 03–04.
- Instructions text this plan covers D-15 capital-era baseline (read-only capital; NW accounts-only; FX not converter; side ledgers later); Plan 04 may tighten wording after all tools exist.
</assumptions>

<tasks>

<task type="tracer">
  <name>End-to-end get_net_worth — CAP-02 tracer</name>
  <reversibility rating="costly">Published MCP tool name get_net_worth (D-01); rename after agents connect breaks CLI clients.</reversibility>
  <files>src/lib/mcp/reads/load-net-worth-asof.ts, src/lib/mcp/tools/net-worth.ts, src/lib/mcp/create-handler.ts</files>
  <read_first>
    - src/app/page.tsx NW load block (accounts + snapshots lte + rates lte + firstHitLocfMap + computeNetWorthRows + enrichment)
    - src/lib/net-worth.ts NetWorthRow / excludeReason contract
    - src/lib/mcp/tools/wallet-ping.ts registerTool pattern
    - src/lib/mcp/as-of.ts, src/lib/mcp/serialize.ts
  </read_first>
  <action>
    Wire ONE path only: get_net_worth. Create loadNetWorthAsOf(asOf: string) in reads/ mirroring Капитал page LOCF batch (omit income/grace loads). Feed computeNetWorthRows only — no twin inclusion math. Enrich rows with accountName, currencyCode, type (D-12). Serialize all bigint money via minorToJson + scale / primaryScale; include isPartial, totalPrimaryMinor string, asOf, primaryCurrencyCode (D-09, D-10). Empty/partial → success payload (D-07). registerGetNetWorth: name get_net_worth (D-01, D-02), description English + Капитал alias forbidding agent rate multiply (D-03 intent for NW / D-14), inputSchema { asOf: optionalAsOfSchema }, annotations readOnlyHint true and openWorldHint false (D-13). Handler: resolveAsOf(asOf) then load then JSON content text. Update createWalletMcpHandler: replace Phase 23 minimal instructions with capital-era D-15 text; call registerGetNetWorth after registerWalletPing. Import only domain read libs (balances/fx/net-worth/locf/prisma via db) — review enforce D-16, no CI ban. Keep responseMode json + legacy stateless.
  </action>
  <verify>
    <automated>test -f src/lib/mcp/reads/load-net-worth-asof.ts &amp;&amp; test -f src/lib/mcp/tools/net-worth.ts &amp;&amp; grep -q 'get_net_worth' src/lib/mcp/tools/net-worth.ts &amp;&amp; grep -q 'readOnlyHint' src/lib/mcp/tools/net-worth.ts &amp;&amp; grep -q 'registerGetNetWorth' src/lib/mcp/create-handler.ts &amp;&amp; grep -q 'computeNetWorthRows' src/lib/mcp/reads/load-net-worth-asof.ts &amp;&amp; npx vitest run src/lib/mcp/tools/net-worth.test.ts src/lib/mcp/as-of.test.ts src/lib/mcp/serialize.test.ts</automated>
    <fails_when>missing files, get_net_worth/readOnlyHint/registerGetNetWorth/computeNetWorthRows markers absent, or vitest non-zero</fails_when>
  </verify>
  <done>get_net_worth registered end-to-end with page-parity honesty and capital-era instructions.</done>
</task>

<task type="auto" tdd="true">
  <name>Green CAP-02 net-worth adapter tests</name>
  <files>src/lib/mcp/tools/net-worth.test.ts, src/lib/mcp/reads/load-net-worth-asof.ts</files>
  <behavior>
    - Serialized payload totalPrimaryMinor is string; isPartial mirrors computeNetWorthRows fixture
    - excludeReason none | no_balance | no_fx preserved on rows
    - Omitted asOf path uses resolveAsOf (mock calendarDateToday or assert schema + resolve unit)
    - Prefer fixture/mocked prisma or pure assembler serialize tests — do not re-prove LOCF math
  </behavior>
  <action>
    Replace Wave 0 todos in net-worth.test.ts with real expects for CAP-02: after computeNetWorthRows-equivalent fixture, assert adapter JSON uses string minors, isPartial, excludeReason, enrichment fields. Mock prisma/read layer or test a serializeNetWorthPayload pure helper extracted from the read module if that keeps tests SQLite-free. Keep HOST regression green separately when running mcp suite.
  </action>
  <verify>
    <automated>npx vitest run src/lib/mcp/tools/net-worth.test.ts src/lib/mcp/localhost-guard.test.ts src/app/api/mcp/route.test.ts</automated>
    <fails_when>vitest non-zero, remaining it.todo on CAP-02 owner cases, or "0 passed" in summary</fails_when>
  </verify>
  <done>CAP-02 adapter tests green; host/route regression still green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| MCP tools/call → get_net_worth | Agent-supplied asOf into LOCF assembler |
| Domain libs → MCP JSON | BigInt honesty must survive serialize |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-24-02 | Spoofing | get_net_worth / load-net-worth-asof | high | mitigate | Only computeNetWorthRows; surface isPartial/excludeReason; never invent rates |
| T-24-03 | Tampering | create-handler / tools | high | mitigate | Register read tools only; no Server Action imports (D-16 review) |
| T-24-01 | Information Disclosure | tool responses | medium | mitigate | Log tool name only if logging added; no money payload logs |
| T-24-04 | Elevation of Privilege | annotations | low | accept | readOnlyHint is UX hint only — not auth; localhost guard remains Phase 23 |
| T-24-SC | Tampering | npm installs | high | accept | No new packages |
</threat_model>

<verification>
npx vitest run src/lib/mcp/
</verification>

<success_criteria>
get_net_worth end-to-end; CAP-02 honesty in tests; wallet_ping retained; capital-era instructions present.
</success_criteria>

<output>
Create `.planning/phases/24-capital-read-tools/24-02-SUMMARY.md` when done
</output>
