---
phase: 24-capital-read-tools
plan: 04
type: execute
wave: 4
depends_on:
  - "24-03"
files_modified:
  - src/lib/mcp/reads/load-fx-rates-asof.ts
  - src/lib/mcp/tools/fx.ts
  - src/lib/mcp/tools/fx.test.ts
  - src/lib/mcp/create-handler.ts
autonomous: true
requirements:
  - CAP-04
estimate:
  tokens: 25000
  raw_tokens: 50000
  tasks: 2
  confidence: high
must_haves:
  truths:
    - list_fx_rates returns as-of LOCF primary↔other rates with rateScale 8 and string rateToPrimaryScaled (CAP-04, D-03, D-09)
    - Optional currencyCode filter works; omitted asOf uses resolveAsOf (D-05, D-06)
    - Tool description states FX list is transparency only — agents must not convert (D-03)
    - Empty rates → success empty array (D-07)
    - All four CAP tools + wallet_ping registered; capital-era instructions finalized (D-15)
    - HOST localhost-guard + route regression still green
  prohibitions:
    - statement: MUST NOT expose a convert_fx or agent-side multiply API
      category: isolation
    - statement: MUST NOT register side-ledger tools
      category: scope
    - statement: MUST NOT widen localhost Host Origin policy
      category: isolation
    - statement: MUST NOT add CI mutate-import ban this phase
      category: scope
  artifacts:
    - path: src/lib/mcp/reads/load-fx-rates-asof.ts
      provides: rates-page LOCF snapshot assembler
    - path: src/lib/mcp/tools/fx.ts
      provides: registerListFxRates
    - path: src/lib/mcp/create-handler.ts
      provides: full CAP catalog + finalized D-15 instructions
  key_links:
    - from: list_fx_rates
      to: firstHitLocfMap on FxRate lte asOf
      via: load-fx-rates-asof
      pattern: "list_fx_rates"
    - from: createWalletMcpHandler
      to: registerListFxRates
      via: final CAP registration
      pattern: "registerListFxRates"
    - from: tool description
      to: agent clients
      via: do-not-convert copy (D-03)
      pattern: "readOnlyHint"
---

## Specless probe fallback

SPEC absent. Truths from ROADMAP CAP-04 + CONTEXT D-03, D-15.

## Phase Goal

External agent can read accounts, net worth, balances, and FX via MCP with the same honesty as Капитал UI.

<objective>
Ship list_fx_rates (CAP-04 / D-03) and finalize createWalletMcpHandler capital-era instructions (D-15) so the four CAP tools + wallet_ping form the complete Phase 24 catalog.

Purpose: Close FX transparency path; seal phase tool surface before Phase 25 side ledgers.
Output: list_fx_rates registered; full mcp vitest + npm test green; HOST regression green.
</objective>

<execution_context>
@/home/artem/Documents/wallet/.cursor/gsd-core/workflows/execute-plan.md
@/home/artem/Documents/wallet/.cursor/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/24-capital-read-tools/24-CONTEXT.md
@.planning/phases/24-capital-read-tools/24-RESEARCH.md
@.planning/phases/24-capital-read-tools/24-PATTERNS.md
@.planning/phases/24-capital-read-tools/24-03-SUMMARY.md
@src/app/currencies/rates/page.tsx
@src/lib/locf.ts
@src/lib/money.ts
@src/lib/mcp/create-handler.ts
@src/lib/mcp/as-of.ts
@src/lib/mcp/serialize.ts
</context>

<interface>
## Plan 03 → Plan 04 contracts

- create-handler already registers wallet_ping, get_net_worth, list_accounts, get_account_balance
- Wave 0 fx.test.ts stub ready to fill
</interface>

<assumptions>
## Planner assumptions

- list_fx_rates optional currencyCode filters non-primary codes; missing LOCF row may be null entry or omitted — prefer include currency with null rate fields for honesty (executor pick; document in SUMMARY).
- D-16: no CI path-ban; full suite + review suffice.
</assumptions>

<tasks>

<task type="auto" tdd="true">
  <name>list_fx_rates LOCF transparency tool (CAP-04, D-03)</name>
  <reversibility rating="costly">Published tool name list_fx_rates (D-01).</reversibility>
  <files>src/lib/mcp/reads/load-fx-rates-asof.ts, src/lib/mcp/tools/fx.ts, src/lib/mcp/tools/fx.test.ts, src/lib/mcp/create-handler.ts</files>
  <behavior>
    - Payload { asOf, primaryCurrencyCode, rateScale: 8, rates: [{ currencyCode, asOfDate, rateToPrimaryScaled: string } ...] }
    - optional currencyCode filter
    - Description forbids agent conversion; annotations readOnlyHint true
    - Empty → rates [] success
  </behavior>
  <action>
    Per D-01, D-02, D-03, D-05, D-06, D-07, D-08, D-09, D-13, D-14: implement loadFxRatesAsOf mirroring rates page LOCF (firstHitLocfMap on FxRate lte asOf for non-primary currencies). Serialize rates via rate string helper + rateScale 8. registerListFxRates with optionalAsOfSchema + optional currencyCode string; resolveAsOf; description must state transparency-only / not a converter / primary amounts from get_net_worth or get_account_balance only. Wire registerListFxRates into createWalletMcpHandler. Fill fx.test.ts (filter + LOCF fixture / mocked reads).
  </action>
  <verify>
    <automated>test -f src/lib/mcp/reads/load-fx-rates-asof.ts &amp;&amp; test -f src/lib/mcp/tools/fx.ts &amp;&amp; grep -q 'list_fx_rates' src/lib/mcp/tools/fx.ts &amp;&amp; grep -q 'registerListFxRates' src/lib/mcp/create-handler.ts &amp;&amp; grep -q 'readOnlyHint' src/lib/mcp/tools/fx.ts &amp;&amp; npx vitest run src/lib/mcp/tools/fx.test.ts</automated>
    <fails_when>files/markers missing, vitest non-zero, or CAP-04 it.todo remains</fails_when>
  </verify>
  <done>list_fx_rates registered with LOCF snapshot and do-not-convert description.</done>
</task>

<task type="auto">
  <name>Finalize D-15 instructions + full CAP regression</name>
  <files>src/lib/mcp/create-handler.ts, .planning/phases/24-capital-read-tools/24-VALIDATION.md</files>
  <action>
    Per D-15: ensure server instructions are short capital-era English + RU aliases covering read-only capital tools, NW accounts-only (no debts), FX list not a converter, side ledgers later. Confirm all four CAP registers present with wallet_ping. Update 24-VALIDATION.md task map Status where green and set nyquist_compliant aspirational notes for executor after tests pass. Run full mcp suite + prior HOST verifies + npm test. D-16: no CI mutate ban added.
  </action>
  <verify>
    <automated>grep -q 'list_accounts' src/lib/mcp/create-handler.ts &amp;&amp; grep -q 'get_net_worth' src/lib/mcp/create-handler.ts &amp;&amp; grep -q 'get_account_balance' src/lib/mcp/create-handler.ts &amp;&amp; grep -q 'list_fx_rates' src/lib/mcp/create-handler.ts src/lib/mcp/tools/fx.ts &amp;&amp; grep -q 'wallet_ping' src/lib/mcp/tools/wallet-ping.ts &amp;&amp; npx vitest run src/lib/mcp/ src/app/api/mcp/route.test.ts &amp;&amp; npm test</automated>
    <fails_when>any CAP tool registration marker missing, vitest or npm test non-zero, or "0 passed" in vitest summary</fails_when>
  </verify>
  <done>Full CAP-01…04 catalog live; mcp + full suite green; HOST regression green; D-15 instructions final.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| MCP list_fx_rates → agent | Rates look convertible — copy must forbid DIY multiply |
| create-handler catalog → clients | Tool surface is published contract |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-24-02 | Spoofing | list_fx_rates | high | mitigate | Description bans agent convert; no convert API; primary only via NW/balance tools |
| T-24-03 | Tampering | create-handler | high | mitigate | Read tools only; D-16 review; no side-ledger tools |
| T-24-01 | Information Disclosure | FX LOCF list | medium | mitigate | Localhost guard unchanged; log tool names not payloads |
| T-24-04 | Tampering / Input | currencyCode/asOf | medium | mitigate | Zod optional filters + optionalAsOfSchema |
| T-24-SC | Tampering | npm installs | high | accept | No new packages this phase |
</threat_model>

<verification>
npx vitest run src/lib/mcp/ &amp;&amp; npm test
</verification>

<success_criteria>
CAP-04 shipped; four CAP tools + ping registered; D-15 done; suites green.
</success_criteria>

<output>
Create `.planning/phases/24-capital-read-tools/24-04-SUMMARY.md` when done
</output>
