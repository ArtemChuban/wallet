---
phase: 24-capital-read-tools
plan: 03
type: execute
wave: 3
depends_on:
  - "24-02"
files_modified:
  - src/lib/mcp/tools/accounts.ts
  - src/lib/mcp/tools/accounts.test.ts
  - src/lib/mcp/reads/load-account-balance-asof.ts
  - src/lib/mcp/tools/balances.ts
  - src/lib/mcp/tools/balances.test.ts
  - src/lib/mcp/create-handler.ts
autonomous: true
requirements:
  - CAP-01
  - CAP-03
estimate:
  tokens: 35000
  raw_tokens: 70000
  tasks: 3
  confidence: high
must_haves:
  truths:
    - list_accounts returns type, currencyCode, currencyScale, creditLimitMinor string|null, isCredit — metadata only (CAP-01, D-04)
    - get_account_balance returns native + primary as-of with server-side convert; missing FX/snapshot → conversionOk false and primary null (CAP-03, D-11)
    - Rows include accountName, currencyCode, type (D-12); money as strings + scale (D-09)
    - Unknown accountId → success payload with error account_not_found (planner A1 / D-07 honesty)
    - Both tools readOnlyHint true; names list_accounts and get_account_balance (D-01, D-02, D-13)
  prohibitions:
    - statement: MUST NOT return live available or debt balances from list_accounts
      category: scope
    - statement: MUST NOT require agent to multiply FX rates for primary amounts
      category: isolation
    - statement: MUST NOT register side-ledger tools
      category: scope
  artifacts:
    - path: src/lib/mcp/tools/accounts.ts
      provides: registerListAccounts
    - path: src/lib/mcp/reads/load-account-balance-asof.ts
      provides: LOCF balance + server convert assembler
    - path: src/lib/mcp/tools/balances.ts
      provides: registerGetAccountBalance
  key_links:
    - from: list_accounts
      to: prisma.account.findMany
      via: currency include + isCreditType
      pattern: "list_accounts"
    - from: get_account_balance
      to: convertOtherMinorToPrimaryMinor
      via: getBalanceAsOf / getRateAsOf when convertible
      pattern: "conversionOk"
    - from: createWalletMcpHandler
      to: registerListAccounts + registerGetAccountBalance
      via: alongside get_net_worth and wallet_ping
      pattern: "registerListAccounts"
---

## Specless probe fallback

SPEC absent. Truths from ROADMAP CAP-01/CAP-03 + CONTEXT D-04, D-09, D-11, D-12.

## Phase Goal

External agent can read accounts, net worth, balances, and FX via MCP with the same honesty as Капитал UI.

<objective>
Expand from NW tracer: ship list_accounts (CAP-01) and get_account_balance (CAP-03) with D-04 metadata-only list and D-11 conversion honesty, wire into createWalletMcpHandler.

Purpose: Complete account catalog + single-account balance path agents need beside NW.
Output: Two more CAP tools registered; green accounts + balances tests.
</objective>

<execution_context>
@/home/artem/Documents/wallet/.cursor/gsd-core/workflows/execute-plan.md
@/home/artem/Documents/wallet/.cursor/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/24-capital-read-tools/24-CONTEXT.md
@.planning/phases/24-capital-read-tools/24-RESEARCH.md
@.planning/phases/24-capital-read-tools/24-PATTERNS.md
@.planning/phases/24-capital-read-tools/24-02-SUMMARY.md
@src/app/accounts/page.tsx
@src/lib/balances.ts
@src/lib/fx.ts
@src/lib/money.ts
@src/lib/account-type.ts
@src/lib/mcp/create-handler.ts
@src/lib/mcp/tools/net-worth.ts
@src/lib/mcp/serialize.ts
@src/lib/mcp/as-of.ts
</context>

<interface>
## Plan 02 → Plan 03 contracts

- createWalletMcpHandler already registers wallet_ping + get_net_worth with capital-era instructions
- serialize + as-of shared helpers available
- Wave 0 accounts.test.ts + balances.test.ts stubs ready to fill
</interface>

<assumptions>
## Planner assumptions

- A1 locked: unknown accountId → `{ error: "account_not_found", ...null amounts }` success content (not hard MCP error).
- Credit metadata: isCredit via isCreditType; creditLimitMinor string|null; no grace DOM on list_accounts (A3).
</assumptions>

<tasks>

<task type="auto" tdd="true">
  <name>list_accounts metadata tool (CAP-01, D-04)</name>
  <reversibility rating="costly">Published tool name list_accounts (D-01).</reversibility>
  <files>src/lib/mcp/tools/accounts.ts, src/lib/mcp/tools/accounts.test.ts</files>
  <behavior>
    - Payload accounts[] with id, name, type, currencyCode, currencyScale, creditLimitMinor string|null, isCredit
    - Empty wallet → { accounts: [] } success
    - registerTool annotations readOnlyHint true
  </behavior>
  <action>
    Per D-01, D-02, D-04, D-09, D-13, D-14: implement registerListAccounts. Query prisma.account.findMany include currency orderBy name. Serialize creditLimitMinor via minorToJson; isCredit from isCreditType. Metadata only — omit LOCF snapshot amounts and grace DOM fields. Description English + RU account alias. Wire registerListAccounts in createWalletMcpHandler in task 3 if preferred, or same commit once balances ready — this task may export register only; final wiring in task 3. Fill accounts.test.ts with fixture/mocked accounts asserting string creditLimitMinor and isCredit.
  </action>
  <verify>
    <automated>test -f src/lib/mcp/tools/accounts.ts &amp;&amp; grep -q 'list_accounts' src/lib/mcp/tools/accounts.ts &amp;&amp; npx vitest run src/lib/mcp/tools/accounts.test.ts</automated>
    <fails_when>accounts.ts missing, list_accounts marker absent, vitest non-zero, or remaining CAP-01 it.todo</fails_when>
  </verify>
  <done>list_accounts metadata tool implemented and tested.</done>
</task>

<task type="auto" tdd="true">
  <name>get_account_balance honesty tool (CAP-03, D-11)</name>
  <reversibility rating="costly">Published tool name get_account_balance (D-01).</reversibility>
  <files>src/lib/mcp/reads/load-account-balance-asof.ts, src/lib/mcp/tools/balances.ts, src/lib/mcp/tools/balances.test.ts</files>
  <behavior>
    - Input accountId required positive int + optional asOf
    - Missing snapshot/FX → conversionOk false, primaryAmountMinor null, native may be present + reason
    - Convert only via convertOtherMinorToPrimaryMinor server-side
    - account_not_found success payload when id missing
  </behavior>
  <action>
    Per D-05, D-06, D-07, D-09, D-11, D-12, D-13: loadAccountBalanceAsOf(accountId, asOf) using getBalanceAsOf / getRateAsOf / primary currency lookup; convert with convertOtherMinorToPrimaryMinor when possible. Enrich accountName, type, currencyCode. registerGetAccountBalance with optionalAsOfSchema + accountId zod int positive; resolveAsOf; annotations readOnlyHint. Description forbids agent conversion. Fill balances.test.ts for conversionOk false path and account_not_found success.
  </action>
  <verify>
    <automated>test -f src/lib/mcp/reads/load-account-balance-asof.ts &amp;&amp; test -f src/lib/mcp/tools/balances.ts &amp;&amp; grep -q 'get_account_balance' src/lib/mcp/tools/balances.ts &amp;&amp; grep -q 'conversionOk' src/lib/mcp/reads/load-account-balance-asof.ts src/lib/mcp/tools/balances.ts &amp;&amp; npx vitest run src/lib/mcp/tools/balances.test.ts</automated>
    <fails_when>files/markers missing, vitest non-zero, or CAP-03 todos remain</fails_when>
  </verify>
  <done>get_account_balance returns server-converted primary or honest null primary.</done>
</task>

<task type="auto">
  <name>Wire list_accounts + get_account_balance into create-handler</name>
  <files>src/lib/mcp/create-handler.ts</files>
  <action>
    Import and call registerListAccounts and registerGetAccountBalance from createWalletMcpHandler after wallet_ping / get_net_worth (order discretionary). Keep capital-era instructions. Confirm no Edge runtime change on route (route untouched this plan).
  </action>
  <verify>
    <automated>grep -q 'registerListAccounts' src/lib/mcp/create-handler.ts &amp;&amp; grep -q 'registerGetAccountBalance' src/lib/mcp/create-handler.ts &amp;&amp; npx vitest run src/lib/mcp/tools/accounts.test.ts src/lib/mcp/tools/balances.test.ts src/lib/mcp/tools/net-worth.test.ts src/app/api/mcp/route.test.ts</automated>
    <fails_when>register markers missing in create-handler, or vitest non-zero</fails_when>
  </verify>
  <done>Both CAP-01 and CAP-03 tools registered in handler factory.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| MCP input accountId/asOf → Prisma reads | Untrusted ids/dates |
| Balance assembler → agent | Primary amounts must be server-authored |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-24-02 | Spoofing | get_account_balance | high | mitigate | Server convertOtherMinorToPrimaryMinor only; conversionOk false on miss |
| T-24-04 | Tampering / Input | balances/accounts zod | medium | mitigate | Zod accountId positive int + optionalAsOfSchema |
| T-24-03 | Tampering | mcp tools imports | high | mitigate | Domain read libs only; D-16 review |
| T-24-01 | Information Disclosure | list_accounts | low | accept | Localhost-only surface; metadata expected for single-user wallet |
| T-24-SC | Tampering | npm installs | high | accept | No new packages |
</threat_model>

<verification>
npx vitest run src/lib/mcp/
</verification>

<success_criteria>
list_accounts + get_account_balance registered and tested; NW tracer still green.
</success_criteria>

<output>
Create `.planning/phases/24-capital-read-tools/24-03-SUMMARY.md` when done
</output>
