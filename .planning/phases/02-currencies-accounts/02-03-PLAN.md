---
phase: 02-currencies-accounts
plan: 03
type: execute
wave: 3
depends_on:
  - 02-02
files_modified:
  - src/app/accounts/actions.ts
  - src/app/currencies/actions.ts
  - src/components/accounts/AccountFormDialog.tsx
  - src/components/accounts/AccountList.tsx
  - src/components/currencies/CurrencyFormDialog.tsx
  - src/components/currencies/CurrencyList.tsx
  - src/app/accounts/page.tsx
  - src/app/currencies/page.tsx
  - src/lib/validations/account.test.ts
  - src/lib/validations/currency.test.ts
  - src/app/accounts/actions.test.ts
  - src/app/currencies/actions.test.ts
autonomous: false
requirements:
  - CURR-01
  - ACCT-01
  - ACCT-02
estimate:
  tokens: 42000
  raw_tokens: 42000
  tasks: 3
  confidence: low
must_haves:
  truths:
    - User can create accounts of all four types FIAT_DEBIT, FIAT_CREDIT, CRYPTO, CASH (ACCT-01)
    - After create, only account name is editable; type, currency, and credit limit stay locked (D-11, D-12, D-13, D-15)
    - After create, only currency name is editable; code and scale stay locked (D-04, D-08)
    - Credit limit remains immutable after create — edit Dialog shows limit read-only (D-11, ACCT-02)
    - Account list orders by name ascending for stable equal-meta display (ACCT-02 ordering)
    - SQLite unique constraints serialize duplicate name/code creates under concurrent submits (ACCT-01 concurrency)
    - Page/main scrolls vertically on long lists; Dialog body scrolls on short viewports (UI overflow E1/E2)
    - Narrow viewports: top nav wraps or scrolls within header (UI overflow E5)
    - Form/list errors surface field messages plus form-level «Не удалось сохранить…» on action failure (UI error E3/E4)
    - Zero accounts and zero currencies empty states remain correct after expansion (UI zero-one-many)
  prohibitions:
    - statement: MUST NOT expose currency or account removal UI, menus, or confirmations
      requirement_id: ACCT-01
      category: values
    - statement: MUST NOT allow post-create mutation of account type, currencyCode, or creditLimitMinor through Server Actions
      requirement_id: ACCT-01
      category: safety
    - statement: MUST NOT introduce primary-currency switch UI while expanding account flows
      requirement_id: CURR-01
      category: values
  artifacts:
    - path: src/app/accounts/actions.test.ts
      provides: immutability and no-removal export tests for account actions
    - path: src/app/currencies/actions.test.ts
      provides: immutability and no-removal export tests for currency actions
    - path: src/components/accounts/AccountFormDialog.tsx
      provides: four-type create + name-only edit UX
  key_links:
    - from: updateAccountName
      to: prisma.account.update
      via: name-only data object
      pattern: "updateAccountName"
    - from: updateCurrencyName
      to: prisma.currency.update
      via: name-only data object
      pattern: "updateCurrencyName"
---

## Phase Goal

**As a** local Wallet user, **I want to** create every account type and safely rename currencies/accounts without unlocking identity fields, **so that** the catalog stays consistent for later balances and net worth.

<objective>
Expand from the credit-account tracer to all four account types, enforce immutability on Server Actions and Dialogs, assert no removal exports, and human-verify Russian chrome.

Purpose: Close ACCT-01 create/edit (CONTEXT-narrowed), finish ACCT-02 immutability, and harden CURR-01 edit rules.
Output: Full typed account create coverage, action immutability tests, RU UI checkpoint.
</objective>

<execution_context>
@/home/artem/Documents/wallet/.cursor/gsd-core/workflows/execute-plan.md
@/home/artem/Documents/wallet/.cursor/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/02-currencies-accounts/02-CONTEXT.md
@.planning/phases/02-currencies-accounts/02-UI-SPEC.md
@.planning/phases/02-currencies-accounts/02-RESEARCH.md
@.planning/phases/02-currencies-accounts/02-02-PLAN.md
@src/app/accounts/actions.ts
@src/app/currencies/actions.ts
@src/components/accounts/AccountFormDialog.tsx
</context>

<tasks>

<task type="auto" tdd="true">
  <name>All four account types + name-only edit UX</name>
  <files>src/components/accounts/AccountFormDialog.tsx, src/components/accounts/AccountList.tsx, src/app/accounts/page.tsx, src/app/accounts/actions.ts, src/lib/validations/account.test.ts</files>
  <read_first>
    .planning/phases/02-currencies-accounts/02-UI-SPEC.md
    .planning/phases/02-currencies-accounts/02-CONTEXT.md
    src/app/accounts/actions.ts
    src/components/accounts/AccountFormDialog.tsx
    src/lib/validations/account.ts
  </read_first>
  <behavior>
    - Zod/action create accepts FIAT_DEBIT, FIAT_CREDIT, CRYPTO, CASH
    - Non-credit types persist creditLimitMinor null
    - Edit Dialog exposes only name input; type/currency/limit shown read-only
  </behavior>
  <action>
    Per D-12, D-15, D-16, ACCT-01: ensure AccountFormDialog Select lists all four Russian type labels and createAccount accepts each type. Credit limit field appears only for FIAT_CREDIT. List page orders accounts by name ascending. Extend account.test.ts for all four types and null limit on non-credit. Keep Dialog open-until-success pending disable behavior from UI-SPEC. Do not add archive/removal affordances.
  </action>
  <verify>
    <automated>npm test -- --run src/lib/validations/account.test.ts && grep -q 'FIAT_DEBIT' src/components/accounts/AccountFormDialog.tsx && grep -q 'CRYPTO' src/components/accounts/AccountFormDialog.tsx && grep -q 'CASH' src/components/accounts/AccountFormDialog.tsx && grep -q 'FIAT_CREDIT' src/components/accounts/AccountFormDialog.tsx</automated>
    <fails_when>account tests fail or any of the four AccountType enum values is missing from AccountFormDialog</fails_when>
  </verify>
  <acceptance_criteria>
    - AccountFormDialog create path references all four AccountType values
    - account.test.ts covers non-credit null-limit and credit required-limit cases
    - Edit mode does not render editable Select for type or currency
  </acceptance_criteria>
  <done>All four account types creatable; name-only edit UX complete</done>
</task>

<task type="auto" tdd="true">
  <name>Immutability + no-removal action export tests</name>
  <files>src/app/accounts/actions.ts, src/app/currencies/actions.ts, src/app/accounts/actions.test.ts, src/app/currencies/actions.test.ts, src/components/accounts/AccountFormDialog.tsx, src/components/currencies/CurrencyFormDialog.tsx, src/components/accounts/AccountList.tsx, src/components/currencies/CurrencyList.tsx</files>
  <read_first>
    .planning/phases/02-currencies-accounts/02-CONTEXT.md
    .planning/phases/02-currencies-accounts/02-RESEARCH.md
    src/app/accounts/actions.ts
    src/app/currencies/actions.ts
    src/app/api/health/route.test.ts
  </read_first>
  <behavior>
    - updateAccountName ignores tampered type/currencyCode/creditLimit fields and only writes name
    - updateCurrencyName ignores tampered code/scale/isPrimary and only writes name
    - actions modules export create/update helpers only — no removal action export names
    - Row edit buttons under currencies/ and accounts/ components use outline or ghost variants only
  </behavior>
  <action>
    Per D-04, D-07, D-08, D-11, D-14, D-15: harden update actions to read only name from FormData. Add src/app/accounts/actions.test.ts and src/app/currencies/actions.test.ts (mock prisma like health route tests) proving name-only updates and that module exports do not include removal action symbols. Grep-harden lists/dialogs so row actions use outline/ghost edit only (UI-SPEC: error styling stays on aria-invalid fields, not row CTAs). Map Prisma unique violations to Russian duplicate messages. Ensure createCurrency still forces isPrimary false.
  </action>
  <verify>
    <automated>npm test -- --run src/app/accounts/actions.test.ts src/app/currencies/actions.test.ts src/lib/validations/account.test.ts src/lib/validations/currency.test.ts && grep -R --include='*.tsx' -E 'variant=\{?"?(outline|ghost)' src/components/accounts src/components/currencies | grep -q .</automated>
    <fails_when>action immutability tests fail, validation tests fail, or no outline/ghost row-action variants found under account/currency components</fails_when>
  </verify>
  <acceptance_criteria>
    - accounts/actions.test.ts and currencies/actions.test.ts exist and pass
    - update actions only pass name into prisma update data
    - Account and currency row edit CTAs use outline or ghost Button variants
  </acceptance_criteria>
  <done>Immutability and no-removal guarantees tested</done>
</task>

<task type="checkpoint:human-verify" gate="blocking-human">
  <name>Russian UI smoke on /currencies and /accounts</name>
  <read_first>
    .planning/phases/02-currencies-accounts/02-UI-SPEC.md
    .planning/phases/02-currencies-accounts/02-CONTEXT.md
  </read_first>
  <what-built>Currencies and accounts list pages with Dialog create/edit, top nav, seeded RUB primary, credit-limit metadata on credit accounts</what-built>
  <how-to-verify>
    1. Ensure DB migrated (Plan 02 deploy) and start the app (npm run dev or docker compose up)
    2. Open http://127.0.0.1:3000/ — ready page still works; nav shows Готовность · Валюты · Счета
    3. Open /currencies — seeded RUB with «Основная»; create a secondary currency; edit name only; confirm no removal control
    4. Open /accounts — empty state Russian copy; create one non-credit and one credit account with limit; edit names only; confirm limit/type/currency read-only on edit; confirm no removal control
    5. Reply approved if chrome matches UI-SPEC Russian copy contract
  </how-to-verify>
  <action>
    Halt for human visual/locale confirmation of D-17–D-20 and UI-SPEC copywriting after automated tests are green. Human verifies Russian nav/labels/empty states/Dialogs and absence of removal controls. Record approval in resume signal.
  </action>
  <resume-signal>Reply: approved RU UI currencies+accounts (or list blocking UI defects)</resume-signal>
  <verify>
    <automated>npm test</automated>
    <fails_when>full suite exits non-zero before human review</fails_when>
    <human-check>
      <what-built>Russian currencies and accounts CRUD chrome (create/edit only)</what-built>
      <how-to-verify>Walk /currencies and /accounts Dialogs; confirm UI-SPEC Russian strings and no removal affordances</how-to-verify>
    </human-check>
  </verify>
  <acceptance_criteria>
    - Human approved Russian UI smoke for /currencies and /accounts
    - npm test exits 0 at checkpoint time
  </acceptance_criteria>
  <done>Human confirmed Phase 2 Russian UI contract</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Tampered edit FormData → update actions | Identity fields must not change after create |
| UI affordances → user intent | Removal chrome would violate D-07/D-14 product posture |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-02-01 | Tampering | updateAccountName / updateCurrencyName | high | mitigate | Name-only updates + unit tests with extra FormData keys |
| T-02-02 | Tampering | createCurrency isPrimary | high | mitigate | Retain hardcoded false; covered by currency action tests |
| T-02-10 | Repudiation / Tampering | missing removal exports | medium | mitigate | Export tests assert create/update-only surface |
| T-02-07 | Spoofing | creditLimit display | medium | mitigate | formatMinorToMajor only; still metadata in list |
| T-02-SC | Tampering | dependency tree | low | accept | No new packages in this plan |
</threat_model>

## Artifacts this phase produces

| Symbol / path | Kind | Introduced in |
|---------------|------|---------------|
| Four-type account create UX | UI | 02-03 |
| `src/app/accounts/actions.test.ts` | test | 02-03 |
| `src/app/currencies/actions.test.ts` | test | 02-03 |
| Human-approved RU UI smoke | checkpoint | 02-03 |

<verification>
Full npm test green; four types in Dialog; immutability tests pass; human RU UI approval.
</verification>

<success_criteria>
User can manage all account types and rename currencies/accounts without changing locked identity fields; no removal UI (CONTEXT-narrowed ACCT-01/CURR-01).
</success_criteria>

<output>
Create `.planning/phases/02-currencies-accounts/02-03-SUMMARY.md` when done
</output>
