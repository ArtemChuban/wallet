---
phase: 02-currencies-accounts
plan: 02
type: execute
wave: 2
depends_on:
  - 02-01
files_modified:
  - prisma/schema.prisma
  - prisma/migrations/
  - src/lib/validations/account.ts
  - src/lib/validations/account.test.ts
  - src/lib/money.test.ts
  - src/lib/foundation.test.ts
  - src/app/accounts/page.tsx
  - src/app/accounts/actions.ts
  - src/components/accounts/AccountList.tsx
  - src/components/accounts/AccountFormDialog.tsx
autonomous: true
requirements:
  - ACCT-01
  - ACCT-02
estimate:
  tokens: 46000
  raw_tokens: 46000
  tasks: 3
  confidence: low
must_haves:
  truths:
    - User can open /accounts and create a FIAT_CREDIT account with required credit limit greater than zero stored as creditLimitMinor BigInt metadata (D-09, D-10, ACCT-02)
    - Credit limit input uses major-unit decimal string parsed with the selected currency scale via parseMajorToMinor (D-09, UI interaction contract)
    - Credit limit displays via formatMinorToMajor plus currency code — never raw minor BigInt (UI-SPEC)
    - Non-credit create rejects or ignores credit limit; FIAT_CREDIT without limit fails with «Укажите кредитный лимит» (ACCT-02 empty/adjacency)
    - Credit limit of exactly 0 fails with «Введите сумму больше 0»; smallest positive major for scale is accepted (ACCT-02 boundary)
    - Duplicate account name rejected with «Счёт с таким названием уже есть» (ACCT-01 idempotency/adjacency)
    - Account list empty state uses «Нет счетов» heading/body and CTA «Добавить счёт» (UI empty E2)
    - Credit-limit field visible/required only when type is FIAT_CREDIT (UI partial E4)
    - Account rows show Russian type label, currency code, and formatted limit for credit (UI populated E2)
    - statement: Long account names (≤120) truncate with ellipsis in the list; full name editable in Dialog
      verification: backstop
  prohibitions:
    - statement: MUST NOT persist outstanding debt as an Account column in this phase (debt is Phase 3 balance snapshots per D-09)
      requirement_id: ACCT-02
      category: values
    - statement: MUST NOT treat creditLimitMinor as an asset or include it in any net-worth helper as positive wealth
      requirement_id: ACCT-02
      category: values
    - statement: MUST NOT expose account removal controls or Server Actions that delete Account rows
      requirement_id: ACCT-01
      category: values
  artifacts:
    - path: prisma/schema.prisma
      provides: AccountType enum and Account model with creditLimitMinor BigInt optional metadata
    - path: prisma/migrations
      provides: Account table migration applied via migrate deploy
    - path: src/lib/validations/account.ts
      provides: Zod create/update with FIAT_CREDIT credit-limit superRefine
    - path: src/app/accounts/actions.ts
      provides: createAccount and updateAccountName Server Actions
    - path: src/app/accounts/page.tsx
      provides: Russian RSC list at /accounts
  key_links:
    - from: src/app/accounts/actions.ts
      to: prisma.account
      via: Zod + parseMajorToMinor for creditLimitMinor
      pattern: "createAccount|creditLimitMinor"
    - from: Account.currencyCode
      to: Currency.code
      via: Prisma relation FK
      pattern: "currencyCode"
    - from: src/components/nav.tsx
      to: /accounts
      via: Next Link (from Plan 01)
      pattern: "/accounts"
---

## Phase Goal

**As a** local Wallet user, **I want to** create typed accounts including a credit account with required credit-limit metadata, **so that** assets and credit cards are named before dated balances exist (ACCT-01, ACCT-02).

<objective>
Add Account + AccountType, migrate, and ship the accounts vertical tracer: create FIAT_CREDIT with creditLimitMinor metadata through Russian Dialog UI.

Purpose: Prove ACCT-01 create path and ACCT-02 limit-as-metadata on one end-to-end slice.
Output: Account schema/migration, account Zod + actions + `/accounts` UI.
CONTEXT narrowing: Phase 2 stores creditLimit only (D-09); outstanding debt and account removal deferred (D-14 / Phase 3 / ACCT-04).
</objective>

<execution_context>
@/home/artem/Documents/wallet/.cursor/gsd-core/workflows/execute-plan.md
@/home/artem/Documents/wallet/.cursor/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/02-currencies-accounts/02-CONTEXT.md
@.planning/phases/02-currencies-accounts/02-RESEARCH.md
@.planning/phases/02-currencies-accounts/02-PATTERNS.md
@.planning/phases/02-currencies-accounts/02-UI-SPEC.md
@.planning/phases/02-currencies-accounts/02-01-PLAN.md
@prisma/schema.prisma
@src/lib/money.ts
@src/lib/db.ts
@src/components/nav.tsx
</context>

<tasks>

<task type="tracer">
  <name>End-to-end create credit account with credit limit metadata</name>
  <reversibility rating="one-way">D-13 locks account currency forever after create; undoing after balances exist would corrupt history.</reversibility>
  <files>prisma/schema.prisma, prisma/migrations/, src/lib/validations/account.ts, src/app/accounts/actions.ts, src/app/accounts/page.tsx, src/components/accounts/AccountList.tsx, src/components/accounts/AccountFormDialog.tsx</files>
  <read_first>
    .planning/phases/02-currencies-accounts/02-CONTEXT.md
    .planning/phases/02-currencies-accounts/02-RESEARCH.md
    .planning/phases/02-currencies-accounts/02-PATTERNS.md
    .planning/phases/02-currencies-accounts/02-UI-SPEC.md
    prisma/schema.prisma
    src/lib/money.ts
    src/lib/validations/currency.ts
    src/app/currencies/actions.ts
    src/components/currencies/CurrencyFormDialog.tsx
    src/components/nav.tsx
  </read_first>
  <action>
    Wire ONE path: create a FIAT_CREDIT account bound to an existing currency with required credit limit metadata (per D-09, D-10, D-12, D-13, D-16, D-17, D-18, D-20). Add Prisma enum AccountType (FIAT_DEBIT, FIAT_CREDIT, CRYPTO, CASH) and model Account with unique name, type, currencyCode FK to Currency, optional creditLimitMinor BigInt, timestamps. Document on creditLimitMinor that it is metadata only and must never be treated as an NW asset. Create a new migration (--create-only then review SQL). Implement src/lib/validations/account.ts: create schema with name, type enum, currencyCode, optional creditLimitMajor string; superRefine requires creditLimitMajor for FIAT_CREDIT and forbids it for other types; Russian messages from UI-SPEC. Update accepts name only. Implement createAccount Server Action: Zod validate; load currency scale; parseMajorToMinor for credit; persist creditLimitMinor only for FIAT_CREDIT else null; never accept a debt field; revalidatePath("/accounts"). Implement updateAccountName name-only. Build /accounts RSC list (force-dynamic, ensureSqlitePragmas) with Russian title «Счета», empty state from UI-SPEC, type labels Дебетовый/Кредитный/Крипто/Наличные, formatted limit for credit rows. AccountFormDialog create: Название, Тип Select, Валюта Select, conditional Кредитный лимит with hint «в единицах выбранной валюты»; edit: name only with type/currency/limit read-only; titles «Новый счёт» / «Изменить название»; CTA «Добавить счёт». Prefer Dialog patterns from Plan 01. No removal UI.
  </action>
  <verify>
    <automated>test -f src/app/accounts/page.tsx && test -f src/app/accounts/actions.ts && test -f src/lib/validations/account.ts && grep -q 'enum AccountType' prisma/schema.prisma && grep -q 'creditLimitMinor' prisma/schema.prisma && grep -q 'model Account' prisma/schema.prisma && grep -q 'createAccount' src/app/accounts/actions.ts && grep -q 'parseMajorToMinor' src/app/accounts/actions.ts && grep -q '/accounts' src/components/nav.tsx</automated>
    <fails_when>accounts page/actions/validation missing, AccountType/Account/creditLimitMinor absent from schema, createAccount or parseMajorToMinor wiring missing, or nav lacks /accounts</fails_when>
  </verify>
  <acceptance_criteria>
    - prisma/schema.prisma contains AccountType and Account.creditLimitMinor as BigInt
    - createAccount persists creditLimitMinor for FIAT_CREDIT using parseMajorToMinor
    - Account form create shows conditional credit limit field for Кредитный
    - No outstanding-debt field on Account model or create form
  </acceptance_criteria>
  <done>Credit-account create path wired end-to-end in code (migrate deploy is Task 3)</done>
</task>

<task type="auto" tdd="true">
  <name>Wave 0 account Zod + creditLimitMinor schema assertions</name>
  <files>src/lib/validations/account.test.ts, src/lib/validations/account.ts, src/lib/money.test.ts</files>
  <read_first>
    .planning/phases/02-currencies-accounts/02-VALIDATION.md
    .planning/phases/02-currencies-accounts/02-UI-SPEC.md
    src/lib/validations/account.ts
    src/lib/money.test.ts
    prisma/schema.prisma
  </read_first>
  <behavior>
    - FIAT_CREDIT create without creditLimitMajor fails; with positive major passes schema
    - FIAT_DEBIT/CRYPTO/CASH with creditLimitMajor fails schema
    - credit limit major "0" fails business rule (> 0) at Zod or action-tested helper level
    - money.test.ts asserts creditLimitMinor BigInt in schema and still rejects Float/Decimal money columns
  </behavior>
  <action>
    Per D-09, D-10, ACCT-01/02: add src/lib/validations/account.test.ts covering create/update Zod rules and credit-limit superRefine (required for FIAT_CREDIT, forbidden otherwise, greater-than-zero). Extend src/lib/money.test.ts schema conventions to require creditLimitMinor BigInt. Keep tests free of debt-field acceptance. Run until green.
  </action>
  <verify>
    <automated>npm test -- --run src/lib/validations/account.test.ts src/lib/money.test.ts</automated>
    <fails_when>vitest exits non-zero or account.test.ts missing</fails_when>
  </verify>
  <acceptance_criteria>
    - account.test.ts covers FIAT_CREDIT required limit and non-credit forbidden limit
    - money.test.ts matches creditLimitMinor BigInt in prisma/schema.prisma
    - npm test command above exits 0
  </acceptance_criteria>
  <done>Account validation Wave 0 tests green</done>
</task>

<task type="auto">
  <name>Task 3: [BLOCKING] prisma migrate deploy for Account</name>
  <files>data/wallet.db, prisma/migrations/, src/lib/foundation.test.ts</files>
  <read_first>
    prisma/schema.prisma
    src/lib/foundation.test.ts
  </read_first>
  <precondition>Account model migration.sql exists from Task 1</precondition>
  <action>
    Schema-gate (Prisma): run DATABASE_URL=file:./data/wallet.db npx prisma migrate deploy and confirm status up to date. Extend foundation.test.ts fresh migrate assertions to expect Account table exists alongside Currency seed RUB primary from Plan 01.
  </action>
  <verify>
    <automated>mkdir -p data && DATABASE_URL="file:./data/wallet.db" npx prisma migrate deploy && test -f data/wallet.db && DATABASE_URL="file:./data/wallet.db" npx prisma migrate status 2>&1 | grep -qi 'up to date\|Database schema is up to date\|No pending migrations' && npm test -- --run src/lib/foundation.test.ts</automated>
    <fails_when>migrate deploy non-zero, pending migrations remain, or foundation tests fail Account/Currency seed checks</fails_when>
  </verify>
  <acceptance_criteria>
    - migrate deploy exits 0 for host file DB
    - foundation fresh-DB test sees Account table and RUB primary
  </acceptance_criteria>
  <done>Blocking Account migrate deploy succeeded</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Browser FormData → createAccount / updateAccountName | Untrusted account fields and credit-limit strings |
| Account.creditLimitMinor → future NW math | Metadata must not cross into assets |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-02-01 | Tampering | accounts/actions.ts update | high | mitigate | updateAccountName accepts name only; ignore type/currency/limit FormData |
| T-02-06 | Tampering | creditLimitMajor input | high | mitigate | Zod + parseMajorToMinor BigInt; reject zero/negative; no float |
| T-02-07 | Spoofing | creditLimit as wealth | high | mitigate | Schema comment metadata-only; no NW helpers in this phase use limit as asset |
| T-02-08 | Tampering | debt field smuggling | medium | mitigate | No debt column; Zod schemas omit debt; reject unknown debt keys if present |
| T-02-09 | Tampering | duplicate account names | medium | mitigate | Prisma unique name + Russian duplicate error mapping |
| T-02-SC | Tampering | npm installs | low | accept | No new packages; reuse Phase 1 pins |
</threat_model>

## Artifacts this phase produces

| Symbol / path | Kind | Introduced in |
|---------------|------|---------------|
| `AccountType` enum | Prisma | 02-02 |
| `Account` + `creditLimitMinor` | Prisma model/field | 02-02 |
| `createAccount` / `updateAccountName` | Server Actions | 02-02 |
| `/accounts` RSC + Dialog | UI | 02-02 |
| `src/lib/validations/account.ts` | Zod | 02-02 |

<verification>
Account migrate deploy up to date; account + money tests green; createAccount path uses parseMajorToMinor for credit limits.
</verification>

<success_criteria>
User can create a credit account with required credit-limit metadata stored as BigInt minor units (ACCT-01 create, ACCT-02 limit portion).
</success_criteria>

<output>
Create `.planning/phases/02-currencies-accounts/02-02-SUMMARY.md` when done
</output>
