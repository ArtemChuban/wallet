---
phase: 02-currencies-accounts
plan: 05
type: execute
wave: 5
depends_on:
  - 02-04
files_modified:
  - src/components/currencies/CurrencyFormDialog.tsx
  - src/components/accounts/AccountFormDialog.tsx
autonomous: true
gap_closure: true
gap_ids:
  - G-02-1
  - G-02-2
requirements:
  - CURR-01
  - ACCT-01
estimate:
  tokens: 28000
  raw_tokens: 28000
  tasks: 2
  confidence: low
must_haves:
  truths:
    - Editing a currency name and saving does not emit Base UI FieldControl uncontrolled default-value console error (G-02-1)
    - Editing an account name and saving does not emit Base UI FieldControl uncontrolled default-value console error (G-02-2)
    - Currency and account name edit still submits via FormData field name and closes dialog on success
    - formKey remount on dialog open still resets local name state for a fresh edit session
  artifacts:
    - path: src/components/currencies/CurrencyFormDialog.tsx
      provides: Controlled edit/create name Input for CurrencyFormBody (G-02-1)
    - path: src/components/accounts/AccountFormDialog.tsx
      provides: Controlled edit/create name Input for AccountFormBody (G-02-2)
  key_links:
    - from: CurrencyFormBody name useState
      to: Input value/onChange + name FormData
      via: controlled binding stable across revalidatePath while mounted
    - from: AccountFormBody name useState
      to: Input value/onChange + name FormData
      via: controlled binding stable across revalidatePath while mounted
    - from: updateCurrencyName / updateAccountName revalidatePath
      to: still-open FormBody
      via: list prop refresh must not flip Input control mode
---

<objective>
Close UAT blockers G-02-1 and G-02-2: stop Base UI FieldControl warnings when renaming currencies/accounts after Server Action revalidation.

Purpose: Same root-cause class in both dialogs — live prop bound into uncontrolled name Input while dialog stays mounted through revalidatePath. Prefer controlled name fields for edit (and create) so RSC refresh cannot change the control after init.
Output: CurrencyFormDialog + AccountFormDialog with mount-init useState name + value/onChange; existing actions/lists untouched.
</objective>

<execution_context>
@/home/artem/Documents/wallet/.cursor/gsd-core/workflows/execute-plan.md
@/home/artem/Documents/wallet/.cursor/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/02-currencies-accounts/02-UAT.md
@.planning/debug/currency-uncontrolled-fieldcontrol.md
@.planning/debug/account-uncontrolled-fieldcontrol.md
@src/components/currencies/CurrencyFormDialog.tsx
@src/components/accounts/AccountFormDialog.tsx
</context>

<tasks>

<task type="tracer">
  <name>End-to-end controlled currency name field (G-02-1)</name>
  <files>src/components/currencies/CurrencyFormDialog.tsx</files>
  <read_first>
    src/components/currencies/CurrencyFormDialog.tsx
    .planning/debug/currency-uncontrolled-fieldcontrol.md
    .planning/phases/02-currencies-accounts/02-UAT.md
  </read_first>
  <action>
    In CurrencyFormBody only (G-02-1): add local React state for the Название field, initialized once at mount — edit mode from currency.name, create mode empty string. Bind the Input with id currency-name to that state via value and onChange (set from event.target.value). Keep the FormData attribute name="name", maxLength, aria-invalid, and disabled={isPending} behavior. Do not add a useEffect that copies currency.name into state after mount (revalidatePath while dialog open is the bug trigger; formKey remount on open already refreshes init). Leave createCurrency/updateCurrencyName, hidden code field, scale Select, success close useEffect, and CurrencyFormDialog shell unchanged. Do not modify actions.ts or CurrencyList.tsx.
  </action>
  <verify>
    <automated>grep -A6 'id="currency-name"' src/components/currencies/CurrencyFormDialog.tsx | grep -q 'value={name}' && grep -A6 'id="currency-name"' src/components/currencies/CurrencyFormDialog.tsx | grep -q 'onChange' && grep -q 'const \[name, setName\] = useState' src/components/currencies/CurrencyFormDialog.tsx && test "$(grep -c defaultValue src/components/currencies/CurrencyFormDialog.tsx)" -eq 0 && npm test -- --run src/app/currencies/actions.test.ts src/lib/validations/currency.test.ts</automated>
  </verify>
  <done>Currency name Input is controlled from mount-init state; currency actions tests still pass; G-02-1 code path fixed in CurrencyFormDialog.</done>
</task>

<task type="auto">
  <name>Controlled account name field (G-02-2)</name>
  <files>src/components/accounts/AccountFormDialog.tsx</files>
  <read_first>
    src/components/accounts/AccountFormDialog.tsx
    src/components/currencies/CurrencyFormDialog.tsx
    .planning/debug/account-uncontrolled-fieldcontrol.md
  </read_first>
  <action>
    Mirror the G-02-1 CurrencyFormBody pattern in AccountFormBody (G-02-2): local name state init from account.name on edit or empty string on create; bind Input id account-name with value/onChange; keep name="name" for FormData and existing pending/error props. Do not sync name from account props after mount. Leave type/currency Selects, credit-limit create Input, edit read-only chrome, actions, and AccountList unchanged.
  </action>
  <verify>
    <automated>grep -A6 'id="account-name"' src/components/accounts/AccountFormDialog.tsx | grep -q 'value={name}' && grep -A6 'id="account-name"' src/components/accounts/AccountFormDialog.tsx | grep -q 'onChange' && grep -q 'const \[name, setName\] = useState' src/components/accounts/AccountFormDialog.tsx && test "$(grep -c defaultValue src/components/accounts/AccountFormDialog.tsx)" -eq 0 && test "$(grep -c defaultValue src/components/currencies/CurrencyFormDialog.tsx)" -eq 0 && npm test</automated>
    <human-check>
      <prompt>Re-run UAT edit flows: rename a currency and an account. Confirm Browser/Next console shows no Base UI FieldControl uncontrolled default-value warning for CurrencyFormBody or AccountFormBody. Confirm names still save and dialogs close.</prompt>
      <pass>No FieldControl default-value console error on currency or account rename; names persist</pass>
      <fail>Console warning still appears, or rename fails / dialog stuck open</fail>
    </human-check>
  </verify>
  <done>Both dialogs use controlled name fields; full npm test green; G-02-1 and G-02-2 ready for UAT re-check.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Client Dialog FormData → updateCurrencyName / updateAccountName | Name string still untrusted; server Zod unchanged |
| RSC revalidate → mounted client FormBody | Prop refresh must not alter control contract mid-edit |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-02-11 | Tampering | CurrencyFormDialog / AccountFormDialog name Input | low | accept | Controlled UI state only; Server Actions still validate name length/content |
| T-02-12 | Information Disclosure | name field in client state | low | accept | Same data already rendered in list/dialog; no new secret surface |
| T-02-SC | Tampering | dependency tree | low | accept | No new packages in this plan |
</threat_model>

## Source audit (gap closure)

| SOURCE | ID | Item | Plan | Status |
|--------|----|------|------|--------|
| UAT | G-02-1 | Currency FieldControl default-value warning | 02-05 | COVERED |
| UAT | G-02-2 | Account FieldControl default-value warning | 02-05 | COVERED |
| GOAL | — | Currencies + accounts CRUD without console blockers | 02-05 | COVERED (gap slice) |
| REQ | CURR-01 | Currency create/edit name | 02-05 | COVERED (edit UX stability) |
| REQ | ACCT-01 | Account create/edit name | 02-05 | COVERED (edit UX stability) |

<verification>
Static: both dialogs have value={name} on name Inputs and zero defaultValue usages. Automated: npm test. Human (end-of-phase): rename currency + account with clean console.
</verification>

<success_criteria>
G-02-1 and G-02-2 closed: rename flows work without Base UI uncontrolled FieldControl console errors; CURR-01/ACCT-01 name edit still persists.
</success_criteria>

<output>
Create `.planning/phases/02-currencies-accounts/02-05-SUMMARY.md` when done
</output>
