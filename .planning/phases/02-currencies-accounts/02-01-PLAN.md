---
phase: 02-currencies-accounts
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - prisma/schema.prisma
  - prisma/migrations/
  - src/lib/money.ts
  - src/lib/money.test.ts
  - src/lib/validations/currency.ts
  - src/lib/validations/currency.test.ts
  - src/lib/foundation.test.ts
  - src/app/layout.tsx
  - src/components/nav.tsx
  - src/app/currencies/page.tsx
  - src/app/currencies/actions.ts
  - src/components/currencies/CurrencyList.tsx
  - src/components/currencies/CurrencyFormDialog.tsx
  - src/components/ui/dialog.tsx
  - src/components/ui/input.tsx
  - src/components/ui/label.tsx
  - src/components/ui/select.tsx
autonomous: true
requirements:
  - CURR-01
estimate:
  tokens: 48000
  raw_tokens: 48000
  tasks: 3
  confidence: low
must_haves:
  truths:
    - User can open /currencies and see seeded primary RUB (code RUB, name Рубль, scale 2) after migrate deploy (D-01, CURR-01)
    - User can create a non-primary currency via Dialog with free-form code, name, and scale 0–18 (D-05, D-06, D-18, D-20)
    - Create always persists isPrimary false; no UI control sets primary (D-02)
    - User can edit currency name only; code and scale stay read-only after create (D-04, D-08)
    - Duplicate currency code is rejected with Russian field error «Код валюты уже существует» (CURR-01 adjacency/idempotency)
    - Empty/whitespace name or code fails Zod validation; scale outside 0–18 fails with «Укажите масштаб от 0 до 18» (CURR-01 empty/boundary)
    - Currency list orders rows by code ascending for stable equal-meta display (CURR-01 ordering)
    - Concurrent second-primary insert cannot succeed while partial unique index Currency_one_primary exists (CURR-01 concurrency)
    - Zero currencies empty state uses UI-SPEC heading/body plus CTA «Добавить валюту» (UI empty E1)
    - Populated currency rows show name, mono code, scale, and «Основная» badge when isPrimary (UI populated E1)
    - Submit pending disables primary CTA and keeps Dialog open until success (UI loading E3)
    - statement: Long currency names (≤120) truncate with ellipsis in the list; full name editable in Dialog
      verification: backstop
  prohibitions:
    - statement: MUST NOT expose primary-currency switch or any create path that sets isPrimary true
      requirement_id: CURR-01
      category: values
    - statement: MUST NOT expose currency removal controls or Server Actions that delete Currency rows (includes primary — D-03, D-07)
      requirement_id: CURR-01
      category: values
  artifacts:
    - path: prisma/schema.prisma
      provides: Currency.isPrimary Boolean and accounts relation stub-ready for Plan 02
    - path: prisma/migrations
      provides: customized migration INSERT RUB primary + Currency_one_primary partial unique index
    - path: src/lib/validations/currency.ts
      provides: Zod create/update schemas for CURR-01
    - path: src/app/currencies/page.tsx
      provides: Russian RSC list at /currencies
    - path: src/app/currencies/actions.ts
      provides: createCurrency and updateCurrencyName Server Actions
    - path: src/components/nav.tsx
      provides: top nav Готовность · Валюты · Счета
    - path: src/lib/money.ts
      provides: parseMajorToMinor and formatMinorToMajor BigInt helpers
  key_links:
    - from: prisma/migrations/*/migration.sql
      to: Currency row RUB
      via: INSERT seed in migrate deploy
      pattern: "RUB"
    - from: src/app/currencies/actions.ts
      to: prisma.currency
      via: Zod safeParse then create/update name-only
      pattern: "createCurrency|updateCurrencyName"
    - from: src/components/nav.tsx
      to: /currencies
      via: Next Link
      pattern: "/currencies"
---

## Phase Goal

**As a** local Wallet user, **I want to** define free-form currencies with exactly one forever-seeded primary, **so that** accounts and later FX/NW can bind to a trustworthy currency catalog (CURR-01).

<objective>
Land the production-quality currency vertical slice: schema primary flag, migration-seeded RUB, Server Actions, Russian `/currencies` Dialog UI, and nav — then block on migrate deploy.

Purpose: Prove CURR-01 end-to-end before account CRUD expands in Plan 02.
Output: Migrated Currency.isPrimary + seed, money parse/format helpers, currency Zod + actions + UI, top nav.
Phase 2 interprets ROADMAP/REQUIREMENTS per CONTEXT: no currency removal UI (D-07); no primary switch (D-02).
</objective>

<execution_context>
@/home/artem/Documents/wallet/.cursor/gsd-core/workflows/execute-plan.md
@/home/artem/Documents/wallet/.cursor/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/02-currencies-accounts/02-CONTEXT.md
@.planning/phases/02-currencies-accounts/02-RESEARCH.md
@.planning/phases/02-currencies-accounts/02-PATTERNS.md
@.planning/phases/02-currencies-accounts/02-UI-SPEC.md
@.planning/phases/02-currencies-accounts/02-VALIDATION.md
@prisma/schema.prisma
@src/lib/money.ts
@src/lib/db.ts
@src/app/layout.tsx
@src/app/page.tsx
</context>

<assumptions>
## Planner assumptions (specless / discretion)

- Currency code charset: printable ASCII, Zod min 1 max 16 (RESEARCH open question A — discretion).
- List sort by `code` ascending is the stable ordering contract for CURR-01.
- Dialog preferred over Sheet (RESEARCH A2 / UI-SPEC).
- Top nav in root layout (RESEARCH A3 / UI-SPEC).
</assumptions>

<tasks>

<task type="tracer">
  <name>End-to-end seeded RUB primary + create secondary currency</name>
  <reversibility rating="one-way">D-01 seed migration and exactly-one primary are a data contract for later FX and NW; changing seed or primary rules after real rows needs a data migration.</reversibility>
  <files>prisma/schema.prisma, prisma/migrations/, src/lib/money.ts, src/lib/validations/currency.ts, src/app/currencies/actions.ts, src/app/currencies/page.tsx, src/components/currencies/CurrencyList.tsx, src/components/currencies/CurrencyFormDialog.tsx, src/components/nav.tsx, src/app/layout.tsx, src/components/ui/dialog.tsx, src/components/ui/input.tsx, src/components/ui/label.tsx, src/components/ui/select.tsx</files>
  <read_first>
    .planning/phases/02-currencies-accounts/02-CONTEXT.md
    .planning/phases/02-currencies-accounts/02-RESEARCH.md
    .planning/phases/02-currencies-accounts/02-PATTERNS.md
    .planning/phases/02-currencies-accounts/02-UI-SPEC.md
    prisma/schema.prisma
    src/lib/money.ts
    src/lib/db.ts
    src/app/layout.tsx
    src/components/ui/button.tsx
    components.json
  </read_first>
  <action>
    Wire ONE happy path through every layer this plan owns (per D-01, D-02, D-05, D-06, D-17, D-18, D-19, D-20). Extend Currency in prisma/schema.prisma with isPrimary Boolean default false and document in a schema comment that exactly-one primary is enforced by migration SQL partial unique index Currency_one_primary. Keep FxRateStub and BalanceAmountStub unchanged. Do not add Account in this plan. Create a new Prisma migration with --create-only (or equivalent non-interactive create), then customize migration.sql to ADD isPrimary, INSERT primary row code RUB name Рубль scale 2 isPrimary 1, and CREATE UNIQUE INDEX Currency_one_primary ON Currency(isPrimary) WHERE isPrimary = 1 (RESEARCH Pattern 1 — seed must live in migrate deploy path, not prisma db seed). Install shadcn dialog input label select via official CLI into src/components/ui (no new npm packages beyond CLI-copied components). Extend src/lib/money.ts with parseMajorToMinor and formatMinorToMajor using BigInt only (no Number/parseFloat; reject scientific notation; scale 0–18). Add src/lib/validations/currency.ts Zod schemas: create requires trimmed code/name and integer scale 0–18 with Russian messages from UI-SPEC; update accepts name only. Implement src/app/currencies/actions.ts Server Actions createCurrency (hardcode isPrimary false; never read primary from FormData) and updateCurrencyName (name only; ignore tampered code/scale). Use ensureSqlitePragmas, prisma, revalidatePath("/currencies"), useActionState-compatible return shape with fieldErrors. Build RSC src/app/currencies/page.tsx (force-dynamic) listing currencies ordered by code, Russian title «Валюты», empty-state copy from UI-SPEC, «Основная» badge for primary. Client CurrencyFormDialog: create fields Код/Название/Масштаб; edit name only with code/scale read-only; titles «Новая валюта» / «Изменить название»; CTA «Добавить валюту» / «Сохранить». Add src/components/nav.tsx top nav links Готовность · Валюты · Счета and mount in layout above children; keep / ready page (D-19). No removal UI or primary toggle.
  </action>
  <verify>
    <automated>test -f src/app/currencies/page.tsx && test -f src/app/currencies/actions.ts && test -f src/lib/validations/currency.ts && grep -q 'isPrimary' prisma/schema.prisma && grep -q 'RUB' prisma/migrations/*/migration.sql && grep -q 'Currency_one_primary' prisma/migrations/*/migration.sql && grep -q 'parseMajorToMinor' src/lib/money.ts && grep -q 'createCurrency' src/app/currencies/actions.ts && grep -q 'updateCurrencyName' src/app/currencies/actions.ts && grep -q '/currencies' src/components/nav.tsx && test -f src/components/ui/dialog.tsx</automated>
    <fails_when>any required file missing, isPrimary/RUB seed/partial index/createCurrency/updateCurrencyName/nav link/dialog component absent, or money parse helper missing</fails_when>
  </verify>
  <acceptance_criteria>
    - prisma/schema.prisma defines Currency.isPrimary
    - A new migration.sql under prisma/migrations contains INSERT for RUB and Currency_one_primary
    - createCurrency hardcodes non-primary create (grep createCurrency action body for isPrimary false assignment)
    - updateCurrencyName path exists; create/edit Dialog components exist under src/components/currencies/
    - Nav contains /currencies and Russian label Валюты
    - No primary-toggle form field in currency create Dialog source
  </acceptance_criteria>
  <done>Single currency happy path wired end-to-end in code (migrate deploy is Task 3)</done>
</task>

<task type="auto" tdd="true">
  <name>Wave 0 currency Zod + money parse/format tests</name>
  <files>src/lib/validations/currency.test.ts, src/lib/money.test.ts, src/lib/money.ts, src/lib/validations/currency.ts</files>
  <read_first>
    .planning/phases/02-currencies-accounts/02-VALIDATION.md
    .planning/phases/02-currencies-accounts/02-UI-SPEC.md
    src/lib/money.test.ts
    src/lib/validations/currency.ts
    src/lib/money.ts
  </read_first>
  <behavior>
    - currency create schema accepts code USDT, name, scale 0 and 18; rejects scale -1 and 19
    - currency create rejects empty name/code; update schema accepts name only
    - parseMajorToMinor/formatMinorToMajor round-trip for scales 0, 2, 8, 18 without float
    - money.test schema conventions still forbid Float/Decimal money columns and still assert RATE_SCALE_E8
  </behavior>
  <action>
    Per D-06 and Phase 1 money contract: author src/lib/validations/currency.test.ts covering CURR-01 Zod rules (scale bounds, required fields, update name-only shape). Extend src/lib/money.test.ts with round-trip cases for parseMajorToMinor/formatMinorToMajor at scales 0, 2, 8, 18 and keep existing RATE_SCALE_E8 plus schema BigInt greps. Fix implementations until npm test for these files is green. Do not add Account creditLimitMinor schema assertions until Plan 02 adds the column (avoid false red).
  </action>
  <verify>
    <automated>npm test -- --run src/lib/validations/currency.test.ts src/lib/money.test.ts</automated>
    <fails_when>vitest exits non-zero or either test file is missing</fails_when>
  </verify>
  <acceptance_criteria>
    - src/lib/validations/currency.test.ts exists and passes
    - src/lib/money.test.ts covers parse/format round-trips for scales 0, 2, 8, 18
    - npm test command above exits 0
  </acceptance_criteria>
  <done>Wave 0 currency and money unit tests green</done>
</task>

<task type="auto">
  <name>Task 3: [BLOCKING] prisma migrate deploy host gate</name>
  <files>data/wallet.db, prisma/migrations/, src/lib/foundation.test.ts</files>
  <read_first>
    .planning/phases/02-currencies-accounts/02-RESEARCH.md
    prisma/schema.prisma
    src/lib/foundation.test.ts
  </read_first>
  <precondition>prisma/migrations contains the new currencies migration.sql from Task 1 including RUB INSERT</precondition>
  <action>
    Schema-gate (Prisma): after schema/migration files are complete, run host migrate deploy with DATABASE_URL=file:./data/wallet.db (same proven Phase 01 command shape). Confirm migrate status reports up to date. Extend src/lib/foundation.test.ts migrate-deploy fresh-DB assertion to expect seeded RUB primary (isPrimary=1) and Currency_one_primary present after deploy on a temp file DB — keep existing init_platform_stub expectations and add the new migration name check. Do not COPY wallet.db into Docker.
  </action>
  <verify>
    <automated>mkdir -p data && DATABASE_URL="file:./data/wallet.db" npx prisma migrate deploy && test -f data/wallet.db && DATABASE_URL="file:./data/wallet.db" npx prisma migrate status 2>&1 | grep -qi 'up to date\|Database schema is up to date\|No pending migrations' && npm test -- --run src/lib/foundation.test.ts</automated>
    <fails_when>migrate deploy non-zero, data/wallet.db missing, pending migrations remain, or foundation.test.ts fails seed/primary assertions</fails_when>
  </verify>
  <acceptance_criteria>
    - DATABASE_URL=file:./data/wallet.db npx prisma migrate deploy exits 0
    - Fresh migrate in foundation test sees Currency row RUB with primary flag
    - prisma migrate status reports no pending migrations
  </acceptance_criteria>
  <done>Blocking migrate deploy succeeded; seed primary proven in automated foundation test</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Browser FormData → Server Actions | Untrusted create/update fields cross into SQLite writes |
| Migration SQL → host SQLite file | DDL + seed INSERT materialize durable currency catalog |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-02-01 | Tampering | currencies/actions.ts | high | mitigate | Zod safeParse; update name-only; ignore extra FormData keys for code/scale/primary |
| T-02-02 | Tampering | createCurrency isPrimary | high | mitigate | Hardcode isPrimary false; partial unique index blocks second primary |
| T-02-03 | Elevation of Privilege | primary designation | medium | mitigate | No switch UI (D-02); seed-only primary |
| T-02-04 | Denial of Service | scale/code inputs | medium | mitigate | Zod max lengths and scale 0–18 bounds |
| T-02-05 | Tampering | money.ts parse | high | mitigate | BigInt parse only; no float path for major→minor |
| T-02-SC | Tampering | shadcn CLI components | low | accept | Official @shadcn registry only; no third-party registries (UI-SPEC) |
</threat_model>

## Artifacts this phase produces

| Symbol / path | Kind | Introduced in |
|---------------|------|---------------|
| `Currency.isPrimary` | Prisma field | 02-01 |
| Migration seed `RUB` / `Currency_one_primary` | SQL | 02-01 |
| `parseMajorToMinor` / `formatMinorToMajor` | money helpers | 02-01 |
| `createCurrency` / `updateCurrencyName` | Server Actions | 02-01 |
| `/currencies` RSC page + Dialog | UI | 02-01 |
| `src/components/nav.tsx` | nav | 02-01 |
| shadcn `dialog`/`input`/`label`/`select` | UI primitives | 02-01 |

<verification>
Host migrate deploy up to date; currency + money unit tests green; nav and /currencies files present with RUB seed in migration SQL.
</verification>

<success_criteria>
User can rely on seeded primary RUB and create/edit secondary currency names through Russian Dialog UI after migrate (CURR-01).
</success_criteria>

<output>
Create `.planning/phases/02-currencies-accounts/02-01-SUMMARY.md` when done
</output>
