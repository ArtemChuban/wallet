---
phase: 17-nw-forecast-overlay-isolation
plan: 03
type: execute
wave: 3
depends_on:
  - "17-01"
  - "17-02"
files_modified:
  - src/components/dashboard/NetWorthHistoryChart.tsx
  - src/components/dashboard/DashboardChartsShell.tsx
  - src/components/dashboard/nw-forecast-ui.test.ts
  - .planning/phases/17-nw-forecast-overlay-isolation/17-VALIDATION.md
autonomous: true
requirements:
  - FCST-01
estimate:
  tokens: 30000
  raw_tokens: 60000
  tasks: 3
  confidence: high
must_haves:
  truths:
    - "Forecast is dashed Line on ComposedChart (not filled Area); strokeDasharray 5 5; muted-foreground stroke; stepAfter (D-09, UI-SPEC)"
    - "Legend shows «Прогноз» when showForecast; visible even with single account (D-10, UI zero-one-many)"
    - "Tooltip: date ≤ today → account stack + «Итого»; date > today → «Прогноз» + amount only (D-11, UI populated)"
    - "Vertical ReferenceLine at today when forecast shown; today + horizonEnd present in merged data (D-12, D-07)"
    - "Partial banner «Прогноз неполный · нет курса» role=status near NW chart when isPartialForecast; all FX-excluded keeps banner + hides Line (D-15, D-16, UI partial)"
    - "No includable slots → hide Line + legend entry; fact chart continues; no empty toast (UI empty E1, D-08)"
    - "Long account names use existing legend/scroll; sparse future points hold flat via stepAfter (UI overflow)"
    - "Partial banner wraps in p-4 card without honesty ellipsis (UI long-text backstop)"
    - "Tooltip amounts use mono formatChartNumber without money ellipsis (UI long-text backstop)"
    - "Forecast chrome never mounts /income stats UI (carried lock)"
  prohibitions:
    - statement: MUST NOT paint forecast as solid Area
      requirement_id: FCST-01
      category: honesty
    - statement: MUST NOT invent fake account stack in future tooltips
      requirement_id: FCST-01
      category: honesty
    - statement: MUST NOT reuse IncomeList CPTY stats chrome on Капитал
      requirement_id: FCST-01
      category: isolation
  artifacts:
    - path: src/components/dashboard/NetWorthHistoryChart.tsx
      provides: ComposedChart + Line + ReferenceLine + tooltip/legend split
    - path: src/components/dashboard/DashboardChartsShell.tsx
      provides: partial banner mount + showForecast merge complete
    - path: src/components/dashboard/nw-forecast-ui.test.ts
      provides: green chart forecast file-scan
    - path: .planning/phases/17-nw-forecast-overlay-isolation/17-VALIDATION.md
      provides: task IDs mapped + wave_0_complete true
  key_links:
    - from: buildNetWorthForecastSeries isPartialForecast
      to: shell partial banner
      via: role=status near NW chart
      pattern: "Прогноз неполный"
    - from: showForecast / today
      to: ReferenceLine + Line
      via: NetWorthHistoryChart props
      pattern: "ReferenceLine"
    - from: asOfDate vs today
      to: tooltip branch
      via: fact stacks vs Прогноз-only
      pattern: "Прогноз"
---

<objective>
Finish Капитал forecast chrome per UI-SPEC (ComposedChart Line/ReferenceLine/tooltip/legend/partial banner) and close Nyquist VALIDATION map (FCST-01, D-09..D-16).

Purpose: Make overlay readable as forecast-not-fact and sampleable under Nyquist.
Output: Green nw-forecast-ui file-scan; UI-SPEC chrome on `/`; 17-VALIDATION task IDs + wave_0_complete.
</objective>

<execution_context>
@/home/artem/Documents/wallet/.cursor/gsd-core/workflows/execute-plan.md
@/home/artem/Documents/wallet/.cursor/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/17-nw-forecast-overlay-isolation/17-CONTEXT.md
@.planning/phases/17-nw-forecast-overlay-isolation/17-UI-SPEC.md
@.planning/phases/17-nw-forecast-overlay-isolation/17-RESEARCH.md
@.planning/phases/17-nw-forecast-overlay-isolation/17-PATTERNS.md
@.planning/phases/17-nw-forecast-overlay-isolation/17-VALIDATION.md
@.planning/phases/17-nw-forecast-overlay-isolation/17-01-PLAN.md
@src/components/dashboard/NetWorthHistoryChart.tsx
@src/components/dashboard/DashboardChartsShell.tsx
@src/components/ui/chart.tsx
</context>

## Artifacts this phase produces

- Forecast domain + tracer wire (Plan 01)
- INISO + docs sync (Plan 02)
- Full chart chrome + VALIDATION gate (this plan)

<tasks>

<task type="auto" tdd="true">
  <name>Chart chrome — ReferenceLine, tooltip split, legend Прогноз</name>
  <files>src/components/dashboard/NetWorthHistoryChart.tsx, src/components/dashboard/nw-forecast-ui.test.ts</files>
  <read_first>
    .planning/phases/17-nw-forecast-overlay-isolation/17-UI-SPEC.md
    .planning/phases/17-nw-forecast-overlay-isolation/17-RESEARCH.md
    src/components/dashboard/NetWorthHistoryChart.tsx
    src/components/debts/DebtPrincipalStackChart.tsx
    src/components/ui/chart.tsx
  </read_first>
  <behavior>
    - ComposedChart hosts stacked Areas + dashed Line + vertical ReferenceLine at today when showForecast (D-09, D-12)
    - Legend when accounts.length &gt; 1 || showForecast; Line name «Прогноз» (D-10)
    - Tooltip ≤ today: stack rows + «Итого»; &gt; today: «Прогноз» + amount only (D-11)
    - Line: strokeDasharray 5 5, stroke var(--muted-foreground), dot false, connectNulls false, type stepAfter (UI-SPEC)
    - ReferenceLine stroke var(--border); today + horizonEnd in data so hinge/future axis not discarded (D-07, D-12)
  </behavior>
  <action>
    Complete NetWorthHistoryChart per UI-SPEC Interaction contract and RESEARCH Pattern 3 (discretion: ComposedChart, muted-foreground stroke, stepAfter). Implement tooltip split and legend visibility rules (D-10, D-11). Add ReferenceLine at today when forecast series shown (D-12). Ensure merged data from shell always includes today hinge + horizonEnd when showForecast (D-06, D-07) — coordinate prop shape with shell if needed. Green nw-forecast-ui.test.ts file-scan expects ComposedChart, strokeDasharray, ReferenceLine, Прогноз. Do not add new shadcn blocks or npm packages. Do not mount /income stats components.
  </action>
  <verify>
    <automated>npx vitest run -t "forecast" src/components/dashboard/nw-forecast-ui.test.ts &amp;&amp; grep -q 'ComposedChart' src/components/dashboard/NetWorthHistoryChart.tsx &amp;&amp; grep -q 'ReferenceLine' src/components/dashboard/NetWorthHistoryChart.tsx &amp;&amp; grep -q 'strokeDasharray' src/components/dashboard/NetWorthHistoryChart.tsx &amp;&amp; grep -q 'Прогноз' src/components/dashboard/NetWorthHistoryChart.tsx</automated>
    <fails_when>vitest non-zero or ComposedChart/ReferenceLine/strokeDasharray/Прогноз missing from NetWorthHistoryChart</fails_when>
  </verify>
  <acceptance_criteria>
    - File-scan green for ComposedChart + dashed Line + ReferenceLine + Прогноз (FCST-01, D-09..D-12)
  </acceptance_criteria>
  <done>Chart chrome contract implemented and scanned.</done>
</task>

<task type="auto" tdd="true">
  <name>Partial banner near NW chart + hide/showForecast wiring</name>
  <files>src/components/dashboard/DashboardChartsShell.tsx, src/components/dashboard/nw-forecast-ui.test.ts</files>
  <read_first>
    .planning/phases/17-nw-forecast-overlay-isolation/17-UI-SPEC.md
    src/components/dashboard/DashboardChartsShell.tsx
    src/app/page.tsx
    src/components/income/IncomeList.tsx
  </read_first>
  <behavior>
    - When isPartialForecast / excludedMissingFxCount &gt; 0: quiet role=status banner with compact «Прогноз неполный · нет курса» near NW chart (D-15, D-16, UI-SPEC default one-liner)
    - When includedSlotCount===0: hide Line + legend; no banner unless excludedMissingFxCount &gt; 0 (D-08, D-16)
    - Banner geometry: rounded-lg border border-border bg-muted/60 p-4; independent of account hero partial (UI-SPEC)
    - Never use «Итог неполный» for forecast strip; never English Forecast (UI-SPEC Do not ship)
  </behavior>
  <action>
    Mount forecast partial banner in DashboardChartsShell near NW chart section per UI-SPEC visual stack (below range / by chart). Wire showForecast from includedSlotCount &gt; 0. Keep compact one-liner copy (discretion lock). Ensure all-FX-excluded path hides Line but keeps banner (D-16). Extend nw-forecast-ui file-scan for Прогноз неполный / role=status / нет курса. Do not reuse IncomeList stats chrome. Do not change account hero partial copy.
  </action>
  <verify>
    <automated>npx vitest run src/components/dashboard/nw-forecast-ui.test.ts &amp;&amp; grep -q 'Прогноз неполный' src/components/dashboard/DashboardChartsShell.tsx &amp;&amp; grep -q 'нет курса' src/components/dashboard/DashboardChartsShell.tsx &amp;&amp; grep -q 'role=\"status\"\\|role=.status' src/components/dashboard/DashboardChartsShell.tsx</automated>
    <fails_when>vitest non-zero or partial banner strings / status role missing from DashboardChartsShell</fails_when>
  </verify>
  <acceptance_criteria>
    - Partial honesty chrome present; hide rules wired (FCST-01, D-15, D-16, D-08)
  </acceptance_criteria>
  <done>Partial banner + hide/showForecast complete.</done>
</task>

<task type="auto">
  <name>VALIDATION map + full suite gate</name>
  <files>.planning/phases/17-nw-forecast-overlay-isolation/17-VALIDATION.md</files>
  <read_first>
    .planning/phases/17-nw-forecast-overlay-isolation/17-VALIDATION.md
    .planning/phases/17-nw-forecast-overlay-isolation/17-01-PLAN.md
    .planning/phases/17-nw-forecast-overlay-isolation/17-02-PLAN.md
  </read_first>
  <action>
    Update 17-VALIDATION.md Per-Task Verification Map with exact Task IDs: 17-01-T1 Wave 0, 17-01-T2 tracer, 17-01-T3 domain, 17-02-T1 checkpoint (manual decision), 17-02-T2 INISO, 17-02-T3 docs sync, 17-03-T1 chart chrome, 17-03-T2 banner, 17-03-T3 this gate. Set wave_0_complete: true and nyquist_compliant: true when maps + suites green. Run wave merge suite from VALIDATION Sampling Rate.
  </action>
  <verify>
    <automated>npx vitest run src/lib/nw-forecast.test.ts src/lib/iniso.test.ts src/lib/historical-series.test.ts src/lib/income.test.ts src/app/income/actions.test.ts src/components/dashboard/nw-forecast-ui.test.ts &amp;&amp; grep -q '17-01-T' .planning/phases/17-nw-forecast-overlay-isolation/17-VALIDATION.md &amp;&amp; grep -q 'wave_0_complete: true' .planning/phases/17-nw-forecast-overlay-isolation/17-VALIDATION.md</automated>
    <fails_when>any vitest non-zero, VALIDATION missing 17-01-T task IDs, or wave_0_complete not true</fails_when>
  </verify>
  <human-check>
    Orca on `/`: switch 30д/90д/1г/всё — dashed «Прогноз» Line, today ReferenceLine hinge, horizon grows with preset; with missing FX for a future slot see «Прогноз неполный · нет курса» and series hide/keep per D-16.
  </human-check>
  <acceptance_criteria>
    - Wave merge suite green; VALIDATION task map filled; wave_0_complete true (FCST-01, ISO-01)
  </acceptance_criteria>
  <done>Nyquist VALIDATION closed for Phase 17 plans.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Client chart props → visual NW story | Overlay must not spoof measured history |
| Partial banner → user honesty | Missing FX must be visible, not silent |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-17-03 | Elevation of privilege (UX) | NetWorthHistoryChart | high | mitigate | Dashed Line + «Прогноз» legend + ReferenceLine hinge + future tooltip without fake stacks |
| T-17-02 | Tampering | FX honesty chrome | high | mitigate | Banner when excludedMissingFxCount &gt; 0; never invent rates in math (Plan 01) |
| T-17-01 | Tampering | income → NW history | high | mitigate | Chart merge only; no BalanceSnapshot writes from chrome |
| T-17-07 | Spoofing | English/forecast-as-fact copy | medium | mitigate | RU «Прогноз» only; ban solid Area forecast |
| T-17-SC | Tampering | npm installs | high | mitigate | Zero new packages; no new shadcn add |

</threat_model>

<verification>
npx vitest run src/lib/nw-forecast.test.ts src/lib/iniso.test.ts src/components/dashboard/nw-forecast-ui.test.ts
</verification>

<success_criteria>
UI-SPEC chrome complete (Line/ReferenceLine/tooltip/legend/banner); file-scan green; VALIDATION mapped; human-check queued for Orca visual.
</success_criteria>

<output>
Create `.planning/phases/17-nw-forecast-overlay-isolation/17-03-SUMMARY.md` when done
</output>
