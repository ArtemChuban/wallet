---
phase: 25-side-ledger-tools-isolation
plan: 03
type: execute
wave: 3
depends_on:
  - "25-02"
files_modified:
  - src/lib/mcp/reads/load-debts.ts
  - src/lib/mcp/tools/debts.ts
  - src/lib/mcp/tools/debts.test.ts
  - src/lib/mcp/create-handler.ts
  - src/lib/disol.test.ts
autonomous: true
requirements:
  - SIDE-01
estimate:
  tokens: 35000
  raw_tokens: 70000
  tasks: 2
  confidence: high
must_haves:
  truths:
    - Agent can call list_debts and receive OPEN debts plus colocated primary totals (iOwe / theyOwe / isPartial) as string minors (SIDE-01)
    - Optional includeClosed default false matches DebtsList OPEN focus
    - get_net_worth / load-net-worth-asof remain debts-import-free (DISOL / D-05)
    - Tool registered list_debts with readOnlyHint true; D-08 Долги one-liner in description
  prohibitions:
    - statement: MUST NOT reimplement remainingMinor or computeDebtPrimaryTotals
      category: isolation
    - statement: MUST NOT fold debts into get_net_worth payloads
      category: isolation
    - statement: MUST NOT change prisma schema
      category: scope
  artifacts:
    - path: src/lib/mcp/reads/load-debts.ts
      provides: debts page assembler + serializeDebtsPayload
    - path: src/lib/mcp/tools/debts.ts
      provides: registerListDebts MCP tool
    - path: src/lib/mcp/create-handler.ts
      provides: list_debts registration
  key_links:
    - from: loadDebts
      to: computeDebtPrimaryTotals
      via: remainingMinor + FX LOCF ≤ today
      pattern: "computeDebtPrimaryTotals"
    - from: registerListDebts
      to: createWalletMcpHandler
      via: register beside SIDE forecast
      pattern: "list_debts"
---

## Specless probe fallback

SPEC absent. Truths from ROADMAP SIDE-01 + CONTEXT D-05…D-08.

## Phase Goal

Agent can read Долги / Доходы / Грейс / forecast overlay without folding side ledgers into historical NW.

<objective>
Ship SIDE-01 list_debts: debts-page-parity loader with colocated computeDebtPrimaryTotals, registerTool + create-handler wire, and MCP DISOL contract coverage (D-05, D-06, D-08).

Purpose: External agent reads Долги + primary totals without folding into NW.
Output: list_debts live; green debts adapter + disol walls.
</objective>

<execution_context>
@/home/artem/Documents/wallet/.cursor/gsd-core/workflows/execute-plan.md
@/home/artem/Documents/wallet/.cursor/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/25-side-ledger-tools-isolation/25-CONTEXT.md
@.planning/phases/25-side-ledger-tools-isolation/25-RESEARCH.md
@.planning/phases/25-side-ledger-tools-isolation/25-PATTERNS.md
@.planning/phases/25-side-ledger-tools-isolation/25-02-SUMMARY.md
@src/app/debts/page.tsx
@src/lib/debts.ts
@src/lib/mcp/tools/accounts.ts
@src/lib/mcp/reads/load-net-worth-asof.ts
@src/lib/mcp/create-handler.ts
@src/lib/disol.test.ts
@src/lib/mcp/serialize.ts
</context>

<interface>
## Plan 02 → Plan 03 contracts

- createWalletMcpHandler already registers get_forecast_overlay + CAP tools
- minorToJson / calendarDateToday patterns established
- Wave 0 debts.test.ts stub ready to fill
</interface>

<assumptions>
## Planner assumptions

- A1 (RESEARCH): optional includeClosed boolean default false.
- Totals live inside list_debts payload (no separate get_debt_totals tool).
- Instructions may mention list_debts; Plan 04 finalizes full SIDE catalog wording.
</assumptions>

<tasks>

<task type="auto" tdd="true">
  <name>list_debts page-parity tool (SIDE-01, D-08)</name>
  <reversibility rating="costly">Published MCP tool name list_debts.</reversibility>
  <files>src/lib/mcp/reads/load-debts.ts, src/lib/mcp/tools/debts.ts, src/lib/mcp/create-handler.ts</files>
  <behavior>
    - Default list excludes CLOSED; includeClosed true appends CLOSED
    - Payload includes debts rows with remainingMinor strings + totals iOwePrimaryMinor / theyOwePrimaryMinor / isPartial strings/bool
    - Uses remainingMinor + assertStatusSynced + computeDebtPrimaryTotals — no twin arithmetic in tool
  </behavior>
  <read_first>
    - src/app/debts/page.tsx prisma batch + remaining + computeDebtPrimaryTotals
    - src/lib/debts.ts remainingMinor / computeDebtPrimaryTotals
    - src/lib/mcp/tools/accounts.ts registerTool list pattern
    - src/lib/mcp/reads/load-net-worth-asof.ts serialize split
  </read_first>
  <action>
    Create loadDebts({ includeClosed, today }) mirroring debts page: Person/Debt prisma batch + FX LOCF ≤ today via firstHitLocfMap, remainingMinor + assertStatusSynced, computeDebtPrimaryTotals for OPEN-only hero totals. Export serializeDebtsPayload with minorToJson money fields and colocated totals object (SIDE-01 discretion — one tool). Default OPEN filter (status !== CLOSED); optional includeClosed. registerListDebts: name list_debts, inputSchema { includeClosed: z.boolean().optional() }, readOnlyHint true / openWorldHint false, description English + Долги alias with D-08 side-ledger one-liner. Handler calls load then JSON content. Register in createWalletMcpHandler beside get_forecast_overlay; mention list_debts in instructions. Payloads UI-parity clean per D-06. No twin domain math. No prisma schema edits. No CI mutate ban (D-07).
  </action>
  <verify>
    <automated>test -f src/lib/mcp/reads/load-debts.ts &amp;&amp; test -f src/lib/mcp/tools/debts.ts &amp;&amp; grep -q 'list_debts' src/lib/mcp/tools/debts.ts &amp;&amp; grep -q 'readOnlyHint' src/lib/mcp/tools/debts.ts &amp;&amp; grep -q 'registerListDebts' src/lib/mcp/create-handler.ts &amp;&amp; grep -q 'computeDebtPrimaryTotals' src/lib/mcp/reads/load-debts.ts &amp;&amp; npx vitest run src/lib/mcp/tools/debts.test.ts</automated>
    <fails_when>files/markers missing, or vitest non-zero</fails_when>
  </verify>
  <done>list_debts registered with page-parity OPEN list + colocated primary totals.</done>
</task>

<task type="auto" tdd="true">
  <name>Green SIDE-01 debts tests + DISOL MCP contract</name>
  <files>src/lib/mcp/tools/debts.test.ts, src/lib/disol.test.ts, src/lib/mcp/reads/load-debts.ts</files>
  <behavior>
    - serializeDebtsPayload totals minors are strings; OPEN filter honored on fixture
    - NW CAP fixture keys never contain debt ledger contribution fields (thin MCP contract per D-05)
    - disol walls still pass for MCP NW paths; list_debts sources may import debts
  </behavior>
  <action>
    Replace Wave 0 todos in debts.test.ts with real SIDE-01 expects on pure serialize/fixtures (string minors, totals, OPEN default). Add thin MCP contract asserts that serialized NW payloads from CAP helpers never include debt-ledger fields (D-05) — do not weaken disol walls on load-net-worth-asof / net-worth tool. Keep disol.test.ts MCP walls green. Prefer SQLite-free pure serialize tests.
  </action>
  <verify>
    <automated>npx vitest run src/lib/mcp/tools/debts.test.ts src/lib/disol.test.ts src/lib/mcp/tools/net-worth.test.ts</automated>
    <fails_when>vitest non-zero, remaining SIDE-01 todos, or disol MCP walls failing</fails_when>
  </verify>
  <done>SIDE-01 adapter green; DISOL MCP walls hold; CAP NW regression green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| MCP tools/call → list_debts | Agent reads personal debt PII via localhost MCP |
| list_debts domain → get_net_worth | DISOL wall — debts must not leak into NW tool |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-25-DISOL | Elevation of Privilege | get_net_worth / load-net-worth-asof | high | mitigate | disol import walls + NW payload contract tests; description one-liner on list_debts |
| T-25-09 | Information Disclosure | list_debts payloads | medium | mitigate | Localhost Host/Origin unchanged; no 0.0.0.0; no money logs |
| T-25-10 | Tampering | load-debts | high | mitigate | Only remainingMinor + computeDebtPrimaryTotals; read-only prisma |
| T-25-SC | Tampering | npm installs | high | accept | No new packages |
</threat_model>

<verification>
npx vitest run src/lib/mcp/tools/debts.test.ts src/lib/disol.test.ts src/lib/mcp/
</verification>

<success_criteria>
list_debts end-to-end with totals; DISOL walls green; get_net_worth still debts-free.
</success_criteria>

<output>
Create `.planning/phases/25-side-ledger-tools-isolation/25-03-SUMMARY.md` when done
</output>
