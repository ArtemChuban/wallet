---
phase: 25-side-ledger-tools-isolation
plan: 04
type: execute
wave: 4
depends_on:
  - "25-03"
files_modified:
  - src/lib/mcp/reads/load-income.ts
  - src/lib/mcp/tools/income.ts
  - src/lib/mcp/tools/income.test.ts
  - src/lib/mcp/reads/load-grace.ts
  - src/lib/mcp/tools/grace.ts
  - src/lib/mcp/tools/grace.test.ts
  - src/lib/mcp/isolation-contract.test.ts
  - src/lib/mcp/create-handler.ts
  - src/lib/iniso.test.ts
  - src/lib/griso.test.ts
autonomous: true
requirements:
  - SIDE-02
  - SIDE-03
estimate:
  tokens: 45000
  raw_tokens: 90000
  tasks: 3
  confidence: high
must_haves:
  truths:
    - Agent can call list_income with page-parity next-open plan/actual/overdue; optional paired from/to occurrence dump (SIDE-02)
    - Agent can call list_grace_obligations with OPEN+CTA rows and isGraceOverdue flags (SIDE-03)
    - SIDE MCP sources never mutate BalanceSnapshot and never import app actions (INISO/GRISO / D-05)
    - Server instructions list all SIDE tools + D-08 one-liners; CAP honesty lines retained
    - Full mcp vitest suite green including wallet_ping + CAP + SIDE
  prohibitions:
    - statement: MUST NOT write or rewrite BalanceSnapshot via income/grace MCP paths
      category: isolation
    - statement: MUST NOT import historical-series into grace/income MCP loaders for list tools
      category: isolation
    - statement: MUST NOT add CI mutate-import ban under mcp path
      category: scope
    - statement: MUST NOT change prisma schema
      category: scope
    - statement: MUST NOT ship named DISOL-01/INISO-01/GRISO-01 essay prose
      category: scope
  artifacts:
    - path: src/lib/mcp/reads/load-income.ts
      provides: income page assembler
    - path: src/lib/mcp/tools/income.ts
      provides: registerListIncome
    - path: src/lib/mcp/reads/load-grace.ts
      provides: grace mergeGraceListRows assembler
    - path: src/lib/mcp/tools/grace.ts
      provides: registerListGraceObligations
    - path: src/lib/mcp/isolation-contract.test.ts
      provides: shared never-write BalanceSnapshot wall across SIDE MCP files
    - path: src/lib/mcp/create-handler.ts
      provides: full SIDE catalog instructions
  key_links:
    - from: loadIncome
      to: nextOpenPlannedAsOf / isIncomeOverdue
      via: page-parity default mode
      pattern: "isIncomeOverdue"
    - from: loadGrace
      to: mergeGraceListRows
      via: OPEN + CTA discriminant
      pattern: "mergeGraceListRows"
    - from: createWalletMcpHandler
      to: registerListIncome / registerListGraceObligations
      via: final SIDE registration
      pattern: "list_income"
---

## Specless probe fallback

SPEC absent. Truths from ROADMAP SIDE-02/03 + CONTEXT D-05…D-08.

## Phase Goal

Agent can read Долги / Доходы / Грейс / forecast overlay without folding side ledgers into historical NW.

<objective>
Ship SIDE-02 list_income and SIDE-03 list_grace_obligations, add shared MCP isolation-contract never-write suite (D-05), and finalize create-handler SIDE catalog + D-08 one-liners (D-06, D-07, D-08).

Purpose: Complete side-ledger MCP surface with INISO/GRISO proofs; leave named CLI-01 prose to Phase 26.
Output: list_income + list_grace_obligations live; isolation-contract green; full mcp suite green.
</objective>

<execution_context>
@/home/artem/Documents/wallet/.cursor/gsd-core/workflows/execute-plan.md
@/home/artem/Documents/wallet/.cursor/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/25-side-ledger-tools-isolation/25-CONTEXT.md
@.planning/phases/25-side-ledger-tools-isolation/25-RESEARCH.md
@.planning/phases/25-side-ledger-tools-isolation/25-PATTERNS.md
@.planning/phases/25-side-ledger-tools-isolation/25-03-SUMMARY.md
@src/app/income/page.tsx
@src/lib/income.ts
@src/lib/credit-grace.ts
@src/components/accounts/CreditGraceDialog.tsx
@src/lib/iniso.test.ts
@src/lib/griso.test.ts
@src/lib/mcp/create-handler.ts
@src/lib/mcp/tools/fx.ts
@src/lib/mcp/as-of.ts
</context>

<interface>
## Prior plans → Plan 04 contracts

- as-of paired from/to helpers from Plan 01
- create-handler already has CAP + get_forecast_overlay + list_debts
- Wave 0 income.test.ts / grace.test.ts stubs ready to fill
</interface>

<assumptions>
## Planner assumptions

- A2 (RESEARCH): default list_income = income page next-open mode; optional from+to both required together → listAllInRange.
- Grace CTA rows included with kind open|cta discriminant (GraceListRow parity).
- Named DISOL/INISO/GRISO essay + connect docs stay Phase 26 (D-08 / deferred).
</assumptions>

<tasks>

<task type="auto" tdd="true">
  <name>list_income page-parity tool (SIDE-02)</name>
  <reversibility rating="costly">Published MCP tool name list_income.</reversibility>
  <files>src/lib/mcp/reads/load-income.ts, src/lib/mcp/tools/income.ts, src/lib/mcp/tools/income.test.ts, src/lib/mcp/create-handler.ts</files>
  <behavior>
    - Default mode returns defs with nextPlannedAsOf / hasActual / overdue via isIncomeOverdue
    - Optional from+to (both YYYY-MM-DD together) returns listAllInRange occurrences with overdue flags
    - Source never calls balanceSnapshot mutate and never imports @/app/**/actions
  </behavior>
  <read_first>
    - src/app/income/page.tsx next-open fold
    - src/lib/income.ts nextOpenPlannedAsOf / isIncomeOverdue / listAllInRange
    - src/lib/mcp/tools/fx.ts optional filter registerTool pattern
    - src/lib/mcp/as-of.ts paired from/to helper
  </read_first>
  <action>
    Create loadIncome defaulting to income page parity (nextOpenPlannedAsOf + hasActual + isIncomeOverdue with calendarDateToday Europe/Moscow). Optional paired from/to uses Plan 01 schema helper → listAllInRange + overdue flags (domain requires explicit window — no invented default range). serialize via minorToJson. registerListIncome: name list_income, readOnlyHint true / openWorldHint false, D-08 Доходы one-liner in description. Register in createWalletMcpHandler. Green income.test.ts replacing Wave 0 todos (serialize + never-write source asserts per D-05). Payloads UI-parity clean per D-06. No prisma schema edits. No CI ban (D-07).
  </action>
  <verify>
    <automated>test -f src/lib/mcp/reads/load-income.ts &amp;&amp; test -f src/lib/mcp/tools/income.ts &amp;&amp; grep -q 'list_income' src/lib/mcp/tools/income.ts &amp;&amp; grep -q 'readOnlyHint' src/lib/mcp/tools/income.ts &amp;&amp; grep -q 'registerListIncome' src/lib/mcp/create-handler.ts &amp;&amp; npx vitest run src/lib/mcp/tools/income.test.ts src/lib/iniso.test.ts</automated>
    <fails_when>files/markers missing, vitest non-zero, or SIDE-02 it.todo remains</fails_when>
  </verify>
  <done>list_income registered; INISO twin still green; never-write asserts in income tests.</done>
</task>

<task type="auto" tdd="true">
  <name>list_grace_obligations tool (SIDE-03)</name>
  <reversibility rating="costly">Published MCP tool name list_grace_obligations.</reversibility>
  <files>src/lib/mcp/reads/load-grace.ts, src/lib/mcp/tools/grace.ts, src/lib/mcp/tools/grace.test.ts, src/lib/mcp/create-handler.ts</files>
  <behavior>
    - Rows use mergeGraceListRows; kind open|cta; CLOSED omitted
    - Overdue via isGraceOverdue(dueAsOf, today)
    - Source never mutates BalanceSnapshot and never imports historical-series
  </behavior>
  <read_first>
    - src/lib/credit-grace.ts mergeGraceListRows / isGraceOverdue
    - src/components/accounts/CreditGraceDialog.tsx listRows call site
    - src/lib/mcp/tools/accounts.ts list registerTool pattern
    - src/lib/griso.test.ts existing walls
  </read_first>
  <action>
    Create loadGrace listing credit accounts with schedules + OPEN obligations, mergeGraceListRows per account (OPEN + CTA), flag overdue with isGraceOverdue. Serialize kind discriminant + minorToJson money. registerListGraceObligations: name list_grace_obligations, readOnlyHint true / openWorldHint false, D-08 Грейс one-liner. Register in createWalletMcpHandler. Green grace.test.ts (OPEN/CTA/overdue + never-write / never-historical-series source asserts per D-05). Optionally extend griso.test.ts with SIDE MCP file never-historical assert if not covered by isolation-contract. Payloads UI-parity clean per D-06. No prisma schema edits.
  </action>
  <verify>
    <automated>test -f src/lib/mcp/reads/load-grace.ts &amp;&amp; test -f src/lib/mcp/tools/grace.ts &amp;&amp; grep -q 'list_grace_obligations' src/lib/mcp/tools/grace.ts &amp;&amp; grep -q 'readOnlyHint' src/lib/mcp/tools/grace.ts &amp;&amp; grep -q 'registerListGraceObligations' src/lib/mcp/create-handler.ts &amp;&amp; grep -q 'mergeGraceListRows' src/lib/mcp/reads/load-grace.ts &amp;&amp; npx vitest run src/lib/mcp/tools/grace.test.ts src/lib/griso.test.ts</automated>
    <fails_when>files/markers missing, vitest non-zero, or SIDE-03 it.todo remains</fails_when>
  </verify>
  <done>list_grace_obligations registered; GRISO twin green; CTA/OPEN parity.</done>
</task>

<task type="auto" tdd="true">
  <name>Finalize SIDE instructions + isolation-contract (D-05…D-08)</name>
  <files>src/lib/mcp/create-handler.ts, src/lib/mcp/isolation-contract.test.ts, src/lib/iniso.test.ts, src/lib/griso.test.ts</files>
  <behavior>
    - create-handler instructions name wallet_ping + all CAP tools + list_debts + list_income + list_grace_obligations + get_forecast_overlay
    - Instructions include D-08 side-ledger / forecast one-liners; no named DISOL/INISO/GRISO essay (Phase 26)
    - isolation-contract.test.ts asserts SIDE MCP tool+read sources have no balanceSnapshot.(create|update|upsert|delete) and no @/app/**/actions imports
    - Full mcp + route regression green
  </behavior>
  <action>
    Finalize createWalletMcpHandler instructions: full SIDE catalog beside CAP honesty lines; replace any residual come-later wording; keep D-08 minimal one-liners only (named CLI-01 prose stays Phase 26). Add src/lib/mcp/isolation-contract.test.ts covering SIDE files under tools/{debts,income,grace,forecast}.ts and reads/load-{debts,income,grace,forecast-overlay}.ts for never-write BalanceSnapshot + never actions imports (D-05). Confirm iniso/griso twins still pass. Run full mcp suite + route test. Do not add CI mutate-import ban (D-07). No prisma schema edits.
  </action>
  <verify>
    <automated>grep -q 'list_debts' src/lib/mcp/create-handler.ts &amp;&amp; grep -q 'list_income' src/lib/mcp/create-handler.ts &amp;&amp; grep -q 'list_grace_obligations' src/lib/mcp/create-handler.ts &amp;&amp; grep -q 'get_forecast_overlay' src/lib/mcp/create-handler.ts &amp;&amp; grep -q 'wallet_ping' src/lib/mcp/create-handler.ts &amp;&amp; test -f src/lib/mcp/isolation-contract.test.ts &amp;&amp; npx vitest run src/lib/mcp/ src/lib/disol.test.ts src/lib/iniso.test.ts src/lib/griso.test.ts src/app/api/mcp/route.test.ts</automated>
    <fails_when>any SIDE/CAP registration marker missing, isolation-contract missing, vitest non-zero, or "0 passed" in summary</fails_when>
  </verify>
  <done>Full SIDE catalog instructed; isolation-contract green; mcp + disol/iniso/griso + route green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| MCP tools/call → list_income / list_grace_obligations | Agent reads income/grace PII via localhost |
| Income/grace MCP → BalanceSnapshot / historical LOCF | INISO/GRISO — reads must not write or rewrite history |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-25-INISO | Tampering | load-income / income.ts | high | mitigate | Read-only prisma; isolation-contract never-write; iniso twin retained |
| T-25-GRISO | Tampering | load-grace / grace.ts | high | mitigate | mergeGraceListRows only; never historical-series; griso + isolation-contract |
| T-25-11 | Information Disclosure | income/grace payloads | medium | mitigate | Localhost guard unchanged; no money logs |
| T-25-12 | Spoofing | readOnlyHint | low | accept | Hint is UX; enforce via tests + review (D-07) |
| T-25-SC | Tampering | npm installs | high | accept | No new packages |
</threat_model>

<verification>
npx vitest run src/lib/mcp/ src/lib/disol.test.ts src/lib/iniso.test.ts src/lib/griso.test.ts src/app/api/mcp/route.test.ts
</verification>

<success_criteria>
list_income + list_grace_obligations live; isolation-contract proves never-write; instructions list full SIDE catalog; twins green.
</success_criteria>

<output>
Create `.planning/phases/25-side-ledger-tools-isolation/25-04-SUMMARY.md` when done
</output>
