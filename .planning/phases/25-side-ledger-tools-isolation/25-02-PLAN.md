---
phase: 25-side-ledger-tools-isolation
plan: 02
type: execute
wave: 2
depends_on:
  - "25-01"
files_modified:
  - src/lib/mcp/reads/load-forecast-overlay.ts
  - src/lib/mcp/tools/forecast.ts
  - src/lib/mcp/tools/forecast.test.ts
  - src/lib/mcp/create-handler.ts
autonomous: true
requirements:
  - SIDE-04
estimate:
  tokens: 45000
  raw_tokens: 90000
  tasks: 2
  confidence: high
must_haves:
  truths:
    - Agent can call get_forecast_overlay and receive sparse points[] with forecastEvents including income + A′ grace kinds (SIDE-04, D-01, D-03)
    - Omitted horizonEnd defaults to calendarDateToday Europe/Moscow + addCalendarDays 365 (D-02, D-04)
    - Anchor NW is accounts-only via existing loadNetWorthAsOf / same batch — debts not folded into overlay math
    - Tool registered get_forecast_overlay with readOnlyHint true; CAP tools + wallet_ping remain
    - Description carries Капитал forecast overlay one-liner (D-08); payloads stay UI-parity clean (D-06)
  prohibitions:
    - statement: MUST NOT reimplement buildNetWorthForecastSeries in the tool
      category: isolation
    - statement: MUST NOT call buildNetWorthSeries for the overlay path
      category: isolation
    - statement: MUST NOT fold debts into forecast anchor
      category: scope
    - statement: MUST NOT invent FX rates when missing
      category: isolation
    - statement: MUST NOT change prisma schema
      category: scope
  artifacts:
    - path: src/lib/mcp/reads/load-forecast-overlay.ts
      provides: DashboardChartsShell forecast fold assembler + serialize
    - path: src/lib/mcp/tools/forecast.ts
      provides: registerGetForecastOverlay MCP tool
    - path: src/lib/mcp/create-handler.ts
      provides: SIDE forecast registration + instructions past come-later
  key_links:
    - from: loadForecastOverlay
      to: buildNetWorthForecastSeries
      via: open income slots + openGraceForecastMembership
      pattern: "buildNetWorthForecastSeries"
    - from: registerGetForecastOverlay
      to: createWalletMcpHandler
      via: register after CAP tools
      pattern: "get_forecast_overlay"
    - from: optionalHorizonEndSchema
      to: get_forecast_overlay handler
      via: free horizonEnd wire field
      pattern: "optionalHorizonEndSchema"
---

## Specless probe fallback

SPEC absent. Truths from ROADMAP SIDE-04 + CONTEXT D-01…D-04, D-06, D-08.

## Phase Goal

Agent can read Долги / Доходы / Грейс / forecast overlay without folding side ledgers into historical NW.

<objective>
Ship production-quality tracer for SIDE-04: page/shell-parity load-forecast-overlay → serialize sparse series → registerGetForecastOverlay → createWalletMcpHandler instructions past capital-era “side ledgers come later” (D-01, D-02, D-03, D-04, D-06, D-08).

Purpose: Prove end-to-end Капитал Прогноз MCP parity on one tool before expanding list catalog.
Output: get_forecast_overlay live beside CAP tools; green forecast adapter tests.
</objective>

<execution_context>
@/home/artem/Documents/wallet/.cursor/gsd-core/workflows/execute-plan.md
@/home/artem/Documents/wallet/.cursor/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/25-side-ledger-tools-isolation/25-CONTEXT.md
@.planning/phases/25-side-ledger-tools-isolation/25-RESEARCH.md
@.planning/phases/25-side-ledger-tools-isolation/25-PATTERNS.md
@.planning/phases/25-side-ledger-tools-isolation/25-01-SUMMARY.md
@src/components/dashboard/DashboardChartsShell.tsx
@src/lib/nw-forecast.ts
@src/lib/income.ts
@src/lib/credit-grace.ts
@src/lib/dates.ts
@src/lib/mcp/create-handler.ts
@src/lib/mcp/tools/net-worth.ts
@src/lib/mcp/reads/load-net-worth-asof.ts
@src/lib/mcp/as-of.ts
@src/lib/mcp/serialize.ts
</context>

<interface>
## Plan 01 → Plan 02 contracts

- `optionalHorizonEndSchema` from `src/lib/mcp/as-of.ts`
- `minorToJson` from `src/lib/mcp/serialize.ts`
- Wave 0 `src/lib/mcp/tools/forecast.test.ts` stub ready to fill
- Existing `loadNetWorthAsOf` for accounts-only anchor
</interface>

<assumptions>
## Planner assumptions

- A3 (RESEARCH): keep domain forecast / displayPrimaryMajor numbers beside string minors for Капитал parity.
- Tracer registers get_forecast_overlay only among SIDE tools; list_debts / list_income / list_grace_obligations land Plans 03–04.
- Instructions this plan replace “Side ledgers (debts/income/grace) come later.” with SIDE catalog mentioning get_forecast_overlay + forecast one-liner; Plan 04 expands list tool names after they exist.
- openWorldHint false with readOnlyHint (parity CAP).
</assumptions>

<tasks>

<task type="tracer">
  <name>End-to-end get_forecast_overlay — SIDE-04 tracer</name>
  <reversibility rating="costly">Published MCP response shape for get_forecast_overlay sparse series (D-01); rename/reshape after agents connect is costly migration.</reversibility>
  <files>src/lib/mcp/reads/load-forecast-overlay.ts, src/lib/mcp/tools/forecast.ts, src/lib/mcp/create-handler.ts</files>
  <read_first>
    - src/components/dashboard/DashboardChartsShell.tsx forecastMeta fold (income listAllInRange open slots + openGraceForecastMembership + buildNetWorthForecastSeries)
    - src/lib/nw-forecast.ts BuildNetWorthForecastSeriesResult / ForecastPoint / forecastHorizonEnd
    - src/lib/mcp/tools/net-worth.ts registerTool pattern
    - src/lib/mcp/reads/load-net-worth-asof.ts anchor pattern
    - src/lib/mcp/as-of.ts optionalHorizonEndSchema
    - .planning/phases/25-side-ledger-tools-isolation/25-RESEARCH.md serializeForecastPayload sketch
  </read_first>
  <action>
    Wire ONE SIDE path only: get_forecast_overlay. Create loadForecastOverlay({ today, horizonEnd }) in reads/ by porting DashboardChartsShell forecast fold — do not invent overlay math. Resolve today via calendarDateToday("Europe/Moscow"); horizonEnd = arg ?? addCalendarDays(today, 365) (D-04; equivalent forecastHorizonEnd 1y/all). Anchor = accounts-only NW today via loadNetWorthAsOf(today) or same LOCF batch — never debts. Membership: open income slots (listAllInRange from today+1..horizonEnd, plannedAsOf &gt; today, no actual) + openGraceForecastMembership grace slots. Call buildNetWorthForecastSeries only — never buildNetWorthSeries. Export pure serializeForecastPayload using minorToJson for anchor/forecastPrimaryMinor/plannedAmountMinor; include points[] with forecastEvents (D-01, D-03); keep isPartialForecast / includedSlotCount / excludedMissingFx* honesty. registerGetForecastOverlay: name get_forecast_overlay, inputSchema { horizonEnd: optionalHorizonEndSchema } (D-02 free date — mention 30d/90d/1y only inside description text), annotations readOnlyHint true and openWorldHint false, description English + Капитал «Прогноз» alias with D-08 forecast one-liner. Handler resolves today/horizon then load then JSON content text. Update createWalletMcpHandler: register after CAP tools; replace come-later sentence with SIDE note that get_forecast_overlay is available and list tools arrive next, plus same forecast one-liner wall. Payloads UI-parity clean per D-06 (no extra meta flags). Enforce read-only via review + tests (D-07) — no CI ban. Keep responseMode json + legacy stateless. No prisma schema edits.
  </action>
  <verify>
    <automated>test -f src/lib/mcp/reads/load-forecast-overlay.ts &amp;&amp; test -f src/lib/mcp/tools/forecast.ts &amp;&amp; grep -q 'get_forecast_overlay' src/lib/mcp/tools/forecast.ts &amp;&amp; grep -q 'readOnlyHint' src/lib/mcp/tools/forecast.ts &amp;&amp; grep -q 'registerGetForecastOverlay' src/lib/mcp/create-handler.ts &amp;&amp; grep -q 'buildNetWorthForecastSeries' src/lib/mcp/reads/load-forecast-overlay.ts &amp;&amp; npx vitest run src/lib/mcp/tools/forecast.test.ts src/lib/mcp/as-of.test.ts</automated>
    <fails_when>missing files, get_forecast_overlay/readOnlyHint/registerGetForecastOverlay/buildNetWorthForecastSeries markers absent, or vitest non-zero</fails_when>
  </verify>
  <done>get_forecast_overlay registered end-to-end with Капитал-parity sparse series and D-04 default horizon.</done>
</task>

<task type="auto" tdd="true">
  <name>Green SIDE-04 forecast adapter tests</name>
  <files>src/lib/mcp/tools/forecast.test.ts, src/lib/mcp/reads/load-forecast-overlay.ts</files>
  <behavior>
    - Serialized points include asOfDate + forecastPrimaryMinor string + optional forecastEvents with kind income|grace
    - Default omitted horizonEnd resolves to today+365 via addCalendarDays (unit-testable without full prisma)
    - Payload includes isPartialForecast / includedSlotCount honesty fields
    - Source of load-forecast-overlay.ts and forecast.ts has no balanceSnapshot mutate calls and no @/app/**/actions imports (D-05)
    - Prefer pure serializeForecastPayload / fixture tests — do not re-prove nw-forecast domain math
  </behavior>
  <action>
    Replace Wave 0 todos in forecast.test.ts with real expects for SIDE-04: after buildNetWorthForecastSeries-equivalent fixture, assert adapter JSON sparse points, string minors, event kinds for income + grace, default horizon helper, UI-parity keys only (D-06). Add readFileSync never-write / never-actions asserts on the forecast MCP sources (D-05). Keep CAP + host regression green when running broader mcp suite later.
  </action>
  <verify>
    <automated>npx vitest run src/lib/mcp/tools/forecast.test.ts src/lib/mcp/tools/net-worth.test.ts src/lib/mcp/localhost-guard.test.ts</automated>
    <fails_when>vitest non-zero, remaining it.todo on SIDE-04 owner cases, or "0 passed" in summary</fails_when>
  </verify>
  <done>SIDE-04 adapter tests green; CAP NW + localhost regression still green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| MCP tools/call → get_forecast_overlay | Agent-supplied horizonEnd into forecast assembler |
| Forecast membership → historical NW | Overlay must not rewrite LOCF / BalanceSnapshot |
| Domain libs → MCP JSON | BigInt honesty must survive serialize |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-25-04 | Tampering | load-forecast-overlay | high | mitigate | Only buildNetWorthForecastSeries; never buildNetWorthSeries; never-write contract tests |
| T-25-05 | Spoofing | forecast honesty | high | mitigate | Surface isPartialForecast / excludedMissingFx; never invent FX |
| T-25-06 | Elevation of Privilege | annotations | low | accept | readOnlyHint UX only; localhost guard Phase 23 |
| T-25-07 | Information Disclosure | forecast payloads | medium | mitigate | No money payload logging; localhost-only publish unchanged |
| T-25-08 | Denial of Service | wide horizonEnd | low | accept | Free date per D-02; reuse page batch queries; agents pick dates |
| T-25-SC | Tampering | npm installs | high | accept | No new packages |
</threat_model>

<verification>
npx vitest run src/lib/mcp/tools/forecast.test.ts src/lib/mcp/
</verification>

<success_criteria>
get_forecast_overlay end-to-end; sparse series + income/grace events; D-04 default horizon; CAP tools retained.
</success_criteria>

<output>
Create `.planning/phases/25-side-ledger-tools-isolation/25-02-SUMMARY.md` when done
</output>
