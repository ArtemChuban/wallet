---
phase: 13-income-schema-domain-math
plan: 02
type: execute
wave: 2
depends_on:
  - "13-01"
files_modified:
  - src/lib/income.ts
  - src/lib/income.test.ts
autonomous: true
requirements:
  - FND-OCC
  - FND-MONEY
estimate:
  tokens: 20000
  raw_tokens: 40000
  tasks: 2
  confidence: high
must_haves:
  truths:
    - "UI-00: still zero user-facing income screens"
    - listOneTimeOccurrences emits at most one slot from definition plannedAsOf when inside inclusive [from, to] (D-14, D-15, FND-OCC)
    - listAllInRange merges recurring + one-time results without inventing a default horizon (D-13, D-14)
    - Occurrence identity is (parentId, plannedAsOf); changing dayOfMonth does not rewrite existing actual plannedAsOf keys (D-06)
    - Month-keyed freeze (A2): if an actual exists for a parent in a calendar month, that plannedAsOf is kept when in range; empty months use current DOM clamp (D-07)
    - plannedAmountMinor remains bigint end-to-end through list helpers (D-05, FND-MONEY)
    - Inclusive range: from==to single day and boundary plannedAsOf == from/to are included (D-15)
  prohibitions:
    - statement: MUST NOT add a default-horizon wrapper as the core list API
      requirement_id: FND-OCC
      category: values
    - statement: MUST NOT rewrite actual.plannedAsOf when regenerating after DOM change
      requirement_id: FND-OCC
      category: safety
    - statement: MUST NOT write BalanceSnapshot or import net-worth / historical-series from income.ts
      requirement_id: ISO-01
      category: isolation
    - statement: MUST NOT add /income UI
      requirement_id: UI-00
      category: scope
  artifacts:
    - path: src/lib/income.ts
      provides: listOneTimeOccurrences, listAllInRange, freeze-aware listRecurringOccurrences
    - path: src/lib/income.test.ts
      provides: freeze/DOM-change, inclusive bounds, one-time slot, bigint amount cases
  key_links:
    - from: listRecurringOccurrences
      to: actuals by parentId+month
      via: month-keyed freeze merge (D-07)
      pattern: "plannedAsOf"
    - from: listAllInRange
      to: listRecurringOccurrences + listOneTimeOccurrences
      via: thin merge
      pattern: "listAllInRange"
    - from: occurrence key
      to: "@@unique schema"
      via: "(parentId, plannedAsOf) identity (D-06, D-09)"
      pattern: "parentId"
---

<objective>
Expand income occurrence API with listOneTimeOccurrences, freeze-aware listRecurringOccurrences (D-06/D-07), inclusive range behavior (D-15), and thin listAllInRange merge (D-13/D-14) with BigInt plan amounts (FND-OCC, FND-MONEY).

Purpose: Complete virtual plan listing Phase 14–17 will call — still pure lib, no UI.
Output: Full occurrence surface in income.ts with green Vitest coverage for freeze and bounds.
</objective>

<execution_context>
@/home/artem/Documents/wallet/.cursor/gsd-core/workflows/execute-plan.md
@/home/artem/Documents/wallet/.cursor/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/13-income-schema-domain-math/13-CONTEXT.md
@.planning/phases/13-income-schema-domain-math/13-RESEARCH.md
@.planning/phases/13-income-schema-domain-math/13-PATTERNS.md
@.planning/phases/13-income-schema-domain-math/13-01-PLAN.md
@src/lib/income.ts
@src/lib/income.test.ts
@src/lib/dates.ts
</context>

<assumptions>
## Planner assumptions

- A2 locked for this plan: month-keyed single slot per parent×month after freeze (RESEARCH Pattern 3) — do not emit both old frozen key and new candidate for the same month.
- One-time actual join: match by (oneTimeIncomeId, plannedAsOf) when actuals provided; definition plannedAsOf is the plan slot.
</assumptions>

## Artifacts this phase produces

- `listOneTimeOccurrences(defs, actuals, from, to)`
- `listAllInRange(...)` thin merge of recurring + one-time
- Freeze-aware `listRecurringOccurrences` completing D-06/D-07
- Vitest cases: DOM change keeps frozen actual key; empty month uses current DOM; inclusive from/to; bigint amounts

<tasks>

<task type="auto" tdd="true">
  <name>Freeze-aware recurring occurrences + inclusive range tests then implement</name>
  <files>src/lib/income.ts, src/lib/income.test.ts</files>
  <read_first>
    .planning/phases/13-income-schema-domain-math/13-CONTEXT.md
    .planning/phases/13-income-schema-domain-math/13-RESEARCH.md
    src/lib/income.ts
    src/lib/income.test.ts
    src/lib/dates.ts
  </read_first>
  <behavior>
    - After actual exists for Jan plannedAsOf under old DOM, changing dayOfMonth regenerates Feb with new clamp but keeps Jan frozen plannedAsOf unchanged (D-06, D-07)
    - Month without actual emits candidate from current dayOfMonth via clampDayOfMonth
    - from==to includes that single day when plannedAsOf equals it (D-15)
    - Slot with plannedAsOf == from and == to included; outside range excluded
    - plannedAmountMinor on returned rows is bigint (FND-MONEY)
  </behavior>
  <action>
    Extend income.test.ts with freeze/DOM-change cases per D-06 and D-07 (month-keyed A2), inclusive range boundary cases per D-15, and bigint plannedAmountMinor asserts (FND-MONEY). Then update listRecurringOccurrences to: walk months overlapping inclusive [from, to]; for each parent×month compute candidate = clampDayOfMonth(y, m, currentDayOfMonth); if an actual for that parent has plannedAsOf in the same YYYY-MM, emit/keep the actual plannedAsOf when it lies in [from, to] instead of a differing candidate; also emit orphaned actual plannedAsOf in range; dedupe by (parentId, plannedAsOf); include candidate only when candidate &gt;= startAsOf and in range (D-15). Callers always pass from/to — no hidden default horizon (D-13). Keep pure TS; import clampDayOfMonth from @/lib/dates only (D-16). Do not add UI or Prisma.
  </action>
  <verify>
    <automated>npx vitest run src/lib/income.test.ts</automated>
    <fails_when>vitest non-zero, or freeze/DOM-change or inclusive-range cases fail</fails_when>
  </verify>
  <acceptance_criteria>
    - income.test.ts covers D-06/D-07 freeze after DOM change and D-15 inclusive bounds
    - listRecurringOccurrences keeps frozen actual plannedAsOf; empty months use current DOM clamp
    - Returned plannedAmountMinor is bigint (FND-MONEY)
  </acceptance_criteria>
  <done>Freeze-aware recurring listing green with inclusive range tests</done>
</task>

<task type="auto" tdd="true">
  <name>listOneTimeOccurrences + listAllInRange merge</name>
  <files>src/lib/income.ts, src/lib/income.test.ts</files>
  <read_first>
    .planning/phases/13-income-schema-domain-math/13-RESEARCH.md
    .planning/phases/13-income-schema-domain-math/13-PATTERNS.md
    src/lib/income.ts
    src/lib/income.test.ts
  </read_first>
  <behavior>
    - One-time def with plannedAsOf in range yields one occurrence; out of range yields none (D-14, D-15)
    - Optional actual joined by (parentId, plannedAsOf) without mutating plan fields (D-08 readiness for Plan 03)
    - listAllInRange returns combined recurring + one-time results for the same explicit window (D-13, D-14)
    - listAllInRange and list helpers require from and to arguments — no default horizon function as core API (D-13)
  </behavior>
  <action>
    Add listOneTimeOccurrences(defs, actuals, from, to) emitting the definition plannedAsOf when from &lt;= plannedAsOf &lt;= to with bigint plannedAmountMinor (D-14, D-15, FND-MONEY, D-12 model feed readiness). Add thin listAllInRange that concatenates or merges listRecurringOccurrences and listOneTimeOccurrences for the same window (D-14) without inventing horizon defaults (D-13). Extend income.test.ts for one-time in/out of range and merge smoke. Do not implement isIncomeOverdue or plan-immutability asserts here (Plan 03). Do not touch BalanceSnapshot or NW modules.
  </action>
  <verify>
    <automated>npx vitest run src/lib/income.test.ts &amp;&amp; grep -q 'listOneTimeOccurrences' src/lib/income.ts &amp;&amp; grep -q 'listAllInRange' src/lib/income.ts</automated>
    <fails_when>vitest non-zero, or listOneTimeOccurrences / listAllInRange exports missing</fails_when>
  </verify>
  <acceptance_criteria>
    - src/lib/income.ts exports listOneTimeOccurrences and listAllInRange (D-14)
    - Tests prove one-time inclusive window and merge with recurring for explicit from/to (D-13, D-15, FND-OCC)
  </acceptance_criteria>
  <done>One-time + merge listing API complete and tested</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Caller-provided defs/actuals/range → occurrence list | Untrusted calendar strings and bigint amounts |
| Income occurrence API → future UI/forecast | Wrong freeze would corrupt plan identity (D-06) |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-13-07 | Tampering | DOM edit regenerating actual keys | high | mitigate | Freeze-merge keeps actual plannedAsOf (D-06, D-07); Vitest freeze cases |
| T-13-08 | Tampering | Hidden default horizon skew | medium | mitigate | Strict from/to only (D-13); tests require explicit window |
| T-13-09 | Tampering | Float coercion in list outputs | high | mitigate | bigint plannedAmountMinor asserts (FND-MONEY) |
| T-13-10 | Tampering | BalanceSnapshot / NW mutation | high | mitigate | No writers; income stays pure; Plan 03 isolation scan |
| T-13-SC | Tampering | npm installs | high | mitigate | No new packages this plan |
</threat_model>

<verification>
npx vitest run src/lib/income.test.ts green for freeze, inclusive range, one-time, and listAllInRange.
</verification>

<success_criteria>
Virtual occurrence API complete for recurring + one-time with freeze identity and explicit inclusive windows; BigInt amounts preserved.
</success_criteria>

<output>
Create `.planning/phases/13-income-schema-domain-math/13-02-SUMMARY.md` when done
</output>
