---
phase: 13-income-schema-domain-math
plan: 03
type: execute
wave: 3
depends_on:
  - "13-02"
files_modified:
  - src/lib/income.ts
  - src/lib/income.test.ts
autonomous: true
requirements:
  - FND-OVER
  - FND-MONEY
  - FND-SCHEMA
  - FND-OCC
estimate:
  tokens: 17500
  raw_tokens: 35000
  tasks: 2
  confidence: high
must_haves:
  truths:
    - "UI-00: Phase 13 ships zero user-facing screens or interactive chrome"
    - isIncomeOverdue(plannedAsOf, hasActual, today) is true iff plannedAsOf &lt; today and hasActual is false (D-08 overdue inputs, FND-OVER)
    - Overdue helper takes injected today — does not call calendarDateToday inside list APIs (D-13)
    - Pure helper documents or asserts one-time plan fields immutable after an actual exists (D-08) without Prisma
    - Schema file-lock tests assert four models, BigInt money, Restrict/Cascade, and both @@unique slot indexes (FND-SCHEMA, D-03..D-05, D-09..D-11)
    - Light isolation scan: income.ts does not import net-worth or historical-series; those files do not import income (ISO-01 Phase 13 slice)
    - Full npm test suite green including foundation migrate gate from Plan 01 (FND-MIG retained)
    - BigInt money paths covered in income.test.ts (FND-MONEY)
  prohibitions:
    - statement: MUST NOT write BalanceSnapshot or mutate computeNetWorthRows from income code
      requirement_id: ISO-01
      category: isolation
    - statement: MUST NOT add /income UI, Server Actions, or Zod income validations this phase
      requirement_id: UI-00
      category: scope
    - statement: MUST NOT add new npm packages
      requirement_id: FND-SCHEMA
      category: safety
    - statement: MUST NOT introduce a single polymorphic income source/actual enum model
      requirement_id: FND-SCHEMA
      category: values
  artifacts:
    - path: src/lib/income.ts
      provides: isIncomeOverdue; one-time plan immutability helper
    - path: src/lib/income.test.ts
      provides: FND-OVER cases, schema locks, light INISO scan, BigInt paths
  key_links:
    - from: isIncomeOverdue
      to: injected today string
      via: plannedAsOf &lt; today &amp;&amp; !hasActual (D-08, FND-OVER)
      pattern: "isIncomeOverdue"
    - from: income.test.ts schema describe
      to: prisma/schema.prisma
      via: readFileSync model/unique/Restrict/Cascade locks (FND-SCHEMA)
      pattern: "RecurringIncome"
    - from: income.test.ts isolation describe
      to: net-worth.ts / historical-series.ts / income.ts
      via: file-scan no cross-imports (ISO-01)
      pattern: "net-worth"
---

<objective>
Finish Phase 13 domain surface: overdue predicate inputs (FND-OVER / D-08), one-time plan immutability helper (D-08), schema file locks (FND-SCHEMA), BigInt path coverage (FND-MONEY), and light ISO-01 isolation scan — then full suite green.

Purpose: Lock ACT/ISO readiness without shipping UI or NW writers.
Output: Complete income.ts helpers + income.test.ts locks; npm test green.
</objective>

<execution_context>
@/home/artem/Documents/wallet/.cursor/gsd-core/workflows/execute-plan.md
@/home/artem/Documents/wallet/.cursor/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/13-income-schema-domain-math/13-CONTEXT.md
@.planning/phases/13-income-schema-domain-math/13-RESEARCH.md
@.planning/phases/13-income-schema-domain-math/13-PATTERNS.md
@.planning/phases/13-income-schema-domain-math/13-UI-SPEC.md
@src/lib/income.ts
@src/lib/income.test.ts
@src/lib/disol.test.ts
@src/lib/debts.test.ts
@prisma/schema.prisma
</context>

<assumptions>
## Planner assumptions

- Full INISO property suite remains Phase 17; this plan adds light file-scan only (RESEARCH Pattern 4).
- D-12 REQUIREMENTS.md FCST-01 text update stays Phase 17 — Phase 13 only ensures OneTimeIncome model exists to feed overlay later.
</assumptions>

## Artifacts this phase produces

- `isIncomeOverdue(plannedAsOf, hasActual, today): boolean`
- Pure one-time plan immutability helper (name discretionary; behavior per D-08 — e.g. assertOneTimePlanImmutable or isOneTimePlanEditable)
- Schema convention locks + light isolation scan in `income.test.ts`
- Phase gate: full `npm test` green

<tasks>

<task type="auto" tdd="true">
  <name>isIncomeOverdue + one-time plan immutability helper</name>
  <files>src/lib/income.ts, src/lib/income.test.ts</files>
  <read_first>
    .planning/phases/13-income-schema-domain-math/13-CONTEXT.md
    .planning/phases/13-income-schema-domain-math/13-RESEARCH.md
    src/lib/income.ts
    src/lib/income.test.ts
    src/lib/debts.ts
  </read_first>
  <behavior>
    - isIncomeOverdue(plan, false, today) true when plan &lt; today; false when plan &gt;= today; false when hasActual true even if plan &lt; today (FND-OVER, D-08)
    - Helper does not read clock internally — today is always an argument (D-13)
    - After actual exists, one-time plan immutability helper rejects changing plannedAsOf or plannedAmountMinor; allows actual amount/date/note changes at the pure-predicate layer (D-08)
    - BigInt path: list helpers still return Nn literals unchanged (FND-MONEY)
  </behavior>
  <action>
    Implement isIncomeOverdue(plannedAsOf, hasActual, today) returning plannedAsOf &lt; today &amp;&amp; !hasActual (FND-OVER, D-08). Add a pure D-08 helper for one-time plan immutability after actual exists (mirror debts assertInitialImmutable style — throw or boolean reject when proposed plan fields differ from stored while hasActual). Extend income.test.ts with overdue matrix (before/on/after today × hasActual) and immutability cases. Keep list APIs free of calendarDateToday calls (D-13). Do not add Server Actions or Zod. Do not touch BalanceSnapshot.
  </action>
  <verify>
    <automated>npx vitest run src/lib/income.test.ts &amp;&amp; grep -q 'isIncomeOverdue' src/lib/income.ts</automated>
    <fails_when>vitest non-zero or isIncomeOverdue export missing</fails_when>
  </verify>
  <acceptance_criteria>
    - isIncomeOverdue covers FND-OVER / D-08 overdue inputs with injected today
    - One-time plan immutability helper covers D-08 after-actual rule at pure layer
    - BigInt money asserts remain green (FND-MONEY)
  </acceptance_criteria>
  <done>Overdue + one-time immutability helpers tested</done>
</task>

<task type="auto" tdd="true">
  <name>Schema file locks + light ISO isolation scan + full suite</name>
  <files>src/lib/income.test.ts</files>
  <read_first>
    .planning/phases/13-income-schema-domain-math/13-RESEARCH.md
    .planning/phases/13-income-schema-domain-math/13-PATTERNS.md
    .planning/phases/13-income-schema-domain-math/13-UI-SPEC.md
    src/lib/debts.test.ts
    src/lib/disol.test.ts
    src/lib/income.test.ts
    prisma/schema.prisma
  </read_first>
  <behavior>
    - readFileSync prisma/schema.prisma matches RecurringIncome, OneTimeIncome, both Actual models, plannedAmountMinor BigInt, onDelete Restrict, onDelete Cascade, and both @@unique parentId/plannedAsOf forms (FND-SCHEMA, D-03..D-05, D-09..D-11)
    - income.ts source does not match net-worth or historical-series import patterns; net-worth.ts and historical-series.ts do not match income import patterns (ISO-01 light)
    - No src/app/income directory exists (UI-00)
    - npm test full suite exits 0
  </behavior>
  <action>
    Add schema convention describe blocks in income.test.ts mirroring debts.test.ts locks for the four income models, BigInt plannedAmountMinor, Restrict and Cascade onDelete wording, and @@unique([recurringIncomeId, plannedAsOf]) plus @@unique([oneTimeIncomeId, plannedAsOf]) (FND-SCHEMA, D-03, D-04, D-05, D-09, D-10, D-11). Add light isolation file-scan (disol.test.ts style) that income.ts does not import NW/historical modules and NW/historical modules do not import income (ISO-01 Phase 13 slice). Assert absence of src/app/income via filesystem check in test or verify command (UI-00). Confirm package.json dependency set unchanged (no new packages). Run full npm test. Do not update REQUIREMENTS FCST-01 text (D-12 deferred to Phase 17). Do not implement forecast overlay.
  </action>
  <verify>
    <automated>npx vitest run src/lib/income.test.ts src/lib/dates.test.ts src/lib/foundation.test.ts &amp;&amp; test ! -e src/app/income &amp;&amp; npm test</automated>
    <fails_when>any vitest/npm test non-zero, src/app/income exists, or schema/isolation describes fail</fails_when>
  </verify>
  <acceptance_criteria>
    - income.test.ts contains FND-SCHEMA locks for four models + unique + Restrict/Cascade (D-03..D-05, D-09..D-11)
    - Isolation scan enforces ISO-01 no NW↔income imports
    - UI-00: src/app/income does not exist
    - npm test exits 0 (phase gate)
  </acceptance_criteria>
  <done>Schema locks, isolation scan, and full suite green — Phase 13 domain complete</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Overdue inputs → UI later | Wrong today injection could mislabel overdue |
| Schema locks → future migrations | Drift from Restrict/Cascade/unique breaks integrity |
| Income ↔ NW modules | Cross-import would enable BalanceSnapshot pollution (ISO-01) |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-13-11 | Tampering | Cascade delete integrity | high | mitigate | Schema locks assert Cascade on actuals + Restrict on Person (D-10, D-11); foundation migrate proved tables |
| T-13-12 | Tampering | Restrict on Person bypass | high | mitigate | Prisma onDelete Restrict + schema file-lock tests |
| T-13-13 | Tampering | BigInt money correctness | high | mitigate | FND-MONEY bigint asserts retained in income.test.ts |
| T-13-14 | Tampering | BalanceSnapshot / computeNetWorthRows | high | mitigate | Light INISO file-scan; no income actions; UI-00 no writers |
| T-13-15 | Information disclosure | Overdue today leakage | low | accept | Single-user local app; today is caller-injected string |
| T-13-SC | Tampering | npm installs | high | mitigate | Verify no new packages; zero installs this plan |
</threat_model>

<verification>
npx vitest run income/dates/foundation tests green; src/app/income absent; npm test green.
</verification>

<success_criteria>
Overdue + immutability helpers done; schema and ISO locks green; UI-00 holds; Phase 13 foundation ready for Phase 14 CRUD.
</success_criteria>

<output>
Create `.planning/phases/13-income-schema-domain-math/13-03-SUMMARY.md` when done
</output>
