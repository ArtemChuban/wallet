---
phase: 13-income-schema-domain-math
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - prisma/schema.prisma
  - prisma/migrations/
  - src/lib/dates.ts
  - src/lib/dates.test.ts
  - src/lib/income.ts
  - src/lib/income.test.ts
  - src/lib/foundation.test.ts
  - data/wallet.db
autonomous: false
requirements:
  - FND-SCHEMA
  - FND-CLAMP
  - FND-MIG
  - FND-OCC
estimate:
  tokens: 25000
  raw_tokens: 50000
  tasks: 3
  confidence: high
must_haves:
  truths:
    - "UI-00: Phase 13 ships zero user-facing screens or interactive chrome (no /income route, nav, or dialogs)"
    - Four Prisma models RecurringIncome, OneTimeIncome, RecurringIncomeActual, OneTimeIncomeActual persist after migrate deploy (D-03, D-04, FND-SCHEMA, FND-MIG)
    - Person and Currency FKs on both definition models use Restrict; definition delete Cascades actuals (D-05, D-10, D-11)
    - Money fields plannedAmountMinor / amountMinor are BigInt minors only (D-05, FND-MONEY readiness)
    - Optional note on all four models; no active or endAsOf columns (D-01, D-02)
    - "@@unique([recurringIncomeId, plannedAsOf]) and @@unique([oneTimeIncomeId, plannedAsOf]) exist (D-09)"
    - clampDayOfMonth lives in @/lib/dates and maps DOM 31 to last day of short months without skipping the month (D-16, FND-CLAMP)
    - Tracer listRecurringOccurrences returns one inclusive-range slot with bigint plannedAmountMinor for a single parent (D-13, D-15, FND-OCC)
    - Occurrence key shape is parentId plus plannedAsOf (D-06)
    - Models can feed both recurring and one-time slots for later list and NW overlay (D-12 data readiness only)
  prohibitions:
    - statement: MUST NOT add src/app/income or any /income UI surface in this phase
      requirement_id: UI-00
      category: scope
    - statement: MUST NOT write BalanceSnapshot or mutate computeNetWorthRows / historical LOCF from income code
      requirement_id: ISO-01
      category: isolation
    - statement: MUST NOT add new npm packages (rrule, date-fns, money libs, etc.)
      requirement_id: FND-SCHEMA
      category: safety
    - statement: MUST NOT introduce a single polymorphic IncomeSource-or-IncomeActual enum schema
      requirement_id: FND-SCHEMA
      category: values
    - statement: MUST NOT add active or endAsOf columns on income definitions
      requirement_id: FND-SCHEMA
      category: values
  artifacts:
    - path: prisma/schema.prisma
      provides: RecurringIncome, OneTimeIncome, RecurringIncomeActual, OneTimeIncomeActual; Person/Currency inverse relations
    - path: prisma/migrations
      provides: income_schema migration SQL with Restrict/Cascade/unique indexes
    - path: src/lib/dates.ts
      provides: clampDayOfMonth
    - path: src/lib/income.ts
      provides: listRecurringOccurrences tracer export (+ types)
    - path: src/lib/income.test.ts
      provides: clamp-consuming occurrence tracer + schema presence locks
    - path: src/lib/foundation.test.ts
      provides: income table allowlist + income_schema migration name assert
  key_links:
    - from: listRecurringOccurrences
      to: clampDayOfMonth
      via: every recurring slot plannedAsOf
      pattern: "clampDayOfMonth"
    - from: RecurringIncomeActual
      to: RecurringIncome
      via: "onDelete Cascade + @@unique slot key"
      pattern: "onDelete: Cascade"
    - from: Person
      to: RecurringIncome / OneTimeIncome
      via: onDelete Restrict
      pattern: "onDelete: Restrict"
    - from: foundation.test.ts
      to: prisma migrate deploy
      via: sqlite_master income table names
      pattern: "income_schema"
---

## Specless probe fallback

**Recorded skip (visible):** phase has no discrete v1.2 REQ-IDs to probe — probe predicates not generated this run. Coverage uses foundation FND-* IDs from RESEARCH Validation Architecture (FND-OCC, FND-CLAMP, FND-OVER, FND-MONEY, FND-SCHEMA, FND-MIG) plus CONTEXT D-01..D-16.

## Phase Goal

Income domain exists as a side ledger — sources, actuals, virtual plan slots, and day-of-month rules ready for UI (ROADMAP Phase 13).

<objective>
Confirm one-way two-definition / two-actual schema (D-03, D-04), then ship four Prisma models + income_schema migration, clampDayOfMonth, and a thin listRecurringOccurrences tracer with Vitest — then blocking host migrate deploy + foundation allowlist (FND-SCHEMA, FND-CLAMP, FND-MIG, FND-OCC).

Purpose: Prove Debt-mirror side ledger + DOM clamp end-to-end before freeze/one-time/overdue expansion.
Output: Migrated schema, dates clamp, income tracer export, green tracer + foundation tests, host DB up to date.
</objective>

<execution_context>
@/home/artem/Documents/wallet/.cursor/gsd-core/workflows/execute-plan.md
@/home/artem/Documents/wallet/.cursor/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/13-income-schema-domain-math/13-CONTEXT.md
@.planning/phases/13-income-schema-domain-math/13-RESEARCH.md
@.planning/phases/13-income-schema-domain-math/13-PATTERNS.md
@.planning/phases/13-income-schema-domain-math/13-VALIDATION.md
@.planning/phases/13-income-schema-domain-math/13-UI-SPEC.md
@prisma/schema.prisma
@src/lib/dates.ts
@src/lib/debts.ts
@src/lib/foundation.test.ts
@docker/entrypoint.sh
</context>

<assumptions>
## Planner assumptions

- A2 (RESEARCH): Month-keyed freeze merge for D-07 — full algorithm in Plan 02; tracer may pass empty actuals and emit current-DOM candidates only.
- A3: Zod validations deferred to Phase 14 — not this phase.
- A1: Person/Currency relation array names `recurringIncomes` / `oneTimeIncomes` (RESEARCH).
- Specless probe: skipped — no discrete REQ-IDs (see Specless section above).
- node_modules may be absent — `npm install` from lockfile before prisma/vitest (Wave 0).
</assumptions>

## Artifacts this phase produces

- Four Prisma models: `RecurringIncome`, `OneTimeIncome`, `RecurringIncomeActual`, `OneTimeIncomeActual`
- Migration under `prisma/migrations/` named with `income_schema`
- `clampDayOfMonth` in `src/lib/dates.ts` (+ matrix in `dates.test.ts`)
- `src/lib/income.ts` pure domain (tracer in this plan; full API in 13-02/13-03)
- `src/lib/income.test.ts` Vitest
- Updated `src/lib/foundation.test.ts` income table allowlist
- Zero UI routes; zero new npm packages

<tasks>

<task type="checkpoint:decision" gate="blocking">
  <name>Confirm one-way two-definition + two-actual income schema (D-03, D-04)</name>
  <decision>Lock income persistence as four models — RecurringIncome, OneTimeIncome, RecurringIncomeActual, OneTimeIncomeActual — not a single polymorphic source/actual + kind enum (CONTEXT D-03, D-04)</decision>
  <context>
    CONTEXT already recorded D-03/D-04 as one-way. Undoing after migrate means a data migration plus dual call-site rewrites across Phases 14–17. Milestone research ARCHITECTURE.md still sketches a single IncomeSource + enum — CONTEXT overrides that sketch. Confirm before schema lands.
  </context>
  <options>
    <option id="four-models">
      <name>Four models (CONTEXT D-03 / D-04) — recommended</name>
      <pros>Matches locked discuss-phase; clear Restrict/Cascade per parent; unique slot keys per actual table; feeds D-12 one-time + recurring</pros>
      <cons>More tables than research sketch; Person/Currency get two relation arrays each</cons>
    </option>
    <option id="single-source-enum">
      <name>Single IncomeSource + kind enum (stale research sketch)</name>
      <pros>Fewer tables</pros>
      <cons>Contradicts locked D-03/D-04; polymorphic actuals harder for Cascade/unique</cons>
    </option>
    <option id="defs-only-defer-actuals">
      <name>Definition models only; defer actual tables</name>
      <pros>Smaller first migrate</pros>
      <cons>Breaks D-04 and D-09/D-10 this phase; blocks ACT readiness</cons>
    </option>
  </options>
  <resume-signal>Select: four-models (recommended), single-source-enum, or defs-only-defer-actuals</resume-signal>
</task>

<task type="tracer">
  <name>End-to-end income schema + clampDayOfMonth + listRecurringOccurrences tracer</name>
  <reversibility rating="one-way">D-03/D-04 four-model SQLite shape; undoing needs migration and rewrites across Phases 14–17.</reversibility>
  <files>prisma/schema.prisma, prisma/migrations/, src/lib/dates.ts, src/lib/dates.test.ts, src/lib/income.ts, src/lib/income.test.ts</files>
  <read_first>
    .planning/phases/13-income-schema-domain-math/13-CONTEXT.md
    .planning/phases/13-income-schema-domain-math/13-RESEARCH.md
    .planning/phases/13-income-schema-domain-math/13-PATTERNS.md
    prisma/schema.prisma
    prisma/migrations/20260904180755_debts_schema/migration.sql
    src/lib/dates.ts
    src/lib/dates.test.ts
    src/lib/debts.ts
    src/lib/debts.test.ts
    src/lib/foundation.test.ts
  </read_first>
  <behavior>
    - clampDayOfMonth(2025, 2, 31) === 2025-02-28; clampDayOfMonth(2024, 2, 31) === 2024-02-29; clampDayOfMonth(2026, 4, 31) === 2026-04-30 (D-16, FND-CLAMP)
    - listRecurringOccurrences with one parent dayOfMonth 31, startAsOf before Feb window, empty actuals, from/to covering Feb returns one occurrence whose plannedAsOf is clamped Feb last day and plannedAmountMinor stays bigint (FND-OCC, D-05)
    - schema defines four income models with BigInt money, optional note, Person/Currency Restrict, Cascade actuals, @@unique slot keys (D-01..D-05, D-09..D-11)
    - income.ts has no Prisma and no net-worth / historical-series imports
  </behavior>
  <action>
    After checkpoint selects four-models (per D-03, D-04): run npm install from package-lock if node_modules missing — restore pins only; add zero new packages. Extend prisma/schema.prisma with RecurringIncome and OneTimeIncome definitions (personId Restrict per D-11, currencyCode Restrict per D-05, plannedAmountMinor BigInt, optional note per D-02, timestamps) — RecurringIncome has dayOfMonth Int and startAsOf String; OneTimeIncome has plannedAsOf String; omit active and endAsOf per D-01 (costly omission). Add RecurringIncomeActual and OneTimeIncomeActual with parent FK onDelete Cascade per D-10, plannedAsOf + actualAsOf strings, amountMinor BigInt, optional note, createdAt, and @@unique on (parentId, plannedAsOf) per D-09. Add recurringIncomes / oneTimeIncomes arrays on Person and Currency. Generate migration non-interactively named income_schema (migrate dev --create-only or equivalent); SQL must ON DELETE RESTRICT for Person/Currency→defs and ON DELETE CASCADE for defs→actuals plus unique indexes. Implement clampDayOfMonth(year, month1to12, dayOfMonth) in src/lib/dates.ts using Date.UTC last-day via day-0 of next month index (PATTERNS / RESEARCH Pattern 2) — UTC family matching addCalendarDays (D-16). Extend dates.test.ts with DOM 31×Feb leap/non-leap and 31×Apr cases (FND-CLAMP). Implement src/lib/income.ts pure tracer export listRecurringOccurrences(defs, actuals, from, to) for the single happy path: walk months overlapping inclusive [from, to], clamp current dayOfMonth, include iff candidate &gt;= startAsOf and from &lt;= candidate &lt;= to (D-13, D-15); actuals may be ignored or empty in tracer; return objects with parentId, plannedAsOf, plannedAmountMinor as bigint (D-05, D-06). Do not add default horizon wrappers (D-13). Do not implement listOneTimeOccurrences / listAllInRange / freeze merge / isIncomeOverdue here (Plans 02–03). Do not create app/income routes or Server Actions. Do not import income into net-worth or historical-series. Do not write BalanceSnapshot.
  </action>
  <verify>
    <automated>test -d node_modules || npm install; npx vitest run src/lib/dates.test.ts src/lib/income.test.ts &amp;&amp; grep -q 'function clampDayOfMonth' src/lib/dates.ts &amp;&amp; grep -q 'listRecurringOccurrences' src/lib/income.ts &amp;&amp; grep -q 'model RecurringIncome' prisma/schema.prisma &amp;&amp; grep -q 'model OneTimeIncome' prisma/schema.prisma &amp;&amp; grep -q 'model RecurringIncomeActual' prisma/schema.prisma &amp;&amp; grep -q 'model OneTimeIncomeActual' prisma/schema.prisma &amp;&amp; ls prisma/migrations/*income_schema*/migration.sql</automated>
    <fails_when>vitest non-zero, clampDayOfMonth or listRecurringOccurrences missing, any of four models absent, or income_schema migration.sql missing</fails_when>
  </verify>
  <acceptance_criteria>
    - prisma/schema.prisma defines RecurringIncome, OneTimeIncome, RecurringIncomeActual, OneTimeIncomeActual with D-05 BigInt, D-02 note, D-10 Cascade, D-11 Restrict, D-09 unique slots, and no active/endAsOf (D-01, D-03, D-04)
    - src/lib/dates.ts exports clampDayOfMonth; dates.test.ts covers 31→Feb leap/non-leap and 31→Apr (D-16, FND-CLAMP)
    - src/lib/income.ts exports listRecurringOccurrences; income.test.ts asserts Feb DOM-31 clamp slot + bigint plannedAmountMinor (FND-OCC)
    - Migration folder name includes income_schema
    - No src/app/income path created (UI-00)
  </acceptance_criteria>
  <done>Tracer path green: four-model schema + clamp + listRecurringOccurrences; migrate deploy is next task</done>
</task>

<task type="auto">
  <name>[BLOCKING] Host prisma migrate deploy + foundation income table allow-list</name>
  <files>data/wallet.db, prisma/migrations/, src/lib/foundation.test.ts</files>
  <read_first>
    .planning/phases/13-income-schema-domain-math/13-RESEARCH.md
    .planning/phases/13-income-schema-domain-math/13-PATTERNS.md
    prisma/schema.prisma
    src/lib/foundation.test.ts
    docker/entrypoint.sh
  </read_first>
  <precondition>prisma/migrations contains the new income_schema migration.sql from the tracer task; node_modules present after npm install</precondition>
  <reversibility rating="costly">Host migrate applies income tables permanently; dropping unique or splitting models later needs another migration (D-09 costly).</reversibility>
  <action>
    If host SQLite is locked by wallet-web, stop the container briefly (Phase 02 lesson). Run DATABASE_URL=file:./data/wallet.db npx prisma migrate deploy (not db push). Confirm migrate status reports up to date. Update src/lib/foundation.test.ts sqlite_master IN-list and expected sorted table array to include RecurringIncome, OneTimeIncome, RecurringIncomeActual, and OneTimeIncomeActual alongside existing Currency/Account/FxRate/BalanceSnapshot/Person/Debt/DebtRepayment/DebtSizeChange/_prisma_migrations (FND-MIG). Assert applied migrations include a name containing income_schema. Run npx prisma generate if client types lag. Do not COPY wallet.db into Docker images. Do not add npm packages.
  </action>
  <verify>
    <automated>mkdir -p data &amp;&amp; DATABASE_URL="file:./data/wallet.db" npx prisma migrate deploy &amp;&amp; test -f data/wallet.db &amp;&amp; DATABASE_URL="file:./data/wallet.db" npx prisma migrate status 2&gt;&amp;1 | grep -qi 'up to date\|Database schema is up to date\|No pending migrations' &amp;&amp; npx vitest run src/lib/foundation.test.ts</automated>
    <fails_when>migrate deploy non-zero, data/wallet.db missing, pending migrations remain, or foundation.test.ts fails income table / income_schema expectations</fails_when>
  </verify>
  <acceptance_criteria>
    - DATABASE_URL=file:./data/wallet.db npx prisma migrate deploy exits 0 (FND-MIG)
    - foundation.test.ts expects four income tables and income_schema migration name
    - prisma migrate status reports no pending migrations
  </acceptance_criteria>
  <done>Blocking host migrate deploy succeeded; foundation proves income tables live</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Schema/migration → SQLite file | On-disk income side ledger; FK Restrict/Cascade/unique must match CONTEXT |
| Pure income helpers → callers | Untrusted DOM / range strings cross into occurrence math |
| Income module → NW / BalanceSnapshot | Isolation boundary; coupling would corrupt capital history (ISO-01) |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-13-01 | Tampering | Person→income defs delete | high | mitigate | onDelete Restrict (D-11) in schema + migration SQL |
| T-13-02 | Tampering | Def→actuals orphan/wipe | medium | mitigate | onDelete Cascade only on intentional definition delete (D-10); Person cannot cascade-wipe via Restrict |
| T-13-03 | Tampering | Duplicate actuals / double facts | medium | mitigate | @@unique([parentId, plannedAsOf]) (D-09) |
| T-13-04 | Tampering | Float money / cent loss | high | mitigate | BigInt minors only on plan/actual amount fields (D-05) |
| T-13-05 | Tampering | BalanceSnapshot / computeNetWorthRows | high | mitigate | No income actions this phase; income.ts must not import NW writers (ISO-01); light scan Plan 03 |
| T-13-06 | Tampering | Invalid DOM schedule corruption | medium | mitigate | clampDayOfMonth + Vitest matrix (D-16, FND-CLAMP) |
| T-13-SC | Tampering | npm installs | high | mitigate | Zero new packages — no install legitimacy checkpoint required |
</threat_model>

<verification>
npx vitest run src/lib/dates.test.ts src/lib/income.test.ts src/lib/foundation.test.ts green; migrate deploy up to date; four models present.
</verification>

<success_criteria>
Income tables migrate on host; clampDayOfMonth + listRecurringOccurrences tracer green; UI-00 no /income UI; ISO-01 no BalanceSnapshot writes from this plan.
</success_criteria>

<output>
Create `.planning/phases/13-income-schema-domain-math/13-01-SUMMARY.md` when done
</output>
