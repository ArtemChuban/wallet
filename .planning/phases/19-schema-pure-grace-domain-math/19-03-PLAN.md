---
phase: 19-schema-pure-grace-domain-math
plan: 03
type: execute
wave: 2
depends_on:
  - "19-01"
files_modified:
  - src/lib/validations/account.ts
  - src/lib/validations/account.test.ts
  - src/lib/validations/credit-grace.ts
  - src/app/accounts/actions.ts
autonomous: true
requirements:
  - CYCLE-01
estimate:
  tokens: 28000
  raw_tokens: 56000
  tasks: 2
  confidence: high
must_haves:
  truths:
    - User/write-path can persist dual DOM on FIAT_CREDIT via updateGraceSchedule Server Action + Zod (CYCLE-01, D-01, D-02, D-03)
    - Partial schedule (one DOM set) rejected by Zod (D-03, T-19-01)
    - Non-FIAT_CREDIT accounts cannot set DOM fields (D-02)
    - Clearing both DOM to null rejected while any OPEN CreditGraceObligation exists (D-14); CLOSED-only history allowed after OPEN cleared
    - updateGraceSchedule updates Account DOM only — never rewrites obligation cycleStartAsOf / dueAsOf rows (D-04, D-05)
    - Obligation Zod ready: amountMinor required, dueAsOf present, OPEN|CLOSED + closedAsOf pairing, note optional (D-05…D-08); duplicate cycle = error semantics documented for P2002 (D-16)
    - Null schedule ⇒ create obligation rejected at schema/action boundary documentation (D-13)
  prohibitions:
    - statement: MUST NOT add grace form UI chrome this phase
      requirement_id: UI-00
      category: scope
    - statement: MUST NOT write snapshot ledger rows from grace schedule action
      requirement_id: CYCLE-01
      category: isolation
  artifacts:
    - path: src/lib/validations/account.ts
      provides: updateGraceScheduleSchema (accountId + dual DOM nullable pairing)
    - path: src/lib/validations/account.test.ts
      provides: both-set / partial / both-null / non-credit reject cases
    - path: src/lib/validations/credit-grace.ts
      provides: obligation create/update Zod ready for Phase 20 (D-05…D-08, D-16)
    - path: src/app/accounts/actions.ts
      provides: updateGraceSchedule action with D-14 OPEN clear guard
  key_links:
    - from: updateGraceSchedule
      to: updateGraceScheduleSchema
      via: FormData safeParse then prisma.account.update
      pattern: "updateGraceSchedule"
    - from: updateGraceSchedule clear path
      to: CreditGraceObligation OPEN count
      via: reject when OPEN greater than zero (D-14)
      pattern: "OPEN"
---

## Specless probe fallback

Empty/null schedule write-path: both-null clear allowed only with zero OPEN (D-13/D-14). Ordering/adjacency owned by Plan 02 pure math.

<objective>
Ship minimal CYCLE-01 write-path: updateGraceScheduleSchema + updateGraceSchedule action (FIAT_CREDIT dual DOM, D-14 clear guard, D-04 no obligation rewrite) and obligation Zod module ready for Phase 20 (D-05…D-08, D-13, D-16).

Purpose: Persist schedule without UI chrome; hand Phase 20 CRUD validated shapes.
Output: Zod + Server Action + tests; no form pages.
</objective>

<execution_context>
@/home/artem/Documents/wallet/.cursor/gsd-core/workflows/execute-plan.md
@/home/artem/Documents/wallet/.cursor/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/19-schema-pure-grace-domain-math/19-CONTEXT.md
@.planning/phases/19-schema-pure-grace-domain-math/19-RESEARCH.md
@.planning/phases/19-schema-pure-grace-domain-math/19-PATTERNS.md
@.planning/phases/19-schema-pure-grace-domain-math/19-UI-SPEC.md
@src/lib/validations/account.ts
@src/lib/validations/account.test.ts
@src/lib/validations/income.ts
@src/app/accounts/actions.ts
</context>

<assumptions>
- A3 (RESEARCH): Server Action + Zod satisfies CYCLE-01 without AccountFormDialog fields (UI-SPEC / Deferred Phase 20).
- CLOSED obligations do not block schedule clear (D-14 OPEN-only) — Claude discretion.
- Obligation create Server Action deferred to Phase 20; this plan ships Zod only + D-16 P2002 comment/message helper if useful.
</assumptions>

## Artifacts this phase produces

- `updateGraceScheduleSchema` (+ exported types) in `src/lib/validations/account.ts`
- `updateGraceSchedule` in `src/app/accounts/actions.ts`
- `src/lib/validations/credit-grace.ts` (create/update obligation schemas)
- Extended `src/lib/validations/account.test.ts`
- Zero UI form components

<tasks>

<task type="auto" tdd="true">
  <name>updateGraceScheduleSchema + dual-DOM Zod tests</name>
  <reversibility rating="costly">Write-path Zod is FIAT_CREDIT schedule contract; changing pairing messages/fields touches Plan 03 action and Phase 20 forms (D-02, D-03).</reversibility>
  <files>src/lib/validations/account.ts, src/lib/validations/account.test.ts</files>
  <read_first>
    .planning/phases/19-schema-pure-grace-domain-math/19-CONTEXT.md
    .planning/phases/19-schema-pure-grace-domain-math/19-PATTERNS.md
    src/lib/validations/account.ts
    src/lib/validations/account.test.ts
    src/lib/validations/income.ts
  </read_first>
  <behavior>
    - both statementDayOfMonth and dueDayOfMonth 1–31 accepted together
    - partial (only one set) rejected with Russian pairing message
    - both null / both omitted accepted as clear intent at schema layer (D-03, D-14 enforced in action)
    - DOM outside 1–31 rejected
  </behavior>
  <action>
    Add updateGraceScheduleSchema in src/lib/validations/account.ts: accountId positive int; statementDayOfMonth and dueDayOfMonth as nullable coerced ints min 1 max 31 (reuse income dayOfMonth range pattern). superRefine both-null-or-both-set (D-03) with Russian message Укажите обе даты или очистите обе. Export UpdateGraceScheduleInput type. Extend account.test.ts: accept both 21 and 15; reject partial; accept both null; reject 0 and 32. Do not add UI components. Do not change createAccountSchema credit-limit rules except if shared day helpers are extracted without behavior change.
  </action>
  <verify>
    <automated>npx vitest run src/lib/validations/account.test.ts</automated>
    <fails_when>non-zero exit, failing assertion, or "0 passed" in the vitest summary</fails_when>
  </verify>
  <acceptance_criteria>
    - updateGraceScheduleSchema exported; pairing refine covers D-03
    - account.test.ts covers both-set / partial / both-null / out-of-range (CYCLE-01, D-03)
  </acceptance_criteria>
  <done>Dual-DOM Zod pairing green</done>
</task>

<task type="auto">
  <name>updateGraceSchedule action + obligation Zod ready</name>
  <reversibility rating="costly">D-14 clear guard and D-15 Cascade semantics are product contracts; D-04 no-rewrite of frozen dues is Phase 20/21 safety (costly if broken).</reversibility>
  <files>src/app/accounts/actions.ts, src/lib/validations/credit-grace.ts</files>
  <read_first>
    .planning/phases/19-schema-pure-grace-domain-math/19-CONTEXT.md
    .planning/phases/19-schema-pure-grace-domain-math/19-RESEARCH.md
    .planning/phases/19-schema-pure-grace-domain-math/19-PATTERNS.md
    src/app/accounts/actions.ts
    src/lib/validations/account.ts
    src/lib/validations/income.ts
  </read_first>
  <action>
    Add updateGraceSchedule(_prev, formData) in src/app/accounts/actions.ts mirroring updateAccountName shape: parse id + DOM fields via updateGraceScheduleSchema.safeParse; load Account; reject if type is not FIAT_CREDIT (D-02); when both DOMs clearing to null, count CreditGraceObligation with status OPEN for that accountId — if count &gt; 0 return error (D-14); otherwise prisma.account.update only statementDayOfMonth and dueDayOfMonth (D-01). Never update CreditGraceObligation rows in this action (D-04, D-05 frozen dues). revalidatePath /accounts and /. Extend AccountActionState errors keys for statementDayOfMonth / dueDayOfMonth if needed. Create src/lib/validations/credit-grace.ts with Phase-20-ready schemas: create obligation requires accountId, cycleStartAsOf, dueAsOf (asOf regex), amountMajor/minor path consistent with income (amount required D-06), optional note; status OPEN|CLOSED with closedAsOf required iff CLOSED (D-07, D-08); document that duplicate (accountId, cycleStartAsOf) maps to unique violation / error (D-16); reject create when schedule absent is an action-level rule for Phase 20 — add a pure exported assert or schema refine comment that both DOMs must be set on account before create (D-13). Do not implement obligation CRUD actions or UI. Do not write snapshot ledger rows from this action.
  </action>
  <verify>
    <automated>npx vitest run src/lib/validations/account.test.ts &amp;&amp; grep -q 'updateGraceSchedule' src/app/accounts/actions.ts &amp;&amp; grep -q 'updateGraceScheduleSchema' src/lib/validations/account.ts &amp;&amp; test -f src/lib/validations/credit-grace.ts &amp;&amp; grep -q 'closedAsOf' src/lib/validations/credit-grace.ts &amp;&amp; rg -n 'export async function updateGraceSchedule' -A80 src/app/accounts/actions.ts | rg -q 'OPEN'</automated>
    <fails_when>account.test.ts non-zero or "0 passed", updateGraceSchedule or updateGraceScheduleSchema missing, validations/credit-grace.ts missing closedAsOf, or updateGraceSchedule body lacks OPEN clear guard</fails_when>
  </verify>
  <acceptance_criteria>
    - updateGraceSchedule persists dual DOM for FIAT_CREDIT only (D-02, CYCLE-01)
    - Clear-to-null blocked when OPEN obligations exist (D-14)
    - Action does not mutate obligation dueAsOf/cycleStartAsOf (D-04, D-05)
    - validations/credit-grace.ts encodes amount required + OPEN|CLOSED/closedAsOf + unique-cycle error readiness (D-06…D-08, D-16)
    - No new form/dialog UI files this plan (UI-SPEC)
  </acceptance_criteria>
  <done>Minimal CYCLE-01 persist path + obligation Zod ready for Phase 20</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| FormData / client → Server Action | Untrusted DOM ints and clear intent |
| Action → SQLite Account / CreditGraceObligation | Type gate + OPEN clear guard |
| Grace write path → snapshot ledger | Must not write snapshots (T-19-03) |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-19-01 | Tampering | Partial / out-of-range DOM | high | mitigate | updateGraceScheduleSchema both-null-or-both + 1–31; DB CHECK already from Plan 01 |
| T-19-02 | Tampering | Obligation create without schedule | medium | mitigate | D-13 documented in credit-grace validations; Phase 20 action enforces; helpers empty |
| T-19-03 | Tampering | Snapshot writes from grace action | high | mitigate | updateGraceSchedule updates Account DOM fields only; no snapshot create/update calls |
| T-19-04 | Elevation of Privilege | Cascade wipe surprise | medium | accept | Locked D-15 — no Restrict switch; document for Phase 20 RU |
| T-19-SC | Tampering | npm installs | high | mitigate | Zero new packages |
</threat_model>

<verification>
npx vitest run src/lib/validations/account.test.ts; updateGraceSchedule + credit-grace validations present; OPEN clear guard in action.
</verification>

<success_criteria>
CYCLE-01 persist path works via Zod + action without UI; D-14 clear guard; obligation Zod ready; no snapshot writes.
</success_criteria>

<output>
Create `.planning/phases/19-schema-pure-grace-domain-math/19-03-SUMMARY.md` when done
</output>
