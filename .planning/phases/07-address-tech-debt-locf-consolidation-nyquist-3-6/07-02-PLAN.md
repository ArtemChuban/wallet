---
phase: 07-address-tech-debt-locf-consolidation-nyquist-3-6
plan: 02
type: execute
wave: 2
depends_on:
  - 07-01
files_modified:
  - src/lib/historical-series.ts
  - src/lib/balances.ts
  - src/lib/fx.ts
autonomous: true
requirements:
  - LOCF-03
  - LOCF-04
estimate:
  tokens: 20000
  raw_tokens: 40000
  tasks: 2
  confidence: high
must_haves:
  truths:
    - historical-series builders call shared locfAmountAsOf and locfRateAsOf from @/lib/locf with no private duplicate scanners (LOCF-03, D-05)
    - CHART-03 and D-16 cases in historical-series.test.ts stay green (as-of balance times as-of FX; primary mode skips null FX)
    - getBalanceAsOf and getRateAsOf remain public Prisma findFirst thin wrappers in balances.ts and fx.ts (LOCF-04)
    - balances.test.ts and fx.test.ts stay green for null-before-first Prisma contracts (BAL-02 / FX D-15)
  prohibitions:
    - statement: MUST NOT coalesce null FX to identity inside series builders
      requirement_id: LOCF-03
      category: values
    - statement: MUST NOT delete getBalanceAsOf or getRateAsOf
      requirement_id: LOCF-04
      category: safety
    - statement: MUST NOT route list or dashboard pages through N times get*AsOf
      requirement_id: LOCF-04
      category: values
    - statement: MUST NOT change Prisma schema
      requirement_id: LOCF-03
      category: safety
  artifacts:
    - path: src/lib/historical-series.ts
      provides: series builders using shared locf wrappers
    - path: src/lib/balances.ts
      provides: retained getBalanceAsOf Prisma wrapper
    - path: src/lib/fx.ts
      provides: retained getRateAsOf Prisma wrapper
  key_links:
    - from: src/lib/historical-series.ts
      to: src/lib/locf.ts
      via: locfAmountAsOf / locfRateAsOf imports
      pattern: "from \"@/lib/locf\""
    - from: src/lib/balances.ts
      to: prisma.balanceSnapshot.findFirst
      via: getBalanceAsOf lte + orderBy desc
      pattern: "getBalanceAsOf"
    - from: src/lib/fx.ts
      to: prisma.fxRate.findFirst
      via: getRateAsOf lte + orderBy desc
      pattern: "getRateAsOf"
---

## Phase Goal

Consolidate triplicate LOCF into one shared path with unchanged semantics, then close Nyquist for phases 3–6 with evidence. No new user-facing features.

## Artifacts this phase produces

| Artifact | Plan | Purpose |
|----------|------|---------|
| `src/lib/locf.ts` | 01 | Shared pure pick + batch Map builders + typed wrappers |
| `src/lib/locf.test.ts` | 01 | Null-before-first + pure↔map parity |
| `src/app/{accounts,currencies/rates,page}.tsx` | 01 | Batch LOCF via firstHitLocfMap |
| `src/lib/historical-series.ts` | 02 | Shared locfAmountAsOf / locfRateAsOf for CHART-03 |
| `src/lib/balances.ts` / `src/lib/fx.ts` | 02 | Keep Prisma get*AsOf wrappers |
| `03\|04\|05\|06-VALIDATION.md` | 03 | Nyquist validated |

## SPECLESS probe fallback (recorded)

phase_req_ids was null/TBD. Spec-less probe skipped. Requirements: RESEARCH **LOCF-01..05** / **NYQ-03..06**.

<objective>
Wave 2: rewire historical-series onto shared locf wrappers (LOCF-03, D-05) and keep Prisma getBalanceAsOf / getRateAsOf thin wrappers with green unit contracts (LOCF-04).

Purpose: Finish consolidation so charts and Prisma tests share one semantics surface.
Output: historical-series imports @/lib/locf; private locf scanners removed; balances/fx wrappers retained; suite green for series + balances + fx.
</objective>

<execution_context>
@/home/artem/Documents/wallet/.cursor/gsd-core/workflows/execute-plan.md
@/home/artem/Documents/wallet/.cursor/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/07-address-tech-debt-locf-consolidation-nyquist-3-6/07-CONTEXT.md
@.planning/phases/07-address-tech-debt-locf-consolidation-nyquist-3-6/07-RESEARCH.md
@.planning/phases/07-address-tech-debt-locf-consolidation-nyquist-3-6/07-PATTERNS.md
@.planning/phases/07-address-tech-debt-locf-consolidation-nyquist-3-6/07-01-PLAN.md
@src/lib/locf.ts
@src/lib/historical-series.ts
@src/lib/historical-series.test.ts
@src/lib/balances.ts
@src/lib/fx.ts
@src/lib/balances.test.ts
@src/lib/fx.test.ts
</context>

<precondition>
Plan 07-01 SUMMARY exists and src/lib/locf.ts exports locfAmountAsOf and locfRateAsOf used by this plan.
</precondition>

<tasks>

<task type="auto" tdd="true">
  <name>Rewire historical-series to shared locfAmountAsOf / locfRateAsOf</name>
  <reversibility rating="costly">Series math now depends on exported locf wrappers; changing return shape later breaks CHART-03 builders and tests.</reversibility>
  <files>src/lib/historical-series.ts</files>
  <read_first>
    .planning/phases/07-address-tech-debt-locf-consolidation-nyquist-3-6/07-PATTERNS.md
    src/lib/locf.ts
    src/lib/historical-series.ts
    src/lib/historical-series.test.ts
  </read_first>
  <behavior>
    - buildNetWorthSeries and buildAccountSeries still use LOCF amount and rate helpers with identical call shapes (account id / currency code / asOfDate)
    - primary-mode account series still skips sample dates when locfRateAsOf returns null (D-16)
    - historical-series.test.ts CHART-03 later-FX regression and D-16 null-FX skip cases pass
  </behavior>
  <action>
    Per D-05, LOCF-03: import locfAmountAsOf and locfRateAsOf from @/lib/locf into src/lib/historical-series.ts. Delete the private function locfAmountAsOf and private function locfRateAsOf definitions in historical-series.ts. Keep all call sites argument order and null handling identical, including D-16 continue when rate is null. Do not change computeNetWorthRows wiring, event∪today sampling, or chart components. Run existing historical-series tests as the regression gate (tdd: confirm red only if import broken, then green).
  </action>
  <verify>
    <automated>npm test -- src/lib/historical-series.test.ts src/lib/locf.test.ts && grep -q 'from "@/lib/locf"' src/lib/historical-series.ts && grep -q 'locfAmountAsOf' src/lib/historical-series.ts && grep -q 'locfRateAsOf' src/lib/historical-series.ts</automated>
    <fails_when>non-zero vitest exit, or historical-series.ts missing @/lib/locf import or locfAmountAsOf/locfRateAsOf usage</fails_when>
  </verify>
  <acceptance_criteria>
    - historical-series.ts imports from @/lib/locf
    - no private function locfAmountAsOf or locfRateAsOf declaration remains inside historical-series.ts (only imports/calls)
    - npm test -- src/lib/historical-series.test.ts src/lib/locf.test.ts exits 0
  </acceptance_criteria>
  <done>Chart series LOCF goes through shared locf.ts; CHART-03 / D-16 tests green (LOCF-03)</done>
</task>

<task type="auto">
  <name>Keep Prisma getBalanceAsOf / getRateAsOf wrappers + green contracts</name>
  <files>src/lib/balances.ts, src/lib/fx.ts</files>
  <read_first>
    .planning/phases/07-address-tech-debt-locf-consolidation-nyquist-3-6/07-RESEARCH.md
    src/lib/balances.ts
    src/lib/fx.ts
    src/lib/balances.test.ts
    src/lib/fx.test.ts
    src/lib/locf.ts
  </read_first>
  <action>
    Per LOCF-04 and RESEARCH discretion: keep getBalanceAsOf in src/lib/balances.ts and getRateAsOf in src/lib/fx.ts as public async Prisma findFirst wrappers (where asOfDate lte, orderBy asOfDate desc). Do not retarget pages to these helpers. Optionally add one short comment in each file pointing pure/batch callers at @/lib/locf — no behavior change. Confirm balances.test.ts and fx.test.ts still pass for null-before-first and latest-row contracts. Do not delete exports. No schema changes.
  </action>
  <verify>
    <automated>npm test -- src/lib/balances.test.ts src/lib/fx.test.ts src/lib/historical-series.test.ts src/lib/locf.test.ts && grep -q 'export async function getBalanceAsOf' src/lib/balances.ts && grep -q 'export async function getRateAsOf' src/lib/fx.ts</automated>
    <fails_when>non-zero vitest exit, or getBalanceAsOf / getRateAsOf export missing from balances.ts / fx.ts</fails_when>
  </verify>
  <acceptance_criteria>
    - getBalanceAsOf and getRateAsOf still exported from balances.ts and fx.ts
    - npm test -- src/lib/balances.test.ts src/lib/fx.test.ts src/lib/historical-series.test.ts src/lib/locf.test.ts exits 0
  </acceptance_criteria>
  <done>Prisma LOCF wrappers retained with green BAL/FX unit contracts (LOCF-04); LOCF consolidation complete</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| historical-series pure math → chart majors | Null FX must skip primary points (D-16), not invent rates |
| Prisma get*AsOf → tests / optional single lookup | Query stays parameterized findFirst |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-07-01 | Tampering | historical-series LOCF rewire | high | mitigate | Keep null FX skip; run historical-series.test.ts CHART-03 / D-16 cases every commit |
| T-07-04 | Tampering | getBalanceAsOf / getRateAsOf | medium | mitigate | Keep Prisma parameterized findFirst; do not replace with raw SQL |
| T-07-05 | Repudiation | invented leading balances/rates | medium | mitigate | Retain null-before-first in wrappers and shared pure helpers |
| T-07-SC | Tampering | npm installs | high | accept | No packages installed this plan |
</threat_model>

<verification>
npm test -- src/lib/locf.test.ts src/lib/balances.test.ts src/lib/fx.test.ts src/lib/historical-series.test.ts then npm test as wave gate.
</verification>

<success_criteria>
historical-series uses @/lib/locf; private duplicate scanners gone; get*AsOf wrappers remain; LOCF-focused Vitest suites green.
</success_criteria>

<output>
Create `.planning/phases/07-address-tech-debt-locf-consolidation-nyquist-3-6/07-02-SUMMARY.md` when done
</output>
