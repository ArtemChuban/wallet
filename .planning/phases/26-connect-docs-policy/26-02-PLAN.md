---
phase: 26-connect-docs-policy
plan: 02
type: execute
wave: 2
depends_on:
  - "26-01"
files_modified:
  - src/lib/mcp/tools/income.ts
  - src/lib/mcp/tools/income.test.ts
  - src/lib/mcp/tools/grace.ts
  - src/lib/mcp/tools/grace.test.ts
  - src/lib/mcp/tools/forecast.ts
  - src/lib/mcp/tools/forecast.test.ts
  - src/lib/mcp/tools/accounts.ts
  - src/lib/mcp/tools/net-worth.ts
  - src/lib/mcp/tools/balances.ts
  - src/lib/mcp/tools/fx.ts
  - .planning/phases/26-connect-docs-policy/26-VALIDATION.md
autonomous: true
requirements:
  - CLI-01
estimate:
  tokens: 25000
  raw_tokens: 50000
  tasks: 3
  confidence: high
must_haves:
  truths:
    - list_income description exports INISO-01 do-not-fold prose (D-09)
    - list_grace_obligations description exports GRISO-01 do-not-fold prose (D-09)
    - get_forecast_overlay description cites INISO-01 and GRISO-01 (D-09, RESEARCH A2)
    - CAP tool descriptions stay free of SIDE isolation rule ids; light honesty polish only (D-09)
    - Full src/lib/mcp vitest suite green including isolation-contract
  prohibitions:
    - statement: MUST NOT add isolation fields on tool JSON payloads
      category: isolation
    - statement: MUST NOT invent a FORECAST-01 rule id
      category: scope
    - statement: MUST NOT install new npm packages
      category: scope
    - statement: MUST NOT change create-handler registration order or add tools
      category: scope
  artifacts:
    - path: src/lib/mcp/tools/income.ts
      provides: INISO-01 in LIST_INCOME_DESCRIPTION
    - path: src/lib/mcp/tools/grace.ts
      provides: GRISO-01 in LIST_GRACE_OBLIGATIONS_DESCRIPTION
    - path: src/lib/mcp/tools/forecast.ts
      provides: INISO-01/GRISO-01 in GET_FORECAST_OVERLAY_DESCRIPTION
  key_links:
    - from: LIST_INCOME_DESCRIPTION
      to: isolation / agent honesty
      via: INISO-01 sentence
      pattern: "INISO-01"
    - from: LIST_GRACE_OBLIGATIONS_DESCRIPTION
      to: isolation / agent honesty
      via: GRISO-01 sentence
      pattern: "GRISO-01"
    - from: GET_FORECAST_OVERLAY_DESCRIPTION
      to: overlay membership
      via: INISO-01/GRISO-01 combined sentence
      pattern: "INISO-01"
---

## Specless probe fallback

SPEC absent. Expands Plan 01 tracer; CONTEXT D-07…D-11 + RESEARCH Pattern 1.

## Phase Goal

Claude Code / Cursor CLI can connect with copy-paste configs; tools declare read-only + isolation; PARITY standing rule is project-visible.

<objective>
Complete CLI-01 SIDE description coverage for list_income, list_grace_obligations, and get_forecast_overlay (D-07, D-08, D-09); apply light CAP honesty polish without SIDE rule ids (D-09); keep D-11 no payload meta; green full mcp vitest suite.

Purpose: Finish agent-facing isolation copy on every SIDE tool after tracer proved the pattern.
Output: Named SIDE descriptions + updated tool tests; optional CAP wording polish; VALIDATION nyquist_compliant true.
</objective>

<execution_context>
@/home/artem/Documents/wallet/.cursor/gsd-core/workflows/execute-plan.md
@/home/artem/Documents/wallet/.cursor/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/26-connect-docs-policy/26-CONTEXT.md
@.planning/phases/26-connect-docs-policy/26-RESEARCH.md
@.planning/phases/26-connect-docs-policy/26-PATTERNS.md
@.planning/phases/26-connect-docs-policy/26-01-SUMMARY.md
@src/lib/mcp/tools/income.ts
@src/lib/mcp/tools/grace.ts
@src/lib/mcp/tools/forecast.ts
@src/lib/mcp/tools/accounts.ts
@src/lib/mcp/tools/net-worth.ts
@src/lib/mcp/tools/balances.ts
@src/lib/mcp/tools/fx.ts
</context>

<assumptions>
## Planner assumptions

- A2: forecast uses INISO-01/GRISO-01 combined — not a new rule id.
- create-handler already has all three rule ids from Plan 01 — do not regress.
- CAP tools already have readOnlyHint/openWorldHint — polish descriptions only.
</assumptions>

<tasks>

<task type="auto" tdd="true">
  <name>Named INISO/GRISO on income + grace descriptions</name>
  <reversibility rating="costly">D-07: published SIDE description surface for income/grace.</reversibility>
  <files>src/lib/mcp/tools/income.ts, src/lib/mcp/tools/income.test.ts, src/lib/mcp/tools/grace.ts, src/lib/mcp/tools/grace.test.ts</files>
  <behavior>
    - LIST_INCOME_DESCRIPTION matches INISO-01 and do-not-fold / historical NW language
    - LIST_GRACE_OBLIGATIONS_DESCRIPTION matches GRISO-01 and do-not-fold / historical NW language
    - Existing payload asserts without isolation meta property stay green (D-11)
  </behavior>
  <read_first>
    - src/lib/mcp/tools/income.ts LIST_INCOME_DESCRIPTION closer
    - src/lib/mcp/tools/grace.ts LIST_GRACE_OBLIGATIONS_DESCRIPTION closer
    - matching *.test.ts description contract blocks
    - 26-RESEARCH.md Pattern 1 templates
  </read_first>
  <action>
    Per D-08 / D-09 / D-11: Replace income and grace description closers with short named sentences from RESEARCH (INISO-01 for Доходы; GRISO-01 for Грейс; do not fold into historical NW / Капитал LOCF). Keep EN+RU bodies and annotations. Update income.test.ts and grace.test.ts description regexes to require the rule ids. Do not add payload meta fields. Do not edit create-handler unless Plan 01 prose regressed (restore only).
  </action>
  <verify>
    <automated>npx vitest run src/lib/mcp/tools/income.test.ts src/lib/mcp/tools/grace.test.ts</automated>
    <fails_when>non-zero exit, "0 passed" in summary, or description contracts missing INISO-01/GRISO-01</fails_when>
  </verify>
  <acceptance_criteria>
    - grep -q 'INISO-01' src/lib/mcp/tools/income.ts
    - grep -q 'GRISO-01' src/lib/mcp/tools/grace.ts
    - vitest exit 0 on income + grace tests
  </acceptance_criteria>
  <done>Income and grace SIDE tools ship named isolation sentences; tests require them.</done>
</task>

<task type="auto" tdd="true">
  <name>Forecast overlay INISO/GRISO sentence + CAP light polish</name>
  <reversibility rating="costly">D-07: forecast description is published agent-facing SIDE copy.</reversibility>
  <files>src/lib/mcp/tools/forecast.ts, src/lib/mcp/tools/forecast.test.ts, src/lib/mcp/tools/accounts.ts, src/lib/mcp/tools/net-worth.ts, src/lib/mcp/tools/balances.ts, src/lib/mcp/tools/fx.ts</files>
  <behavior>
    - GET_FORECAST_OVERLAY_DESCRIPTION matches both INISO-01 and GRISO-01 (combined overlay membership sentence)
    - forecast tests keep Прогноз / LOCF asserts and gain rule-id asserts
    - CAP tools retain readOnlyHint true and openWorldHint false; descriptions may gain light honesty clarity without SIDE rule ids
  </behavior>
  <read_first>
    - src/lib/mcp/tools/forecast.ts GET_FORECAST_OVERLAY_DESCRIPTION
    - src/lib/mcp/tools/forecast.test.ts description contracts
    - CAP registerTool description strings in accounts/net-worth/balances/fx
  </read_first>
  <action>
    Per D-09 / RESEARCH A2: Replace forecast description closer with INISO-01/GRISO-01 combined sentence (income + A′ grace overlay; do not fold into historical NW LOCF). Do not invent FORECAST-01. Update forecast.test.ts accordingly. On CAP tools (accounts, net-worth, balances, fx) apply light honesty polish only if wording is weak (accounts-only / transparency / do not multiply rates) — do not inject SIDE isolation rule identifiers into CAP descriptions (D-09). Preserve existing annotations.
    <!-- planner-discipline-allow: DISOL-01 -->
    <!-- planner-discipline-allow: INISO-01 -->
    <!-- planner-discipline-allow: GRISO-01 -->
  </action>
  <verify>
    <automated>npx vitest run src/lib/mcp/tools/forecast.test.ts src/lib/mcp/tools/fx.test.ts</automated>
    <fails_when>non-zero exit, "0 passed" in summary, or forecast description missing INISO-01 or GRISO-01</fails_when>
  </verify>
  <acceptance_criteria>
    - grep -q 'INISO-01' src/lib/mcp/tools/forecast.ts
    - grep -q 'GRISO-01' src/lib/mcp/tools/forecast.ts
    - CAP annotation blocks still present on accounts/net-worth/balances/fx
  </acceptance_criteria>
  <done>Forecast ships dual-rule SIDE sentence; CAP honesty polished without SIDE rule ids.</done>
</task>

<task type="auto">
  <name>Full mcp suite green + VALIDATION nyquist</name>
  <files>.planning/phases/26-connect-docs-policy/26-VALIDATION.md</files>
  <action>
    Run full MCP vitest suite. Update 26-VALIDATION.md: check remaining Wave 0 SIDE regex bullets; set nyquist_compliant true; mark CLI-01 unit rows green in Per-Task Verification Map; set status field appropriately. Confirm isolation-contract still requires named rules and ping annotations.
  </action>
  <verify>
    <automated>npx vitest run src/lib/mcp &amp;&amp; grep -q 'nyquist_compliant: true' .planning/phases/26-connect-docs-policy/26-VALIDATION.md</automated>
    <fails_when>vitest non-zero, "0 passed" in summary, or nyquist_compliant not true</fails_when>
  </verify>
  <acceptance_criteria>
    - npx vitest run src/lib/mcp exits 0
    - VALIDATION nyquist_compliant: true
  </acceptance_criteria>
  <done>CLI-01 code surface complete; Nyquist validation marked compliant for unit contracts.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Agent ← tool descriptions | Prose guidance only; agents may ignore |
| CAP vs SIDE description surfaces | Wrong rule names on CAP would mis-teach agents |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-26-04 | Tampering | CAP descriptions | medium | mitigate | D-09 — no SIDE rule ids on CAP; SIDE-only named prose |
| T-26-05 | Elevation of Privilege | forecast overlay copy | medium | mitigate | INISO-01/GRISO-01 combined sentence; existing GRISO/INISO walls unchanged |
| T-26-06 | Information Disclosure | tool payloads | low | mitigate | D-11 — keep no isolation meta on payloads |
| T-26-SC | Tampering | npm installs | high | accept | No new packages this plan |
</threat_model>

<verification>
npx vitest run src/lib/mcp
</verification>

<success_criteria>
All SIDE tools export named isolation sentences; CAP free of SIDE rule ids; full mcp suite green; VALIDATION nyquist_compliant.
</success_criteria>

<output>
Create `.planning/phases/26-connect-docs-policy/26-02-SUMMARY.md` when done
</output>
