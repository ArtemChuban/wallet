---
phase: 26-connect-docs-policy
plan: 03
type: execute
wave: 1
depends_on: []
files_modified:
  - README.md
autonomous: true
requirements:
  - CLI-02
estimate:
  tokens: 12000
  raw_tokens: 24000
  tasks: 2
  confidence: high
must_haves:
  truths:
    - README has English MCP section after Quick start with Claude and Cursor copy-paste configs (D-01…D-06)
    - Documented URL is http://127.0.0.1:3000/api/mcp (D-05)
    - Both snippets include type http and url (D-06)
    - README MCP section has no smoke/ping/tools-list steps and no PARITY policy (D-02, D-14)
  prohibitions:
    - statement: MUST NOT add connect smoke to OPERATOR.md
      category: scope
    - statement: MUST NOT create docs/mcp.md
      category: scope
    - statement: MUST NOT pre-document stdio remote-bridge fallback in README
      category: scope
    - statement: MUST NOT use streamable-http or url-only Cursor snippet as primary
      category: scope
  artifacts:
    - path: README.md
      provides: Claude + Cursor connect snippets after Quick start
  key_links:
    - from: README MCP section
      to: http://127.0.0.1:3000/api/mcp
      via: copy-paste configs
      pattern: "127.0.0.1:3000/api/mcp"
---

## Specless probe fallback

SPEC absent. ROADMAP CLI-02 + CONTEXT D-01…D-06, D-14, D-17…D-19 + RESEARCH Pattern 2.

## Phase Goal

Claude Code / Cursor CLI can connect with copy-paste configs; tools declare read-only + isolation; PARITY standing rule is project-visible.

<objective>
Ship CLI-02 README-only connect docs (D-01…D-06, D-14, D-17): English MCP section after Quick start with Claude type http and Cursor type http + url snippets for http://127.0.0.1:3000/api/mcp; copy-paste only — no smoke, no PARITY, no pre-documented remote-bridge fallback.

Purpose: Operators can paste configs once the app is already on loopback :3000.
Output: README MCP section between Quick start and Host data contract.
</objective>

<execution_context>
@/home/artem/Documents/wallet/.cursor/gsd-core/workflows/execute-plan.md
@/home/artem/Documents/wallet/.cursor/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/26-connect-docs-policy/26-CONTEXT.md
@.planning/phases/26-connect-docs-policy/26-RESEARCH.md
@.planning/phases/26-connect-docs-policy/26-PATTERNS.md
@README.md
</context>

<assumptions>
## Planner assumptions

- A1: Cursor accepting type http is UAT risk (Plan 04 / verify-work); do not weaken primary snippet preemptively (D-19).
- Dual-client live smoke lives in 26-UAT.md only (D-18) — not this plan.
</assumptions>

<tasks>

<task type="auto">
  <name>Insert English README MCP section after Quick start</name>
  <reversibility rating="reversible">D-01: README-only connect snippets; easy to edit or move.</reversibility>
  <files>README.md</files>
  <read_first>
    - README.md Quick start through Host data contract insert locus
    - 26-RESEARCH.md Pattern 2 Claude/Cursor snippet shapes
    - 26-PATTERNS.md README assignment
  </read_first>
  <action>
    Per D-01 / D-02 / D-03 / D-04 / D-05 / D-06 / D-14 / D-17: Insert a new English section (title discretionary, e.g. MCP Claude Code / Cursor) immediately after the Quick start block and before Host data contract. Include one-line prerequisite that wallet must already be running on 127.0.0.1:3000 (Quick start above). Provide Claude snippet(s): claude mcp add --transport http with URL http://127.0.0.1:3000/api/mcp and/or JSON mcpServers entry with type http + url. Provide Cursor JSON for .cursor/mcp.json (or user mcp.json) with type http + url same URL. Do not use streamable-http. Do not ship url-only as the primary Cursor snippet. Copy-paste configs only — no live smoke steps, no wallet_ping walkthrough, no tools/list procedure. Do not mention PARITY. Do not pre-document the stdio remote-bridge fallback package (document only after real UAT fail per D-17/D-19). Do not edit OPERATOR.md or create docs/mcp.md.
  </action>
  <verify>
    <automated>python3 - &lt;&lt;'PY'
from pathlib import Path
text = Path("README.md").read_text()
qs = text.find("## Quick start")
mcp = text.find("## MCP")
host = text.find("## Host data contract")
assert qs != -1 and mcp != -1 and host != -1 and qs &lt; mcp &lt; host, f"order qs={qs} mcp={mcp} host={host}"
assert "127.0.0.1:3000/api/mcp" in text
assert '"type": "http"' in text or '"type":"http"' in text
assert "streamable-http" not in text[mcp:host]
# Claude transport + Cursor-ish url both present in MCP slice
slice_ = text[mcp:host]
assert "http" in slice_ and "url" in slice_.lower()
print("README MCP section OK")
PY</automated>
    <fails_when>python assert fails, section order wrong, URL missing, or streamable-http appears in MCP section</fails_when>
  </verify>
  <acceptance_criteria>
    - MCP heading between Quick start and Host data contract
    - URL http://127.0.0.1:3000/api/mcp present
    - type http present for both client configs
  </acceptance_criteria>
  <done>README ships English Claude + Cursor copy-paste configs after Quick start.</done>
</task>

<task type="auto">
  <name>Guard README MCP section against smoke and policy bleed</name>
  <files>README.md</files>
  <action>
    Re-read the new MCP section. Ensure it stays connect-only (D-02, D-14, D-18): no step-by-step smoke, no ping invocation recipe, no tools/list recipe, no PARITY wording, no OPERATOR cross-link that turns README into a UAT script. If any slipped in, delete them. Keep prerequisite one-liner only.
    <!-- planner-discipline-allow: PARITY -->
  </action>
  <verify>
    <automated>python3 - &lt;&lt;'PY'
from pathlib import Path
text = Path("README.md").read_text()
mcp = text.find("## MCP")
host = text.find("## Host data contract")
assert mcp != -1 and host != -1 and mcp &lt; host
slice_ = text[mcp:host].lower()
banned = ["tools/list", "wallet_ping", "parity-01", "parity 01", "smoke test", "agent mcp list"]
hits = [b for b in banned if b in slice_]
assert not hits, f"banned tokens in MCP section: {hits}"
print("README MCP hygiene OK")
PY</automated>
    <fails_when>python assert fails or banned smoke/policy tokens found in MCP section</fails_when>
  </verify>
  <acceptance_criteria>
    - MCP section free of tools/list, wallet_ping, PARITY-01, smoke-test recipes
  </acceptance_criteria>
  <done>README MCP section is connect-snippet-only.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Operator ← README | Docs may steer bind address / transport |
| Client config → localhost MCP | Mis-documented URL widens exposure |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-26-07 | Information Disclosure | README URL | high | mitigate | D-05 — document 127.0.0.1 only; never 0.0.0.0 |
| T-26-08 | Tampering | Cursor/Claude transport docs | medium | mitigate | D-06 type http + url; forbid streamable-http primary; D-17 no pre-doc remote bridge |
| T-26-09 | Elevation of Privilege | README scope creep | low | mitigate | D-02/D-14 — connect only; no PARITY/smoke |
| T-26-SC | Tampering | npm installs | high | accept | No packages — docs only |
</threat_model>

<verification>
python3 checks for section order, URL, type http, and banned smoke/policy tokens in MCP slice
</verification>

<success_criteria>
README MCP section after Quick start with dual-client type http + url configs for loopback MCP URL; connect-only.
</success_criteria>

<output>
Create `.planning/phases/26-connect-docs-policy/26-03-SUMMARY.md` when done
</output>
