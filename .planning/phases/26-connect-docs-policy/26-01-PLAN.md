---
phase: 26-connect-docs-policy
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/lib/mcp/isolation-contract.test.ts
  - src/lib/mcp/tools/wallet-ping.ts
  - src/lib/mcp/create-handler.ts
  - src/lib/mcp/tools/debts.ts
  - src/lib/mcp/tools/debts.test.ts
  - .planning/phases/26-connect-docs-policy/26-VALIDATION.md
autonomous: true
requirements:
  - CLI-01
estimate:
  tokens: 22000
  raw_tokens: 44000
  tasks: 2
  confidence: high
must_haves:
  truths:
    - wallet_ping declares readOnlyHint true and openWorldHint false (D-10)
    - create-handler instructions contain DISOL-01, INISO-01, and GRISO-01 short sentences (D-07, D-08)
    - list_debts description exports DISOL-01 do-not-fold prose (D-09)
    - isolation-contract requires named rules and ping annotations (no longer asserts absence)
  prohibitions:
    - statement: MUST NOT add isolation fields on tool JSON payloads
      category: isolation
    - statement: MUST NOT register new MCP tools or change prisma schema
      category: scope
    - statement: MUST NOT install new npm packages
      category: scope
    - statement: MUST NOT put connect smoke steps in README or OPERATOR
      category: scope
  artifacts:
    - path: src/lib/mcp/tools/wallet-ping.ts
      provides: CLI-01 annotations on wallet_ping
    - path: src/lib/mcp/create-handler.ts
      provides: named DISOL/INISO/GRISO in server instructions
    - path: src/lib/mcp/tools/debts.ts
      provides: DISOL-01 in LIST_DEBTS_DESCRIPTION
    - path: src/lib/mcp/isolation-contract.test.ts
      provides: flipped named-rule + ping annotation contracts
  key_links:
    - from: isolation-contract.test.ts
      to: create-handler.ts instructions
      via: toMatch DISOL-01 INISO-01 GRISO-01
      pattern: "DISOL-01"
    - from: registerWalletPing
      to: ToolAnnotations
      via: readOnlyHint + openWorldHint false
      pattern: "readOnlyHint"
---

## Specless probe fallback

SPEC absent. Truths from ROADMAP Phase 26 + CONTEXT D-07…D-11 + RESEARCH Pattern 1 / Wave 0.

## Phase Goal

Claude Code / Cursor CLI can connect with copy-paste configs; tools declare read-only + isolation; PARITY standing rule is project-visible.

<objective>
Tracer for CLI-01 (D-07, D-08, D-09, D-10, D-11): flip isolation-contract to require named DISOL/INISO/GRISO + wallet_ping annotation scan; annotate wallet_ping; rewrite create-handler instructions with short named SIDE sentences; upgrade list_debts DESCRIPTION with DISOL-01; green debts description contract.

Purpose: Prove end-to-end named-isolation path before expanding other SIDE tools / docs.
Output: isolation-contract green with presence asserts; annotated wallet_ping; named instructions; DISOL-01 on list_debts; VALIDATION Wave 0 rows updated.
</objective>

<execution_context>
@/home/artem/Documents/wallet/.cursor/gsd-core/workflows/execute-plan.md
@/home/artem/Documents/wallet/.cursor/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/26-connect-docs-policy/26-CONTEXT.md
@.planning/phases/26-connect-docs-policy/26-RESEARCH.md
@.planning/phases/26-connect-docs-policy/26-PATTERNS.md
@.planning/phases/26-connect-docs-policy/26-VALIDATION.md
@src/lib/mcp/isolation-contract.test.ts
@src/lib/mcp/create-handler.ts
@src/lib/mcp/tools/wallet-ping.ts
@src/lib/mcp/tools/debts.ts
@src/lib/mcp/tools/debts.test.ts
</context>

<assumptions>
## Planner assumptions

- A2 (RESEARCH): forecast overlay cites INISO-01/GRISO-01 combined — Plan 02; tracer only needs those ids present in create-handler instructions.
- CAP light polish deferred to Plan 02 (D-09).
- No new npm packages; annotations are hints only (not authz).
</assumptions>

## Artifacts this plan produces

- Flipped `isolation-contract.test.ts`
- Annotated `wallet-ping.ts`
- Named-rule `create-handler.ts` instructions
- `LIST_DEBTS_DESCRIPTION` with DISOL-01
- Updated `debts.test.ts` description regexes
- `26-VALIDATION.md` Wave 0 progress

<tasks>

<task type="tracer" tdd="true">
  <name>End-to-end CLI-01 named isolation — debts + ping + instructions</name>
  <reversibility rating="costly">D-07: named DISOL/INISO/GRISO copy is published agent-facing instruction/description surface; undo needs coordinated prose + test flip across handler and SIDE tools.</reversibility>
  <files>src/lib/mcp/isolation-contract.test.ts, src/lib/mcp/tools/wallet-ping.ts, src/lib/mcp/create-handler.ts, src/lib/mcp/tools/debts.ts, src/lib/mcp/tools/debts.test.ts</files>
  <behavior>
    - isolation-contract: create-handler source matches DISOL-01, INISO-01, and GRISO-01 individually (replace Phase 25 absence assert)
    - isolation-contract: wallet-ping.ts source matches readOnlyHint true and openWorldHint false
    - LIST_DEBTS_DESCRIPTION matches DISOL-01 and do-not-fold / historical NW language
    - Existing BalanceSnapshot / actions import walls and tool catalog asserts stay green
    - No payload isolation meta fields (keep existing D-11 style asserts in debts tests)
  </behavior>
  <read_first>
    - src/lib/mcp/isolation-contract.test.ts (lines with Phase 25 absence assert on named rules)
    - src/lib/mcp/tools/wallet-ping.ts (registerTool without annotations)
    - src/lib/mcp/tools/debts.ts (annotations block + LIST_DEBTS_DESCRIPTION closer)
    - src/lib/mcp/create-handler.ts (instructions SIDE one-liners)
    - .planning/phases/26-connect-docs-policy/26-RESEARCH.md Pattern 1 templates
  </read_first>
  <action>
    Per D-10 / D-07 / D-08 / D-09 / D-11: First rewrite isolation-contract so create-handler must match DISOL-01, INISO-01, and GRISO-01 (three positive toMatch; remove the Phase 25 combined absence assert) and add a readFileSync scan of wallet-ping.ts requiring readOnlyHint true and openWorldHint false. Run vitest — expect RED. Then add annotations on registerWalletPing matching debts.ts shape; rewrite create-handler instructions SIDE closers to short named sentences (forecast may use INISO-01/GRISO-01 combined; debts/income/grace each name their rule + do not fold into historical NW / Капитал LOCF); replace list_debts description closer with DISOL-01 sentence from RESEARCH template; keep EN+RU body and existing annotations; do not add payload meta fields. Update debts.test.ts description contracts to require DISOL-01 (and fold language). Keep catalog/register asserts and never-write walls. Do not touch income/grace/forecast description exports yet (Plan 02).
  </action>
  <verify>
    <automated>npx vitest run src/lib/mcp/isolation-contract.test.ts src/lib/mcp/tools/debts.test.ts</automated>
    <fails_when>non-zero exit, "0 passed" in summary, or remaining absence assert on named rules in isolation-contract</fails_when>
  </verify>
  <acceptance_criteria>
    - grep -q 'readOnlyHint' src/lib/mcp/tools/wallet-ping.ts
    - grep -q 'DISOL-01' src/lib/mcp/create-handler.ts
    - grep -q 'INISO-01' src/lib/mcp/create-handler.ts
    - grep -q 'GRISO-01' src/lib/mcp/create-handler.ts
    - grep -q 'DISOL-01' src/lib/mcp/tools/debts.ts
    - isolation-contract + debts.test vitest exit 0
  </acceptance_criteria>
  <done>Named isolation present in instructions; wallet_ping annotated; list_debts ships DISOL-01; isolation-contract requires presence not absence.</done>
</task>

<task type="auto">
  <name>Mark VALIDATION Wave 0 rows for CLI-01 flip</name>
  <files>.planning/phases/26-connect-docs-policy/26-VALIDATION.md</files>
  <action>
    Update 26-VALIDATION.md: check off Wave 0 Requirements bullets completed by this tracer (isolation-contract flip, wallet_ping annotation scan, debts DISOL description contracts); keep wave_0_complete: false until Plan 02 finishes SIDE income/grace/forecast regex bullets; leave nyquist_compliant false until Plan 02; refresh Per-Task Verification Map status for 26-01 rows to green where applicable. Optionally annotate Wave 0 checklist which bullets are tracer-done vs Plan-02-pending.
  </action>
  <verify>
    <automated>grep -q 'wave_0_complete: false' .planning/phases/26-connect-docs-policy/26-VALIDATION.md &amp;&amp; npx vitest run src/lib/mcp/isolation-contract.test.ts</automated>
    <fails_when>wave_0_complete not false, or vitest non-zero</fails_when>
  </verify>
  <acceptance_criteria>
    - Frontmatter wave_0_complete: false (full Wave 0 closes in Plan 02)
    - Wave 0 isolation-contract / wallet_ping / debts tracer bullets marked done
  </acceptance_criteria>
  <done>VALIDATION reflects tracer CLI-01 progress; wave_0_complete stays false until Plan 02.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| MCP client → tool descriptions/instructions | Untrusted agents read prose; may ignore hints |
| Docs/tests → repo | Static contracts only; no new network surface |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-26-01 | Elevation of Privilege | SIDE tools / instructions | medium | mitigate | Named DISOL/INISO/GRISO in instructions + list_debts; existing disol walls unchanged |
| T-26-02 | Spoofing | wallet_ping annotations | low | accept | Annotations are hints only (spec); Host/Origin + no write tools remain real controls |
| T-26-03 | Tampering | Tool JSON payloads | medium | mitigate | D-11 — no isolation meta fields; keep debts payload asserts |
| T-26-SC | Tampering | npm installs | high | accept | No new packages this plan — legitimacy gate N/A |
</threat_model>

<verification>
npx vitest run src/lib/mcp/isolation-contract.test.ts src/lib/mcp/tools/debts.test.ts
</verification>

<success_criteria>
isolation-contract requires DISOL/INISO/GRISO + ping annotations; list_debts ships DISOL-01; VALIDATION Wave 0 tracer scope complete.
</success_criteria>

<output>
Create `.planning/phases/26-connect-docs-policy/26-01-SUMMARY.md` when done
</output>
