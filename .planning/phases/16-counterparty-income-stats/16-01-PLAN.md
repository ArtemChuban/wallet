---
phase: 16-counterparty-income-stats
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/lib/income.ts
  - src/lib/income.test.ts
  - src/app/income/page.tsx
  - src/components/income/IncomeList.tsx
  - src/components/income/income-ui.test.ts
autonomous: true
requirements:
  - CPTY-01
estimate:
  tokens: 35000
  raw_tokens: 70000
  tasks: 3
  confidence: high
must_haves:
  truths:
    - "Person with ≥1 actual shows all-time native Σ in Person group header with «за всё время» (CPTY-01, D-01, D-03, D-10, D-13, D-15)"
    - "Stats Σ uses every Prisma actual row for that Person (recurring+one-time merged), not next-open IncomeRow slot only (D-01, D-02, D-13, D-14)"
    - "Primary conversion uses locfRateAsOf per fact actualAsOf — never today-only firstHitLocfMap (D-05, D-14)"
    - "Missing FX excludes fact from primary only, sets isPartial; native unchanged; never invent 0 rate (D-08)"
    - "Plan-only Person (0 actuals) keeps Phase 14 header with no stats digits (D-03)"
    - "No page-level «всего получено» hero; totals per-Person only (D-04, D-11)"
  prohibitions:
    - statement: MUST NOT sum unfilled plan slots into stats
      requirement_id: CPTY-01
      category: integrity
    - statement: MUST NOT use today-first FX map for income stats conversion
      requirement_id: CPTY-01
      category: honesty
    - statement: MUST NOT coerce null LOCF rate to 0n primary
      requirement_id: CPTY-01
      category: honesty
    - statement: MUST NOT import debt aggregates or net-worth / historical-series / Prisma into income.ts
      requirement_id: CPTY-01
      category: isolation
    - statement: MUST NOT install new npm packages
      requirement_id: CPTY-01
      category: safety
  artifacts:
    - path: src/lib/income.ts
      provides: computePersonIncomeStats + IncomeActualFactInput / PersonIncomeStats types
    - path: src/lib/income.test.ts
      provides: Wave 0 + green computePersonIncomeStats suite
    - path: src/app/income/page.tsx
      provides: flatten all actuals + FxRate load + attach serializable stats props
    - path: src/components/income/IncomeList.tsx
      provides: PersonGroup header native totals + window hint (tracer chrome)
    - path: src/components/income/income-ui.test.ts
      provides: Wave 0 counterparty stats file-scan describe
  key_links:
    - from: page.tsx flatten of r.actuals/o.actuals
      to: computePersonIncomeStats
      via: IncomeActualFactInput[] + RateRow[] + primaryScale
      pattern: "computePersonIncomeStats"
    - from: computePersonIncomeStats
      to: locfRateAsOf(rates, code, fact.actualAsOf)
      via: per-fact LOCF then convertOtherMinorToPrimaryMinor
      pattern: "locfRateAsOf"
    - from: PersonIncomeListItem.stats
      to: PersonGroup header
      via: optional serializable display strings when ≥1 actual
      pattern: "за всё время"
---

<objective>
Wave 0 Nyquist scaffolds + tracer end-to-end counterparty income stats (all-actuals → pure dual aggregate → Person header) + domain edge cases for multi-ccy / partial / merged kinds (CPTY-01, D-01..D-08, D-10, D-13..D-15).

Purpose: Prove receipt-date LOCF hybrid math and all-time membership before UI polish expands chrome.
Output: Green computePersonIncomeStats suite; /income page attaches stats; Person header shows native «за всё время» for Persons with actuals.
</objective>

<execution_context>
@/home/artem/Documents/wallet/.cursor/gsd-core/workflows/execute-plan.md
@/home/artem/Documents/wallet/.cursor/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/16-counterparty-income-stats/16-CONTEXT.md
@.planning/phases/16-counterparty-income-stats/16-RESEARCH.md
@.planning/phases/16-counterparty-income-stats/16-PATTERNS.md
@.planning/phases/16-counterparty-income-stats/16-UI-SPEC.md
@.planning/phases/16-counterparty-income-stats/16-VALIDATION.md
@src/lib/debts.ts
@src/lib/locf.ts
@src/lib/money.ts
@src/lib/income.ts
@src/app/income/page.tsx
@src/components/income/IncomeList.tsx
@src/lib/debts.test.ts
</context>

## User story

**As a** user tracking pay from people on «Доходы»,
**I want to** see how much I have actually received from each Person (native + primary honesty),
**so that** I trust counterparty totals without confusing plan slots or invented FX.

## Artifacts this phase produces

- Wave 0 failing→green tests (this plan)
- computePersonIncomeStats + page wiring + tracer header (this plan)
- Full UI-SPEC hybrid chrome + REQUIREMENTS CPTY-01 sync (Plan 02)

<tasks>

<task type="auto" tdd="true">
  <name>Wave 0 — RED scaffolds for computePersonIncomeStats + UI file-scan</name>
  <files>src/lib/income.test.ts, src/components/income/income-ui.test.ts</files>
  <read_first>
    .planning/phases/16-counterparty-income-stats/16-VALIDATION.md
    .planning/phases/16-counterparty-income-stats/16-RESEARCH.md
    src/lib/debts.test.ts
    src/lib/income.test.ts
    src/components/income/income-ui.test.ts
  </read_first>
  <behavior>
    - describe computePersonIncomeStats: native multi-ccy buckets; primary convert via rate @ actualAsOf; missing FX → isPartial + native intact; empty facts → empty/no stats; recurring+one-time merge one Σ; ≥2 actuals on one parent counted (all-time) (CPTY-01, D-01, D-02, D-05, D-07, D-08, D-13)
    - describe counterparty stats (income-ui): file-scan expects «за всё время», partial honesty copy «Итог неполный» or «нет курса», page wires computePersonIncomeStats and fxRate load; forbids mounting debts page totals hero on income page; keeps DestructiveConfirmStep / no browser native confirm API (D-04, D-10, D-11, D-15)
    - Isolation describe still bans net-worth / historical-series / Prisma in income.ts; locf + money imports allowed (ISO light)
  </behavior>
  <action>
    Per 16-VALIDATION Wave 0 and RESEARCH Validation Architecture: add RED vitest cases for computePersonIncomeStats in src/lib/income.test.ts (factory mirroring debts.test debtInput pattern — PATTERNS). Add describe "counterparty stats" file-scan in income-ui.test.ts reading IncomeList.tsx and page.tsx. Do not implement production helper/UI yet — tests must fail until Task 2. Keep existing isolation suite; do not ban @/lib/locf. Zero new packages (D-01..D-08, D-13 coverage targets).
  </action>
  <verify>
    <automated>npx vitest run -t "computePersonIncomeStats" src/lib/income.test.ts; test $? -ne 0</automated>
    <fails_when>vitest exits 0 (Wave 0 must be RED) or describe/tests missing</fails_when>
  </verify>
  <acceptance_criteria>
    - Wave 0 domain + UI describes exist and fail before implementation (CPTY-01)
    - Isolation describe still present and not weakened
  </acceptance_criteria>
  <done>RED scaffolds committed; ready for tracer GREEN.</done>
</task>

<task type="tracer" tdd="true">
  <name>End-to-end Person stats — all actuals → computePersonIncomeStats → header</name>
  <files>src/lib/income.ts, src/lib/income.test.ts, src/app/income/page.tsx, src/components/income/IncomeList.tsx, src/components/income/income-ui.test.ts</files>
  <read_first>
    .planning/phases/16-counterparty-income-stats/16-CONTEXT.md
    .planning/phases/16-counterparty-income-stats/16-PATTERNS.md
    .planning/phases/16-counterparty-income-stats/16-UI-SPEC.md
    src/lib/debts.ts
    src/lib/locf.ts
    src/lib/money.ts
    src/lib/income.ts
    src/app/income/page.tsx
    src/app/debts/page.tsx
    src/components/income/IncomeList.tsx
  </read_first>
  <behavior>
    - computePersonIncomeStats(facts, rates, primaryScale) returns per-person nativeByCurrency + primaryTotalMinor + isPartial + excludedFactCount (D-05, D-07, D-08)
    - Each fact always adds to native bucket; primary uses identity for primary currency else locfRateAsOf(..., actualAsOf) + convertOtherMinorToPrimaryMinor; null rate → exclude primary only + isPartial (D-01, D-05, D-08, D-14)
    - page flattens ALL recurring/oneTime actuals (not slotActual), loads FxRate where asOfDate lte maxActualAsOf, attaches stats only when Person has ≥1 actual (D-02, D-03, D-13)
    - PersonGroup shows right-aligned «за всё время» + native Body/semibold mono amounts when stats present; identity-omit hides redundant primary when single primary currency and !isPartial (D-06 amendment, D-10, D-12, D-15)
    - List IncomeRow mapping stays next-open-only (Phase 15 unchanged)
  </behavior>
  <action>
    Implement IncomeActualFactInput / PersonIncomeStats types and computePersonIncomeStats in src/lib/income.ts per RESEARCH Pattern 1 — mirror debts exclude/isPartial semantics; call locfRateAsOf per fact actualAsOf (historical-series shape); never import @/lib/debts aggregates, Prisma, net-worth, or historical-series (D-05, D-08, ISO). Wire page.tsx: extend currency select with isPrimary; compute primaryScale from primary currency; flatten every actual from recurringIncomes/oneTimeIncomes into fact inputs; FxRate.findMany ordered desc with asOfDate lte max over fact dates (or skip query when zero facts — RESEARCH A2); call computePersonIncomeStats; formatMinorToMajor for serializable PersonIncomeListItem.stats; omit stats prop when zero actuals (D-03, D-04). Extend PersonGroup header with optional stats block (name | stats text-right | CTAs) showing window hint and native lines — tracer may identity-omit primary (UI-SPEC). Do not mount page hero. Do not model withdrawal/cash-out date (D-09 out of scope). Green Wave 0 happy-path + all-time multi-actual cases; partial/multi-ccy may still expand in Task 3.
  </action>
  <verify>
    <automated>npx vitest run src/lib/income.test.ts src/components/income/income-ui.test.ts &amp;&amp; grep -q 'computePersonIncomeStats' src/lib/income.ts &amp;&amp; grep -q 'computePersonIncomeStats' src/app/income/page.tsx &amp;&amp; grep -q 'locfRateAsOf' src/lib/income.ts &amp;&amp; grep -q 'за всё время' src/components/income/IncomeList.tsx</automated>
    <fails_when>vitest non-zero, helper/page wiring missing, or window hint absent from IncomeList</fails_when>
  </verify>
  <acceptance_criteria>
    - Happy path: Person with actuals shows all-time native Σ in header (CPTY-01, D-01, D-10, D-13)
    - FX path uses locfRateAsOf on actualAsOf inside income.ts (D-05)
    - Plan-only groups have no stats digits (D-03)
  </acceptance_criteria>
  <done>Tracer path green end-to-end through lib + page + header.</done>
</task>

<task type="auto" tdd="true">
  <name>Domain expansion — multi-ccy, no_fx partial, merged kinds</name>
  <files>src/lib/income.ts, src/lib/income.test.ts</files>
  <read_first>
    src/lib/income.ts
    src/lib/income.test.ts
    src/lib/debts.ts
    .planning/phases/16-counterparty-income-stats/16-RESEARCH.md
  </read_first>
  <behavior>
    - Multiple currencies → one native line per currencyCode (stable sort asc) + one rolled primary of convertible facts (D-07)
    - Missing LOCF @ actualAsOf → excludedFactCount++, isPartial true, native still includes excluded fact amount (D-08)
    - Recurring and one-time facts for same personId share one PersonIncomeStats (D-02)
    - Empty fact list → empty map / no person entry (D-03)
    - Never invent rates; never silent zero primary for excluded facts (D-08)
  </behavior>
  <action>
    Finish computePersonIncomeStats edge branches so all Wave 0 domain cases pass (D-02, D-07, D-08). Ensure nativeByCurrency sorted by currencyCode asc. Keep primaryTotalMinor as sum of convertible facts only. Confirm isolation file-scan still green with locf/money imports only. No UI chrome beyond what tracer already shipped — Plan 02 owns partial line / multi-ccy display polish (D-11, D-12).
  </action>
  <verify>
    <automated>npx vitest run -t "computePersonIncomeStats" src/lib/income.test.ts &amp;&amp; npx vitest run -t "income isolation" src/lib/income.test.ts &amp;&amp; grep -q 'isPartial' src/lib/income.ts &amp;&amp; grep -q 'excludedFactCount' src/lib/income.ts</automated>
    <fails_when>vitest non-zero or isPartial/excludedFactCount missing from helper</fails_when>
  </verify>
  <acceptance_criteria>
    - All computePersonIncomeStats Wave 0 cases green (CPTY-01)
    - Isolation suite green (ISO light)
  </acceptance_criteria>
  <done>Domain aggregate complete for Plan 02 UI polish.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Prisma actuals / FxRate → RSC page | Trusted DB rows; must not invent rates when LOCF null |
| RSC → client IncomeList props | Display strings only; no client BigInt money math for Σ |
| income.ts pure lib | Must not import Prisma / net-worth / debt aggregates |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-16-01 | Tampering / Info disclosure | computePersonIncomeStats | high | mitigate | null LOCF → exclude + isPartial; never coerce 0n rate; vitest no_fx case |
| T-16-02 | Elevation / Info disclosure | income stats facts | high | mitigate | Facts only from income actual tables; no Debt join; no computeDebtPrimaryTotals import |
| T-16-03 | Tampering | page → client props | medium | mitigate | formatMinorToMajor on RSC; pass display strings |
| T-16-04 | Tampering | income.ts coupling | high | mitigate | Keep ISO isolation file-scan; allow locf+money only |
| T-16-05 | XSS | Person name in header | low | accept | Existing React text escaping; no dangerouslySetInnerHTML |
| T-16-SC | Tampering | npm installs | high | mitigate | Zero new packages this phase |
</threat_model>

<verification>
npx vitest run src/lib/income.test.ts src/components/income/income-ui.test.ts
</verification>

<success_criteria>
computePersonIncomeStats covers native/primary/partial/merge; page flattens all actuals with per-actualAsOf LOCF; Person header shows «за всё время» native totals for Persons with actuals; isolation green.
</success_criteria>

<output>
Create `.planning/phases/16-counterparty-income-stats/16-01-SUMMARY.md` when done
</output>
