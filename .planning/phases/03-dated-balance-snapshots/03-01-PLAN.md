---
phase: 03-dated-balance-snapshots
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - prisma/schema.prisma
  - prisma/migrations/
  - src/lib/balances.ts
  - src/lib/balances.test.ts
  - src/lib/validations/balance.ts
  - src/lib/validations/balance.test.ts
  - src/lib/foundation.test.ts
  - src/lib/money.test.ts
autonomous: true
requirements:
  - BAL-01
  - BAL-02
estimate:
  tokens: 21000
  raw_tokens: 42000
  tasks: 3
  confidence: high
must_haves:
  truths:
    - Balance as of date D is the latest BalanceSnapshot with asOfDate less than or equal to D (BAL-02, D-09 adjacency: equal asOfDate is included via lte)
    - Before an account's first snapshot, getBalanceAsOf returns null and never invents 0n (BAL-02 empty, roadmap success criterion 4)
    - Same (accountId, asOfDate) upsert overwrites amountMinor (BAL-01 adjacency collision, D-09)
    - setBalanceSchema accepts YYYY-MM-DD past/today shape; future-date rejection is enforced with calendarDateToday in the Server Action path covered by Plan 02 (BAL-01, D-12)
    - Non-credit amountMinor must be greater than or equal to 0 (planner assumption A2)
    - calendarDateToday defaults to Europe/Moscow for D-12 today gate (RESEARCH A3)
    - After migrate deploy, SQLite has BalanceSnapshot with unique accountId_asOfDate and BalanceAmountStub is gone
  prohibitions:
    - statement: MUST NOT invent a zero balance when no snapshot exists for asOfDate D
      requirement_id: BAL-02
      category: values
    - statement: MUST NOT keep BalanceAmountStub alongside BalanceSnapshot after this plan's migration
      requirement_id: BAL-01
      category: safety
    - statement: MUST NOT store outstanding debt as a second money column on BalanceSnapshot
      requirement_id: BAL-01
      category: values
  artifacts:
    - path: prisma/schema.prisma
      provides: BalanceSnapshot model + Account.balanceSnapshots relation; stub removed
    - path: prisma/migrations
      provides: DROP BalanceAmountStub / CREATE BalanceSnapshot + unique index migration
    - path: src/lib/balances.ts
      provides: getBalanceAsOf, creditDebtMinor, calendarDateToday
    - path: src/lib/validations/balance.ts
      provides: setBalanceSchema and deleteBalanceSchema Zod contracts
  key_links:
    - from: src/lib/balances.ts
      to: prisma.balanceSnapshot
      via: findFirst where asOfDate lte D orderBy desc
      pattern: "getBalanceAsOf"
    - from: prisma/schema.prisma
      to: "@@unique accountId_asOfDate"
      via: named unique for compound upsert
      pattern: "accountId_asOfDate"
---

## Phase Goal

**As a** local Wallet user, **I want to** record account balances as of a chosen date and read the correct as-of balance over time, **so that** capital history stays trustworthy before FX and net worth (BAL-01, BAL-02).

<objective>
Land the BalanceSnapshot data contract and LOCF helpers: Wave 0 red tests, replace BalanceAmountStub, Zod balance schemas, then block on prisma migrate deploy.

Purpose: Prove BAL-02 LOCF and BAL-01 overwrite identity before UI/actions (Plan 02).
Output: Migrated BalanceSnapshot, src/lib/balances.ts, src/lib/validations/balance.ts, green Wave 0 tests.
</objective>

<execution_context>
@/home/artem/Documents/wallet/.cursor/gsd-core/workflows/execute-plan.md
@/home/artem/Documents/wallet/.cursor/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/03-dated-balance-snapshots/03-CONTEXT.md
@.planning/phases/03-dated-balance-snapshots/03-RESEARCH.md
@.planning/phases/03-dated-balance-snapshots/03-PATTERNS.md
@.planning/phases/03-dated-balance-snapshots/03-VALIDATION.md
@.planning/phases/03-dated-balance-snapshots/03-UI-SPEC.md
@prisma/schema.prisma
@src/lib/money.ts
@src/lib/foundation.test.ts
@src/lib/db.ts
</context>

<assumptions>
## Planner assumptions (specless / discretion)

- A2: Non-credit amountMinor must be greater than or equal to 0 (no negative debit/cash/crypto in v1).
- A3: calendarDateToday default time zone is Europe/Moscow for D-12 / dialog default.
- A1: Prisma model name is BalanceSnapshot (RESEARCH discretion lock).
- Edge BAL-02 unclassified probe: resolved as LOCF latest asOfDate less than or equal to D; null before first — same as BAL-02 truths above (not a silent drop).
</assumptions>

<assumption_delta_decision>
noun: asOfDate (user-selected calendar date on BalanceSnapshot)
decision: no-change
rationale: Assumption-delta fired on ROADMAP "chosen date" wording; asOfDate stays a field on BalanceSnapshot, not a new primary identity or selectable source-of-truth model.
</assumption_delta_decision>

<context_lock_waiver>
D-05 (credit available storage) and D-09 (unique overwrite) already locked in 03-CONTEXT.md. Keep reversibility ratings on implementing tasks in later plans; skip checkpoint:decision — do not re-ask (planner-reversibility: gating a decision already made).
</context_lock_waiver>

<tasks>

<task type="auto">
  <name>Wave 0 balance Zod + LOCF tests (red-first)</name>
  <files>src/lib/validations/balance.test.ts, src/lib/balances.test.ts</files>
  <read_first>
    .planning/phases/03-dated-balance-snapshots/03-VALIDATION.md
    .planning/phases/03-dated-balance-snapshots/03-RESEARCH.md
    .planning/phases/03-dated-balance-snapshots/03-PATTERNS.md
    src/lib/validations/account.test.ts
    src/lib/money.test.ts
    src/app/accounts/actions.test.ts
  </read_first>
  <behavior>
    - setBalanceSchema accepts accountId, amountMajor, asOfDate YYYY-MM-DD; rejects empty amount and malformed date with Russian messages from UI-SPEC
    - getBalanceAsOf between two snapshots returns earlier amount; on exact snapshot date returns that amount; before first returns null not 0n (BAL-02)
    - creditDebtMinor(limit, available) equals limit minus available
    - upsert same (accountId, asOfDate) overwrites amountMinor (BAL-01 adjacency)
  </behavior>
  <action>
    Per BAL-01/BAL-02 and 03-VALIDATION Wave 0: author src/lib/validations/balance.test.ts covering setBalanceSchema shape (past/today date strings, required amountMajor, Russian invalid-date/amount messages from 03-UI-SPEC Copywriting). Author src/lib/balances.test.ts covering LOCF table cases from RESEARCH (asOf between → earlier; asOf equal → that row; before first → null; overwrite same date). Prefer pure helper tests; mock prisma via vi.mock("@/lib/db") using the pattern in src/app/accounts/actions.test.ts when exercising getBalanceAsOf. Do not implement production modules here — leave tests red until Task 2.
  </action>
  <verify>
    <automated>test -f src/lib/validations/balance.test.ts && test -f src/lib/balances.test.ts && grep -q 'getBalanceAsOf\|asOfDate\|null' src/lib/balances.test.ts && grep -q 'setBalanceSchema\|asOfDate\|amountMajor' src/lib/validations/balance.test.ts</automated>
    <fails_when>either test file missing, or LOCF/null assertions absent from balances.test.ts, or setBalanceSchema/asOfDate coverage absent from balance.test.ts</fails_when>
  </verify>
  <acceptance_criteria>
    - src/lib/validations/balance.test.ts exists with date/amount Zod cases
    - src/lib/balances.test.ts exists with LOCF, null-before-first, and overwrite cases
  </acceptance_criteria>
  <done>Wave 0 failing/pending test files authored before schema/helper implementation</done>
</task>

<task type="tracer">
  <name>End-to-end BalanceSnapshot schema + LOCF helpers + Zod</name>
  <reversibility rating="costly">D-09 unique (accountId, asOfDate) is the LOCF identity contract; relaxing uniqueness later breaks as-of reads across Phases 4–6.</reversibility>
  <files>prisma/schema.prisma, prisma/migrations/, src/lib/balances.ts, src/lib/validations/balance.ts, src/lib/money.test.ts</files>
  <read_first>
    .planning/phases/03-dated-balance-snapshots/03-CONTEXT.md
    .planning/phases/03-dated-balance-snapshots/03-RESEARCH.md
    .planning/phases/03-dated-balance-snapshots/03-PATTERNS.md
    prisma/schema.prisma
    src/lib/money.ts
    src/lib/validations/account.ts
    src/lib/validations/balance.test.ts
    src/lib/balances.test.ts
  </read_first>
  <action>
    Wire ONE data path for BAL-02 LOCF and BAL-01 snapshot identity (per D-09). In prisma/schema.prisma remove BalanceAmountStub; add BalanceSnapshot with id, accountId FK to Account onDelete Restrict, asOfDate String YYYY-MM-DD (same comment convention as FxRateStub), amountMinor BigInt, and @@unique([accountId, asOfDate], name: "accountId_asOfDate"). Add balanceSnapshots BalanceSnapshot[] on Account. Create a new Prisma migration non-interactively (--create-only or migrate diff equivalent) that DROP TABLE BalanceAmountStub and CREATE TABLE BalanceSnapshot with the unique index. Implement src/lib/balances.ts with getBalanceAsOf(accountId, asOfDate) using findFirst where asOfDate lte ordered desc (never coerce missing to 0n), creditDebtMinor(creditLimitMinor, availableMinor), and calendarDateToday(timeZone?) defaulting Europe/Moscow (A3). Implement src/lib/validations/balance.ts with strict setBalanceSchema (accountId coerce positive int, amountMajor trimmed string, asOfDate YYYY-MM-DD regex «Укажите дату») and deleteBalanceSchema (snapshot id). Keep future-date and credit 0..limit enforcement for Server Actions in Plan 02 (need DB account). Update money.test.ts schema greps if they still require BalanceAmountStub by name — amountMinor BigInt assertion must still pass on BalanceSnapshot. Implement until Wave 0 tests are green.
  </action>
  <verify>
    <automated>npm test -- --run src/lib/validations/balance.test.ts src/lib/balances.test.ts src/lib/money.test.ts && grep -q 'model BalanceSnapshot' prisma/schema.prisma && grep -q 'accountId_asOfDate' prisma/schema.prisma && ! grep -q 'model BalanceAmountStub' prisma/schema.prisma && grep -q 'getBalanceAsOf' src/lib/balances.ts && grep -q 'setBalanceSchema' src/lib/validations/balance.ts</automated>
    <fails_when>unit tests fail, BalanceSnapshot/unique missing, BalanceAmountStub model still present, or helpers/schemas missing</fails_when>
  </verify>
  <acceptance_criteria>
    - prisma/schema.prisma defines BalanceSnapshot with named unique accountId_asOfDate and no BalanceAmountStub model
    - A new migration.sql under prisma/migrations creates BalanceSnapshot and drops BalanceAmountStub
    - src/lib/balances.ts exports getBalanceAsOf, creditDebtMinor, calendarDateToday
    - npm test for balance + balances + money test files exits 0
  </acceptance_criteria>
  <done>BalanceSnapshot data path wired; Wave 0 green (migrate deploy is Task 3)</done>
</task>

<task type="auto">
  <name>Task 3: [BLOCKING] prisma migrate deploy host gate</name>
  <files>data/wallet.db, prisma/migrations/, src/lib/foundation.test.ts</files>
  <read_first>
    .planning/phases/03-dated-balance-snapshots/03-RESEARCH.md
    prisma/schema.prisma
    src/lib/foundation.test.ts
  </read_first>
  <precondition>prisma/migrations contains the new BalanceSnapshot migration.sql from Task 2</precondition>
  <action>
    Schema-gate (Prisma): after schema/migration files are complete, stop wallet-web briefly if host SQLite is locked (Phase 02 STATE lesson), then run host migrate deploy with DATABASE_URL=file:./data/wallet.db (same proven Phase 02 command shape — migrate deploy, not db push). Confirm migrate status reports up to date. Update src/lib/foundation.test.ts table allow-list: replace BalanceAmountStub with BalanceSnapshot in the sqlite_master IN-list and expected array; add assertion that the new balance_snapshot migration name appears in _prisma_migrations. Do not COPY wallet.db into Docker.
  </action>
  <verify>
    <automated>mkdir -p data && DATABASE_URL="file:./data/wallet.db" npx prisma migrate deploy && test -f data/wallet.db && DATABASE_URL="file:./data/wallet.db" npx prisma migrate status 2>&1 | grep -qi 'up to date\|Database schema is up to date\|No pending migrations' && npm test -- --run src/lib/foundation.test.ts</automated>
    <fails_when>migrate deploy non-zero, data/wallet.db missing, pending migrations remain, or foundation.test.ts fails BalanceSnapshot table expectations</fails_when>
  </verify>
  <acceptance_criteria>
    - DATABASE_URL=file:./data/wallet.db npx prisma migrate deploy exits 0
    - foundation.test.ts expects BalanceSnapshot and not BalanceAmountStub
    - prisma migrate status reports no pending migrations
  </acceptance_criteria>
  <done>Blocking migrate deploy succeeded; foundation test proves BalanceSnapshot live</done>
</task>

</tasks>

## Artifacts this phase produces

| Symbol | Kind | File |
|--------|------|------|
| `BalanceSnapshot` | Prisma model | `prisma/schema.prisma` |
| `accountId_asOfDate` | named @@unique | `prisma/schema.prisma` |
| `getBalanceAsOf` | function | `src/lib/balances.ts` |
| `creditDebtMinor` | function | `src/lib/balances.ts` |
| `calendarDateToday` | function | `src/lib/balances.ts` |
| `setBalanceSchema` | Zod schema | `src/lib/validations/balance.ts` |
| `deleteBalanceSchema` | Zod schema | `src/lib/validations/balance.ts` |

(Plan 01 subset; Plans 02–03 add Server Action and UI symbols.)

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Migration SQL → host SQLite file | DDL replaces stub with BalanceSnapshot + unique index |
| Zod balance schemas → later Server Actions | Untrusted asOfDate/amountMajor will cross into SQLite writes in Plan 02 |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-03-01 | Tampering | asOfDate Zod shape | medium | mitigate | YYYY-MM-DD regex in setBalanceSchema; future gate in Plan 02 actions (D-12) |
| T-03-02 | Tampering | LOCF helper | high | mitigate | getBalanceAsOf returns null when empty — never coerce to 0n (BAL-02) |
| T-03-03 | Tampering | unique overwrite | medium | accept | Intended D-09 upsert identity; uniqueness enforced in schema |
| T-03-04 | Tampering | debt column smuggling | high | mitigate | BalanceSnapshot has only amountMinor; no debtMinor field |
| T-03-SC | Tampering | npm/pip/cargo installs | high | accept | No new packages this phase — legitimacy audit N/A for installs |
</threat_model>

<verification>
Wave 0 balance/LOCF tests green; schema has BalanceSnapshot unique; migrate deploy up to date; foundation.test expects BalanceSnapshot.
</verification>

<success_criteria>
BAL-02 LOCF helper and BAL-01 snapshot identity exist on a migrated host DB; stub gone; ready for set-balance UI in Plan 02.
</success_criteria>

<output>
Create `.planning/phases/03-dated-balance-snapshots/03-01-SUMMARY.md` when done
</output>
