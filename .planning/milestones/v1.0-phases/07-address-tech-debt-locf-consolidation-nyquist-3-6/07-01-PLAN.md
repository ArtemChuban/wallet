---
phase: 07-address-tech-debt-locf-consolidation-nyquist-3-6
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/lib/locf.ts
  - src/lib/locf.test.ts
  - src/app/accounts/page.tsx
  - src/app/currencies/rates/page.tsx
  - src/app/page.tsx
autonomous: true
requirements:
  - LOCF-01
  - LOCF-02
  - LOCF-05
estimate:
  tokens: 25000
  raw_tokens: 50000
  tasks: 2
  confidence: high
must_haves:
  truths:
    - Shared pure pickLatestAsOf returns null when no row has asOfDate less than or equal to D (D-03, LOCF-01)
    - Shared firstHitLocfMap on desc-sorted asOfDate-lte batches yields same latest row as pickLatestAsOf for that key and today (LOCF-05 pure↔map per Validation Architecture; Prisma LOCF contracts closed by LOCF-04 green balances/fx suites in plan 02)
    - /accounts current balance Map uses firstHitLocfMap after one findMany with asOfDate lte today and orderBy asOfDate desc (D-04, LOCF-02)
    - /currencies/rates current rate Map uses firstHitLocfMap with the same batch precondition (D-04, LOCF-02)
    - / dashboard balance and FX Maps both use firstHitLocfMap from the same snapshotsLteToday and ratesLteToday prefetch that feeds chart props (D-04, LOCF-02, Pitfall 5)
  prohibitions:
    - statement: MUST NOT call getBalanceAsOf or getRateAsOf in a loop from list or dashboard pages
      requirement_id: LOCF-02
      category: values
    - statement: MUST NOT invent balance 0n or unit FX before the first dated row
      requirement_id: LOCF-01
      category: values
    - statement: MUST NOT change Prisma schema or add npm packages
      requirement_id: LOCF-01
      category: safety
    - statement: MUST NOT add nav Валюты landing changes or PROJECT.md Active sync
      requirement_id: LOCF-02
      category: safety
  artifacts:
    - path: src/lib/locf.ts
      provides: pickLatestAsOf, firstHitLocfMap, locfAmountAsOf, locfRateAsOf
    - path: src/lib/locf.test.ts
      provides: null-before-first and pure↔map parity coverage (LOCF-05); Prisma LOCF predicate parity is LOCF-04 via balances/fx keep-green in plan 02
    - path: src/app/accounts/page.tsx
      provides: batch LOCF via firstHitLocfMap
    - path: src/app/currencies/rates/page.tsx
      provides: batch FX LOCF via firstHitLocfMap
    - path: src/app/page.tsx
      provides: dual batch LOCF Maps via firstHitLocfMap
  key_links:
    - from: src/app/accounts/page.tsx
      to: src/lib/locf.ts
      via: firstHitLocfMap(snapshotsLteToday, accountId)
      pattern: "firstHitLocfMap"
    - from: src/app/currencies/rates/page.tsx
      to: src/lib/locf.ts
      via: firstHitLocfMap(ratesLteToday, currencyCode)
      pattern: "firstHitLocfMap"
    - from: src/app/page.tsx
      to: src/lib/locf.ts
      via: firstHitLocfMap for account and currency keys on shared prefetch
      pattern: "firstHitLocfMap"
    - from: src/lib/locf.test.ts
      to: src/lib/locf.ts
      via: parity pickLatestAsOf vs firstHitLocfMap
      pattern: "pickLatestAsOf|firstHitLocfMap"
---

## Phase Goal

Consolidate triplicate LOCF into one shared path with unchanged semantics, then close Nyquist for phases 3–6 with evidence. No new user-facing features.

## Artifacts this phase produces

| Artifact | Plan | Purpose |
|----------|------|---------|
| `src/lib/locf.ts` | 01 | Shared pure pick + batch Map builders + typed wrappers |
| `src/lib/locf.test.ts` | 01 | Null-before-first + pure↔map parity (LOCF-01/05) |
| `src/app/accounts/page.tsx` | 01 | Rewire balance batch LOCF |
| `src/app/currencies/rates/page.tsx` | 01 | Rewire FX batch LOCF |
| `src/app/page.tsx` | 01 | Rewire dashboard dual Maps |
| `src/lib/historical-series.ts` | 02 | Call shared locfAmountAsOf / locfRateAsOf |
| `src/lib/balances.ts` / `src/lib/fx.ts` | 02 | Keep Prisma get*AsOf thin wrappers |
| `03\|04\|05\|06-VALIDATION.md` | 03 | status validated + nyquist_compliant true |

## SPECLESS probe fallback (recorded)

phase_req_ids from ROADMAP was null/TBD. Spec-less probe predicates were **skipped** this run. Plan `requirements` use RESEARCH-derived IDs **LOCF-01..LOCF-05** and **NYQ-03..NYQ-06** instead.

<objective>
Wave 1 tracer + page expansion: create src/lib/locf.ts and locf.test.ts, prove one RSC path on /accounts, then rewire rates and home Maps to the same builder (LOCF-01, LOCF-02, LOCF-05; D-03, D-04).

Purpose: Kill triplicate page Map loops behind one batch helper before series rewire.
Output: locf module + green parity tests; three pages call firstHitLocfMap; findMany orderBy desc + lte today unchanged.
</objective>

<execution_context>
@/home/artem/Documents/wallet/.cursor/gsd-core/workflows/execute-plan.md
@/home/artem/Documents/wallet/.cursor/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/07-address-tech-debt-locf-consolidation-nyquist-3-6/07-CONTEXT.md
@.planning/phases/07-address-tech-debt-locf-consolidation-nyquist-3-6/07-RESEARCH.md
@.planning/phases/07-address-tech-debt-locf-consolidation-nyquist-3-6/07-PATTERNS.md
@.planning/phases/07-address-tech-debt-locf-consolidation-nyquist-3-6/07-VALIDATION.md
@src/lib/historical-series.ts
@src/lib/balances.ts
@src/lib/fx.ts
@src/lib/net-worth.test.ts
@src/app/accounts/page.tsx
@src/app/currencies/rates/page.tsx
@src/app/page.tsx
</context>

<assumptions>
- Hybrid API per RESEARCH discretion: pickLatestAsOf + firstHitLocfMap + typed locfAmountAsOf / locfRateAsOf wrappers; no new packages.
- firstHitLocfMap precondition stays on callers: findMany where asOfDate lte D, orderBy asOfDate desc (D-04).
- Scope lock D-02: no nav Валюты change, no PROJECT.md Active sync.
- SPECLESS probe skipped — requirements are LOCF-*/NYQ-* from RESEARCH.
</assumptions>

<tasks>

<task type="tracer" tdd="true">
  <name>End-to-end shared LOCF — locf.ts + /accounts batch Map</name>
  <reversibility rating="costly">Shared locf.ts becomes the cross-page LOCF contract; renaming or splitting later touches all RSC callers and series (hybrid API per D-01 discretion / RESEARCH).</reversibility>
  <files>src/lib/locf.ts, src/lib/locf.test.ts, src/app/accounts/page.tsx</files>
  <read_first>
    .planning/phases/07-address-tech-debt-locf-consolidation-nyquist-3-6/07-CONTEXT.md
    .planning/phases/07-address-tech-debt-locf-consolidation-nyquist-3-6/07-RESEARCH.md
    .planning/phases/07-address-tech-debt-locf-consolidation-nyquist-3-6/07-PATTERNS.md
    .planning/phases/07-address-tech-debt-locf-consolidation-nyquist-3-6/07-VALIDATION.md
    src/lib/historical-series.ts
    src/lib/net-worth.test.ts
    src/app/accounts/page.tsx
  </read_first>
  <behavior>
    - pickLatestAsOf on rows with only asOfDate after D returns null (D-03, LOCF-01)
    - pickLatestAsOf picks the max asOfDate among rows matching key with asOfDate less than or equal to D
    - firstHitLocfMap on desc-sorted rows stores the first key hit as the latest less-than-or-equal row (LOCF-05)
    - firstHitLocfMap result for a key equals pickLatestAsOf for that key at a date after the newest row
    - accounts/page.tsx imports firstHitLocfMap from @/lib/locf and builds locfByAccount from snapshotsLteToday without a hand-rolled first-hit loop
  </behavior>
  <action>
    Per D-01, D-03, D-04, LOCF-01, LOCF-05: create src/lib/locf.ts exporting pure pickLatestAsOf(rows, matchesKey, asOfOf, asOfDate) returning T or null (semantics from private locfAmountAsOf in historical-series.ts — latest matching row with asOfOf(row) less than or equal to asOfDate). Export firstHitLocfMap(rows, keyOf) that sets Map entry only when key absent (precondition documented in a short JSDoc: rows already filtered asOfDate less than or equal to D and sorted asOfDate desc). Export thin typed wrappers locfAmountAsOf(snapshots, accountId, asOfDate) and locfRateAsOf(rates, currencyCode, asOfDate) returning amountMinor or rateToPrimaryScaled or null via pickLatestAsOf (RESEARCH open Q2). Create src/lib/locf.test.ts in pure Vitest style (describe/it/expect only — no Prisma mocks) covering null-before-first and firstHitLocfMap↔pickLatestAsOf parity on desc fixtures. Write tests first (tdd), then implement until green. Rewire src/app/accounts/page.tsx: keep findMany where asOfDate lte today and orderBy asOfDate desc; replace the inline locfByAccount loop with firstHitLocfMap(snapshotsLteToday, snap =&gt; snap.accountId). Leave historyByAccount grouping unchanged. Do not touch rates page, home page, or historical-series yet. No schema changes. No npm installs.
  </action>
  <verify>
    <automated>npm test -- src/lib/locf.test.ts && test -f src/lib/locf.ts && grep -q 'firstHitLocfMap' src/app/accounts/page.tsx && grep -q 'pickLatestAsOf' src/lib/locf.ts && grep -q 'locfAmountAsOf' src/lib/locf.ts</automated>
    <fails_when>non-zero vitest exit, locf.ts missing, accounts page lacks firstHitLocfMap, or pickLatestAsOf/locfAmountAsOf absent from locf.ts</fails_when>
  </verify>
  <acceptance_criteria>
    - npm test -- src/lib/locf.test.ts exits 0
    - src/lib/locf.ts exports pickLatestAsOf, firstHitLocfMap, locfAmountAsOf, locfRateAsOf
    - src/app/accounts/page.tsx contains firstHitLocfMap and imports from @/lib/locf
    - snapshotsLteToday query still uses asOfDate lte today and orderBy asOfDate desc
  </acceptance_criteria>
  <done>Shared LOCF module exists with green parity tests; /accounts current balances use firstHitLocfMap end-to-end</done>
</task>

<task type="auto">
  <name>Rewire rates and home batch Maps to firstHitLocfMap</name>
  <files>src/app/currencies/rates/page.tsx, src/app/page.tsx</files>
  <read_first>
    .planning/phases/07-address-tech-debt-locf-consolidation-nyquist-3-6/07-PATTERNS.md
    src/app/accounts/page.tsx
    src/app/currencies/rates/page.tsx
    src/app/page.tsx
    src/lib/locf.ts
  </read_first>
  <action>
    Per D-02, D-04, LOCF-02: in src/app/currencies/rates/page.tsx import firstHitLocfMap from @/lib/locf; keep findMany asOfDate lte today orderBy asOfDate desc; replace the inline locfByCurrency first-hit loop with firstHitLocfMap(ratesLteToday, rate =&gt; rate.currencyCode). In src/app/page.tsx replace both balance and FX inline first-hit loops with firstHitLocfMap on snapshotsLteToday (accountId) and ratesLteToday (currencyCode). Keep a single shared prefetch feeding Maps and chart serialization (RESEARCH Pitfall 5). Do not change NW math, chart components, or nav. Do not sync PROJECT.md Active. Do not edit historical-series yet.
  </action>
  <verify>
    <automated>npm test -- src/lib/locf.test.ts && grep -q 'firstHitLocfMap' src/app/currencies/rates/page.tsx && grep -q 'firstHitLocfMap' src/app/page.tsx && grep -q 'from "@/lib/locf"' src/app/page.tsx && grep -q 'from "@/lib/locf"' src/app/currencies/rates/page.tsx</automated>
    <fails_when>non-zero vitest exit, or rates/home page missing firstHitLocfMap or @/lib/locf import</fails_when>
  </verify>
  <acceptance_criteria>
    - rates page and page.tsx both import from @/lib/locf and call firstHitLocfMap
    - page.tsx still passes serialized LOCF payloads into DashboardChartsShell from the same prefetch arrays
    - npm test -- src/lib/locf.test.ts exits 0
  </acceptance_criteria>
  <done>All three RSC batch LOCF call sites use shared firstHitLocfMap (LOCF-02)</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| RSC page → SQLite via Prisma | Prefetched balance/FX rows cross into pure LOCF builders |
| Pure locf.ts → UI formatting | Null LOCF must remain null through list/dashboard display |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-07-01 | Tampering | src/lib/locf.ts pickLatestAsOf / firstHitLocfMap | high | mitigate | Preserve null-before-first; parity tests; never coalesce missing LOCF to 0n or unit rate (D-03) |
| T-07-02 | Tampering | accounts / rates / page batch Maps | medium | mitigate | Keep findMany parameterized Prisma; orderBy desc precondition; shared builder only |
| T-07-03 | Information Disclosure | N/A local single-user | low | accept | No multi-user auth surface in v1 |
| T-07-SC | Tampering | npm installs | high | accept | No new packages this plan — Package Legitimacy N/A |
</threat_model>

<verification>
Quick: npm test -- src/lib/locf.test.ts. Confirm three pages reference firstHitLocfMap. Full suite deferred to Plan 02 after series rewire.
</verification>

<success_criteria>
locf.ts and locf.test.ts exist and pass; /accounts, /currencies/rates, and / use firstHitLocfMap for current LOCF Maps; D-03/D-04 semantics unchanged.
</success_criteria>

<output>
Create `.planning/phases/07-address-tech-debt-locf-consolidation-nyquist-3-6/07-01-SUMMARY.md` when done
</output>
