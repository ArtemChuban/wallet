---
phase: 03-dated-balance-snapshots
plan: 03
type: execute
wave: 3
depends_on:
  - 03-02
files_modified:
  - src/app/accounts/actions.ts
  - src/app/accounts/actions.test.ts
  - src/app/accounts/page.tsx
  - src/components/accounts/AccountList.tsx
autonomous: false
requirements:
  - BAL-01
  - BAL-02
estimate:
  tokens: 20000
  raw_tokens: 40000
  tasks: 3
  confidence: high
must_haves:
  truths:
    - Snapshot history expands inline under the account row (not Sheet/Dialog-only history) (D-13)
    - History ordered newest first by asOfDate descending (D-14, BAL-01 ordering)
    - Non-credit history rows show date + amount only with no «текущий» marker (D-15)
    - Credit history rows show date + available only — debt not repeated on history lines (D-16)
    - Delete lives only on history rows with confirm «Удалить снимок за {date}? Это нельзя отменить.» (D-10, D-11, UI-SPEC)
    - After last snapshot deleted, expand collapses and row reverts to «Задать первый баланс» with no invented 0 (UI empty E4)
    - Expand/collapse aria-labels «Показать историю балансов» / «Скрыть историю балансов»; 44×44 tap target (UI-SPEC)
    - History panel uses muted background; delete uses destructive styling (UI-SPEC color)
    - User can overwrite a past snapshot by set-balance with that date and delete via history (BAL-01 mutation)
  prohibitions:
    - statement: MUST NOT place delete controls inside SetBalanceDialog
      requirement_id: BAL-01
      category: values
    - statement: MUST NOT invent zero after all snapshots for an account are deleted
      requirement_id: BAL-02
      category: values
    - statement: MUST NOT mark a history row as current/latest with a special badge
      requirement_id: BAL-02
      category: values
  artifacts:
    - path: src/app/accounts/actions.ts
      provides: deleteBalanceSnapshot Server Action
    - path: src/components/accounts/AccountList.tsx
      provides: expand history UI + delete controls
    - path: src/app/accounts/page.tsx
      provides: history rows serialized for each account
  key_links:
    - from: history Удалить button
      to: deleteBalanceSnapshot
      via: Server Action + confirm then revalidatePath("/accounts")
      pattern: "deleteBalanceSnapshot"
    - from: page.tsx history query
      to: AccountList expand panel
      via: newest-first snapshot props as strings
      pattern: "asOfDate|amountMinor"
---

## Phase Goal

**As a** local Wallet user, **I want to** record account balances as of a chosen date and read the correct as-of balance over time, **so that** capital history stays trustworthy before FX and net worth (BAL-01, BAL-02).

<objective>
Complete history mutation UX: inline expandable newest-first snapshot list, deleteBalanceSnapshot (history-only), then Russian UI human smoke on /accounts.

Purpose: Finish BAL-01 overwrite/delete and honest empty after last delete (BAL-02).
Output: Expandable history + delete + human-verified Russian chrome.
</objective>

<execution_context>
@/home/artem/Documents/wallet/.cursor/gsd-core/workflows/execute-plan.md
@/home/artem/Documents/wallet/.cursor/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/03-dated-balance-snapshots/03-CONTEXT.md
@.planning/phases/03-dated-balance-snapshots/03-UI-SPEC.md
@.planning/phases/03-dated-balance-snapshots/03-PATTERNS.md
@.planning/phases/03-dated-balance-snapshots/03-02-PLAN.md
@src/app/accounts/actions.ts
@src/app/accounts/page.tsx
@src/components/accounts/AccountList.tsx
@src/components/accounts/SetBalanceDialog.tsx
</context>

<tasks>

<task type="tracer">
  <name>End-to-end expand history + delete one snapshot</name>
  <files>src/app/accounts/actions.ts, src/app/accounts/actions.test.ts, src/app/accounts/page.tsx, src/components/accounts/AccountList.tsx</files>
  <read_first>
    .planning/phases/03-dated-balance-snapshots/03-CONTEXT.md
    .planning/phases/03-dated-balance-snapshots/03-UI-SPEC.md
    .planning/phases/03-dated-balance-snapshots/03-PATTERNS.md
    src/app/accounts/actions.ts
    src/app/accounts/page.tsx
    src/components/accounts/AccountList.tsx
    src/lib/validations/balance.ts
  </read_first>
  <behavior>
    - deleteBalanceSnapshot validates id via deleteBalanceSchema; deletes row; revalidatePath("/accounts"); errors return «Не удалось удалить снимок. Попробуйте снова.»
    - page loads per-account snapshots ordered asOfDate desc; serialize amountMinor as string
    - AccountList useState expand; ChevronRight/ChevronDown; history rows per D-14–D-16; delete only in history with window.confirm copy from UI-SPEC
  </behavior>
  <action>
    Per D-10, D-11, D-13, D-14, D-15, D-16: add the history-row delete Server Action on accounts actions (wire it only from expanded history controls — keep SetBalanceDialog free of delete wiring). Extend actions.test.ts for delete export + success/error paths. Load newest-first histories in page.tsx for accounts that have snapshots. Make AccountList a client island (or row client child) with per-row expand state; show history only when expanded and snapshots.length greater than 0; on zero snapshots after delete, no expand affordance and first-balance CTA returns. Confirm before delete using UI-SPEC string with DD.MM.YYYY date. Disable delete control while pending. Non-credit history: date + amount; credit history: date + available only.
  </action>
  <verify>
    <automated>npm test -- --run src/app/accounts/actions.test.ts && grep -q 'deleteBalanceSnapshot' src/app/accounts/actions.ts && grep -q 'Показать историю балансов\|ChevronRight\|ChevronDown' src/components/accounts/AccountList.tsx && grep -q 'Удалить' src/components/accounts/AccountList.tsx && ! grep -q 'deleteBalanceSnapshot' src/components/accounts/SetBalanceDialog.tsx</automated>
    <fails_when>tests fail, deleteBalanceSnapshot missing, expand/delete UI absent from AccountList, or SetBalanceDialog wires deleteBalanceSnapshot</fails_when>
  </verify>
  <acceptance_criteria>
    - deleteBalanceSnapshot exported and tested
    - AccountList has expand aria-label and Удалить in history region
    - SetBalanceDialog.tsx has no import/call of the delete Server Action (verify uses negative grep on that symbol name)
  </acceptance_criteria>
  <done>History expand + delete path works end-to-end on /accounts</done>
</task>

<task type="auto">
  <name>History empty/overflow polish + full automated suite</name>
  <files>src/components/accounts/AccountList.tsx, src/app/accounts/page.tsx</files>
  <read_first>
    .planning/phases/03-dated-balance-snapshots/03-UI-SPEC.md
    src/components/accounts/AccountList.tsx
  </read_first>
  <action>
    Per UI Considerations: ensure zero-snapshot accounts have no expand control; after last delete UX collapses to «Задать первый баланс»; history panel bg-muted/40; destructive styling on delete; page/main scroll for long history (no nested max-height trap required). Optional «История» section label. Geist Mono for amounts/dates. Run full npm test suite green.
  </action>
  <verify>
    <automated>npm test && grep -q 'bg-muted\|destructive\|Задать первый баланс' src/components/accounts/AccountList.tsx</automated>
    <fails_when>npm test non-zero, or AccountList lacks muted/destructive/first-balance styling cues</fails_when>
  </verify>
  <acceptance_criteria>
    - npm test exits 0
    - AccountList includes muted panel and first-balance CTA strings
  </acceptance_criteria>
  <done>History polish complete; full suite green</done>
</task>

<task type="checkpoint:human-verify" gate="blocking-human">
  <name>Russian UI smoke on /accounts balances</name>
  <files>src/components/accounts/AccountList.tsx, src/components/accounts/SetBalanceDialog.tsx</files>
  <read_first>
    .planning/phases/03-dated-balance-snapshots/03-UI-SPEC.md
    .planning/phases/03-dated-balance-snapshots/03-VALIDATION.md
  </read_first>
  <what-built>Dated balance snapshots on /accounts — SetBalanceDialog, LOCF row display, credit available/debt, expandable history with delete</what-built>
  <how-to-verify>
    1. Ensure Plan 01 migrate deploy applied; start app (npm run dev or docker compose up)
    2. Open /accounts — non-credit: «Задать первый баланс» → set past date balance → row shows amount · на DD.MM.YYYY
    3. Set again same date (overwrite); set older date; confirm LOCF as of today is latest less than or equal to today
    4. Expand history newest first; delete one snapshot with confirm; after last delete CTA returns (no 0.00)
    5. FIAT_CREDIT: dialog «Доступный лимит»; row shows доступно + долг; history shows available only
    6. Future date rejected with Russian error
    7. Reply approved if chrome matches UI-SPEC
  </how-to-verify>
  <action>
    Halt for human visual/locale confirmation of BAL-01/BAL-02 and 03-UI-SPEC after automated tests are green. Record approval in resume signal.
  </action>
  <resume-signal>Reply: approved RU UI balances on /accounts (or list blocking UI defects)</resume-signal>
  <verify>
    <automated>npm test</automated>
    <fails_when>full suite exits non-zero before human review</fails_when>
    <human-check>
      <what-built>Russian set-balance Dialog, LOCF rows, credit available/debt, history delete on /accounts</what-built>
      <how-to-verify>Walk checklist in how-to-verify; confirm UI-SPEC Russian strings and no invented zero</how-to-verify>
    </human-check>
  </verify>
  <acceptance_criteria>
    - Human confirms Russian set-balance + history + credit + LOCF behavior on /accounts
    - npm test exits 0 at checkpoint time
  </acceptance_criteria>
  <done>Human-verified Phase 03 UI smoke passed</done>
</task>

</tasks>

## Artifacts this phase produces

| Symbol | Kind | File |
|--------|------|------|
| `deleteBalanceSnapshot` | Server Action | `src/app/accounts/actions.ts` |
| Account history expand UI | component behavior | `src/components/accounts/AccountList.tsx` |
| Per-account history props | RSC serialization | `src/app/accounts/page.tsx` |

### Phase-wide symbols (Plans 01–03)

| Symbol | Kind | File |
|--------|------|------|
| `BalanceSnapshot` | Prisma model | `prisma/schema.prisma` |
| `getBalanceAsOf` | function | `src/lib/balances.ts` |
| `creditDebtMinor` | function | `src/lib/balances.ts` |
| `calendarDateToday` | function | `src/lib/balances.ts` |
| `setBalanceSchema` / `deleteBalanceSchema` | Zod | `src/lib/validations/balance.ts` |
| `upsertBalanceSnapshot` | Server Action | `src/app/accounts/actions.ts` |
| `deleteBalanceSnapshot` | Server Action | `src/app/accounts/actions.ts` |
| `SetBalanceDialog` | component | `src/components/accounts/SetBalanceDialog.tsx` |

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Browser → deleteBalanceSnapshot | Untrusted snapshot id |
| History UI → confirm → delete | Accidental destructive action |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-03-09 | Tampering | deleteBalanceSnapshot id | medium | mitigate | Zod positive int id; prisma delete by id only |
| T-03-10 | Repudiation | accidental delete | medium | mitigate | window.confirm with dated Russian copy before delete (D-11 UI) |
| T-03-11 | Elevation of Privilege | delete from Dialog | low | mitigate | Delete controls only in history list; dialog must not wire delete (D-11) |
| T-03-SC | Tampering | npm installs | high | accept | No new packages |
</threat_model>

<verification>
delete + expand wired; full npm test green; human Russian smoke on /accounts passed.
</verification>

<success_criteria>
User can expand history, delete/overwrite past snapshots, and see honest empty CTA after last delete; Phase 03 BAL-01/BAL-02 UI complete.
</success_criteria>

<output>
Create `.planning/phases/03-dated-balance-snapshots/03-03-SUMMARY.md` when done
</output>
