---
phase: 03-dated-balance-snapshots
plan: 02
type: execute
wave: 2
depends_on:
  - 03-01
files_modified:
  - src/app/accounts/actions.ts
  - src/app/accounts/actions.test.ts
  - src/app/accounts/page.tsx
  - src/components/accounts/AccountList.tsx
  - src/components/accounts/SetBalanceDialog.tsx
  - src/components/accounts/SetBalanceDialog.test.ts
autonomous: true
requirements:
  - BAL-01
  - BAL-02
estimate:
  tokens: 24000
  raw_tokens: 48000
  tasks: 3
  confidence: high
must_haves:
  truths:
    - User can set a non-credit account balance for today or a past asOfDate via Dialog on /accounts and see LOCF amount plus producing snapshot date on the row (BAL-01, BAL-02, D-01, D-02, D-03)
    - Future asOfDate is rejected server-side with «Дата не может быть в будущем» even if client max is bypassed (D-12, T-03-01)
    - Same-date set overwrites via prisma.balanceSnapshot.upsert on accountId_asOfDate (D-09)
    - When LOCF is null, row shows emphasized CTA «Задать первый баланс» and never shows 0.00 (D-04, UI empty E2)
    - When LOCF exists, secondary CTA is «Задать баланс»; dialog submit is «Сохранить баланс» (UI-SPEC copy)
    - FIAT_CREDIT set-balance stores available remaining in amountMinor; debt shown on row equals creditLimitMinor minus available (D-05, D-06, D-08)
    - Credit available outside 0..creditLimit rejected with «Введите сумму от 0 до кредитного лимита» (D-07)
    - Non-credit LOCF display pattern is amount CODE · на DD.MM.YYYY; credit is доступно … · долг … · на DD.MM.YYYY (UI-SPEC populated)
    - BigInt amountMinor / derived debt / limits cross RSC→client as strings (Phase 02 pattern)
    - statement: Long account names (≤120) truncate with ellipsis in the list; full name editable in existing edit Dialog
      verification: backstop
  prohibitions:
    - statement: MUST NOT add a /balances route or /accounts/[id] page in this phase
      requirement_id: BAL-01
      category: values
    - statement: MUST NOT put snapshot delete controls inside SetBalanceDialog
      requirement_id: BAL-01
      category: values
    - statement: MUST NOT persist outstanding debt as a writable FormData field or DB column
      requirement_id: BAL-01
      category: values
    - statement: MUST NOT invent 0.00 when LOCF is null
      requirement_id: BAL-02
      category: values
  artifacts:
    - path: src/app/accounts/actions.ts
      provides: upsertBalanceSnapshot Server Action
    - path: src/components/accounts/SetBalanceDialog.tsx
      provides: Russian set-balance Dialog with amount + asOfDate
    - path: src/app/accounts/page.tsx
      provides: RSC batch LOCF-as-of-today props for AccountList
    - path: src/components/accounts/AccountList.tsx
      provides: LOCF row display + first-balance CTA + SetBalanceDialog triggers
  key_links:
    - from: SetBalanceDialog
      to: upsertBalanceSnapshot
      via: useActionState + FormData accountId/amountMajor/asOfDate
      pattern: "upsertBalanceSnapshot"
    - from: src/app/accounts/page.tsx
      to: getBalanceAsOf / batch LOCF
      via: calendarDateToday + balances helpers
      pattern: "getBalanceAsOf|calendarDateToday"
    - from: upsertBalanceSnapshot
      to: prisma.balanceSnapshot.upsert
      via: accountId_asOfDate compound unique
      pattern: "accountId_asOfDate"
---

## Phase Goal

**As a** local Wallet user, **I want to** record account balances as of a chosen date and read the correct as-of balance over time, **so that** capital history stays trustworthy before FX and net worth (BAL-01, BAL-02).

<objective>
Ship the set-balance vertical slice on /accounts: upsertBalanceSnapshot Server Action, SetBalanceDialog, and current LOCF display including credit available/debt derivation.

Purpose: User can write dated balances (BAL-01) and see correct as-of-today LOCF on the list (BAL-02) without history delete yet.
Output: Working Dialog → upsert → LOCF row for non-credit and FIAT_CREDIT.
</objective>

<execution_context>
@/home/artem/Documents/wallet/.cursor/gsd-core/workflows/execute-plan.md
@/home/artem/Documents/wallet/.cursor/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/03-dated-balance-snapshots/03-CONTEXT.md
@.planning/phases/03-dated-balance-snapshots/03-RESEARCH.md
@.planning/phases/03-dated-balance-snapshots/03-PATTERNS.md
@.planning/phases/03-dated-balance-snapshots/03-UI-SPEC.md
@.planning/phases/03-dated-balance-snapshots/03-01-PLAN.md
@src/app/accounts/actions.ts
@src/app/accounts/page.tsx
@src/components/accounts/AccountList.tsx
@src/components/accounts/AccountFormDialog.tsx
@src/lib/balances.ts
@src/lib/validations/balance.ts
@src/lib/money.ts
</context>

<assumptions>
## Planner assumptions

- A4: Inline useState / dialog state sufficient; no shadcn Collapsible required.
- Delete confirmation and expandable history are Plan 03 (D-10, D-11, D-13).
</assumptions>

<context_lock_waiver>
D-05 already locked in 03-CONTEXT.md (store available remaining; debt derived). Keep one-way reversibility rating on the credit task; skip checkpoint:decision — do not re-ask.
</context_lock_waiver>

<tasks>

<task type="tracer">
  <name>End-to-end set non-credit balance via Dialog + LOCF on list</name>
  <files>src/app/accounts/actions.ts, src/app/accounts/actions.test.ts, src/app/accounts/page.tsx, src/components/accounts/SetBalanceDialog.tsx, src/components/accounts/SetBalanceDialog.test.ts, src/components/accounts/AccountList.tsx</files>
  <read_first>
    .planning/phases/03-dated-balance-snapshots/03-CONTEXT.md
    .planning/phases/03-dated-balance-snapshots/03-PATTERNS.md
    .planning/phases/03-dated-balance-snapshots/03-UI-SPEC.md
    src/components/accounts/AccountFormDialog.tsx
    src/app/accounts/actions.ts
    src/app/accounts/page.tsx
    src/components/accounts/AccountList.tsx
    src/lib/balances.ts
    src/lib/validations/balance.ts
  </read_first>
  <behavior>
    - upsertBalanceSnapshot safeParse setBalanceSchema; rejects asOfDate greater than calendarDateToday; parses amount via parseMajorToMinor with account currency scale; upserts on accountId_asOfDate; revalidatePath("/accounts")
    - SetBalanceDialog remounts form body via formKey on open; default asOfDate today prop; max=today; labels «Баланс» / «Дата» / title «Задать баланс» for non-credit
    - AccountList shows LOCF amount + на DD.MM.YYYY when present; «Задать первый баланс» accent Button when null
  </behavior>
  <action>
    Per D-01, D-02, D-03, D-04, D-09, D-12: add upsertBalanceSnapshot to src/app/accounts/actions.ts (Zod → ensureSqlitePragmas → load Account+currency → reject future asOfDate vs calendarDateToday → parseMajorToMinor → prisma.balanceSnapshot.upsert where accountId_asOfDate → revalidatePath("/accounts")). Extend actions.test.ts with vi.mock balanceSnapshot.upsert and future-date / happy-path cases; keep existing no-account-removal export checks. Create SetBalanceDialog.tsx mirroring AccountFormDialog formKey remount + useActionState; hidden accountId; amountMajor + type=date asOfDate; pending disables «Сохранить баланс». Add SetBalanceDialog.test.ts source-contract for formKey and type=date. Extend page.tsx to compute today via calendarDateToday, batch LOCF for each account (findMany asOfDate lte today then first-per-accountId, or getBalanceAsOf per account at personal scale), serialize amountMinor as string. Extend AccountList to show LOCF / first-balance CTA and open SetBalanceDialog — do not add history expand or delete yet. No new routes.
  </action>
  <verify>
    <automated>npm test -- --run src/app/accounts/actions.test.ts src/components/accounts/SetBalanceDialog.test.ts src/lib/balances.test.ts && test -f src/components/accounts/SetBalanceDialog.tsx && grep -q 'upsertBalanceSnapshot' src/app/accounts/actions.ts && grep -q 'Задать первый баланс' src/components/accounts/AccountList.tsx && grep -q 'calendarDateToday\|getBalanceAsOf' src/app/accounts/page.tsx</automated>
    <fails_when>tests fail, SetBalanceDialog missing, upsertBalanceSnapshot absent, first-balance CTA absent, or page lacks LOCF/today helpers</fails_when>
  </verify>
  <acceptance_criteria>
    - upsertBalanceSnapshot exported from actions.ts and covered by actions.test.ts
    - SetBalanceDialog exists with formKey remount and date input
    - AccountList contains «Задать первый баланс»
    - page.tsx loads LOCF using balances helpers
  </acceptance_criteria>
  <done>Non-credit set-balance path works end-to-end on /accounts list</done>
</task>

<task type="auto" tdd="true">
  <name>Credit available input + derived debt on list row</name>
  <reversibility rating="one-way">D-05 stores available remaining in amountMinor; flipping to debt-first later needs data migration and copy rewrite. CONTEXT-lock waiver: already decided — no checkpoint.</reversibility>
  <files>src/app/accounts/actions.ts, src/app/accounts/actions.test.ts, src/components/accounts/SetBalanceDialog.tsx, src/components/accounts/AccountList.tsx</files>
  <read_first>
    .planning/phases/03-dated-balance-snapshots/03-CONTEXT.md
    .planning/phases/03-dated-balance-snapshots/03-UI-SPEC.md
    src/lib/balances.ts
    src/app/accounts/actions.ts
    src/components/accounts/SetBalanceDialog.tsx
    src/components/accounts/AccountList.tsx
  </read_first>
  <behavior>
    - FIAT_CREDIT: reject availableMinor outside 0..creditLimitMinor with «Введите сумму от 0 до кредитного лимита»
    - Persist available in amountMinor only; debt = creditDebtMinor(limit, available) on read
    - Credit dialog title «Задать доступный лимит», field «Доступный лимит», hint about долг = лимит − доступно
    - Credit row shows доступно + долг + на DD.MM.YYYY (D-08)
  </behavior>
  <action>
    Per D-05, D-06, D-07, D-08: extend upsertBalanceSnapshot so FIAT_CREDIT validates 0n less than or equal to availableMinor less than or equal to creditLimitMinor after parse; non-credit keeps amountMinor greater than or equal to 0 (A2). Never accept a debt FormData field. Update SetBalanceDialog labels/title/hint for FIAT_CREDIT. On AccountList credit rows with LOCF, display available and derived debt via creditDebtMinor + formatMinorToMajor. Extend actions.test.ts for credit bounds and absence of debt in upsert create/update data.
  </action>
  <verify>
    <automated>npm test -- --run src/app/accounts/actions.test.ts src/lib/validations/balance.test.ts src/lib/balances.test.ts && grep -q 'Доступный лимит\|Задать доступный лимит' src/components/accounts/SetBalanceDialog.tsx && grep -q 'creditDebtMinor\|доступно\|долг' src/components/accounts/AccountList.tsx</automated>
    <fails_when>tests fail, credit dialog labels missing, or list lacks available/debt display wiring</fails_when>
  </verify>
  <acceptance_criteria>
    - Credit out-of-range returns the UI-SPEC Russian bounds message
    - SetBalanceDialog uses credit-specific title and amount label
    - AccountList shows available and debt for FIAT_CREDIT LOCF rows
  </acceptance_criteria>
  <done>Credit available semantics live on write and list read (D-05–D-08)</done>
</task>

<task type="auto">
  <name>Set-balance pending UX + Russian error copy smoke</name>
  <files>src/components/accounts/SetBalanceDialog.tsx, src/app/accounts/actions.ts</files>
  <read_first>
    .planning/phases/03-dated-balance-snapshots/03-UI-SPEC.md
    src/components/accounts/SetBalanceDialog.tsx
    src/components/accounts/AccountFormDialog.tsx
  </read_first>
  <action>
    Per UI-SPEC Interaction contract: disable primary «Сохранить баланс» while useActionState isPending; keep Dialog open until success then close; surface field errors for future date, invalid amount, credit bounds, and form-level «Не удалось сохранить. Проверьте поля и попробуйте снова.» Map parseMajorToMinor fractional-digit errors to Russian scale message like createAccount. Ensure secondary row CTA «Задать баланс» uses outline/ghost Button; first-balance CTA stays accent default.
  </action>
  <verify>
    <automated>grep -q 'isPending\|Сохранить баланс' src/components/accounts/SetBalanceDialog.tsx && grep -q 'Дата не может быть в будущем\|Не удалось сохранить' src/app/accounts/actions.ts src/components/accounts/SetBalanceDialog.tsx && npm test -- --run src/app/accounts/actions.test.ts src/components/accounts/SetBalanceDialog.test.ts</automated>
    <fails_when>pending/submit copy missing, Russian future/save errors absent from dialog or actions, or tests fail</fails_when>
  </verify>
  <acceptance_criteria>
    - SetBalanceDialog references isPending and «Сохранить баланс»
    - Future-date or save-failure Russian strings appear in actions and/or dialog
    - Related unit tests exit 0
  </acceptance_criteria>
  <done>Set-balance Dialog pending and error UX matches UI-SPEC</done>
</task>

</tasks>

## Artifacts this phase produces

| Symbol | Kind | File |
|--------|------|------|
| `upsertBalanceSnapshot` | Server Action | `src/app/accounts/actions.ts` |
| `BalanceActionState` | type (or extended AccountActionState) | `src/app/accounts/actions.ts` |
| `SetBalanceDialog` | component | `src/components/accounts/SetBalanceDialog.tsx` |
| LOCF row props (serialized) | RSC→client contract | `src/app/accounts/page.tsx` / `AccountList.tsx` |

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Browser FormData → upsertBalanceSnapshot | Untrusted accountId, amountMajor, asOfDate |
| Server Action → SQLite BalanceSnapshot | Persist after Zod + account existence + bounds checks |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-03-01 | Tampering | asOfDate future | high | mitigate | Server compare to calendarDateToday; ignore client-only max (D-12) |
| T-03-05 | Tampering | accountId | medium | mitigate | Zod positive int + prisma account findUnique must exist |
| T-03-06 | Tampering | credit available bounds | high | mitigate | After parseMajorToMinor enforce 0..creditLimitMinor (D-07) |
| T-03-07 | Tampering | debt field smuggling | high | mitigate | Zod .strict(); no debt key; upsert data only amountMinor |
| T-03-08 | Tampering | float money | high | mitigate | parseMajorToMinor BigInt only (reuse Phase 02 T-02-05) |
| T-03-SC | Tampering | npm installs | high | accept | No new packages |
</threat_model>

<verification>
actions + SetBalanceDialog tests green; upsert + LOCF list wiring present; credit labels and debt derivation present.
</verification>

<success_criteria>
User can set past/today balances on /accounts and see correct current LOCF including credit available/debt; future dates rejected.
</success_criteria>

<output>
Create `.planning/phases/03-dated-balance-snapshots/03-02-SUMMARY.md` when done
</output>
