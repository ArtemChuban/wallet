---
phase: 05-net-worth-dashboard
plan: 02
type: execute
wave: 2
depends_on:
  - 05-01
files_modified:
  - src/lib/net-worth.ts
  - src/lib/net-worth.test.ts
  - src/app/page.tsx
  - src/components/dashboard/DashboardAccountList.tsx
autonomous: true
requirements:
  - NW-01
  - NW-02
  - NW-03
  - ACCT-03
estimate:
  tokens: 21000
  raw_tokens: 42000
  tasks: 3
  confidence: high
must_haves:
  truths:
    - FIAT_CREDIT native column shows доступно plus долг mirroring AccountList LocfDisplay without as-of date (D-15, NW-02, E12)
    - FIAT_CREDIT primary column shows debt in PRIMARY only; available never appears as positive asset (D-15, ACCT-03, E12)
    - Hero total subtracts converted credit debt via creditDebtMinor; creditLimitMinor never added (ACCT-03, NW-01)
    - Row with null LOCF shows native «нет баланса» and primary «—»; excluded from total (D-09, E3, E11)
    - Non-primary row with LOCF but null FX shows native balance and primary «— · нет курса»; excluded from total (D-10, E10)
    - When isPartial true, hero still shows partial total and callout «Итог неполный» with UI-SPEC body between hero and list (D-11, E9)
    - Zero excluded accounts means no partial-warning banner (E9 zero-one-many)
    - Account list grows vertically within max-w-3xl main; hero and callout text wrap (E8, E7, E9 overflow)
  prohibitions:
    - statement: MUST NOT add available credit to contributionPrimaryMinor or hero sum
      requirement_id: ACCT-03
      category: values
    - statement: MUST NOT show expandable history or set-balance Dialog on dashboard rows
      requirement_id: NW-02
      category: values
    - statement: MUST NOT link «нет курса» hint to rates CRUD (deferred per CONTEXT)
      requirement_id: NW-03
      category: values
  artifacts:
    - path: src/components/dashboard/DashboardAccountList.tsx
      provides: credit, missing-balance, and missing-FX row display
    - path: src/app/page.tsx
      provides: isPartial callout banner wiring
    - path: src/lib/net-worth.ts
      provides: credit debt and exclusion flags for all row states
  key_links:
    - from: DashboardAccountList
      to: computeNetWorthRows output
      via: excludeReason and formatted debt/available strings from page
      pattern: "excludeReason|debtNativeMinor"
    - from: src/app/page.tsx
      to: partial warning UI
      via: isPartial flag from computeNetWorthRows
      pattern: "isPartial|Итог неполный"
---

## Phase Goal

**As a** local Wallet user, **I want to** see true current net worth and per-account balances in native and primary currency, **so that** I know my capital at a glance (NW-01, NW-02, NW-03, ACCT-03).

<objective>
Complete dashboard row semantics: credit available/debt display, honest exclusion states, and partial-total warning when any account is omitted from the hero sum.

Purpose: Satisfy ACCT-03 and D-09–D-11 before empty-state and human smoke (Plan 03).
Output: Full read-only list states and partial-warning callout on /.
</objective>

<execution_context>
@/home/artem/Documents/wallet/.cursor/gsd-core/workflows/execute-plan.md
@/home/artem/Documents/wallet/.cursor/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/05-net-worth-dashboard/05-CONTEXT.md
@.planning/phases/05-net-worth-dashboard/05-RESEARCH.md
@.planning/phases/05-net-worth-dashboard/05-PATTERNS.md
@.planning/phases/05-net-worth-dashboard/05-UI-SPEC.md
@.planning/phases/05-net-worth-dashboard/05-01-PLAN.md
@src/components/accounts/AccountList.tsx
@src/lib/balances.ts
@src/lib/net-worth.ts
@src/app/page.tsx
@src/components/dashboard/DashboardAccountList.tsx
</context>

<context_lock_waiver>
D-15 and ACCT-03 credit display/NW semantics locked in 05-CONTEXT.md. Keep one-way reversibility rating on credit task; skip checkpoint:decision — do not re-ask.
</context_lock_waiver>

<tasks>

<task type="auto" tdd="true">
  <name>Credit rows — available native, debt primary, NW subtraction</name>
  <reversibility rating="one-way">Credit NW semantics (debt-only primary column) are the v1 contract Phase 6 charts will reuse; showing available as asset would misstate capital. CONTEXT-lock waiver: D-15 and ACCT-03 already decided — no checkpoint.</reversibility>
  <files>src/lib/net-worth.ts, src/lib/net-worth.test.ts, src/components/dashboard/DashboardAccountList.tsx, src/app/page.tsx</files>
  <read_first>
    .planning/phases/05-net-worth-dashboard/05-CONTEXT.md
    .planning/phases/05-net-worth-dashboard/05-UI-SPEC.md
    src/lib/balances.ts
    src/lib/net-worth.ts
    src/components/accounts/AccountList.tsx
    src/components/dashboard/DashboardAccountList.tsx
    src/app/page.tsx
  </read_first>
  <behavior>
    - FIAT_CREDIT with LOCF sets nativeDisplay from available; debtNativeMinor from creditDebtMinor; contributionPrimaryMinor negative converted debt
    - Primary column formatted string uses debt only with optional «долг» prefix per UI-SPEC table
    - Increasing available decreases debt and makes total less negative; increasing limit alone does not inflate hero
  </behavior>
  <action>
    Per D-15 and ACCT-03: ensure computeNetWorthRows handles FIAT_CREDIT with creditLimitMinor and locfAmountMinor as available — derive debtNativeMinor via creditDebtMinor, set contributionPrimaryMinor to negative identity or converted debt, set primaryDisplayMinor to debt magnitude only. Extend net-worth.test.ts with credit cases if not already green. Update page.tsx to pass credit-specific formatted strings to DashboardAccountList. Update DashboardAccountList credit native column to «доступно {available} {CODE} · долг {debt} {CODE}» without «на DD.MM.YYYY»; primary column «долг {debtPrimary} {PRIMARY}» or plain debt amount per UI-SPEC. Never render available in primary column.
  </action>
  <verify>
    <automated>npx vitest run src/lib/net-worth.test.ts -t "subtracts credit debt|available" && grep -q 'доступно\|долг' src/components/dashboard/DashboardAccountList.tsx && grep -q 'creditDebtMinor\|FIAT_CREDIT' src/lib/net-worth.ts</automated>
    <fails_when>credit unit tests fail, dashboard list lacks доступно/долг copy, or net-worth.ts omits FIAT_CREDIT branch</fails_when>
  </verify>
  <acceptance_criteria>
    - computeNetWorthRows subtracts credit debt in primary minor
    - DashboardAccountList renders credit native available plus debt lines
    - Primary column for credit shows debt not available
  </acceptance_criteria>
  <done>Credit accounts display ACCT-03 semantics and reduce hero total correctly</done>
</task>

<task type="auto">
  <name>Missing balance and missing FX row states</name>
  <files>src/lib/net-worth.ts, src/components/dashboard/DashboardAccountList.tsx, src/app/page.tsx</files>
  <read_first>
    .planning/phases/05-net-worth-dashboard/05-UI-SPEC.md
    .planning/phases/05-net-worth-dashboard/05-CONTEXT.md
    src/lib/net-worth.ts
    src/components/dashboard/DashboardAccountList.tsx
  </read_first>
  <action>
    Per D-09 and D-10: when excludeReason is no_balance, render native «нет баланса» in text-muted-foreground Label size and primary «—»; account remains in flat list. When excludeReason is no_fx but native LOCF exists, render native amount CODE and primary «— · нет курса» with muted hint segment. Ensure page passes excludeReason or equivalent booleans from computeNetWorthRows rows. Confirm includedInTotal false rows never add to hero. Do not add deep link on «нет курса».
  </action>
  <verify>
    <automated>grep -q 'нет баланса' src/components/dashboard/DashboardAccountList.tsx && grep -q 'нет курса' src/components/dashboard/DashboardAccountList.tsx && npx vitest run src/lib/net-worth.test.ts -t "excludes|no_fx|no_balance"</automated>
    <fails_when>missing-state copy absent from list component or exclusion unit tests fail</fails_when>
  </verify>
  <acceptance_criteria>
    - DashboardAccountList contains «нет баланса» and «нет курса» strings
    - Unit tests confirm no_balance and no_fx exclusion
    - Excluded rows still appear in list
  </acceptance_criteria>
  <done>Honest per-row missing data display without inventing zero balances or rates</done>
</task>

<task type="auto">
  <name>Partial-total warning callout when isPartial</name>
  <files>src/app/page.tsx</files>
  <read_first>
    .planning/phases/05-net-worth-dashboard/05-UI-SPEC.md
    .planning/phases/05-net-worth-dashboard/05-CONTEXT.md
    src/app/page.tsx
    src/lib/net-worth.ts
  </read_first>
  <action>
    Per D-11: when computeNetWorthRows returns isPartial true, render callout between hero and DashboardAccountList using UI-SPEC copy verbatim — heading «Итог неполный» and body «Не все счета учтены в сумме: у части счетов нет баланса или нет курса валюты. Задайте балансы на странице «Счета» и курсы на «Курсы».» Style with rounded-lg border border-border bg-muted/60 p-4 and role status; optional lucide AlertTriangle at Label size. Always render hero amount even when partial. Omit callout when isPartial false.
  </action>
  <verify>
    <automated>grep -q 'Итог неполный' src/app/page.tsx && grep -q 'isPartial' src/app/page.tsx && npm test -- --run src/lib/net-worth.test.ts</automated>
    <fails_when>partial warning copy or isPartial wiring missing, or net-worth tests fail</fails_when>
  </verify>
  <acceptance_criteria>
    - page.tsx references isPartial from computeNetWorthRows
    - Partial warning heading and body match UI-SPEC
    - Hero block remains when isPartial true
  </acceptance_criteria>
  <done>Partial NW visibly warned when any account excluded from hero sum (D-11)</done>
</task>

</tasks>

## Artifacts this phase produces

| Symbol | Kind | File |
|--------|------|------|
| credit row display | UI strings | `src/components/dashboard/DashboardAccountList.tsx` |
| partial warning callout | RSC fragment | `src/app/page.tsx` |
| exclusion row flags | compute output | `src/lib/net-worth.ts` |

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Dashboard read-only UI | No FormData; display must not reinterpret available as asset |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-05-04 | Tampering | credit display vs sum | critical | mitigate | Primary column debt-only; unit tests plus grep for доступно absent from primary column math path |
| T-05-05 | Spoofing | partial warning omitted | medium | mitigate | isPartial from lib not UI heuristic; banner wired to any exclusion |
| T-05-06 | Information disclosure | account name XSS | low | accept | React text escaping; names validated at create (Phase 02) |
</threat_model>

<verification>
Credit rows show available+debt native and debt primary; missing states render UI-SPEC copy; isPartial shows «Итог неполный» banner; net-worth tests green.
</verification>

<success_criteria>
Dashboard list handles all account types and exclusion states; partial total is honest and visibly warned.
</success_criteria>

<output>
Create `.planning/phases/05-net-worth-dashboard/05-02-SUMMARY.md` when done
</output>
