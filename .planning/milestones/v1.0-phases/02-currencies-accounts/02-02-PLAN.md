---
phase: 02-currencies-accounts
plan: 02
type: execute
wave: 2
depends_on:
  - 02-01
files_modified:
  - src/app/layout.tsx
  - src/components/nav.tsx
  - src/app/currencies/page.tsx
  - src/app/currencies/actions.ts
  - src/components/currencies/CurrencyList.tsx
  - src/components/currencies/CurrencyFormDialog.tsx
  - src/components/ui/dialog.tsx
  - src/components/ui/input.tsx
  - src/components/ui/label.tsx
  - src/components/ui/select.tsx
autonomous: true
requirements:
  - CURR-01
estimate:
  tokens: 40000
  raw_tokens: 40000
  tasks: 2
  confidence: low
must_haves:
  truths:
    - User can open /currencies and see seeded primary RUB after Plan 01 migrate (D-01, CURR-01)
    - User can create a non-primary currency via Dialog with free-form code, name, and scale 0–18 (D-05, D-06, D-18, D-20)
    - Create always persists isPrimary false; no UI control sets primary (D-02)
    - User can edit currency name only; code and scale stay read-only after create (D-04, D-08)
    - Duplicate currency code is rejected with Russian field error «Код валюты уже существует» (CURR-01 adjacency/idempotency)
    - Currency list orders rows by code ascending for stable equal-meta display (CURR-01 ordering)
    - Zero currencies empty state uses UI-SPEC heading/body plus CTA «Добавить валюту» (UI empty E1)
    - Populated currency rows show name, mono code, scale, and «Основная» badge when isPrimary (UI populated E1)
    - Submit pending disables primary CTA and keeps Dialog open until success (UI loading E3)
    - statement: Long currency names (≤120) truncate with ellipsis in the list; full name editable in Dialog
      verification: backstop
  prohibitions:
    - statement: MUST NOT expose primary-currency switch or any create path that sets isPrimary true
      requirement_id: CURR-01
      category: values
    - statement: MUST NOT expose currency removal controls or Server Actions that delete Currency rows (includes primary — D-03, D-07)
      requirement_id: CURR-01
      category: values
  artifacts:
    - path: src/app/currencies/page.tsx
      provides: Russian RSC list at /currencies
    - path: src/app/currencies/actions.ts
      provides: createCurrency and updateCurrencyName Server Actions
    - path: src/components/nav.tsx
      provides: top nav Готовность · Валюты · Счета
  key_links:
    - from: src/app/currencies/actions.ts
      to: prisma.currency
      via: Zod safeParse then create/update name-only
      pattern: "createCurrency|updateCurrencyName"
    - from: src/components/nav.tsx
      to: /currencies
      via: Next Link
      pattern: "/currencies"
---

## Phase Goal

**As a** local Wallet user, **I want to** create and rename free-form currencies in Russian UI, **so that** the catalog is usable before accounts (CURR-01).

<objective>
Ship the currencies UI vertical slice on top of Plan 01 data: Server Actions, Russian `/currencies` Dialog UI, shadcn primitives, and top nav.

Purpose: Prove CURR-01 create/edit end-to-end in the browser after seeded RUB exists.
Output: createCurrency / updateCurrencyName, `/currencies` page, nav links including /accounts stub for Plan 03.
</objective>

<execution_context>
@/home/artem/Documents/wallet/.cursor/gsd-core/workflows/execute-plan.md
@/home/artem/Documents/wallet/.cursor/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/02-currencies-accounts/02-CONTEXT.md
@.planning/phases/02-currencies-accounts/02-RESEARCH.md
@.planning/phases/02-currencies-accounts/02-PATTERNS.md
@.planning/phases/02-currencies-accounts/02-UI-SPEC.md
@.planning/phases/02-currencies-accounts/02-01-PLAN.md
@src/lib/validations/currency.ts
@src/lib/db.ts
@src/app/layout.tsx
@src/components/ui/button.tsx
</context>

<tasks>

<task type="tracer">
  <name>End-to-end create secondary currency via Dialog + nav</name>
  <files>src/app/currencies/actions.ts, src/app/currencies/page.tsx, src/components/currencies/CurrencyList.tsx, src/components/currencies/CurrencyFormDialog.tsx, src/components/nav.tsx, src/app/layout.tsx, src/components/ui/dialog.tsx, src/components/ui/input.tsx, src/components/ui/label.tsx, src/components/ui/select.tsx</files>
  <read_first>
    .planning/phases/02-currencies-accounts/02-CONTEXT.md
    .planning/phases/02-currencies-accounts/02-RESEARCH.md
    .planning/phases/02-currencies-accounts/02-PATTERNS.md
    .planning/phases/02-currencies-accounts/02-UI-SPEC.md
    src/lib/validations/currency.ts
    src/lib/db.ts
    src/app/layout.tsx
    src/components/ui/button.tsx
    components.json
  </read_first>
  <action>
    Wire ONE happy path through UI/action layers (per D-02, D-04, D-05, D-08, D-17, D-18, D-19, D-20). Install shadcn dialog input label select via official CLI into src/components/ui (no new npm packages beyond CLI-copied components). Implement src/app/currencies/actions.ts using RESEARCH Pattern 2 (Server Action + Zod + useActionState) and PATTERNS.md ## No Analog Found for currencies/actions.ts — createCurrency hardcodes isPrimary false and never reads primary from FormData; updateCurrencyName updates name only and ignores tampered code/scale; use ensureSqlitePragmas, prisma, revalidatePath("/currencies"), useActionState-compatible return shape with fieldErrors from Plan 01 currency Zod schemas. Build RSC src/app/currencies/page.tsx (force-dynamic) listing currencies ordered by code, Russian title «Валюты», empty-state copy from UI-SPEC («Нет валют» + body), «Основная» badge for primary. Client CurrencyFormDialog: create fields Код/Название/Масштаб; edit name only with code/scale read-only; titles «Новая валюта» / «Изменить название»; CTA «Добавить валюту» / «Сохранить». Add src/components/nav.tsx top nav links Готовность · Валюты · Счета and mount in layout above children; keep / ready page (D-19). No removal UI or primary toggle.
  </action>
  <verify>
    <automated>test -f src/app/currencies/page.tsx && test -f src/app/currencies/actions.ts && grep -q 'createCurrency' src/app/currencies/actions.ts && grep -q 'updateCurrencyName' src/app/currencies/actions.ts && grep -q '/currencies' src/components/nav.tsx && grep -q '/accounts' src/components/nav.tsx && test -f src/components/ui/dialog.tsx && npm test -- --run src/lib/validations/currency.test.ts</automated>
    <fails_when>currencies page/actions missing, createCurrency/updateCurrencyName/nav links/dialog absent, or currency unit tests fail</fails_when>
  </verify>
  <acceptance_criteria>
    - createCurrency hardcodes non-primary create (isPrimary false assignment in action body)
    - updateCurrencyName path exists; create/edit Dialog components exist under src/components/currencies/
    - Nav contains /currencies and /accounts with Russian labels Валюты and Счета
    - No primary-toggle form field in currency create Dialog source
  </acceptance_criteria>
  <done>Currency create/edit UI path wired end-to-end on Plan 01 schema</done>
</task>

<task type="auto">
  <name>Currency Dialog pending UX + list chrome polish</name>
  <files>src/components/currencies/CurrencyFormDialog.tsx, src/components/currencies/CurrencyList.tsx, src/app/currencies/page.tsx</files>
  <read_first>
    .planning/phases/02-currencies-accounts/02-UI-SPEC.md
    src/components/currencies/CurrencyFormDialog.tsx
    src/components/currencies/CurrencyList.tsx
    src/app/currencies/page.tsx
  </read_first>
  <action>
    Per D-20 and UI-SPEC loading/populated contracts: ensure useActionState pending disables primary CTA and keeps Dialog open until success; list rows truncate long names with ellipsis; mono code styling; «Основная» badge only when isPrimary. Map Prisma unique violations to «Код валюты уже существует». Row edit CTAs use outline or ghost Button variants only.
  </action>
  <verify>
    <automated>grep -q 'Добавить валюту\|useActionState\|isPending\|pending' src/components/currencies/CurrencyFormDialog.tsx && grep -q 'Основная\|isPrimary' src/components/currencies/CurrencyList.tsx src/app/currencies/page.tsx && grep -R --include='*.tsx' -E 'variant=\{?"?(outline|ghost)' src/components/currencies | grep -q .</automated>
    <fails_when>pending/CTA wiring missing, primary badge path absent, or no outline/ghost row-action variants under currencies components</fails_when>
  </verify>
  <acceptance_criteria>
    - CurrencyFormDialog references pending/disabled submit behavior
    - List/page shows primary badge path
    - Row edit uses outline or ghost variants
  </acceptance_criteria>
  <done>Currency UI chrome matches UI-SPEC pending and list contracts</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Browser FormData → Server Actions | Untrusted create/update fields cross into SQLite writes |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-02-01 | Tampering | currencies/actions.ts | high | mitigate | Zod safeParse; update name-only; ignore extra FormData keys for code/scale/primary |
| T-02-02 | Tampering | createCurrency isPrimary | high | mitigate | Hardcode isPrimary false; Plan 01 partial unique index blocks second primary |
| T-02-03 | Elevation of Privilege | primary designation | medium | mitigate | No switch UI (D-02); seed-only primary |
| T-02-04 | Denial of Service | scale/code inputs | medium | mitigate | Reuse Plan 01 Zod bounds |
| T-02-SC | Tampering | shadcn CLI components | low | accept | Official @shadcn registry only; no third-party registries (UI-SPEC) |
</threat_model>

## Artifacts this phase produces

| Symbol / path | Kind | Introduced in |
|---------------|------|---------------|
| `createCurrency` / `updateCurrencyName` | Server Actions | 02-02 |
| `/currencies` RSC page + Dialog | UI | 02-02 |
| `src/components/nav.tsx` | nav | 02-02 |
| shadcn `dialog`/`input`/`label`/`select` | UI primitives | 02-02 |

<verification>
Nav and /currencies present; createCurrency/updateCurrencyName wired; currency unit tests still green.
</verification>

<success_criteria>
User can create/edit secondary currency names through Russian Dialog UI with seeded RUB primary visible (CURR-01).
</success_criteria>

<output>
Create `.planning/phases/02-currencies-accounts/02-02-SUMMARY.md` when done
</output>
