---
phase: 02-currencies-accounts
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - prisma/schema.prisma
  - prisma/migrations/
  - src/lib/money.ts
  - src/lib/money.test.ts
  - src/lib/validations/currency.ts
  - src/lib/validations/currency.test.ts
  - src/lib/foundation.test.ts
autonomous: true
requirements:
  - CURR-01
estimate:
  tokens: 36000
  raw_tokens: 36000
  tasks: 3
  confidence: low
must_haves:
  truths:
    - After migrate deploy, SQLite has seeded primary RUB (code RUB, name Рубль, scale 2, isPrimary) (D-01, CURR-01)
    - Currency.isPrimary exists; Currency_one_primary partial unique index blocks a second primary (D-02, CURR-01 concurrency)
    - parseMajorToMinor and formatMinorToMajor round-trip BigInt for scales 0, 2, 8, 18 without float (D-06 adjacency)
    - Currency Zod create accepts free-form code/name and scale 0–18; update accepts name only (D-04, D-05, D-06, D-08)
    - Empty/whitespace name or code fails Zod; scale outside 0–18 fails with «Укажите масштаб от 0 до 18» (CURR-01 empty/boundary)
  prohibitions:
    - statement: MUST NOT add Account model or account UI in this plan
      requirement_id: CURR-01
      category: values
    - statement: MUST NOT use prisma db seed as the only RUB path — seed lives in migrate deploy SQL (D-01)
      requirement_id: CURR-01
      category: safety
  artifacts:
    - path: prisma/schema.prisma
      provides: Currency.isPrimary Boolean ready for Plan 02 UI and Plan 03 Account FK
    - path: prisma/migrations
      provides: customized migration INSERT RUB primary + Currency_one_primary partial unique index
    - path: src/lib/validations/currency.ts
      provides: Zod create/update schemas for CURR-01 (No-Analog / RESEARCH Pattern 2)
    - path: src/lib/money.ts
      provides: parseMajorToMinor and formatMinorToMajor BigInt helpers
  key_links:
    - from: prisma/migrations/*/migration.sql
      to: Currency row RUB
      via: INSERT seed in migrate deploy
      pattern: "RUB"
    - from: src/lib/validations/currency.ts
      to: Zod create/update
      via: safeParse-ready schemas for later Server Actions
      pattern: "createCurrencySchema|updateCurrencyNameSchema|scale"
---

## Phase Goal

**As a** local Wallet user, **I want to** rely on a forever-seeded primary RUB and validated currency money helpers, **so that** later UI and accounts bind to a trustworthy catalog (CURR-01).

<objective>
Land currency data contract only: schema primary flag, migration-seeded RUB, money parse/format, currency Zod, Wave 0 tests, then block on migrate deploy (D-01).

Purpose: Prove CURR-01 persistence/invariants before Plan 02 currencies UI.
Output: Migrated Currency.isPrimary + seed, money helpers, currency Zod + tests.
Phase 2 interprets ROADMAP/REQUIREMENTS per CONTEXT: no currency removal (D-07); no primary switch (D-02).
</objective>

<execution_context>
@/home/artem/Documents/wallet/.cursor/gsd-core/workflows/execute-plan.md
@/home/artem/Documents/wallet/.cursor/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/02-currencies-accounts/02-CONTEXT.md
@.planning/phases/02-currencies-accounts/02-RESEARCH.md
@.planning/phases/02-currencies-accounts/02-PATTERNS.md
@.planning/phases/02-currencies-accounts/02-VALIDATION.md
@prisma/schema.prisma
@src/lib/money.ts
@src/lib/db.ts
</context>

<assumptions>
## Planner assumptions (specless / discretion)

- Currency code charset: printable ASCII, Zod min 1 max 16 (RESEARCH Q2 RESOLVED).
- List sort by `code` ascending remains the CURR-01 ordering contract for Plan 02 UI.
</assumptions>

<context_lock_waiver>
D-01 already locked in 02-CONTEXT.md (seed RUB primary in migration). Keep one-way reversibility rating for the record; skip checkpoint:decision — do not re-ask (planner-reversibility: gating a decision already made).
</context_lock_waiver>

<tasks>

<task type="auto">
  <name>Wave 0 currency Zod + money parse/format tests (red-first)</name>
  <files>src/lib/validations/currency.test.ts, src/lib/money.test.ts</files>
  <read_first>
    .planning/phases/02-currencies-accounts/02-VALIDATION.md
    .planning/phases/02-currencies-accounts/02-UI-SPEC.md
    .planning/phases/02-currencies-accounts/02-RESEARCH.md
    .planning/phases/02-currencies-accounts/02-PATTERNS.md
    src/lib/money.test.ts
    src/lib/money.ts
  </read_first>
  <behavior>
    - currency create schema accepts code USDT, name, scale 0 and 18; rejects scale -1 and 19
    - currency create rejects empty name/code; update schema accepts name only
    - parseMajorToMinor/formatMinorToMajor round-trip for scales 0, 2, 8, 18 without float
    - money.test schema conventions still forbid Float/Decimal money columns and still assert RATE_SCALE_E8
  </behavior>
  <action>
    Per D-06 and Phase 1 money contract: author src/lib/validations/currency.test.ts covering CURR-01 Zod rules (scale bounds, required fields, update name-only shape) against the API Task 2 will implement. Extend src/lib/money.test.ts with round-trip cases for parseMajorToMinor/formatMinorToMajor at scales 0, 2, 8, 18 and keep existing RATE_SCALE_E8 plus schema BigInt greps. Do not implement production helpers/schemas here — leave tests red until Task 2. Do not add Account creditLimitMinor schema assertions until Plan 03 adds the column.
  </action>
  <verify>
    <automated>test -f src/lib/validations/currency.test.ts && grep -q 'parseMajorToMinor\|formatMinorToMajor' src/lib/money.test.ts && grep -q 'scale' src/lib/validations/currency.test.ts</automated>
    <fails_when>currency.test.ts missing, money.test.ts lacks parse/format coverage, or scale assertions absent from currency tests</fails_when>
  </verify>
  <acceptance_criteria>
    - src/lib/validations/currency.test.ts exists with scale 0–18 and empty-field cases
    - src/lib/money.test.ts covers parse/format round-trips for scales 0, 2, 8, 18
  </acceptance_criteria>
  <done>Wave 0 failing/pending test files authored before schema/money implementation</done>
</task>

<task type="tracer">
  <name>End-to-end Currency.isPrimary + RUB seed migration + money/Zod</name>
  <reversibility rating="one-way">D-01 seed migration and exactly-one primary are a data contract for later FX and NW; changing seed or primary rules after real rows needs a data migration. CONTEXT-lock waiver: D-01 already locked in 02-CONTEXT.md — skip checkpoint:decision; do not re-ask.</reversibility>
  <files>prisma/schema.prisma, prisma/migrations/, src/lib/money.ts, src/lib/validations/currency.ts</files>
  <read_first>
    .planning/phases/02-currencies-accounts/02-CONTEXT.md
    .planning/phases/02-currencies-accounts/02-RESEARCH.md
    .planning/phases/02-currencies-accounts/02-PATTERNS.md
    prisma/schema.prisma
    src/lib/money.ts
    src/lib/money.test.ts
    src/lib/validations/currency.test.ts
  </read_first>
  <action>
    Wire ONE data-path through every layer this plan owns (per D-01, D-02, D-05, D-06, D-08). Extend Currency in prisma/schema.prisma with isPrimary Boolean default false and document in a schema comment that exactly-one primary is enforced by migration SQL partial unique index Currency_one_primary. Keep FxRateStub and BalanceAmountStub unchanged. Do not add Account in this plan. Create a new Prisma migration with --create-only (or equivalent non-interactive create), then customize migration.sql to ADD isPrimary, INSERT primary row code RUB name Рубль scale 2 isPrimary 1, and CREATE UNIQUE INDEX Currency_one_primary ON Currency(isPrimary) WHERE isPrimary = 1 (RESEARCH Pattern 1 — seed must live in migrate deploy path, not prisma db seed). Extend src/lib/money.ts with parseMajorToMinor and formatMinorToMajor using BigInt only (no Number/parseFloat; reject scientific notation; scale 0–18). Add src/lib/validations/currency.ts Zod schemas using RESEARCH Pattern 2 (Server Action + Zod + useActionState) and PATTERNS.md ## No Analog Found for validations/currency.ts — create requires trimmed printable-ASCII code max 16, trimmed name, integer scale 0–18 with Russian messages from UI-SPEC; update accepts name only. Implement until Wave 0 tests from Task 1 are green.
  </action>
  <verify>
    <automated>npm test -- --run src/lib/validations/currency.test.ts src/lib/money.test.ts && grep -q 'isPrimary' prisma/schema.prisma && grep -q 'RUB' prisma/migrations/*/migration.sql && grep -q 'Currency_one_primary' prisma/migrations/*/migration.sql && grep -q 'parseMajorToMinor' src/lib/money.ts</automated>
    <fails_when>unit tests fail, isPrimary/RUB seed/partial index absent, or money parse helper missing</fails_when>
  </verify>
  <acceptance_criteria>
    - prisma/schema.prisma defines Currency.isPrimary
    - A new migration.sql under prisma/migrations contains INSERT for RUB and Currency_one_primary
    - src/lib/validations/currency.ts exists with create/update schemas
    - npm test for currency + money test files exits 0
  </acceptance_criteria>
  <done>Currency data path wired end-to-end in code; Wave 0 green (migrate deploy is Task 3)</done>
</task>

<task type="auto">
  <name>Task 3: [BLOCKING] prisma migrate deploy host gate</name>
  <files>data/wallet.db, prisma/migrations/, src/lib/foundation.test.ts</files>
  <read_first>
    .planning/phases/02-currencies-accounts/02-RESEARCH.md
    prisma/schema.prisma
    src/lib/foundation.test.ts
  </read_first>
  <precondition>prisma/migrations contains the new currencies migration.sql from Task 2 including RUB INSERT</precondition>
  <action>
    Schema-gate (Prisma): after schema/migration files are complete, run host migrate deploy with DATABASE_URL=file:./data/wallet.db (same proven Phase 01 command shape). Confirm migrate status reports up to date. Extend src/lib/foundation.test.ts migrate-deploy fresh-DB assertion to expect seeded RUB primary (isPrimary=1) and Currency_one_primary present after deploy on a temp file DB — keep existing init_platform_stub expectations and add the new migration name check. Do not COPY wallet.db into Docker.
  </action>
  <verify>
    <automated>mkdir -p data && DATABASE_URL="file:./data/wallet.db" npx prisma migrate deploy && test -f data/wallet.db && DATABASE_URL="file:./data/wallet.db" npx prisma migrate status 2>&1 | grep -qi 'up to date\|Database schema is up to date\|No pending migrations' && npm test -- --run src/lib/foundation.test.ts</automated>
    <fails_when>migrate deploy non-zero, data/wallet.db missing, pending migrations remain, or foundation.test.ts fails seed/primary assertions</fails_when>
  </verify>
  <acceptance_criteria>
    - DATABASE_URL=file:./data/wallet.db npx prisma migrate deploy exits 0
    - Fresh migrate in foundation test sees Currency row RUB with primary flag
    - prisma migrate status reports no pending migrations
  </acceptance_criteria>
  <done>Blocking migrate deploy succeeded; seed primary proven in automated foundation test</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Migration SQL → host SQLite file | DDL + seed INSERT materialize durable currency catalog |
| Zod schemas → later Server Actions | Untrusted create/update fields will cross into SQLite writes in Plan 02 |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-02-02 | Tampering | createCurrency isPrimary (schema) | high | mitigate | Partial unique index Currency_one_primary; seed-only primary (D-01/D-02) |
| T-02-04 | Denial of Service | scale/code inputs | medium | mitigate | Zod max lengths and scale 0–18 bounds in currency.ts |
| T-02-05 | Tampering | money.ts parse | high | mitigate | BigInt parse only; no float path for major→minor |
| T-02-SC | Tampering | npm/pip/cargo installs | low | accept | No new packages in this plan |
</threat_model>

## Artifacts this phase produces

| Symbol / path | Kind | Introduced in |
|---------------|------|---------------|
| `Currency.isPrimary` | Prisma field | 02-01 |
| Migration seed `RUB` / `Currency_one_primary` | SQL | 02-01 |
| `parseMajorToMinor` / `formatMinorToMajor` | money helpers | 02-01 |
| `src/lib/validations/currency.ts` | Zod | 02-01 |

<verification>
Host migrate deploy up to date; currency + money unit tests green; RUB seed in migration SQL.
</verification>

<success_criteria>
Seeded primary RUB and currency Zod/money helpers exist after migrate (CURR-01 data contract).
</success_criteria>

<output>
Create `.planning/phases/02-currencies-accounts/02-01-SUMMARY.md` when done
</output>
