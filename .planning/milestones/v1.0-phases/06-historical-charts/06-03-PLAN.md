---
phase: 06-historical-charts
plan: 03
type: execute
wave: 3
depends_on:
  - 06-02
files_modified:
  - src/lib/historical-series.ts
  - src/lib/historical-series.test.ts
  - src/components/dashboard/AccountHistoryChart.tsx
  - src/components/dashboard/NetWorthHistoryChart.tsx
  - src/components/dashboard/DashboardAccountList.tsx
  - src/app/page.tsx
autonomous: false
requirements:
  - CHART-01
  - CHART-02
  - CHART-03
estimate:
  tokens: 20000
  raw_tokens: 40000
  tasks: 3
  confidence: high
must_haves:
  truths:
    - Credit account charts plot stacked debt and available Areas with stackId credit (D-11, CHART-02)
    - Credit primary mode converts both debt and available segments to primary (D-12)
    - Empty series in window mounts axes with zero points and no copy overlay (D-13, UI Considerations empty)
    - Exactly one point shows dots with no connecting line or area segment (D-15)
    - Credit legend/tooltip labels are долг and доступно (UI-SPEC)
    - Credit stack fills use var(--chart-5) debt and var(--chart-2) available (UI-SPEC Color)
    - NW and account charts still honor as-of LOCF per sample date (CHART-03)
    - statement: Empty axes single-point and stacked credit paint correctly in browser on /
      verification: backstop
    - statement: Changing shared range updates both NW chart and open account expand charts
      verification: backstop
  prohibitions:
    - statement: MUST NOT plot assets vs liabilities breakdown (v2 NW-04)
      requirement_id: CHART-01
      category: values
    - statement: MUST NOT show empty-state copy overlay on empty charts
      requirement_id: CHART-01
      category: values
    - statement: MUST NOT stack debt-only in primary credit mode
      requirement_id: CHART-02
      category: values
    - statement: MUST NOT add mutations inside expand body
      requirement_id: CHART-02
      category: safety
  artifacts:
    - path: src/lib/historical-series.ts
      provides: credit stack fields debt and available on account series
    - path: src/components/dashboard/AccountHistoryChart.tsx
      provides: stacked AreaChart for FIAT_CREDIT
    - path: src/components/dashboard/NetWorthHistoryChart.tsx
      provides: empty-axes and single-dot contracts
  key_links:
    - from: buildAccountSeries
      to: creditDebtMinor
      via: debt segment plus available LOCF for stack
      pattern: "creditDebtMinor"
    - from: AccountHistoryChart
      to: Area stackId credit
      via: debt and available dataKeys
      pattern: "stackId"
---

## Phase Goal

**As a** local Wallet user, **I want to** trust historical net-worth and per-account charts built from as-of balances and as-of FX, **so that** capital history stays honest when rates change (CHART-01, CHART-02, CHART-03).

## Artifacts this phase produces

| Artifact | Plan | Purpose |
|----------|------|---------|
| Credit stack series + AreaChart | 03 | D-11/D-12 CHART-02 complete |
| Empty/single-point chart polish | 03 | D-13/D-15 paint contracts |
| Human UI smoke | 03 | End-of-phase Russian chrome verify |

<objective>
Complete CHART-02 credit stacked series (debt+available, both convert in primary), lock D-13/D-15 empty and single-point chart contracts on NW and account charts, and human-verify Russian dashboard charts smoke.

Purpose: Close Phase 6 UI contract and sparse UX locks.
Output: Credit Area stack, empty/single-point polish, human sign-off.
</objective>

<execution_context>
@/home/artem/Documents/wallet/.cursor/gsd-core/workflows/execute-plan.md
@/home/artem/Documents/wallet/.cursor/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/06-historical-charts/06-CONTEXT.md
@.planning/phases/06-historical-charts/06-UI-SPEC.md
@.planning/phases/06-historical-charts/06-RESEARCH.md
@.planning/phases/06-historical-charts/06-VALIDATION.md
@.planning/phases/06-historical-charts/06-02-PLAN.md
@src/lib/historical-series.ts
@src/lib/balances.ts
@src/components/dashboard/AccountHistoryChart.tsx
@src/components/dashboard/NetWorthHistoryChart.tsx
@src/app/page.tsx
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Credit stacked debt plus available series native and primary</name>
  <files>src/lib/historical-series.ts, src/lib/historical-series.test.ts, src/components/dashboard/AccountHistoryChart.tsx</files>
  <read_first>
    .planning/phases/06-historical-charts/06-CONTEXT.md
    .planning/phases/06-historical-charts/06-RESEARCH.md
    .planning/phases/06-historical-charts/06-UI-SPEC.md
    .planning/phases/06-historical-charts/06-PATTERNS.md
    src/lib/historical-series.ts
    src/lib/balances.ts
    src/lib/net-worth.ts
    src/components/dashboard/AccountHistoryChart.tsx
  </read_first>
  <behavior>
    - FIAT_CREDIT native points include debtMinor via creditDebtMinor(limit, available) and availableMinor from LOCF
    - Primary mode converts both debt and available with same as-of FX; skip entire date if FX null (D-12, D-16)
    - Chart series is not NW contribution — both segments positive stack heights (RESEARCH pitfall 5)
    - AccountHistoryChart uses AreaChart two Area components stackId credit dataKeys debt and available fills var(--chart-5) and var(--chart-2) dots when length equals 1 connectNulls false
  </behavior>
  <action>
    Per D-11, D-12, D-16, CHART-02, CHART-03: extend buildAccountSeries for type FIAT_CREDIT to emit debt and available major numbers (and bigint fields for tests) using creditDebtMinor; in primary mode convert both segments via convertOtherMinorToPrimaryMinor with LOCF rate at D; skip date when rate null. Add Vitest cases for stacked native amounts and primary dual conversion. Update AccountHistoryChart to branch isCredit into stacked AreaChart with legend/tooltip labels долг and доступно; keep LineChart path for non-credit. Do not use NW contributionPrimaryMinor as the stack height.
  </action>
  <verify>
    <automated>npm test -- src/lib/historical-series.test.ts && grep -q 'stackId' src/components/dashboard/AccountHistoryChart.tsx && grep -q 'долг' src/components/dashboard/AccountHistoryChart.tsx && grep -q 'доступно' src/components/dashboard/AccountHistoryChart.tsx && grep -q 'creditDebtMinor' src/lib/historical-series.ts</automated>
    <fails_when>tests fail, stackId or Russian stack labels missing in AccountHistoryChart, or creditDebtMinor unused in historical-series</fails_when>
  </verify>
  <acceptance_criteria>
    - Credit stack tests green for native and primary
    - AccountHistoryChart has stackId and долг/доступно labels
    - historical-series imports or calls creditDebtMinor
  </acceptance_criteria>
  <done>Credit expand charts show stacked debt and available in native and primary modes</done>
</task>

<task type="auto">
  <name>Empty axes and single-point dot contracts on NW and account charts</name>
  <files>src/components/dashboard/NetWorthHistoryChart.tsx, src/components/dashboard/AccountHistoryChart.tsx</files>
  <read_first>
    .planning/phases/06-historical-charts/06-UI-SPEC.md
    .planning/phases/06-historical-charts/06-RESEARCH.md
    src/components/dashboard/NetWorthHistoryChart.tsx
    src/components/dashboard/AccountHistoryChart.tsx
    src/lib/dates.ts
  </read_first>
  <action>
    Per D-13, D-15, UI-SPEC Interaction: for data length 0 always render XAxis YAxis with explicit Y domain 0 to 1 and X tick labels for window start and today via formatAsOfDisplay — axes only, no overlay message string. For data length 1 set Line/Area dot true so the single mark paints; with one point Recharts draws no segment which satisfies no-line. Keep ChartContainer className h-[200px] w-full. Apply same empty/single rules to NW LineChart and account Line/Area charts. Omit chart section remains page responsibility when accounts.length is 0 (Phase 05).
  </action>
  <verify>
    <automated>npm test -- src/lib/historical-series.test.ts && grep -q 'h-\[200px\]' src/components/dashboard/NetWorthHistoryChart.tsx && grep -q 'h-\[200px\]' src/components/dashboard/AccountHistoryChart.tsx && grep -E -q 'dot=\{[^}]*length' src/components/dashboard/NetWorthHistoryChart.tsx && grep -E -q 'domain|YAxis' src/components/dashboard/NetWorthHistoryChart.tsx</automated>
    <fails_when>tests fail, 200px height missing on either chart, NW chart lacks length-based dot or YAxis/domain empty handling</fails_when>
  </verify>
  <acceptance_criteria>
    - Both chart components use h-[200px]
    - NetWorthHistoryChart sets dot from data length
    - NetWorthHistoryChart includes YAxis or domain handling for empty series
  </acceptance_criteria>
  <done>Empty and single-point chart contracts implemented for NW and account charts</done>
</task>

<task type="checkpoint:human-verify" gate="blocking-human">
  <name>Russian charts UI smoke on /</name>
  <files>src/app/page.tsx, src/components/dashboard/DashboardChartsShell.tsx, src/components/dashboard/DashboardAccountList.tsx</files>
  <read_first>
    .planning/phases/06-historical-charts/06-UI-SPEC.md
    .planning/phases/06-historical-charts/06-VALIDATION.md
    src/app/page.tsx
  </read_first>
  <what-built>Historical charts on / — NW LineChart under Капитал, shared 30д/90д/1г/всё range, account expand chart-only with native/primary toggle, credit stacked долг/доступно, empty axes and single-point dots (D-01–D-16, CHART-01–03)</what-built>
  <how-to-verify>
    1. Plans 01–03 tasks complete; npm test green; start app (npm run dev or docker compose up)
    2. Layout: Hero Капитал → partial banner if any → Период presets → NW chart → account list (D-03)
    3. Change 30д/90д/1г/всё — NW points update; open account expand uses same preset (D-05–D-08)
    4. Expand non-credit: chart only; toggle В валюте счёта / В {PRIMARY}; primary-currency account hides toggle (D-02, D-04, D-09, D-10)
    5. Expand credit: stacked Areas долг + доступно; primary mode converts both (D-11, D-12)
    6. Empty window: axes with zero points, no overlay copy (D-13); one-point series: single dot, no line (D-15)
    7. No History nav item; no set/delete inside expand
  </how-to-verify>
  <action>
    Halt for human visual confirmation of CHART-01–03 and 06-UI-SPEC after automated tests are green. Record approval in resume signal.
  </action>
  <resume-signal>Reply: approved RU UI charts on / (or list blocking UI defects)</resume-signal>
  <verify>
    <automated>npm test</automated>
    <fails_when>full suite exits non-zero before human review</fails_when>
    <human-check>
      <what-built>Russian historical charts on / with NW series, shared range, account expand, credit stack</what-built>
      <how-to-verify>Walk checklist in how-to-verify; confirm UI-SPEC Russian strings and D-13/D-15 paint</how-to-verify>
    </human-check>
  </verify>
  <acceptance_criteria>
    - Human confirms Russian chart chrome on /
    - npm test exits 0 at checkpoint time
  </acceptance_criteria>
  <done>Human-verify PASS recorded for Phase 6 charts chrome</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Credit chart tooltips | долг/доступно labels plus amounts from DB-derived series |
| Human UAT browser | Read-only visual confirmation; no new write paths |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-06-08 | Tampering | Credit Area tooltips | medium | mitigate | Static Russian labels; amounts via formatMinorToMajor React text |
| T-06-09 | Repudiation | Chart paint UAT | low | accept | Manual checkpoint records human confirmation; no audit log in v1 |
| T-06-10 | Elevation | Expand body | low | mitigate | Still no Server Actions or delete/set controls (D-04) |
| T-06-SC | Tampering | npm recharts | high | mitigate | No new installs this plan; pin remains from Plan 01 |
</threat_model>

<verification>
npm test green; human confirms Russian chart smoke including credit stack empty axes single-point shared range.
</verification>

<success_criteria>
D-11–D-16 chart UX complete; CHART-01–03 satisfied; human checkpoint passed.
</success_criteria>

<output>
Create `.planning/phases/06-historical-charts/06-03-SUMMARY.md` when done
</output>
