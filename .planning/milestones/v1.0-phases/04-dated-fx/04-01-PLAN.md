---
phase: 04-dated-fx
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - prisma/schema.prisma
  - prisma/migrations/
  - src/lib/fx.ts
  - src/lib/fx.test.ts
  - src/lib/money.ts
  - src/lib/money.test.ts
  - src/lib/validations/fx.ts
  - src/lib/validations/fx.test.ts
  - src/lib/foundation.test.ts
autonomous: true
requirements:
  - FX-01
  - FX-02
estimate:
  tokens: 21000
  raw_tokens: 42000
  tasks: 3
  confidence: high
must_haves:
  truths:
    - Rate as of date D is the latest FxRate with asOfDate less than or equal to D (FX-02 boundary/adjacency: equal asOfDate included via lte)
    - Before a currency's first rate, getRateAsOf returns null and never invents 0 or 1 (FX-01 empty, FX-02, D-15)
    - Same (currencyCode, asOfDate) upsert overwrites rateToPrimaryScaled (FX-01 adjacency, D-11)
    - After inserting a later rate, as-of reads for earlier D still return the prior row (FX-02 forward-effective)
    - Stored rateToPrimaryScaled is primary major units per 1 other major unit times 10^8 (scale 8 BigInt, FX-01 precision)
    - invertRateScaled uses integer (RATE_SCALE_E8 squared) / scaled truncating toward zero and rejects result less than or equal to 0n (FX-01 precision)
    - convertOtherMinorToPrimaryMinor uses truncating BigInt division and is unit-tested without rates-tab UI (FX-02 precision, D-17)
    - setFxRateSchema accepts currencyCode, rateMajor, asOfDate YYYY-MM-DD, direction toPrimary|fromPrimary; future-date and primary-pair rejection live in Server Action path covered by Plan 02 (FX-01 boundary, D-08, D-09)
    - After migrate deploy, SQLite has FxRate with unique currencyCode_asOfDate and FxRateStub is gone
    - Primary currency never gets FxRate rows; primary identity is caller short-circuit with no self-rate storage (D-16)
  prohibitions:
    - statement: MUST NOT invent rate 0 or 1 when no FxRate exists for asOfDate D
      requirement_id: FX-02
      category: values
    - statement: MUST NOT keep FxRateStub alongside FxRate after this plan's migration
      requirement_id: FX-01
      category: safety
    - statement: MUST NOT store a second rate column or UI direction flag in the database
      requirement_id: FX-01
      category: values
    - statement: MUST NOT create FxRate rows for the primary currency against itself
      requirement_id: FX-01
      category: values
  artifacts:
    - path: prisma/schema.prisma
      provides: FxRate model + Currency.fxRates relation; stub removed
    - path: prisma/migrations
      provides: DROP FxRateStub / CREATE FxRate + unique index + FK migration
    - path: src/lib/fx.ts
      provides: getRateAsOf, convertOtherMinorToPrimaryMinor
    - path: src/lib/money.ts
      provides: parseRateToScaled, formatRateScaled, invertRateScaled
    - path: src/lib/validations/fx.ts
      provides: setFxRateSchema and deleteFxRateSchema Zod contracts
  key_links:
    - from: src/lib/fx.ts
      to: prisma.fxRate
      via: findFirst where asOfDate lte D orderBy desc
      pattern: "getRateAsOf"
    - from: prisma/schema.prisma
      to: "@@unique currencyCode_asOfDate"
      via: named unique for compound upsert
      pattern: "currencyCode_asOfDate"
---

## Phase Goal

**As a** local Wallet user, **I want to** maintain dated exchange rates between the primary currency and other currencies, **so that** multi-currency amounts convert honestly as of any date (FX-01, FX-02).

<objective>
Land the FxRate data contract and LOCF helpers: Wave 0 red tests, replace FxRateStub, rate parse/format/invert + Zod, then block on prisma migrate deploy.

Purpose: Prove FX-02 LOCF and FX-01 overwrite identity before Currencies rates UI (Plan 02).
Output: Migrated FxRate, src/lib/fx.ts, rate helpers on money.ts, src/lib/validations/fx.ts, green Wave 0 tests.
</objective>

<execution_context>
@/home/artem/Documents/wallet/.cursor/gsd-core/workflows/execute-plan.md
@/home/artem/Documents/wallet/.cursor/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/04-dated-fx/04-CONTEXT.md
@.planning/phases/04-dated-fx/04-RESEARCH.md
@.planning/phases/04-dated-fx/04-PATTERNS.md
@.planning/phases/04-dated-fx/04-VALIDATION.md
@.planning/phases/04-dated-fx/04-UI-SPEC.md
@prisma/schema.prisma
@src/lib/money.ts
@src/lib/balances.ts
@src/lib/foundation.test.ts
@src/lib/db.ts
</context>

<assumptions>
## Planner assumptions (specless / discretion)

- A1: Prisma model name is FxRate (RESEARCH discretion lock).
- A3: rateToPrimaryScaled means major primary units per 1 major other × 10^8 (1 USD = 90.00 RUB stores 90 × 10^8).
- A4: convertOtherMinorToPrimaryMinor truncates toward zero via BigInt division (RESEARCH A4); NW rounding policy deferred.
- A5: Russian positivity message «Курс должен быть больше 0» (UI-SPEC).
- A7: getRateAsOf(primaryCode, D) is unused — callers short-circuit identity (D-16); helper may return null if invoked with primary rather than inventing ×1.
- Edge FX-01/FX-02 probes resolved into must_haves.truths above (boundary, adjacency, empty, ordering deferred to Plan 03 history, precision) — not silent drops.
</assumptions>

<assumption_delta_decision>
noun: asOfDate (user-selected calendar date on FxRate)
decision: no-change
rationale: Assumption-delta fired on ROADMAP dated-rate wording; asOfDate stays a field on FxRate parallel to BalanceSnapshot, not a new primary identity model.
</assumption_delta_decision>

<context_lock_waiver>
D-11 (unique currencyCode+asOfDate) and D-15 (null before first rate) already locked in 04-CONTEXT.md. Keep reversibility ratings on implementing tasks; skip checkpoint:decision — do not re-ask (planner-reversibility: gating a decision already made).
</context_lock_waiver>

<tasks>

<task type="auto">
  <name>Wave 0 FX Zod + LOCF + rate-helper tests (red-first)</name>
  <files>src/lib/validations/fx.test.ts, src/lib/fx.test.ts, src/lib/money.test.ts</files>
  <read_first>
    .planning/phases/04-dated-fx/04-VALIDATION.md
    .planning/phases/04-dated-fx/04-RESEARCH.md
    .planning/phases/04-dated-fx/04-PATTERNS.md
    .planning/phases/04-dated-fx/04-UI-SPEC.md
    src/lib/validations/balance.test.ts
    src/lib/balances.test.ts
    src/lib/money.test.ts
  </read_first>
  <behavior>
    - setFxRateSchema accepts currencyCode, rateMajor, asOfDate YYYY-MM-DD, direction toPrimary|fromPrimary (default toPrimary); rejects empty rateMajor and malformed date with Russian messages from 04-UI-SPEC
    - deleteFxRateSchema accepts positive id; rejects non-positive
    - getRateAsOf between two rates returns earlier; on exact date returns that row; before first returns null not 0/1 (FX-02, D-15)
    - upsert same (currencyCode, asOfDate) overwrites rateToPrimaryScaled; later insert leaves earlier as-of unchanged (forward-effective)
    - parseRateToScaled / formatRateScaled round-trip at scale 8; invertRateScaled rejects less than or equal to 0n and documents truncation
    - convertOtherMinorToPrimaryMinor: known scale-2/scale-2 example; returns computed bigint (null-handling for missing rate is caller/getRateAsOf responsibility)
  </behavior>
  <action>
    Per FX-01/FX-02 and 04-VALIDATION Wave 0: author src/lib/validations/fx.test.ts covering setFxRateSchema and deleteFxRateSchema shapes (Russian «Укажите дату», «Введите курс», direction enum). Author src/lib/fx.test.ts covering LOCF table cases from RESEARCH (asOf between → earlier; asOf equal → that row; before first → null; overwrite same date; forward-effective after later insert). Mock prisma.fxRate.findFirst via vi.mock("@/lib/db") using balances.test.ts pattern. Extend src/lib/money.test.ts with parseRateToScaled, formatRateScaled, invertRateScaled cases (reject less than or equal to 0n; truncation). Add convert helper cases in fx.test.ts. Do not implement production modules here — leave tests red until Task 2.
  </action>
  <verify>
    <automated>test -f src/lib/validations/fx.test.ts && test -f src/lib/fx.test.ts && grep -q 'getRateAsOf\|asOfDate\|null' src/lib/fx.test.ts && grep -q 'setFxRateSchema\|asOfDate\|rateMajor' src/lib/validations/fx.test.ts && grep -q 'invertRateScaled\|parseRateToScaled' src/lib/money.test.ts</automated>
    <fails_when>any listed test file missing, or LOCF/null assertions absent from fx.test.ts, or setFxRateSchema/rateMajor coverage absent from fx validation test, or invert/parse rate coverage absent from money.test.ts</fails_when>
  </verify>
  <acceptance_criteria>
    - src/lib/validations/fx.test.ts exists with date/rateMajor/direction Zod cases
    - src/lib/fx.test.ts exists with LOCF, null-before-first, overwrite, and forward-effective cases
    - src/lib/money.test.ts includes invertRateScaled and parseRateToScaled cases
  </acceptance_criteria>
  <done>Wave 0 failing/pending test files authored before schema/helper implementation</done>
</task>

<task type="tracer">
  <name>End-to-end FxRate schema + LOCF helpers + Zod + rate scale helpers</name>
  <reversibility rating="one-way">D-15 null-before-first is the honesty contract Phase 5–6 callers must handle (inventing a default later rewrites history); D-11 unique (currencyCode, asOfDate) is costly LOCF identity — relaxing uniqueness breaks as-of reads.</reversibility>
  <files>prisma/schema.prisma, prisma/migrations/, src/lib/fx.ts, src/lib/money.ts, src/lib/validations/fx.ts, src/lib/money.test.ts</files>
  <read_first>
    .planning/phases/04-dated-fx/04-CONTEXT.md
    .planning/phases/04-dated-fx/04-RESEARCH.md
    .planning/phases/04-dated-fx/04-PATTERNS.md
    prisma/schema.prisma
    src/lib/money.ts
    src/lib/balances.ts
    src/lib/validations/balance.ts
    src/lib/validations/fx.test.ts
    src/lib/fx.test.ts
  </read_first>
  <action>
    Wire ONE data path for FX-02 LOCF and FX-01 rate identity (per D-11, D-15, D-16, D-17). In prisma/schema.prisma remove FxRateStub; add FxRate with id, currencyCode FK to Currency onDelete Restrict, asOfDate String YYYY-MM-DD, rateToPrimaryScaled BigInt with stub comment semantics, and @@unique([currencyCode, asOfDate], name: "currencyCode_asOfDate"). Add fxRates FxRate[] on Currency. Create a new Prisma migration non-interactively that DROP TABLE FxRateStub and CREATE TABLE FxRate with FK and unique index (mirror balance_snapshot migration; no row backfill). Implement src/lib/fx.ts with getRateAsOf(currencyCode, asOfDate) that awaits ensureSqlitePragmas() before Prisma findFirst where asOfDate lte ordered desc (never coerce missing to 0/1), plus convertOtherMinorToPrimaryMinor(otherMinor, rateToPrimaryScaled, otherScale, primaryScale) using truncating BigInt math from RESEARCH. Implement parseRateToScaled, formatRateScaled, invertRateScaled beside RATE_SCALE_E8 in src/lib/money.ts (reuse parseMajorToMinor/formatMinorToMajor at scale 8; integer invert). Implement src/lib/validations/fx.ts with strict setFxRateSchema (currencyCode, rateMajor, asOfDate regex «Укажите дату», direction enum default toPrimary) and deleteFxRateSchema (id). Keep future-date, rate greater than 0, and isPrimary rejection for Server Actions in Plan 02. Keep money.test.ts BigInt schema assertion green after rename (rateToPrimaryScaled BigInt still required on FxRate). Implement until Wave 0 tests are green.
  </action>
  <verify>
    <automated>npm test -- --run src/lib/validations/fx.test.ts src/lib/fx.test.ts src/lib/money.test.ts && grep -q 'model FxRate' prisma/schema.prisma && grep -q 'currencyCode_asOfDate' prisma/schema.prisma && ! grep -q 'model FxRateStub' prisma/schema.prisma && grep -q 'getRateAsOf' src/lib/fx.ts && grep -q 'ensureSqlitePragmas' src/lib/fx.ts && grep -q 'setFxRateSchema' src/lib/validations/fx.ts && grep -q 'invertRateScaled' src/lib/money.ts</automated>
    <fails_when>unit tests fail, FxRate/unique missing, FxRateStub model still present, helpers/schemas missing, or ensureSqlitePragmas absent from fx.ts</fails_when>
  </verify>
  <acceptance_criteria>
    - prisma/schema.prisma defines FxRate with named unique currencyCode_asOfDate and no FxRateStub model
    - A new migration.sql under prisma/migrations creates FxRate and drops FxRateStub
    - src/lib/fx.ts exports getRateAsOf and convertOtherMinorToPrimaryMinor; awaits ensureSqlitePragmas before Prisma
    - src/lib/money.ts exports parseRateToScaled, formatRateScaled, invertRateScaled
    - npm test for fx validation + fx + money test files exits 0
  </acceptance_criteria>
  <done>FxRate data path wired; Wave 0 green (migrate deploy is Task 3)</done>
</task>

<task type="auto">
  <name>Task 3: [BLOCKING] prisma migrate deploy host gate</name>
  <files>data/wallet.db, prisma/migrations/, src/lib/foundation.test.ts</files>
  <read_first>
    .planning/phases/04-dated-fx/04-RESEARCH.md
    prisma/schema.prisma
    src/lib/foundation.test.ts
  </read_first>
  <precondition>prisma/migrations contains the new FxRate migration.sql from Task 2</precondition>
  <action>
    Schema-gate (Prisma): after schema/migration files are complete, stop wallet-web briefly if host SQLite is locked (Phase 02/03 STATE lesson), then run host migrate deploy with DATABASE_URL=file:./data/wallet.db (same proven Phase 03 command shape — migrate deploy, not db push). Confirm migrate status reports up to date. Update src/lib/foundation.test.ts table allow-list: replace FxRateStub with FxRate in the sqlite_master IN-list and expected array; add assertion that the new fx_rate migration name appears in _prisma_migrations. Do not COPY wallet.db into Docker.
  </action>
  <verify>
    <automated>mkdir -p data && DATABASE_URL="file:./data/wallet.db" npx prisma migrate deploy && test -f data/wallet.db && DATABASE_URL="file:./data/wallet.db" npx prisma migrate status 2>&1 | grep -qi 'up to date\|Database schema is up to date\|No pending migrations' && npm test -- --run src/lib/foundation.test.ts</automated>
    <fails_when>migrate deploy non-zero, data/wallet.db missing, pending migrations remain, or foundation.test.ts fails FxRate table expectations</fails_when>
  </verify>
  <acceptance_criteria>
    - DATABASE_URL=file:./data/wallet.db npx prisma migrate deploy exits 0
    - foundation.test.ts expects FxRate and not FxRateStub
    - prisma migrate status reports no pending migrations
  </acceptance_criteria>
  <done>Blocking migrate deploy succeeded; foundation test proves FxRate live</done>
</task>

</tasks>

## Artifacts this phase produces

| Symbol | Kind | File |
|--------|------|------|
| `FxRate` | Prisma model | `prisma/schema.prisma` |
| `currencyCode_asOfDate` | named @@unique | `prisma/schema.prisma` |
| `getRateAsOf` | function | `src/lib/fx.ts` |
| `convertOtherMinorToPrimaryMinor` | function | `src/lib/fx.ts` |
| `parseRateToScaled` | function | `src/lib/money.ts` |
| `formatRateScaled` | function | `src/lib/money.ts` |
| `invertRateScaled` | function | `src/lib/money.ts` |
| `setFxRateSchema` | Zod schema | `src/lib/validations/fx.ts` |
| `deleteFxRateSchema` | Zod schema | `src/lib/validations/fx.ts` |

(Plan 01 subset; Plans 02–03 add Server Action and UI symbols.)

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Migration SQL → host SQLite file | DDL replaces stub with FxRate + unique index + Currency FK |
| Zod FX schemas → later Server Actions | Untrusted asOfDate/rateMajor/currencyCode will cross into SQLite writes in Plan 02 |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-04-01 | Tampering | asOfDate / rate Zod shape | medium | mitigate | YYYY-MM-DD regex + rateMajor string in setFxRateSchema; future and greater-than-zero gates in Plan 02 actions (D-08, D-09) |
| T-04-02 | Tampering | LOCF helper | high | mitigate | getRateAsOf returns null when empty — never coerce to 0 or 1 (D-15 / FX-02) |
| T-04-03 | Tampering | unique overwrite | medium | accept | Intended D-11 upsert identity; uniqueness enforced in schema |
| T-04-04 | Tampering | Float rate path | high | mitigate | BigInt + RATE_SCALE_E8 only; parseRateToScaled via parseMajorToMinor scale 8 |
| T-04-05 | Tampering | primary self-rate row | high | mitigate | No primary FxRate rows in schema usage; Plan 02 Server Action rejects isPrimary (D-16) |
| T-04-SC | Tampering | npm/pip/cargo installs | high | accept | No new packages this phase — legitimacy audit N/A for installs |
</threat_model>

<verification>
Wave 0 FX/LOCF/rate-helper tests green; schema has FxRate unique; migrate deploy up to date; foundation.test expects FxRate.
</verification>

<success_criteria>
FX-02 LOCF helper and FX-01 rate identity exist on a migrated host DB; stub gone; ready for rates UI in Plan 02.
</success_criteria>

<output>
Create `.planning/phases/04-dated-fx/04-01-SUMMARY.md` when done
</output>
