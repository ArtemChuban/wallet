---
phase: 14-dohody-crud-nav
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/lib/income.test.ts
  - src/components/nav.tsx
  - src/components/nav.test.ts
  - src/app/income/page.tsx
  - src/lib/validations/income.ts
  - src/lib/validations/income.test.ts
  - src/lib/income.ts
autonomous: true
requirements:
  - SRC-01
  - SRC-02
  - UI-01
estimate:
  tokens: 25000
  raw_tokens: 50000
  tasks: 3
  confidence: high
must_haves:
  truths:
    - "Nav shows «Доходы» after «Счета» linking to /income (D-13, D-14, UI-01)"
    - "/income page renders title «Доходы» and honesty copy that income tracking does not change account balances (D-11, UI-01)"
    - "src/app/income/page.tsx exists; Phase 13 UI-00 absence assertion is replaced with a positive route-exists check (UI-01)"
    - "Zod accepts recurring create shape: personId, currencyCode, plannedAmountMajor > 0, dayOfMonth 1–31, startAsOf YYYY-MM-DD (SRC-01)"
    - "Zod accepts one-time create shape: personId, currencyCode, plannedAmountMajor > 0, plannedAsOf YYYY-MM-DD, optional note (SRC-02)"
    - "dayOfMonth boundary: 1 and 31 accepted; 0 and 32 rejected (SRC-01 edge)"
    - "plannedAmountMajor rejects zero/negative/empty; money stays major-string → BigInt minors via existing parse helpers later (SRC-01/02 precision)"
    - "nextOpenPlannedAsOf returns earliest unfilled recurring slot in horizon, including past unfilled slots (D-02, D-09, D-10)"
    - "Empty people state copy paths ready: page can show «Нет людей» chrome (UI-SPEC empty E1)"
  prohibitions:
    - statement: MUST NOT ship record-actual controls, overdue «заполни» row chrome, or variance UI
      requirement_id: UI-01
      category: absence
    - statement: MUST NOT install new npm packages
      requirement_id: UI-01
      category: safety
    - statement: MUST NOT add totals hero or DebtDetailDialog-style row-click detail on /income
      requirement_id: UI-01
      category: absence
  artifacts:
    - path: src/app/income/page.tsx
      provides: force-dynamic RSC shell + header honesty + empty people chrome
    - path: src/components/nav.tsx
      provides: «Доходы» link after Счета
    - path: src/lib/validations/income.ts
      provides: create/update Zod schemas for recurring and one-time
    - path: src/lib/validations/income.test.ts
      provides: SRC-01/SRC-02 boundary + optional-note tests
    - path: src/lib/income.ts
      provides: nextOpenPlannedAsOf helper
    - path: src/lib/income.test.ts
      provides: UI-00 flip + nextOpenPlannedAsOf D-10 cases
  key_links:
    - from: nav.tsx links
      to: /income page
      via: href /income
      pattern: "/income"
    - from: nextOpenPlannedAsOf
      to: listRecurringOccurrences
      via: earliest unfilled occurrence key
      pattern: "nextOpenPlannedAsOf"
    - from: income.test.ts
      to: src/app/income/page.tsx
      via: positive existsSync route assert
      pattern: "src/app/income"
---

## Specless probe fallback

Unresolved probe predicates recorded as assumptions / truths:

| Req | Category | Resolution |
|-----|----------|------------|
| SRC-01 | boundary | dayOfMonth 1/31 accept; 0/32 reject — in validations tests |
| SRC-01 | precision | major string + positive refine; BigInt minors only at action layer (Plan 02) |
| SRC-01 | idempotency | create is insert-not-upsert — two submits → two rows (Plan 02 actions) |
| SRC-01 | concurrency | local SQLite last-write-wins; no optimistic locking this phase (assumption) |
| SRC-02 | boundary/precision/idempotency | same money/date Zod rules; optional note empty → undefined |
| UI-01 | absence | no actual/overdue chrome this plan; full CRUD chrome in Plan 03 |
| UI-01 | authority | local single-user app — no auth gate on Server Actions (assumption) |

<assumption_delta_decision>
- **Noun primary:** OneTimeIncome.note (and recurring note) remain optional fields on definitions.
- **Decision:** no-change
- **Rationale:** ROADMAP/REQ «optional note» is a field optionality cue, not a singular→plural identity shift; schema already has optional note (Phase 13). Do not promote note to required.
</assumption_delta_decision>

## Phase Goal

User can manage income sources from a dedicated «Доходы» section (ROADMAP Phase 14).

<objective>
Flip UI-00, ship nav + thin `/income` shell (D-11, D-13, D-14), Zod income schemas (SRC-01, SRC-02), and `nextOpenPlannedAsOf` for list sort/display (D-02, D-09, D-10).

Purpose: Prove route + nav + validation contracts before Server Actions and full Debts-clone UI.
Output: Working `/income` empty shell, updated nav tests, income Zod + next-open helper with green vitest.
</objective>

<execution_context>
@/home/artem/Documents/wallet/.cursor/gsd-core/workflows/execute-plan.md
@/home/artem/Documents/wallet/.cursor/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/14-dohody-crud-nav/14-CONTEXT.md
@.planning/phases/14-dohody-crud-nav/14-RESEARCH.md
@.planning/phases/14-dohody-crud-nav/14-PATTERNS.md
@.planning/phases/14-dohody-crud-nav/14-VALIDATION.md
@.planning/phases/14-dohody-crud-nav/14-UI-SPEC.md
@src/components/nav.tsx
@src/components/nav.test.ts
@src/app/debts/page.tsx
@src/lib/income.ts
@src/lib/income.test.ts
@src/lib/validations/debts.ts
@src/lib/validations/debts.test.ts
@src/lib/dates.ts
</context>

<assumptions>
## Planner assumptions

- A1 (RESEARCH): Edit locks person+currency after create — schemas for update omit those fields (Plan 02/03).
- A2: Recurring create shows visible `startAsOf` defaulting to Moscow today — page/dialog in later plans; Zod requires the field.
- A3: next-open horizon ≈ `addCalendarDays(max(today, startAsOf), 400)`; if all filled, fall back to `startAsOf` for sort key (document in helper JSDoc).
- A4: RU type labels «Ежемесячный» / «Разовый» — Plan 03 UI.
- Concurrent writes: SQLite last-write-wins; no extra locking (probe concurrency).
- Authority: single-user local; Server Actions unauthenticated (probe authority).
- No Prisma schema changes this phase — schema push gate N/A.
</assumptions>

## Artifacts this phase produces

- `src/app/income/page.tsx` (+ later actions / components in Plans 02–03)
- `src/components/income/IncomeList.tsx`, `IncomeFormDialog.tsx` (Plan 03)
- `src/app/income/actions.ts` + `actions.test.ts` (Plan 02)
- `src/lib/validations/income.ts` + `.test.ts` (this plan)
- `nextOpenPlannedAsOf` in `src/lib/income.ts` (this plan)
- Nav «Доходы» + flipped UI-00 (this plan)
- Extended `deletePerson` Restrict across debts+income (Plan 02)

<tasks>

<task type="tracer">
  <name>End-to-end «Доходы» nav + /income shell — flip UI-00</name>
  <files>src/lib/income.test.ts, src/components/nav.tsx, src/components/nav.test.ts, src/app/income/page.tsx</files>
  <read_first>
    .planning/phases/14-dohody-crud-nav/14-CONTEXT.md
    .planning/phases/14-dohody-crud-nav/14-UI-SPEC.md
    .planning/phases/14-dohody-crud-nav/14-PATTERNS.md
    src/lib/income.test.ts
    src/components/nav.tsx
    src/components/nav.test.ts
    src/app/debts/page.tsx
  </read_first>
  <behavior>
    - income.test.ts asserts src/app/income/page.tsx exists (replaces Phase 13 absence check) (UI-01)
    - nav links href order is /, /accounts, /income, /debts, /currencies/rates with labels Главная, Счета, Доходы, Долги, Валюты (D-13, D-14)
    - /income page is force-dynamic, title «Доходы», honesty line exact UI-SPEC copy under D-11
  </behavior>
  <action>
    First rewrite the Phase 13 isolation test that asserted the income app route directory is absent: require `src/app/income/page.tsx` to exist via existsSync (UI-01). Then insert `{ href: "/income", label: "Доходы" }` immediately after `/accounts` in `src/components/nav.tsx` (D-13, D-14). Update `nav.test.ts` expected href/label arrays to the five-link order. Create thin `src/app/income/page.tsx` mirroring debts shell: `export const dynamic = "force-dynamic"`, `ensureSqlitePragmas`, load people ordered by name (no income includes required yet — empty shell OK), main `max-w-3xl`, h1 «Доходы», honesty subtitle exactly `Учёт доходов не меняет остатки на счетах.` (D-11). When people.length === 0 show empty chrome «Нет людей» + body from UI-SPEC (E1); do not add IncomeList/IncomeFormDialog yet. Do not add Server Actions. Do not add overdue or actual UI. Zero new packages.
  </action>
  <verify>
    <automated>npx vitest run src/lib/income.test.ts src/components/nav.test.ts &amp;&amp; test -f src/app/income/page.tsx &amp;&amp; grep -q 'Доходы' src/components/nav.tsx &amp;&amp; grep -q 'href: "/income"' src/components/nav.tsx &amp;&amp; grep -q 'Учёт доходов не меняет остатки на счетах.' src/app/income/page.tsx</automated>
    <fails_when>vitest non-zero, page.tsx missing, nav missing Доходы or /income href, or honesty copy absent from page</fails_when>
  </verify>
  <acceptance_criteria>
    - src/app/income/page.tsx is tracked and exports force-dynamic page
    - nav.test.ts expects /income after /accounts
    - income.test.ts no longer asserts income route absence; asserts page module presence
    - Page shows D-11 honesty copy verbatim
  </acceptance_criteria>
  <done>Nav → /income empty shell works; UI-00 flipped; nav tests green.</done>
</task>

<task type="auto" tdd="true">
  <name>Zod income validations for recurring + one-time (SRC-01, SRC-02)</name>
  <files>src/lib/validations/income.ts, src/lib/validations/income.test.ts</files>
  <read_first>
    src/lib/validations/debts.ts
    src/lib/validations/debts.test.ts
    .planning/phases/14-dohody-crud-nav/14-RESEARCH.md
    prisma/schema.prisma
  </read_first>
  <behavior>
    - createRecurringIncomeSchema accepts dayOfMonth 1 and 31; rejects 0 and 32 (SRC-01 boundary)
    - createRecurringIncomeSchema requires startAsOf YYYY-MM-DD, currencyCode, personId, positive plannedAmountMajor (SRC-01)
    - createOneTimeIncomeSchema accepts optional note omitted/empty; requires plannedAsOf + positive plannedAmountMajor (SRC-02)
    - WithNewPerson variants use name instead of personId (D-07 readiness)
    - update schemas are .strict() and omit personId/currencyCode/kind (RESEARCH A1)
    - Duplicate create payloads remain valid Zod — idempotency is insert semantics at action layer, not Zod reject
  </behavior>
  <action>
    Mirror `src/lib/validations/debts.ts` primitives (asOfDateSchema, personNameSchema, currencyCodeSchema, optionalNoteSchema, isStrictlyPositiveMajor). Add createRecurringIncomeSchema + createRecurringIncomeWithNewPersonSchema: plannedAmountMajor, dayOfMonth int 1–31, startAsOf, note optional (SRC-01, D-08 field set). Add createOneTimeIncomeSchema + WithNewPerson: plannedAsOf, plannedAmountMajor, note optional (SRC-02). Add updateRecurringIncomeSchema / updateOneTimeIncomeSchema for amount + schedule fields + note only — no person/currency (A1). All `.strict()`. Write vitest covering accept/reject boundaries including dayOfMonth edges and empty note → undefined. Do not implement Server Actions here.
  </action>
  <verify>
    <automated>npx vitest run src/lib/validations/income.test.ts</automated>
    <fails_when>vitest non-zero, or "0 passed" / no test files found</fails_when>
  </verify>
  <acceptance_criteria>
    - income.test.ts covers dayOfMonth 1/31 accept and 0/32 reject
    - one-time optional note path covered
    - schemas exported for Plan 02 actions to import
  </acceptance_criteria>
  <done>SRC-01/SRC-02 Zod contracts green.</done>
</task>

<task type="auto" tdd="true">
  <name>nextOpenPlannedAsOf helper for list sort/display (D-02, D-09, D-10)</name>
  <files>src/lib/income.ts, src/lib/income.test.ts</files>
  <read_first>
    src/lib/income.ts
    src/lib/income.test.ts
    src/lib/dates.ts
    .planning/phases/14-dohody-crud-nav/14-RESEARCH.md
  </read_first>
  <behavior>
    - Given recurring def startAsOf in past, empty actuals, today after start → returns earliest unfilled slot (may be past) (D-10)
    - Given actual covering earliest slot → returns next open later slot (D-09)
    - One-time display uses definition plannedAsOf directly in page mapping later — helper may be recurring-only
    - Module stays pure: no Prisma / net-worth / historical-series imports
  </behavior>
  <action>
    Add pure `nextOpenPlannedAsOf(def, actuals, todayAsOf)` (or equivalent name) wrapping `listRecurringOccurrences`: from=def.startAsOf, to=`addCalendarDays(max(todayAsOf, startAsOf), 400)`, find earliest occurrence whose `occurrenceKeyString` is not among actuals (D-02, D-09, D-10). If none in window, return startAsOf as sort fallback (A3). Extend income.test.ts with past-unfilled and skip-filled cases. Keep existing ISO file-scan on income.ts. Do not touch UI components.
  </action>
  <verify>
    <automated>npx vitest run src/lib/income.test.ts &amp;&amp; grep -q 'nextOpenPlannedAsOf' src/lib/income.ts</automated>
    <fails_when>vitest non-zero, or nextOpenPlannedAsOf export missing from income.ts</fails_when>
  </verify>
  <acceptance_criteria>
    - Helper exported from income.ts
    - Test proves past unfilled slot returned (D-10)
    - income.ts isolation scans still pass
  </acceptance_criteria>
  <done>Next-open date helper ready for page mapping.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Browser → Server Actions (later plans) | Untrusted FormData; this plan only Zod contracts |
| RSC page → Prisma | Trusted local DB read for empty shell |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-14-01 | Tampering | income validations | medium | mitigate | `.strict()` Zod + positive major refine; dayOfMonth int bounds (this plan) |
| T-14-02 | Spoofing | note/name fields | low | mitigate | trim/max-length Zod; React text escaping in Plan 03 |
| T-14-03 | Tampering | side-ledger isolation | high | mitigate | Route shell only; mutation isolation enforced Plan 02 file-scan |
| T-14-04 | Elevation | Server Actions | low | accept | Local single-user app — no multi-user auth (assumption) |
| T-14-SC | Tampering | npm installs | high | mitigate | Zero new packages — legitimacy gate N/A |
</threat_model>

<verification>
npx vitest run src/lib/income.test.ts src/components/nav.test.ts src/lib/validations/income.test.ts
</verification>

<success_criteria>
Nav «Доходы» works; /income empty shell with D-11 copy; Zod SRC-01/02 green; nextOpenPlannedAsOf tested.
</success_criteria>

<output>
Create `.planning/phases/14-dohody-crud-nav/14-01-SUMMARY.md` when done
</output>
