---
phase: 16-counterparty-income-stats
plan: 02
type: execute
wave: 2
depends_on:
  - "16-01"
files_modified:
  - src/components/income/IncomeList.tsx
  - src/components/income/income-ui.test.ts
  - src/app/income/page.tsx
  - .planning/REQUIREMENTS.md
  - .planning/phases/16-counterparty-income-stats/16-VALIDATION.md
autonomous: true
requirements:
  - CPTY-01
estimate:
  tokens: 25000
  raw_tokens: 50000
  tasks: 3
  confidence: high
must_haves:
  truths:
    - "Person header shows hybrid native (prominent) + primary (secondary) per UI-SPEC identity-omit rule (CPTY-01, D-05, D-06, D-12)"
    - "Multi-currency Person shows one native line per currency + one rolled primary (D-07)"
    - "isPartial Person shows compact «Итог неполный · нет курса» under amounts; no page banner (D-08, D-11)"
    - "REQUIREMENTS.md CPTY-01 wording matches hybrid native+primary @ actualAsOf (D-05)"
    - "No global «всего получено» hero; chart unused on /income (D-04)"
  prohibitions:
    - statement: MUST NOT mount debts page totals hero or page-level partial banner on /income
      requirement_id: CPTY-01
      category: absence
    - statement: MUST NOT invent success-green/amber for FX partial chrome
      requirement_id: CPTY-01
      category: visual
    - statement: MUST NOT add period picker or withdrawal-date FX modeling
      requirement_id: CPTY-01
      category: scope
  artifacts:
    - path: src/components/income/IncomeList.tsx
      provides: full UI-SPEC PersonGroup hybrid stats chrome
    - path: src/components/income/income-ui.test.ts
      provides: green counterparty stats file-scan suite
    - path: .planning/REQUIREMENTS.md
      provides: CPTY-01 hybrid wording synced to D-05
    - path: .planning/phases/16-counterparty-income-stats/16-VALIDATION.md
      provides: task map filled; nyquist_compliant ready for auditor
  key_links:
    - from: PersonIncomeListItem.stats.isPartial
      to: header partial line role=status
      via: «Итог неполный · нет курса»
      pattern: "Итог неполный"
    - from: nativeByCurrency display lines
      to: primary secondary line
      via: identity-omit when single primary && !isPartial
      pattern: "font-mono"
    - from: REQUIREMENTS CPTY-01
      to: CONTEXT D-05
      via: hybrid wording sync
      pattern: "CPTY-01"
---

<objective>
Finish Person-header hybrid UI per 16-UI-SPEC (multi-ccy, primary secondary, per-group partial), sync REQUIREMENTS CPTY-01 to hybrid (D-05), and close Wave 0 UI file-scans + VALIDATION map (CPTY-01, D-04..D-12, D-15).

Purpose: User-visible honesty chrome matches locked discuss + design contract.
Output: Green income-ui counterparty stats suite; REQUIREMENTS hybrid text; VALIDATION task rows filled.
</objective>

<execution_context>
@/home/artem/Documents/wallet/.cursor/gsd-core/workflows/execute-plan.md
@/home/artem/Documents/wallet/.cursor/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/16-counterparty-income-stats/16-CONTEXT.md
@.planning/phases/16-counterparty-income-stats/16-UI-SPEC.md
@.planning/phases/16-counterparty-income-stats/16-PATTERNS.md
@.planning/phases/16-counterparty-income-stats/16-VALIDATION.md
@.planning/phases/16-counterparty-income-stats/16-01-PLAN.md
@src/components/income/IncomeList.tsx
@src/app/income/page.tsx
@src/components/debts/DebtsPrimaryTotalsHero.tsx
</context>

<tasks>

<task type="auto" tdd="true">
  <name>PersonGroup hybrid chrome — multi-ccy, primary, partial</name>
  <files>src/components/income/IncomeList.tsx, src/app/income/page.tsx, src/components/income/income-ui.test.ts</files>
  <read_first>
    .planning/phases/16-counterparty-income-stats/16-UI-SPEC.md
    .planning/phases/16-counterparty-income-stats/16-CONTEXT.md
    src/components/income/IncomeList.tsx
    src/app/income/page.tsx
    src/components/debts/DebtsPrimaryTotalsHero.tsx
  </read_first>
  <behavior>
    - Layout: name left; stats column text-right; CTAs far right; gap scale 4/8/16 only (D-10, D-12)
    - Native lines: text-base font-semibold font-mono; primary line text-sm text-muted-foreground font-mono when shown (D-06, D-12)
    - Identity-omit: hide primary when nativeByCurrency.length===1 AND that currency is primary AND !isPartial; else show primary (UI-SPEC amendment to D-06)
    - Multi-ccy: one native line per currency sorted asc + one primary (D-07)
    - Partial: single line «Итог неполный · нет курса» with role=status under amounts; per-group only (D-08, D-11)
    - Window hint «за всё время» Label muted (D-15); no period picker (D-13)
    - No page hero / no chart on /income (D-04)
  </behavior>
  <action>
    Complete PersonGroup stats UI to match 16-UI-SPEC Copywriting + Interaction contracts (D-04, D-06..D-12, D-15). Ensure page serializes whatever fields UI needs (native lines with code/display, primaryDisplay, primaryCode, isPartial). Compress debts hero tone to one partial line — do not mount debts totals hero component; do not add page-level partial banner; do not use warning/destructive tokens for FX partial (Phase 15 overdue chrome unchanged). Prefer inline header markup over new registry components; zero new npm / no shadcn add. Update income-ui counterparty stats scans for required copy. No withdrawal-date modeling (D-09 deferred).
  </action>
  <verify>
    <automated>npx vitest run -t "counterparty stats" src/components/income/income-ui.test.ts &amp;&amp; grep -q 'за всё время' src/components/income/IncomeList.tsx &amp;&amp; grep -q 'Итог неполный' src/components/income/IncomeList.tsx &amp;&amp; grep -q 'нет курса' src/components/income/IncomeList.tsx</automated>
    <fails_when>vitest non-zero or required RU stats strings missing from IncomeList</fails_when>
  </verify>
  <acceptance_criteria>
    - Hybrid header chrome matches UI-SPEC including identity-omit and partial line (CPTY-01, D-05..D-12)
    - File-scan counterparty stats describe green
  </acceptance_criteria>
  <done>Person header hybrid UI complete per UI-SPEC.</done>
</task>

<task type="auto">
  <name>Sync REQUIREMENTS CPTY-01 to hybrid (D-05)</name>
  <files>.planning/REQUIREMENTS.md</files>
  <read_first>
    .planning/REQUIREMENTS.md
    .planning/phases/16-counterparty-income-stats/16-CONTEXT.md
  </read_first>
  <action>
    Update REQUIREMENTS.md CPTY-01 checkbox text from primary-only Σ wording to hybrid native-first + primary secondary via FX LOCF as-of each fact actualAsOf with partial honesty when rates missing — per CONTEXT D-05. Keep CPTY-01 id and Phase 16 mapping row; leave checkbox unchecked until phase verify. Do not change other requirement IDs.
  </action>
  <verify>
    <automated>grep -n 'CPTY-01' .planning/REQUIREMENTS.md | head -5 &amp;&amp; grep -v '^#' .planning/REQUIREMENTS.md | grep -i 'CPTY-01' | grep -Eiq 'hybrid|native|actualAsOf|получен'</automated>
    <fails_when>CPTY-01 line still primary-only with no hybrid/native/actualAsOf signal</fails_when>
  </verify>
  <acceptance_criteria>
    - CPTY-01 wording reflects hybrid native+primary @ actualAsOf (D-05)
  </acceptance_criteria>
  <done>REQUIREMENTS.md CPTY-01 synced to locked hybrid decision.</done>
</task>

<task type="auto">
  <name>VALIDATION map + full suite gate</name>
  <files>.planning/phases/16-counterparty-income-stats/16-VALIDATION.md, src/components/income/income-ui.test.ts</files>
  <read_first>
    .planning/phases/16-counterparty-income-stats/16-VALIDATION.md
    .planning/phases/16-counterparty-income-stats/16-01-PLAN.md
    .planning/phases/16-counterparty-income-stats/16-02-PLAN.md
  </read_first>
  <action>
    Fill 16-VALIDATION.md Per-Task Verification Map rows for all Plan 01/02 tasks (Wave, Requirement CPTY-01, Threat refs T-16-01/T-16-02, automated commands matching plan verifies). Mark Wave 0 checkboxes complete; set wave_0_complete true; set nyquist_compliant true only if every task has automated verify and sampling rules satisfied — else leave false and note gap. Ensure income-ui scans cover page.tsx fxRate + computePersonIncomeStats wiring and absence of debts totals hero mount on income page; keep DestructiveConfirmStep / no browser native confirm API assertions. Run full targeted suite as phase gate.
  </action>
  <verify>
    <automated>npx vitest run src/lib/income.test.ts src/components/income/income-ui.test.ts src/app/income/actions.test.ts src/lib/validations/income.test.ts &amp;&amp; grep -q 'computePersonIncomeStats' .planning/phases/16-counterparty-income-stats/16-VALIDATION.md &amp;&amp; grep -q 'wave_0_complete: true' .planning/phases/16-counterparty-income-stats/16-VALIDATION.md</automated>
    <fails_when>vitest non-zero, VALIDATION missing task map coverage, or wave_0_complete not true</fails_when>
  </verify>
  <acceptance_criteria>
    - VALIDATION maps Plan 01/02 tasks; Wave 0 marked complete
    - Targeted income suites green (CPTY-01 gate)
  </acceptance_criteria>
  <done>Phase 16 validation contract closed for execute/verify handoff.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| RSC stats props → PersonGroup | Client renders display-only; must not invent primary zeros |
| UI copy → user trust | Partial honesty language must appear when isPartial |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-16-01 | Info disclosure | PersonGroup partial chrome | high | mitigate | Show «Итог неполный · нет курса» when isPartial; never silent empty primary |
| T-16-02 | Elevation | IncomeList data sources | high | mitigate | Stats props from income page only; no debt direction labels |
| T-16-06 | Spoofing | REQUIREMENTS drift | medium | mitigate | Sync CPTY-01 text to D-05 hybrid so verify/audit match product |
| T-16-SC | Tampering | npm installs | high | mitigate | Zero new packages; no shadcn add |
</threat_model>

<verification>
npx vitest run src/lib/income.test.ts src/components/income/income-ui.test.ts
grep -n 'CPTY-01' .planning/REQUIREMENTS.md
</verification>

<success_criteria>
UI-SPEC hybrid header live; REQUIREMENTS CPTY-01 hybrid; VALIDATION Wave 0 complete; file-scans green; no page hero.
</success_criteria>

<output>
Create `.planning/phases/16-counterparty-income-stats/16-02-SUMMARY.md` when done
</output>
