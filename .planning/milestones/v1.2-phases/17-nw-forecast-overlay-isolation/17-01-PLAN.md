---
phase: 17-nw-forecast-overlay-isolation
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/lib/nw-forecast.ts
  - src/lib/nw-forecast.test.ts
  - src/lib/iniso.test.ts
  - src/app/page.tsx
  - src/components/dashboard/DashboardChartsShell.tsx
  - src/components/dashboard/NetWorthHistoryChart.tsx
  - src/components/dashboard/nw-forecast-ui.test.ts
autonomous: true
requirements:
  - FCST-01
  - ISO-01
estimate:
  tokens: 45000
  raw_tokens: 90000
  tasks: 3
  confidence: high
must_haves:
  truths:
    - "On `/`, NW chart shows dashed future overlay from open planned income (recurring + future one-time) when includable slots exist (FCST-01, D-01, D-02, D-08, D-09)"
    - "Today NW hinge is accounts-only; slots with plannedAsOf === today and filled/overdue slots stay out of overlay (D-02, D-03)"
    - "Forward series is cumulative stair-step from today's NW anchor through horizon end (D-04, D-06)"
    - "Horizon mirrors range preset: 30d→+30, 90d→+90, 1y→+365, all→+365 (D-05)"
    - "FX converts future slots with locfRateAsOf as of today; missing rate excludes slot + isPartialForecast; never invent rates (D-13, D-14, D-16)"
    - "No includable open slots → hide forecast Line + «Прогноз» legend; fact chart continues (UI empty E1, D-08)"
    - "RSC page load + client useMemo merge — no forecast-specific skeleton (UI loading E1)"
    - "net-worth.ts / historical-series.ts stay free of income/nw-forecast imports (ISO-01, D-17, D-18)"
  prohibitions:
    - statement: MUST NOT feed income into computeNetWorthRows or past buildNetWorthSeries
      requirement_id: ISO-01
      category: isolation
    - statement: MUST NOT invent FX rates 0/1 for missing LOCF
      requirement_id: FCST-01
      category: honesty
    - statement: MUST NOT use fixed independent 90d horizon instead of preset mirror
      requirement_id: FCST-01
      category: integrity
    - statement: MUST NOT install new npm packages
      requirement_id: FCST-01
      category: safety
  artifacts:
    - path: src/lib/nw-forecast.ts
      provides: forecastHorizonEnd + buildNetWorthForecastSeries (D-04..D-06, D-13..D-16, D-18)
    - path: src/lib/nw-forecast.test.ts
      provides: Wave 0→green forecast membership/cumulative/horizon/FX suite
    - path: src/lib/iniso.test.ts
      provides: Wave 0 RED INISO scaffolds (green in Plan 02)
    - path: src/app/page.tsx
      provides: income defs/actuals load + anchorPrimaryMinor + forecast payload to shell
    - path: src/components/dashboard/DashboardChartsShell.tsx
      provides: useMemo forecast recompute on range + merge fact/forecast rows
    - path: src/components/dashboard/NetWorthHistoryChart.tsx
      provides: tracer ComposedChart + dashed Line path (full chrome Plan 03)
    - path: src/components/dashboard/nw-forecast-ui.test.ts
      provides: Wave 0 chart file-scan describe
  key_links:
    - from: page.tsx income load + computeNetWorthRows anchor
      to: DashboardChartsShell forecast props
      via: serializable defs/actuals/rates + anchorPrimaryMinor + today
      pattern: "anchorPrimaryMinor"
    - from: DashboardChartsShell range preset
      to: forecastHorizonEnd + buildNetWorthForecastSeries
      via: useMemo keyed by range (discretion — recompute per preset)
      pattern: "forecastHorizonEnd"
    - from: open ForecastSlot[]
      to: buildNetWorthForecastSeries
      via: listAllInRange filter plannedAsOf > today ∧ no actual (D-01, D-02)
      pattern: "buildNetWorthForecastSeries"
---

<objective>
Wave 0 RED scaffolds + tracer end-to-end NW forecast overlay (open slot → pure cumulative builder → Капитал chart merge) + domain expansion for membership/horizon/FX (FCST-01, D-01..D-08, D-13..D-16, D-18).

Purpose: Prove preset-mirrored overlay math and import walls before full chart chrome / INISO green.
Output: Green nw-forecast suite; page+shell wire forecast; thin dashed Line on ComposedChart; INISO RED scaffolds ready for Plan 02.
</objective>

<execution_context>
@/home/artem/Documents/wallet/.cursor/gsd-core/workflows/execute-plan.md
@/home/artem/Documents/wallet/.cursor/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/17-nw-forecast-overlay-isolation/17-CONTEXT.md
@.planning/phases/17-nw-forecast-overlay-isolation/17-RESEARCH.md
@.planning/phases/17-nw-forecast-overlay-isolation/17-PATTERNS.md
@.planning/phases/17-nw-forecast-overlay-isolation/17-UI-SPEC.md
@.planning/phases/17-nw-forecast-overlay-isolation/17-VALIDATION.md
@src/lib/historical-series.ts
@src/lib/income.ts
@src/lib/locf.ts
@src/lib/money.ts
@src/lib/dates.ts
@src/lib/disol.test.ts
@src/app/page.tsx
@src/components/dashboard/DashboardChartsShell.tsx
@src/components/dashboard/NetWorthHistoryChart.tsx
</context>

## User story

**As a** user on Капитал,
**I want to** see a forward dashed NW overlay from open planned pay (recurring + future one-time) without changing historical NW,
**so that** I can plan ahead while trusting past LOCF as account-only fact.

## Artifacts this phase produces

- Wave 0 failing→green nw-forecast + INISO RED scaffolds (this plan)
- `nw-forecast.ts` + page/shell tracer merge + thin ComposedChart Line (this plan)
- Full INISO green + FCST-01 docs sync (Plan 02)
- Full chart chrome / tooltip / legend / ReferenceLine / partial banner (Plan 03)

<tasks>

<task type="auto" tdd="true">
  <name>Wave 0 — RED scaffolds for nw-forecast + INISO + chart file-scan</name>
  <files>src/lib/nw-forecast.test.ts, src/lib/iniso.test.ts, src/components/dashboard/nw-forecast-ui.test.ts</files>
  <read_first>
    .planning/phases/17-nw-forecast-overlay-isolation/17-VALIDATION.md
    .planning/phases/17-nw-forecast-overlay-isolation/17-RESEARCH.md
    src/lib/historical-series.test.ts
    src/lib/disol.test.ts
    src/lib/income.test.ts
  </read_first>
  <behavior>
    - describe forecastHorizonEnd: 30d→+30, 90d→+90, 1y→+365, all→+365 from today (D-05)
    - describe forecast membership/cumulative: open recurring + one-time included; filled/overdue/today excluded; stair-step from anchor (D-01, D-02, D-03, D-04, D-06)
    - describe forecast FX: locfRateAsOf(..., today); missing → exclude + isPartialForecast; includedSlotCount 0 → empty points (D-13, D-14, D-08, D-16)
    - describe INISO-01: net-worth.ts / historical-series.ts ban income|nw-forecast imports; past buildNetWorthSeries golden identity (ISO-01, D-17, D-18)
    - describe NetWorthHistoryChart forecast file-scan: ComposedChart, strokeDasharray, Прогноз, ReferenceLine (D-09..D-12) — expect RED until Plan 03 chrome complete; tracer may partially satisfy
  </behavior>
  <action>
    Per 17-VALIDATION Wave 0 and RESEARCH Validation Architecture: create src/lib/nw-forecast.test.ts importing forecastHorizonEnd + buildNetWorthForecastSeries from ./nw-forecast (file may not exist yet — RED). Mirror historical-series.test fixture style with BigInt minors + RATE_SCALE_E8. Create src/lib/iniso.test.ts mirroring disol.test.ts file-scan walls plus past-series identity property stub (D-17, D-18). Create src/components/dashboard/nw-forecast-ui.test.ts file-scan describe reading NetWorthHistoryChart.tsx / DashboardChartsShell.tsx for forecast chrome tokens. Do not implement production modules yet — domain + INISO describes must fail until later tasks. Zero new packages.
  </action>
  <verify>
    <automated>npx vitest run -t "forecast" src/lib/nw-forecast.test.ts; test $? -ne 0</automated>
    <fails_when>vitest exits 0 (Wave 0 must be RED) or forecast describe/tests missing</fails_when>
  </verify>
  <acceptance_criteria>
    - Wave 0 nw-forecast + INISO + chart describes exist and fail before implementation (FCST-01, ISO-01)
  </acceptance_criteria>
  <done>RED scaffolds committed; ready for tracer GREEN.</done>
</task>

<task type="tracer" tdd="true">
  <name>End-to-end open recurring slot → nw-forecast → Капитал merge</name>
  <files>src/lib/nw-forecast.ts, src/lib/nw-forecast.test.ts, src/app/page.tsx, src/components/dashboard/DashboardChartsShell.tsx, src/components/dashboard/NetWorthHistoryChart.tsx</files>
  <read_first>
    .planning/phases/17-nw-forecast-overlay-isolation/17-CONTEXT.md
    .planning/phases/17-nw-forecast-overlay-isolation/17-PATTERNS.md
    .planning/phases/17-nw-forecast-overlay-isolation/17-RESEARCH.md
    src/lib/historical-series.ts
    src/lib/income.ts
    src/lib/locf.ts
    src/lib/money.ts
    src/lib/dates.ts
    src/app/page.tsx
    src/components/dashboard/DashboardChartsShell.tsx
    src/components/dashboard/NetWorthHistoryChart.tsx
  </read_first>
  <behavior>
    - forecastHorizonEnd(preset, today) mirrors lookback magnitudes with all→1y cap (D-05)
    - buildNetWorthForecastSeries: cumulative from anchorPrimaryMinor; sparse today∪pay∪horizonEnd; FX via locfRateAsOf(..., today) (D-04, D-06, D-13)
    - Tracer happy path: one open primary-currency recurring slot with plannedAsOf &gt; today produces forecast points and showForecast true
    - page loads RecurringIncome/OneTimeIncome + actuals; passes serializable payload + anchor from computeNetWorthRows; does not pass income into buildNetWorthSeries (D-18, ISO-01)
    - DashboardChartsShell useMemo recomputes forecast on range change (discretion pick — not page preload-slice) (D-05)
    - NetWorthHistoryChart switches to ComposedChart with dashed Line dataKey forecast name Прогноз for tracer path (D-09, D-10) — tooltip/ReferenceLine/banner may complete in Plan 03
  </behavior>
  <action>
    Create src/lib/nw-forecast.ts per RESEARCH Pattern 1 / PATTERNS: export forecastHorizonEnd and buildNetWorthForecastSeries accepting ForecastSlot[] (no internal listAllInRange — membership at boundary). Import dates/locf/money only — ban prisma, BalanceSnapshot, net-worth, historical-series (D-18). Implement cumulative stair-step + FX@today exclude counts (D-04, D-13, D-14). Wire page.tsx: load income defs+actuals for max 1y window; compute anchorPrimaryMinor from existing computeNetWorthRows hero path; pass payload to DashboardChartsShell without feeding income into historical series (ISO-01). In shell: revive payload; listAllInRange(from=today+1d, to=forecastHorizonEnd); filter open slots (plannedAsOf &gt; today; one-time actual null; recurring via occurrenceKeyString filled-set) enrich currency from defs (D-01, D-02, D-03); call buildNetWorthForecastSeries; merge fact+forecast rows so X spans past+future when series shown (D-07). Switch NetWorthHistoryChart to ComposedChart + Area stacks + dashed Line strokeDasharray 5 5 stroke var(--muted-foreground) type stepAfter when showForecast (D-09; UI-SPEC). Keep net-worth.ts / historical-series.ts untouched mathematically. Green tracer happy-path nw-forecast tests; domain edge cases expand in Task 3.
  </action>
  <verify>
    <automated>npx vitest run src/lib/nw-forecast.test.ts &amp;&amp; grep -q 'forecastHorizonEnd' src/lib/nw-forecast.ts &amp;&amp; grep -q 'buildNetWorthForecastSeries' src/lib/nw-forecast.ts &amp;&amp; grep -q 'buildNetWorthForecastSeries\|forecastHorizonEnd' src/components/dashboard/DashboardChartsShell.tsx &amp;&amp; grep -q 'ComposedChart' src/components/dashboard/NetWorthHistoryChart.tsx &amp;&amp; grep -q 'Прогноз' src/components/dashboard/NetWorthHistoryChart.tsx</automated>
    <fails_when>vitest non-zero, nw-forecast exports missing, shell missing horizon/builder wiring, or ComposedChart/Прогноз absent from chart</fails_when>
  </verify>
  <acceptance_criteria>
    - Happy path open recurring slot → cumulative overlay points → dashed Прогноз Line on `/` chart path (FCST-01, D-01, D-04, D-09)
    - Income never enters computeNetWorthRows / buildNetWorthSeries call sites (ISO-01, D-18)
  </acceptance_criteria>
  <done>Tracer path green end-to-end through lib + page + shell + chart Line.</done>
</task>

<task type="auto" tdd="true">
  <name>Domain expansion — one-time membership, horizon, FX exclude/hide</name>
  <reversibility rating="costly">D-01 recurring+one-time membership locks REQ text, tests, and chart inputs; undo needs coordinated docs+suite+call-site changes.</reversibility>
  <files>src/lib/nw-forecast.ts, src/lib/nw-forecast.test.ts, src/components/dashboard/DashboardChartsShell.tsx</files>
  <read_first>
    src/lib/nw-forecast.ts
    src/lib/nw-forecast.test.ts
    src/lib/income.ts
    .planning/phases/17-nw-forecast-overlay-isolation/17-RESEARCH.md
    .planning/phases/17-nw-forecast-overlay-isolation/17-CONTEXT.md
  </read_first>
  <behavior>
    - Future open one-time slots included alongside recurring (D-01) — supersedes stale REQ recurring-only text
    - Filled recurring (actual key set) and one-time with actual excluded; overdue past unfilled ignored (D-02)
    - plannedAsOf === today excluded; strict &gt; today (D-03)
    - All four forecastHorizonEnd mappings green (D-05)
    - Missing FX@today → excludedMissingFxCount++ / isPartialForecast; includedSlotCount===0 → points=[] hide series (D-08, D-14, D-16)
    - Sparse sample dates only; cumulative stair-step totals match fixture majors (D-04, D-06)
  </behavior>
  <action>
    Finish buildNetWorthForecastSeries + shell open-slot filter so all Wave 0 forecast domain cases pass (D-01..D-08, D-13..D-16). Ensure recurring filled-set uses occurrenceKeyString against RecurringIncomeActual keys (RESEARCH Pitfall 3). Enrich ForecastSlot currency from def maps. Keep builder free of listAllInRange. Confirm isPartialForecast true when any slot excluded for FX even if some remain convertible. Hide series when includedSlotCount===0 (caller showForecast false). Do not implement partial banner UI or full tooltip/ReferenceLine polish here — Plan 03. Do not green INISO past-series suite here — Plan 02.
  </action>
  <verify>
    <automated>npx vitest run src/lib/nw-forecast.test.ts &amp;&amp; grep -q 'isPartialForecast' src/lib/nw-forecast.ts &amp;&amp; grep -q 'excludedMissingFxCount' src/lib/nw-forecast.ts &amp;&amp; grep -q 'forecastHorizonEnd' src/lib/nw-forecast.ts</automated>
    <fails_when>vitest non-zero or isPartialForecast/excludedMissingFxCount/forecastHorizonEnd missing from nw-forecast.ts</fails_when>
  </verify>
  <acceptance_criteria>
    - All nw-forecast Wave 0 domain cases green including one-time membership and horizon mirror (FCST-01, D-01, D-05)
    - FX exclude + empty-hide rules green (D-14, D-08, D-16)
  </acceptance_criteria>
  <done>Forecast domain complete for Plan 02 INISO + Plan 03 chrome.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Prisma income/FxRate → RSC page | Trusted DB; forecast is read-path only |
| RSC → client shell props | Serializable minors/strings; client revives BigInt |
| nw-forecast.ts pure lib | Must not import prisma / net-worth / historical-series |
| Chart overlay UX | Forecast must not read as measured past NW |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-17-01 | Tampering | income → BalanceSnapshot / NW history | high | mitigate | No income args to computeNetWorthRows/buildNetWorthSeries; INISO Plan 02; actions write-gate unchanged |
| T-17-02 | Tampering | nw-forecast FX conversion | high | mitigate | locfRateAsOf(..., today) only; null → exclude + isPartialForecast; never coerce 0/1 rates; vitest FX cases |
| T-17-03 | Elevation of privilege (UX) | NetWorthHistoryChart Line | medium | mitigate | Dashed Line + «Прогноз» name; solid Area reserved for fact stacks (D-09/D-10); Plan 03 ReferenceLine hinge |
| T-17-04 | Tampering | nw-forecast.ts coupling | high | mitigate | Import wall: money/locf/dates only; file-scan in INISO |
| T-17-05 | Info disclosure | new endpoints | low | accept | No new public API; RSC same local trust model |
| T-17-SC | Tampering | npm installs | high | mitigate | Zero new packages this phase |

</threat_model>

<verification>
npx vitest run src/lib/nw-forecast.test.ts
</verification>

<success_criteria>
forecastHorizonEnd + buildNetWorthForecastSeries green for membership/cumulative/horizon/FX; page+shell merge open slots into dashed overlay; historical path stays income-free.
</success_criteria>

<output>
Create `.planning/phases/17-nw-forecast-overlay-isolation/17-01-SUMMARY.md` when done
</output>
