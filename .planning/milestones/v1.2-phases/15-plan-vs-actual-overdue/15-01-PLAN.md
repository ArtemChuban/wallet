---
phase: 15-plan-vs-actual-overdue
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/lib/validations/income.ts
  - src/lib/validations/income.test.ts
  - src/app/income/actions.ts
  - src/app/income/actions.test.ts
  - src/lib/income.ts
  - src/lib/income.test.ts
autonomous: true
requirements:
  - ACT-01
  - ACT-03
estimate:
  tokens: 27500
  raw_tokens: 55000
  tasks: 3
  confidence: high
must_haves:
  truths:
    - "Zod/action rejects non-positive actualAmountMajor; upsert writes actual columns only; plan amount/date on definition unchanged (ACT-01, D-19)"
    - "actual amount round-trips via currency.scale + parseMajorToMinor; delta = actualMinor - planMinor with no FX (ACT-01 precision, ACT-03, D-13, D-14)"
    - "upsertRecurringIncomeActual uses compound unique recurringIncomeId_plannedAsOf (slot key); re-record updates amount/actualAsOf/note only (ACT-01, D-04)"
    - "actualAsOf accepts any valid YYYY-MM-DD including future — no repayment-style today upper bound (D-19)"
    - "incomeVarianceMinor(actual, plan) returns actual - plan; RU phrases больше плана / меньше плана / как план for >, <, 0 (ACT-03, D-14)"
    - "Income actual actions revalidatePath /income only; no BalanceSnapshot / net-worth imports (ISO light)"
  prohibitions:
    - statement: MUST NOT mutate recurring/one-time definition plan amount or plan date columns inside actual upsert/delete actions
      requirement_id: ACT-01
      category: integrity
    - statement: MUST NOT reject future actualAsOf the way debt repayment rejects future asOfDate
      requirement_id: ACT-01
      category: boundary
    - statement: MUST NOT convert variance delta through primary FX in this phase
      requirement_id: ACT-03
      category: absence
    - statement: MUST NOT install new npm packages
      requirement_id: ACT-01
      category: safety
    - statement: MUST NOT write BalanceSnapshot or revalidatePath dashboard root from income actual actions
      requirement_id: ACT-01
      category: isolation
  artifacts:
    - path: src/lib/validations/income.ts
      provides: upsert/delete actual Zod schemas (recurring + one-time)
    - path: src/lib/validations/income.test.ts
      provides: ACT-01 boundary/precision cases for actualAmountMajor and actualAsOf
    - path: src/app/income/actions.ts
      provides: upsertRecurringIncomeActual Server Action (tracer path)
    - path: src/app/income/actions.test.ts
      provides: upsert recurring actual mocks + isolation file-scan kept green
    - path: src/lib/income.ts
      provides: incomeVarianceMinor + incomeVariancePhrase pure helpers
    - path: src/lib/income.test.ts
      provides: variance formula + RU phrase branch tests
  key_links:
    - from: upsertRecurringIncomeActualSchema
      to: upsertRecurringIncomeActual
      via: Zod safeParse then parseMajorToMinor from parent currency.scale
      pattern: "upsertRecurringIncomeActual"
    - from: prisma.recurringIncomeActual.upsert
      to: compound unique recurringIncomeId_plannedAsOf
      via: plannedAsOf slot key not actualAsOf
      pattern: "recurringIncomeId_plannedAsOf"
    - from: incomeVarianceMinor
      to: ACT-03 inline/dialog chrome (Plan 03)
      via: actualMinor - plannedMinor bigint
      pattern: "incomeVarianceMinor"
---

## Specless probe fallback

Coverage acceptances lifted into must_haves.truths (EDGE/PROHIB absent — descriptor-less prohibitions authored above).

| Req | Category | Acceptance |
|-----|----------|------------|
| ACT-01 | boundary | Zod/action rejects non-positive actual; upsert writes actual columns only; plan unchanged |
| ACT-01 | precision | Round-trip via currency scale; delta = actualMinor - planMinor no FX |
| ACT-03 | state (formula) | Pure variance helper + RU phrases ready for UI Plan 03 |

<objective>
Ship ACT-01 write-path tracer for recurring actual upsert plus complete Zod actual schemas and pure variance helpers (D-04, D-13, D-14, D-19).

Purpose: Prove compound-unique upsert + positive-amount validation before page join and fact UI.
Output: Working upsertRecurringIncomeActual, all actual Zod schemas tested, incomeVarianceMinor/phrase green.
</objective>

<execution_context>
@/home/artem/Documents/wallet/.cursor/gsd-core/workflows/execute-plan.md
@/home/artem/Documents/wallet/.cursor/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/15-plan-vs-actual-overdue/15-CONTEXT.md
@.planning/phases/15-plan-vs-actual-overdue/15-RESEARCH.md
@.planning/phases/15-plan-vs-actual-overdue/15-PATTERNS.md
@.planning/phases/15-plan-vs-actual-overdue/15-VALIDATION.md
@.planning/phases/15-plan-vs-actual-overdue/15-UI-SPEC.md
@src/app/income/actions.ts
@src/app/income/actions.test.ts
@src/app/accounts/actions.ts
@src/app/debts/actions.ts
@src/lib/validations/income.ts
@src/lib/validations/income.test.ts
@src/lib/validations/debts.ts
@src/lib/income.ts
@src/lib/income.test.ts
@src/lib/money.ts
</context>

<assumptions>
## Planner assumptions

- A1: No Prisma schema changes — RecurringIncomeActual / OneTimeIncomeActual already exist; schema push gate N/A.
- A2: Past recurring actual edit UI omitted this phase (CONTEXT discretion / RESEARCH).
- A3: List-level Δ for recurring filled months not required under D-05/D-07; ACT-03 via one-time + dialog (Plan 03).
- A4: Action names upsertRecurringIncomeActual / upsertOneTimeIncomeActual / deleteRecurringIncomeActual / deleteOneTimeIncomeActual (RESEARCH discretion).
- Concurrent writes: SQLite last-write-wins; no optimistic locking.
- Authority: local single-user; Server Actions unauthenticated.
</assumptions>

## Artifacts this phase produces

- Actual Zod schemas + tests (this plan)
- upsertRecurringIncomeActual (this plan); remaining actual actions (Plan 02)
- incomeVarianceMinor / incomeVariancePhrase (this plan)
- Page join + overdue props (Plan 02)
- IncomeFactDialog + list overdue/variance chrome + --warning tokens (Plan 03)

<tasks>

<task type="tracer" tdd="true">
  <name>End-to-end upsertRecurringIncomeActual — Zod + action + isolation</name>
  <files>src/lib/validations/income.ts, src/lib/validations/income.test.ts, src/app/income/actions.ts, src/app/income/actions.test.ts</files>
  <read_first>
    .planning/phases/15-plan-vs-actual-overdue/15-CONTEXT.md
    .planning/phases/15-plan-vs-actual-overdue/15-RESEARCH.md
    .planning/phases/15-plan-vs-actual-overdue/15-PATTERNS.md
    src/app/accounts/actions.ts
    src/app/income/actions.ts
    src/app/income/actions.test.ts
    src/lib/validations/debts.ts
    src/lib/validations/income.ts
    prisma/schema.prisma
  </read_first>
  <behavior>
    - upsertRecurringIncomeActualSchema rejects actualAmountMajor of 0, negative, empty; accepts positive major string (ACT-01, D-19)
    - upsertRecurringIncomeActualSchema requires recurringIncomeId, plannedAsOf YYYY-MM-DD, actualAsOf YYYY-MM-DD, optional note (D-02)
    - upsertRecurringIncomeActual loads parent, parses minor via currency.scale, upserts on recurringIncomeId_plannedAsOf, never updates definition plan columns (ACT-01, D-04)
    - Future actualAsOf accepted (D-19) — do not copy debt repayment future-date rejection
    - Isolation scan still forbids BalanceSnapshot / net-worth imports / dashboard-root revalidate from actions.ts
  </behavior>
  <action>
    Add upsertRecurringIncomeActualSchema (.strict) with fields recurringIncomeId, plannedAsOf, actualAmountMajor, actualAsOf, optional note — mirror createRepaymentSchema shape but use actualAmountMajor/actualAsOf names and isStrictlyPositiveMajor refine (D-02, D-19, ACT-01). Extend IncomeActionState error keys for actualAmountMajor, actualAsOf, plannedAsOf, recurringIncomeId. Implement upsertRecurringIncomeActual: ensureSqlitePragmas; Zod safeParse; findUnique recurring parent (fail closed if missing); resolve scale via parent currencyCode + parseMajorToMinor (reuse resolvePlannedMinor pattern or sibling helper for actualAmountMajor); prisma.recurringIncomeActual.upsert where recurringIncomeId_plannedAsOf with update/create of amountMinor, actualAsOf, note only — plannedAsOf is slot key only (D-04, ACT-01). On success revalidatePath("/income") only. Extend actions.test.ts with Zod failure + mocked upsert success; keep existing isolation describe green. Zero new packages. No UI this task.
  </action>
  <verify>
    <automated>npx vitest run src/lib/validations/income.test.ts src/app/income/actions.test.ts &amp;&amp; grep -q 'upsertRecurringIncomeActual' src/app/income/actions.ts &amp;&amp; grep -q 'recurringIncomeId_plannedAsOf' src/app/income/actions.ts &amp;&amp; grep -q 'revalidatePath("/income")' src/app/income/actions.ts</automated>
    <fails_when>vitest non-zero, upsertRecurringIncomeActual or compound unique key missing, or isolation describe fails</fails_when>
  </verify>
  <acceptance_criteria>
    - upsertRecurringIncomeActual exported and uses compound unique on plannedAsOf slot (D-04)
    - Non-positive actualAmountMajor rejected by Zod (ACT-01, D-19)
    - Isolation file-scan still green (no BalanceSnapshot / net-worth / dashboard-root revalidate)
  </acceptance_criteria>
  <done>Recurring actual upsert path works end-to-end at action layer with tests green.</done>
</task>

<task type="auto" tdd="true">
  <name>Zod one-time upsert + delete actual schemas (ACT-01)</name>
  <files>src/lib/validations/income.ts, src/lib/validations/income.test.ts</files>
  <read_first>
    src/lib/validations/income.ts
    src/lib/validations/income.test.ts
    src/lib/validations/debts.ts
    .planning/phases/15-plan-vs-actual-overdue/15-RESEARCH.md
  </read_first>
  <behavior>
    - upsertOneTimeIncomeActualSchema same money/date rules as recurring; fields oneTimeIncomeId, plannedAsOf, actualAmountMajor, actualAsOf, note? (ACT-01, D-02, D-19)
    - deleteRecurringIncomeActualSchema / deleteOneTimeIncomeActualSchema accept positive int id like deleteRepaymentSchema (D-04)
    - Empty/zero/negative actualAmountMajor rejected; invalid date rejected with UI-SPEC-aligned messages where schemas carry messages
  </behavior>
  <action>
    Add upsertOneTimeIncomeActualSchema and deleteRecurringIncomeActualSchema / deleteOneTimeIncomeActualSchema per RESEARCH discretion names (ACT-01, D-02, D-04, D-19). Mirror positive-major refine and asOfDateSchema reuse from existing income/debts validations. Export inferred types. Extend income.test.ts with boundary cases for actualAmountMajor and id coerce. Do not implement remaining actions yet (Plan 02).
  </action>
  <verify>
    <automated>npx vitest run src/lib/validations/income.test.ts &amp;&amp; grep -q 'upsertOneTimeIncomeActualSchema' src/lib/validations/income.ts &amp;&amp; grep -q 'deleteRecurringIncomeActualSchema' src/lib/validations/income.ts &amp;&amp; grep -q 'deleteOneTimeIncomeActualSchema' src/lib/validations/income.ts</automated>
    <fails_when>vitest non-zero or any of the three schema exports missing</fails_when>
  </verify>
  <acceptance_criteria>
    - All four actual Zod schemas exist (recurring upsert from tracer + one-time upsert + two deletes)
    - Tests cover non-positive actualAmountMajor rejection (ACT-01)
  </acceptance_criteria>
  <done>Full actual Zod surface ready for Plan 02 actions.</done>
</task>

<task type="auto" tdd="true">
  <name>incomeVarianceMinor + RU phrase helpers (ACT-03, D-14)</name>
  <files>src/lib/income.ts, src/lib/income.test.ts</files>
  <read_first>
    src/lib/income.ts
    src/lib/income.test.ts
    .planning/phases/15-plan-vs-actual-overdue/15-CONTEXT.md
    .planning/phases/15-plan-vs-actual-overdue/15-UI-SPEC.md
  </read_first>
  <behavior>
    - incomeVarianceMinor(actual, plan) returns actual - plan as bigint (D-14, ACT-03)
    - incomeVariancePhrase(delta): positive → больше плана; negative → меньше плана; zero → как план (D-14)
    - No FX conversion in helpers (D-13)
  </behavior>
  <action>
    Add pure helpers incomeVarianceMinor and incomeVariancePhrase next to isIncomeOverdue in src/lib/income.ts (D-13, D-14, ACT-03). No Prisma imports. Cover three phrase branches + signed delta in income.test.ts. Helpers are source-currency minors only — no primary conversion.
  </action>
  <verify>
    <automated>npx vitest run src/lib/income.test.ts &amp;&amp; grep -q 'incomeVarianceMinor' src/lib/income.ts &amp;&amp; grep -q 'больше плана' src/lib/income.ts &amp;&amp; grep -q 'меньше плана' src/lib/income.ts &amp;&amp; grep -q 'как план' src/lib/income.ts</automated>
    <fails_when>vitest non-zero or helper/phrase strings missing from income.ts</fails_when>
  </verify>
  <acceptance_criteria>
    - Variance formula is actual − plan in source minor (D-14)
    - Three RU phrases match UI-SPEC Copywriting Contract (ACT-03)
  </acceptance_criteria>
  <done>Pure variance helpers green for Plan 03 chrome.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Browser/FormData → Server Actions | Untrusted actualAmountMajor, actualAsOf, plannedAsOf, parent ids |
| Actual write → definition tables | Must not cross into plan column mutation |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-15-01 | Tampering | upsertRecurringIncomeActual / Zod | high | mitigate | Zod .strict + isStrictlyPositiveMajor; parseMajorToMinor + currency.scale; reject overflow |
| T-15-02 | Tampering | parent id spoof | medium | mitigate | findUnique parent before upsert; fail closed |
| T-15-04 | Tampering | NW / BalanceSnapshot coupling | high | mitigate | No BalanceSnapshot imports; revalidatePath("/income") only; keep isolation file-scan |
| T-15-05 | Tampering | note XSS | low | mitigate | React text binding later; Zod optional note max 500 (existing pattern) |
| T-15-SC | Tampering | npm installs | high | mitigate | Zero new packages this phase |
</threat_model>

<verification>
npx vitest run src/lib/validations/income.test.ts src/app/income/actions.test.ts src/lib/income.test.ts
</verification>

<success_criteria>
Recurring actual upsert works with positive-amount Zod; variance helpers encode ACT-03 formula; isolation scan green; one-time/delete schemas ready for Plan 02.
</success_criteria>

<output>
Create `.planning/phases/15-plan-vs-actual-overdue/15-01-SUMMARY.md` when done
</output>
