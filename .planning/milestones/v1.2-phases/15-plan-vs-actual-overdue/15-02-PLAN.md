---
phase: 15-plan-vs-actual-overdue
plan: 02
type: execute
wave: 2
depends_on:
  - "15-01"
files_modified:
  - src/app/income/actions.ts
  - src/app/income/actions.test.ts
  - src/app/income/page.tsx
  - src/components/income/IncomeFormDialog.tsx
  - src/components/income/IncomeList.tsx
autonomous: true
requirements:
  - ACT-01
  - ACT-02
estimate:
  tokens: 25000
  raw_tokens: 50000
  tasks: 2
  confidence: high
must_haves:
  truths:
    - "upsertOneTimeIncomeActual + deleteRecurringIncomeActual + deleteOneTimeIncomeActual persist/remove facts; plan definition columns unchanged (ACT-01, D-04)"
    - "One-time upsert asserts plannedAsOf matches definition.plannedAsOf server-side (ACT-01 integrity)"
    - "Page loads full actual rows for recurring and one-time; maps hasActual / overdue via isIncomeOverdue(nextPlannedAsOf, hasActual, Moscow today) (ACT-02, D-05, D-06)"
    - "Recurring hasActual on list row is false for next-open slot (FIFO); one-time hasActual true when actual exists on plan key (D-05, D-07)"
    - "Sort within Person group stays nearest nextPlannedAsOf ascending — no overdue-first pass (D-11)"
    - "IncomeRow carries optional actualId, actualAmountMinor, actualAsOf, hasActual for UI chrome (ACT-02)"
    - "Definition «Изменить» path remains available on rows (D-20) — fact props do not remove IncomeFormDialog"
  prohibitions:
    - statement: MUST NOT invent overdue boolean in JSX — only isIncomeOverdue with injected Moscow today
      requirement_id: ACT-02
      category: integrity
    - statement: MUST NOT expand list to multi-slot rows or keep filled recurring months as residual rows
      requirement_id: ACT-02
      category: absence
    - statement: MUST NOT write BalanceSnapshot or revalidatePath dashboard root from new actual actions
      requirement_id: ACT-01
      category: isolation
    - statement: MUST NOT use actualAsOf as the Prisma compound unique key
      requirement_id: ACT-01
      category: integrity
  artifacts:
    - path: src/app/income/actions.ts
      provides: upsertOneTimeIncomeActual + deleteRecurringIncomeActual + deleteOneTimeIncomeActual
    - path: src/app/income/actions.test.ts
      provides: mocks for one-time upsert/deletes + isolation still green
    - path: src/app/income/page.tsx
      provides: actual field join + overdue/hasActual/actual* props mapped into IncomeRow
    - path: src/components/income/IncomeFormDialog.tsx
      provides: IncomeRow type extended with fact/overdue fields
  key_links:
    - from: page.tsx actuals include
      to: IncomeRow.hasActual / overdue
      via: isIncomeOverdue(nextPlannedAsOf, hasActual, today)
      pattern: "isIncomeOverdue"
    - from: nextOpenPlannedAsOf
      to: recurring next-open join
      via: match actual.plannedAsOf === nextPlannedAsOf (usually none)
      pattern: "nextOpenPlannedAsOf"
    - from: upsertOneTimeIncomeActual
      to: oneTimeIncomeId_plannedAsOf
      via: server assert plannedAsOf === definition.plannedAsOf
      pattern: "oneTimeIncomeId_plannedAsOf"
---

## Specless probe fallback

| Req | Category | Acceptance |
|-----|----------|------------|
| ACT-01 | boundary/state | Remaining actual CRUD; plan columns untouched |
| ACT-02 | state | isIncomeOverdue true props ready for «заполни» chrome (Plan 03) |

<objective>
Complete ACT-01 actual Server Actions (one-time upsert + deletes) and RSC page join so list rows carry overdue/hasActual/actual fields (D-04, D-05, D-06, D-07, D-11, D-20).

Purpose: Data plumbing for overdue chrome and variance without shipping UI chrome yet.
Output: Four actual actions total; page maps serializable fact props; IncomeRow type extended.
</objective>

<execution_context>
@/home/artem/Documents/wallet/.cursor/gsd-core/workflows/execute-plan.md
@/home/artem/Documents/wallet/.cursor/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/15-plan-vs-actual-overdue/15-CONTEXT.md
@.planning/phases/15-plan-vs-actual-overdue/15-RESEARCH.md
@.planning/phases/15-plan-vs-actual-overdue/15-PATTERNS.md
@.planning/phases/15-plan-vs-actual-overdue/15-01-PLAN.md
@src/app/income/actions.ts
@src/app/income/actions.test.ts
@src/app/income/page.tsx
@src/components/income/IncomeFormDialog.tsx
@src/components/income/IncomeList.tsx
@src/lib/income.ts
@src/lib/validations/income.ts
</context>

<tasks>

<task type="auto" tdd="true">
  <name>One-time upsert + delete actual actions (ACT-01, D-04)</name>
  <files>src/app/income/actions.ts, src/app/income/actions.test.ts</files>
  <read_first>
    src/app/income/actions.ts
    src/app/income/actions.test.ts
    src/app/debts/actions.ts
    src/lib/validations/income.ts
    .planning/phases/15-plan-vs-actual-overdue/15-RESEARCH.md
    .planning/phases/15-plan-vs-actual-overdue/15-PATTERNS.md
  </read_first>
  <behavior>
    - upsertOneTimeIncomeActual upserts on oneTimeIncomeId_plannedAsOf; asserts plannedAsOf === definition.plannedAsOf (ACT-01, D-04)
    - deleteRecurringIncomeActual / deleteOneTimeIncomeActual delete by id; success revalidatePath /income only (D-04)
    - Future actualAsOf allowed on one-time upsert (D-19)
    - Isolation describe remains green after additions
  </behavior>
  <action>
    Implement upsertOneTimeIncomeActual, deleteRecurringIncomeActual, deleteOneTimeIncomeActual using Plan 01 Zod schemas (ACT-01, D-04, D-19). Mirror deleteRepayment delete-by-id pattern. For one-time upsert: findUnique parent; reject if client plannedAsOf !== definition.plannedAsOf; upsert amountMinor/actualAsOf/note only — never mutate definition plan columns. Do not copy debt repayment future-date ban. Every success path revalidatePath("/income") only. Extend actions.test.ts mocks; keep isolation file-scan. No UI dialog yet.
  </action>
  <verify>
    <automated>npx vitest run src/app/income/actions.test.ts &amp;&amp; grep -q 'upsertOneTimeIncomeActual' src/app/income/actions.ts &amp;&amp; grep -q 'deleteRecurringIncomeActual' src/app/income/actions.ts &amp;&amp; grep -q 'deleteOneTimeIncomeActual' src/app/income/actions.ts &amp;&amp; grep -q 'oneTimeIncomeId_plannedAsOf' src/app/income/actions.ts</automated>
    <fails_when>vitest non-zero or any of the three action exports / compound key missing</fails_when>
  </verify>
  <acceptance_criteria>
    - All four actual actions exist (recurring upsert from 15-01 + three this task) (D-04)
    - One-time upsert enforces slot plannedAsOf === definition.plannedAsOf (ACT-01)
    - Isolation scan still green
  </acceptance_criteria>
  <done>Full actual Server Action surface complete.</done>
</task>

<task type="auto">
  <name>Page join actuals + IncomeRow overdue/hasActual props (ACT-02, D-05)</name>
  <files>src/app/income/page.tsx, src/components/income/IncomeFormDialog.tsx, src/components/income/IncomeList.tsx</files>
  <read_first>
    src/app/income/page.tsx
    src/components/income/IncomeFormDialog.tsx
    src/components/income/IncomeList.tsx
    src/lib/income.ts
    .planning/phases/15-plan-vs-actual-overdue/15-RESEARCH.md
    .planning/phases/15-plan-vs-actual-overdue/15-PATTERNS.md
    .planning/phases/15-plan-vs-actual-overdue/15-CONTEXT.md
  </read_first>
  <behavior>
    - Recurring and one-time includes select full actual fields id, plannedAsOf, amountMinor, actualAsOf, note (Pitfall 1 fix)
    - hasActual: one-time true when actual on plan key; recurring next-open usually false (D-05, D-07)
    - overdue = isIncomeOverdue(nextPlannedAsOf, hasActual, today) with Moscow today already on page (ACT-02)
    - Serialize BigInt actualAmountMinor as string like plannedAmountMinor
    - Sort unchanged nearest nextPlannedAsOf within person (D-11)
    - IncomeFormDialog IncomeRow type gains hasActual, overdue, optional actualId/actualAmountMinor/actualAsOf; list still compiles (D-20 coexistence)
  </behavior>
  <action>
    Fix RESEARCH Pitfall 1: expand recurring actuals select beyond id keys; add oneTimeIncomes.actuals include with same fields (ACT-02). After nextOpenPlannedAsOf, join matching actual for recurring next-open key (expect none when unfilled); for one-time join by plannedAsOf. Set hasActual, overdue via isIncomeOverdue (import from @/lib/income), and optional actual* fields on each IncomeRow (D-05, D-06, D-07, D-11). Keep Person-group sort by nextPlannedAsOf. Extend IncomeRow type in IncomeFormDialog.tsx; thread props through IncomeList without adding fact CTA chrome yet (Plan 03) — definition IncomeFormDialog «Изменить» stays (D-20). Do not add IncomeFactDialog this task.
  </action>
  <verify>
    <automated>npx vitest run src/lib/income.test.ts src/app/income/actions.test.ts &amp;&amp; grep -q 'isIncomeOverdue' src/app/income/page.tsx &amp;&amp; grep -q 'hasActual' src/app/income/page.tsx &amp;&amp; grep -q 'hasActual' src/components/income/IncomeFormDialog.tsx &amp;&amp; grep -q 'amountMinor' src/app/income/page.tsx</automated>
    <fails_when>vitest non-zero, page missing isIncomeOverdue/hasActual join, or IncomeRow missing hasActual</fails_when>
  </verify>
  <acceptance_criteria>
    - page.tsx loads actual amount/date/id for both kinds (ACT-02)
    - overdue/hasActual computed with isIncomeOverdue + Moscow today (D-05, ACT-02)
    - Definition edit dialog type still accepts IncomeRow (D-20)
  </acceptance_criteria>
  <done>RSC props ready for Plan 03 overdue/variance chrome.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| FormData → one-time upsert | Client may spoof plannedAsOf vs definition |
| RSC → client props | Serialized actual amounts/ids cross render boundary |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-15-02 | Tampering | upsertOneTime / parent id | medium | mitigate | findUnique parent; fail closed |
| T-15-03 | Tampering | one-time plannedAsOf spoof | high | mitigate | Server assert plannedAsOf === definition.plannedAsOf before upsert |
| T-15-04 | Tampering | NW coupling in new actions | high | mitigate | Isolation file-scan; revalidatePath("/income") only |
| T-15-01 | Tampering | amount parse | high | mitigate | Zod + parseMajorToMinor + scale (Plan 01 patterns reused) |
| T-15-SC | Tampering | npm installs | high | mitigate | Zero new packages |
</threat_model>

<verification>
npx vitest run src/app/income/actions.test.ts src/lib/income.test.ts
</verification>

<success_criteria>
All four actual actions work; /income RSC joins actuals and exposes overdue/hasActual on rows for chrome; isolation green.
</success_criteria>

<output>
Create `.planning/phases/15-plan-vs-actual-overdue/15-02-SUMMARY.md` when done
</output>
