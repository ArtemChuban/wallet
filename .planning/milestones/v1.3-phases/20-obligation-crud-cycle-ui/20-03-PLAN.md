---
phase: 20-obligation-crud-cycle-ui
plan: 03
type: execute
wave: 3
depends_on:
  - 20-02
files_modified:
  - src/components/accounts/AccountList.tsx
  - src/components/accounts/CreditGraceDialog.tsx
  - src/components/accounts/CreditGraceAmountDialog.tsx
  - src/components/accounts/credit-grace-ui.test.ts
autonomous: true
requirements:
  - OBL-03
  - UX-01
  - CYCLE-02
estimate:
  tokens: 30000
  raw_tokens: 60000
  tasks: 2
  confidence: high
must_haves:
  truths:
    - "OPEN overdue rows use warning chrome + interest hint inside grace dialog (D-07, OBL-03, Phase 18 D-07)"
    - "AccountList Грейс button shows просрочено mark when any OPEN is overdue — never whole account row (D-07, OBL-03)"
    - "OPEN sort: overdue first, then nearest dueAsOf, then cycleStartAsOf (D-08)"
    - "Gap day: current null still lists overdue OPEN + next CTA (CYCLE-02 / Pitfall 7)"
    - "AccountList credit LOCF debt label is Задолженность (UX-01 / Phase 18 D-14)"
    - "Amount field shows UX-01 disclaimer; grace dialog never duplicates snapshot debt amount (D-16)"
    - "Clear schedule enabled only when OPEN count is zero; CLOSED may remain (D-14)"
  prohibitions:
    - statement: MUST NOT apply warning/destructive chrome to whole credit account row name or LOCF line for overdue
      requirement_id: OBL-03
      category: values
    - statement: MUST NOT render snapshot debt amount inside grace dialog
      requirement_id: UX-01
      category: isolation
    - statement: MUST NOT autofill 21 or 15 into empty DOM fields
      requirement_id: CYCLE-02
      category: values
  artifacts:
    - path: src/components/accounts/AccountList.tsx
      provides: overdue chip on Грейс only; Задолженность LOCF label
    - path: src/components/accounts/CreditGraceDialog.tsx
      provides: overdue row chrome + interest hint + D-08 sort + clear schedule control
    - path: src/components/accounts/CreditGraceAmountDialog.tsx
      provides: UX-01 amount disclaimer
    - path: src/components/accounts/credit-grace-ui.test.ts
      provides: full source-scan green for OBL-03 / UX-01 locks
  key_links:
    - from: isGraceOverdue
      to: OPEN row warning + Грейс button chip
      via: dueAsOf lt today and status OPEN (D-07, OBL-03)
      pattern: "isGraceOverdue"
    - from: amount field
      to: UX-01 disclaimer copy
      via: UI-SPEC verbatim under Платёж для беспроцентного (D-16)
      pattern: "Задолженность"
    - from: Очистить расписание
      to: updateGraceSchedule both-null
      via: disabled or server message when OPEN count gt 0 (D-14)
      pattern: "Очистить расписание"
---

<objective>
Ship overdue highlight (OBL-03 / D-07/D-08), UX-01 RU debt≠grace copy (D-16 + AccountList Задолженность), clear-schedule UI (D-14), and finish CYCLE-02 gap-day list behavior.

Purpose: Close Phase 20 success criteria 4–5 and remaining CONTEXT locks after CRUD shell.
Output: Green credit-grace-ui source-scan + targeted vitest; ROADMAP SC overdue + UX-01 true.
</objective>

<execution_context>
@/home/artem/Documents/wallet/.cursor/gsd-core/workflows/execute-plan.md
@/home/artem/Documents/wallet/.cursor/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/20-obligation-crud-cycle-ui/20-CONTEXT.md
@.planning/phases/20-obligation-crud-cycle-ui/20-UI-SPEC.md
@.planning/phases/20-obligation-crud-cycle-ui/20-PATTERNS.md
@.planning/phases/18-bank-contract-study-discuss-locks/18-CONTEXT.md
@src/components/accounts/AccountList.tsx
@src/components/accounts/CreditGraceDialog.tsx
@src/components/accounts/CreditGraceAmountDialog.tsx
@src/components/income/IncomeList.tsx
@src/lib/credit-grace.ts
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Overdue chrome + OPEN sort + gap-day list (OBL-03 / D-07 / D-08)</name>
  <files>src/components/accounts/CreditGraceDialog.tsx, src/components/accounts/AccountList.tsx, src/components/accounts/credit-grace-ui.test.ts</files>
  <read_first>
    src/lib/credit-grace.ts
    src/components/income/IncomeList.tsx
    src/components/accounts/CreditGraceDialog.tsx
    src/components/accounts/AccountList.tsx
    .planning/phases/20-obligation-crud-cycle-ui/20-UI-SPEC.md
  </read_first>
  <behavior>
    - OPEN + isGraceOverdue(dueAsOf, today) → bg-warning/15 text-warning-foreground row chrome + interest hint «Срок оплаты прошёл — банк может начислить проценты.» (D-07, OBL-03)
    - Any OPEN overdue → «просрочено» chip on Грейс button only; account name / LOCF line stay non-warning (D-07)
    - Sort OPEN: overdue first, then nearest dueAsOf, then cycleStartAsOf (D-08)
    - When resolveCurrentAndNext.current is null (gap day), still list overdue OPEN rows and next CTA (CYCLE-02)
  </behavior>
  <action>
    In CreditGraceDialog apply isGraceOverdue for OPEN rows: warning fill classes matching IncomeList overdue tone (not destructive); show UI-SPEC interest hint under/beside due (D-07, Phase 18 D-07). Sort OPEN per D-08. Ensure hybrid list always includes persisted overdue OPEN even when current candidate is null (Pitfall 7 / CYCLE-02). On AccountList, when any obligation is OPEN and overdue vs injected today, add short «просрочено» chip on the Грейс button only — never warning/destructive classes on account name or LOCF debt line (D-07). Unskip plan-03 overdue asserts in credit-grace-ui.test.ts (interest hint, просрочено near Грейс, no warning on account name) while keeping file green after prior waves. Do not wire Капитал forecast (Phase 21).
  </action>
  <verify>
    <automated>npx vitest run src/lib/credit-grace.test.ts src/components/accounts/credit-grace-ui.test.ts &amp;&amp; grep -q 'isGraceOverdue' src/components/accounts/CreditGraceDialog.tsx &amp;&amp; grep -q 'просрочено' src/components/accounts/AccountList.tsx &amp;&amp; grep -q 'Срок оплаты прошёл' src/components/accounts/CreditGraceDialog.tsx</automated>
    <fails_when>vitest non-zero, isGraceOverdue unused in grace dialog, просрочено absent from AccountList, or interest hint copy missing</fails_when>
  </verify>
  <done>OBL-03 overdue highlight on dialog rows + Грейс button mark only; D-08 sort + gap-day OPEN retained</done>
</task>

<task type="auto">
  <name>UX-01 copy + clear schedule + source-scan gate</name>
  <files>src/components/accounts/AccountList.tsx, src/components/accounts/CreditGraceAmountDialog.tsx, src/components/accounts/CreditGraceDialog.tsx, src/components/accounts/credit-grace-ui.test.ts</files>
  <read_first>
    src/components/accounts/AccountList.tsx
    src/components/accounts/CreditGraceAmountDialog.tsx
    src/components/accounts/CreditGraceDialog.tsx
    .planning/phases/20-obligation-crud-cycle-ui/20-UI-SPEC.md
    .planning/phases/18-bank-contract-study-discuss-locks/18-CONTEXT.md
  </read_first>
  <behavior>
    - FIAT_CREDIT LOCF debt chrome uses «Задолженность» verbatim (UX-01 / Phase 18 D-14)
    - Amount dialog always shows UX-01 disclaimer under «Платёж для беспроцентного» (D-16)
    - Grace dialog sources do not embed snapshot LOCF debt amount beside grace amounts
    - «Очистить расписание» enabled only when OPEN count is 0; submits both DOM null via updateGraceSchedule; CLOSED may remain; UI returns to empty-schedule hint (D-14)
    - Empty DOM inputs never preset to 21 or 15 (D-15)
    - credit-grace-ui.test.ts fully green for locked strings + confirm pattern + FIAT_CREDIT-only Грейс
  </behavior>
  <action>
    Rename FIAT_CREDIT LOCF debt label on AccountList to «Задолженность» (UX-01 / Phase 18 D-14); leave dashboard chart copy alone (RESEARCH A2 / Q2 RESOLVED). In CreditGraceAmountDialog place UI-SPEC disclaimer verbatim beside/under amount field (D-16). Ensure CreditGraceDialog never renders snapshot LOCF debt amount. Add «Очистить расписание» control calling updateGraceSchedule with both DOM cleared; disable or surface UI-SPEC error when OPEN count &gt; 0; CLOSED rows may remain collapsed (D-14 / Phase 19 D-14). Confirm empty DOM fields have no defaultValue 21/15 (D-15). Finish unskipping plan-03 credit-grace-ui.test.ts: Грейс, Задолженность, Платёж для беспроцентного, DestructiveConfirmStep import, disclaimer substring, no native confirm API in grace files, grace button gated to credit type — full file must exit 0. Do not modify AccountFormDialog schedule fields. Do not edit accounts actions.ts (reuse updateGraceSchedule only).
  </action>
  <verify>
    <automated>npx vitest run src/components/accounts/credit-grace-ui.test.ts src/app/accounts/actions.test.ts &amp;&amp; grep -q 'Задолженность' src/components/accounts/AccountList.tsx &amp;&amp; grep -q 'Это не' src/components/accounts/CreditGraceAmountDialog.tsx &amp;&amp; grep -q 'Очистить расписание' src/components/accounts/CreditGraceDialog.tsx &amp;&amp; ! grep -E 'defaultValue=\{?[&quot;']?21|&quot;15&quot;|defaultValue=.15' src/components/accounts/CreditGraceDialog.tsx</automated>
    <fails_when>vitest non-zero, Задолженность missing on AccountList, disclaimer missing in amount dialog, clear-schedule control absent, or DOM autofill 21/15 defaults present</fails_when>
  </verify>
  <acceptance_criteria>
    - UX-01: Задолженность on account card; disclaimer on amount; no snapshot debt inside grace dialog (D-16)
    - D-14 clear schedule gated on zero OPEN
    - D-15 no bank DOM autofill
    - OBL-03 + CYCLE-02 chrome complete with source-scan green
  </acceptance_criteria>
  <done>Phase 20 UI locks D-07/D-08/D-14/D-15/D-16 + OBL-03 + UX-01 green</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Client today string → overdue chrome | Calendar compare must use injected RSC today |
| Clear-schedule FormData → Account DOM | OPEN-count gate already in updateGraceSchedule |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-20-03 | Tampering | Snapshot write via clear schedule | high | mitigate | updateGraceSchedule already Account-DOM-only; no new snapshot calls |
| T-20-08 | Spoofing | Client-skewed today for overdue | medium | mitigate | Inject today from page (calendarDateToday server); isGraceOverdue pure compare |
| T-20-09 | Information Disclosure | Snapshot debt duplicated in grace UI | medium | mitigate | D-16 disclaimer + source-scan forbids debt amount in grace dialog |
| T-20-SC | Tampering | npm installs | high | mitigate | Zero new packages |
</threat_model>

<verification>
npx vitest run src/components/accounts/credit-grace-ui.test.ts src/lib/credit-grace.test.ts src/app/accounts/actions.test.ts; AccountList Задолженность + overdue chip; amount disclaimer; clear schedule control.
</verification>

<success_criteria>
OBL-03 overdue highlight; UX-01 debt≠grace copy; D-07/D-08/D-14/D-15/D-16; CYCLE-02 gap-day OPEN retained; full Wave 0 UI scan green.
</success_criteria>

<output>
Create `.planning/phases/20-obligation-crud-cycle-ui/20-03-SUMMARY.md` when done
</output>
