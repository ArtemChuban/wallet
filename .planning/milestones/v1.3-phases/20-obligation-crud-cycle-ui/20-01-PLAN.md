---
phase: 20-obligation-crud-cycle-ui
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/lib/validations/credit-grace.test.ts
  - src/app/accounts/actions.test.ts
  - src/components/accounts/credit-grace-ui.test.ts
  - src/components/ui/destructive-confirm-step.tsx
  - src/lib/credit-grace.ts
  - src/lib/credit-grace.test.ts
  - src/app/accounts/actions.ts
  - src/app/accounts/page.tsx
  - src/components/accounts/AccountList.tsx
  - src/components/accounts/CreditGraceDialog.tsx
  - src/components/accounts/CreditGraceAmountDialog.tsx
autonomous: true
requirements:
  - CYCLE-02
  - OBL-01
estimate:
  tokens: 50000
  raw_tokens: 100000
  tasks: 3
  confidence: high
must_haves:
  truths:
    - "Wave 0: credit-grace Zod suite green; actions/UI Wave 0 uses it.todo or describe.skip for Plan 02–03 cases so full-file vitest stays non-red"
    - "FIAT_CREDIT AccountList row exposes Грейс control opening separate CreditGraceDialog (D-01, D-02)"
    - "Grace dialog top holds Дата выписки / Оплатить до; empty schedule shows fields + empty-schedule hint immediately (D-03, D-04, D-15)"
    - "After schedule saved, hybrid list merges resolveCurrentAndNext candidates with persisted rows; missing window shows Ввести сумму CTA only — never invents DB rows (D-05, CYCLE-02)"
    - "User can create OPEN obligation with positive amountMinor via sibling amount dialog + createCreditGraceObligation; server freezes dueAsOf via dueAsOfForCycle (D-09, OBL-01, Phase 19 D-05)"
    - "DestructiveConfirmStep accepts optional children + pendingLabel without breaking existing callers"
  prohibitions:
    - statement: MUST NOT put dual DOM fields inside AccountFormDialog
      requirement_id: CYCLE-02
      category: scope
    - statement: MUST NOT invent OPEN obligation rows without amountMinor
      requirement_id: OBL-01
      category: isolation
    - statement: MUST NOT write BalanceSnapshot from grace create or schedule UI path
      requirement_id: OBL-01
      category: isolation
    - statement: MUST NOT autofill bank 21/15 into empty DOM inputs
      requirement_id: CYCLE-02
      category: values
  artifacts:
    - path: src/lib/validations/credit-grace.test.ts
      provides: Wave 0 Zod coverage for create/update amount + closedAsOf pairing
    - path: src/components/accounts/credit-grace-ui.test.ts
      provides: Wave 0 UI source-scan with describe.skip / it.todo owners for Plans 01–03 (no hard-fail cross-wave poison)
    - path: src/components/ui/destructive-confirm-step.tsx
      provides: optional children + pendingLabel API
    - path: src/components/accounts/CreditGraceDialog.tsx
      provides: schedule form + hybrid list + CTAs (D-01…D-05)
    - path: src/components/accounts/CreditGraceAmountDialog.tsx
      provides: create amount dialog (D-09)
    - path: src/app/accounts/actions.ts
      provides: createCreditGraceObligation server action
  key_links:
    - from: AccountList Грейс button
      to: CreditGraceDialog
      via: controlled open on FIAT_CREDIT row only (D-02)
      pattern: "Грейс"
    - from: CreditGraceDialog schedule form
      to: updateGraceSchedule
      via: useActionState (D-03, D-13)
      pattern: "updateGraceSchedule"
    - from: createCreditGraceObligation
      to: dueAsOfForCycle
      via: server recomputes dueAsOf ignoring client spoof (T-20-02)
      pattern: "dueAsOfForCycle"
    - from: hybrid list
      to: resolveCurrentAndNext
      via: CTA when no persisted row for candidate window (D-05)
      pattern: "resolveCurrentAndNext"
---

## Phase Goal

User manages grace cycles and obligations on the credit account with clear RU copy (ROADMAP Phase 20 / CYCLE-02, OBL-01…03, UX-01).

<objective>
Ship Wave 0 Nyquist stubs, extend DestructiveConfirmStep, then tracer: AccountList «Грейс» → CreditGraceDialog schedule + hybrid current/next CTAs → CreditGraceAmountDialog create → createCreditGraceObligation (D-01…D-05, D-09, D-13, D-15, CYCLE-02, OBL-01).

Purpose: Prove separate grace surface + schedule-in-dialog + candidate⊕persisted list + one amount create path before lifecycle/overdue expansion.
Output: Green Wave 0 Zod + non-poisoning todo/skip stubs; confirm API extension; create action; grace dialog shell with create path.
</objective>

<execution_context>
@/home/artem/Documents/wallet/.cursor/gsd-core/workflows/execute-plan.md
@/home/artem/Documents/wallet/.cursor/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/20-obligation-crud-cycle-ui/20-CONTEXT.md
@.planning/phases/20-obligation-crud-cycle-ui/20-RESEARCH.md
@.planning/phases/20-obligation-crud-cycle-ui/20-PATTERNS.md
@.planning/phases/20-obligation-crud-cycle-ui/20-VALIDATION.md
@.planning/phases/20-obligation-crud-cycle-ui/20-UI-SPEC.md
@.planning/phases/18-bank-contract-study-discuss-locks/18-CONTEXT.md
@.planning/phases/19-schema-pure-grace-domain-math/19-CONTEXT.md
@src/app/accounts/actions.ts
@src/app/accounts/actions.test.ts
@src/app/accounts/page.tsx
@src/components/accounts/AccountList.tsx
@src/components/ui/destructive-confirm-step.tsx
@src/lib/credit-grace.ts
@src/lib/validations/credit-grace.ts
@src/components/income/IncomeFactDialog.tsx
@src/components/debts/DebtDetailDialog.tsx
</context>

<assumptions>
## Planner assumptions

- A1 (RESEARCH): Optional children + pendingLabel on DestructiveConfirmStep preferred over local-only confirm footer.
- A2 (RESEARCH): AccountList + grace dialog own UX-01 rename; dashboard debt chrome rename out of scope (Plan 03 still owns AccountList «Задолженность»).
- A3 (RESEARCH): revalidatePath("/") on obligation CRUD mirrors updateGraceSchedule.
- Sibling CreditGraceAmountDialog (IncomeFact pattern); close/reopen steps deferred to Plan 02.
- Overdue chrome + collapsed CLOSED + clear-schedule UI deferred to Plans 02–03.
- Zero new npm packages.
</assumptions>

## Artifacts this plan produces

- Wave 0: `credit-grace.test.ts` (validations), extended `actions.test.ts` stubs, `credit-grace-ui.test.ts`
- `DestructiveConfirmStep` children + pendingLabel
- Optional `mergeGraceListRows` pure helper in `credit-grace.ts` (testable D-05)
- `createCreditGraceObligation` in accounts actions
- `CreditGraceDialog` + `CreditGraceAmountDialog`
- AccountList «Грейс» entry + page include of dual DOM + obligations

<tasks>

<task type="auto">
  <name>Wave 0 Nyquist stubs — Zod, actions CRUD, UI source-scan</name>
  <files>src/lib/validations/credit-grace.test.ts, src/app/accounts/actions.test.ts, src/components/accounts/credit-grace-ui.test.ts</files>
  <read_first>
    .planning/phases/20-obligation-crud-cycle-ui/20-VALIDATION.md
    .planning/phases/20-obligation-crud-cycle-ui/20-RESEARCH.md
    src/lib/validations/credit-grace.ts
    src/lib/validations/account.test.ts
    src/app/accounts/actions.test.ts
    src/components/accounts/AccountFormDialog.test.ts
  </read_first>
  <behavior>
    - createCreditGraceObligationSchema rejects non-positive amountMajor; accepts positive + optional note
    - updateCreditGraceObligationSchema CLOSED requires closedAsOf; OPEN rejects closedAsOf set
    - actions.test: createCreditGraceObligation + create isolation/P2002 use it.todo until Plan 01 T3 greens them; update/close/reopen blocks are describe.skip or it.todo owned by Plan 02 — never hard-failing expects in Wave 0
    - credit-grace-ui.test.ts: describe blocks named by plan owner — Plan 01 tracer chrome (Грейс / amount create titles) it.todo until T3; Plan 02 confirm/collapsed describe.skip; Plan 03 overdue/UX-01 describe.skip — `npx vitest run` on the file exits 0
  </behavior>
  <action>
    Create src/lib/validations/credit-grace.test.ts covering createCreditGraceObligationSchema and updateCreditGraceObligationSchema: positive amount required; CLOSED↔closedAsOf pairing; OPEN with closedAsOf rejected (per Phase 19 D-06…D-08) — this suite must be fully green in Wave 0. Extend src/app/accounts/actions.test.ts with named describes: (1) createCreditGraceObligation — it.todo stubs for create success, P2002 RU, and no balanceSnapshot calls (Plan 01 T3 converts to real expects); (2) updateCreditGraceObligation / closeCreditGraceObligation / reopenCreditGraceObligation — describe.skip or it.todo only (Plan 02 owns greening). Do not plant hard-failing expects for Plan 02 cases. Create src/components/accounts/credit-grace-ui.test.ts as readFileSync source-scan analog of AccountFormDialog.test.ts with three owner describes: plan-01 (Грейс on AccountList, CreditGraceDialog/AmountDialog exist) as it.todo until T3; plan-02 (DestructiveConfirmStep import, no native confirm API, Показать оплаченные) as describe.skip; plan-03 (Задолженность, overdue interest hint, просрочено, UX-01 disclaimer) as describe.skip. Wave 0 verify must leave `npx vitest run src/app/accounts/actions.test.ts src/components/accounts/credit-grace-ui.test.ts` exit 0 (todos/skips only). Do not implement production UI or actions here.
  </action>
  <verify>
    <automated>test -f src/lib/validations/credit-grace.test.ts &amp;&amp; test -f src/components/accounts/credit-grace-ui.test.ts &amp;&amp; npx vitest run src/lib/validations/credit-grace.test.ts src/app/accounts/actions.test.ts src/components/accounts/credit-grace-ui.test.ts</automated>
    <fails_when>missing Wave 0 test files, vitest non-zero (including hard-fail stubs), or Zod suite "0 passed"</fails_when>
  </verify>
  <done>Wave 0 Zod green; actions + UI files present with todo/skip only — no red poison for later full-file verifies</done>
</task>

<task type="auto">
  <name>Extend DestructiveConfirmStep — children + pendingLabel</name>
  <files>src/components/ui/destructive-confirm-step.tsx</files>
  <read_first>
    src/components/ui/destructive-confirm-step.tsx
    .planning/phases/20-obligation-crud-cycle-ui/20-UI-SPEC.md
    src/components/debts/DebtDetailDialog.tsx
  </read_first>
  <action>
    Extend DestructiveConfirmStep props with optional children (ReactNode rendered between message and footer) and optional pendingLabel string. When pending is true, button label uses pendingLabel if provided, else keep existing default pending copy. Existing Debt/Income/AccountList callers must remain type-compatible with zero prop changes. No new packages. Prep for Plan 02 close date field (D-11) and pending labels «Сохранение…» / «Возврат…» from UI-SPEC.
  </action>
  <verify>
    <automated>grep -q 'pendingLabel' src/components/ui/destructive-confirm-step.tsx &amp;&amp; grep -q 'children' src/components/ui/destructive-confirm-step.tsx</automated>
    <fails_when>pendingLabel or children prop absent from destructive-confirm-step.tsx</fails_when>
  </verify>
  <done>Confirm step supports close-date children + custom pending label</done>
</task>

<task type="tracer">
  <name>End-to-end Грейс → schedule → CTA → create obligation</name>
  <reversibility rating="costly">D-01 separate grace dialog + AccountList entry; folding DOM back into AccountForm later fights list/button chrome.</reversibility>
  <files>src/app/accounts/page.tsx, src/components/accounts/AccountList.tsx, src/components/accounts/CreditGraceDialog.tsx, src/components/accounts/CreditGraceAmountDialog.tsx, src/app/accounts/actions.ts, src/lib/credit-grace.ts, src/lib/credit-grace.test.ts, src/app/accounts/actions.test.ts</files>
  <read_first>
    .planning/phases/20-obligation-crud-cycle-ui/20-CONTEXT.md
    .planning/phases/20-obligation-crud-cycle-ui/20-UI-SPEC.md
    .planning/phases/20-obligation-crud-cycle-ui/20-PATTERNS.md
    src/app/accounts/page.tsx
    src/components/accounts/AccountList.tsx
    src/app/accounts/actions.ts
    src/lib/credit-grace.ts
    src/lib/validations/credit-grace.ts
    src/components/income/IncomeFactDialog.tsx
    src/components/debts/DebtDetailDialog.tsx
  </read_first>
  <behavior>
    - page loads statementDayOfMonth, dueDayOfMonth, creditGraceObligations (amountMinor serialized string) onto AccountListItem
    - FIAT_CREDIT row shows outline/sm «Грейс» opening CreditGraceDialog titled «Беспроцентный период» (D-01, D-02); non-credit rows omit button
    - Empty schedule: DOM fields empty (D-15), empty-schedule hint UI-SPEC verbatim; cycle list hidden until both DOM saved (D-04)
    - Schedule save uses updateGraceSchedule; DOM non-recalc hint visible (D-13); no blocking confirm
    - Hybrid list: resolveCurrentAndNext ⊕ persisted by cycleStartAsOf; missing window → «Ввести сумму» only (D-05, CYCLE-02)
    - Amount create dialog: label «Платёж для беспроцентного»; createCreditGraceObligation with assertAccountHasGraceSchedule; server dueAsOf = dueAsOfForCycle; amountMinor via parseMajorToMinor; P2002 → UI-SPEC duplicate RU; never balanceSnapshot.* (OBL-01)
  </behavior>
  <action>
    Extend AccountListItem with statementDayOfMonth, dueDayOfMonth, and serialized creditGraceObligations. Update accounts page.tsx include creditGraceObligations ordered by cycleStartAsOf desc; serialize BigInt amountMinor to string like creditLimitMinor. On FIAT_CREDIT rows only add outline/sm «Грейс» button opening CreditGraceDialog (D-01, D-02) — not whole-row click; do not add dual DOM to AccountFormDialog (D-03). Implement CreditGraceDialog: top schedule form («Дата выписки» / «Оплатить до») via useActionState(updateGraceSchedule); empty DOM start empty (D-15); empty-schedule hint and DOM non-recalc hint per 20-UI-SPEC (D-04, D-13). When schedule present, merge resolveCurrentAndNext(schedule, today) with persisted obligations — optional pure mergeGraceListRows in credit-grace.ts with unit tests; render OPEN rows + CTA «Ввести сумму» for candidate windows lacking rows; never create Prisma rows from list render (D-05 / Phase 19 D-06). Sibling CreditGraceAmountDialog (IncomeFact pattern) for create: required amount + optional note; frozen cycle/due display; submit «Сохранить платёж». Add createCreditGraceObligation in actions.ts: createCreditGraceObligationSchema.safeParse; FIAT_CREDIT + assertAccountHasGraceSchedule; overwrite client dueAsOf with dueAsOfForCycle(cycleStart, account.dueDayOfMonth); parseMajorToMinor; prisma.creditGraceObligation.create; catch P2002 → RU duplicate message from UI-SPEC; revalidatePath /accounts and /; never call balanceSnapshot APIs (T-20-03). Convert Wave 0 createCreditGraceObligation it.todo cases to real expects (create + P2002 + no balanceSnapshot); leave update/close/reopen as describe.skip or it.todo for Plan 02. Convert plan-01 UI describe it.todos to real asserts (Грейс, dialog files exist); leave plan-02/plan-03 describes skipped. Copy from 20-UI-SPEC for titles/hints. Defer close/reopen UI, overdue marks, collapsed CLOSED, clear-schedule control, and AccountList debt-label rename to Plans 02–03.
  </action>
  <verify>
    <automated>npx vitest run src/lib/credit-grace.test.ts src/lib/validations/credit-grace.test.ts &amp;&amp; npx vitest run src/app/accounts/actions.test.ts -t 'createCreditGraceObligation' &amp;&amp; npx vitest run src/components/accounts/credit-grace-ui.test.ts &amp;&amp; test -f src/components/accounts/CreditGraceDialog.tsx &amp;&amp; test -f src/components/accounts/CreditGraceAmountDialog.tsx &amp;&amp; grep -q 'Грейс' src/components/accounts/AccountList.tsx &amp;&amp; grep -q 'createCreditGraceObligation' src/app/accounts/actions.ts &amp;&amp; grep -q 'dueAsOfForCycle' src/app/accounts/actions.ts &amp;&amp; ! grep -q 'statementDayOfMonth' src/components/accounts/AccountFormDialog.tsx</automated>
    <fails_when>vitest non-zero on math/Zod/create-filtered actions/UI suite, grace dialog/amount dialog missing, Грейс absent from AccountList, createCreditGraceObligation or dueAsOfForCycle missing from actions, or statementDayOfMonth appears in AccountFormDialog</fails_when>
  </verify>
  <acceptance_criteria>
    - Separate grace dialog from AccountForm; schedule fields only in grace dialog (D-01, D-03)
    - Hybrid list + CTA without inventing DB rows (D-05, CYCLE-02)
    - create path persists OPEN amountMinor with server-frozen dueAsOf (OBL-01)
    - No snapshot ledger writes from create action (actions.test)
  </acceptance_criteria>
  <done>Tracer green: Грейс entry + schedule + cycle CTAs + create obligation end-to-end</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| FormData / client → Server Actions | Untrusted DOM, amount, cycleStart, spoofed dueAsOf |
| Grace dialog UI → AccountList props | Serialized BigInt + schedule ints from RSC |
| Grace writes → BalanceSnapshot | Must remain disconnected (GRISO preview) |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-20-01 | Tampering | Crafted amount / DOM FormData | high | mitigate | Zod create + updateGraceScheduleSchema; positive amount refine |
| T-20-02 | Tampering | Client spoofed dueAsOf on create | high | mitigate | Server dueAsOfForCycle overwrite at createCreditGraceObligation |
| T-20-03 | Tampering | BalanceSnapshot write from grace | high | mitigate | create touches creditGraceObligation only; actions.test asserts snapshot APIs unused |
| T-20-04 | Tampering | Create without schedule | medium | mitigate | assertAccountHasGraceSchedule before create |
| T-20-05 | Tampering | Duplicate cycle insert | medium | mitigate | @@unique + P2002 → UI-SPEC RU; prefer update later |
| T-20-SC | Tampering | npm installs | high | mitigate | Zero new packages — no install checkpoint |
</threat_model>

<verification>
Wave 0 Zod + todo/skip stubs green; create-filtered actions.test + UI suite green; CreditGraceDialog/AmountDialog exist; Грейс on AccountList; AccountFormDialog free of dual DOM fields.
</verification>

<success_criteria>
CYCLE-02 current/next CTAs visible after schedule save; OBL-01 create path works; D-01…D-05/D-09/D-13/D-15 honored; GRISO preview on create.
</success_criteria>

<output>
Create `.planning/phases/20-obligation-crud-cycle-ui/20-01-SUMMARY.md` when done
</output>
