---
phase: 22-graceiso-regression-polish
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/lib/griso.test.ts
  - src/app/accounts/actions.test.ts
autonomous: true
requirements:
  - GRISO-01
estimate:
  tokens: 30000
  raw_tokens: 60000
  tasks: 3
  confidence: high
must_haves:
  truths:
    - "Wave 0: src/lib/griso.test.ts exists under describe GRISO-01 isolation with non-poisoning it.todo stubs so vitest exits 0"
    - "net-worth.ts and historical-series.ts file-scan ban credit-grace and nw-forecast imports (D-05, C-01, GRISO-01)"
    - "nw-forecast.ts retains INISO bans: no prisma / BalanceSnapshot / net-worth / historical-series (D-06)"
    - "credit-grace.ts bans prisma, net-worth, historical-series imports (D-07)"
    - "Past buildNetWorthSeries golden identity: two identical account/snapshot/rate calls bitwise-equal; conceptual grace fixture never passed (D-09, D-11, C-01)"
    - "Public BuildNetWorthSeriesInput keys exclude grace/obligation/creditGrace (and keep income forecast keys banned) via Object.keys + AssertNever (D-09)"
    - "At least one golden fixture uses FIAT_CREDIT + creditLimitMinor + snapshots so LOCF stays account-only (D-10)"
    - "updateGraceSchedule happy path never calls prisma.balanceSnapshot upsert/delete (D-04 schedule gap)"
    - "All five grace mutations covered by never-calls (schedule + create/update/close/reopen); existing smokes in credit-grace.test.ts left intact (D-02, D-03, D-04)"
    - "Phase 21 verify trio still green after suite lands"
  prohibitions:
    - statement: MUST NOT ban BalanceSnapshot string across whole accounts/actions.ts or AccountList / SetBalanceDialog / BAL upsert paths
      requirement_id: GRISO-01
      category: isolation
    - statement: MUST NOT ban the word grace inside nw-forecast.ts (ForecastSlotKind grace is intentional)
      requirement_id: GRISO-01
      category: isolation
    - statement: MUST NOT change overlay math, membership, or historical LOCF builders
      requirement_id: GRISO-01
      category: values
    - statement: MUST NOT delete credit-grace.test.ts GRISO smoke when adding twin
      requirement_id: GRISO-01
      category: integrity
    - statement: MUST NOT remove balanceSnapshot from accounts prisma test double
      requirement_id: GRISO-01
      category: isolation
  artifacts:
    - path: src/lib/griso.test.ts
      provides: GRISO-01 file-scan walls + past-series golden + FIAT_CREDIT fixture (INISO twin)
    - path: src/app/accounts/actions.test.ts
      provides: updateGraceSchedule never-calls + five-mutation write-gate coverage
  key_links:
    - from: griso.test.ts readFileSync walls
      to: net-worth.ts / historical-series.ts / nw-forecast.ts / credit-grace.ts
      via: not.toMatch import regex twin of iniso.test.ts
      pattern: "GRISO-01 isolation"
    - from: buildNetWorthSeries golden
      to: BuildNetWorthSeriesInput
      via: void conceptual grace fixture never in API args
      pattern: "_gracePresentConceptually"
    - from: updateGraceSchedule / create|update|close|reopenCreditGraceObligation
      to: prisma.balanceSnapshot.upsert|delete
      via: expect(...).not.toHaveBeenCalled on happy paths
      pattern: "never calls balanceSnapshot"
---

## Phase Goal

Historical NW and BalanceSnapshot stay grace-free; milestone isolation is regression-proof (ROADMAP Phase 22 / GRISO-01).

<objective>
**Nyquist Wave-0:** plant non-poisoning `griso.test.ts` stubs, then tracer-green the INISO twin (file-scan + golden + FIAT_CREDIT per D-01/D-05–D-07/D-09–D-11), then close the `updateGraceSchedule` write-gate gap and confirm all five grace mutations never call balanceSnapshot upsert/delete (D-02/D-04). Leave existing `credit-grace.test.ts` smokes (D-03 discretion).

Purpose: Automated GRISO regression twin suitable for v1.3 milestone close — prove walls, do not change product math (C-03).
Output: Green `src/lib/griso.test.ts` + expanded `accounts/actions.test.ts` write-gates; Phase 21 suites still green.
</objective>

<execution_context>
@/home/artem/Documents/wallet/.cursor/gsd-core/workflows/execute-plan.md
@/home/artem/Documents/wallet/.cursor/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/22-graceiso-regression-polish/22-CONTEXT.md
@.planning/phases/22-graceiso-regression-polish/22-RESEARCH.md
@.planning/milestones/v1.2-phases/17-nw-forecast-overlay-isolation/17-CONTEXT.md
@src/lib/iniso.test.ts
@src/lib/disol.test.ts
@src/lib/credit-grace.test.ts
@src/lib/historical-series.ts
@src/lib/net-worth.ts
@src/lib/nw-forecast.ts
@src/lib/credit-grace.ts
@src/app/accounts/actions.ts
@src/app/accounts/actions.test.ts
</context>

<assumptions>
## Planner assumptions

- A1 (RESEARCH): Production import walls already PASS; gap is missing twin suite + schedule write-gate test, not production leak.
- A2 (RESEARCH): Accounts prisma mock must keep balanceSnapshot for BAL-01 — per-path never-calls only (D-08); do not copy income whole-file absence assert.
- A3 (RESEARCH): Optional Orca UAT deferred to verify gate (D-13); primary evidence is automated suite.
- Carried locks C-01/C-02/C-03 already locked in Phases 18–21 — this plan proves them via tests; no product reopen.
- Discretion: leave credit-grace GRISO smokes; mirror INISO describe naming with GRISO prefixes; optionally add describe("GRISO write-gates") listing all five without removing embedded asserts.
- Zero new npm packages; vitest stays 4.x project lock.
- Schema-gate: no prisma/schema edits.
</assumptions>

## Artifacts this plan produces

- `src/lib/griso.test.ts` — Wave 0 stubs → green INISO twin
- Expanded GRISO never-calls in `src/app/accounts/actions.test.ts` (schedule gap + five-mutation clarity)

<tasks>

<task type="auto">
  <name>Wave 0 Nyquist stubs — griso.test.ts scaffold</name>
  <files>src/lib/griso.test.ts</files>
  <read_first>
    .planning/phases/22-graceiso-regression-polish/22-RESEARCH.md
    src/lib/iniso.test.ts
    src/lib/credit-grace.test.ts
  </read_first>
  <behavior>
    - File src/lib/griso.test.ts exists and is picked up by vitest include
    - Top-level describe is "GRISO-01 isolation" (D-01)
    - Stub it.todo (or describe.skip) names cover: NW/historical credit-grace+nw-forecast walls (D-05); nw-forecast INISO bans retained (D-06); credit-grace self-wall (D-07); past-series golden identity (D-09); FIAT_CREDIT golden fixture (D-10)
    - Full-file vitest on griso.test.ts exits 0 (todos/skips must not fail)
    - Do not delete or weaken credit-grace.test.ts GRISO smoke (D-03)
  </behavior>
  <action>
    Create src/lib/griso.test.ts as a structural skeleton twin of src/lib/iniso.test.ts (D-01). Use describe("GRISO-01 isolation") and plant it.todo / describe.skip cases for: (1) net-worth.ts + historical-series.ts ban credit-grace and nw-forecast module paths (D-05); (2) nw-forecast.ts prisma / BalanceSnapshot / net-worth / historical-series bans identical to INISO (D-06); (3) credit-grace.ts bans prisma + net-worth + historical-series (D-07); (4) past-series golden identity with conceptual grace fixture voided (D-09); (5) FIAT_CREDIT + snapshots golden (D-10). Do not plant hard-failing expects that poison Wave 0. No production lib edits. Do not relocate or remove existing GRISO smoke in credit-grace.test.ts.
  </action>
  <verify>
    <automated>npx vitest run src/lib/griso.test.ts</automated>
    <fails_when>vitest non-zero exit, file missing, or summary shows failed tests (todos/skips must not fail)</fails_when>
  </verify>
  <done>griso.test.ts exists; Wave 0 vitest exits 0 with non-poisoning stubs for D-05–D-07/D-09–D-10</done>
</task>

<task type="tracer" tdd="true">
  <name>End-to-end GRISO twin — file-scan walls + past-series golden</name>
  <files>src/lib/griso.test.ts</files>
  <read_first>
    src/lib/iniso.test.ts
    src/lib/historical-series.ts
    src/lib/historical-series.test.ts
    src/lib/net-worth.ts
    src/lib/nw-forecast.ts
    src/lib/credit-grace.ts
    .planning/phases/22-graceiso-regression-polish/22-CONTEXT.md
  </read_first>
  <behavior>
    - For each of net-worth.ts and historical-series.ts: source must not match @/lib/credit-grace or ./credit-grace; must not match @/lib/nw-forecast or ./nw-forecast (D-05)
    - nw-forecast.ts: same four INISO bans as iniso.test.ts (prisma import patterns, BalanceSnapshot word, net-worth/historical-series paths) (D-06) — do not add a ban on the word grace
    - credit-grace.ts: no prisma client imports; no @/lib/net-worth|historical-series or ./net-worth|./historical-series (D-07); prefer import/path walls over scanning unrelated comment prose
    - Golden: buildNetWorthSeries(input) twice with identical accounts/snapshots/rates/preset/today; void _gracePresentConceptually (or obligations object) never passed into API; outputs equal; keys sorted equal accounts/preset/primaryScale/rates/snapshots/today; not.toContain grace, obligation, creditGrace; keep income/forecast/recurring/oneTime banned; ForbiddenKeys AssertNever extends INISO union with grace synonyms (D-09, D-11)
    - Second fixture: FIAT_CREDIT account with creditLimitMinor + snapshots; series still account-only LOCF (D-10) — mirror historical-series.test.ts credit patterns for field shapes
    - All Wave 0 todos for walls/golden become green its
  </behavior>
  <action>
    Replace Wave 0 stubs in src/lib/griso.test.ts with green tests copied structurally from src/lib/iniso.test.ts (per D-01). Wire readFileSync walls for D-05/D-06/D-07 exactly as RESEARCH Pattern 1. Implement past-series golden identity (D-09/D-11) with void conceptual grace fixture never passed into buildNetWorthSeries. Extend forbidden key list + AssertNever Extract with grace, obligation, creditGrace while retaining income keys. Add FIAT_CREDIT + creditLimitMinor + snapshots fixture (D-10). Do not edit production math modules unless a wall unexpectedly fails — if so, restore isolation without changing A′ overlay behavior (C-03). Do not introduce a whole-file accounts BalanceSnapshot string ban (D-08). Leave credit-grace.test.ts smokes untouched (D-03).
  </action>
  <verify>
    <automated>npx vitest run src/lib/griso.test.ts src/lib/iniso.test.ts src/lib/nw-forecast.test.ts src/lib/credit-grace.test.ts src/components/dashboard/nw-forecast-ui.test.ts</automated>
    <fails_when>vitest non-zero, remaining it.todo on walls/golden, missing FIAT_CREDIT fixture, or Phase 21/INISO regressions</fails_when>
  </verify>
  <done>GRISO twin green (SC2–SC3); INISO + Phase 21 prior verify trio still green; smokes retained</done>
</task>

<task type="auto" tdd="true">
  <name>Grace mutation write-gates — schedule gap + five-path coverage</name>
  <files>src/app/accounts/actions.test.ts</files>
  <read_first>
    src/app/accounts/actions.test.ts
    src/app/accounts/actions.ts
    src/app/income/actions.test.ts
    .planning/phases/22-graceiso-regression-polish/22-RESEARCH.md
  </read_first>
  <behavior>
    - updateGraceSchedule FIAT_CREDIT happy path (DOM set and/or clear-with-zero-OPEN): after success, prisma.balanceSnapshot.upsert and .delete were never called (D-04 gap)
    - createCreditGraceObligation / updateCreditGraceObligation / closeCreditGraceObligation / reopenCreditGraceObligation retain never-calls (existing dedicated or embedded asserts stay green)
    - Optional: describe("GRISO write-gates") documents all five mutation names for SC1 clarity without removing embedded asserts
    - prisma mock still exposes balanceSnapshot for BAL upsert/delete tests (D-08)
    - Do not add a whole-file absence of BalanceSnapshot on accounts/actions.ts (JSDoc already documents never-writes — comment false-positive pitfall)
  </behavior>
  <action>
    Expand src/app/accounts/actions.test.ts write-gates per D-02/D-04. Add a dedicated GRISO it under describe("updateGraceSchedule ...") that runs a successful FIAT_CREDIT schedule update (reuse existing happy-path FormData pattern: accountId + statementDayOfMonth/dueDayOfMonth) and asserts prisma.balanceSnapshot.upsert and prisma.balanceSnapshot.delete were not called — this closes the documented schedule gap. Keep create/update/close/reopen never-calls. Optionally add describe("GRISO write-gates") listing all five exports for SC1 readability without deleting embedded asserts (belt + suspenders OK). Do not strip balanceSnapshot from the prisma test double. Do not ban BalanceSnapshot across AccountList, SetBalanceDialog, or upsertBalanceSnapshot/deleteBalanceSnapshot paths (D-08). Prefer write-API never-calls over scanning grace JSDoc for the BalanceSnapshot token (RESEARCH Pitfall 1). No production actions.ts changes expected if bodies already clean.
  </action>
  <verify>
    <automated>npx vitest run src/lib/griso.test.ts src/app/accounts/actions.test.ts src/lib/nw-forecast.test.ts src/lib/credit-grace.test.ts src/components/dashboard/nw-forecast-ui.test.ts</automated>
    <fails_when>vitest non-zero, updateGraceSchedule missing never-calls, BAL upsert tests broken, or balanceSnapshot stripped from prisma mock</fails_when>
  </verify>
  <done>All five grace mutations have never-calls; schedule gap closed; BAL paths still green; GRISO + Phase 21 suites green</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Grace FormData → accounts server actions | Untrusted mutation inputs; must not reach BalanceSnapshot writes |
| Test suite → production NW libs | Regression walls only; no runtime trust change |
| Overlay (nw-forecast) ↔ historical LOCF | Soft boundary; file-scan enforces no import coupling |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-22-01 | Tampering | updateGraceSchedule / grace obligation actions | high | mitigate | Per-path never-calls on prisma.balanceSnapshot.upsert/delete for all five grace mutations (D-04) |
| T-22-02 | Tampering | net-worth.ts / historical-series.ts | high | mitigate | griso.test.ts file-scan bans credit-grace + nw-forecast imports (D-05); golden identity (D-09) |
| T-22-03 | Tampering | credit-grace.ts | medium | mitigate | D-07 file-scan: no prisma / net-worth / historical-series imports |
| T-22-04 | Information Disclosure | nw-forecast.ts | medium | mitigate | Retain INISO prisma/BalanceSnapshot/history bans (D-06) |
| T-22-05 | Denial of Service | accounts BAL UX | low | accept | D-08 scopes walls to grace paths only — whole-file ban would break legitimate snapshot UX |
| T-22-SC | Tampering | npm installs | high | mitigate | No new packages this plan — Package Legitimacy N/A |
</threat_model>

<verification>
Full sampling gate after all tasks:

```bash
npx vitest run \
  src/lib/griso.test.ts \
  src/lib/iniso.test.ts \
  src/lib/nw-forecast.test.ts \
  src/lib/credit-grace.test.ts \
  src/components/dashboard/nw-forecast-ui.test.ts \
  src/app/accounts/actions.test.ts
```

Prior Phase 21 trio must remain green (reuse verbatim): `npx vitest run src/lib/nw-forecast.test.ts src/lib/credit-grace.test.ts src/components/dashboard/nw-forecast-ui.test.ts`
</verification>

<success_criteria>
- GRISO twin suite proves SC2–SC3 (file-scan + golden + FIAT_CREDIT)
- SC1: five grace mutations never call balanceSnapshot write APIs (schedule gap closed)
- Existing smokes retained; Phase 21 + INISO still green
- No production overlay/LOCF math changes; D-08 BAL paths intact
</success_criteria>

<output>
Create `.planning/phases/22-graceiso-regression-polish/22-01-SUMMARY.md` when done
</output>
