---
phase: 21-kapital-forecast-integration
plan: 03
type: execute
wave: 3
depends_on:
  - 21-02
files_modified:
  - src/components/dashboard/NetWorthHistoryChart.tsx
  - src/components/dashboard/nw-forecast-ui.test.ts
autonomous: true
requirements:
  - GRFCST-01
estimate:
  tokens: 35000
  raw_tokens: 70000
  tasks: 2
  confidence: high
must_haves:
  truths:
    - "One dashed «Прогноз» Line only — no on-line visual distinction for grace vs income days (C-02, D-08)"
    - "Future tooltip: aggregate «Прогноз» amount block first, then grace block with «Платёж для беспроцентного» + per-obligation rows + «NW без изменения (оплата карты)» (C-04, D-10…D-12)"
    - "Today tooltip: account stack + «Итого», then same grace block for folded overdue / due-today events (D-09)"
    - "Grace block shows obligation amount (primary after FX) even though ΔNW=0 (D-11); one row per OPEN (D-12)"
    - "FX-excluded grace omitted from tooltip — honesty is banner only (D-13)"
    - "0 grace events that day → omit grace block; income/fact block may still show (UI-SPEC empty)"
    - statement: "Long account names in grace rows wrap via existing tooltip flex / min-w-40 pattern (UI-SPEC)"
      verification: backstop
  prohibitions:
    - statement: MUST NOT add second Line/Area/dot style for grace days
      requirement_id: GRFCST-01
      category: scope
    - statement: MUST NOT use warning/destructive colors for overdue folded grace on Капитал chart
      requirement_id: GRFCST-01
      category: values
    - statement: MUST NOT aggregate multiple same-day OPEN into one tooltip line
      requirement_id: GRFCST-01
      category: values
  artifacts:
    - path: src/components/dashboard/NetWorthHistoryChart.tsx
      provides: two-block tooltip (forecast/fact then grace rows) reading forecastEvents
    - path: src/components/dashboard/nw-forecast-ui.test.ts
      provides: green Plan 03 tooltip RU + single-series file-scan
  key_links:
    - from: NetWorthChartPoint.forecastEvents
      to: custom Chart tooltip
      via: payload[0].payload (existing pattern)
      pattern: "forecastEvents"
    - from: grace tooltip heading
      to: C-04 copy
      via: locked RU strings
      pattern: "Платёж для беспроцентного"
---

## Phase Goal

Open grace obligations appear on Капитал «Прогноз» with the same FX honesty as income forecast (ROADMAP Phase 21 / GRFCST-01, GRFCST-02).

<objective>
Tracer: NetWorthHistoryChart tooltip reads forecastEvents — today + future two-block layout with C-04 grace copy (C-02, C-04, D-08…D-13, GRFCST-01).

Purpose: Make A′ visible via tooltip without a second series.
Output: Green UI file-scan for tooltip RU + single dashed series; operator human-check for feel.
</objective>

<execution_context>
@/home/artem/Documents/wallet/.cursor/gsd-core/workflows/execute-plan.md
@/home/artem/Documents/wallet/.cursor/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/21-kapital-forecast-integration/21-CONTEXT.md
@.planning/phases/21-kapital-forecast-integration/21-UI-SPEC.md
@.planning/phases/21-kapital-forecast-integration/21-PATTERNS.md
@.planning/phases/21-kapital-forecast-integration/21-02-SUMMARY.md
@src/components/dashboard/NetWorthHistoryChart.tsx
@src/components/dashboard/DashboardChartsShell.tsx
@src/components/dashboard/nw-forecast-ui.test.ts
</context>

<assumptions>
## Planner assumptions

- **RESOLVED RESEARCH Q2:** Future first block stays aggregate «Прогноз» amount — not per-income expansion (D-10).
- Discretion (UI-SPEC): «NW без изменения (оплата карты)» once under grace heading; amounts per row (D-11/D-12).
- Paint contract unchanged: strokeDasharray 5 5, muted-foreground, stepAfter, dot false (D-08).
- human_verify_mode=end-of-phase → human-check here, not checkpoint:human-verify.
</assumptions>

## Artifacts this plan produces

- Tooltip two-block grace chrome on NetWorthHistoryChart
- Green Plan 03 UI scans (C-04 strings + single series)

<tasks>

<task type="tracer">
  <name>End-to-end tooltip two-block grace chrome</name>
  <reversibility rating="costly">D-09…D-13 tooltip metadata contract becomes Капитал UX SoT shared with income forecast chrome.</reversibility>
  <files>src/components/dashboard/NetWorthHistoryChart.tsx, src/components/dashboard/nw-forecast-ui.test.ts</files>
  <read_first>
    .planning/phases/21-kapital-forecast-integration/21-CONTEXT.md
    .planning/phases/21-kapital-forecast-integration/21-UI-SPEC.md
    .planning/phases/21-kapital-forecast-integration/21-PATTERNS.md
    src/components/dashboard/NetWorthHistoryChart.tsx
    src/components/dashboard/nw-forecast-ui.test.ts
  </read_first>
  <behavior>
    - NetWorthChartPoint allows forecastEvents array (kind grace/income metadata from shell)
    - asOfDate &gt; today: block A = «Прогноз» + amount; block B = grace heading + N rows if events (D-10)
    - asOfDate ≤ today: block A = account stack + «Итого»; block B = grace rows for today events including folded overdue (D-09)
    - Grace heading «Платёж для беспроцентного»; sub «NW без изменения (оплата карты)»; row = accountName · amount (C-04, D-11, D-12)
    - Separator border-t border-border/50 + pt-1/mt-1 (UI-SPEC spacing xs)
    - Still exactly one dashed Line named «Прогноз» (D-08, C-02)
    - File-scan asserts C-04 strings present; single strokeDasharray Line pattern retained
  </behavior>
  <action>
    Extend NetWorthHistoryChart tooltip to read forecastEvents from payload point. Implement two-block layout per UI-SPEC DOM: income/forecast or fact stack first, grace block below when ≥1 FX-included grace event (D-10, D-13). Use locked RU strings (C-04). One row per obligation (D-12); show amount despite ΔNW=0 (D-11). Today hinge includes folded overdue events beside stack (D-09). Prefer subcopy once under grace heading. Do not add second series, dots, or warning colors for grace days (D-08). Convert Plan 03 Wave 0 UI todos to real expects for C-04 strings; keep banner Plan 02 asserts green. Match existing Geist Mono amount styling.
  </action>
  <verify>
    <automated>npx vitest run src/components/dashboard/nw-forecast-ui.test.ts src/lib/nw-forecast.test.ts &amp;&amp; grep -q 'Платёж для беспроцентного' src/components/dashboard/NetWorthHistoryChart.tsx &amp;&amp; grep -q 'NW без изменения (оплата карты)' src/components/dashboard/NetWorthHistoryChart.tsx &amp;&amp; grep -c 'strokeDasharray' src/components/dashboard/NetWorthHistoryChart.tsx | grep -q '^[1-9]'</automated>
    <fails_when>vitest non-zero, C-04 RU strings missing from chart source, or strokeDasharray absent</fails_when>
  </verify>
  <acceptance_criteria>
    - Two-block tooltip order locked (D-10); today grace visible (D-09)
    - Per-obligation rows + amounts (D-11, D-12); FX-excluded omitted (D-13)
    - Single dashed «Прогноз» series only (C-02, D-08)
    - UI file-scan green for C-04
  </acceptance_criteria>
  <done>Tracer green: tooltip grace chrome + file-scan</done>
</task>

<task type="auto">
  <name>Phase sampling gate + operator tooltip feel check</name>
  <files>src/components/dashboard/nw-forecast-ui.test.ts</files>
  <read_first>
    .planning/phases/21-kapital-forecast-integration/21-VALIDATION.md
    .planning/OPERATOR.md
    src/components/dashboard/nw-forecast-ui.test.ts
  </read_first>
  <action>
    Ensure Plan 02+03 UI describes are fully green (no leftover skip/todo for this phase chrome). Run the phase quick sampling command covering math + UI. Do not change production paint beyond tooltip work already in tracer. Prepare for end-of-phase UAT: agent drives npm run dev + Orca on `/` per OPERATOR.md.
  </action>
  <verify>
    <automated>npx vitest run src/lib/nw-forecast.test.ts src/components/dashboard/nw-forecast-ui.test.ts src/lib/credit-grace.test.ts</automated>
    <fails_when>vitest non-zero or any remaining it.todo/describe.skip for Phase 21 banner/tooltip owners still failing the suite contract</fails_when>
    <human-check>
      <what>On Капитал `/`, hover today (folded overdue if any) and a future due day with income+grace — one dashed «Прогноз»; tooltip shows forecast/fact block then grace block with C-04 copy; partial banner lists FX codes when rates missing.</what>
      <why>Layout/tooltip feel is subjective; automated file-scan cannot judge two-block readability.</why>
    </human-check>
  </verify>
  <acceptance_criteria>
    - Phase sampling suite green (GRFCST-01/02 automated map)
    - Human-check notes tooltip two-block feel acceptable for UAT handoff
  </acceptance_criteria>
  <done>Phase 21 chrome sampling green; human-check recorded for verify-work</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Chart datum forecastEvents → tooltip DOM | Account names/amounts render as text in browser |
| Tooltip UI ← builder exclusions | FX-excluded events must not appear (honesty = banner) |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-21-09 | Tampering | Tooltip accountName / amounts | low | accept | React text nodes; no HTML injection APIs |
| T-21-10 | Information Disclosure | Showing FX-excluded grace in tooltip | medium | mitigate | Only render events that survived builder FX gate (D-13); banner owns honesty |
| T-21-11 | Tampering | Second series implying historical change | high | mitigate | Keep single dashed Line; overlay-only copy (C-02 / D-08) |
| T-21-SC | Tampering | npm installs | high | mitigate | Zero new packages |
</threat_model>

<verification>
npx vitest run src/lib/nw-forecast.test.ts src/components/dashboard/nw-forecast-ui.test.ts src/lib/credit-grace.test.ts; C-04 strings in chart; human-check tooltip feel.
</verification>

<success_criteria>
GRFCST-01 tooltip visibility complete (D-08…D-13, C-04); single series; sampling green for execute→verify-work handoff.
</success_criteria>

<output>
Create `.planning/phases/21-kapital-forecast-integration/21-03-SUMMARY.md` when done
</output>
