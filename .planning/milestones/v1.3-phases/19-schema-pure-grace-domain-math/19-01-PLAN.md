---
phase: 19-schema-pure-grace-domain-math
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - prisma/schema.prisma
  - prisma/migrations/
  - src/lib/credit-grace.ts
  - src/lib/credit-grace.test.ts
  - src/lib/foundation.test.ts
  - data/wallet.db
autonomous: false
requirements:
  - CYCLE-01
estimate:
  tokens: 45000
  raw_tokens: 90000
  tasks: 3
  confidence: high
must_haves:
  truths:
    - "UI-00: Phase 19 ships zero user-facing screens or interactive chrome (no grace form pages)"
    - Credit Account stores statementDayOfMonth + dueDayOfMonth Int? only as dual-DOM schedule SoT (D-01, CYCLE-01)
    - Dual DOM allowed only on FIAT_CREDIT; non-credit must keep both null (D-02)
    - Both DOM null or both set — SQLite CHECK Account_grace_dom_invariant rejects partial (D-03)
    - CreditGraceObligation persists cycleStartAsOf, frozen dueAsOf, amountMinor BigInt, OPEN|CLOSED, optional closedAsOf/note (D-05, D-06, D-07, D-08)
    - Unique (accountId, cycleStartAsOf) on CreditGraceObligation (D-09, D-16)
    - Account delete Cascades creditGraceObligations (D-15)
    - cycleStartAsOf(y,m,statementDOM) uses clampDayOfMonth; dueAsOfForCycle uses next calendar month + dueDOM clamp — not sole addCalendarDays as due engine (D-09, D-10, CYCLE-01)
    - "adjacency probe: inclusive [from,to] with from===to equal to a cycleStartAsOf emits exactly one window; abutting cycles stay separate rows"
    - "empty probe: both DOM null → helpers return empty; single matching month window length 1"
    - "ordering probe: listCycleWindows ascending by cycleStartAsOf (expanded Plan 02); unique key forbids equal-key duplicates (D-16)"
  prohibitions:
    - statement: MUST NOT add duration-days or grace-anchor columns as schedule SoT
      requirement_id: CYCLE-01
      category: values
    - statement: MUST NOT derive dues or amounts from the snapshot ledger
      requirement_id: CYCLE-01
      category: isolation
    - statement: MUST NOT add grace form/UI routes this phase
      requirement_id: UI-00
      category: scope
    - statement: MUST NOT import Prisma into credit-grace pure helpers
      requirement_id: CYCLE-01
      category: isolation
  artifacts:
    - path: prisma/schema.prisma
      provides: statementDayOfMonth, dueDayOfMonth on Account; GraceObligationStatus; CreditGraceObligation; creditGraceObligations relation
    - path: prisma/migrations
      provides: credit_grace_dual_dom migration with Account_grace_dom_invariant CHECK + obligation OPEN/closedAsOf CHECK + Cascade FK
    - path: src/lib/credit-grace.ts
      provides: cycleStartAsOf, dueAsOfForCycle tracer exports
    - path: src/lib/credit-grace.test.ts
      provides: 21→15 next-month due + Feb statement clamp fixtures
    - path: src/lib/foundation.test.ts
      provides: CreditGraceObligation table allowlist + credit_grace_dual_dom migration name assert
  key_links:
    - from: dueAsOfForCycle
      to: clampDayOfMonth
      via: next-month DOM clamp
      pattern: "clampDayOfMonth"
    - from: CreditGraceObligation
      to: Account
      via: "onDelete Cascade + @@unique([accountId, cycleStartAsOf])"
      pattern: "onDelete: Cascade"
    - from: Account_grace_dom_invariant
      to: statementDayOfMonth / dueDayOfMonth
      via: both-null-or-both + FIAT_CREDIT-only CHECK
      pattern: "Account_grace_dom_invariant"
---

## Specless probe fallback

EDGE_ABSENT / PROHIB_ABSENT — SPEC absent. Probe predicates authored into `must_haves.truths` (adjacency / empty / ordering). Unresolved coverage treated as assumptions below (do not drop).

## Phase Goal

Credit grace schedule and obligation identity exist as data + pure math ready for UI (ROADMAP Phase 19 / CYCLE-01).

<objective>
Confirm one-way dual-DOM CHECK (D-03) + unique cycle key (D-09), then ship Account dual DOM + CreditGraceObligation (Cascade D-15) + customized migrate CHECKs + tracer cycleStartAsOf / dueAsOfForCycle using clampDayOfMonth next-month due (D-01, D-02, D-05…D-10, D-15, D-16, CYCLE-01).

Purpose: Prove dual-DOM SoT + obligation child + due engine end-to-end before window/write-path expansion.
Output: Migrated schema, credit-grace tracer exports, green Vitest 21→15, host DB up to date, foundation allowlist.
</objective>

<execution_context>
@/home/artem/Documents/wallet/.cursor/gsd-core/workflows/execute-plan.md
@/home/artem/Documents/wallet/.cursor/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/19-schema-pure-grace-domain-math/19-CONTEXT.md
@.planning/phases/19-schema-pure-grace-domain-math/19-RESEARCH.md
@.planning/phases/19-schema-pure-grace-domain-math/19-PATTERNS.md
@.planning/phases/19-schema-pure-grace-domain-math/19-VALIDATION.md
@.planning/phases/19-schema-pure-grace-domain-math/19-UI-SPEC.md
@prisma/schema.prisma
@prisma/migrations/20260903005200_account_credit_limit_check/migration.sql
@src/lib/dates.ts
@src/lib/income.ts
@src/lib/foundation.test.ts
</context>

<assumptions>
## Planner assumptions

- A1 (RESEARCH): Prisma 7.10 still needs custom migration.sql for CHECK — same RedefineTables pattern as Account_credit_limit_invariant.
- A2 (RESEARCH): Gap-day current/next table locked in Plan 02 tests; tracer only needs cycleStart + dueAsOfForCycle.
- A3 (RESEARCH): CYCLE-01 demoable set/clear via Server Action lands in Plan 03; this plan proves storage + due math.
- Specless adjacency/empty/ordering: predicates in must_haves; Plan 02 owns listCycleWindows matrix; Plan 01 unique constraint covers equal-key collision (D-16).
- CLOSED rows do not block schedule clear (D-14 OPEN-only) — Plan 03.
- node_modules may be absent — npm install from lockfile before prisma/vitest.
</assumptions>

## Artifacts this phase produces

- `statementDayOfMonth` / `dueDayOfMonth` Int? on `Account`
- `GraceObligationStatus` enum OPEN | CLOSED
- `CreditGraceObligation` model + `creditGraceObligations` relation on Account
- Migration folder name containing `credit_grace_dual_dom` with `Account_grace_dom_invariant` (+ keep `Account_credit_limit_invariant`) and obligation status/closedAsOf CHECK
- `cycleStartAsOf`, `dueAsOfForCycle` in `src/lib/credit-grace.ts` (this plan); later `listCycleWindows`, `resolveCurrentAndNext`, `isGraceOverdue` (Plan 02)
- `src/lib/credit-grace.test.ts`
- Foundation allowlist entry for `CreditGraceObligation`
- Plan 03: `updateGraceScheduleSchema`, `updateGraceSchedule` action, `src/lib/validations/credit-grace.ts`
- Zero UI routes; zero new npm packages

<tasks>

<task type="checkpoint:decision" gate="blocking">
  <name>Confirm one-way dual-DOM CHECK + unique cycle key (D-03, D-09)</name>
  <decision>Lock SQLite Account_grace_dom_invariant (both-null-or-both + FIAT_CREDIT-only, D-02/D-03) and CreditGraceObligation @@unique([accountId, cycleStartAsOf]) (D-09 / D-16) before migrate lands</decision>
  <context>
    CONTEXT rates D-03 and D-09 as one-way. Undoing the CHECK or unique after Phase 20–21 write paths means a data migration plus forecast membership rewrites. Milestone ARCHITECTURE.md still sketches duration-days / Restrict — CONTEXT overrides (dual DOM + Cascade D-15). Confirm before schema lands.
  </context>
  <options>
    <option id="dual-dom-unique">
      <name>Dual-DOM CHECK + unique cycle key (CONTEXT D-03 / D-09) — recommended</name>
      <pros>Matches locked discuss-phase; FIAT_CREDIT pairing; frozen cycle identity for forecast; Cascade wipe on account delete (D-15)</pros>
      <cons>Account table rebuild in migrate; cannot partial-schedule; duplicate cycle creates hard error</cons>
    </option>
    <option id="duration-days-sot">
      <name>Duration-days / grace-anchor SoT (stale research sketch)</name>
      <pros>Matches older ARCHITECTURE.md wording</pros>
      <cons>Contradicts Phase 18/19 D-01/D-10 and bank 21→15 next</cons>
    </option>
    <option id="defer-unique">
      <name>Dual DOM without unique cycle key</name>
      <pros>Smaller first migrate</pros>
      <cons>Breaks D-09/D-16; duplicate cycles corrupt Phase 20/21</cons>
    </option>
  </options>
  <resume-signal>Select: dual-dom-unique (recommended), duration-days-sot, or defer-unique</resume-signal>
</task>

<task type="tracer">
  <name>End-to-end dual DOM schema + cycleStart/dueAsOf tracer</name>
  <reversibility rating="one-way">D-03 CHECK + D-09 unique cycle key on SQLite; undoing needs migration and Phase 20–21 rewrites.</reversibility>
  <files>prisma/schema.prisma, prisma/migrations/, src/lib/credit-grace.ts, src/lib/credit-grace.test.ts</files>
  <read_first>
    .planning/phases/19-schema-pure-grace-domain-math/19-CONTEXT.md
    .planning/phases/19-schema-pure-grace-domain-math/19-RESEARCH.md
    .planning/phases/19-schema-pure-grace-domain-math/19-PATTERNS.md
    prisma/schema.prisma
    prisma/migrations/20260903005200_account_credit_limit_check/migration.sql
    src/lib/dates.ts
    src/lib/income.ts
    src/lib/income.test.ts
  </read_first>
  <behavior>
    - cycleStartAsOf(2026, 1, 21) === 2026-01-21; dueAsOfForCycle(2026-01-21, 15) === 2026-02-15 (D-09, D-10, CYCLE-01)
    - cycleStartAsOf(2025, 2, 31) === 2025-02-28; dueAsOfForCycle(2025-02-28, 15) === 2025-03-15 (statement clamp)
    - schema has statementDayOfMonth + dueDayOfMonth on Account; CreditGraceObligation with amountMinor BigInt, dueAsOf frozen field, GraceObligationStatus OPEN|CLOSED, closedAsOf optional, Cascade FK, @@unique([accountId, cycleStartAsOf]) (D-01, D-05…D-09, D-15, D-16)
    - credit-grace.ts imports clampDayOfMonth only from dates; no Prisma client imports
  </behavior>
  <action>
    After checkpoint selects dual-dom-unique (per D-03, D-09): run npm install from package-lock if node_modules missing — restore pins only; add zero new packages. Extend prisma/schema.prisma Account with statementDayOfMonth Int? and dueDayOfMonth Int? only (D-01 dual-DOM SoT; omit duration-days / grace-anchor columns). Add creditGraceObligations CreditGraceObligation[] relation. Add enum GraceObligationStatus OPEN CLOSED (D-07). Add model CreditGraceObligation: accountId FK onDelete Cascade (D-15), cycleStartAsOf String, dueAsOf String (frozen at create per D-05), amountMinor BigInt required (D-06), status default OPEN, closedAsOf String?, note String?, timestamps, @@unique([accountId, cycleStartAsOf]) (D-09, D-16). No currency FK on obligation — inherit Account.currencyCode (D-08). Generate migration non-interactively named credit_grace_dual_dom (migrate dev --create-only or equivalent). Edit migration.sql with RedefineTables twin of Account_credit_limit_check: keep Account_credit_limit_invariant; add Account_grace_dom_invariant enforcing FIAT_CREDIT-only dual DOM and both-null-or-both with DOM BETWEEN 1 AND 31 (D-02, D-03); create CreditGraceObligation with ON DELETE CASCADE and unique index; add CHECK that OPEN implies closedAsOf null and CLOSED implies closedAsOf set. Implement src/lib/credit-grace.ts pure exports cycleStartAsOf(year, month1to12, statementDayOfMonth) via clampDayOfMonth and dueAsOfForCycle(cycleStart, dueDayOfMonth) advancing to next calendar month then clampDayOfMonth (D-09, D-10) — never use sole day-shift as the due engine. Isolation header: pure TypeScript; no Prisma; do not import net-worth or historical-series. Create src/lib/credit-grace.test.ts Vitest matrix for 21→15 and Feb-31 statement clamp cases above. Do not implement listCycleWindows / resolveCurrentAndNext / isGraceOverdue / Server Actions / UI routes here (Plans 02–03). Do not touch snapshot ledger writers.
  </action>
  <verify>
    <automated>test -d node_modules || npm install; npx vitest run src/lib/credit-grace.test.ts &amp;&amp; grep -q 'function cycleStartAsOf' src/lib/credit-grace.ts &amp;&amp; grep -q 'function dueAsOfForCycle' src/lib/credit-grace.ts &amp;&amp; grep -q 'statementDayOfMonth' prisma/schema.prisma &amp;&amp; grep -q 'dueDayOfMonth' prisma/schema.prisma &amp;&amp; grep -q 'model CreditGraceObligation' prisma/schema.prisma &amp;&amp; grep -q 'GraceObligationStatus' prisma/schema.prisma &amp;&amp; ls prisma/migrations/*credit_grace_dual_dom*/migration.sql &amp;&amp; grep -q 'Account_grace_dom_invariant' prisma/migrations/*credit_grace_dual_dom*/migration.sql &amp;&amp; grep -q 'Account_credit_limit_invariant' prisma/migrations/*credit_grace_dual_dom*/migration.sql</automated>
    <fails_when>vitest non-zero, cycleStartAsOf/dueAsOfForCycle missing, dual DOM or CreditGraceObligation/GraceObligationStatus absent from schema, credit_grace_dual_dom migration.sql missing, or either Account CHECK name absent from migration SQL</fails_when>
  </verify>
  <acceptance_criteria>
    - prisma/schema.prisma defines statementDayOfMonth, dueDayOfMonth, CreditGraceObligation, GraceObligationStatus with Cascade + unique cycle key (D-01, D-05…D-09, D-15, D-16)
    - migration SQL contains Account_grace_dom_invariant and retains Account_credit_limit_invariant (D-02, D-03)
    - cycleStartAsOf / dueAsOfForCycle green for 2026-01-21→2026-02-15 and Feb clamp path (D-09, D-10, CYCLE-01)
    - No grace UI routes under src/app created this task
  </acceptance_criteria>
  <done>Tracer path green: dual-DOM schema + Cascade obligation + next-month due math; migrate deploy is next task</done>
</task>

<task type="auto">
  <name>[BLOCKING] Host prisma migrate deploy + foundation CreditGraceObligation allow-list</name>
  <files>data/wallet.db, prisma/migrations/, src/lib/foundation.test.ts</files>
  <read_first>
    .planning/phases/19-schema-pure-grace-domain-math/19-RESEARCH.md
    .planning/phases/19-schema-pure-grace-domain-math/19-PATTERNS.md
    prisma/schema.prisma
    src/lib/foundation.test.ts
  </read_first>
  <precondition>prisma/migrations contains the new credit_grace_dual_dom migration.sql from the tracer task; node_modules present after npm install</precondition>
  <reversibility rating="costly">Host migrate applies dual DOM + obligation table permanently; dropping CHECK/unique later needs another migration (D-03, D-09 costly undo).</reversibility>
  <action>
    If host SQLite is locked by wallet-web, stop the container briefly. Run DATABASE_URL=file:./data/wallet.db npx prisma migrate deploy (not db push). Confirm migrate status reports up to date. Update src/lib/foundation.test.ts sqlite_master IN-list and expected sorted table array to include CreditGraceObligation alongside existing tables (Currency, Account, FxRate, Person, Debt, income models, _prisma_migrations). Assert applied migrations include a name containing credit_grace_dual_dom. Run npx prisma generate if client types lag. Do not COPY wallet.db into Docker images. Do not add npm packages.
  </action>
  <verify>
    <automated>mkdir -p data &amp;&amp; DATABASE_URL="file:./data/wallet.db" npx prisma migrate deploy &amp;&amp; test -f data/wallet.db &amp;&amp; DATABASE_URL="file:./data/wallet.db" npx prisma migrate status 2&gt;&amp;1 | grep -qi 'up to date\|Database schema is up to date\|No pending migrations' &amp;&amp; npx vitest run src/lib/foundation.test.ts</automated>
    <fails_when>migrate deploy non-zero, data/wallet.db missing, pending migrations remain, or foundation.test.ts fails CreditGraceObligation / credit_grace_dual_dom expectations</fails_when>
  </verify>
  <acceptance_criteria>
    - DATABASE_URL=file:./data/wallet.db npx prisma migrate deploy exits 0
    - foundation.test.ts expects CreditGraceObligation table and credit_grace_dual_dom migration name
    - prisma migrate status reports no pending migrations
  </acceptance_criteria>
  <done>Blocking host migrate deploy succeeded; foundation proves CreditGraceObligation live</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Schema/migration → SQLite file | Dual DOM CHECKs + Cascade obligation FK; corrupt schedule breaks CYCLE-01 |
| Pure credit-grace helpers → callers | Untrusted DOM ints / date strings cross into cycle math |
| Grace module → snapshot ledger / NW | Isolation; coupling would corrupt capital history (GRISO later) |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-19-01 | Tampering | Account dual DOM / CHECK | high | mitigate | Account_grace_dom_invariant + FIAT_CREDIT-only both-null-or-both (D-02, D-03); Zod in Plan 03 |
| T-19-02 | Tampering | Obligation without schedule | medium | mitigate | Document reject in helpers/actions (D-13); create blocked when DOM null — Plan 03 Zod + Phase 20 CRUD |
| T-19-03 | Tampering | Snapshot mutation via grace | high | mitigate | Pure lib isolation; no snapshot writes from grace code; NW/historical-series stay grace-free |
| T-19-04 | Elevation of Privilege | Cascade account delete | medium | accept | Locked D-15 — Cascade wipes obligations; document vs Restrict; RU chrome Phase 20 |
| T-19-05 | Tampering | Duplicate cycle rows | medium | mitigate | @@unique([accountId, cycleStartAsOf]) (D-09, D-16) |
| T-19-SC | Tampering | npm installs | high | mitigate | Zero new packages — no install legitimacy checkpoint required |
</threat_model>

<verification>
npx vitest run src/lib/credit-grace.test.ts src/lib/foundation.test.ts green; migrate deploy up to date; dual DOM + CreditGraceObligation present.
</verification>

<success_criteria>
Account stores dual DOM; obligation model keyed by cycle start with Cascade; dueAsOfForCycle is next-month clamp; host migrate green; no UI chrome.
</success_criteria>

<output>
Create `.planning/phases/19-schema-pure-grace-domain-math/19-01-SUMMARY.md` when done
</output>
