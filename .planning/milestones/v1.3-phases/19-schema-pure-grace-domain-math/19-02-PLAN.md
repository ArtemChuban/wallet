---
phase: 19-schema-pure-grace-domain-math
plan: 02
type: execute
wave: 2
depends_on:
  - "19-01"
files_modified:
  - src/lib/credit-grace.ts
  - src/lib/credit-grace.test.ts
autonomous: true
requirements:
  - CYCLE-01
estimate:
  tokens: 25000
  raw_tokens: 50000
  tasks: 2
  confidence: high
must_haves:
  truths:
    - listCycleWindows(schedule, from, to) emits { cycleStartAsOf, dueAsOf } candidates via month walk + clamp; no Prisma writes (D-11, D-12, CYCLE-01)
    - Both DOM null ⇒ listCycleWindows / resolveCurrentAndNext return empty (D-13, empty probe)
    - resolveCurrentAndNext(schedule, today) is deterministic for on-cycle, gap-day, and post-due cases (RESEARCH Pattern 3; Claude discretion locked in tests)
    - isGraceOverdue(dueAsOf, today) is false on inclusive due day and true the calendar day after (D-07; Phase 18 D-04)
    - "adjacency: from===to on a cycleStartAsOf yields one window; two abutting months yield two separate windows ordered ascending"
    - "ordering: listCycleWindows sorted ascending by cycleStartAsOf; stable for equal compare keys (unique identity is D-16)"
    - Editing Account DOM later does not rewrite stored obligation dues — helpers are candidates only (D-04, D-05 documented via pure API)
  prohibitions:
    - statement: MUST NOT auto-create obligation rows from helpers
      requirement_id: CYCLE-01
      category: isolation
    - statement: MUST NOT use sole day-shift as due engine
      requirement_id: CYCLE-01
      category: values
    - statement: MUST NOT import Prisma or NW/historical-series modules into credit-grace.ts
      requirement_id: CYCLE-01
      category: isolation
  artifacts:
    - path: src/lib/credit-grace.ts
      provides: listCycleWindows, resolveCurrentAndNext, isGraceOverdue (+ schedule type)
    - path: src/lib/credit-grace.test.ts
      provides: null-empty, 21→15 windows, gap-day, overdue inclusive, adjacency/ordering matrix
  key_links:
    - from: listCycleWindows
      to: cycleStartAsOf / dueAsOfForCycle
      via: month walk like listRecurringOccurrences
      pattern: "listCycleWindows"
    - from: isGraceOverdue
      to: injected today
      via: dueAsOf less-than today string compare
      pattern: "isGraceOverdue"
---

## Specless probe fallback

Adjacency / empty / ordering predicates implemented as Vitest cases in this plan (see must_haves.truths).

<objective>
Expand credit-grace pure API with listCycleWindows, resolveCurrentAndNext, and isGraceOverdue — candidates only, null schedule empty, overdue calendar not status (D-04, D-07, D-11, D-12, D-13, CYCLE-01).

Purpose: Hand Phase 20 cycle list + overdue highlight a deterministic pure surface.
Output: Full credit-grace.ts API + green Vitest matrix including specless probes.
</objective>

<execution_context>
@/home/artem/Documents/wallet/.cursor/gsd-core/workflows/execute-plan.md
@/home/artem/Documents/wallet/.cursor/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/19-schema-pure-grace-domain-math/19-CONTEXT.md
@.planning/phases/19-schema-pure-grace-domain-math/19-RESEARCH.md
@.planning/phases/19-schema-pure-grace-domain-math/19-PATTERNS.md
@.planning/phases/19-schema-pure-grace-domain-math/19-01-PLAN.md
@src/lib/credit-grace.ts
@src/lib/credit-grace.test.ts
@src/lib/income.ts
@src/lib/dates.ts
</context>

<assumptions>
- Gap-day semantics follow RESEARCH Pattern 3 table (current none or last; next = M+1) — lock exact returns in tests (Claude discretion).
- Specless probes: adjacency/empty/ordering covered by Vitest; no SPEC descriptors.
</assumptions>

## Artifacts this phase produces

- `listCycleWindows(schedule, from, to)`
- `resolveCurrentAndNext(schedule, today)`
- `isGraceOverdue(dueAsOf, today)`
- Schedule input type `{ statementDayOfMonth: number; dueDayOfMonth: number } | null` (or equivalent both-set / null)
- Expanded `src/lib/credit-grace.test.ts` matrix

<tasks>

<task type="auto" tdd="true">
  <name>listCycleWindows + resolveCurrentAndNext candidates</name>
  <reversibility rating="costly">Pure window API becomes Phase 20/21 contract; renaming return fields later touches UI + forecast (D-11).</reversibility>
  <files>src/lib/credit-grace.ts, src/lib/credit-grace.test.ts</files>
  <read_first>
    .planning/phases/19-schema-pure-grace-domain-math/19-CONTEXT.md
    .planning/phases/19-schema-pure-grace-domain-math/19-RESEARCH.md
    .planning/phases/19-schema-pure-grace-domain-math/19-PATTERNS.md
    src/lib/credit-grace.ts
    src/lib/credit-grace.test.ts
    src/lib/income.ts
  </read_first>
  <behavior>
    - null / both-unset schedule → listCycleWindows returns [] (D-13, empty probe)
    - schedule 21/15, from 2026-01-01 to 2026-02-28 includes window cycleStart 2026-01-21 due 2026-02-15 (D-10, D-11)
    - from===to===2026-01-21 yields exactly one window (adjacency)
    - two abutting months yield two windows ascending by cycleStartAsOf (ordering)
    - resolveCurrentAndNext deterministic for today on statement day, mid-cycle, gap after due before next statement (RESEARCH Pattern 3)
    - helpers never write Prisma / never invent obligation rows (D-12)
  </behavior>
  <action>
    Extend src/lib/credit-grace.ts: define a schedule type requiring both statementDayOfMonth and dueDayOfMonth when present. Export listCycleWindows(schedule, from, to) that returns [] when schedule is null/unset (D-13); otherwise walk months overlapping inclusive [from, to] (income monthsOverlapping pattern), emit { cycleStartAsOf, dueAsOf } via cycleStartAsOf + dueAsOfForCycle (D-09, D-10, D-11), include a window when its cycleStartAsOf falls in [from, to] (document in tests if due-only overlap differs — pick cycleStart membership and lock it). Sort ascending by cycleStartAsOf (ordering probe). Export resolveCurrentAndNext(schedule, today) with deterministic current/next per RESEARCH Pattern 3 gap-day table; empty when schedule null (D-13). Keep module pure — no Prisma, no auto-create rows (D-12). Do not recompute stored obligation dues — candidates only (D-04, D-05). Expand credit-grace.test.ts with null-empty, 21→15 range, from===to adjacency, ascending order, and at least one gap-day resolveCurrentAndNext case. Do not add UI or Server Actions.
  </action>
  <verify>
    <automated>npx vitest run src/lib/credit-grace.test.ts</automated>
    <fails_when>non-zero exit, failing assertion, or "0 passed" in the vitest summary</fails_when>
  </verify>
  <acceptance_criteria>
    - listCycleWindows and resolveCurrentAndNext exported from src/lib/credit-grace.ts (D-11)
    - null schedule → empty arrays/objects per tests (D-13)
    - 21→15 fixture window present; adjacency and ascending order cases green
    - No Prisma imports in credit-grace.ts (D-12)
  </acceptance_criteria>
  <done>Cycle window + current/next candidates green and deterministic</done>
</task>

<task type="auto" tdd="true">
  <name>isGraceOverdue + GRISO isolation smoke</name>
  <files>src/lib/credit-grace.ts, src/lib/credit-grace.test.ts</files>
  <read_first>
    .planning/phases/19-schema-pure-grace-domain-math/19-CONTEXT.md
    .planning/phases/19-schema-pure-grace-domain-math/19-PATTERNS.md
    src/lib/credit-grace.ts
    src/lib/income.ts
    src/lib/net-worth.ts
    src/lib/historical-series.ts
  </read_first>
  <behavior>
    - isGraceOverdue(2026-02-15, 2026-02-15) === false; isGraceOverdue(2026-02-15, 2026-02-16) === true (D-07)
    - today injected — helpers do not read the clock
    - net-worth.ts and historical-series.ts do not import credit-grace
  </behavior>
  <action>
    Export isGraceOverdue(dueAsOf, today) returning dueAsOf &lt; today via YYYY-MM-DD string compare (inclusive due day not overdue — D-07, Phase 18 D-04). Inject today; do not call calendarDateToday inside the helper. Add Vitest cases for on-due and day-after. Add a small file-scan test (in the grace test file) that net-worth and historical-series modules contain no import of the grace domain module path (GRISO prep / T-19-03). Do not modify net-worth.ts or historical-series.ts. Do not persist overdue as an enum value.
    <!-- planner-discipline-allow: credit-grace -->
  </action>
  <verify>
    <automated>npx vitest run src/lib/credit-grace.test.ts &amp;&amp; ! grep -E 'credit-grace' src/lib/net-worth.ts src/lib/historical-series.ts</automated>
    <fails_when>vitest non-zero, "0 passed" in summary, or grep finds credit-grace in net-worth.ts or historical-series.ts</fails_when>
  </verify>
  <acceptance_criteria>
    - isGraceOverdue false on due day, true next day (D-07)
    - Isolation: net-worth and historical-series stay free of credit-grace imports
    - Status enum remains OPEN|CLOSED only — overdue is calendar-derived
  </acceptance_criteria>
  <done>Overdue predicate + NW isolation smoke green</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Pure helpers ← schedule/DOM ints | Untrusted ints 1–31 and date strings |
| credit-grace → NW / historical-series | Must remain disconnected (T-19-03) |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-19-01 | Tampering | Partial/out-of-range DOM in helpers | medium | mitigate | Null schedule empty; Zod range Plan 03; clamp in cycle math |
| T-19-02 | Tampering | Client invents obligation via helpers | medium | mitigate | D-12 candidates only — no Prisma writes in credit-grace.ts |
| T-19-03 | Tampering | NW/historical import of grace | high | mitigate | Isolation smoke grep; no edits to NW modules |
| T-19-SC | Tampering | npm installs | high | mitigate | Zero new packages |
</threat_model>

<verification>
npx vitest run src/lib/credit-grace.test.ts green; NW/historical-series free of credit-grace imports.
</verification>

<success_criteria>
listCycleWindows / resolveCurrentAndNext / isGraceOverdue shipped; null empty; overdue after inclusive due; specless adjacency/empty/ordering tested.
</success_criteria>

<output>
Create `.planning/phases/19-schema-pure-grace-domain-math/19-02-SUMMARY.md` when done
</output>
