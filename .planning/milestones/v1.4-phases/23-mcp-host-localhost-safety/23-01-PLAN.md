---
phase: 23-mcp-host-localhost-safety
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - package.json
  - package-lock.json
  - src/lib/mcp/localhost-guard.test.ts
  - src/app/api/mcp/route.test.ts
  - .planning/phases/23-mcp-host-localhost-safety/23-VALIDATION.md
autonomous: false
requirements:
  - HOST-01
  - HOST-02
estimate:
  tokens: 20000
  raw_tokens: 40000
  tasks: 3
  confidence: high
must_haves:
  truths:
    - Human confirmed mcp-handler@^2 legitimacy and export shape before install (T-23-SC)
    - package.json lists mcp-handler@^2 and @modelcontextprotocol/server@^2 after install
    - Wave 0 Vitest stubs exist for localhost-guard and /api/mcp route smoke (D-15)
    - Wave 0 MCP test files exit 0 with todo/skip only (no hard-fail poison for later verifies)
  prohibitions:
    - statement: MUST NOT spawn sidecar or agent subprocess
      category: isolation
    - statement: MUST NOT widen Compose host publish beyond loopback bind
      category: scope
    - statement: MUST NOT register write-capable MCP tools
      category: scope
    - statement: MUST NOT set Edge runtime on MCP route
      category: isolation
    - statement: MUST NOT modify health readiness route this phase
      category: scope
    - statement: MUST NOT add DISOL INISO or GRISO isolation copy this phase
      category: scope
    - statement: MUST NOT register capital or side-ledger MCP tools
      category: scope
  artifacts:
    - path: package.json
      provides: mcp-handler and @modelcontextprotocol/server dependencies
    - path: src/lib/mcp/localhost-guard.test.ts
      provides: Wave 0 HOST-02 guard stub cases
    - path: src/app/api/mcp/route.test.ts
      provides: Wave 0 HOST-01 route smoke stubs with mocks
  key_links:
    - from: blocking-human SUS checkpoint
      to: npm install of mcp-handler
      via: human approve before install
      pattern: "checkpoint:human-verify"
    - from: 23-VALIDATION.md Wave 0 checklist
      to: stub test files
      via: Nyquist file-exists rows
      pattern: "localhost-guard.test.ts"
---

## Specless probe fallback

SPEC absent. `must_haves.truths` authored from ROADMAP success criteria + CONTEXT D-01…D-16. Prohibitions are descriptor-less anti-goals for this phase.

## Phase Goal

Running wallet exposes in-process Streamable HTTP MCP on localhost only — same Next lifecycle, no sidecar (ROADMAP Phase 23 / HOST-01, HOST-02).

<objective>
Clear mcp-handler SUS supply-chain gate (T-23-SC), install pinned MCP packages, and land Wave 0 Vitest stubs for localhost-guard + /api/mcp route smoke (D-15, HOST-01, HOST-02). Leave `/api/health` untouched this plan (D-16).

Purpose: Unblock Plan 02 tracer without red-poisoning the suite; prove legitimacy before deps land.
Output: Approved install of mcp-handler@^2 + @modelcontextprotocol/server@^2; stub test files; VALIDATION Wave 0 rows marked present.
</objective>

<execution_context>
@/home/artem/Documents/wallet/.cursor/gsd-core/workflows/execute-plan.md
@/home/artem/Documents/wallet/.cursor/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/OPERATOR.md
@.planning/phases/23-mcp-host-localhost-safety/23-CONTEXT.md
@.planning/phases/23-mcp-host-localhost-safety/23-RESEARCH.md
@.planning/phases/23-mcp-host-localhost-safety/23-PATTERNS.md
@.planning/phases/23-mcp-host-localhost-safety/23-VALIDATION.md
@package.json
@src/app/api/health/route.test.ts
@src/lib/account-type.test.ts
</context>

<assumptions>
## Planner assumptions

- A1 (RESEARCH): Missing Host port rejected when listen PORT is not 80/443 — Plan 02 implements; Wave 0 stubs name the case as it.todo.
- A2 (RESEARCH): Reject body `{ error: "Forbidden", reason: "bad_host" | "bad_origin" }` — Plan 02.
- A3 (RESEARCH): NFT includes for MCP packages only if standalone miss after first build — Plan 02 discretionary.
- A4 (RESEARCH): serverInfo.version "1.4.0" cosmetic — Plan 02.
- mcp-handler remains install-only until upstream forwards responseMode; implement create path with SDK in Plan 02 (D-08 intent).
</assumptions>

## Artifacts this phase produces

- `src/lib/mcp/localhost-guard.ts` + `localhost-guard.test.ts` (HOST-02)
- `src/lib/mcp/create-handler.ts` (SDK `createMcpHandler` + `responseMode: "json"`)
- `src/lib/mcp/tools/wallet-ping.ts` (`wallet_ping` only — D-05, D-06)
- `src/app/api/mcp/route.ts` + `route.test.ts` (thin guarded GET/POST/DELETE — D-08)
- `package.json` / lock: `mcp-handler@^2`, `@modelcontextprotocol/server@^2`
- Optional `next.config.ts` NFT includes only if standalone miss
- Compose publish unchanged (`127.0.0.1:3000:3000`) — UAT checklist only (D-14)
- Health route untouched (D-16)

## Artifacts this plan produces

- Human SUS approval for mcp-handler@^2
- Locked deps in package.json / package-lock.json
- Wave 0 stubs: `src/lib/mcp/localhost-guard.test.ts`, `src/app/api/mcp/route.test.ts`
- Updated `23-VALIDATION.md` Wave 0 file-exists rows

<tasks>

<task type="checkpoint:human-verify" gate="blocking-human">
  <name>Confirm mcp-handler SUS legitimacy + export shape before install</name>
  <read_first>
    .planning/phases/23-mcp-host-localhost-safety/23-RESEARCH.md
    .planning/phases/23-mcp-host-localhost-safety/23-CONTEXT.md
  </read_first>
  <what-built>Supply-chain gate before installing mcp-handler@^2 (Package Legitimacy SUS too-new; threat T-23-SC). @modelcontextprotocol/server@^2 is RESEARCH-OK and rides with the same install after approval.</what-built>
  <how-to-verify>
    1. Open https://www.npmjs.com/package/mcp-handler — confirm official Vercel package (github.com/vercel/mcp-handler), latest 2.x lineage, high weekly downloads
    2. Open https://www.npmjs.com/package/@modelcontextprotocol/server — confirm official MCP TS SDK server package ^2
    3. Spot-check export shape: mcp-handler documents Next Route Handler helper; RESEARCH already verified 2.1.1 create path lacks responseMode — confirm you accept install-for-peer/docs while Plan 02 implements via @modelcontextprotocol/server createMcpHandler + .fetch
    4. Reply approved with pins mcp-handler@^2 and @modelcontextprotocol/server@^2
  </how-to-verify>
  <action>
    Halt until a human confirms the RESEARCH Package Legitimacy Audit for mcp-handler ([SUS] too-new) before any npm install of MCP packages (HOST-01, T-23-SC). Human opens npmjs.com/package/mcp-handler and npmjs.com/package/@modelcontextprotocol/server, confirms official publishers (Vercel / Model Context Protocol), and acknowledges Plan 02 will mount SDK createMcpHandler.fetch with responseMode json because mcp-handler 2.1.1 does not forward that option (D-08 intent via RESEARCH). Record confirmation in the resume signal. Do not run npm install in this task.
  </action>
  <resume-signal>Reply: approved mcp-handler@^2 @modelcontextprotocol/server@^2 (install-only mcp-handler; SDK create path for responseMode)</resume-signal>
  <verify>
    <automated>test -f .planning/phases/23-mcp-host-localhost-safety/23-RESEARCH.md &amp;&amp; grep -v '^#' .planning/phases/23-mcp-host-localhost-safety/23-RESEARCH.md | grep -c 'mcp-handler' | grep -q '[1-9]'</automated>
    <fails_when>RESEARCH missing or mcp-handler token absent outside markdown headings</fails_when>
  </verify>
  <acceptance_criteria>
    - Human explicitly approved mcp-handler@^2 and @modelcontextprotocol/server@^2
    - Human acknowledged SDK create path for responseMode while mcp-handler remains installed
  </acceptance_criteria>
  <done>Package legitimacy gate passed; executor may install pinned MCP packages</done>
</task>

<task type="auto">
  <name>Wave 0 Nyquist stubs — localhost-guard + mcp route smoke</name>
  <files>src/lib/mcp/localhost-guard.test.ts, src/app/api/mcp/route.test.ts, .planning/phases/23-mcp-host-localhost-safety/23-VALIDATION.md</files>
  <read_first>
    .planning/phases/23-mcp-host-localhost-safety/23-VALIDATION.md
    .planning/phases/23-mcp-host-localhost-safety/23-RESEARCH.md
    .planning/phases/23-mcp-host-localhost-safety/23-PATTERNS.md
    src/app/api/health/route.test.ts
    src/lib/account-type.test.ts
  </read_first>
  <behavior>
    - localhost-guard.test.ts names D-15 matrix cases as it.todo (or empty describe with it.todo): good Host 127.0.0.1/localhost/[::1] with PORT, evil host, wrong port, evil Origin, missing Origin OK (HOST-02)
    - route.test.ts names mock-handler smoke as it.todo: good Host calls fetch; bad Host returns 403 reason bad_host and does not call fetch (HOST-01, HOST-02, D-15)
    - Both files exit 0 under vitest with zero hard-failing expects until Plan 02 greens them
  </behavior>
  <action>
    Create src/lib/mcp/localhost-guard.test.ts and src/app/api/mcp/route.test.ts as Wave 0 Nyquist stubs (D-15). Follow health route.test.ts / account-type.test.ts Vitest style. Use it.todo (or describe with only it.todo) for RESEARCH matrix cases and route mock smoke — suite must exit 0. Do not implement production guard/handler/route here (Plan 02). Do not touch docker-compose.yml. Leave src/app/api/health/route.ts unchanged (D-16). Update 23-VALIDATION.md Wave 0 checkboxes and Per-Task Verification Map file-exists columns for these two stub paths; set task IDs 23-01-02 (stubs) and leave Plan 02 rows for greening.
  </action>
  <verify>
    <automated>test -f src/lib/mcp/localhost-guard.test.ts &amp;&amp; test -f src/app/api/mcp/route.test.ts &amp;&amp; npx vitest run src/lib/mcp/localhost-guard.test.ts src/app/api/mcp/route.test.ts</automated>
    <fails_when>missing Wave 0 test files, vitest non-zero (including hard-fail stubs), or "0 passed" with unexpected failures</fails_when>
  </verify>
  <acceptance_criteria>
    - Both Wave 0 test files exist under src/lib/mcp and src/app/api/mcp
    - vitest run on those files exits 0 with todo/skip only
    - 23-VALIDATION.md Wave 0 stub checkboxes reflect files present
  </acceptance_criteria>
  <done>Wave 0 stubs present and green (todo-only); Plan 02 may convert to real expects</done>
</task>

<task type="auto">
  <name>Install mcp-handler@^2 and @modelcontextprotocol/server@^2</name>
  <reversibility rating="costly">MCP package pins become milestone baseline; later phases import server SDK — coordinated dep bump to undo.</reversibility>
  <files>package.json, package-lock.json</files>
  <read_first>
    .planning/phases/23-mcp-host-localhost-safety/23-RESEARCH.md
    .planning/phases/23-mcp-host-localhost-safety/23-CONTEXT.md
    package.json
  </read_first>
  <precondition>Human resume-signal approved mcp-handler@^2 and @modelcontextprotocol/server@^2 from the SUS legitimacy checkpoint</precondition>
  <action>
    After SUS checkpoint approval (T-23-SC, HOST-01): run npm install mcp-handler@^2 @modelcontextprotocol/server@^2 from repo root so both land in dependencies and lockfile. Do not import or wire create paths yet (Plan 02). Do not add other packages. Do not edit next.config.ts yet. Do not change docker-compose.yml ports or health route.
  </action>
  <verify>
    <automated>node -e "const p=require('./package.json'); const d=p.dependencies||{}; if(!d['mcp-handler']||!String(d['mcp-handler']).includes('2')) process.exit(1); if(!d['@modelcontextprotocol/server']||!String(d['@modelcontextprotocol/server']).includes('2')) process.exit(1);" &amp;&amp; test -d node_modules/mcp-handler &amp;&amp; test -d node_modules/@modelcontextprotocol/server</automated>
    <fails_when>package.json missing either dep at ^2, or node_modules packages absent</fails_when>
  </verify>
  <acceptance_criteria>
    - package.json dependencies include mcp-handler and @modelcontextprotocol/server at major 2
    - node_modules contains both packages
  </acceptance_criteria>
  <done>MCP deps installed; Plan 02 tracer may import SDK createMcpHandler</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| npm registry → local node_modules | Untrusted package tarballs cross into the app supply chain |
| Future client → `/api/mcp` (Plan 02) | Untrusted Host/Origin headers will cross into Next process |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-23-01 | Spoofing | Host/Origin (DNS rebinding) | high | mitigate | Plan 02 localhost-guard hostname allowlist (D-01, D-02); Wave 0 stubs reserve cases |
| T-23-02 | Information Disclosure | Compose publish / LAN | high | mitigate | Leave docker-compose.yml `127.0.0.1:3000:3000`; forbid host bind widen (HOST-02, D-14) |
| T-23-03 | Spoofing | Host port vs PORT | high | mitigate | Plan 02 PORT equality (D-03); Wave 0 wrong-port it.todo |
| T-23-04 | Spoofing | CSRF foreign Origin | high | mitigate | Plan 02 reject non-loopback Origin even when Host good (D-02) |
| T-23-05 | Elevation of Privilege | CORS widen on `/api/mcp` | medium | mitigate | No Access-Control-* headers (D-12); Plan 02 route must not add CORS |
| T-23-SC | Tampering | npm install mcp-handler | high | mitigate | blocking-human Package Legitimacy checkpoint before install; pin ^2; server package OK |
</threat_model>

<verification>
Wave 0 vitest stubs exit 0; package.json shows both MCP deps; SUS checkpoint recorded approved.
</verification>

<success_criteria>
mcp-handler legitimacy approved; deps installed; Wave 0 stub files exist and do not fail the suite.
</success_criteria>

<output>
Create `.planning/phases/23-mcp-host-localhost-safety/23-01-SUMMARY.md` when done
</output>
