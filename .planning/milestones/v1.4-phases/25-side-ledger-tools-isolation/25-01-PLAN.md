---
phase: 25-side-ledger-tools-isolation
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/lib/mcp/tools/debts.test.ts
  - src/lib/mcp/tools/income.test.ts
  - src/lib/mcp/tools/grace.test.ts
  - src/lib/mcp/tools/forecast.test.ts
  - src/lib/mcp/as-of.ts
  - src/lib/mcp/as-of.test.ts
  - src/lib/disol.test.ts
  - .planning/phases/25-side-ledger-tools-isolation/25-VALIDATION.md
autonomous: true
requirements:
  - SIDE-01
  - SIDE-02
  - SIDE-03
  - SIDE-04
estimate:
  tokens: 20000
  raw_tokens: 40000
  tasks: 3
  confidence: high
must_haves:
  truths:
    - Wave 0 SIDE tool test stubs exist under src/lib/mcp/tools/ and vitest exits 0 with todo/skip only (SIDE-01…04 scaffold)
    - optionalHorizonEndSchema accepts YYYY-MM-DD and rejects garbage with English message (D-02 prep)
    - disol walls cover MCP NW loader + get_net_worth tool paths (D-05 start)
  prohibitions:
    - statement: MUST NOT install new npm packages this phase
      category: scope
    - statement: MUST NOT register SIDE MCP tools yet
      category: scope
    - statement: MUST NOT change prisma schema
      category: scope
    - statement: MUST NOT add CI mutate-import ban under mcp path
      category: scope
    - statement: MUST NOT widen localhost Host Origin policy
      category: isolation
  artifacts:
    - path: src/lib/mcp/tools/debts.test.ts
      provides: Wave 0 SIDE-01 stub
    - path: src/lib/mcp/tools/income.test.ts
      provides: Wave 0 SIDE-02 stub
    - path: src/lib/mcp/tools/grace.test.ts
      provides: Wave 0 SIDE-03 stub
    - path: src/lib/mcp/tools/forecast.test.ts
      provides: Wave 0 SIDE-04 stub
    - path: src/lib/mcp/as-of.ts
      provides: optionalHorizonEndSchema for forecast / date-pair helpers
    - path: src/lib/disol.test.ts
      provides: MCP NW path debts-import walls
  key_links:
    - from: optionalHorizonEndSchema
      to: get_forecast_overlay (Plan 02)
      via: zod YYYY-MM-DD optional wire field
      pattern: "optionalHorizonEndSchema"
    - from: disol.test.ts
      to: load-net-worth-asof.ts / net-worth.ts
      via: readFileSync import wall
      pattern: "@/lib/debts"
---

## Specless probe fallback

SPEC absent. Truths from ROADMAP Phase 25 + CONTEXT D-01…D-08 + RESEARCH Wave 0 map.

## Phase Goal

Agent can read Долги / Доходы / Грейс / forecast overlay without folding side ledgers into historical NW.

<objective>
Land Wave 0 Nyquist stubs for all four SIDE tool suites, extend as-of with optionalHorizonEndSchema (and paired from/to date helpers for list_income), and extend disol walls to MCP NW paths so Plan 02 tracer can register get_forecast_overlay without inventing date schemas or missing DISOL coverage (D-02, D-05).

Purpose: Shared date adapters + green stub suite before any SIDE registerTool.
Output: four tools/*.test.ts stubs; as-of horizon schema green; disol MCP walls; VALIDATION Wave 0 rows updated.
</objective>

<execution_context>
@/home/artem/Documents/wallet/.cursor/gsd-core/workflows/execute-plan.md
@/home/artem/Documents/wallet/.cursor/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/25-side-ledger-tools-isolation/25-CONTEXT.md
@.planning/phases/25-side-ledger-tools-isolation/25-RESEARCH.md
@.planning/phases/25-side-ledger-tools-isolation/25-PATTERNS.md
@.planning/phases/25-side-ledger-tools-isolation/25-VALIDATION.md
@src/lib/mcp/as-of.ts
@src/lib/mcp/as-of.test.ts
@src/lib/disol.test.ts
@src/lib/mcp/tools/accounts.test.ts
</context>

<assumptions>
## Planner assumptions

- A1 (RESEARCH): optional includeClosed on list_debts default false — Plans 03.
- A2 (RESEARCH): list_income page-parity default + optional paired from/to — Plan 04; schema helpers land here.
- A3: openWorldHint false with readOnlyHint on SIDE tools (Plans 02–04).
- No new npm packages; vitest/zod/MCP SDK already installed.
- No prisma schema changes this phase.
</assumptions>

## Artifacts this plan produces

- Wave 0 stubs: `tools/{debts,income,grace,forecast}.test.ts`
- `optionalHorizonEndSchema` (+ optional paired date helpers) in `as-of.ts`
- Extended `disol.test.ts` MCP NW walls
- VALIDATION.md Wave 0 file-exists updates

<tasks>

<task type="auto" tdd="true">
  <name>Wave 0 Nyquist stubs for SIDE tool suites</name>
  <files>src/lib/mcp/tools/debts.test.ts, src/lib/mcp/tools/income.test.ts, src/lib/mcp/tools/grace.test.ts, src/lib/mcp/tools/forecast.test.ts, .planning/phases/25-side-ledger-tools-isolation/25-VALIDATION.md</files>
  <behavior>
    - Each SIDE tool test file exists and runs under vitest with it.todo or describe.skip only (exit 0)
    - No hard-fail expects until later plans fill behaviors
  </behavior>
  <action>
    Create four colocated Wave 0 stub test files under src/lib/mcp/tools/ mirroring Phase 24 CAP stub style: describe blocks naming SIDE-01…SIDE-04 owners, each with it.todo cases for debts totals+OPEN / income overdue+no-write / grace OPEN+CTA / forecast sparse series+default horizon (D-01…D-05 scaffold). Do not implement tools or register yet. Update 25-VALIDATION.md Wave 0 checklist and per-task File Exists column to present for stub paths. No package installs. No prisma schema edits.
  </action>
  <verify>
    <automated>test -f src/lib/mcp/tools/debts.test.ts &amp;&amp; test -f src/lib/mcp/tools/income.test.ts &amp;&amp; test -f src/lib/mcp/tools/grace.test.ts &amp;&amp; test -f src/lib/mcp/tools/forecast.test.ts &amp;&amp; npx vitest run src/lib/mcp/tools/debts.test.ts src/lib/mcp/tools/income.test.ts src/lib/mcp/tools/grace.test.ts src/lib/mcp/tools/forecast.test.ts</automated>
    <fails_when>any stub file missing, vitest non-zero, or hard-fail assertion poison (unexpected failed expects)</fails_when>
  </verify>
  <done>Four SIDE tool stub tests exist and vitest exits 0 with todo/skip only.</done>
</task>

<task type="auto" tdd="true">
  <name>as-of optionalHorizonEndSchema + paired from/to helpers (D-02)</name>
  <files>src/lib/mcp/as-of.ts, src/lib/mcp/as-of.test.ts</files>
  <behavior>
    - optionalHorizonEndSchema accepts YYYY-MM-DD; rejects garbage with English message horizonEnd must be YYYY-MM-DD
    - Existing optionalAsOfSchema / resolveAsOf behavior unchanged
    - Export a reusable YYYY-MM-DD string schema (or paired optional from/to object refine) for list_income Plan 04 — both required together when either set
  </behavior>
  <action>
    Per D-02 and PATTERNS as-of analog: add optionalHorizonEndSchema as zod optional string regex ^\d{4}-\d{2}-\d{2}$ with English message "horizonEnd must be YYYY-MM-DD". Keep resolveAsOf unchanged — default horizon math (today+365) stays in forecast tool/loader per D-04, not here. Add a small helper or zod refine for optional paired from+to (both YYYY-MM-DD, both required together) for list_income. Cover new cases in as-of.test.ts. No CAP tool registration changes.
  </action>
  <verify>
    <automated>grep -q 'optionalHorizonEndSchema' src/lib/mcp/as-of.ts &amp;&amp; npx vitest run src/lib/mcp/as-of.test.ts</automated>
    <fails_when>optionalHorizonEndSchema marker missing, vitest non-zero, or "0 passed" in summary</fails_when>
  </verify>
  <done>Horizon + paired date wire schemas green; resolveAsOf still Europe/Moscow today.</done>
</task>

<task type="auto" tdd="true">
  <name>Extend disol walls to MCP NW paths (D-05)</name>
  <files>src/lib/disol.test.ts</files>
  <behavior>
    - load-net-worth-asof.ts and tools/net-worth.ts appear in disol import-wall file list
    - Existing walls for net-worth.ts / historical-series / page.tsx remain
  </behavior>
  <action>
    Per D-05 and RESEARCH isolation sketch: extend the disol.test.ts for-loop file list with src/lib/mcp/reads/load-net-worth-asof.ts and src/lib/mcp/tools/net-worth.ts so MCP CAP NW paths stay debts-domain-free. Do not add SIDE tool files to this wall (list_debts legitimately imports debts). Keep existing asserts. Run disol suite green.
  </action>
  <verify>
    <automated>grep -q 'load-net-worth-asof' src/lib/disol.test.ts &amp;&amp; grep -q 'mcp/tools/net-worth' src/lib/disol.test.ts &amp;&amp; npx vitest run src/lib/disol.test.ts</automated>
    <fails_when>MCP NW wall paths missing from disol.test.ts, or vitest non-zero</fails_when>
  </verify>
  <done>DISOL walls cover CAP MCP NW loader + tool; suite green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| MCP date wire → zod schemas | Agent-supplied horizonEnd / from / to strings |
| CAP NW MCP sources → debts domain | DISOL wall must hold |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-25-01 | Tampering | as-of date schemas | medium | mitigate | Strict YYYY-MM-DD zod; English messages; no silent Date parse |
| T-25-02 | Elevation of Privilege | disol MCP walls | high | mitigate | Extend import walls to load-net-worth-asof + net-worth tool (D-05) |
| T-25-03 | Information Disclosure | Wave 0 stubs | low | accept | Stubs only; no live PII paths yet |
| T-25-SC | Tampering | npm installs | high | accept | No new packages this plan |
</threat_model>

<verification>
npx vitest run src/lib/mcp/tools/debts.test.ts src/lib/mcp/tools/income.test.ts src/lib/mcp/tools/grace.test.ts src/lib/mcp/tools/forecast.test.ts src/lib/mcp/as-of.test.ts src/lib/disol.test.ts
</verification>

<success_criteria>
Four SIDE stubs green; optionalHorizonEndSchema exported; disol covers MCP NW paths; VALIDATION Wave 0 updated.
</success_criteria>

<output>
Create `.planning/phases/25-side-ledger-tools-isolation/25-01-SUMMARY.md` when done
</output>
