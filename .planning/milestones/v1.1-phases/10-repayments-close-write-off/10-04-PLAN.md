---
phase: 10-repayments-close-write-off
plan: 04
type: execute
wave: 4
depends_on:
  - "10-03"
files_modified:
  - src/app/debts/actions.ts
  - src/app/debts/actions.test.ts
autonomous: true
gap_closure: true
gap_ids:
  - G-10-5
requirements:
  - REPAY-01
  - REPAY-03
  - DEBT-05
estimate:
  tokens: 20000
  raw_tokens: 40000
  tasks: 2
  confidence: high
must_haves:
  truths:
    - createRepayment / createSizeChange / forgiveRemaining / deleteRepayment / deleteSizeChange map Prisma P2025 and app-level record-missing throws to actionable Russian refresh message (G-10-5)
    - Staleness / not-found failure paths call revalidatePath("/debts") so next list/detail open is fresh (G-10-5)
    - forgiveRemaining maps assertSizeDelta domain failures to actionable Russian (never opaque catch-all) (G-10-5 / H4)
    - After deleteRepayment succeeds, createRepayment on same debt still succeeds (peer-tab delete of event) (G-10-5)
    - Missing debt / P2025 on createRepayment returns mapped refresh message, not generic save catch-all (G-10-5)
  prohibitions:
    - statement: Do not add a forgive-flag column or migration for DebtSizeChange
      category: values
    - statement: Do not change timeline labels to introduce a distinct forgive/write-off label
      category: values
    - statement: Do not call revalidatePath on dashboard root
      requirement_id: DISOL-01
      category: isolation
    - statement: Do not rebuild DebtDetailDialog stale-refresh UX in this plan (follow-up only)
      category: values
  artifacts:
    - path: src/app/debts/actions.ts
      provides: isRecordNotFound helper + mapped RU + revalidatePath on staleness paths
    - path: src/app/debts/actions.test.ts
      provides: peer-delete-then-create + P2025 mapped-message coverage
  key_links:
    - from: src/app/debts/actions.ts catch blocks
      to: Prisma.PrismaClientKnownRequestError P2025
      via: isRecordNotFound (mirror isUniqueNameViolation / isForeignKeyViolation)
      pattern: "P2025"
    - from: createRepayment / createSizeChange / forgiveRemaining / deleteRepayment / deleteSizeChange
      to: revalidatePath("/debts")
      via: called on mapped staleness returns before returning state
      pattern: "Обновите страницу"
---

## Gap

**gap_id:** G-10-5  
**truth:** Two tabs delete+create on same debt — no corrupt ledger; failed writes show actionable Russian error (not opaque catch-all)  
**source:** `.planning/phases/10-repayments-close-write-off/10-UAT.md` test 5 + `.planning/debug/concurrent-stale-write-opaque-error.md`

<objective>
Close G-10-5: map Prisma record-not-found / app missing-record throws on debt event create/delete/forgive to actionable Russian refresh guidance, revalidate `/debts` on those failures, map forgiveRemaining assertSizeDelta fallthrough, and lock with vitest.

Purpose: Concurrent/stale-tab writes must not surface opaque catch-all; user gets clear refresh path while ledger stays consistent.
Output: Updated catch blocks in actions.ts + vitest coverage in actions.test.ts.
</objective>

<execution_context>
@/home/artem/Documents/wallet/.cursor/gsd-core/workflows/execute-plan.md
@/home/artem/Documents/wallet/.cursor/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/10-repayments-close-write-off/10-UAT.md
@.planning/debug/concurrent-stale-write-opaque-error.md
@.planning/phases/10-repayments-close-write-off/10-03-SUMMARY.md
@.planning/phases/10-repayments-close-write-off/10-PATTERNS.md
@src/app/debts/actions.ts
@src/app/debts/actions.test.ts
</context>

<assumptions>
- Exact RU copy for staleness: «Долг или запись не найдены. Обновите страницу.» (from debug Proposed Fix).
- App-level throws REPAYMENT_NOT_FOUND / SIZE_CHANGE_NOT_FOUND count as record-missing (same message + revalidate).
- Dialog auto-close / remaining drift banner is out of scope (debug optional follow-up).
- Existing domain field mappings (OVER_REPAY, OVER_FLOOR, etc.) stay unchanged.
</assumptions>

<tasks>

<task type="tracer" tdd="true">
  <name>End-to-end: createRepayment P2025 → refresh RU + revalidate</name>
  <files>src/app/debts/actions.ts, src/app/debts/actions.test.ts</files>
  <read_first>
    .planning/debug/concurrent-stale-write-opaque-error.md
    .planning/phases/10-repayments-close-write-off/10-PATTERNS.md
    src/app/debts/actions.ts
    src/app/debts/actions.test.ts
  </read_first>
  <behavior>
    - When createRepayment hits Prisma P2025 (or findUniqueOrThrow not-found), return message «Долг или запись не найдены. Обновите страницу.» (G-10-5)
    - That failure path calls revalidatePath("/debts") and never revalidatePath("/")
    - Message is not the generic save catch-all used for unknown errors
    - Dedicated vitest title exactly: maps P2025 not-found to refresh RU and revalidates /debts (G-10-5)
  </behavior>
  <action>
    Per 10-PATTERNS.md `src/app/debts/actions.ts` helper analog (isUniqueNameViolation / isForeignKeyViolation next to Prisma known-request checks) and DISOL revalidate pattern (revalidatePath("/debts") only; never "/"): add helper isRecordNotFound — true when error is Prisma.PrismaClientKnownRequestError with code P2025 (G-10-5 / H1). In createRepayment catch, before the generic save fallback: if isRecordNotFound(error), call revalidatePath("/debts") and return { message: «Долг или запись не найдены. Обновите страницу.» }. Keep existing OVER_REPAY / AMOUNT_NOT_POSITIVE / fractional / bad-amount mappings. Do not touch DebtDetailDialog. Do not add schema columns or timeline label changes. Author vitest first with title containing "maps P2025 not-found to refresh RU and revalidates /debts (G-10-5)": mock findUniqueOrThrow (or $transaction) to reject with PrismaClientKnownRequestError code P2025 (same construction as existing P2002 person tests in actions.test.ts); assert mapped refresh message, revalidatePath("/debts") called, revalidatePath("/") not called (PATTERNS DISOL assert), debtRepayment.create not called.
  </action>
  <verify>
    <automated>grep -q 'maps P2025 not-found to refresh' src/app/debts/actions.test.ts && npx vitest run src/app/debts/actions.test.ts -t "maps P2025 not-found to refresh"</automated>
    <fails_when>grep miss for unique P2025 refresh test title, non-zero vitest exit, zero tests matched by -t, or missing refresh RU / isRecordNotFound coverage</fails_when>
  </verify>
  <done>createRepayment P2025 path returns refresh RU, revalidates /debts, tracer test green</done>
</task>

<task type="auto" tdd="true">
  <name>Expand staleness mapping + peer-delete create still succeeds</name>
  <files>src/app/debts/actions.ts, src/app/debts/actions.test.ts</files>
  <read_first>
    .planning/phases/10-repayments-close-write-off/10-PATTERNS.md
    src/app/debts/actions.ts
    src/app/debts/actions.test.ts
    src/lib/debts.ts
  </read_first>
  <behavior>
    - createSizeChange / forgiveRemaining / deleteRepayment / deleteSizeChange map isRecordNotFound and app missing-record throws to same refresh RU and call revalidatePath("/debts") (G-10-5)
    - forgiveRemaining maps assertSizeDelta failures (size change would make remaining less than 0; size delta must not be 0) to actionable Russian messages — never opaque save catch-all (G-10-5 / H4)
    - Sequence: deleteRepayment success then createRepayment on same debt still succeeds (G-10-5)
    - Missing-debt / P2025 createRepayment path remains mapped (regression)
  </behavior>
  <action>
    Per 10-PATTERNS.md actions.ts catch-helper + DISOL revalidatePath("/debts")-only pattern: apply the same isRecordNotFound branch (refresh RU + revalidatePath("/debts")) to catch blocks of createSizeChange, forgiveRemaining, deleteRepayment, and deleteSizeChange (G-10-5). For deleteRepayment / deleteSizeChange: treat REPAYMENT_NOT_FOUND / SIZE_CHANGE_NOT_FOUND like record-missing — same refresh message and revalidatePath("/debts") (replace prior delete-generic-only returns for those codes). In forgiveRemaining: wrap assertSizeDelta so OVER_FLOOR / DELTA_ZERO (or raw assert messages) map to actionable Russian form/message errors matching createSizeChange wording; do not leave them to the opaque save fallback (H4). Keep NOTHING_TO_FORGIVE mapping. Add vitest: (a) after successful deleteRepayment mock sequence, createRepayment with open ledger still succeeds; (b) P2025 / missing-debt createRepayment returns mapped refresh message and must not equal the generic save catch-all string. Do not modify DebtDetailDialog. Do not add forgive-flag column or distinct forgive timeline label.
  </action>
  <verify>
    <automated>npx vitest run src/app/debts/actions.test.ts && grep -q 'isRecordNotFound\|P2025' src/app/debts/actions.ts && grep -q 'Долг или запись не найдены. Обновите страницу.' src/app/debts/actions.ts</automated>
    <fails_when>non-zero vitest exit, missing isRecordNotFound or refresh RU in actions.ts, or peer-delete-then-createRepayment / P2025 mapped cases absent</fails_when>
  </verify>
  <acceptance_criteria>
    - grep -q 'Обновите страницу' src/app/debts/actions.ts
    - npx vitest run src/app/debts/actions.test.ts exits 0
    - actions.test.ts covers delete-then-createRepayment success and P2025 mapped message
  </acceptance_criteria>
  <done>All five event actions map staleness; forgive assertSizeDelta mapped; both vitest cases green; G-10-5 closable</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Client FormData → Server Actions | Untrusted ids/amounts; concurrent tabs may submit stale debtId / event ids |
| Server Action → Prisma/SQLite | Record may already be deleted by peer tab |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-10-G5-01 | Information Disclosure | create*/delete*/forgive catch | low | mitigate | Map P2025 to fixed RU refresh copy; no Prisma stack/meta to client |
| T-10-G5-02 | Tampering | stale debtId / event id | medium | mitigate | Transaction + findUniqueOrThrow; fail closed with refresh guidance + revalidatePath |
| T-10-G5-03 | Denial of Service | repeated stale submits | low | accept | Single-user local app; busy_timeout already set; no new rate limit |
| T-10-SC | Tampering | npm/pip/cargo installs | high | accept | No new packages in this plan |
</threat_model>

<verification>
npx vitest run src/app/debts/actions.test.ts green; P2025 and peer-delete-then-create cases present; isRecordNotFound used on create/delete/forgive paths; DISOL still /debts-only.
</verification>

<success_criteria>
G-10-5 closable: concurrent/stale not-found failures show «Долг или запись не найдены. Обновите страницу.» with revalidatePath("/debts"); forgiveRemaining never opaque for assertSizeDelta; vitest proves delete→create still works and P2025 is mapped.
</success_criteria>

## Source Audit (gap_closure scope)

| SOURCE | ID | Item | Plan | Status |
|--------|-----|------|------|--------|
| UAT | G-10-5 | Map P2025/not-found + forgive assertSizeDelta + revalidate + vitest | 10-04 | COVERED |
| DEBUG | H1/H4 | Catch-all masks P2025; forgive unmapped assertSizeDelta | 10-04 | COVERED |
| DEBUG | optional UX | Dialog close on drift | — | EXCLUDED (follow-up; out of plan scope) |
| GOAL | Phase 10 | Partial repayments / history / close (already shipped 01–03) | 01–03 | COVERED prior |
| REQ | REPAY-01/03, DEBT-05 | Concurrent failure UX for those actions | 10-04 | COVERED |

<output>
Create `.planning/phases/10-repayments-close-write-off/10-04-SUMMARY.md` when done
</output>
