---
phase: 10-repayments-close-write-off
plan: 03
type: execute
wave: 3
depends_on:
  - "10-02"
files_modified:
  - prisma/schema.prisma
  - prisma/migrations/
  - src/lib/foundation.test.ts
  - src/lib/validations/debts.ts
  - src/lib/validations/debts.test.ts
  - src/app/debts/actions.ts
  - src/app/debts/actions.test.ts
  - src/app/debts/page.tsx
  - src/components/debts/DebtDetailDialog.tsx
autonomous: false
requirements:
  - DEBT-05
estimate:
  tokens: 30000
  raw_tokens: 60000
  tasks: 3
  confidence: high
must_haves:
  truths:
    - User early-closes via «Простить остаток» creating size-change deltaMinor = −remaining with isForgive true; status CLOSED (DEBT-05 / D-08 / D-09 / Phase 8 D-04)
    - Manual «Изменение суммы» form accepts up/down deltas via createSizeChangeSchema + assertSizeDelta (D-08)
    - Forgive confirm states remaining amount being written off and that debt will close (D-09); asOfDate required, note optional (D-10)
    - Timeline labels «Списание» for isForgive size-changes vs «Изменение суммы» for manual (D-07)
    - Forgive computes delta server-side; client-supplied deltaMajor ignored/absent on forgive path (T-10-02)
    - User can delete size-change events with confirm; remaining/status recompute including reopen (D-06)
    - «Простить остаток» hidden when remaining is 0 (RESEARCH A1)
    - statement: isForgive Boolean @default(false) on DebtSizeChange persists across reload (discretion lock)
      verification: backstop
  prohibitions:
    - statement: MUST NOT add writeOffMinor column or WRITE_OFF repayment type
      requirement_id: DEBT-05
      category: values
    - statement: MUST NOT set status CLOSED without inserting size-change for early close
      requirement_id: DEBT-05
      category: values
    - statement: MUST NOT call revalidatePath on dashboard root
      requirement_id: DEBT-05
      category: isolation
    - statement: MUST NOT use native browser confirm for forgive or size-change delete
      requirement_id: DEBT-05
      category: values
  artifacts:
    - path: prisma/schema.prisma
      provides: DebtSizeChange.isForgive Boolean @default(false)
    - path: prisma/migrations/
      provides: migrate adding isForgive
    - path: src/app/debts/actions.ts
      provides: createSizeChange, forgiveRemaining, deleteSizeChange
    - path: src/lib/validations/debts.ts
      provides: forgiveRemainingSchema (debtId, asOfDate, note?)
    - path: src/components/debts/DebtDetailDialog.tsx
      provides: size-change form, forgive confirm, «Списание» labels
  key_links:
    - from: src/app/debts/actions.ts
      to: prisma DebtSizeChange
      via: isForgive true on forgive path
      pattern: "isForgive"
    - from: src/app/debts/actions.ts
      to: src/lib/debts.ts
      via: assertSizeDelta + statusForRemaining
      pattern: "forgive"
    - from: src/components/debts/DebtDetailDialog.tsx
      to: DestructiveConfirmStep
      via: forgive + size-change delete confirms
      pattern: "Простить остаток"
---

## Phase Goal

Partial dated repayments with history/delete, auto-close at zero, early write-off close.

<objective>
Lock isForgive persistence, ship size-change + forgive Server Actions, and finish detail UI for early close + «Списание» labels (DEBT-05 / D-07–D-10).

Purpose: Complete early write-off without resurrecting writeOffMinor.
Output: migrated isForgive, createSizeChange/forgiveRemaining/deleteSizeChange, green tests, forgive UX.
</objective>

<execution_context>
@/home/artem/Documents/wallet/.cursor/gsd-core/workflows/execute-plan.md
@/home/artem/Documents/wallet/.cursor/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/10-repayments-close-write-off/10-CONTEXT.md
@.planning/phases/10-repayments-close-write-off/10-RESEARCH.md
@.planning/phases/10-repayments-close-write-off/10-PATTERNS.md
@.planning/phases/08-debts-schema-domain-math/08-CONTEXT.md
@prisma/schema.prisma
@src/app/debts/actions.ts
@src/lib/debts.ts
@src/lib/validations/debts.ts
@src/components/debts/DebtDetailDialog.tsx
@src/lib/foundation.test.ts
</context>

<assumptions>
## Planner assumptions (specless / discretion)

- A-edge DEBT-05 unclassified: early close = size-change delta = −remaining (Phase 8 D-04), not a status-only toggle; label «Списание» via isForgive (D-07).
- Discretion A2 locked by checkpoint: prefer Boolean isForgive over note sentinel.
- Discretion A1: hide «Простить остаток» when remaining === 0n.
- Future asOfDate rejection applies to createSizeChange and forgiveRemaining same as repayments (A3).
</assumptions>

## Artifacts this phase produces

- `prisma/schema.prisma` — `DebtSizeChange.isForgive Boolean @default(false)`
- `prisma/migrations/*_debt_size_change_is_forgive/` — SQL migration
- `src/lib/foundation.test.ts` — migration name / column awareness if required by existing gates
- `src/lib/validations/debts.ts` — `forgiveRemainingSchema`
- `src/app/debts/actions.ts` — `createSizeChange`, `forgiveRemaining`, `deleteSizeChange`
- `src/components/debts/DebtDetailDialog.tsx` — size-change form, forgive confirm, «Списание» rows

<tasks>

<task type="checkpoint:decision" gate="blocking-human">
  <name>Confirm DebtSizeChange.isForgive Boolean (one-way)</name>
  <decision>Persist forgive vs manual size-change distinction for D-07 «Списание» labels (CONTEXT deferred to Claude; RESEARCH recommends Boolean).</decision>
  <context>
    One-way door: SQLite schema change consumed by Phase 10 UI and Phase 11 charts on same detail surface. Undoing after migrate needs another migration plus forgive/timeline rewrites. Auto-mode must not auto-pick — gate is blocking-human. Do not resurrect superseded forgive-amount column on Debt.
  </context>
  <options>
    <option id="isForgive-boolean">
      <name>isForgive Boolean @default(false) on DebtSizeChange (RECOMMENDED)</name>
      <pros>Durable «Списание» vs «Изменение суммы» after later events; minimal schema; matches RESEARCH A2</pros>
      <cons>Requires migration + Prisma client regenerate</cons>
    </option>
    <option id="note-sentinel">
      <name>Encode forgive via reserved note sentinel</name>
      <pros>No schema migration</pros>
      <cons>Collides with user notes; fragile parsing</cons>
    </option>
    <option id="infer-at-display">
      <name>Infer label by delta === −remaining at render</name>
      <pros>No persistence change</pros>
      <cons>Wrong after subsequent events (RESEARCH Pitfall 3)</cons>
    </option>
  </options>
  <resume-signal>Select: isForgive-boolean (recommended), note-sentinel, or infer-at-display</resume-signal>
</task>

<task type="auto">
  <name>Schema isForgive + migrate deploy + forgive/size-change actions</name>
  <reversibility rating="one-way">Adding isForgive to SQLite requires a forward migration; removing it later needs another migration and data backfill.</reversibility>
  <files>prisma/schema.prisma, prisma/migrations/, src/lib/foundation.test.ts, src/lib/validations/debts.ts, src/lib/validations/debts.test.ts, src/app/debts/actions.ts, src/app/debts/actions.test.ts</files>
  <read_first>
    .planning/phases/10-repayments-close-write-off/10-RESEARCH.md
    .planning/phases/08-debts-schema-domain-math/08-01-PLAN.md
    prisma/schema.prisma
    prisma/migrations/20260904180755_debts_schema/migration.sql
    src/app/debts/actions.ts
    src/lib/debts.ts
    src/lib/validations/debts.ts
    src/lib/foundation.test.ts
  </read_first>
  <behavior>
    - After checkpoint selects isForgive-boolean: schema field + migrate deploy (DEBT-05 / D-07)
    - createSizeChange: createSizeChangeSchema → assertSizeDelta → insert isForgive false → status sync (D-08)
    - forgiveRemaining: forgiveRemainingSchema (debtId, asOfDate, note?) only — server sets deltaMinor = −remainingBefore, isForgive true; reject remaining 0 (D-08/D-09/D-10 / T-10-02)
    - deleteSizeChange: mirror deleteRepayment with status recompute (D-06)
    - Future asOfDate rejected on create/forgive
    - revalidatePath("/debts") only
  </behavior>
  <action>
    After checkpoint selects isForgive-boolean: add isForgive Boolean @default(false) to DebtSizeChange in prisma/schema.prisma. Create migration via npx prisma migrate dev --name debt_size_change_is_forgive (or equivalent SQL folder). Run DATABASE_URL=file:./data/wallet.db npx prisma migrate deploy (not db push); if SQLite locked by wallet-web, stop container briefly. Update foundation.test.ts only if migrate-name assertions require the new migration name. Add forgiveRemainingSchema { debtId, asOfDate, note? }.strict() — no client delta. Implement createSizeChange, forgiveRemaining, deleteSizeChange in actions.ts with $transaction, assertSizeDelta / remainingMinor / statusForRemaining, isForgive true only on forgive path, future-date gate, revalidatePath("/debts") only. Extend actions.test.ts: forgive → CLOSED + isForgive, smuggled delta ignored, over-floor size-change rejected, deleteSizeChange reopen, DISOL. If checkpoint chose note-sentinel or infer-at-display, implement that persistence approach instead and skip Boolean column — never resurrect superseded forgive-amount column on Debt.
  </action>
  <verify>
    <automated>mkdir -p data && DATABASE_URL="file:./data/wallet.db" npx prisma migrate deploy && DATABASE_URL="file:./data/wallet.db" npx prisma migrate status 2>&1 | grep -qi 'up to date\|Database schema is up to date\|No pending migrations' && npx vitest run src/app/debts/actions.test.ts src/lib/validations/debts.test.ts && grep -q 'isForgive' prisma/schema.prisma && grep -q 'forgiveRemaining\|createSizeChange' src/app/debts/actions.ts</automated>
    <fails_when>migrate pending/non-zero, vitest non-zero, isForgive missing from schema (when option A), or forgive/size-change actions missing</fails_when>
  </verify>
  <acceptance_criteria>
    - Host migrate deploy up to date; isForgive on DebtSizeChange when isForgive-boolean chosen
    - forgiveRemaining creates size-change −remaining with CLOSED status (DEBT-05)
    - createSizeChange + deleteSizeChange tested; DISOL holds
    - Schema stays size-change ledger (no superseded forgive-amount column on Debt)
  </acceptance_criteria>
  <done>Migrated isForgive (or chosen alt) + size-change/forgive/delete actions green</done>
</task>

<task type="auto">
  <name>Detail UI: size-change form, forgive confirm, «Списание» labels</name>
  <files>src/components/debts/DebtDetailDialog.tsx, src/app/debts/page.tsx</files>
  <read_first>
    .planning/phases/10-repayments-close-write-off/10-CONTEXT.md
    .planning/phases/10-repayments-close-write-off/10-PATTERNS.md
    src/components/debts/DebtDetailDialog.tsx
    src/components/debts/DestructiveConfirmStep.tsx
    src/app/debts/page.tsx
  </read_first>
  <behavior>
    - Manual «Изменение суммы» form (deltaMajor, asOfDate, note) posts createSizeChange (D-08)
    - «Простить остаток» requires DestructiveConfirmStep stating remaining amount and that debt will close; fields asOfDate + optional note (D-09 / D-10)
    - Hide forgive when remaining is 0 (A1)
    - Timeline: isForgive → «Списание»; else size-change → «Изменение суммы»; repayment → «Погашение» (D-07)
    - Size-change delete uses DestructiveConfirmStep (D-06)
    - CLOSED debts retain full controls (D-13)
  </behavior>
  <action>
    Extend DebtDetailDialog with manual size-change form and «Простить остаток» flow using DestructiveConfirmStep confirm copy that includes formatted remaining and close intent (D-08/D-09). Forgive form: asOfDate required default calendarDateToday, note optional (D-10); no client delta field. Hide forgive CTA when BigInt(remainingMinor) === 0n (A1). Map timeline labels per D-07 using isForgive from page props. Wire deleteSizeChange confirm. Pass isForgive boolean from page.tsx event serialization. Keep Dialog (not Sheet). <!-- planner-discipline-allow: window.confirm -->
  </action>
  <verify>
    <automated>npx vitest run src/app/debts/actions.test.ts && grep -q 'Простить остаток' src/components/debts/DebtDetailDialog.tsx && grep -q 'Списание' src/components/debts/DebtDetailDialog.tsx && grep -q 'Изменение суммы' src/components/debts/DebtDetailDialog.tsx && grep -q 'DestructiveConfirmStep' src/components/debts/DebtDetailDialog.tsx && ! grep -R --include='*.tsx' -n 'window\.confirm' src/components/debts/</automated>
    <fails_when>vitest non-zero, missing forgive/Списание/Изменение суммы copy, or native confirm grepped under debts components</fails_when>
  </verify>
  <acceptance_criteria>
    - Forgive + size-change UI present with D-07 labels and D-09 confirm
    - No native browser confirm under debts components
    - actions suite green
  </acceptance_criteria>
  <done>Early close UX complete; Phase 10 feature surface closed</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Browser → forgiveRemaining / createSizeChange | Untrusted FormData; forgive must not trust client delta |
| Browser → deleteSizeChange | Untrusted id |
| Schema migration | Host SQLite file mutation |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-10-02 | Tampering | forgiveRemaining | high | mitigate | Server-only delta = −remaining; forgiveRemainingSchema has no deltaMajor; .strict() |
| T-10-01 | Tampering | createSizeChange | high | mitigate | assertSizeDelta prevents remaining &lt; 0 |
| T-10-03 | Tampering | deleteSizeChange | high | mitigate | Positive int Zod + Prisma id delete + ledger reload |
| T-10-04 | Tampering | revalidatePath | high | mitigate | /debts only; tests |
| T-10-05 | Tampering | note XSS | medium | mitigate | optionalNoteSchema + React text |
| T-10-SC | Tampering | npm installs | high | accept | No new packages; schema-only migration |
</threat_model>

<verification>
Migrate up to date; forgive/size-change/deleteSizeChange tests green; detail shows «Простить остаток» / «Списание»; no writeOffMinor; no native confirm.
</verification>

<success_criteria>
User can early-close via forgive size-change and manual size-change; timeline labels correct; schema migrated.
</success_criteria>

<output>
Create `.planning/phases/10-repayments-close-write-off/10-03-SUMMARY.md` when done
</output>
