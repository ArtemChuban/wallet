---
phase: 09-people-debts-crud-nav
plan: 03
type: execute
wave: 3
depends_on:
  - "09-02"
files_modified:
  - src/app/debts/actions.ts
  - src/app/debts/actions.test.ts
  - src/app/debts/page.tsx
  - src/components/debts/DebtFormDialog.tsx
  - src/components/debts/DebtsList.tsx
  - src/lib/validations/debts.ts
  - src/lib/validations/debts.test.ts
autonomous: true
requirements:
  - DEBT-01
estimate:
  tokens: 30000
  raw_tokens: 60000
  tasks: 3
  confidence: high
must_haves:
  truths:
    - User can create a debt with direction, currency, positive initial amount, optional due date, optional note attached to an existing person (DEBT-01 / D-11)
    - Debt create dialog supports new-person name field in the same dialog; one submit creates person + debt atomically (D-05 / D-06)
    - Creating debt from a person group pre-selects that person; user may switch person or new-person path (D-12)
    - After create, edit allows only direction, due date, note; person, currency, and initial show as read-only (D-09 / D-10)
    - Compact debt rows show only direction label («Я должен» / «Мне должны») + formatted remaining + currency code (D-02)
    - Remaining on rows equals remainingMinor(initial, sizeDeltas, repayments) computed on the server and serialized as string (Phase 8 math)
    - Debts within a person group sort newest first by id (D-04)
    - Debt delete lives only inside edit dialog; confirm copy mentions cascade of repayments and size-change history (D-13 / D-14 / D-15)
    - createDebtSchema rejects initial amount ≤ 0 with Russian validation message (DEBT-01 boundary)
    - Money uses BigInt minors via parseMajorToMinor — no float money math (DEBT-01 precision)
    - statement: Optional debt note wraps in dialog and is omitted from compact rows
      verification: backstop
  prohibitions:
    - statement: MUST NOT allow editing initial amount, currency, or person after debt create
      requirement_id: DEBT-01
      category: safety
    - statement: MUST NOT expose repayment, close, write-off, status picker, charts, or primary totals UI in this phase
      requirement_id: DEBT-01
      category: values
    - statement: MUST NOT put debt delete controls on the list row — delete only inside edit dialog
      requirement_id: DEBT-01
      category: values
    - statement: MUST NOT use a browser native confirm dialog for debt delete
      requirement_id: DEBT-01
      category: values
  artifacts:
    - path: src/app/debts/actions.ts
      provides: createDebt, createDebtWithNewPerson (or branched create), updateDebtMeta, deleteDebt
    - path: src/components/debts/DebtFormDialog.tsx
      provides: create + edit dialogs with locked fields and in-dialog delete
    - path: src/components/debts/DebtsList.tsx
      provides: compact nested debt rows with remaining
    - path: src/lib/validations/debts.ts
      provides: optional thin compound create schema if exported
  key_links:
    - from: src/app/debts/page.tsx
      to: src/lib/debts.ts
      via: remainingMinor on RSC load
      pattern: "remainingMinor"
    - from: src/app/debts/actions.ts
      to: src/lib/validations/debts.ts
      via: createDebtSchema / updateDebtMetaSchema
      pattern: "updateDebtMetaSchema"
    - from: src/components/debts/DebtFormDialog.tsx
      to: src/components/debts/DestructiveConfirmStep.tsx
      via: debt delete cascade confirm
      pattern: "Удалить долг"
---

## Phase Goal

User can manage people and debts in a dedicated «Долги» section (without repayments UI yet).

<objective>
Deliver DEBT-01 create/edit/delete on `/debts`: full create (including D-06 compound person+debt), meta-only edit with locked fields, compact remaining rows, and cascade-aware delete confirm inside the edit dialog (D-02, D-06, D-09–D-15).

Purpose: Complete debts CRUD without repayments UI so Phase 10 can add repayments on a working list.
Output: DebtFormDialog, debt Server Actions, remaining display on grouped rows.
</objective>

<execution_context>
@/home/artem/Documents/wallet/.cursor/gsd-core/workflows/execute-plan.md
@/home/artem/Documents/wallet/.cursor/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/09-people-debts-crud-nav/09-02-PLAN.md
@.planning/phases/09-people-debts-crud-nav/09-CONTEXT.md
@.planning/phases/09-people-debts-crud-nav/09-RESEARCH.md
@.planning/phases/09-people-debts-crud-nav/09-UI-SPEC.md
@.planning/phases/09-people-debts-crud-nav/09-PATTERNS.md
@src/app/debts/actions.ts
@src/lib/debts.ts
@src/lib/validations/debts.ts
@src/lib/money.ts
@src/components/debts/DestructiveConfirmStep.tsx
@src/components/accounts/AccountFormDialog.tsx
</context>

<assumptions>
## Planner assumptions (specless / discretion)

- A-edge DEBT-01 idempotency: createDebt is intentionally non-idempotent — each successful submit inserts a new Debt row; updateDebtMeta with identical payload is a no-op write of same meta.
- A-edge DEBT-01 concurrency: SQLite single-writer; nested person+debt create is one atomic Prisma nested write.
- Compound validation: prefer action-local branch (createDebtSchema vs createPersonSchema + debt fields) or thin createDebtWithNewPersonSchema export — either OK if tests cover both paths (RESEARCH Q2).
- Status on create remains schema default OPEN; do not expose status controls (Phase 10 owns close).
</assumptions>

## Artifacts this phase produces

- `createDebt` / compound create / `updateDebtMeta` / `deleteDebt` Server Actions
- `DebtFormDialog` create + edit (locked fields, in-dialog delete)
- Compact debt rows with `remainingMinor` display
- Optional `createDebtWithNewPersonSchema` in validations if exported for tests

<tasks>

<task type="auto" tdd="true">
  <name>Debt create actions: existing person + compound new person</name>
  <files>src/app/debts/actions.ts, src/app/debts/actions.test.ts, src/lib/validations/debts.ts, src/lib/validations/debts.test.ts</files>
  <read_first>
    .planning/phases/09-people-debts-crud-nav/09-RESEARCH.md
    src/app/debts/actions.ts
    src/app/accounts/actions.ts
    src/lib/validations/debts.ts
    src/lib/money.ts
    src/lib/debts.ts
  </read_first>
  <behavior>
    - createDebt path: createDebtSchema → lookup currency scale → parseMajorToMinor → prisma.debt.create status OPEN → revalidatePath("/debts")
    - new-person path: one submit validates person name + debt fields without requiring personId; nested prisma.person.create with debts.create OR $transaction (D-06)
    - Rejects non-positive initial via Zod Russian message
    - Never posts to NW routes
  </behavior>
  <action>
    Add vitest cases for createDebt existing-person success, positive-amount boundary rejection, and compound new-person+debt atomic create (D-06 / DEBT-01). Implement Server Actions: reuse createDebtSchema for existing personId path; for new-person path branch with createPersonSchema fields + debt fields minus personId (or add thin compound schema export and tests). Parse money with parseMajorToMinor + currency.scale like accounts. Nested write preferred for atomicity. Direction enum I_OWE/THEY_OWE only. Do not wire repayments schemas. Do not edit initial after create in this action set.
  </action>
  <verify>
    <automated>npx vitest run src/app/debts/actions.test.ts src/lib/validations/debts.test.ts && grep -q 'createDebt' src/app/debts/actions.ts && grep -q 'parseMajorToMinor' src/app/debts/actions.ts</automated>
    <fails_when>non-zero vitest exit, missing createDebt, or money parse helper unused in actions</fails_when>
  </verify>
  <acceptance_criteria>
    - Both existing-person and new-person create paths covered and green
    - Initial amount ≤ 0 rejected with Russian validation
    - revalidatePath("/debts") on success
  </acceptance_criteria>
  <done>Debt create (including D-06 compound) actions green</done>
</task>

<task type="auto" tdd="true">
  <name>updateDebtMeta + deleteDebt actions with lock/smuggle tests</name>
  <files>src/app/debts/actions.ts, src/app/debts/actions.test.ts</files>
  <read_first>
    src/app/debts/actions.ts
    src/app/accounts/actions.test.ts
    src/lib/validations/debts.ts
    prisma/schema.prisma
  </read_first>
  <behavior>
    - updateDebtMeta uses updateDebtMetaSchema; prisma update writes only direction/dueDate/note; smuggled initial/person/currency FormData ignored (D-09)
    - deleteDebt validates id, prisma.debt.delete (Cascade events), revalidatePath("/debts") (D-13)
  </behavior>
  <action>
    Add tests mirroring accounts updateAccountName smuggle pattern for updateDebtMeta (DEBT-01 / D-09). Implement updateDebtMeta and deleteDebt with ensureSqlitePragmas. deleteDebt relies on schema Cascade for repayments/sizeChanges — do not manually delete child rows. Return UI-SPEC Russian failure messages on errors.
  </action>
  <verify>
    <automated>npx vitest run src/app/debts/actions.test.ts && grep -q 'updateDebtMeta' src/app/debts/actions.ts && grep -q 'deleteDebt' src/app/debts/actions.ts</automated>
    <fails_when>non-zero vitest exit or missing update/delete exports</fails_when>
  </verify>
  <acceptance_criteria>
    - updateDebtMeta ignores smuggled initial/person/currency fields
    - deleteDebt calls prisma.debt.delete and revalidates /debts
    - actions.test.ts green
  </acceptance_criteria>
  <done>Meta update and cascade delete actions green</done>
</task>

<task type="auto">
  <name>DebtFormDialog + compact remaining rows + cascade confirm</name>
  <files>src/components/debts/DebtFormDialog.tsx, src/components/debts/DebtsList.tsx, src/app/debts/page.tsx</files>
  <read_first>
    .planning/phases/09-people-debts-crud-nav/09-UI-SPEC.md
    .planning/phases/09-people-debts-crud-nav/09-PATTERNS.md
    src/app/debts/page.tsx
    src/components/debts/DebtsList.tsx
    src/components/debts/DestructiveConfirmStep.tsx
    src/components/accounts/AccountFormDialog.tsx
    src/lib/debts.ts
    src/lib/money.ts
  </read_first>
  <action>
    Implement DebtFormDialog create/edit mirroring AccountFormDialog (formKey remount, useActionState). Create: direction Select with labels «Я должен»/«Мне должны» (D-11), currency Select, initial Input, optional due/note, person Select or new-person name field (D-06), preselect personId from group CTA (D-12). Edit: person name, currency code, initial formatted major as read-only text (D-10); editable direction/due/note only → updateDebtMeta (D-09). Delete control only inside edit dialog (D-14): DestructiveConfirmStep with cascade copy «Удалить долг? Будут удалены долг и вся история погашений и изменений суммы. Это нельзя отменить.» (D-15). On page RSC load include debts with repayments/sizeChanges, compute remainingMinor, serialize BigInt strings, order debts by id desc within person (D-04). DebtsList compact rows: direction label + formatMinorToMajor(remaining) + currency code only (D-02). Wire header/group «Новый долг» CTAs to DebtFormDialog. No repayment UI, charts, or status picker.
  </action>
  <verify>
    <automated>npx vitest run src/app/debts/actions.test.ts src/lib/debts.test.ts && test -f src/components/debts/DebtFormDialog.tsx && grep -q 'remainingMinor' src/app/debts/page.tsx && grep -q 'Я должен' src/components/debts/DebtFormDialog.tsx && grep -q 'Мне должны' src/components/debts/DebtFormDialog.tsx && grep -q 'Будут удалены долг и вся история' src/components/debts/DebtFormDialog.tsx && ! grep -R --include='*.tsx' -n 'window\.confirm' src/components/debts/</automated>
    <fails_when>non-zero vitest exit, DebtFormDialog missing, remainingMinor unused on page, direction/cascade copy missing, or window.confirm present under debts components</fails_when>
  </verify>
  <acceptance_criteria>
    - DebtFormDialog create/edit/delete UX matches D-06, D-09–D-15 and UI-SPEC copy
    - Compact rows use server remainingMinor display (D-02)
    - DISOL debts.test.ts still green; no window.confirm in debts components
  </acceptance_criteria>
  <done>DEBT-01 UI complete with remaining rows and cascade delete confirm</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Browser → createDebt / updateDebtMeta / deleteDebt | Untrusted FormData including smuggled initial fields |
| RSC → client | remainingMinor serialized as string (BigInt boundary) |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-09-01 | Tampering | updateDebtMeta | high | mitigate | updateDebtMetaSchema omits initial; .strict(); action writes meta fields only |
| T-09-07 | Tampering | createDebt amount | medium | mitigate | Zod positive major + parseMajorToMinor scale check |
| T-09-08 | Tampering | compound create | medium | mitigate | Nested create / transaction — no orphan person on debt failure |
| T-09-09 | Destruction | deleteDebt | medium | mitigate | In-dialog confirm with cascade loss copy; Cascade FK for events |
| T-09-SC | Tampering | npm installs | high | accept | No new packages |
</threat_model>

<verification>
create/update/delete debt actions green; DebtFormDialog wired; remaining rows; cascade confirm; DISOL green.
</verification>

<success_criteria>
User creates/edits debts with locked post-create fields; compound new-person works; compact remaining rows; delete with cascade confirm inside edit dialog.
</success_criteria>

<output>
Create `.planning/phases/09-people-debts-crud-nav/09-03-SUMMARY.md` when done
</output>
