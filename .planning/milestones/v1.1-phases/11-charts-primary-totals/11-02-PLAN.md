---
phase: 11-charts-primary-totals
plan: 02
type: execute
wave: 2
depends_on:
  - "11-01"
files_modified:
  - src/lib/validations/debts.ts
  - src/lib/validations/debts.test.ts
  - src/app/debts/actions.ts
  - src/app/debts/actions.test.ts
  - src/components/debts/DebtFormDialog.tsx
  - src/components/debts/DebtDetailDialog.tsx
  - src/components/debts/DebtPrincipalStackChart.tsx
autonomous: true
requirements:
  - DCHART-01
  - DCHART-02
estimate:
  tokens: 25000
  raw_tokens: 50000
  tasks: 3
  confidence: high
must_haves:
  truths:
    - Open date is immutable after create — edit UI does not offer openedAsOf; update action never writes it (D-09)
    - Chart remains single stacked composition with stepAfter Areas; no RangePreset chrome (D-03 / D-05)
    - Empty timeline still shows flat open→today chart above «Пока нет событий» (D-02 / D-04)
    - Size-change events change stack height only — no third Area series (D-06)
    - statement: Tooltip/labels use plain text for Погашено and Остаток without HTML note injection (T-11-03)
      verification: backstop
  prohibitions:
    - statement: MUST NOT expose editable openedAsOf on debt edit
      requirement_id: DCHART-01
      category: values
    - statement: MUST NOT add RangePreset 30d/90d/1y controls on the debt chart
      requirement_id: DCHART-02
      category: values
    - statement: MUST NOT add a third stacked series for size-change events
      requirement_id: DCHART-01
      category: values
  artifacts:
    - path: src/components/debts/DebtFormDialog.tsx
      provides: create-only Дата; edit shows read-only open date or omits field
    - path: src/components/debts/DebtPrincipalStackChart.tsx
      provides: polished stepAfter stack + RU tooltip
    - path: src/app/debts/actions.ts
      provides: updateDebtMeta never writes openedAsOf
  key_links:
    - from: src/components/debts/DebtFormDialog.tsx
      to: src/lib/validations/debts.ts
      via: create-only openedAsOf vs updateDebtMetaSchema omit
      pattern: "openedAsOf"
    - from: src/components/debts/DebtPrincipalStackChart.tsx
      to: src/components/ui/chart.tsx
      via: ChartContainer + ChartTooltip text content
      pattern: "ChartTooltip"
---

<objective>
Harden D-09 open-date immutability and finish debt-chart UX polish on the tracer chart (D-03 / D-05 / D-06 / DCHART-01 / DCHART-02).

Purpose: Close edit-path mass-assignment and empty-history chart behavior after the tracer slice.
Output: Immutable open date on update/edit; polished single stack chart with empty-timeline coverage.
</objective>

<execution_context>
@/home/artem/Documents/wallet/.cursor/gsd-core/workflows/execute-plan.md
@/home/artem/Documents/wallet/.cursor/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/11-charts-primary-totals/11-CONTEXT.md
@.planning/phases/11-charts-primary-totals/11-RESEARCH.md
@.planning/phases/11-charts-primary-totals/11-PATTERNS.md
@.planning/phases/11-charts-primary-totals/11-01-PLAN.md
@src/components/debts/DebtFormDialog.tsx
@src/components/debts/DebtDetailDialog.tsx
@src/components/debts/DebtPrincipalStackChart.tsx
@src/app/debts/actions.ts
@src/lib/validations/debts.ts
</context>

<assumptions>
## Planner assumptions (specless / discretion)

- A-edge D-09: edit mode may show read-only openedAsOf value for honesty or omit the field entirely — either acceptable if update cannot change it.
- Discretion: RU tooltip labels «Погашено» / «Остаток»; chart colors via ChartConfig tokens.
- Tracer already implemented full series algorithm; this plan expands UI/immutability, not a second series API.
</assumptions>

## Artifacts this phase produces

- Edit-path open-date immutability (Zod + action + form)
- Polished `DebtPrincipalStackChart` tooltips / empty-history mount behavior
- Action tests proving update ignores smuggled openedAsOf

<tasks>

<task type="auto" tdd="true">
  <name>D-09 update path rejects openedAsOf mutation</name>
  <files>src/lib/validations/debts.test.ts, src/app/debts/actions.test.ts, src/lib/validations/debts.ts, src/app/debts/actions.ts</files>
  <read_first>
    .planning/phases/11-charts-primary-totals/11-CONTEXT.md
    .planning/phases/11-charts-primary-totals/11-PATTERNS.md
    src/lib/validations/debts.ts
    src/lib/validations/debts.test.ts
    src/app/debts/actions.ts
    src/app/debts/actions.test.ts
  </read_first>
  <behavior>
    - updateDebtMetaSchema has no openedAsOf key; safeParse with smuggled openedAsOf fails via .strict() (D-09 / T-11-02)
    - updateDebtMeta Prisma data object never includes openedAsOf even if FormData contains it (D-09)
  </behavior>
  <action>
    Confirm updateDebtMetaSchema remains without openedAsOf and .strict() (per D-09). Extend validations tests if gaps remain after 11-01. Add/adjust actions.test.ts coverage so updateDebtMeta ignores FormData openedAsOf and never passes it to prisma.debt.update data. Do not alter create path openedAsOf requirement from Plan 01.
  </action>
  <verify>
    <automated>npx vitest run src/lib/validations/debts.test.ts src/app/debts/actions.test.ts</automated>
    <fails_when>vitest non-zero, or update tests missing openedAsOf immutability assertions</fails_when>
  </verify>
  <acceptance_criteria>
    - updateDebtMetaSchema.shape lacks openedAsOf
    - actions test proves smuggled openedAsOf does not appear in prisma.debt.update data
    - npx vitest run src/lib/validations/debts.test.ts src/app/debts/actions.test.ts exits 0
  </acceptance_criteria>
  <done>Update path cannot mutate openedAsOf (D-09 / T-11-02)</done>
</task>

<task type="auto">
  <name>Edit UI create-only Дата + chart empty-history polish</name>
  <reversibility rating="costly">Stacked chart composition couples DCHART-01/02 into one UI (D-03); swapping to two charts later is a coordinated UI rewrite.</reversibility>
  <files>src/components/debts/DebtFormDialog.tsx, src/components/debts/DebtDetailDialog.tsx, src/components/debts/DebtPrincipalStackChart.tsx</files>
  <read_first>
    .planning/phases/11-charts-primary-totals/11-CONTEXT.md
    .planning/phases/11-charts-primary-totals/11-PATTERNS.md
    src/components/debts/DebtFormDialog.tsx
    src/components/debts/DebtDetailDialog.tsx
    src/components/debts/DebtPrincipalStackChart.tsx
    src/components/dashboard/NetWorthHistoryChart.tsx
  </read_first>
  <behavior>
    - Debt edit mode does not submit openedAsOf; create mode keeps required «Дата» (D-08 / D-09)
    - История always shows chart above timeline; empty events still render flat series then empty copy (D-02 / D-04)
    - Single stack only: repaidMajor + remainingMajor Areas, stepAfter, no preset buttons (D-03 / D-05 / D-06)
    - Tooltip content is text labels only (T-11-03)
  </behavior>
  <action>
    In DebtFormDialog, ensure openedAsOf input is create-only (per D-09): edit mode shows read-only open date or omits the control and never posts openedAsOf. Polish DebtPrincipalStackChart: ChartConfig keys, RU tooltip «Погашено»/«Остаток», formatAsOfDisplay / formatChartNumber, confirm two stepAfter Areas share stackId and omit sign stackOffset and RangePreset UI (D-03 / D-05 / D-06). In DebtDetailDialog history tabpanel, keep chart first even when timeline.length === 0 (D-02). Do not add a second chart component.
  </action>
  <verify>
    <automated>npx vitest run src/lib/debts.test.ts src/lib/validations/debts.test.ts && grep -q 'stepAfter' src/components/debts/DebtPrincipalStackChart.tsx && grep -q 'DebtPrincipalStackChart' src/components/debts/DebtDetailDialog.tsx && ! grep -E 'RangePreset|30д|90д|1г' src/components/debts/DebtPrincipalStackChart.tsx</automated>
    <fails_when>vitest non-zero, stepAfter or chart mount missing, or RangePreset/preset chrome appears in debt chart file</fails_when>
  </verify>
  <acceptance_criteria>
    - Edit DebtFormDialog path has no writable name=openedAsOf control
    - DebtPrincipalStackChart has exactly the two stack series keys (repaidMajor, remainingMajor) and stepAfter
    - History tabpanel still mounts chart when timeline is empty
  </acceptance_criteria>
  <done>Create-only open date in UI; polished single stack chart with empty-history coverage</done>
</task>

<task type="auto">
  <name>Keep debts chart suites green</name>
  <files>src/lib/debts.test.ts, src/lib/validations/debts.test.ts, src/app/debts/actions.test.ts</files>
  <read_first>
    .planning/phases/11-charts-primary-totals/11-VALIDATION.md
    src/lib/debts.test.ts
    src/lib/validations/debts.test.ts
  </read_first>
  <behavior>
    - Full debts domain + validations + actions suites pass after immutability/chart polish
  </behavior>
  <action>
    Run and fix any regressions from Tasks 1–2 in debts.test.ts, validations/debts.test.ts, and actions.test.ts. Do not expand into /debts hero or Капитал banner (Plans 03–04).
  </action>
  <verify>
    <automated>npx vitest run src/lib/debts.test.ts src/lib/validations/debts.test.ts src/app/debts/actions.test.ts</automated>
    <fails_when>any of the three vitest targets exits non-zero</fails_when>
  </verify>
  <acceptance_criteria>
    - npx vitest run src/lib/debts.test.ts src/lib/validations/debts.test.ts src/app/debts/actions.test.ts exits 0
  </acceptance_criteria>
  <done>Chart + immutability regression suite green</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Browser edit form → updateDebtMeta | Untrusted FormData may smuggle openedAsOf |
| Chart tooltip render | Client-side label strings only |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-11-02 | Tampering | updateDebtMeta | high | mitigate | Schema omit + action ignore + edit UI create-only (D-09) |
| T-11-03 | XSS | DebtPrincipalStackChart | medium | mitigate | Plain-text tooltip formatter; no note HTML |
| T-11-SC | Tampering | npm installs | low | accept | No new packages |
</threat_model>

<verification>
Immutability tests + chart greps + debts/validations/actions vitest green.
</verification>

<success_criteria>
Open date cannot change after create; debt detail still shows one polished stacked chart (including empty timeline).
</success_criteria>

<output>
Create `.planning/phases/11-charts-primary-totals/11-02-SUMMARY.md` when done
</output>
