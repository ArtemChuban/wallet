---
phase: 12-address-tech-debt-debts-refresh-nyquist-10-11
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/components/debts/DebtDetailDialog.tsx
  - src/components/debts/DebtFormDialog.tsx
  - src/components/debts/PersonFormDialog.tsx
  - src/components/debts/DebtsList.tsx
  - src/app/debts/page.tsx
  - src/lib/debts.ts
autonomous: true
requirements:
  - TD-REFRESH-01
  - TD-STATUS-01
  - TD-ASSERT-01
estimate:
  tokens: 25000
  raw_tokens: 50000
  tasks: 3
  confidence: high
must_haves:
  truths:
    - After successful debt/person mutation from DebtDetailDialog, client calls router.refresh while server still revalidates only /debts (TD-REFRESH-01)
    - /debts RSC calls assertStatusSynced after every remainingMinor recompute before serializing list or totals props (TD-STATUS-01)
    - assertInitialImmutable JSDoc states runtime DEBT-03 is Zod updateDebtMetaSchema omit plus strict; helper remains the unit-test contract (TD-ASSERT-01)
    - CLOSED bucketing stays status-based; desync hard-throws instead of coercing display status
  artifacts:
    - path: src/components/debts/DebtDetailDialog.tsx
      provides: shell onSuccess router.refresh before close
    - path: src/app/debts/page.tsx
      provides: assertStatusSynced after remainingMinor on list + totals maps
    - path: src/lib/debts.ts
      provides: assertInitialImmutable documented as Zod-backed test contract
  key_links:
    - from: src/components/debts/DebtDetailDialog.tsx
      to: next/navigation useRouter
      via: router.refresh on mutation success
      pattern: "router.refresh"
    - from: src/app/debts/page.tsx
      to: src/lib/debts.ts
      via: assertStatusSynced(d.status, remaining)
      pattern: "assertStatusSynced"
    - from: src/app/debts/actions.ts
      to: revalidatePath
      via: keep /debts only (DISOL-01)
      pattern: "revalidatePath(\"/debts\")"
---

## Phase Goal

Close v1.1 audit tech debt — debts list refresh after mutations, Nyquist validate phases 10–11, small maintainability leftovers from audit.

## Artifacts this phase produces

| Artifact | Plan | Purpose |
|----------|------|---------|
| Client `router.refresh` on debts mutation success | 01 | TD-REFRESH-01 belt-and-suspenders |
| `assertStatusSynced` on `/debts` page | 01 | TD-STATUS-01 hard desync fail |
| `assertInitialImmutable` JSDoc → Zod contract | 01 | TD-ASSERT-01 document path |
| `src/components/ui/destructive-confirm-step.tsx` | 02 | TD-UIHOME-01 shared home |
| `10-VALIDATION.md` / `11-VALIDATION.md` validated | 03 | NYQ-10 / NYQ-11 |
| `12-VALIDATION.md` reconciled | 03 | NYQ-12 |

## SPECLESS probe fallback (recorded)

phase_req_ids was TBD (no REQUIREMENTS.md Phase 12 REQ rows). Spec-less probe skipped (visible skip). Requirements: RESEARCH **TD-*** / **NYQ-*** IDs from audit tech_debt. No CONTEXT.md / no D-XX locks — honor RESEARCH + audit scope only.

<objective>
Tracer + expand: reliable `/debts` client refresh after mutations (TD-REFRESH-01), page-level status/remaining hard assert (TD-STATUS-01), and document assertInitialImmutable as Zod-backed test contract (TD-ASSERT-01).

Purpose: Kill audit fragile-refresh + status-desync + unused-assert debt before UI home move and Nyquist docs.
Output: router.refresh on all debts success shells; assertStatusSynced wired on page; JSDoc on assertInitialImmutable.
</objective>

<execution_context>
@/home/artem/Documents/wallet/.cursor/gsd-core/workflows/execute-plan.md
@/home/artem/Documents/wallet/.cursor/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/v1.1-MILESTONE-AUDIT.md
@.planning/phases/12-address-tech-debt-debts-refresh-nyquist-10-11/12-RESEARCH.md
@.planning/phases/12-address-tech-debt-debts-refresh-nyquist-10-11/12-PATTERNS.md
@.planning/phases/12-address-tech-debt-debts-refresh-nyquist-10-11/12-VALIDATION.md
@src/app/debts/page.tsx
@src/app/debts/actions.ts
@src/app/debts/actions.test.ts
@src/lib/debts.ts
@src/components/debts/DebtDetailDialog.tsx
@src/components/debts/DebtFormDialog.tsx
@src/components/debts/PersonFormDialog.tsx
@src/components/debts/DebtsList.tsx
@node_modules/next/dist/docs/01-app/03-api-reference/04-functions/use-router.md
</context>

<tasks>

<task type="tracer">
  <name>End-to-end debts refresh + status assert — DebtDetailDialog + page</name>
  <files>src/components/debts/DebtDetailDialog.tsx, src/app/debts/page.tsx</files>
  <read_first>
    src/components/debts/DebtDetailDialog.tsx
    src/app/debts/page.tsx
    src/lib/debts.ts
    src/app/debts/actions.ts
    .planning/phases/12-address-tech-debt-debts-refresh-nyquist-10-11/12-PATTERNS.md
    node_modules/next/dist/docs/01-app/03-api-reference/04-functions/use-router.md
  </read_first>
  <action>
    Per TD-REFRESH-01 and TD-STATUS-01 (RESEARCH Pattern 1–2 / PATTERNS DebtDetailDialog + page): In DebtDetailDialog shell, import useRouter from next/navigation, call router.refresh() then onOpenChange(false) inside the existing onSuccess shell wrapper (currently closes via onOpenChange(false)) so all body success paths (useActionState effects, confirm deletes, forgive) inherit one refresh. Do not refresh on failure paths. Do not edit Server Actions revalidatePath calls — keep existing /debts-only invalidation (DISOL-01). In page.tsx, import assertStatusSynced from @/lib/debts; immediately after each remainingMinor(...) computation (people.debts map and totalsInputs flatMap), call assertStatusSynced(d.status, remaining) before building the return object. Hard throw only — do not coerce status from remaining for display. Keep DebtsList CLOSED bucketing on status (no change this task).
  </action>
  <verify>
    <automated>npx vitest run src/lib/debts.test.ts -t assert && npx vitest run src/app/debts/actions.test.ts && grep -q 'router.refresh' src/components/debts/DebtDetailDialog.tsx && test "$(grep -c 'assertStatusSynced' src/app/debts/page.tsx)" -ge 2</automated>
    <fails_when>non-zero vitest exit, "0 passed" for assert or actions suites, missing router.refresh in DebtDetailDialog, or assertStatusSynced appears fewer than twice in page.tsx</fails_when>
  </verify>
  <acceptance_criteria>
    - DebtDetailDialog contains useRouter and router.refresh on the shell success path
    - page.tsx imports assertStatusSynced and invokes it in both remainingMinor map sites
    - src/app/debts/actions.test.ts still passes (revalidatePath("/debts") kept; dashboard root not revalidated)
    - assert* tests in src/lib/debts.test.ts pass
  </acceptance_criteria>
  <done>One mutation success path refreshes client cache; /debts RSC hard-fails on status/remaining desync (TD-REFRESH-01, TD-STATUS-01)</done>
</task>

<task type="auto" tdd="false">
  <name>Expand router.refresh to Form, Person, DebtsList delete</name>
  <files>src/components/debts/DebtFormDialog.tsx, src/components/debts/PersonFormDialog.tsx, src/components/debts/DebtsList.tsx</files>
  <read_first>
    src/components/debts/DebtFormDialog.tsx
    src/components/debts/PersonFormDialog.tsx
    src/components/debts/DebtsList.tsx
    src/components/debts/DebtDetailDialog.tsx
    .planning/phases/12-address-tech-debt-debts-refresh-nyquist-10-11/12-PATTERNS.md
  </read_first>
  <action>
    Per TD-REFRESH-01 expand from tracer: DebtFormDialog and PersonFormDialog shell onSuccess handlers call router.refresh() before setOpen(false), matching DebtDetailDialog. In DebtsList handleConfirmDelete, after result.success, call setConfirmOpen(false) then router.refresh() (import useRouter). Do not refresh on blocked-delete or result.success === false. Do not add revalidatePath for dashboard root. Leave DestructiveConfirmStep import paths unchanged this plan (Plan 02 owns TD-UIHOME-01).
  </action>
  <verify>
    <automated>npx vitest run src/app/debts/actions.test.ts && grep -q 'router.refresh' src/components/debts/DebtFormDialog.tsx && grep -q 'router.refresh' src/components/debts/PersonFormDialog.tsx && grep -q 'router.refresh' src/components/debts/DebtsList.tsx && grep -q 'router.refresh' src/components/debts/DebtDetailDialog.tsx</automated>
    <fails_when>non-zero actions.test exit, or any of DebtFormDialog / PersonFormDialog / DebtsList / DebtDetailDialog lacks router.refresh</fails_when>
  </verify>
  <acceptance_criteria>
    - All four files under src/components/debts listed in files contain router.refresh
    - actions.test.ts still green for /debts revalidate and DISOL dashboard-root exclusion
  </acceptance_criteria>
  <done>All debts mutation success UIs refresh client RSC props (TD-REFRESH-01 complete)</done>
</task>

<task type="auto" tdd="false">
  <name>Document assertInitialImmutable as Zod test contract</name>
  <files>src/lib/debts.ts</files>
  <read_first>
    src/lib/debts.ts
    src/lib/validations/debts.ts
    src/lib/debts.test.ts
    .planning/phases/12-address-tech-debt-debts-refresh-nyquist-10-11/12-RESEARCH.md
  </read_first>
  <action>
    Per TD-ASSERT-01 RESEARCH Q1 / PATTERNS debts.ts: Extend JSDoc on assertInitialImmutable to state (1) DEBT-03 / create-time principal immutability, (2) runtime enforcement is updateDebtMetaSchema omitting initial fields with .strict() in src/lib/validations/debts.ts, (3) this helper is the unit-test contract and is not called from Server Actions because update path accepts no proposed initial. Do not wire a fake call into updateDebtMeta. Do not change function body. Keep existing debts.test.ts assert coverage.
  </action>
  <verify>
    <automated>npx vitest run src/lib/debts.test.ts -t assert && grep -q 'updateDebtMetaSchema' src/lib/debts.ts && grep -q 'assertInitialImmutable' src/lib/debts.ts</automated>
    <fails_when>non-zero vitest exit, "0 passed", or debts.ts JSDoc/body missing updateDebtMetaSchema citation near assertInitialImmutable</fails_when>
  </verify>
  <acceptance_criteria>
    - assertInitialImmutable JSDoc cites updateDebtMetaSchema and .strict() / omit behavior
    - function signature and throw message unchanged
    - assert tests still pass
  </acceptance_criteria>
  <done>TD-ASSERT-01 closed via document path; status assert already wired in tracer (TD-STATUS-01)</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Client Dialog → Server Action | Untrusted FormData; existing Zod gates must stay |
| RSC page → DebtsList props | status/remaining must match or throw |
| Debts actions → Next cache | Must not invalidate Капитал `/` |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-12-01 | Tampering | debts Server Actions / client refresh | medium | mitigate | Keep revalidatePath("/debts") only; actions.test forbids dashboard root; router.refresh never substitutes for path invalidate |
| T-12-02 | Tampering | page.tsx status vs remaining | medium | mitigate | assertStatusSynced after every remainingMinor; hard throw on desync |
| T-12-03 | Spoofing | CLOSED bucket vs hero OPEN filter | low | mitigate | Keep status as single UI source; assert keeps DB status honest |
| T-12-04 | Tampering | updateDebtMeta initial smuggle | medium | mitigate | Document Zod omit+.strict as runtime DEBT-03; do not weaken schemas |
| T-12-SC | Tampering | npm/pip installs | high | accept | No package installs this plan |
</threat_model>

<verification>
DebtDetail/Form/Person/DebtsList call router.refresh on success; page asserts status sync twice; assertInitialImmutable documents Zod contract; actions DISOL tests green.
</verification>

<success_criteria>
TD-REFRESH-01, TD-STATUS-01, TD-ASSERT-01 observable in code + greps + vitest.
</success_criteria>

<output>
Create `.planning/phases/12-address-tech-debt-debts-refresh-nyquist-10-11/12-01-SUMMARY.md` when done
</output>
