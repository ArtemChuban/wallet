---
phase: 08-debts-schema-domain-math
plan: 02
type: execute
wave: 2
depends_on:
  - "08-01"
files_modified:
  - src/lib/debts.ts
  - src/lib/debts.test.ts
autonomous: true
requirements:
  - DEBT-02
  - DEBT-03
estimate:
  tokens: 18000
  raw_tokens: 36000
  tasks: 2
  confidence: high
must_haves:
  truths:
    - assertRepaymentAmount rejects amountMinor less than or equal to 0n and amountMinor greater than remainingBefore (D-15, D-05 symmetry)
    - assertSizeDelta rejects deltaMinor === 0n and rejects downs that would make currentPrincipal plus delta less than sum of repayments (D-05, D-15)
    - assertInitialImmutable rejects any attempt to treat initialAmountMinor as editable after create (DEBT-03 / D-03)
    - assertStatusSynced throws when stored status disagrees with statusForRemaining(remaining) (D-13)
    - Events are allowed on CLOSED debts at the helper layer — status is recomputed from remaining after apply (D-14); callers pass post-event remaining into statusForRemaining
    - computeDebtPrimaryTotals includes OPEN debts only; CLOSED ignored (D-16)
    - Missing FX for non-primary OPEN debt excludes that debt and sets isPartial true (D-17); primary currency uses identity without rate (D-18)
    - Aggregates expose iOwePrimaryMinor and theyOwePrimaryMinor plus per-debt rows (D-19)
    - Boundary: remaining before repay of exactly remainingBefore accepts; remainingBefore + 1n rejects; size-down exactly to sumRepayments accepts; one minor below rejects
  prohibitions:
    - statement: MUST NOT clamp negative remaining to 0n inside remaining helpers — illegal events must throw via asserts
      requirement_id: DEBT-02
      category: values
    - statement: MUST NOT provide an update-initial helper that mutates Debt.initialAmountMinor
      requirement_id: DEBT-03
      category: safety
    - statement: MUST NOT invent FX rates or count missing FX as included primary contribution 0n without isPartial
      requirement_id: DEBT-02
      category: values
  artifacts:
    - path: src/lib/debts.ts
      provides: assertRepaymentAmount, assertSizeDelta, assertInitialImmutable, assertStatusSynced, computeDebtPrimaryTotals
    - path: src/lib/debts.test.ts
      provides: reject/boundary cases, immutability, totals OPEN/partial/identity cases
  key_links:
    - from: assertRepaymentAmount
      to: remainingMinor
      via: remainingBefore argument from current ledger state
      pattern: "assertRepaymentAmount"
    - from: computeDebtPrimaryTotals
      to: convertOtherMinorToPrimaryMinor
      via: non-primary OPEN debts with rateToPrimaryScaled
      pattern: "convertOtherMinorToPrimaryMinor"
    - from: assertInitialImmutable
      to: Debt.initialAmountMinor
      via: domain gate before any edit attempt (Server Actions Phase 9/10)
      pattern: "assertInitialImmutable"
---

<objective>
Expand the debts domain module with over-repayment / illegal size-down / initial-immutability / status-sync asserts (DEBT-03, D-05, D-12–D-15) and OPEN-only primary totals with NW-style FX honesty (D-16–D-19).

Purpose: Lock validation and totals API Phase 9–11 will call — still no UI and no NW imports.
Output: Complete `src/lib/debts.ts` assert + totals surface with green Vitest coverage.
</objective>

<execution_context>
@/home/artem/Documents/wallet/.cursor/gsd-core/workflows/execute-plan.md
@/home/artem/Documents/wallet/.cursor/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/08-debts-schema-domain-math/08-CONTEXT.md
@.planning/phases/08-debts-schema-domain-math/08-RESEARCH.md
@.planning/phases/08-debts-schema-domain-math/08-PATTERNS.md
@.planning/phases/08-debts-schema-domain-math/08-01-PLAN.md
@src/lib/debts.ts
@src/lib/debts.test.ts
@src/lib/net-worth.ts
@src/lib/money.ts
@src/lib/net-worth.test.ts
</context>

<assumptions>
## Planner assumptions (D-18 adaptation)

- A6: CONTEXT D-18 says totals helper takes explicit `asOfDate` for LOCF. Phase 8 pure helper takes **caller-resolved** `rateToPrimaryScaled` instead (RESEARCH A5 / Pattern 3). Caller resolves LOCF as-of that date outside the helper; primary identity and missing-FX `isPartial` honesty unchanged. `asOfDate` is not a field on `computeDebtPrimaryTotals` input in Phase 8.
</assumptions>

## Artifacts this phase produces

- `assertRepaymentAmount(amountMinor, remainingBefore): void`
- `assertSizeDelta(deltaMinor, currentPrincipal, sumRepayments): void`
- `assertInitialImmutable(...): void` — rejects mutation of create-time principal
- `assertStatusSynced(status, remaining): void`
- `computeDebtPrimaryTotals(input): { rows, iOwePrimaryMinor, theyOwePrimaryMinor, isPartial }`
- Types mirroring NW row shape for debt totals (direction, remaining, FX flags)

<tasks>

<task type="auto" tdd="true">
  <name>Domain asserts: repayment, size delta, initial immutability, status sync</name>
  <files>src/lib/debts.ts, src/lib/debts.test.ts</files>
  <read_first>
    .planning/phases/08-debts-schema-domain-math/08-CONTEXT.md
    .planning/phases/08-debts-schema-domain-math/08-RESEARCH.md
    src/lib/debts.ts
    src/lib/debts.test.ts
    src/lib/money.test.ts
  </read_first>
  <behavior>
    - assertRepaymentAmount(5n, 5n) ok; assertRepaymentAmount(6n, 5n) throws; assertRepaymentAmount(0n, 5n) throws; assertRepaymentAmount(-1n, 5n) throws
    - assertSizeDelta(0n, ...) throws; assertSizeDelta(-3n, principal 10n, repaid 8n) throws (next principal 7n less than repaid 8n); assertSizeDelta(-2n, 10n, 8n) ok; assertSizeDelta(+5n, ...) ok
    - assertInitialImmutable rejects proposed new initial after create (DEBT-03 / D-03)
    - assertStatusSynced("OPEN", 0n) throws; assertStatusSynced("CLOSED", 0n) ok; assertStatusSynced("OPEN", 1n) ok
  </behavior>
  <action>
    Per D-05, D-12–D-15, DEBT-03 / D-03: extend src/lib/debts.test.ts first with throw/accept cases matching RESEARCH Code Examples for assertRepaymentAmount and assertSizeDelta (including exact boundary: repay equal remaining ok; repay remaining+1n throws; size-down to principal == sumRepayments ok; one minor further throws). Add assertInitialImmutable coverage that any helper meant to guard edits of initialAmountMinor throws when a caller attempts to change the create-time principal (size changes remain the only adjustment path). Add assertStatusSynced cases for desync bugs. Then implement the four exports in src/lib/debts.ts using throw new Error messages in the same style as money.ts rejects. Repayment amounts must be strictly greater than 0n (A2). Do not add Prisma. Do not implement series builders (D-23). Keep DISOL-01 tests intact.
  </action>
  <verify>
    <automated>npx vitest run src/lib/debts.test.ts && grep -q 'assertRepaymentAmount' src/lib/debts.ts && grep -q 'assertSizeDelta' src/lib/debts.ts && grep -q 'assertInitialImmutable' src/lib/debts.ts && grep -q 'assertStatusSynced' src/lib/debts.ts</automated>
    <fails_when>non-zero vitest exit, or any of the four assert exports missing from debts.ts</fails_when>
  </verify>
  <acceptance_criteria>
    - src/lib/debts.ts exports assertRepaymentAmount, assertSizeDelta, assertInitialImmutable, assertStatusSynced
    - Tests cover over-repay, zero/illegal size delta, initial immutability, status desync
    - Boundary cases at remaining equality and one-step-over fail/pass as specified in behavior
    - npx vitest run src/lib/debts.test.ts exits 0
  </acceptance_criteria>
  <done>Domain asserts green; DEBT-03 initial immutability helper locked</done>
</task>

<task type="auto" tdd="true">
  <name>computeDebtPrimaryTotals OPEN-only with FX honesty</name>
  <reversibility rating="costly">Totals return shape becomes the Phase 11 `/debts` hero contract; renaming aggregates later churns UI.</reversibility>
  <files>src/lib/debts.ts, src/lib/debts.test.ts</files>
  <read_first>
    .planning/phases/08-debts-schema-domain-math/08-CONTEXT.md
    .planning/phases/08-debts-schema-domain-math/08-PATTERNS.md
    src/lib/net-worth.ts
    src/lib/net-worth.test.ts
    src/lib/money.ts
    src/lib/debts.ts
    src/lib/debts.test.ts
  </read_first>
  <behavior>
    - CLOSED debts omitted from aggregates and rows input filter (D-16)
    - Primary I_OWE remaining uses identity; rateToPrimaryScaled null still included
    - Non-primary without rate: excludeReason no_fx, includedInTotal false, isPartial true, contribution 0n (D-17)
    - Non-primary with rate: convertOtherMinorToPrimaryMinor into iOwe or theyOwe bucket by direction (D-18, D-19)
    - Mixed OPEN set: both aggregates correct; isPartial true if any excluded
  </behavior>
  <action>
    Per D-16–D-19: define DebtPrimaryTotals input types (id, direction I_OWE|THEY_OWE, status, remainingMinor, currencyScale, isPrimaryCurrency, rateToPrimaryScaled null|bigint, primaryScale) mirroring computeNetWorthRows patterns from src/lib/net-worth.ts without importing net-worth into debts or vice versa. Implement computeDebtPrimaryTotals that filters to OPEN only, converts via identity or convertOtherMinorToPrimaryMinor from @/lib/money, splits aggregates into iOwePrimaryMinor and theyOwePrimaryMinor, sets isPartial when any OPEN row is excluded for missing FX. Do not invent rates. Do not include CLOSED. Author Vitest cases mirroring net-worth.test.ts partial/identity patterns. Helper stays pure; caller supplies asOfDate-resolved rates outside. **D-18 adaptation (A6):** CONTEXT D-18 says helper takes explicit asOfDate for LOCF; pure-helper design (RESEARCH A5 / Pattern 3) takes caller-resolved rateToPrimaryScaled instead — asOfDate/LOCF stay at the call site (Phase 11 pages). Honesty semantics (identity primary, null rate = exclude + isPartial) unchanged. Do not add asOfDate to the totals input unless a later phase needs it for API clarity. No chart series (D-23).
  </action>
  <verify>
    <automated>npx vitest run src/lib/debts.test.ts && grep -q 'computeDebtPrimaryTotals' src/lib/debts.ts && grep -q 'iOwePrimaryMinor' src/lib/debts.ts && grep -q 'theyOwePrimaryMinor' src/lib/debts.ts && grep -q 'convertOtherMinorToPrimaryMinor' src/lib/debts.ts</automated>
    <fails_when>non-zero vitest exit, totals export or aggregate field names missing, or money conversion import unused/absent</fails_when>
  </verify>
  <acceptance_criteria>
    - computeDebtPrimaryTotals returns rows plus iOwePrimaryMinor, theyOwePrimaryMinor, isPartial
    - Tests prove OPEN-only, primary identity, no_fx exclusion with isPartial
    - src/lib/debts.ts imports convertOtherMinorToPrimaryMinor from @/lib/money only (not from net-worth)
    - npx vitest run src/lib/debts.test.ts exits 0
  </acceptance_criteria>
  <done>Primary totals API locked for Phase 11 UI</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Event amount → assert* | Untrusted repayment/delta values must be rejected before persistence (actions later) |
| Totals input rates → aggregates | Caller-supplied FX; missing rate must not silently become zero contribution |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-08-01 | Tampering | assertRepaymentAmount / assertSizeDelta | high | mitigate | Throw on remaining less than 0 paths; no clamp-to-zero |
| T-08-06 | Information disclosure | computeDebtPrimaryTotals | low | accept | Single-user local app; totals are user-owned data |
| T-08-04 | Tampering | debts ↔ NW coupling | high | mitigate | Totals live only in debts.ts; keep DISOL-01 tests green |
| T-08-07 | Tampering | assertInitialImmutable bypass | medium | mitigate | Dedicated helper + tests; Server Actions Phase 9 must call it |
| T-08-SC | Tampering | npm installs | high | mitigate | No new packages |
</threat_model>

<verification>
npx vitest run src/lib/debts.test.ts covers asserts, boundaries, immutability, and primary totals.
</verification>

<success_criteria>
Illegal repayments/size-downs throw; initialAmountMinor cannot be edited via domain helper; OPEN-only primary totals with isPartial honesty.
</success_criteria>

<output>
Create `.planning/phases/08-debts-schema-domain-math/08-02-SUMMARY.md` when done
</output>
