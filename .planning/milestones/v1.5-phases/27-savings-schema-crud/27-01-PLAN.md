---
phase: 27-savings-schema-crud
plan: 01
type: execute
wave: 0
depends_on: []
files_modified:
  - src/lib/validations/account.test.ts
  - src/lib/account-type.test.ts
  - src/lib/net-worth.test.ts
  - src/lib/savings-accrual-display.test.ts
  - src/app/accounts/actions.test.ts
autonomous: true
requirements:
  - ACCT-01
  - ACCT-02
  - ACCT-03
estimate:
  tokens: 20000
  raw_tokens: 40000
  tasks: 2
  confidence: high
must_haves:
  truths:
    - Failing Vitest cases exist for SAVINGS Zod createAccountSchema refine before implementation (ACCT-01, D-01…D-04, D-15) — update Zod refine owned by Plan 03
    - Failing cases exist for isAssetType("SAVINGS") and accountTypeLabel «Накопительный» (ACCT-02, ACCT-03, D-11, D-14)
    - Failing cases exist for computeNetWorthRows including SAVINGS LOCF like ASSET (ACCT-02, D-14)
    - Failing cases exist for next-accrual / days-until clamp display helper (ACCT-03, D-05, D-12)
    - Hard-fail createAccount SAVINGS persist cases exist (ACCT-01); update + no-BalanceSnapshot contracts planted as it.todo or describe.skip owned by Plan 03 (D-08, D-16) — Phase 20 Wave 0 poison fix
  artifacts:
    - path: src/lib/validations/account.test.ts
      provides: SAVINGS createAccountSchema refine expectations (red); no update-schema hard-fails
    - path: src/lib/account-type.test.ts
      provides: SAVINGS soft-read + label expectations (red)
    - path: src/lib/net-worth.test.ts
      provides: SAVINGS NW inclusion expectation (red)
    - path: src/lib/savings-accrual-display.test.ts
      provides: clamp + today/next-month / «сегодня» expectations (red)
    - path: src/app/accounts/actions.test.ts
      provides: create SAVINGS hard-fail; update/no-snapshot as todo/skip for Plan 03
  key_links:
    - from: Wave 0 red tests
      to: Plans 27-02…27-04 GREEN
      via: same test file paths; executors make red→green without rewriting contracts
      pattern: "SAVINGS"
---

<objective>
Author Wave 0 failing Vitest contracts for SAVINGS schema+CRUD (ACCT-01…03, D-01…D-16 coverage seeds) so tracer/expansion plans turn them green.

Purpose: Nyquist feedback before schema/UI work; lock expected I/O for rate/DOM/NW/list helper.
Output: Extended/new `*.test.ts` files that fail against current code; zero production implementation.
</objective>

<execution_context>
@/home/artem/Documents/wallet/.cursor/gsd-core/workflows/execute-plan.md
@/home/artem/Documents/wallet/.cursor/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/27-savings-schema-crud/27-CONTEXT.md
@.planning/phases/27-savings-schema-crud/27-RESEARCH.md
@.planning/phases/27-savings-schema-crud/27-PATTERNS.md
@.planning/phases/27-savings-schema-crud/27-VALIDATION.md
@src/lib/validations/account.test.ts
@src/lib/account-type.test.ts
@src/lib/net-worth.test.ts
@src/app/accounts/actions.test.ts
</context>

## Artifacts this phase produces

- `AccountType.SAVINGS` enum member + `annualRateBps` / `accrualDayOfMonth` on `Account`
- Migration with `Account_savings_rate_invariant` (keep credit + grace CHECKs)
- `parsePercentToBps` / `formatBpsToPercentMajor` (scale-2)
- `nextAccrualAsOf` / days-until RU helpers in `src/lib/savings-accrual-display.ts`
- Extended `AccountTypeSoft` / `isAssetType` / `accountTypeLabel` / `NetWorthAccountType`
- Extended `createAccountSchema` + coherent `updateAccount` schema/action for SAVINGS
- `AccountFormDialog` TYPE_OPTIONS + gated «Годовой %» / «День начисления»
- `AccountList` secondary `Накопительный · CCY · 16.50% · через N дн.|сегодня`
- Wave 0 red tests listed in this plan (contracts only here)

<tasks>

<task type="auto">
  <name>Wave 0: red Zod + soft-read + NW SAVINGS contracts</name>
  <files>src/lib/validations/account.test.ts, src/lib/account-type.test.ts, src/lib/net-worth.test.ts</files>
  <read_first>
    .planning/phases/27-savings-schema-crud/27-CONTEXT.md
    .planning/phases/27-savings-schema-crud/27-RESEARCH.md
    .planning/phases/27-savings-schema-crud/27-VALIDATION.md
    src/lib/validations/account.ts
    src/lib/validations/account.test.ts
    src/lib/account-type.ts
    src/lib/account-type.test.ts
    src/lib/net-worth.ts
    src/lib/net-worth.test.ts
  </read_first>
  <behavior>
    - createAccountSchema accepts type SAVINGS with annualRatePercentMajor "16.50" + accrualDayOfMonth 15 (D-01, D-04, D-05, ACCT-01)
    - createAccountSchema accepts annualRatePercentMajor "0" for SAVINGS (D-02)
    - createAccountSchema rejects missing rate, missing DOM, negative rate for SAVINGS (D-02, D-03, D-07)
    - createAccountSchema rejects savings fields on ASSET and rejects creditLimitMajor on SAVINGS (D-15)
    - isAssetType("SAVINGS") === true; accountTypeLabel("SAVINGS") === "Накопительный" (D-11, D-14, ACCT-02, ACCT-03)
    - computeNetWorthRows with type SAVINGS contributes positive LOCF like ASSET (D-14, ACCT-02)
  </behavior>
  <action>
    Extend existing Vitest files only — no production code. Add cases for createAccountSchema SAVINGS happy path (percent major → later bps 1650 in action tests), 0% allowed, reject null/missing rate, reject missing DOM, reject negative percent, reject savings fields when type ASSET, reject creditLimitMajor when type SAVINGS (per D-01…D-05, D-07, D-15, ACCT-01). Extend account-type tests for isAssetType SAVINGS and label «Накопительный» (D-11, D-14). Extend net-worth.test.ts with a SAVINGS fixture row proving LOCF inclusion like ASSET (D-14, ACCT-02). Do not implement schema enum or Zod production changes here — tests must fail until Plan 27-02.
  </action>
  <verify>
    <automated>npx vitest run src/lib/validations/account.test.ts src/lib/account-type.test.ts src/lib/net-worth.test.ts; test $? -ne 0 &amp;&amp; grep -q 'SAVINGS' src/lib/validations/account.test.ts &amp;&amp; grep -q 'Накопительный' src/lib/account-type.test.ts &amp;&amp; grep -q 'SAVINGS' src/lib/net-worth.test.ts</automated>
    <fails_when>vitest exits 0 (all green — Wave 0 must stay red), or SAVINGS/Накопительный cases missing from the three test files</fails_when>
  </verify>
  <acceptance_criteria>
    - New/extended cases reference SAVINGS, «Накопительный», rate/DOM refine, and NW inclusion
    - Running the three files exits non-zero because production code lacks SAVINGS support
    - No prisma/schema.prisma or component production edits in this task
  </acceptance_criteria>
  <done>Red contracts for Zod + soft-read + NW SAVINGS committed</done>
</task>

<task type="auto">
  <name>Wave 0: red accrual-display + actions SAVINGS contracts</name>
  <files>src/lib/savings-accrual-display.test.ts, src/app/accounts/actions.test.ts</files>
  <read_first>
    .planning/phases/27-savings-schema-crud/27-RESEARCH.md
    .planning/phases/27-savings-schema-crud/27-UI-SPEC.md
    src/lib/dates.ts
    src/app/accounts/actions.ts
    src/app/accounts/actions.test.ts
  </read_first>
  <behavior>
    - nextAccrualAsOf("2026-02-10", 31) clamps Feb → 2026-02-28; after that day rolls to March clamp (D-05, D-12, ACCT-03)
    - When today equals next accrual calendar day, countdown copy is «сегодня»; else «через N дн.» for N≥1 (UI-SPEC / D-12)
    - createAccount with SAVINGS persists annualRateBps + accrualDayOfMonth — hard-fail until Plan 02 (D-01, D-04, ACCT-01)
    - update SAVINGS name+rate+DOM + no BalanceSnapshot on metadata — it.todo or describe.skip owned by Plan 03; never hard-fail in Wave 0 (D-08, D-16)
  </behavior>
  <action>
    Create src/lib/savings-accrual-display.test.ts importing planned helpers nextAccrualAsOf and formatAccrualCountdown (names discretionary but stable in test) covering Feb-31 clamp and «сегодня» / «через N дн.» (D-05, D-12, ACCT-03). Extend actions.test.ts with named owner blocks (Phase 20 poison-fix pattern): (1) createAccount SAVINGS — hard-fail expects for annualRateBps Int + accrualDayOfMonth (Plan 02 greens); (2) updateAccount SAVINGS name+rate+DOM and BalanceSnapshot-not-called on metadata — describe.skip or it.todo only, owned by Plan 03 — do not plant hard-failing expects for update/D-16 until Plan 03. Production helpers/actions stay unimplemented. Do not add npm packages.
  </action>
  <verify>
    <automated>npx vitest run src/lib/savings-accrual-display.test.ts src/app/accounts/actions.test.ts; test $? -ne 0 &amp;&amp; test -f src/lib/savings-accrual-display.test.ts &amp;&amp; grep -q 'SAVINGS' src/app/accounts/actions.test.ts &amp;&amp; (grep -qE 'describe\.skip|it\.todo' src/app/accounts/actions.test.ts)</automated>
    <fails_when>vitest exits 0 (create/display must stay red), savings-accrual-display.test.ts missing, SAVINGS absent from actions.test.ts, or update/D-16 planted as hard-fail instead of todo/skip</fails_when>
  </verify>
  <acceptance_criteria>
    - savings-accrual-display.test.ts exists with clamp + countdown cases
    - actions.test.ts has hard-fail create SAVINGS; update/no-snapshot as todo/skip for Plan 03
    - Combined vitest run exits non-zero (create + display red; update skipped)
  </acceptance_criteria>
  <done>Red display + create contracts; Plan-03 update stubs as todo/skip committed</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Test contracts → production Zod/actions | Wave 0 encodes ASVS input rules; wrong red tests mislead GREEN plans |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-27-01 | Tampering | create/update type/currency | high | mitigate | Red tests require server-side type from DB on edit; create enum ASSET\|FIAT_CREDIT\|SAVINGS only (implemented 27-02/03) |
| T-27-02 | Tampering | annualRateBps | high | mitigate | Red Zod cases reject negative rate; 0 allowed (D-02, D-03) |
| T-27-03 | Tampering | savings fields on ASSET | high | mitigate | Red refine rejects savings fields on non-SAVINGS (D-15) |
| T-27-04 | Tampering | accrualDayOfMonth | medium | mitigate | Red cases require DOM 1–31 for SAVINGS (D-05) |
| T-27-05 | Tampering | BalanceSnapshot auto interest | high | mitigate | Red actions case forbids snapshot writes on metadata update (D-16) |
| T-27-SC | Tampering | npm installs | high | mitigate | Zero new packages — no install task |
</threat_model>

<verification>
Wave 0 vitest subset exits non-zero with new SAVINGS cases present; no production schema/UI changes.
</verification>

<success_criteria>
All five test targets listed in must_haves.artifacts exist with red SAVINGS expectations ready for Plans 27-02…04.
</success_criteria>

<output>
Create `.planning/phases/27-savings-schema-crud/27-01-SUMMARY.md` when done
</output>
