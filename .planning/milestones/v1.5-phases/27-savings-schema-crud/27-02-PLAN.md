---
phase: 27-savings-schema-crud
plan: 02
type: execute
wave: 1
depends_on:
  - 27-01
files_modified:
  - prisma/schema.prisma
  - prisma/migrations/
  - src/lib/account-type.ts
  - src/lib/net-worth.ts
  - src/lib/validations/account.ts
  - src/lib/savings-rate.ts
  - src/app/accounts/actions.ts
  - src/components/accounts/AccountFormDialog.tsx
  - src/lib/foundation.test.ts
  - data/wallet.db
autonomous: false
requirements:
  - ACCT-01
  - ACCT-02
estimate:
  tokens: 50000
  raw_tokens: 100000
  tasks: 3
  confidence: high
must_haves:
  truths:
    - User can create SAVINGS with name, currency, annual rate (percent→bps), accrual DOM 1–31 (ACCT-01, D-01…D-05, D-07, D-14, D-15)
    - 0% annual rate persists as annualRateBps 0; null rate forbidden (D-02)
    - SQLite Account_savings_rate_invariant enforces rate+DOM iff type SAVINGS (D-15)
    - isAssetType/NetWorthAccountType include SAVINGS so principal sums in NW like ASSET (ACCT-02, D-14)
    - Create form shows «Годовой %» + «День начисления» only when type SAVINGS; no DOM default (D-07, D-10, D-13)
    - "UI empty create: opens with empty name; type default ASSET; SAVINGS gated fields empty when user switches to Накопительный"
    - "UI partial create: switching away from SAVINGS hides gated fields from submit; FIAT_CREDIT shows credit limit only"
    - Host DB migrated; credit+grace CHECKs retained alongside savings CHECK
  artifacts:
    - path: prisma/schema.prisma
      provides: AccountType.SAVINGS; annualRateBps; accrualDayOfMonth
    - path: prisma/migrations
      provides: savings_account RedefineTables + Account_savings_rate_invariant
    - path: src/lib/savings-rate.ts
      provides: parsePercentToBps; formatBpsToPercentMajor
    - path: src/lib/account-type.ts
      provides: SAVINGS soft union; isAssetType; label Накопительный
    - path: src/lib/net-worth.ts
      provides: NetWorthAccountType includes SAVINGS
    - path: src/lib/validations/account.ts
      provides: create write enum ASSET|FIAT_CREDIT|SAVINGS + savings refine
    - path: src/app/accounts/actions.ts
      provides: createAccount persists SAVINGS rate+DOM
    - path: src/components/accounts/AccountFormDialog.tsx
      provides: TYPE_OPTIONS SAVINGS + type-gated create fields
  key_links:
    - from: AccountFormDialog create submit
      to: createAccount → createAccountSchema → Account row
      via: FormData annualRatePercentMajor + accrualDayOfMonth → annualRateBps
      pattern: "parsePercentToBps"
    - from: Account_savings_rate_invariant
      to: annualRateBps / accrualDayOfMonth
      via: CHECK iff SAVINGS
      pattern: "Account_savings_rate_invariant"
    - from: NetWorthAccountType
      to: computeNetWorthRows
      via: non-credit LOCF path includes SAVINGS
      pattern: "SAVINGS"
---

<objective>
Confirm one-way savings CHECK (D-15), then ship end-to-end tracer: schema+migration+soft NW+Zod create+createAccount+create-form gates (ACCT-01, ACCT-02, D-01…D-05, D-07, D-10, D-13, D-14, D-15).

Purpose: Prove SAVINGS create path through every layer before edit/list expansion.
Output: Migrated DB, green Wave 0 create/NW/type tests, create dialog can add Накопительный.
</objective>

<execution_context>
@/home/artem/Documents/wallet/.cursor/gsd-core/workflows/execute-plan.md
@/home/artem/Documents/wallet/.cursor/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/27-savings-schema-crud/27-CONTEXT.md
@.planning/phases/27-savings-schema-crud/27-RESEARCH.md
@.planning/phases/27-savings-schema-crud/27-PATTERNS.md
@.planning/phases/27-savings-schema-crud/27-UI-SPEC.md
@.planning/phases/27-savings-schema-crud/27-VALIDATION.md
@.planning/phases/27-savings-schema-crud/27-01-SUMMARY.md
@prisma/schema.prisma
@prisma/migrations/20260909090903_credit_grace_dual_dom/migration.sql
@src/lib/money.ts
@src/lib/validations/account.ts
@src/app/accounts/actions.ts
@src/components/accounts/AccountFormDialog.tsx
</context>

## Artifacts this phase produces

- `AccountType.SAVINGS`; `annualRateBps`; `accrualDayOfMonth`
- `Account_savings_rate_invariant` migration (keep `Account_credit_limit_invariant` + `Account_grace_dom_invariant`)
- `parsePercentToBps` / `formatBpsToPercentMajor` in `src/lib/savings-rate.ts`
- Soft-read + NW union extensions
- Create Zod/action/form path for SAVINGS
- (Later plans) update action, list countdown, accrual-display module

<tasks>

<task type="checkpoint:decision" gate="blocking">
  <name>Confirm one-way Account_savings_rate_invariant + SAVINGS enum (D-14, D-15)</name>
  <decision>Lock SQLite Account_savings_rate_invariant (rate+DOM present iff type=SAVINGS else both null; annualRateBps &gt;= 0; DOM 1–31) and Prisma AccountType.SAVINGS before migration lands</decision>
  <context>
    Orchestrator + RESEARCH treat schema CHECK/migration as one-way: undoing after Phases 28–30 forecast/MCP readers requires another RedefineTables migrate and data cleanup. CONTEXT D-14/D-15 lock distinct SAVINGS (not ASSET flag) and CHECK twin of credit invariants. Confirm before schema lands.
  </context>
  <options>
    <option id="savings-check-enum">
      <name>SAVINGS enum + Account_savings_rate_invariant (CONTEXT D-14/D-15) — recommended</name>
      <pros>Matches discuss-phase; dual Zod+CHECK honesty; NW soft-read path clear; Phases 28–30 build on same columns</pros>
      <cons>Account table rebuild; cannot store rate on ASSET; migration must preserve credit+grace CHECKs</cons>
    </option>
    <option id="flag-on-asset">
      <name>Boolean isSavings on ASSET (rejected milestone)</name>
      <pros>Fewer enum members</pros>
      <cons>Contradicts D-14 / REQUIREMENTS Out of Scope; soft-read chaos</cons>
    </option>
    <option id="defer-check">
      <name>Columns without SQLite CHECK</name>
      <pros>Smaller first migrate</pros>
      <cons>Breaks D-15 dual enforcement; dirty rows possible</cons>
    </option>
  </options>
  <resume-signal>Select: savings-check-enum (recommended), flag-on-asset, or defer-check</resume-signal>
</task>

<task type="tracer">
  <name>End-to-end create SAVINGS — schema through create form</name>
  <reversibility rating="one-way">D-14/D-15 SAVINGS enum + Account_savings_rate_invariant on SQLite; undo needs RedefineTables and Phase 28–30 rewrites.</reversibility>
  <files>prisma/schema.prisma, prisma/migrations/, src/lib/savings-rate.ts, src/lib/account-type.ts, src/lib/net-worth.ts, src/lib/validations/account.ts, src/app/accounts/actions.ts, src/components/accounts/AccountFormDialog.tsx</files>
  <read_first>
    .planning/phases/27-savings-schema-crud/27-CONTEXT.md
    .planning/phases/27-savings-schema-crud/27-RESEARCH.md
    .planning/phases/27-savings-schema-crud/27-PATTERNS.md
    .planning/phases/27-savings-schema-crud/27-UI-SPEC.md
    prisma/schema.prisma
    prisma/migrations/20260909090903_credit_grace_dual_dom/migration.sql
    src/lib/money.ts
    src/lib/account-type.ts
    src/lib/net-worth.ts
    src/lib/validations/account.ts
    src/app/accounts/actions.ts
    src/components/accounts/AccountFormDialog.tsx
  </read_first>
  <behavior>
    - parsePercentToBps("16.50") === 1650; parsePercentToBps("0") === 0 (D-01, D-02)
    - createAccountSchema + createAccount accept SAVINGS with rate+DOM; reject ASSET+savings fields (D-04, D-15, ACCT-01)
    - isAssetType("SAVINGS") true; accountTypeLabel «Накопительный»; NetWorthAccountType includes SAVINGS (D-11, D-14, ACCT-02)
    - AccountFormDialog TYPE_OPTIONS includes SAVINGS; gated «Годовой %» / «День начисления» visible only for SAVINGS; no DOM default (D-07, D-10, D-13)
  </behavior>
  <action>
    After checkpoint selects savings-check-enum (per D-14, D-15): extend prisma AccountType with SAVINGS; add annualRateBps Int? and accrualDayOfMonth Int? with doc comments mirroring credit metadata (D-14, D-15). Generate migration folder name containing savings_account (create-only), then edit migration.sql as RedefineTables twin of credit_grace_dual_dom — recreate new_Account with all prior columns plus savings columns; keep Account_credit_limit_invariant and Account_grace_dom_invariant; add Account_savings_rate_invariant CHECK (type=SAVINGS ⇒ both rate+DOM set, bps&gt;=0, DOM BETWEEN 1 AND 31; else both null). INSERT old columns only. Add src/lib/savings-rate.ts using parseMajorToMinor/formatMinorToMajor scale 2 for parsePercentToBps / formatBpsToPercentMajor; reject negatives; clamp to Prisma Int max (D-01, D-03). Extend AccountTypeSoft, isAssetType, accountTypeLabel «Накопительный» (D-11, D-14). Add SAVINGS to NetWorthAccountType (D-14, ACCT-02). Extend createAccountSchema write enum to ASSET|FIAT_CREDIT|SAVINGS with superRefine: SAVINGS requires percent (0 OK) + DOM 1–31 and rejects creditLimit; FIAT_CREDIT keeps credit rules and rejects savings fields; ASSET rejects both (D-02…D-05, D-15). Wire createAccount to parse percent→bps and persist both fields for SAVINGS only (ACCT-01). Extend AccountFormDialog create: TYPE_OPTIONS + SAVINGS; type-gated inputs labels «Годовой %» and «День начисления»; no default DOM; mirror credit-limit visibility pattern (D-07, D-10, D-13). Do not implement edit updateAccount, list countdown, or interest ÷12. Do not add npm packages.
  </action>
  <verify>
    <automated>npx vitest run src/lib/validations/account.test.ts src/lib/account-type.test.ts src/lib/net-worth.test.ts &amp;&amp; npx vitest run src/app/accounts/actions.test.ts -t 'createAccount' &amp;&amp; grep -q 'SAVINGS' prisma/schema.prisma &amp;&amp; grep -q 'annualRateBps' prisma/schema.prisma &amp;&amp; grep -q 'accrualDayOfMonth' prisma/schema.prisma &amp;&amp; ls prisma/migrations/*savings_account*/migration.sql &amp;&amp; grep -q 'Account_savings_rate_invariant' prisma/migrations/*savings_account*/migration.sql &amp;&amp; grep -q 'Account_credit_limit_invariant' prisma/migrations/*savings_account*/migration.sql &amp;&amp; grep -q 'Account_grace_dom_invariant' prisma/migrations/*savings_account*/migration.sql &amp;&amp; grep -q 'Годовой %' src/components/accounts/AccountFormDialog.tsx &amp;&amp; grep -q 'День начисления' src/components/accounts/AccountFormDialog.tsx</automated>
    <fails_when>vitest non-zero on create/NW/type cases (create-filtered actions only — Plan 03 owns update/D-16), schema/migration CHECK names missing, credit/grace CHECKs dropped from migration SQL, or form labels absent</fails_when>
  </verify>
  <acceptance_criteria>
    - prisma/schema.prisma has SAVINGS + annualRateBps + accrualDayOfMonth (D-14, D-15)
    - migration SQL has Account_savings_rate_invariant and retains both prior Account CHECKs (D-15)
    - create Zod/action + soft NW tests green for SAVINGS (ACCT-01, ACCT-02, D-01…D-05); update describes stay todo/skip
    - Create form exposes gated RU labels only for SAVINGS (D-10, D-13)
  </acceptance_criteria>
  <done>Tracer create path green without Plan 03 update cases; host migrate deploy is next task</done>
</task>

<task type="auto">
  <name>[BLOCKING] Host prisma migrate deploy after savings schema</name>
  <files>data/wallet.db, prisma/migrations/, src/lib/foundation.test.ts</files>
  <read_first>
    .planning/phases/27-savings-schema-crud/27-RESEARCH.md
    prisma/schema.prisma
    src/lib/foundation.test.ts
  </read_first>
  <precondition>prisma/migrations contains savings_account migration.sql from the tracer task with Account_savings_rate_invariant; node_modules present</precondition>
  <reversibility rating="one-way">Host migrate applies SAVINGS columns + CHECK permanently; dropping later needs another migration.</reversibility>
  <action>
    If host SQLite locked by wallet-web, stop container briefly. Run DATABASE_URL=file:./data/wallet.db npx prisma migrate deploy (project-standard migrate — not db push). Confirm migrate status up to date. Run npx prisma generate if client types lag. Update foundation.test.ts only if it asserts Account column/CHECK/migration name lists — add savings_account migration name expectation when such asserts exist. Do not COPY wallet.db into Docker images. Do not add npm packages.
  </action>
  <verify>
    <automated>mkdir -p data &amp;&amp; DATABASE_URL="file:./data/wallet.db" npx prisma migrate deploy &amp;&amp; test -f data/wallet.db &amp;&amp; DATABASE_URL="file:./data/wallet.db" npx prisma migrate status 2&gt;&amp;1 | grep -qi 'up to date\|Database schema is up to date\|No pending migrations' &amp;&amp; npx vitest run src/lib/foundation.test.ts</automated>
    <fails_when>migrate deploy non-zero, data/wallet.db missing, pending migrations remain, or foundation.test.ts fails</fails_when>
  </verify>
  <acceptance_criteria>
    - DATABASE_URL=file:./data/wallet.db npx prisma migrate deploy exits 0
    - prisma migrate status reports no pending migrations
    - Live DB ready for later verification that needs SAVINGS columns
  </acceptance_criteria>
  <done>Blocking host migrate deploy succeeded; SAVINGS schema live</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Browser FormData → createAccount | Untrusted type/rate/DOM strings |
| Zod → Prisma Account insert | Must match CHECK or SQLite rejects |
| Soft NW cast → computeNetWorthRows | Missing SAVINGS excludes principal (ACCT-02) |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-27-01 | Tampering | create type/currency | high | mitigate | Zod write enum ASSET\|FIAT_CREDIT\|SAVINGS only; currency from Currency table |
| T-27-02 | Tampering | annualRateBps | high | mitigate | parsePercentToBps rejects negative; CHECK annualRateBps &gt;= 0; Zod min 0 (D-02, D-03) |
| T-27-03 | Tampering | savings fields on ASSET | high | mitigate | Zod superRefine + Account_savings_rate_invariant null-iff-non-SAVINGS (D-15) |
| T-27-04 | Tampering | accrualDayOfMonth | medium | mitigate | Zod + CHECK BETWEEN 1 AND 31 (D-05) |
| T-27-05 | Tampering | auto interest snapshot | high | mitigate | Tracer does not add interest writers; createAccount only inserts Account (D-16) |
| T-27-SC | Tampering | npm installs | high | mitigate | Zero new packages |
</threat_model>

<verification>
Vitest create/NW/type green; migration CHECK triad present; migrate deploy up to date; create form gated labels present.
</verification>

<success_criteria>
User can create SAVINGS end-to-end; principal type included in NW unions; host DB migrated with savings CHECK.
</success_criteria>

<output>
Create `.planning/phases/27-savings-schema-crud/27-02-SUMMARY.md` when done
</output>
