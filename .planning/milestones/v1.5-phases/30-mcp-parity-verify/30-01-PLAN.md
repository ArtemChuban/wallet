---
phase: 30-mcp-parity-verify
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/lib/mcp/tools/accounts.ts
  - src/lib/mcp/tools/accounts.test.ts
  - src/lib/mcp/reads/load-forecast-overlay.ts
  - src/lib/mcp/tools/forecast.ts
  - src/lib/mcp/tools/forecast.test.ts
  - src/lib/mcp/create-handler.ts
  - src/lib/mcp/isolation-contract.test.ts
autonomous: true
requirements:
  - MCP-01
  - MCP-02
  - PARITY-01
estimate:
  tokens: 55000
  raw_tokens: 55000
  tasks: 3
  confidence: low
must_haves:
  truths:
    - "No visual changes this phase — MCP + Vitest + Orca only (UI-SPEC)"
    - "Do not reopen Phase 29 chart/tooltip/banner chrome (UI-SPEC)"
    - "Done bar is Vitest contracts plus Orca MCP tool calls, not chart DOM UAT (D-13, D-14, UI-SPEC)"
    - "Every list_accounts row always emits annualRateBps, accrualDayOfMonth, and annualRatePercent — values for type SAVINGS, null for other types (MCP-01, D-01, boundary/empty)"
    - "annualRatePercent is a JSON number from Number(formatBpsToPercentMajor(bps)); annualRateBps Int stays source of truth (MCP-01, D-02, precision)"
    - "Agents filter with type === SAVINGS — catalog has no isSavings boolean (MCP-01, D-03, adjacency)"
    - "list_accounts tool description names SAVINGS rate, DOM, and percent fields; SAVISO tag stays off list_accounts (MCP-01, D-04, D-12)"
    - "Empty accounts array still serializes to { accounts: [] }; when rows exist Object.keys allow-list is stable and includes the three rate keys (MCP-01, empty/ordering)"
    - "loadForecastOverlay builds interest the same way as UI: SAVINGS today LOCF → listInterestSlotsInRange → ForecastSlot kind interest → slots concat income then interest then grace (MCP-02, D-05, D-08, ordering)"
    - "Default horizonEnd remains today+365 when omitted (MCP-02, D-06)"
    - "Interest forecastEvents carry kind, parentId, plannedAmountMinor, displayPrimaryMajor, currencyCode, accountId, accountName — no rate fields on the event (MCP-02, D-07)"
    - "get_forecast_overlay description and create-handler instructions use triple tag INISO-01/GRISO-01/SAVISO-01; prose is income + interest + grace; retired grace-line mark wording is gone (PARITY-01, D-09, D-10, D-11)"
    - statement: "Live Orca confirms list_accounts SAVINGS fields, overlay kind interest, and BalanceSnapshot count unchanged after forecast reads (D-14, D-15)"
      verification: backstop
  prohibitions:
    - statement: "Do not register new MCP tool names — extend list_accounts and get_forecast_overlay only"
      status: unresolved
      verification: judgment
    - statement: "Do not put isolation meta fields on JSON payloads"
      status: unresolved
      verification: judgment
    - statement: "Do not rewrite README connect docs this phase"
      status: unresolved
      verification: judgment
    - statement: "Do not put SAVISO on list_accounts"
      status: unresolved
      verification: judgment
    - statement: "Do not write BalanceSnapshot from forecast or interest MCP paths"
      status: unresolved
      verification: judgment
    - statement: "Do not mark the savings pending todo resolved mid Phase 30"
      status: unresolved
      verification: judgment
    - statement: "Do not implement ASSET ↔ SAVINGS conversion or chart legend split"
      status: unresolved
      verification: judgment
  artifacts:
    - path: src/lib/mcp/tools/accounts.ts
      provides: ListAccountRow rate fields + serialize/load + description (D-01…D-04)
    - path: src/lib/mcp/reads/load-forecast-overlay.ts
      provides: interest membership wired into buildNetWorthForecastSeries (D-05)
    - path: src/lib/mcp/tools/forecast.ts
      provides: GET_FORECAST_OVERLAY_DESCRIPTION triple tag (D-09, D-10)
    - path: src/lib/mcp/create-handler.ts
      provides: server instructions SAVISO-01 closer (D-09, D-10)
    - path: src/lib/mcp/tools/accounts.test.ts
      provides: SAVINGS field matrix + key allow-list
    - path: src/lib/mcp/tools/forecast.test.ts
      provides: interest event fixture + description asserts + loader source-scan
    - path: src/lib/mcp/isolation-contract.test.ts
      provides: SAVISO-01 required; stale grace-line mark absent
  key_links:
    - from: serializeListAccountsPayload
      to: formatBpsToPercentMajor
      via: Number(...) for annualRatePercent on SAVINGS only
      pattern: annualRatePercent
    - from: loadForecastOverlay
      to: listInterestSlotsInRange
      via: today LOCF SAVINGS filter matching page.tsx
      pattern: listInterestSlotsInRange
    - from: interestSlots
      to: buildNetWorthForecastSeries
      via: slots [...openSlots, ...interestSlots, ...graceSlots]
      pattern: interestSlots
    - from: GET_FORECAST_OVERLAY_DESCRIPTION
      to: createWalletMcpHandler instructions
      via: INISO-01/GRISO-01/SAVISO-01 triple tag
      pattern: SAVISO-01
---

<objective>
Ship MCP-01 / MCP-02 / PARITY-01 in one vertical slice: list_accounts SAVINGS catalog fields, get_forecast_overlay interest membership matching UI, and SAVISO-01 isolation copy — Wave 0 red contracts first, then production green (D-01…D-12).

Purpose: Agents match Phase 29 UI savings read surfaces in the same milestone.
Output: Extended accounts/forecast/isolation Vitest contracts + production MCP glue (no new tool names, no UI chrome).
</objective>

<execution_context>
@/home/artem/Documents/wallet/.cursor/gsd-core/workflows/execute-plan.md
@/home/artem/Documents/wallet/.cursor/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/OPERATOR.md
@.planning/phases/30-mcp-parity-verify/30-CONTEXT.md
@.planning/phases/30-mcp-parity-verify/30-RESEARCH.md
@.planning/phases/30-mcp-parity-verify/30-PATTERNS.md
@.planning/phases/30-mcp-parity-verify/30-VALIDATION.md
@.planning/phases/30-mcp-parity-verify/30-UI-SPEC.md
@src/lib/mcp/tools/accounts.ts
@src/lib/mcp/tools/accounts.test.ts
@src/lib/mcp/reads/load-forecast-overlay.ts
@src/lib/mcp/tools/forecast.ts
@src/lib/mcp/tools/forecast.test.ts
@src/lib/mcp/create-handler.ts
@src/lib/mcp/isolation-contract.test.ts
@src/lib/savings-interest.ts
@src/lib/savings-rate.ts
@src/app/page.tsx
@src/components/dashboard/DashboardChartsShell.tsx
</context>

## Artifacts this phase produces

- `src/lib/mcp/tools/accounts.ts` — rate/DOM/percent catalog fields + description
- `src/lib/mcp/reads/load-forecast-overlay.ts` — interest membership
- `src/lib/mcp/tools/forecast.ts` / `src/lib/mcp/create-handler.ts` — SAVISO triple-tag copy
- Extended Vitest: `accounts.test.ts`, `forecast.test.ts`, `isolation-contract.test.ts`
- `COVERAGE.md` (plan 30-02) — no external API declaration
- `{phase}-UAT.md` at verify (plan 30-02) — Orca MCP evidence

## Flagged assumptions (unclassified edges — not dropped)

- MCP-02: live membership/amounts vs UI Proгноз are confirmed by Orca (Vitest stays SQLite-free fixtures + source-scan of loader).
- PARITY-01: same-milestone close means this phase finishes MCP after UI Phase 29; no new tool names; savings todo stays open until `/gsd-complete-milestone` v1.5 (D-16).
- A3: `Number(formatBpsToPercentMajor(bps))` (e.g. 16.5) is the accepted JSON number shape for D-02.

## Prohibitions

Must NOT: new MCP tool names; payload isolation meta; README connect rewrite; SAVISO on list_accounts; BalanceSnapshot writes from forecast/interest paths; mark savings todo resolved; Phase 31 conversion; chart legend; reopen Phase 29 overlay chrome.

<tasks>

<task type="tracer">
  <name>End-to-end MCP SAVINGS catalog + interest overlay + SAVISO</name>
  <reversibility rating="costly">D-01 catalog shape, D-05 interest membership wire, and D-09 SAVISO instruction surface are published agent contracts — undo needs coordinated agent/catalog updates.</reversibility>
  <files>src/lib/mcp/tools/accounts.test.ts, src/lib/mcp/tools/forecast.test.ts, src/lib/mcp/isolation-contract.test.ts, src/lib/mcp/tools/accounts.ts, src/lib/mcp/reads/load-forecast-overlay.ts, src/lib/mcp/tools/forecast.ts, src/lib/mcp/create-handler.ts</files>
  <read_first>
    .planning/phases/30-mcp-parity-verify/30-CONTEXT.md
    .planning/phases/30-mcp-parity-verify/30-PATTERNS.md
    .planning/phases/30-mcp-parity-verify/30-RESEARCH.md
    src/lib/mcp/tools/accounts.ts
    src/lib/mcp/tools/accounts.test.ts
    src/lib/mcp/reads/load-forecast-overlay.ts
    src/lib/mcp/tools/forecast.ts
    src/lib/mcp/tools/forecast.test.ts
    src/lib/mcp/create-handler.ts
    src/lib/mcp/isolation-contract.test.ts
    src/app/page.tsx
    src/components/dashboard/DashboardChartsShell.tsx
    src/lib/savings-rate.ts
    src/lib/savings-interest.ts
  </read_first>
  <behavior>
    - Wave 0 red first: accounts.test expects SAVINGS row with annualRateBps, accrualDayOfMonth, annualRatePercent typeof number; non-SAVINGS nulls; Object.keys allow-list includes the three keys
    - Wave 0 red: forecast.test expects serialized kind interest event with accountName and plannedAmountMinor string; description matches SAVISO-01; description and tests drop retired grace-line mark / income+grace-only copy; source-scan load-forecast-overlay.ts contains listInterestSlotsInRange
    - Wave 0 red: isolation-contract requires SAVISO-01 in create-handler source and forbids the retired grace-line mark glyph in create-handler and forecast description sources
    - GREEN: ListAccountInput/Row + serialize + loadListAccounts pass annualRateBps/accrualDayOfMonth; percent via Number(formatBpsToPercentMajor) for SAVINGS only (D-01, D-02)
    - GREEN: loadForecastOverlay loads SAVINGS with today LOCF like page.tsx, maps via listInterestSlotsInRange like shell, concat [...openSlots, ...interestSlots, ...graceSlots] (D-05)
    - GREEN: forecast description + create-handler instructions use INISO-01/GRISO-01/SAVISO-01 and income + interest + grace prose (D-09, D-10)
  </behavior>
  <action>
    Per D-01 through D-11 and PARITY-01: first extend the three Vitest files so the MCP trio fails for missing catalog fields, missing interest membership/source-scan, and missing SAVISO-01. Then implement production: extend accounts types/serialize/load/description (D-04; no SAVISO on list_accounts per D-12); wire interest in load-forecast-overlay mirroring page filter + shell map/concat (prefer inline mirror in the loader — do not edit DashboardChartsShell unless extracting a pure non-visual helper per D-08); replace overlay closer in forecast.ts and create-handler.ts with the triple tag. Reuse formatBpsToPercentMajor. Do not add tool names, payload isolation keys, README edits, BalanceSnapshot writes, or todo resolution (D-16). Horizon default stays resolveForecastHorizonEnd / today+365 (D-06).
  </action>
  <verify>
    <automated>./node_modules/.bin/vitest run src/lib/mcp/tools/accounts.test.ts src/lib/mcp/tools/forecast.test.ts src/lib/mcp/isolation-contract.test.ts</automated>
    <fails_when>non-zero exit, or "0 passed" in the vitest summary line</fails_when>
  </verify>
  <acceptance_criteria>
    - MCP trio vitest exits 0
    - SAVINGS serialize row has numeric annualRatePercent and non-null rate/DOM; ASSET row has nulls for all three
    - load-forecast-overlay.ts source contains listInterestSlotsInRange and interestSlots concat before grace
    - GET_FORECAST_OVERLAY_DESCRIPTION and create-handler instructions contain SAVISO-01
    - No new registerTool names beyond existing catalog
  </acceptance_criteria>
  <done>One agent-visible path: list_accounts SAVINGS fields + overlay interest events + SAVISO copy, locked by green Wave 0 trio.</done>
</task>

<task type="auto" tdd="true">
  <name>Catalog edge matrix and list_accounts description</name>
  <files>src/lib/mcp/tools/accounts.ts, src/lib/mcp/tools/accounts.test.ts</files>
  <read_first>
    src/lib/mcp/tools/accounts.ts
    src/lib/mcp/tools/accounts.test.ts
    src/lib/savings-rate.ts
    .planning/phases/30-mcp-parity-verify/30-CONTEXT.md
  </read_first>
  <behavior>
    - Empty wallet still returns { accounts: [] } (MCP-01 empty)
    - Every non-empty row Object.keys allow-list is exactly the prior keys plus accrualDayOfMonth, annualRateBps, annualRatePercent sorted (MCP-01 ordering)
    - 1650 bps → annualRatePercent === 16.5 and typeof number (MCP-01 precision, D-02)
    - type ASSET and FIAT_CREDIT force null rate fields even if input somehow carried bps (MCP-01 boundary)
    - Payload has no isSavings key (MCP-01 adjacency, D-03)
    - registerListAccounts description string mentions annualRateBps, accrualDayOfMonth, and annualRatePercent / SAVINGS (D-04)
    - list_accounts description and accounts.ts source do not contain SAVISO-01 (D-12)
  </behavior>
  <action>
    Per D-01…D-04 and D-12: flesh accounts.test.ts edge cases above if the tracer left any thin; ensure serialize always emits the three keys; description mentions the three fields and SAVINGS; keep metadata-only (no live balances). Do not add isSavings. Prefer formatBpsToPercentMajor + Number for percent.
  </action>
  <verify>
    <automated>./node_modules/.bin/vitest run src/lib/mcp/tools/accounts.test.ts</automated>
    <fails_when>non-zero exit, or failing expect on null keys / typeof annualRatePercent / Object.keys allow-list / description</fails_when>
  </verify>
  <acceptance_criteria>
    - accounts.test.ts exits 0 with SAVINGS/non-SAVINGS matrix, empty array, key allow-list, and description asserts
    - accounts.ts description mentions the three rate fields
    - rg for SAVISO-01 in accounts.ts is empty
  </acceptance_criteria>
  <done>MCP-01 catalog edge + description contracts green.</done>
</task>

<task type="auto" tdd="true">
  <name>Interest event shape, copy close, never-write scans</name>
  <files>src/lib/mcp/tools/forecast.test.ts, src/lib/mcp/isolation-contract.test.ts, src/lib/mcp/tools/forecast.ts, src/lib/mcp/create-handler.ts, src/lib/mcp/reads/load-forecast-overlay.ts</files>
  <read_first>
    src/lib/mcp/tools/forecast.test.ts
    src/lib/mcp/isolation-contract.test.ts
    src/lib/mcp/tools/forecast.ts
    src/lib/mcp/create-handler.ts
    src/lib/mcp/reads/load-forecast-overlay.ts
    src/components/dashboard/DashboardChartsShell.tsx
  </read_first>
  <behavior>
    - Serialized interest event includes kind interest, parentId, plannedAmountMinor string, displayPrimaryMajor number, currencyCode, accountId, accountName; event object has no annualRateBps / annualRatePercent / accrualDayOfMonth (D-07)
    - GET_FORECAST_OVERLAY_DESCRIPTION matches INISO-01, GRISO-01, SAVISO-01 and income + interest + grace; does not match retired grace-line mark (D-10, D-11)
    - isolation-contract create-handler source matches SAVISO-01 and does not match retired grace-line mark; never-write balanceSnapshot mutator scans still pass (D-11, SAVISO)
    - forecast.test source-scan: load-forecast-overlay.ts contains listInterestSlotsInRange and slots spread including interestSlots between open and grace (D-05 ordering)
  </behavior>
  <action>
    Per D-05, D-07, D-09…D-12: complete forecast/isolation asserts and any remaining copy fixes so both description and create-handler share the triple-tag closer. Keep serializeForecastPayload unchanged if kind interest already supported. Do not add isolation fields to JSON payloads. Do not touch README.
  </action>
  <verify>
    <automated>./node_modules/.bin/vitest run src/lib/mcp/tools/forecast.test.ts src/lib/mcp/isolation-contract.test.ts</automated>
    <fails_when>non-zero exit, or missing SAVISO-01 / interest kind expect, or never-write scan failure</fails_when>
  </verify>
  <acceptance_criteria>
    - forecast.test and isolation-contract exit 0
    - Interest events lack rate fields; description/instructions carry SAVISO-01
    - Loader source still has no balanceSnapshot create/update/upsert/delete
  </acceptance_criteria>
  <done>MCP-02 event shape + SAVISO copy + never-write contracts green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Agent → `/api/mcp` | Untrusted tool args; localhost Host/Origin guard unchanged |
| MCP read → Prisma | Catalog + LOCF snapshot reads only — no snapshot writes |
| Tool description → Agent | Isolation tags advisory text only — not payload enforcement |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-30-01 | Information Disclosure | list_accounts serialize | medium | mitigate | Metadata-only rows; no live balances; rate fields only |
| T-30-02 | Tampering | load-forecast-overlay interest path | high | mitigate | Never-write source-scan; no BalanceSnapshot mutators; Orca snap count in plan 02 |
| T-30-03 | Elevation of Privilege | MCP tool annotations | low | accept | readOnlyHint advisory only; enforcement is source-scan + localhost guard |
| T-30-04 | Spoofing | `/api/mcp` Host/Origin | medium | mitigate | Existing withLocalhostGuard unchanged |
| T-30-SC | Tampering | npm installs | high | mitigate | No new packages this phase — legitimacy N/A |
</threat_model>

<verification>
MCP trio green: `./node_modules/.bin/vitest run src/lib/mcp/tools/accounts.test.ts src/lib/mcp/tools/forecast.test.ts src/lib/mcp/isolation-contract.test.ts`
</verification>

<success_criteria>
Agents can list SAVINGS rate/DOM/percent via list_accounts and see kind interest on get_forecast_overlay with SAVISO-01 named beside INISO/GRISO; Wave 0 contracts green.
</success_criteria>

<output>
Create `.planning/phases/30-mcp-parity-verify/30-01-SUMMARY.md` when done
</output>
