---
phase: 30-mcp-parity-verify
plan: 02
type: execute
wave: 2
depends_on:
  - 30-01
files_modified:
  - .planning/phases/30-mcp-parity-verify/30-UAT.md
  - .planning/phases/30-mcp-parity-verify/COVERAGE.md
autonomous: true
requirements:
  - MCP-01
  - MCP-02
  - PARITY-01
estimate:
  tokens: 25000
  raw_tokens: 25000
  tasks: 2
  confidence: low
must_haves:
  truths:
    - "No visual changes this phase — MCP + Vitest + Orca only (UI-SPEC)"
    - "Do not reopen Phase 29 chart/tooltip/banner chrome (UI-SPEC)"
    - "Done bar is Vitest contracts plus Orca MCP tool calls, not chart DOM UAT (D-13, D-14, UI-SPEC)"
    - "Orca shows list_accounts SAVINGS annualRateBps, accrualDayOfMonth, annualRatePercent (MCP-01, D-14)"
    - "Orca shows get_forecast_overlay forecastEvents with kind interest (MCP-02, D-14)"
    - "BalanceSnapshot count (and spot-check get_net_worth) unchanged after overlay-only MCP reads (SAVISO live, D-14, D-15)"
    - "If no накопительный exists, agent creates one via existing UI then calls MCP (D-15)"
    - "Savings pending todo remains open until /gsd-complete-milestone v1.5 (D-16)"
    - "COVERAGE.md declares no external API integration for this phase"
  prohibitions:
    - statement: "Do not register new MCP tool names — extend list_accounts and get_forecast_overlay only"
      status: unresolved
      verification: judgment
    - statement: "Do not put isolation meta fields on JSON payloads"
      status: unresolved
      verification: judgment
    - statement: "Do not rewrite README connect docs this phase"
      status: unresolved
      verification: judgment
    - statement: "Do not mark the savings pending todo resolved mid Phase 30"
      status: unresolved
      verification: judgment
    - statement: "Do not implement ASSET ↔ SAVINGS conversion or chart legend split"
      status: unresolved
      verification: judgment
    - statement: "Do not write BalanceSnapshot from forecast MCP reads during UAT"
      status: unresolved
      verification: judgment
  artifacts:
    - path: .planning/phases/30-mcp-parity-verify/30-UAT.md
      provides: Orca evidence for MCP-01/MCP-02/PARITY-01 live bar
    - path: .planning/phases/30-mcp-parity-verify/COVERAGE.md
      provides: no-external-API declaration for api_coverage_gate
  key_links:
    - from: orca-ide browser
      to: http://localhost:3000/api/mcp
      via: tools/call list_accounts + get_forecast_overlay
      pattern: interest
    - from: BalanceSnapshot count before
      to: BalanceSnapshot count after overlay
      via: unchanged integer
      pattern: BalanceSnapshot
---

<objective>
Close the verify bar (D-13…D-16): agent-driven Orca UAT for two MCP reads + snap count, keep COVERAGE.md honest, leave savings todo open.

Purpose: PARITY-01 live proof without chart DOM UAT or full SIDE smoke.
Output: 30-UAT.md results + confirmed COVERAGE.md; suite still green.
</objective>

<execution_context>
@/home/artem/Documents/wallet/.cursor/gsd-core/workflows/execute-plan.md
@/home/artem/Documents/wallet/.cursor/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/OPERATOR.md
@.planning/phases/30-mcp-parity-verify/30-CONTEXT.md
@.planning/phases/30-mcp-parity-verify/30-RESEARCH.md
@.planning/phases/30-mcp-parity-verify/30-UI-SPEC.md
@.planning/phases/30-mcp-parity-verify/30-01-SUMMARY.md
@.planning/phases/30-mcp-parity-verify/COVERAGE.md
@src/lib/mcp/tools/accounts.ts
@src/lib/mcp/tools/forecast.ts
@src/lib/mcp/create-handler.ts
</context>

## Artifacts this phase produces

- `.planning/phases/30-mcp-parity-verify/30-UAT.md` — Orca MCP UAT log (pass/issue/blocked)
- `.planning/phases/30-mcp-parity-verify/COVERAGE.md` — no external API declaration (already seeded; confirm/keep)

## Flagged assumptions

- Orca/agent can initialize + tools/call against live `/api/mcp` without new transport work (RESEARCH A1).
- Snap-count method: both DB BalanceSnapshot count before/after overlay and spot-check get_net_worth identity (RESEARCH discretion).

## Prohibitions

Must NOT: new tool names; payload isolation meta; README rewrite; resolve savings todo; Phase 31 conversion; chart legend; reopen Phase 29 overlay; write snaps during overlay UAT.

<tasks>

<task type="auto">
  <name>Confirm COVERAGE.md and prohibition file scans</name>
  <files>.planning/phases/30-mcp-parity-verify/COVERAGE.md</files>
  <read_first>
    .planning/phases/30-mcp-parity-verify/COVERAGE.md
    .planning/phases/30-mcp-parity-verify/30-CONTEXT.md
    AGENTS.md
  </read_first>
  <action>
    Confirm COVERAGE.md still declares no external API integration with the one-line reason (in-repo MCP field parity only). Run file scans: create-handler and forecast tool registration still only existing tool names; README connect section untouched this phase; savings todo file still under .planning/todos/pending/; no isolation key added to serialize payloads. Do not edit production MCP code unless a scan proves plan 01 regressed.
  </action>
  <verify>
    <automated>./node_modules/.bin/vitest run src/lib/mcp/tools/accounts.test.ts src/lib/mcp/tools/forecast.test.ts src/lib/mcp/isolation-contract.test.ts && test -f .planning/phases/30-mcp-parity-verify/COVERAGE.md && grep -q 'No external API integration' .planning/phases/30-mcp-parity-verify/COVERAGE.md && test -f .planning/todos/pending/2026-09-10-savings-account-type-with-interest-nw-forecast.md</automated>
    <fails_when>non-zero exit; COVERAGE.md missing the no-external declaration; savings todo file absent from pending; vitest failure</fails_when>
  </verify>
  <acceptance_criteria>
    - MCP trio still green
    - COVERAGE.md contains No external API integration
    - Savings todo still pending (D-16)
  </acceptance_criteria>
  <done>Coverage gate artifact + D-16 todo presence locked; contracts still green.</done>
</task>

<task type="auto">
  <name>Orca UAT: list_accounts + overlay interest + snap count</name>
  <files>.planning/phases/30-mcp-parity-verify/30-UAT.md</files>
  <read_first>
    .planning/OPERATOR.md
    .planning/phases/30-mcp-parity-verify/30-CONTEXT.md
    .planning/phases/30-mcp-parity-verify/30-UI-SPEC.md
  </read_first>
  <action>
    Per D-13…D-15 and OPERATOR.md: start or reuse npm run dev on :3000; drive with orca-ide (prefer http://localhost:3000). If no накопительный, create one via existing account UI (no UI redesign). Record BalanceSnapshot count (and/or get_net_worth) before get_forecast_overlay. Call list_accounts and assert SAVINGS row shows annualRateBps, accrualDayOfMonth, annualRatePercent. Call get_forecast_overlay and assert a forecastEvent with kind interest. Re-check snap count / NW unchanged. Write .planning/phases/30-mcp-parity-verify/30-UAT.md with each check pass/issue/blocked/skipped. Ask human only on hard blockers. Not a full SIDE-tool smoke. Do not mark savings todo resolved (D-16).
  </action>
  <verify>
    <automated>test -f .planning/phases/30-mcp-parity-verify/30-UAT.md && grep -E -q 'list_accounts|interest|BalanceSnapshot|snap' .planning/phases/30-mcp-parity-verify/30-UAT.md</automated>
    <fails_when>30-UAT.md missing, or file lacks list_accounts / interest / snap evidence tokens</fails_when>
  </verify>
  <acceptance_criteria>
    - 30-UAT.md exists with evidence for MCP-01 fields, MCP-02 interest kind, and unchanged snap/NW
    - No chart DOM UAT required
    - Savings todo still pending
  </acceptance_criteria>
  <done>Live PARITY-01 proof captured in 30-UAT.md per OPERATOR agent-driven UAT.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Orca/agent → localhost MCP | Live tool calls on loopback only |
| Forecast read → DB | Count snaps before/after to detect accidental writes |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-30-02 | Tampering | Live get_forecast_overlay | high | mitigate | UAT snap count before/after; fail UAT if count rises |
| T-30-04 | Spoofing | MCP Host/Origin | medium | mitigate | Use localhost URL; existing guard |
| T-30-05 | Information Disclosure | UAT log | low | accept | UAT.md may include account names/rates from local DB — local-only |
| T-30-SC | Tampering | npm installs | high | mitigate | No installs in this plan |
</threat_model>

<verification>
MCP trio still green + 30-UAT.md present with list_accounts / interest / snap evidence.
</verification>

<success_criteria>
Orca proves MCP-01 and MCP-02 live; historical snaps unchanged; COVERAGE.md and D-16 hold; PARITY-01 closed for this milestone slice.
</success_criteria>

<output>
Create `.planning/phases/30-mcp-parity-verify/30-02-SUMMARY.md` when done
</output>
