---
phase: 29-kapital-overlay-saviso
plan: 02
type: execute
wave: 1
depends_on:
  - 29-01
files_modified:
  - src/lib/nw-forecast.ts
  - src/app/page.tsx
  - src/components/dashboard/DashboardChartsShell.tsx
  - src/components/dashboard/NetWorthHistoryChart.tsx
autonomous: true
requirements:
  - INT-02
  - INT-03
  - SAVISO-01
  - SAVISO-02
estimate:
  tokens: 40000
  raw_tokens: 80000
  tasks: 2
  confidence: high
must_haves:
  truths:
    - "On Капитал /, the dashed Прогноз line includes future SAVINGS interest credits as a positive primary addend on the accrual day only (INT-02, D-03, D-08, D-11)"
    - "A non-primary interest slot with no FX rate as of today is dropped and its currency code is listed; a rate dated today includes the slot; a rate dated after today does not (INT-03 boundary, C-03)"
    - "Interest truncate-toward-0 stays the Phase 28 helper; FX uses convertOtherMinorToPrimaryMinor; this phase adds no second rounding mode (INT-03 precision, C-04)"
    - "Grace forecast samples subtract display primary minor; the tooltip amount stays the positive payment with a minus prefix; the subtitle is Ожидаемый платёж (D-09, D-11)"
    - "The Прогноз figure is the NW level with no plus prefix; a minus appears only when that level is negative (D-10)"
    - "Viewing the overlay writes no BalanceSnapshot and buildNetWorthSeries on SAVINGS still equals snapshot minors (SAVISO-01, SAVISO-02, D-12)"
    - "Hide the dashed Line and the Прогноз legend entry when no FX-included slot exists; the fact chart stays and no empty copy is shown"
    - "Chart shell uses the existing RSC load plus client useMemo and adds no overlay skeleton"
    - "A missing FX rate drops that interest or grace slot and shows one banner Прогноз неполный · нет курса with currency codes only"
    - "On a future accrual day the dashed line steps up by that day's primary credit and the tooltip shows the interest block that day only"
    - "A non-accrual forecast day omits the Накопительный block and still shows the Прогноз level"
    - "An interest row whose FX rate is missing is absent; the banner lists the currency code and there is no inline row error"
    - "Same-day SAVINGS credits show the header once, the subtitle once, and one name plus plus-amount row per account, sorted by amount descending then account name А→Я (D-01, D-02, D-04, D-05, D-07)"
    - "When some interest slots are included and some are FX-dropped, included rows render and dropped currencies appear only as banner codes"
    - "Each interest or grace event is one tooltip row; the tooltip grows with the rows and does not show a horizon sum or a collapsed total (D-13)"
    - "Zero interest events omit the block; one event shows one row; many events show N rows under one header"
    - "A forecast point with no grace events omits the grace block"
    - "On a grace payment day the subtitle is Ожидаемый платёж, the row amount has a minus, and the dashed line falls by that primary amount (D-09, D-11)"
    - "A grace slot with a missing FX rate omits the row, uses the same banner, and adds no kind tag"
    - "A long account name uses min-w-0 flex-1 break-words and the amount stays shrink-0"
    - "Zero grace events omit the block; one event shows one minus row; many events show N rows under one header"
    - "Interest and grace subtitles are the fixed short Russian strings, once per block (D-05, D-09)"
    - "Future tooltip order is Прогноз level, then the interest block, then the grace block (D-06)"
    - statement: "Currency codes wrap inside the existing p-4 card with no ellipsis and no kind word"
      verification: backstop
    - statement: "The partial-forecast banner is the fixed Russian template plus the code list and wraps in the card"
      verification: backstop
    - statement: "The same muted dash steps up on an accrual day and down on a grace day; the today hinge can show a gap when overdue grace folds onto today while Итого stays the fact total"
      verification: backstop
  prohibitions:
    - statement: "Forecast code must not write BalanceSnapshot"
      status: unresolved
      verification: judgment
    - statement: "Forecast code must not rewrite historical net-worth LOCF"
      status: unresolved
      verification: judgment
    - statement: "Forecast code must not invent FX rates when a conversion rate is missing"
      status: unresolved
      verification: judgment
  artifacts:
    - path: src/lib/nw-forecast.ts
      provides: grace addend is the negation of display primary minor; event magnitude stays unsigned
    - path: src/app/page.tsx
      provides: forecastSavings RSC object with accountId, accountName, string balanceMinor, annualRateBps, accrualDayOfMonth, currencyCode, currencyScale, isPrimaryCurrency
    - path: src/components/dashboard/DashboardChartsShell.tsx
      provides: listInterestSlotsInRange fed by the BigInt InterestAccountInput adapter
    - path: src/components/dashboard/NetWorthHistoryChart.tsx
      provides: ForecastInterestTooltipBlock and grace subtitle plus minus prefix
  key_links:
    - from: page.tsx forecastSavings
      to: listInterestSlotsInRange
      via: RSC string balanceMinor; shell adapter accountId, accountName, BigInt balanceMinor, annualRateBps, accrualDayOfMonth, currencyCode, currencyScale, isPrimaryCurrency
      pattern: annualRateBps
    - from: interest ForecastSlot
      to: buildNetWorthForecastSeries
      via: slots concat after income and before grace
      pattern: kind
    - from: forecastDeltaMinor grace
      to: forecastPrimaryMinor
      via: return the negation of displayPrimaryMinor
      pattern: displayPrimaryMinor
    - from: point forecastEvents
      to: ForecastInterestTooltipBlock
      via: future tooltip only, kind interest
      pattern: Накопительный
    - from: excludedMissingFxCurrencies
      to: existing partial banner
      via: same code list, no kind word
      pattern: нет курса
---

<objective>
Wire future SAVINGS interest onto the Капитал dashed line and make the grace payment lower that same line. Implements D-01, D-02, D-03, D-04, D-05, D-06, D-07, D-08, D-09, D-10, D-11, D-12, and D-13. C-01 one series. C-02 horizon unchanged. C-03 one FX banner. C-04 math stays Phase 28. C-05 no MCP edits. C-06 savings todo stays open.

Purpose: INT-02 and INT-03 become visible on `/` without writing snapshots (SAVISO).
Output: grace sign flip, forecastSavings prop, interest tooltip block, green Wave 0 files.
</objective>

<execution_context>
@/home/artem/Documents/wallet/.cursor/gsd-core/workflows/execute-plan.md
@/home/artem/Documents/wallet/.cursor/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/29-kapital-overlay-saviso/29-CONTEXT.md
@.planning/phases/29-kapital-overlay-saviso/29-RESEARCH.md
@.planning/phases/29-kapital-overlay-saviso/29-PATTERNS.md
@.planning/phases/29-kapital-overlay-saviso/29-UI-SPEC.md
@.planning/phases/29-kapital-overlay-saviso/29-01-PLAN.md
@src/lib/nw-forecast.ts
@src/lib/savings-interest.ts
@src/app/page.tsx
@src/components/dashboard/DashboardChartsShell.tsx
@src/components/dashboard/NetWorthHistoryChart.tsx
@src/lib/nw-forecast.test.ts
@src/components/dashboard/nw-forecast-ui.test.ts
@src/lib/saviso.test.ts
</context>

## Artifacts this phase produces

- `forecastDeltaMinor` grace arm returns the negation of `displayPrimaryMinor` in `src/lib/nw-forecast.ts` (existing function, new sign).
- `forecastSavings` prop on `DashboardChartsShell` and the matching object on `src/app/page.tsx`.
- `ForecastInterestTooltipBlock` in `src/components/dashboard/NetWorthHistoryChart.tsx`.
- No new route, no new npm package, no new MCP tool.

## Flagged assumptions (unclassified edges — not dropped)

- INT-02 stays an assumption that one dashed series plus tooltip rows is the whole surface (C-01). Do not add a second Line.
- SAVISO-01 stays an assumption that isolation is "no snapshot write and no historical input change" (D-12). Do not add an interest argument to `buildNetWorthSeries`.
- SAVISO-02 stays an assumption that `src/lib/saviso.test.ts` is the regression. Do not replace it with a prose comment.
- Research A1: overdue grace folded onto today dips the dashed sample; fact `nw` on that point stays the historical total. D-11 does not carve out a today exception.

## MCP blast radius (C-05)

`loadForecastOverlay` calls `buildNetWorthForecastSeries`. Grace-day `forecastPrimaryMinor` will drop after D-11. Do not add interest membership to the loader. Do not edit `GET_FORECAST_OVERLAY_DESCRIPTION`, `create-handler.ts` instructions, or `src/lib/mcp/tools/forecast.test.ts`. Phase 30 owns copy and interest events.

<tasks>

<task type="tracer">
  <name>End-to-end interest credit and grace dip on Прогноз</name>
  <precondition>src/lib/savings-interest.ts exports listInterestSlotsInRange from Phase 28</precondition>
  <reversibility rating="costly">D-08 and D-11: primary-only tooltip amounts and the grace line sign are shared by the chart, this builder, and the MCP loader that already calls the builder. Undo is a coordinated caller change, not a data migration.</reversibility>
  <files>src/lib/nw-forecast.ts, src/app/page.tsx, src/components/dashboard/DashboardChartsShell.tsx, src/components/dashboard/NetWorthHistoryChart.tsx</files>
  <read_first>
    .planning/phases/29-kapital-overlay-saviso/29-CONTEXT.md
    .planning/phases/29-kapital-overlay-saviso/29-PATTERNS.md
    .planning/phases/29-kapital-overlay-saviso/29-UI-SPEC.md
    src/lib/nw-forecast.ts
    src/lib/nw-forecast.test.ts
    src/lib/savings-interest.ts
    src/app/page.tsx
    src/components/dashboard/DashboardChartsShell.tsx
    src/components/dashboard/NetWorthHistoryChart.tsx
  </read_first>
  <behavior>
    - Grace arm of forecastDeltaMinor returns the negation of displayPrimaryMinor; displayPrimaryMajor stays the unsigned major (D-11, D-09)
    - One primary SAVINGS account's future accrual raises forecastPrimaryMinor by that day's interestMinor (INT-02, D-08)
    - Events stay on that sample date only, so later days keep the level and do not repeat the row (D-03)
    - Missing FX drops the slot and records the currency code; a rate as of today includes it (INT-03)
  </behavior>
  <action>
    Flip only the grace arm in forecastDeltaMinor to the negation of displayPrimaryMinor (D-11). Leave income and interest returning displayPrimaryMinor. Do not negate displayPrimaryMajor. Do not import savings-interest into nw-forecast.ts (C-04). Do not sort events in the builder (D-02 belongs in the tooltip). Rewrite the three comments that still describe a neutral grace addend so they match the signed line. Keep the exhaustive never default.

    In page.tsx add a forecastSavings prop beside forecastGrace (D-12). Map accounts where type is SAVINGS and annualRateBps and accrualDayOfMonth are non-null. The RSC object uses the InterestAccountInput keys. balanceMinor is a string on this prop. Keys: accountId, accountName, balanceMinor (today LOCF amount via toString, or the string 0 when LOCF is missing), annualRateBps, accrualDayOfMonth, currencyCode, currencyScale, isPrimaryCurrency. These keys override any shorter id or name field list in an analog note. Use the existing firstHitLocfMap and the existing account query. Do not call a snapshot write. Do not import savings-interest on the server page. Do not overload the historical accounts prop. Do not mark the savings todo resolved (C-06).

    In DashboardChartsShell add forecastSavings next to forecastGrace. The object passed to listInterestSlotsInRange uses the same callee keys, with balanceMinor revived by BigInt: accountId, accountName, balanceMinor, annualRateBps, accrualDayOfMonth, currencyCode, currencyScale, isPrimaryCurrency. Inside the existing forecast useMemo, after forecastHorizonEnd (C-02), call listInterestSlotsInRange with only those adapted savings rows (not income slots, not grace slots). Map each slot to ForecastSlot kind interest, parentId from the slot, plannedAmountMinor from interestMinor, currency fields copied through. Concat slots as income, then interest, then grace. Add forecastSavings to the useMemo dependency list. Leave the banner string as it is (C-03, INT-03). Do not copy a dipped forecast into fact nw. Do not special-case grace-on-today back to a zero addend (D-11).

    In NetWorthHistoryChart add ForecastInterestTooltipBlock mirroring ForecastGraceTooltipBlock (D-01, D-04, D-05, D-06, D-07, D-08). Header Накопительный once. Subtitle Ожидаемое начисление once. One row per interest event: account name and a plus prefix on formatChartNumber of displayPrimaryMajor. Mount it only on the future tooltip, between the Прогноз level and the grace block (D-03, D-06). Level stays formatChartNumber of the forecast value with no extra plus (D-10). Change the grace subtitle to Ожидаемый платёж and prefix the grace amount with a minus on the positive major (D-09). Delete the previous grace subtitle. One dashed Line stays (C-01). Do not render the interest block on the today branch.

    Do not edit src/lib/savings-interest.ts. Do not edit any file under src/lib/mcp/. Do not add a package (T-29-SC).
  </action>
  <verify>
    <automated>./node_modules/.bin/vitest run src/lib/nw-forecast.test.ts src/lib/saviso.test.ts && grep -q 'listInterestSlotsInRange' src/components/dashboard/DashboardChartsShell.tsx && grep -q 'annualRateBps' src/app/page.tsx && grep -q 'accrualDayOfMonth' src/app/page.tsx && grep -q 'annualRateBps' src/components/dashboard/DashboardChartsShell.tsx && grep -q 'accrualDayOfMonth' src/components/dashboard/DashboardChartsShell.tsx</automated>
    <fails_when>non-zero exit, or "0 passed" in the vitest summary line</fails_when>
  </verify>
  <acceptance_criteria>
    - vitest on nw-forecast.test.ts and saviso.test.ts exits 0
    - DashboardChartsShell.tsx contains listInterestSlotsInRange
    - page.tsx forecastSavings and the shell adapter both name accountId, accountName, balanceMinor, annualRateBps, accrualDayOfMonth, currencyCode, currencyScale, and isPrimaryCurrency
    - Shell balanceMinor passed into listInterestSlotsInRange is BigInt of the string prop
    - forecastDeltaMinor grace arm returns the negation of displayPrimaryMinor
    - NetWorthHistoryChart future tooltip contains Накопительный and Ожидаемый платёж
    - No path under src/lib/mcp/ is edited
  </acceptance_criteria>
  <done>One primary savings credit raises the dashed line on its accrual day, a grace payment lowers that line, and the future tooltip shows the interest row and the signed grace row. Historical series tests stay green.</done>
</task>

<task type="auto">
  <name>Tooltip sort, type size, and accrual-day-only rows</name>
  <files>src/components/dashboard/NetWorthHistoryChart.tsx</files>
  <read_first>
    .planning/phases/29-kapital-overlay-saviso/29-UI-SPEC.md
    src/components/dashboard/NetWorthHistoryChart.tsx
    src/components/dashboard/nw-forecast-ui.test.ts
    src/lib/money.ts
  </read_first>
  <behavior>
    - Interest rows sort by displayPrimaryMajor descending, then accountName localeCompare with locale ru and sensitivity base; equal amount and equal base name keep builder order (D-02)
    - Probe amounts 20, 20, 10 and names Альфа, Яндекс, Бета render Альфа, Яндекс, Бета
    - Missing interest name falls back to счёт plus accountId; grace fallback stays обязательство plus parentId (D-01)
    - Both future and today tooltip roots use text-sm; amounts stay font-mono font-semibold tabular-nums
    - Interest block is absent when asOfDate is today or earlier (D-03)
    - Row text is this accrual day's credit only: no balance after the credit, no horizon sum, no month delta, no capitalization label (D-07, D-13)
  </behavior>
  <action>
    In ForecastInterestTooltipBlock, sort a copy of the interest events by displayPrimaryMajor descending, then accountName via localeCompare with locale ru and sensitivity base (D-02). Keep the sort stable for equal amount and equal base name. Do not sort inside nw-forecast.ts. Name fallback for interest is счёт plus accountId (D-01). Grace name fallback stays обязательство plus parentId. Change text-xs to text-sm on both the future and today tooltip roots so interest and grace inherit Label 14px. Leave amount classes as font-mono font-semibold tabular-nums. Do not mount the interest block on the today return (D-03). Each row uses that event's displayPrimaryMajor only (D-07, D-13). Do not add a second Line, a legend split, or a kind word on the banner (C-01, C-03).
  </action>
  <verify>
    <automated>./node_modules/.bin/vitest run src/components/dashboard/nw-forecast-ui.test.ts src/lib/nw-forecast.test.ts src/lib/saviso.test.ts</automated>
    <fails_when>non-zero exit, or "0 passed" in the summary line</fails_when>
    <human-check>End-of-phase agent UAT, not a mid-plan stop. On http://localhost:3000/ with npm run dev, hover an accrual day and a grace due day: interest block once with a plus amount, later forecast days keep the higher line without that block, grace row has a minus and the dashed line is lower, overdue grace on today leaves fact Итого unchanged. Record the result at verify-work.</human-check>
  </verify>
  <acceptance_criteria>
    - nw-forecast-ui.test.ts exits 0
    - Chart source contains localeCompare with ru and sensitivity base
    - Chart source contains Ожидаемое начисление once as a subtitle, not once per row
    - Today tooltip branch does not render ForecastInterestTooltipBlock
    - saviso.test.ts still exits 0
  </acceptance_criteria>
  <done>Wave 0 UI scan is green. Rows sort amount-desc then А→Я. Tooltip type size is text-sm. Interest rows exist only on future accrual days.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| RSC page → client shell | Account rate, DOM, and balance minor cross as strings |
| Client slots → buildNetWorthForecastSeries | Currency codes and planned amounts are untrusted until the FX gate |
| Forecast display → snapshot store | The overlay must not post a balance |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-29-01 | Tampering | page.tsx, nw-forecast.ts, DashboardChartsShell.tsx | high | mitigate | No balanceSnapshot create, update, upsert, or delete. saviso.test.ts scans those files. Historical builder input stays accounts, snapshots, rates, primaryScale, preset, today (D-12) |
| T-29-02 | Tampering | forecastDeltaMinor grace arm | high | mitigate | Grace returns the negation of displayPrimaryMinor. mergeFactAndForecast copies forecast onto an existing fact point and does not write that dipped number into nw (D-11) |
| T-29-03 | Tampering | FX gate in nw-forecast.ts | high | mitigate | Non-primary slots use locfRateAsOf as of today and convertOtherMinorToPrimaryMinor. A miss drops the slot and appends the currency code. No invented rate (INT-03) |
| T-29-04 | Tampering | listInterestSlotsInRange inputs | medium | mitigate | Shell adapter is accountId, accountName, BigInt balanceMinor, annualRateBps, accrualDayOfMonth, currencyCode, currencyScale, isPrimaryCurrency. Do not pass income or grace slots into the enumerator (C-04) |
| T-29-05 | Information disclosure | tooltip rows | low | accept | One row per event already scoped to accountId and accountName. Sort is display-only. Local single-user dashboard, no new read surface beyond the existing chart |
| T-29-SC | Tampering | npm installs | high | mitigate | No package install. Package legitimacy audit recorded no new packages |
</threat_model>

<verification>
./node_modules/.bin/vitest run src/lib/nw-forecast.test.ts src/lib/saviso.test.ts src/components/dashboard/nw-forecast-ui.test.ts exits 0. MCP files are untouched. Banner string is unchanged.
</verification>

<success_criteria>
Капитал dashed Прогноз rises by a future savings credit and falls by a grace payment in primary. Missing FX lists the currency code on the existing banner. Historical NW and BalanceSnapshot stay snapshot LOCF.
</success_criteria>

<output>
Create `.planning/phases/29-kapital-overlay-saviso/29-02-SUMMARY.md` when done
</output>
