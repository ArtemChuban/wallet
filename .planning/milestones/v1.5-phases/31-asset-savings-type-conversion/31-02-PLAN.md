---
phase: 31-asset-savings-type-conversion
plan: 02
type: execute
wave: 2
depends_on:
  - 31-01
files_modified:
  - src/components/accounts/AccountFormDialog.tsx
  - src/components/accounts/AccountFormDialog.test.ts
  - .planning/phases/31-asset-savings-type-conversion/31-UAT.md
autonomous: true
requirements:
  - ACCT-04
estimate:
  tokens: 25000
  raw_tokens: 50000
  tasks: 2
  confidence: high
must_haves:
  truths:
    - "Edit of exact ASSET or SAVINGS shows type Select with only Актив and Накопительный options (D-01, D-13)"
    - "Edit of FIAT_CREDIT or legacy soft types keeps muted type label — no Select (D-14)"
    - "Draft type-gate: showSavingsFields keys off draft accountType === SAVINGS in edit; leave SAVINGS clears rate/DOM; re-enter starts empty (D-05, D-06, D-07)"
    - "Currency always muted locked label on edit (D-03); create type list unchanged (D-04)"
    - "Convertible edit DialogDescription is Валюта не меняется.; locked edit keeps Тип и валюта не меняются. (UI-SPEC)"
    - "One Save submits name+type+rate/DOM; Save stays enabled while empty; success Сохранено; no second-step confirm chrome (D-02, D-08, D-09, D-10, D-11)"
    - "31-UAT.md lists agent-driven Orca checks for both convert directions, locked types, and snapshot isolation (OPERATOR)"
  artifacts:
    - path: src/components/accounts/AccountFormDialog.tsx
      provides: edit type unlock + draft gate + description split
    - path: src/components/accounts/AccountFormDialog.test.ts
      provides: source-scan locks for canConvertType / CONVERT options / draft gate
    - path: .planning/phases/31-asset-savings-type-conversion/31-UAT.md
      provides: UAT scaffold for verify-work
  key_links:
    - from: canConvertType
      to: type Select
      via: account.type === ASSET || account.type === SAVINGS exact (not isAssetType)
      pattern: "canConvertType"
    - from: Select onValueChange
      to: annualRate / accrualDom state
      via: clear both when next !== SAVINGS
      pattern: "setAnnualRate"
    - from: edit FormData type hidden/select
      to: updateAccount (Plan 01)
      via: name=type value draft accountType
      pattern: "name=\"type\""
---

<objective>
Unlock AccountFormDialog edit type Select for ASSET|SAVINGS only, draft type-gate + RU copy per UI-SPEC, source-scan tests, and agent-driven UAT scaffold (D-01, D-04…D-14, D-02/D-03 UI half).

Purpose: User completes ACCT-04 from «Изменить счёт» without confirm friction; UI matches server matrix from Plan 01.
Output: Dialog unlock + tests + 31-UAT.md checklist.
</objective>

<execution_context>
@/home/artem/Documents/wallet/.cursor/gsd-core/workflows/execute-plan.md
@/home/artem/Documents/wallet/.cursor/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/OPERATOR.md
@.planning/phases/31-asset-savings-type-conversion/31-CONTEXT.md
@.planning/phases/31-asset-savings-type-conversion/31-RESEARCH.md
@.planning/phases/31-asset-savings-type-conversion/31-PATTERNS.md
@.planning/phases/31-asset-savings-type-conversion/31-UI-SPEC.md
@.planning/phases/31-asset-savings-type-conversion/31-01-PLAN.md
@src/components/accounts/AccountFormDialog.tsx
@src/components/accounts/AccountFormDialog.test.ts
@src/lib/account-type.ts
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Unlock edit type Select + draft savings gate + copy split</name>
  <reversibility rating="costly">Reopens Phase 27 D-08 edit UI contract for the ASSET↔SAVINGS pair (D-01).</reversibility>
  <files>src/components/accounts/AccountFormDialog.tsx</files>
  <behavior>
    - canConvertType true only when mode edit and account.type is exactly ASSET or SAVINGS — never isAssetType() (D-01, D-13, D-14, RESEARCH Pitfall 2)
    - When canConvertType: Select options CONVERT_TYPE_OPTIONS = ASSET + SAVINGS only (labels Актив / Накопительный); hidden or select name=type bound to draft accountType; DialogDescription Валюта не меняется.
    - When not canConvertType: muted accountTypeLabel; DialogDescription Тип и валюта не меняются.; currency always muted mono (D-03, D-14)
    - showSavingsFields true when accountType === SAVINGS for create and edit (replace frozen account?.type edit branch) (D-05)
    - onValueChange leaving SAVINGS clears annual rate and DOM state; toggling back starts empty (D-06); convert-to-SAVINGS fields empty (D-07); same-type SAVINGS open still prefills from props on mount (RESEARCH A2)
    - Create TYPE_OPTIONS unchanged including FIAT_CREDIT (D-04)
    - Submit label Сохранить / pending Сохранение…; success path still Сохранено from action (D-11); Save not disabled for empty rate/DOM (D-08); no DestructiveConfirmStep / confirm chrome for type change (D-09, D-10)
  </behavior>
  <action>
    Implement edit unlock per 31-UI-SPEC Interaction & Copywriting and CONTEXT D-01…D-14.
    Exact peer check for unlock; CONVERT_TYPE_OPTIONS without FIAT_CREDIT; reuse create Select clear-on-leave-SAVINGS pattern for edit draft.
    Keep credit-limit create-only gates unchanged. Do not change AccountList chrome. Do not add npm packages or new shadcn blocks.
  </action>
  <verify>
    <automated>grep -E -q 'canConvertType|CONVERT_TYPE_OPTIONS' src/components/accounts/AccountFormDialog.tsx && grep -F -q 'Валюта не меняется' src/components/accounts/AccountFormDialog.tsx && grep -v '^[[:space:]]*//' src/components/accounts/AccountFormDialog.tsx | grep -F -q 'accountType === "SAVINGS"'</automated>
    <fails_when>non-zero exit from any grep, or convertible description / draft SAVINGS gate tokens absent from dialog source</fails_when>
  </verify>
  <done>Edit convertible accounts expose ASSET|SAVINGS Select with draft gate and split DialogDescription; locked types stay label-only; create path unchanged.</done>
</task>

<task type="auto" tdd="true">
  <name>Dialog source-scan locks + 31-UAT scaffold</name>
  <files>src/components/accounts/AccountFormDialog.test.ts, .planning/phases/31-asset-savings-type-conversion/31-UAT.md</files>
  <behavior>
    - Source-scan asserts canConvertType (or equivalent) uses exact ASSET/SAVINGS peer check and does not call isAssetType for unlock
    - CONVERT_TYPE_OPTIONS / edit options string content includes ASSET and SAVINGS and does not include FIAT_CREDIT in that convert list
    - showSavingsFields edit path keys off accountType === SAVINGS (draft), not only account?.type
    - Leave-SAVINGS clear of setAnnualRate/setAccrualDom present in onValueChange path
    - 31-UAT.md scaffold lists: ASSET→SAVINGS fill rate+DOM save; SAVINGS→ASSET fields clear after refresh; FIAT_CREDIT type locked; list secondary rate line updates; BalanceSnapshot count unchanged after convert (D-16); agent-driven via npm run dev + orca-ide per OPERATOR — status pending until verify-work
  </behavior>
  <action>
    Extend AccountFormDialog.test.ts source-scan style (readFileSync dialog src) with the locks above (per D-01, D-05, D-06, D-13, D-14).
    Write 31-UAT.md frontmatter status pending + numbered expected tests for ACCT-04 UI flows and snapshot isolation; leave results empty/pending for `/gsd-verify-work` (human_verify_mode end-of-phase — no blocking checkpoint here).
  </action>
  <verify>
    <automated>./node_modules/.bin/vitest run src/components/accounts/AccountFormDialog.test.ts && test -f .planning/phases/31-asset-savings-type-conversion/31-UAT.md && grep -E -q 'ASSET|SAVINGS|BalanceSnapshot|orca' .planning/phases/31-asset-savings-type-conversion/31-UAT.md</automated>
    <fails_when>non-zero exit, vitest "0 passed", missing 31-UAT.md, or UAT file lacks ASSET/SAVINGS/BalanceSnapshot/orca tokens</fails_when>
  </verify>
  <done>Source-scan locks green; 31-UAT.md scaffold ready for agent-driven Orca verify-work.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Edit dialog UI → FormData | User can change visible type; server Plan 01 remains authoritative (D-15) |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-31-06 | Tampering | Soft-type unlock via isAssetType | medium | mitigate | Exact ASSET\|SAVINGS peer check only; source-scan lock (D-14) |
| T-31-07 | Elevation of privilege | FIAT_CREDIT in edit options | medium | mitigate | CONVERT_TYPE_OPTIONS excludes FIAT_CREDIT; create list separate (D-13, D-04) |
| T-31-08 | Spoofing | DialogDescription lies about type lock | low | mitigate | Split copy convertible vs locked (UI-SPEC) |
| T-31-SC | Tampering | npm installs | high | mitigate | Zero new packages (UI-SPEC hard constraint) |
</threat_model>

<verification>
./node_modules/.bin/vitest run src/components/accounts/AccountFormDialog.test.ts src/app/accounts/actions.test.ts
grep -E -q 'ASSET|SAVINGS|BalanceSnapshot' .planning/phases/31-asset-savings-type-conversion/31-UAT.md
</verification>

<success_criteria>
User can convert ASSET↔SAVINGS in «Изменить счёт» with draft gate and locked peers; UAT scaffold documents Orca checks for verify-work.
</success_criteria>

<output>
Create `.planning/phases/31-asset-savings-type-conversion/31-02-SUMMARY.md` when done
</output>
