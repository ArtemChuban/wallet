---
phase: 31-asset-savings-type-conversion
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/lib/validations/account.ts
  - src/lib/validations/account.test.ts
  - src/app/accounts/actions.ts
  - src/app/accounts/actions.test.ts
autonomous: true
requirements:
  - ACCT-04
estimate:
  tokens: 35000
  raw_tokens: 70000
  tasks: 3
  confidence: high
must_haves:
  truths:
    - "ASSET→SAVINGS via updateAccount persists type SAVINGS plus annualRateBps and accrualDayOfMonth when rate+DOM provided (ACCT-04, D-02, D-15)"
    - "SAVINGS→ASSET via updateAccount persists type ASSET with annualRateBps null and accrualDayOfMonth null in one prisma.account.update (ACCT-04, D-15, D-16)"
    - "Forbidden transitions (FIAT_CREDIT/legacy forged type; convertible peer forged to FIAT_CREDIT) reject without calling account.update (D-15)"
    - "Missing rate or DOM on convert-to-SAVINGS returns Укажите годовой процент / Укажите день начисления (D-12)"
    - "Conversion never calls balanceSnapshot.upsert or balanceSnapshot.delete (D-16)"
    - "Client currency on update is still ignored (D-03)"
    - "updateAccountSchema accepts optional type ASSET|SAVINGS and rejects FIAT_CREDIT on the type field (D-15)"
  artifacts:
    - path: src/lib/validations/account.ts
      provides: updateAccountSchema optional type enum ASSET|SAVINGS
    - path: src/app/accounts/actions.ts
      provides: ASSET↔SAVINGS transition matrix + Prisma null clear; currency still ignored
    - path: src/app/accounts/actions.test.ts
      provides: conversion matrix + never-calls snapshot; Phase 27 forge-ignore case rewritten
    - path: src/lib/validations/account.test.ts
      provides: optional type accept/reject cases
  key_links:
    - from: updateAccountSchema.safeParse
      to: updateAccount transition matrix
      via: optional validated.data.type after findUnique dbType
      pattern: "effectiveType"
    - from: SAVINGS→ASSET branch
      to: prisma.account.update data
      via: type ASSET + annualRateBps null + accrualDayOfMonth null
      pattern: "annualRateBps: null"
    - from: conversion tests
      to: prisma.balanceSnapshot mocks
      via: never.toHaveBeenCalled upsert/delete
      pattern: "balanceSnapshot"
---

<objective>
Tracer + server write path for ACCT-04: optional update `type`, `updateAccount` ASSET↔SAVINGS matrix with Prisma null-clear, Vitest never-calls BalanceSnapshot, rewrite Phase 27 forge-type ignore (D-02, D-03, D-12, D-15, D-16).

Purpose: Prove conversion is one Account-row metadata update under existing CHECK — no migration, no new action, no snapshot writes.
Output: Green Zod + actions tests locking the matrix; production `updateAccount` / `updateAccountSchema` accepting the pair only.
</objective>

<execution_context>
@/home/artem/Documents/wallet/.cursor/gsd-core/workflows/execute-plan.md
@/home/artem/Documents/wallet/.cursor/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/31-asset-savings-type-conversion/31-CONTEXT.md
@.planning/phases/31-asset-savings-type-conversion/31-RESEARCH.md
@.planning/phases/31-asset-savings-type-conversion/31-PATTERNS.md
@src/lib/validations/account.ts
@src/app/accounts/actions.ts
@src/app/accounts/actions.test.ts
@src/lib/validations/account.test.ts
@src/lib/savings-rate.ts
</context>

<tasks>

<task type="tracer" tdd="true">
  <name>End-to-end ASSET→SAVINGS updateAccount — one path</name>
  <reversibility rating="costly">Reopens Phase 27 D-08 write contract: Zod + updateAccount accept type for ASSET↔SAVINGS (D-01/D-15 server half).</reversibility>
  <files>src/lib/validations/account.ts, src/app/accounts/actions.ts, src/app/accounts/actions.test.ts</files>
  <behavior>
    - ASSET row + FormData type=SAVINGS + name + annualRatePercentMajor + accrualDayOfMonth → success; prisma.account.update data includes type SAVINGS, annualRateBps from parsePercentToBps, accrualDayOfMonth; currency FormData ignored (D-03)
    - Same convert call: prisma.balanceSnapshot.upsert and delete never called (D-16)
    - RED first: add describe updateAccount ASSET↔SAVINGS (ACCT-04) with the ASSET→SAVINGS + never-calls cases before production green
  </behavior>
  <action>
    Wire ONE happy path through every server layer touched this phase (per D-02, D-15, D-16, RESEARCH Pattern 1–2).
    Extend updateAccountSchema with optional type z.enum(["ASSET","SAVINGS"]) only — keep rate/DOM optional at schema; no new migration and no new server action.
    In updateAccount: parse optional type from FormData; after findUnique compute effectiveType = requested ?? dbType; when dbType is ASSET and requested is SAVINGS, require rate+DOM with Russian field errors matching create (D-12), parsePercentToBps, then prisma.account.update with name + type SAVINGS + annualRateBps + accrualDayOfMonth.
    Keep ignoring client currency always (D-03). Update docstring: currency always ignored; type accepted only for ASSET↔SAVINGS.
    Do not touch BalanceSnapshot APIs. Do not add MCP write tools.
    Tests: add the ASSET→SAVINGS case + never-calls snapshot asserts using existing vi.mock prisma shape in actions.test.ts (prior Phase 27/30 vitest style).
  </action>
  <verify>
    <automated>./node_modules/.bin/vitest run src/app/accounts/actions.test.ts</automated>
    <fails_when>non-zero exit, or "0 passed" in the summary line, or ASSET→SAVINGS / never-calls cases absent from output</fails_when>
  </verify>
  <done>ASSET→SAVINGS conversion succeeds end-to-end on mocked prisma; BalanceSnapshot mocks untouched; schema accepts optional ASSET|SAVINGS type.</done>
</task>

<task type="auto" tdd="true">
  <name>SAVINGS→ASSET null-clear + forbidden matrix + rewrite forge ignore</name>
  <reversibility rating="costly">Write-path transition matrix is the durable D-15 invariant; undoing requires coordinated Zod + action + test rewrite.</reversibility>
  <files>src/app/accounts/actions.ts, src/app/accounts/actions.test.ts</files>
  <behavior>
    - SAVINGS→ASSET: update data equals name + type ASSET + annualRateBps null + accrualDayOfMonth null (never omit nulls); never-calls snapshot
    - Same-type SAVINGS edit without type change still updates name+bps+DOM; currency ignored
    - Rewrite prior Phase 27 case that set type=ASSET on SAVINGS update expecting type ignored — that peer pair is now conversion (RESEARCH Pitfall 1)
    - FIAT_CREDIT dbType + forged type ASSET/SAVINGS → generic Не удалось сохранить…; account.update not called
    - Legacy soft dbType (e.g. CRYPTO) + forged convertible type → same reject
    - ASSET/SAVINGS dbType + forged type FIAT_CREDIT → Zod or action reject; update not called
    - Convert-to-SAVINGS missing rate or DOM → field errors Укажите годовой процент / Укажите день начисления; update not called (D-12)
    - ASSET name-only (no type / type=ASSET) still writes only name; forged currency ignored (D-03)
  </behavior>
  <action>
    Expand updateAccount after the tracer path (per D-15, D-16, D-12, RESEARCH Pattern 2).
    When effectiveType is ASSET and dbType was SAVINGS (or requested ASSET from SAVINGS): single prisma.account.update with type ASSET and both savings columns set to null explicitly — undefined/omit forbidden under Account_savings_rate_invariant.
    When effectiveType stays SAVINGS (same-type): keep existing name+bps+DOM write; omit type field or set SAVINGS consistently with tests.
    When requested !== dbType and the pair is not exactly ASSET↔SAVINGS (including non-convertible dbType): return generic message Не удалось сохранить. Проверьте поля и попробуйте снова. without calling update (discretion A1 from RESEARCH).
    Rewrite describe updateAccount SAVINGS forge case: intentional type=ASSET becomes conversion assert with null clears; add a same-type SAVINGS update without type=ASSET that still proves currency ignore.
    Cover never-calls snapshot on both conversion directions.
  </action>
  <verify>
    <automated>./node_modules/.bin/vitest run src/app/accounts/actions.test.ts</automated>
    <fails_when>non-zero exit, or "0 passed" in the summary line, or failing assertion on null clear / forbidden reject / forge rewrite</fails_when>
  </verify>
  <done>Full ACCT-04 server matrix green: both directions, rejects, Russian missing-field errors, D-16 never-calls, Phase 27 ignore-type peer case rewritten.</done>
</task>

<task type="auto" tdd="true">
  <name>Zod updateAccountSchema optional type unit locks</name>
  <files>src/lib/validations/account.ts, src/lib/validations/account.test.ts</files>
  <behavior>
    - updateAccountSchema accepts { name, type: "ASSET" } and { name, type: "SAVINGS", annualRatePercentMajor, accrualDayOfMonth }
    - Rejects type FIAT_CREDIT and legacy enum strings on the type field (error path includes type)
    - Name-only and name+rate+DOM without type still accepted (action gates by DB type)
  </behavior>
  <action>
    Confirm updateAccountSchema optional type enum is ASSET|SAVINGS only (per D-15 Zod half). Extend account.test.ts describe for updateAccountSchema with the accept/reject cases above — reuse create SAVINGS Russian strings only indirectly via action tests already covering D-12; Zod layer need not refine rate requiredness (action after findUnique owns that, matching Phase 27 pattern).
  </action>
  <verify>
    <automated>./node_modules/.bin/vitest run src/lib/validations/account.test.ts</automated>
    <fails_when>non-zero exit, or "0 passed" in the summary line, or optional type cases missing from run</fails_when>
  </verify>
  <done>Zod unit suite locks optional convertible type and rejects non-peer enums on update schema.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Browser FormData → updateAccount | Untrusted type/rate/DOM/currency fields; UI not sole gate (D-15) |
| updateAccount → Prisma/SQLite | CHECK Account_savings_rate_invariant must hold after every write |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-31-01 | Tampering | updateAccount FormData type | high | mitigate | Zod enum ASSET\|SAVINGS + post-findUnique matrix; reject FIAT_CREDIT/legacy forges (D-15) |
| T-31-02 | Tampering | SAVINGS→ASSET without null clear | high | mitigate | Explicit annualRateBps/accrualDayOfMonth null in same update; Vitest asserts keys (D-15) |
| T-31-03 | Tampering | Convert path BalanceSnapshot write | high | mitigate | Never call snapshot APIs; Vitest never-calls (D-16) |
| T-31-04 | Tampering | Spoof currency on edit | medium | mitigate | Continue ignoring client currency (D-03); name-only/convert tests assert |
| T-31-05 | Information disclosure | Forbidden-transition message | low | accept | Generic Russian save failure only — no internal type/DB detail |
| T-31-SC | Tampering | npm installs | high | mitigate | No new packages this phase — legitimacy gate N/A |
</threat_model>

<verification>
./node_modules/.bin/vitest run src/lib/validations/account.test.ts src/app/accounts/actions.test.ts
</verification>

<success_criteria>
ACCT-04 server half true: ASSET↔SAVINGS updates persist correctly; forbidden peers rejected; snapshots untouched; Phase 27 forge-ignore peer rewritten.
</success_criteria>

<output>
Create `.planning/phases/31-asset-savings-type-conversion/31-01-SUMMARY.md` when done
</output>
