import { describe, it } from "vitest";

/**
 * GRISO-01 isolation twin of INISO (Phase 22 Wave 0 stubs).
 * Non-poisoning it.todo — walls/golden land in Plan 01 Task 2.
 */
describe("GRISO-01 isolation", () => {
  it.todo(
    "net-worth.ts + historical-series.ts ban credit-grace and nw-forecast (D-05)",
  );
  it.todo(
    "nw-forecast.ts bans prisma / BalanceSnapshot / net-worth / historical-series (D-06)",
  );
  it.todo(
    "credit-grace.ts bans prisma + net-worth + historical-series (D-07)",
  );
  it.todo(
    "past-series golden identity with conceptual grace fixture voided (D-09)",
  );
  it.todo("FIAT_CREDIT + creditLimitMinor + snapshots golden (D-10)");
});
