import { describe, it } from "vitest";

/**
 * Wave 0 Nyquist stubs (CAP-02).
 * Later plans green get_net_worth honesty against net-worth.ts + computeNetWorthRows.
 */
describe("get_net_worth (CAP-02)", () => {
  it.todo("returns totalPrimaryMinor string + isPartial + excludeReason rows");

  it.todo("matches computeNetWorthRows honesty for same fixture inputs");

  it.todo("omitted asOf defaults via resolveAsOf / calendarDateToday Europe/Moscow");
});
