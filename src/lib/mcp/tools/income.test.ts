import { describe, it } from "vitest";

/**
 * Wave 0 Nyquist stubs (SIDE-02).
 * Later plans green list_income overdue/actual + never-write BalanceSnapshot.
 */
describe("list_income (SIDE-02)", () => {
  it.todo("returns plan/actual/overdue flags with string minors (page-parity default)");

  it.todo("optional paired from+to dumps listAllInRange occurrences with overdue");

  it.todo("income MCP sources never write BalanceSnapshot / historical LOCF");
});
