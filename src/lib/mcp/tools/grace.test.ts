import { describe, it } from "vitest";

/**
 * Wave 0 Nyquist stubs (SIDE-03).
 * Later plans green list_grace_obligations OPEN + CTA rows.
 */
describe("list_grace_obligations (SIDE-03)", () => {
  it.todo("returns OPEN grace rows with kind discriminant + CTA fields");

  it.todo("overdue / due presentation matches credit-grace domain helpers");

  it.todo("grace MCP sources never rewrite historical LOCF / BalanceSnapshot");
});
