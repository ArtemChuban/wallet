# Host-side Prisma tooling. Compose overrides to file:/data/wallet.db in the container.
DATABASE_URL=file:./data/wallet.db

# Publish the app only on loopback when using Compose (Plan 04):
# ports: ["127.0.0.1:3000:3000"]
